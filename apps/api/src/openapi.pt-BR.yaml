openapi: 3.1.0
info:
  title: SnapOtter API
  version: 2.0.0
  description: |
    REST API do SnapOtter, uma infraestrutura auto-hospedada de processamento de arquivos com 200+ ferramentas para image, video, audio, PDF e documentos.

    ## Autenticação

    A maioria dos endpoints exige autenticação por um de dois métodos:

    1. **Cookie de sessão** - Chame `POST /api/auth/login` com nome de usuário e senha. A resposta inclui um campo `token`. Passe-o como `Authorization: Bearer <token>` nas solicitações seguintes.

    2. **Chave de API** - Gere uma chave pela interface de Configurações ou por `POST /api/v1/api-keys`. As chaves têm o prefixo `si_`. Passe-a como `Authorization: Bearer si_...`.

    Endpoints marcados com um ícone de cadeado exigem autenticação. Endpoints exclusivos para administradores são indicados em sua descrição.
  license:
    name: AGPL-3.0
    url: https://github.com/snapotter-hq/snapotter/blob/main/LICENSE
servers:
  - url: /
    description: Current instance
tags:
  - name: Tools
    description: Ferramentas de processamento de arquivos para imagem, vídeo, áudio, PDF e arquivos. Cada uma aceita um upload de arquivo multipart e retorna uma URL de download.
  - name: Batch
    description: Processe vários arquivos por uma ferramenta em uma única requisição.
  - name: Pipelines
    description: Encadeie várias ferramentas em fluxos de trabalho reutilizáveis.
  - name: Files
    description: Envie, baixe e gerencie arquivos processados.
  - name: Auth
    description: Login, logout, gerenciamento de sessão e administração de usuários.
  - name: API Keys
    description: Crie e gerencie API keys para acesso programático.
  - name: Settings
    description: Configuração global do sistema (escrita apenas para admin).
  - name: Teams
    description: Organize usuários em equipes.
  - name: Roles
    description: Gerenciamento de funções personalizadas com permissões granulares.
  - name: Audit
    description: Log de auditoria para rastrear ações administrativas.
  - name: Analytics
    description: Configuração de analytics e consentimento do usuário.
  - name: Features
    description: Instalação e gerenciamento de bundles de recursos de IA.
  - name: Admin
    description: Diagnósticos administrativos de saúde.
  - name: System
    description: Verificações de saúde, configuração e progresso de jobs.
  - name: Enterprise
    description: Administração enterprise controlada por licença, conformidade, SCIM e APIs de integração.
components:
  securitySchemes:
    bearerAuth:
      type: http
      scheme: bearer
      description: Session token from login or API key (prefixed with si_)
  responses:
    RateLimited:
      description: Too many requests. The client has exceeded the rate limit.
      headers:
        Retry-After:
          schema:
            type: integer
          description: Seconds until the rate limit window resets
        X-RateLimit-Limit:
          schema:
            type: integer
          description: Maximum requests per time window
        X-RateLimit-Remaining:
          schema:
            type: integer
          description: Remaining requests in current window
      content:
        application/json:
          schema:
            type: object
            properties:
              error:
                type: string
                example: Rate limit exceeded
  schemas:
    Error:
      type: object
      required:
        - error
      properties:
        error:
          type: string
          example: Invalid image format
        details:
          type: string
          description: Additional error details (e.g. Zod validation errors)
    ToolResponse:
      type: object
      properties:
        jobId:
          type: string
          description: Unique job identifier
        downloadUrl:
          type: string
          description: URL to download the processed image
          example: /api/v1/download/abc123/output.png
        originalSize:
          type: integer
          description: Original file size in bytes
        processedSize:
          type: integer
          description: Processed file size in bytes
        previewUrl:
          type: string
          description: URL to a WebP preview (present when output is not browser-previewable, e.g. TIFF, HEIF)
        savedFileId:
          type: string
          description: ID of the auto-saved file in the library (present when fileId was provided in the request)
    UnauthorizedError:
      type: object
      properties:
        statusCode:
          type: integer
          example: 401
        error:
          type: string
          example: Unauthorized
        message:
          type: string
          example: Authentication required
    ForbiddenError:
      type: object
      properties:
        statusCode:
          type: integer
          example: 403
        error:
          type: string
          example: Forbidden
        message:
          type: string
          example: Insufficient permissions
    ConflictError:
      type: object
      properties:
        statusCode:
          type: integer
          example: 409
        error:
          type: string
          example: Conflict
        message:
          type: string
          example: Resource already exists
    FeatureNotInstalledError:
      type: object
      properties:
        error:
          type: string
          example: Feature not installed
        code:
          type: string
          enum:
            - FEATURE_NOT_INSTALLED
            - FEATURE_INCOMPATIBLE
          example: FEATURE_NOT_INSTALLED
        feature:
          type: string
          description: Feature bundle identifier
        featureName:
          type: string
          description: Human-readable feature name
        estimatedSize:
          type: string
          description: Estimated download size
        requestedQuality:
          type: string
          enum:
            - fast
            - balanced
            - best
          description: OCR tier that could not be honored; Fast is reported when Korean is explicitly requested with Fast or the legacy Tesseract alias
        compatibilityReason:
          type: string
          description: Machine-readable runtime compatibility reason, when available
        guidance:
          type: string
          description: Actionable recovery guidance, when available
    HealthResponse:
      type: object
      properties:
        status:
          type: string
          enum:
            - healthy
            - unhealthy
        version:
          type: string
    AdminHealthResponse:
      type: object
      properties:
        status:
          type: string
          enum:
            - healthy
            - degraded
        version:
          type: string
        uptime:
          type: string
        storage:
          type: object
          properties:
            mode:
              type: string
            available:
              type: string
        database:
          type: string
          enum:
            - ok
            - error
        queue:
          type: object
          properties:
            active:
              type: integer
            pending:
              type: integer
        ai:
          type: object
          properties:
            gpu:
              type: boolean
security:
  - bearerAuth: []
paths:
  /api/v1/health:
    get:
      operationId: healthCheck
      tags:
        - System
      summary: Verificação de saúde
      description: Retorna o status de saúde do servidor. Usado pelo HEALTHCHECK do Docker. Endpoint público.
      security: []
      responses:
        '200':
          description: Server health
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/HealthResponse'
        '503':
          description: Database unavailable
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/HealthResponse'
  /api/v1/tools/image/resize:
    post:
      operationId: resizeImage
      tags:
        - Tools
      summary: Redimensionar Imagem
      description: Redimensiona uma imagem para dimensões específicas ou por porcentagem.
      security:
        - bearerAuth: []
      requestBody:
        required: true
        content:
          multipart/form-data:
            schema:
              type: object
              required:
                - file
              properties:
                file:
                  type: string
                  format: binary
                  description: Image file to process
                settings:
                  type: string
                  description: |
                    JSON string with options:
                    - `width` (number, optional) - Target width in pixels
                    - `height` (number, optional) - Target height in pixels
                    - `fit` (string, default "contain") - One of: contain, cover, fill, inside, outside
                    - `withoutEnlargement` (boolean, default false) - Prevent upscaling
                    - `percentage` (number, optional) - Scale by percentage instead of fixed dimensions
      responses:
        '200':
          description: Processed image
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ToolResponse'
        '400':
          description: Invalid input
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Error'
        '401':
          description: Authentication required
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/UnauthorizedError'
  /api/v1/tools/image/content-aware-resize:
    post:
      operationId: contentAwareResize
      tags:
        - Tools
      summary: Redimensionamento com reconhecimento de conteúdo
      description: Redimensiona uma imagem por seam carving para remover ou inserir conteúdo de forma inteligente, preservando os elementos importantes.
      security:
        - bearerAuth: []
      requestBody:
        required: true
        content:
          multipart/form-data:
            schema:
              type: object
              required:
                - file
              properties:
                file:
                  type: string
                  format: binary
                  description: Image file to process
                settings:
                  type: string
                  description: |
                    JSON string with options:
                    - `width` (number, optional) - Target width in pixels
                    - `height` (number, optional) - Target height in pixels
                    - `protectFaces` (boolean, default false) - Detect and protect faces from distortion
                    - `blurRadius` (number 0-20, default 4) - Blur radius for energy map
                    - `sobelThreshold` (number 1-20, default 2) - Edge detection threshold
                    - `square` (boolean, default false) - Crop to square aspect ratio

                    At least one of `width`, `height`, or `square` must be provided.
      responses:
        '200':
          description: Processed image
          content:
            application/json:
              schema:
                allOf:
                  - $ref: '#/components/schemas/ToolResponse'
                  - type: object
                    properties:
                      width:
                        type: integer
                      height:
                        type: integer
        '400':
          description: Invalid input
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Error'
        '401':
          description: Authentication required
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/UnauthorizedError'
  /api/v1/tools/image/crop:
    post:
      operationId: cropImage
      tags:
        - Tools
      summary: Recortar Imagem
      description: Recorta uma imagem para uma região específica.
      security:
        - bearerAuth: []
      requestBody:
        required: true
        content:
          multipart/form-data:
            schema:
              type: object
              required:
                - file
              properties:
                file:
                  type: string
                  format: binary
                  description: Image file to process
                settings:
                  type: string
                  description: |
                    JSON string with options:
                    - `left` (number, required) - Left offset in pixels (min 0)
                    - `top` (number, required) - Top offset in pixels (min 0)
                    - `width` (number, required) - Width of crop region in pixels
                    - `height` (number, required) - Height of crop region in pixels
                    - `unit` (string, optional) - One of: px, percent
      responses:
        '200':
          description: Processed image
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ToolResponse'
        '400':
          description: Invalid input
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Error'
        '401':
          description: Authentication required
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/UnauthorizedError'
  /api/v1/tools/image/rotate:
    post:
      operationId: rotateImage
      tags:
        - Tools
      summary: Girar e Espelhar Imagem
      description: Gira uma imagem por ângulo ou espelha na horizontal/vertical.
      security:
        - bearerAuth: []
      requestBody:
        required: true
        content:
          multipart/form-data:
            schema:
              type: object
              required:
                - file
              properties:
                file:
                  type: string
                  format: binary
                  description: Image file to process
                settings:
                  type: string
                  description: |
                    JSON string with options:
                    - `angle` (number, default 0) - Rotation angle in degrees
                    - `horizontal` (boolean, default false) - Flip horizontally
                    - `vertical` (boolean, default false) - Flip vertically
      responses:
        '200':
          description: Processed image
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ToolResponse'
        '400':
          description: Invalid input
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Error'
        '401':
          description: Authentication required
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/UnauthorizedError'
  /api/v1/tools/image/convert:
    post:
      operationId: convertFormat
      tags:
        - Tools
      summary: Converter Imagem
      description: Converte uma imagem para um formato diferente.
      security:
        - bearerAuth: []
      requestBody:
        required: true
        content:
          multipart/form-data:
            schema:
              type: object
              required:
                - file
              properties:
                file:
                  type: string
                  format: binary
                  description: Image file to process
                settings:
                  type: string
                  description: |
                    JSON string with options:
                    - `format` (string, required) - One of: jpg, png, webp, avif, tiff, gif, heic, heif, jxl, bmp, ico, jp2, qoi, psd
                    - `quality` (number 1-100, optional) - Output quality
      responses:
        '200':
          description: Processed image
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ToolResponse'
        '400':
          description: Invalid input
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Error'
        '401':
          description: Authentication required
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/UnauthorizedError'
  /api/v1/tools/image/compress:
    post:
      operationId: compressImage
      tags:
        - Tools
      summary: Comprimir Imagem
      description: Reduz o tamanho do arquivo de imagem por nível de qualidade ou tamanho-alvo.
      security:
        - bearerAuth: []
      requestBody:
        required: true
        content:
          multipart/form-data:
            schema:
              type: object
              required:
                - file
              properties:
                file:
                  type: string
                  format: binary
                  description: Image file to process
                settings:
                  type: string
                  description: |
                    JSON string with options:
                    - `mode` (string, default "quality") - One of: quality, targetSize
                    - `quality` (number 1-100, optional) - Compression quality level
                    - `targetSizeKb` (number, optional) - Target file size in kilobytes
      responses:
        '200':
          description: Processed image
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ToolResponse'
        '400':
          description: Invalid input
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Error'
        '401':
          description: Authentication required
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/UnauthorizedError'
  /api/v1/tools/image/strip-metadata:
    post:
      operationId: stripMetadata
      tags:
        - Tools
      summary: Remover Metadados da Imagem
      description: Remove metadados EXIF, GPS, ICC ou XMP de uma imagem.
      security:
        - bearerAuth: []
      requestBody:
        required: true
        content:
          multipart/form-data:
            schema:
              type: object
              required:
                - file
              properties:
                file:
                  type: string
                  format: binary
                  description: Image file to process
                settings:
                  type: string
                  description: |
                    JSON string with options:
                    - `stripExif` (boolean, default false) - Remove EXIF data
                    - `stripGps` (boolean, default false) - Remove GPS data
                    - `stripIcc` (boolean, default false) - Remove ICC profile
                    - `stripXmp` (boolean, default false) - Remove XMP data
                    - `stripAll` (boolean, default true) - Remove all metadata
      responses:
        '200':
          description: Processed image
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ToolResponse'
        '400':
          description: Invalid input
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Error'
        '401':
          description: Authentication required
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/UnauthorizedError'
  /api/v1/tools/image/border:
    post:
      operationId: addBorder
      tags:
        - Tools
      summary: Borda e moldura
      description: Adiciona borda, cantos arredondados, preenchimento ou sombra a uma imagem.
      security:
        - bearerAuth: []
      requestBody:
        required: true
        content:
          multipart/form-data:
            schema:
              type: object
              required:
                - file
              properties:
                file:
                  type: string
                  format: binary
                  description: Image file to process
                settings:
                  type: string
                  description: |
                    JSON string with options:
                    - `borderWidth` (number 0-2000, default 10) - Border thickness in pixels
                    - `borderColor` (hex string, default "#000000") - Border color
                    - `padding` (number 0-200, default 0) - Inner padding in pixels
                    - `paddingColor` (hex string, default "#FFFFFF") - Padding fill color
                    - `cornerRadius` (number 0-2000, default 0) - Corner rounding radius
                    - `shadow` (boolean, default false) - Enable drop shadow
                    - `shadowBlur` (number 1-200, default 15) - Drop shadow blur radius
                    - `shadowOffsetX` (number -50 to 50, default 0) - Shadow horizontal offset
                    - `shadowOffsetY` (number -50 to 50, default 5) - Shadow vertical offset
                    - `shadowColor` (hex string, default "#000000") - Drop shadow color
                    - `shadowOpacity` (number 0-100, default 40) - Shadow opacity percentage
      responses:
        '200':
          description: Processed image
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ToolResponse'
        '400':
          description: Invalid input
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Error'
        '401':
          description: Authentication required
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/UnauthorizedError'
  /api/v1/tools/image/beautify:
    post:
      operationId: beautifyScreenshot
      tags:
        - Tools
      summary: Embelezar captura de tela
      description: |
        Adiciona fundos com gradiente, molduras de dispositivo, sombras, marcas d'água e
        dimensionamento para redes sociais a capturas de tela. Suporta fundos sólido, linear-gradient,
        radial-gradient, image e transparent. Inclui molduras de dispositivo macOS, Windows, navegador, iPhone,
        MacBook e iPad. Presets de redes sociais para Twitter, LinkedIn,
        Instagram, Facebook e Product Hunt.
      security:
        - bearerAuth: []
      requestBody:
        required: true
        content:
          multipart/form-data:
            schema:
              type: object
              required:
                - file
              properties:
                file:
                  type: string
                  format: binary
                  description: Screenshot or image file to beautify
                backgroundImage:
                  type: string
                  format: binary
                  description: Optional image for image backgrounds (used when backgroundType is "image")
                settings:
                  type: string
                  description: |
                    JSON string with options:
                    - `backgroundType` (string, default "linear-gradient") -- One of: solid, linear-gradient, radial-gradient, image, transparent
                    - `backgroundColor` (hex string, default "#667eea") -- Background color (for solid backgrounds)
                    - `gradientStops` (array, default [{color:"#667eea",position:0},{color:"#764ba2",position:100}]) -- Array of {color, position} objects for gradient backgrounds
                    - `gradientAngle` (number 0-360, default 135) -- Gradient angle in degrees
                    - `padding` (number 0-256, default 64) -- Space between screenshot and canvas edge in pixels
                    - `borderRadius` (number 0-64, default 12) -- Corner rounding radius for the screenshot
                    - `shadowPreset` (string, default "subtle") -- One of: none, subtle, medium, dramatic, custom
                    - `shadowBlur` (number 0-100, default 20) -- Shadow blur radius (used with custom shadow preset)
                    - `shadowOffsetX` (number -50 to 50, default 0) -- Shadow horizontal offset
                    - `shadowOffsetY` (number -50 to 50, default 10) -- Shadow vertical offset
                    - `shadowColor` (hex string, default "#000000") -- Shadow color
                    - `shadowOpacity` (number 0-100, default 30) -- Shadow opacity percentage
                    - `frame` (string, default "none") -- One of: none, macos-light, macos-dark, windows-light, windows-dark, browser-light, browser-dark, iphone, iphone-dark, macbook, macbook-dark, ipad, ipad-dark
                    - `frameTitle` (string, optional) -- Title text shown in window title bars
                    - `socialPreset` (string, default "none") -- One of: none, twitter, linkedin, instagram-square, instagram-story, facebook, producthunt
                    - `watermarkText` (string, optional) -- Watermark text to overlay
                    - `watermarkPosition` (string, default "bottom-right") -- One of: top-left, top-right, bottom-left, bottom-right, center
                    - `watermarkOpacity` (number 0-100, default 50) -- Watermark opacity percentage
                    - `outputFormat` (string, default "png") -- One of: png, jpeg, webp
      responses:
        '200':
          description: Beautified image
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ToolResponse'
        '400':
          description: Invalid input
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Error'
        '401':
          description: Authentication required
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/UnauthorizedError'
  /api/v1/tools/image/adjust-colors:
    post:
      operationId: adjustColors
      tags:
        - Tools
      summary: Ajustes de cor
      description: |
        Ferramenta consolidada de ajuste de cor. Ajusta brilho, contraste, exposição, saturação, temperatura, tonalidade, matiz, nitidez, canais de cor e efeitos em uma única passagem.
      security:
        - bearerAuth: []
      requestBody:
        required: true
        content:
          multipart/form-data:
            schema:
              type: object
              required:
                - file
              properties:
                file:
                  type: string
                  format: binary
                  description: Image file to process
                settings:
                  type: string
                  description: |
                    JSON string with options:
                    - `brightness` (number -100 to 100, default 0) - Brightness adjustment
                    - `contrast` (number -100 to 100, default 0) - Contrast adjustment
                    - `exposure` (number -100 to 100, default 0) - Exposure adjustment (gamma)
                    - `saturation` (number -100 to 100, default 0) - Color saturation
                    - `temperature` (number -100 to 100, default 0) - Color temperature (cool to warm)
                    - `tint` (number -100 to 100, default 0) - Tint shift (green to magenta)
                    - `hue` (number -180 to 180, default 0) - Hue rotation in degrees
                    - `sharpness` (number 0 to 100, default 0) - Sharpness enhancement
                    - `red` (number 0-200, default 100) - Red channel multiplier
                    - `green` (number 0-200, default 100) - Green channel multiplier
                    - `blue` (number 0-200, default 100) - Blue channel multiplier
                    - `effect` (string, default "none") - One of: none, grayscale, sepia, invert
      responses:
        '200':
          description: Processed image
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ToolResponse'
        '400':
          description: Invalid input
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Error'
        '401':
          description: Authentication required
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/UnauthorizedError'
  /api/v1/tools/image/sharpening:
    post:
      operationId: sharpenImage
      tags:
        - Tools
      summary: Aplicar Nitidez na Imagem
      description: Aumenta a nitidez de uma imagem usando os métodos adaptativo, unsharp mask ou high-pass, com redução de ruído opcional.
      security:
        - bearerAuth: []
      requestBody:
        required: true
        content:
          multipart/form-data:
            schema:
              type: object
              required:
                - file
              properties:
                file:
                  type: string
                  format: binary
                  description: Image file to process
                settings:
                  type: string
                  description: |
                    JSON string with options:
                    - `method` (string, default "adaptive") - One of: adaptive, unsharp-mask, high-pass
                    - `sigma` (number 0.5-10, default 1.0) - Gaussian sigma (adaptive)
                    - `m1` (number 0-10, default 1.0) - Flat area sharpening (adaptive)
                    - `m2` (number 0-20, default 3.0) - Jagged area sharpening (adaptive)
                    - `x1` (number 0-10, default 2.0) - Flat/jagged threshold (adaptive)
                    - `y2` (number 0-50, default 12) - Maximum brightening (adaptive)
                    - `y3` (number 0-50, default 20) - Maximum darkening (adaptive)
                    - `amount` (number 0-1000, default 100) - Sharpen amount (unsharp-mask)
                    - `radius` (number 0.1-5, default 1.0) - Blur radius (unsharp-mask)
                    - `threshold` (number 0-255, default 0) - Luminance threshold (unsharp-mask)
                    - `strength` (number 0-100, default 50) - Blend strength (high-pass)
                    - `kernelSize` (integer, default 3) - One of: 3, 5 (high-pass)
                    - `denoise` (string, default "off") - One of: off, light, medium, strong
      responses:
        '200':
          description: Processed image
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ToolResponse'
        '400':
          description: Invalid input
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Error'
        '401':
          description: Authentication required
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/UnauthorizedError'
  /api/v1/tools/image/optimize-for-web:
    post:
      operationId: optimizeForWeb
      tags:
        - Tools
      summary: Otimizar para web
      description: Otimiza uma imagem para entrega na web convertendo para formatos modernos, ajustando a qualidade e, opcionalmente, redimensionando.
      security:
        - bearerAuth: []
      requestBody:
        required: true
        content:
          multipart/form-data:
            schema:
              type: object
              required:
                - file
              properties:
                file:
                  type: string
                  format: binary
                  description: Image file to process
                settings:
                  type: string
                  description: |
                    JSON string with options:
                    - `format` (string, default "webp") - One of: webp, jpeg, avif, png, jxl
                    - `quality` (number 1-100, default 80) - Output quality
                    - `maxWidth` (number, optional) - Maximum width in pixels
                    - `maxHeight` (number, optional) - Maximum height in pixels
                    - `progressive` (boolean, default true) - Enable progressive encoding
                    - `stripMetadata` (boolean, default true) - Remove metadata
      responses:
        '200':
          description: Processed image
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ToolResponse'
        '400':
          description: Invalid input
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Error'
        '401':
          description: Authentication required
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/UnauthorizedError'
  /api/v1/tools/image/optimize-for-web/preview:
    post:
      operationId: optimizeForWebPreview
      tags:
        - Tools
      summary: Otimizar para web (preview)
      description: |
        Endpoint leve de preview para ajuste de parâmetros ao vivo. Retorna a
        imagem otimizada em binário diretamente (não JSON). As informações de tamanho ficam nos
        cabeçalhos da resposta. Não cria workspace nem persiste resultados.
      security:
        - bearerAuth: []
      requestBody:
        required: true
        content:
          multipart/form-data:
            schema:
              type: object
              required:
                - file
              properties:
                file:
                  type: string
                  format: binary
                  description: Image file to preview
                settings:
                  type: string
                  description: Same settings as the main optimize-for-web endpoint
      responses:
        '200':
          description: Optimized image binary
          headers:
            X-Original-Size:
              schema:
                type: string
              description: Original file size in bytes
            X-Processed-Size:
              schema:
                type: string
              description: Processed file size in bytes
            X-Output-Filename:
              schema:
                type: string
              description: Suggested output filename
          content:
            image/*:
              schema:
                type: string
                format: binary
        '400':
          description: Invalid input
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Error'
        '422':
          description: Processing failed
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Error'
  /api/v1/tools/image/image-enhancement:
    post:
      operationId: enhanceImage
      tags:
        - Tools
      summary: Aprimoramento de imagem
      description: |
        Aprimoramento automático de imagem com IA. Analisa a imagem e aplica
        correções de exposição, contraste, balanço de branco, saturação, nitidez
        e ruído com base no modo de cena selecionado.
      security:
        - bearerAuth: []
      requestBody:
        required: true
        content:
          multipart/form-data:
            schema:
              type: object
              required:
                - file
              properties:
                file:
                  type: string
                  format: binary
                  description: Image file to process
                settings:
                  type: string
                  description: |
                    JSON string with options:
                    - `mode` (string, default "auto") - One of: auto, portrait, landscape, low-light, food, document
                    - `intensity` (number 0-100, default 50) - Overall enhancement intensity
                    - `corrections` (object, optional) - Toggle individual corrections:
                      - `exposure` (boolean, default true)
                      - `contrast` (boolean, default true)
                      - `whiteBalance` (boolean, default true)
                      - `saturation` (boolean, default true)
                      - `sharpness` (boolean, default true)
                      - `denoise` (boolean, default true)
                    - `deepEnhance` (boolean, default false) - Use AI noise removal for deeper enhancement (requires noise-removal feature)
      responses:
        '200':
          description: Processed image
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ToolResponse'
        '400':
          description: Invalid input
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Error'
        '401':
          description: Authentication required
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/UnauthorizedError'
  /api/v1/tools/image/image-enhancement/analyze:
    post:
      operationId: analyzeImageEnhancement
      tags:
        - Tools
      summary: Analisar imagem para aprimoramento
      description: |
        Analisa uma imagem e retorna as correções sugeridas sem aplicá-las.
        Útil para prever quais aprimoramentos seriam feitos.
      security:
        - bearerAuth: []
      requestBody:
        required: true
        content:
          multipart/form-data:
            schema:
              type: object
              required:
                - file
              properties:
                file:
                  type: string
                  format: binary
                  description: Image file to analyze
      responses:
        '200':
          description: Image analysis with suggested corrections
          content:
            application/json:
              schema:
                type: object
                properties:
                  corrections:
                    type: object
                    description: Suggested correction values
                  metrics:
                    type: object
                    description: Image quality metrics
        '400':
          description: Invalid input
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Error'
        '422':
          description: Analysis failed
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Error'
  /api/v1/tools/image/noise-removal:
    post:
      operationId: removeNoise
      tags:
        - Tools
      summary: Remoção de ruído
      description: |
        Remoção de ruído com IA usando o sidecar Python. Suporta vários
        níveis de qualidade, do preview rápido à qualidade máxima. Requer o
        bundle de recurso noise-removal instalado.
      security:
        - bearerAuth: []
      requestBody:
        required: true
        content:
          multipart/form-data:
            schema:
              type: object
              required:
                - file
              properties:
                file:
                  type: string
                  format: binary
                  description: Image file to process
                settings:
                  type: string
                  description: |
                    JSON string with options:
                    - `tier` (string, default "balanced") - One of: quick, balanced, quality, maximum
                    - `strength` (number, default 50) - Denoising strength
                    - `detailPreservation` (number, default 50) - Detail preservation level
                    - `colorNoise` (number, default 30) - Color noise reduction
                    - `format` (string, default "original") - One of: original, png, jpeg, webp, avif, jxl
                    - `quality` (number, default 90) - Output quality (1-100)
                clientJobId:
                  type: string
                  description: Client-provided job ID for SSE progress tracking
      responses:
        '202':
          description: Accepted for async processing. Track progress via SSE at /api/v1/jobs/{jobId}/progress.
          content:
            application/json:
              schema:
                type: object
                properties:
                  jobId:
                    type: string
                  async:
                    type: boolean
                    example: true
        '400':
          description: Invalid input
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Error'
        '401':
          description: Authentication required
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/UnauthorizedError'
        '422':
          description: Processing failed
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Error'
        '501':
          description: Feature not installed
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/FeatureNotInstalledError'
  /api/v1/tools/image/red-eye-removal:
    post:
      operationId: removeRedEye
      tags:
        - Tools
      summary: Remoção de olhos vermelhos
      description: |
        Detecção e remoção de olhos vermelhos com IA. Detecta rostos e corrige
        artefatos de olhos vermelhos em fotografias. Requer o bundle de recurso
        red-eye-removal instalado.
      security:
        - bearerAuth: []
      requestBody:
        required: true
        content:
          multipart/form-data:
            schema:
              type: object
              required:
                - file
              properties:
                file:
                  type: string
                  format: binary
                  description: Image file to process
                settings:
                  type: string
                  description: |
                    JSON string with options:
                    - `sensitivity` (number 0-100, default 50) - Detection sensitivity
                    - `strength` (number 0-100, default 70) - Correction strength
                    - `format` (string, optional) - Output format
                    - `quality` (number 1-100, default 90) - Output quality
                clientJobId:
                  type: string
                  description: Client-provided job ID for SSE progress tracking
      responses:
        '202':
          description: Accepted for async processing. Track progress via SSE at /api/v1/jobs/{jobId}/progress.
          content:
            application/json:
              schema:
                type: object
                properties:
                  jobId:
                    type: string
                  async:
                    type: boolean
                    example: true
        '400':
          description: Invalid input
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Error'
        '401':
          description: Authentication required
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/UnauthorizedError'
        '422':
          description: Processing failed
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Error'
        '501':
          description: Feature not installed
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/FeatureNotInstalledError'
  /api/v1/tools/image/restore-photo:
    post:
      operationId: restorePhoto
      tags:
        - Tools
      summary: Restauração de fotos
      description: |
        Pipeline de restauração de fotos com IA. Repara riscos, aprimora rostos,
        remove ruído e, opcionalmente, coloriza fotografias antigas ou danificadas. Requer
        o bundle de recurso photo-restoration instalado.
      security:
        - bearerAuth: []
      requestBody:
        required: true
        content:
          multipart/form-data:
            schema:
              type: object
              required:
                - file
              properties:
                file:
                  type: string
                  format: binary
                  description: Image file to process
                settings:
                  type: string
                  description: |
                    JSON string with options:
                    - `scratchRemoval` (boolean, default true) - Enable scratch repair
                    - `faceEnhancement` (boolean, default true) - Enhance detected faces
                    - `fidelity` (number 0-1, default 0.7) - Face enhancement fidelity
                    - `denoise` (boolean, default true) - Enable denoising
                    - `denoiseStrength` (number 0-100, default 25) - Denoising strength
                    - `colorize` (boolean, default false) - Colorize grayscale photos
                    - `colorizeStrength` (number 0-100, default 85) - Colorization strength
                clientJobId:
                  type: string
                  description: Client-provided job ID for SSE progress tracking
      responses:
        '202':
          description: Accepted for async processing. Track progress via SSE at /api/v1/jobs/{jobId}/progress.
          content:
            application/json:
              schema:
                type: object
                properties:
                  jobId:
                    type: string
                  async:
                    type: boolean
                    example: true
        '400':
          description: Invalid input
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Error'
        '401':
          description: Authentication required
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/UnauthorizedError'
        '422':
          description: Processing failed
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Error'
        '501':
          description: Feature not installed
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/FeatureNotInstalledError'
  /api/v1/tools/image/passport-photo/analyze:
    post:
      operationId: analyzePassportPhoto
      tags:
        - Tools
      summary: Foto de passaporte - analisar
      description: |
        Fase 1 da geração de foto de passaporte. Detecta pontos de referência do rosto e remove
        o fundo. Retorna dados dos pontos de referência e um preview codificado em base64 para
        o cliente usar na fase de geração. Requer o bundle de recurso passport-photo
        instalado.
      security:
        - bearerAuth: []
      requestBody:
        required: true
        content:
          multipart/form-data:
            schema:
              type: object
              required:
                - file
              properties:
                file:
                  type: string
                  format: binary
                  description: Photo with a clear, front-facing face
                clientJobId:
                  type: string
                  description: Client-provided job ID for SSE progress tracking
      responses:
        '200':
          description: Face analysis and background removal result
          content:
            application/json:
              schema:
                type: object
                properties:
                  jobId:
                    type: string
                    description: Job ID to pass to the generate endpoint
                  filename:
                    type: string
                  preview:
                    type: string
                    description: Base64-encoded PNG preview of the background-removed image
                  previewWidth:
                    type: integer
                  previewHeight:
                    type: integer
                  landmarks:
                    type: object
                    description: Normalized face landmark coordinates (0-1)
                    properties:
                      leftEye:
                        type: object
                        properties:
                          x:
                            type: number
                          'y':
                            type: number
                      rightEye:
                        type: object
                        properties:
                          x:
                            type: number
                          'y':
                            type: number
                      eyeCenter:
                        type: object
                        properties:
                          x:
                            type: number
                          'y':
                            type: number
                      chin:
                        type: object
                        properties:
                          x:
                            type: number
                          'y':
                            type: number
                      forehead:
                        type: object
                        properties:
                          x:
                            type: number
                          'y':
                            type: number
                      crown:
                        type: object
                        properties:
                          x:
                            type: number
                          'y':
                            type: number
                      nose:
                        type: object
                        properties:
                          x:
                            type: number
                          'y':
                            type: number
                      faceCenterX:
                        type: number
                  imageWidth:
                    type: integer
                  imageHeight:
                    type: integer
        '400':
          description: Invalid input
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Error'
        '401':
          description: Authentication required
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/UnauthorizedError'
        '422':
          description: No face detected or analysis failed
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Error'
        '501':
          description: Feature not installed
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/FeatureNotInstalledError'
  /api/v1/tools/image/passport-photo/generate:
    post:
      operationId: generatePassportPhoto
      tags:
        - Tools
      summary: Foto de passaporte - gerar
      description: |
        Fase 2 da geração de foto de passaporte. Usa o jobId e os pontos de referência da
        fase de análise para recortar, redimensionar e dispor a foto na especificação de
        país/documento indicada. Resposta rápida (sem nova execução de IA).
      security:
        - bearerAuth: []
      requestBody:
        required: true
        content:
          application/json:
            schema:
              type: object
              required:
                - jobId
                - filename
                - countryCode
                - landmarks
                - imageWidth
                - imageHeight
              properties:
                jobId:
                  type: string
                  description: Job ID from the analyze phase
                filename:
                  type: string
                  description: Original filename from analyze phase
                countryCode:
                  type: string
                  description: ISO country code for passport specification
                documentType:
                  type: string
                  default: passport
                  description: Document type (passport, visa, etc.)
                bgColor:
                  type: string
                  default: '#FFFFFF'
                  description: Background color (hex)
                printLayout:
                  type: string
                  default: none
                  description: Print sheet layout (none, 4x6, A4, letter)
                maxFileSizeKb:
                  type: number
                  default: 0
                  description: Max file size in KB (0 = no limit)
                dpi:
                  type: number
                  minimum: 72
                  maximum: 1200
                  default: 300
                  description: Output DPI
                customWidthMm:
                  type: number
                  description: Custom photo width in mm (overrides country spec)
                customHeightMm:
                  type: number
                  description: Custom photo height in mm (overrides country spec)
                zoom:
                  type: number
                  minimum: 0.5
                  maximum: 3
                  default: 1
                  description: Zoom level (>1 = tighter crop)
                adjustX:
                  type: number
                  default: 0
                  description: Horizontal position adjustment
                adjustY:
                  type: number
                  default: 0
                  description: Vertical position adjustment
                landmarks:
                  type: object
                  description: Face landmarks from analyze phase
                imageWidth:
                  type: integer
                  description: Original image width from analyze phase
                imageHeight:
                  type: integer
                  description: Original image height from analyze phase
      responses:
        '200':
          description: Generated passport photo
          content:
            application/json:
              schema:
                type: object
                properties:
                  jobId:
                    type: string
                  downloadUrl:
                    type: string
                  dimensions:
                    type: object
                    properties:
                      widthMm:
                        type: number
                      heightMm:
                        type: number
                      widthPx:
                        type: integer
                      heightPx:
                        type: integer
                      dpi:
                        type: integer
                  spec:
                    type: object
                    properties:
                      country:
                        type: string
                      countryCode:
                        type: string
                      documentType:
                        type: string
                      documentLabel:
                        type: string
                  printDownloadUrl:
                    type: string
                    description: URL for print sheet download (if printLayout was specified)
        '400':
          description: Invalid settings or unknown country code
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Error'
        '401':
          description: Authentication required
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/UnauthorizedError'
        '422':
          description: Generation failed
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Error'
  /api/v1/tools/image/colorize:
    post:
      operationId: colorizeImage
      tags:
        - Tools
      summary: Colorizar
      description: |
        Colorização de fotos com IA. Converte fotografias em preto e branco ou
        escala de cinza para cores completas usando DDColor, com fallback OpenCV DNN. Requer
        o bundle de recurso colorize instalado.
      security:
        - bearerAuth: []
      requestBody:
        required: true
        content:
          multipart/form-data:
            schema:
              type: object
              required:
                - file
              properties:
                file:
                  type: string
                  format: binary
                  description: Grayscale or B&W image file
                settings:
                  type: string
                  description: |
                    JSON string with options:
                    - `intensity` (number 0-1, default 1.0) - Colorization intensity
                    - `model` (string, default "auto") - One of: auto, ddcolor, opencv
                clientJobId:
                  type: string
                  description: Client-provided job ID for SSE progress tracking
      responses:
        '202':
          description: Accepted for async processing. Track progress via SSE at /api/v1/jobs/{jobId}/progress.
          content:
            application/json:
              schema:
                type: object
                properties:
                  jobId:
                    type: string
                  async:
                    type: boolean
                    example: true
        '400':
          description: Invalid input
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Error'
        '401':
          description: Authentication required
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/UnauthorizedError'
        '422':
          description: Processing failed
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Error'
        '501':
          description: Feature not installed
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/FeatureNotInstalledError'
  /api/v1/tools/image/enhance-faces:
    post:
      operationId: enhanceFaces
      tags:
        - Tools
      summary: Aprimoramento de rostos
      description: |
        Aprimoramento de rostos com IA usando GFPGAN ou CodeFormer. Detecta rostos
        na imagem e aprimora detalhes faciais, textura da pele e nitidez.
        Requer o bundle de recurso enhance-faces instalado.
      security:
        - bearerAuth: []
      requestBody:
        required: true
        content:
          multipart/form-data:
            schema:
              type: object
              required:
                - file
              properties:
                file:
                  type: string
                  format: binary
                  description: Image file containing faces
                settings:
                  type: string
                  description: |
                    JSON string with options:
                    - `model` (string, default "auto") - One of: auto, gfpgan, codeformer
                    - `strength` (number 0-1, default 0.8) - Enhancement strength
                    - `onlyCenterFace` (boolean, default false) - Only enhance the center face
                    - `sensitivity` (number 0-1, default 0.5) - Face detection sensitivity
                clientJobId:
                  type: string
                  description: Client-provided job ID for SSE progress tracking
      responses:
        '202':
          description: Accepted for async processing. Track progress via SSE at /api/v1/jobs/{jobId}/progress.
          content:
            application/json:
              schema:
                type: object
                properties:
                  jobId:
                    type: string
                  async:
                    type: boolean
                    example: true
        '400':
          description: Invalid input
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Error'
        '401':
          description: Authentication required
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/UnauthorizedError'
        '422':
          description: Processing failed
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Error'
        '501':
          description: Feature not installed
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/FeatureNotInstalledError'
  /api/v1/tools/image/image-to-base64:
    post:
      operationId: imageToBase64
      tags:
        - Tools
      summary: Imagem para Base64
      description: |
        Converte uma ou mais imagens em strings codificadas em Base64 com data URIs.
        Suporta conversão de formato e redimensionamento opcionais. Retorna tanto a string
        Base64 bruta quanto um data URI pronto para uso para cada arquivo.
      security:
        - bearerAuth: []
      requestBody:
        required: true
        content:
          multipart/form-data:
            schema:
              type: object
              required:
                - file
              properties:
                file:
                  type: array
                  items:
                    type: string
                    format: binary
                  description: One or more image files to convert
                settings:
                  type: string
                  description: |
                    JSON string with options:
                    - `outputFormat` (string, default "original") - One of: original, jpeg, png, webp, avif, jxl
                    - `quality` (integer 1-100, default 80) - Output quality for lossy formats
                    - `maxWidth` (integer, default 0) - Max width in pixels (0 = no limit)
                    - `maxHeight` (integer, default 0) - Max height in pixels (0 = no limit)
      responses:
        '200':
          description: Base64-encoded results
          content:
            application/json:
              schema:
                type: object
                properties:
                  results:
                    type: array
                    items:
                      type: object
                      properties:
                        filename:
                          type: string
                        mimeType:
                          type: string
                        width:
                          type: integer
                        height:
                          type: integer
                        originalSize:
                          type: integer
                          description: Original file size in bytes
                        encodedSize:
                          type: integer
                          description: Base64 string size in bytes
                        overheadPercent:
                          type: number
                          description: Size overhead of Base64 encoding
                        base64:
                          type: string
                          description: Raw Base64-encoded image data
                        dataUri:
                          type: string
                          description: Complete data URI (data:mime;base64,...)
                  errors:
                    type: array
                    items:
                      type: object
                      properties:
                        filename:
                          type: string
                        error:
                          type: string
        '400':
          description: Invalid input
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Error'
        '401':
          description: Authentication required
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/UnauthorizedError'
  /api/v1/tools/image/watermark-text:
    post:
      operationId: addTextWatermark
      tags:
        - Tools
      summary: Marca d'água de texto
      description: Adiciona uma marca d'água de texto a uma imagem.
      security:
        - bearerAuth: []
      requestBody:
        required: true
        content:
          multipart/form-data:
            schema:
              type: object
              required:
                - file
              properties:
                file:
                  type: string
                  format: binary
                  description: Image file to process
                settings:
                  type: string
                  description: |
                    JSON string with options:
                    - `text` (string 1-500, required) - Watermark text
                    - `fontSize` (number 8-1000, default 48) - Font size in pixels
                    - `color` (hex string, default "#000000") - Text color
                    - `opacity` (number 0-100, default 50) - Opacity percentage
                    - `position` (string, default "center") - One of: center, top-left, top-right, bottom-left, bottom-right, tiled
                    - `rotation` (number -360 to 360, default 0) - Text rotation angle
      responses:
        '200':
          description: Processed image
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ToolResponse'
        '400':
          description: Invalid input
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Error'
        '401':
          description: Authentication required
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/UnauthorizedError'
  /api/v1/tools/image/text-overlay:
    post:
      operationId: addTextOverlay
      tags:
        - Tools
      summary: Sobreposição de texto
      description: Adiciona sobreposição de texto estilizado com caixa de fundo opcional.
      security:
        - bearerAuth: []
      requestBody:
        required: true
        content:
          multipart/form-data:
            schema:
              type: object
              required:
                - file
              properties:
                file:
                  type: string
                  format: binary
                  description: Image file to process
                settings:
                  type: string
                  description: |
                    JSON string with options:
                    - `text` (string 1-500, required) - Overlay text
                    - `fontSize` (number 8-200, default 48) - Font size in pixels
                    - `color` (hex string, default "#FFFFFF") - Text color
                    - `position` (string, default "bottom") - One of: top, center, bottom
                    - `backgroundBox` (boolean, default false) - Show background box behind text
                    - `backgroundColor` (hex string, default "#000000") - Background box color
                    - `shadow` (boolean, default true) - Add text shadow
      responses:
        '200':
          description: Processed image
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ToolResponse'
        '400':
          description: Invalid input
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Error'
        '401':
          description: Authentication required
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/UnauthorizedError'
  /api/v1/tools/image/replace-color:
    post:
      operationId: replaceColor
      tags:
        - Tools
      summary: Substituir cor
      description: Substitui uma cor específica em uma imagem por outra cor ou por transparência.
      security:
        - bearerAuth: []
      requestBody:
        required: true
        content:
          multipart/form-data:
            schema:
              type: object
              required:
                - file
              properties:
                file:
                  type: string
                  format: binary
                  description: Image file to process
                settings:
                  type: string
                  description: |
                    JSON string with options:
                    - `sourceColor` (hex string, default "#FF0000") - Color to replace
                    - `targetColor` (hex string, default "#00FF00") - Replacement color
                    - `makeTransparent` (boolean, default false) - Make matched pixels transparent instead
                    - `tolerance` (number 0-255, default 30) - Color matching tolerance
      responses:
        '200':
          description: Processed image
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ToolResponse'
        '400':
          description: Invalid input
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Error'
        '401':
          description: Authentication required
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/UnauthorizedError'
  /api/v1/tools/image/color-blindness:
    post:
      operationId: simulateColorBlindness
      tags:
        - Tools
      summary: Simulação de daltonismo
      description: Simula como uma imagem é vista por pessoas com vários tipos de deficiência na visão de cores.
      security:
        - bearerAuth: []
      requestBody:
        required: true
        content:
          multipart/form-data:
            schema:
              type: object
              required:
                - file
              properties:
                file:
                  type: string
                  format: binary
                  description: Image file to process
                settings:
                  type: string
                  description: |
                    JSON string with options:
                    - `simulationType` (enum, default "deuteranomaly") -- Type of color vision deficiency to simulate. One of: protanopia, deuteranopia, tritanopia, protanomaly, deuteranomaly, tritanomaly, achromatopsia, blueConeMonochromacy
      responses:
        '200':
          description: Processed image
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ToolResponse'
        '400':
          description: Invalid input
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Error'
        '401':
          description: Authentication required
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/UnauthorizedError'
  /api/v1/tools/image/gif-tools:
    post:
      operationId: processGif
      tags:
        - Tools
      summary: Ferramentas de GIF
      description: Redimensiona, otimiza, ajusta a velocidade, inverte, extrai quadros ou gira GIFs animados.
      security:
        - bearerAuth: []
      requestBody:
        required: true
        content:
          multipart/form-data:
            schema:
              type: object
              required:
                - file
              properties:
                file:
                  type: string
                  format: binary
                  description: GIF file to process
                settings:
                  type: string
                  description: |
                    JSON string with options:
                    - `mode` (string, default "resize") - One of: resize, optimize, speed, reverse, extract, rotate
                    - `width` (number 1-16384, optional) - Target width in pixels (resize mode)
                    - `height` (number 1-16384, optional) - Target height in pixels (resize mode)
                    - `percentage` (number 1-500, optional) - Scale by percentage (resize mode)
                    - `colors` (number 2-256, default 256) - Color palette size (optimize mode)
                    - `dither` (number 0-1, default 1.0) - Dither amount (optimize mode)
                    - `effort` (number 1-10, default 7) - Compression effort (optimize mode)
                    - `speedFactor` (number 0.1-10, default 1.0) - Speed multiplier (speed mode)
                    - `extractMode` (string, default "single") - One of: single, range, all (extract mode)
                    - `frameNumber` (number, default 0) - Frame index to extract (extract/single mode)
                    - `frameStart` (number, default 0) - Start frame index (extract/range mode)
                    - `frameEnd` (number, optional) - End frame index (extract/range mode)
                    - `extractFormat` (string, default "png") - One of: png, webp (extract mode)
                    - `angle` (number, optional) - Rotation angle, one of: 90, 180, 270 (rotate mode)
                    - `flipH` (boolean, default false) - Flip horizontally (rotate mode)
                    - `flipV` (boolean, default false) - Flip vertically (rotate mode)
                    - `loop` (number 0-100, default 0) - Loop count, 0 = infinite
      responses:
        '200':
          description: Processed image
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ToolResponse'
        '400':
          description: Invalid input
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Error'
        '401':
          description: Authentication required
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/UnauthorizedError'
  /api/v1/tools/image/gif-tools/info:
    post:
      operationId: getGifInfo
      tags:
        - Tools
      summary: Informações do GIF
      description: Obtém metadados de um GIF animado, incluindo dimensões, contagem de quadros, atrasos e duração.
      security: []
      requestBody:
        required: true
        content:
          multipart/form-data:
            schema:
              type: object
              required:
                - file
              properties:
                file:
                  type: string
                  format: binary
                  description: GIF file to inspect
      responses:
        '200':
          description: GIF metadata
          content:
            application/json:
              schema:
                type: object
                properties:
                  width:
                    type: integer
                  height:
                    type: integer
                  pages:
                    type: integer
                    description: Number of frames
                  delay:
                    type: array
                    items:
                      type: integer
                    description: Per-frame delay in milliseconds
                  loop:
                    type: integer
                    description: Loop count (0 = infinite)
                  fileSize:
                    type: integer
                  duration:
                    type: integer
                    description: Total duration in milliseconds
        '400':
          description: Invalid input
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Error'
  /api/v1/tools/image/smart-crop:
    post:
      operationId: smartCrop
      tags:
        - Tools
      summary: Recorte inteligente
      description: 'Recorte inteligente com três modos: foco no objeto, foco no rosto ou corte automático.'
      security:
        - bearerAuth: []
      requestBody:
        required: true
        content:
          multipart/form-data:
            schema:
              type: object
              required:
                - file
              properties:
                file:
                  type: string
                  format: binary
                  description: Image file to process
                settings:
                  type: string
                  description: |
                    JSON string with options:
                    - `mode` (string, default "subject") - One of: subject, face, trim, attention (alias for subject), content (alias for trim)
                    - `strategy` (string, default "attention") - One of: attention, entropy (subject mode)
                    - `width` (integer, optional) - Target width in pixels
                    - `height` (integer, optional) - Target height in pixels
                    - `padding` (integer 0-50, default 0) - Padding percentage around focus area
                    - `facePreset` (string, default "head-shoulders") - One of: closeup, head-shoulders, upper-body, half-body (face mode)
                    - `sensitivity` (number 0-1, default 0.5) - Face detection sensitivity (face mode)
                    - `threshold` (integer 0-255, default 30) - Trim tolerance (trim mode)
                    - `padToSquare` (boolean, default false) - Pad to square after trimming (trim mode)
                    - `padColor` (string, default "#ffffff") - Hex color for padding (trim mode)
                    - `targetSize` (integer, optional) - Target size for padded output (trim mode)
                    - `quality` (integer 1-100, optional) - Output quality
      responses:
        '200':
          description: Processed image
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ToolResponse'
        '400':
          description: Invalid input
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Error'
        '401':
          description: Authentication required
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/UnauthorizedError'
  /api/v1/tools/image/vectorize:
    post:
      operationId: vectorizeImage
      tags:
        - Tools
      summary: Imagem para SVG
      description: Converte uma imagem raster para o formato vetorial SVG usando potrace (P&B) ou VTracer (colorido).
      security:
        - bearerAuth: []
      requestBody:
        required: true
        content:
          multipart/form-data:
            schema:
              type: object
              required:
                - file
              properties:
                file:
                  type: string
                  format: binary
                  description: Image file to process
                settings:
                  type: string
                  description: |
                    JSON string with options:
                    - `colorMode` (string, default "bw") - One of: bw, color
                    - `threshold` (number 0-255, default 128) - B&W binarization threshold
                    - `colorPrecision` (number 1-16, default 6) - Color bits per channel
                    - `layerDifference` (number 1-128, default 6) - Color gradient step
                    - `filterSpeckle` (number 1-256, default 4) - Noise filter size
                    - `pathMode` (string, default "spline") - One of: none, polygon, spline
                    - `cornerThreshold` (number 0-180, default 60) - Corner detection angle
                    - `invert` (boolean, default false) - Invert colors before tracing
      responses:
        '200':
          description: Processed image (downloadUrl points to .svg file)
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ToolResponse'
        '400':
          description: Invalid input
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Error'
        '401':
          description: Authentication required
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/UnauthorizedError'
  /api/v1/tools/image/svg-to-raster:
    post:
      operationId: svgToRaster
      tags:
        - Tools
      summary: SVG para raster
      description: Converte um arquivo SVG para um formato de imagem raster em escala e DPI personalizados.
      security:
        - bearerAuth: []
      requestBody:
        required: true
        content:
          multipart/form-data:
            schema:
              type: object
              required:
                - file
              properties:
                file:
                  type: string
                  format: binary
                  description: SVG file to convert
                settings:
                  type: string
                  description: |
                    JSON string with options:
                    - `width` (number 1-65536, optional) - Output width in pixels
                    - `height` (number 1-65536, optional) - Output height in pixels
                    - `dpi` (number 36-2400, default 300) - Render density for SVG rasterization
                    - `quality` (number 1-100, default 90) - Output quality for lossy formats
                    - `backgroundColor` (hex string, default "#00000000") - Background color
                    - `outputFormat` (string, default "png") - One of: png, jpg, webp, avif, tiff, gif, heif, jxl
      responses:
        '200':
          description: Processed image
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ToolResponse'
        '400':
          description: Invalid input
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Error'
        '401':
          description: Authentication required
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/UnauthorizedError'
  /api/v1/tools/image/svg-to-raster/batch:
    post:
      operationId: svgToRasterBatch
      tags:
        - Tools
      summary: SVG para raster (lote)
      description: Converte vários arquivos SVG em imagens raster. Retorna um arquivo ZIP.
      security:
        - bearerAuth: []
      requestBody:
        required: true
        content:
          multipart/form-data:
            schema:
              type: object
              required:
                - file
              properties:
                file:
                  type: array
                  items:
                    type: string
                    format: binary
                  description: SVG files to convert
                settings:
                  type: string
                  description: Same settings as single-file endpoint
                clientJobId:
                  type: string
                  description: Optional client-generated job ID for progress tracking
      responses:
        '200':
          description: ZIP archive containing processed images
          content:
            application/zip:
              schema:
                type: string
                format: binary
        '400':
          description: Invalid input
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Error'
        '401':
          description: Authentication required
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/UnauthorizedError'
        '422':
          description: All files failed processing
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Error'
  /api/v1/tools/image/image-to-pdf:
    post:
      operationId: imageToPdf
      tags:
        - Tools
      summary: Imagem para PDF
      description: Converte uma ou mais imagens em um documento PDF.
      security:
        - bearerAuth: []
      requestBody:
        required: true
        content:
          multipart/form-data:
            schema:
              type: object
              required:
                - file
              properties:
                file:
                  type: array
                  items:
                    type: string
                    format: binary
                  description: One or more image files to include in the PDF
                settings:
                  type: string
                  description: |
                    JSON string with options:
                    - `pageSize` (string, default "A4") - One of: A4, Letter, A3, A5
                    - `orientation` (string, default "portrait") - One of: portrait, landscape
                    - `margin` (number 0-500, default 20) - Page margin in points
                    - `targetSize` (object, optional) - Target output file size
                      - `value` (number, required) - Positive number (decimals allowed)
                      - `unit` (string, required) - One of: KB, MB
                      Minimum effective target is 50KB. When set, images are compressed as JPEG with quality auto-tuned to meet the target.
                    - `collate` (boolean, default true) - When true, all images go into one PDF. When false, each image becomes a separate PDF and the result is a ZIP.
      responses:
        '200':
          description: Generated PDF (downloadUrl points to .pdf file)
          content:
            application/json:
              schema:
                allOf:
                  - $ref: '#/components/schemas/ToolResponse'
                  - type: object
                    properties:
                      pages:
                        type: integer
                        description: Number of images/pages in the PDF
                      compression:
                        type: object
                        description: Present only when targetSize was specified
                        properties:
                          targetRequested:
                            type: integer
                            description: Target size in bytes
                          targetMet:
                            type: boolean
                            description: Whether the target was achieved
                          jpegQuality:
                            type: integer
                            description: Final JPEG quality used (10-95)
        '400':
          description: Invalid input
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Error'
        '401':
          description: Authentication required
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/UnauthorizedError'
        '422':
          description: Processing failed
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Error'
  /api/v1/tools/pdf/pdf-to-image:
    post:
      operationId: pdfToImage
      tags:
        - Tools
      summary: PDF para imagem
      description: Converte páginas de PDF em imagens. Retorna downloads individuais das páginas e um ZIP com todas as páginas.
      security:
        - bearerAuth: []
      requestBody:
        required: true
        content:
          multipart/form-data:
            schema:
              type: object
              required:
                - file
              properties:
                file:
                  type: string
                  format: binary
                  description: PDF file to convert
                settings:
                  type: string
                  description: |
                    JSON string with options:
                    - `format` (string, default "png") - Output format: png, jpg, webp, avif, tiff, gif, heic, heif, jxl
                    - `dpi` (number 36-2400, default 150) - Resolution in dots per inch
                    - `quality` (number 1-100, default 85) - Output quality
                    - `colorMode` (string, default "color") - One of: color, grayscale, bw
                    - `pages` (string, default "all") - Page selection, e.g. "all", "1-3", "1,3,5"
      responses:
        '200':
          description: Converted pages
          content:
            application/json:
              schema:
                type: object
                properties:
                  jobId:
                    type: string
                  pageCount:
                    type: integer
                    description: Total pages in the PDF
                  selectedPages:
                    type: array
                    items:
                      type: integer
                  format:
                    type: string
                  pages:
                    type: array
                    items:
                      type: object
                      properties:
                        page:
                          type: integer
                        downloadUrl:
                          type: string
                        size:
                          type: integer
                  zipUrl:
                    type: string
                    description: URL to download all pages as ZIP
                  zipSize:
                    type: integer
        '400':
          description: Invalid input
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Error'
        '401':
          description: Authentication required
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/UnauthorizedError'
  /api/v1/tools/pdf/pdf-to-image/info:
    post:
      operationId: getPdfInfo
      tags:
        - Tools
      summary: Contagem de páginas do PDF
      description: Obtém o número de páginas de um arquivo PDF.
      security: []
      requestBody:
        required: true
        content:
          multipart/form-data:
            schema:
              type: object
              required:
                - file
              properties:
                file:
                  type: string
                  format: binary
                  description: PDF file to inspect
      responses:
        '200':
          description: PDF info
          content:
            application/json:
              schema:
                type: object
                properties:
                  pageCount:
                    type: integer
        '400':
          description: Invalid input
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Error'
  /api/v1/tools/pdf/pdf-to-image/preview:
    post:
      operationId: previewPdfPages
      tags:
        - Tools
      summary: Miniaturas de páginas do PDF
      description: Gera previews em miniatura para cada página de um PDF (máx. 200 páginas, JPEG de 300px de largura).
      security: []
      requestBody:
        required: true
        content:
          multipart/form-data:
            schema:
              type: object
              required:
                - file
              properties:
                file:
                  type: string
                  format: binary
                  description: PDF file to preview
      responses:
        '200':
          description: Page thumbnails
          content:
            application/json:
              schema:
                type: object
                properties:
                  pageCount:
                    type: integer
                  thumbnails:
                    type: array
                    items:
                      type: object
                      properties:
                        page:
                          type: integer
                        dataUrl:
                          type: string
                          description: Base64-encoded JPEG data URL
                        width:
                          type: integer
                        height:
                          type: integer
        '400':
          description: Invalid input
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Error'
  /api/v1/tools/image/split:
    post:
      operationId: splitImage
      tags:
        - Tools
      summary: Dividir Imagem
      description: Divide uma imagem em uma grade de blocos. Retorna um arquivo ZIP.
      security:
        - bearerAuth: []
      requestBody:
        required: true
        content:
          multipart/form-data:
            schema:
              type: object
              required:
                - file
              properties:
                file:
                  type: string
                  format: binary
                  description: Image file to split
                settings:
                  type: string
                  description: |
                    JSON string with options:
                    - `columns` (number 1-100, default 3) - Number of columns
                    - `rows` (number 1-100, default 3) - Number of rows
                    - `tileWidth` (number, min 10, optional) - Fixed tile width in pixels
                    - `tileHeight` (number, min 10, optional) - Fixed tile height in pixels
                    - `outputFormat` (string, default "original") - One of: original, png, jpg, webp, avif, jxl
                    - `quality` (number 1-100, default 90) - Output quality
      responses:
        '200':
          description: ZIP archive with results
          content:
            application/zip:
              schema:
                type: string
                format: binary
        '400':
          description: Invalid input
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Error'
        '401':
          description: Authentication required
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/UnauthorizedError'
  /api/v1/tools/image/bulk-rename:
    post:
      operationId: bulkRename
      tags:
        - Tools
      summary: Renomeação em massa
      description: Renomeia várias imagens usando um modelo de padrão. Suporta os tokens {{index}}, {{padded}} e {{original}}. Retorna um arquivo ZIP.
      security:
        - bearerAuth: []
      requestBody:
        required: true
        content:
          multipart/form-data:
            schema:
              type: object
              required:
                - file
              properties:
                file:
                  type: array
                  items:
                    type: string
                    format: binary
                  description: Multiple image files to rename
                settings:
                  type: string
                  description: |
                    JSON string with options:
                    - `pattern` (string 1-1000, default "image-{{index}}") - Naming pattern template
                    - `startIndex` (number, default 1) - Starting index number
      responses:
        '200':
          description: ZIP archive with results
          content:
            application/zip:
              schema:
                type: string
                format: binary
        '400':
          description: Invalid input
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Error'
        '401':
          description: Authentication required
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/UnauthorizedError'
  /api/v1/tools/image/favicon:
    post:
      operationId: generateFavicon
      tags:
        - Tools
      summary: Gerador de favicon
      description: Gera um conjunto completo de favicons (PNGs de 16px a 512px, ICO e manifest.json) a partir de uma imagem. Retorna um arquivo ZIP.
      security:
        - bearerAuth: []
      requestBody:
        required: true
        content:
          multipart/form-data:
            schema:
              type: object
              required:
                - file
              properties:
                file:
                  type: string
                  format: binary
                  description: Image file to use as favicon source
      responses:
        '200':
          description: ZIP archive with results
          content:
            application/zip:
              schema:
                type: string
                format: binary
        '400':
          description: Invalid input
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Error'
        '401':
          description: Authentication required
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/UnauthorizedError'
  /api/v1/tools/image/watermark-image:
    post:
      operationId: addImageWatermark
      tags:
        - Tools
      summary: Marca d'água de imagem
      description: Adiciona uma sobreposição de marca d'água de imagem.
      security:
        - bearerAuth: []
      requestBody:
        required: true
        content:
          multipart/form-data:
            schema:
              type: object
              required:
                - file
                - watermark
              properties:
                file:
                  type: string
                  format: binary
                  description: Main image file
                watermark:
                  type: string
                  format: binary
                  description: Watermark overlay image
                settings:
                  type: string
                  description: |
                    JSON string with options:
                    - `position` (string, default "bottom-right") - One of: center, top-left, top-right, bottom-left, bottom-right
                    - `opacity` (number 0-100, default 50) - Watermark opacity percentage
                    - `scale` (number 1-100, default 25) - Watermark size as percentage of base image
      responses:
        '200':
          description: Processed image
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ToolResponse'
        '400':
          description: Invalid input
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Error'
        '401':
          description: Authentication required
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/UnauthorizedError'
  /api/v1/tools/image/compose:
    post:
      operationId: composeImages
      tags:
        - Tools
      summary: Composição de imagem
      description: Compõe uma imagem de sobreposição sobre uma imagem base com modos de mesclagem.
      security:
        - bearerAuth: []
      requestBody:
        required: true
        content:
          multipart/form-data:
            schema:
              type: object
              required:
                - file
                - overlay
              properties:
                file:
                  type: string
                  format: binary
                  description: Base image file
                overlay:
                  type: string
                  format: binary
                  description: Overlay image file
                settings:
                  type: string
                  description: |
                    JSON string with options:
                    - `x` (number, default 0) - Horizontal offset of overlay
                    - `y` (number, default 0) - Vertical offset of overlay
                    - `opacity` (number 0-100, default 100) - Overlay opacity percentage
                    - `blendMode` (string, default "over") - One of: over, multiply, screen, overlay, darken, lighten, hard-light, soft-light, difference, exclusion
      responses:
        '200':
          description: Processed image
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ToolResponse'
        '400':
          description: Invalid input
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Error'
        '401':
          description: Authentication required
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/UnauthorizedError'
  /api/v1/tools/image/meme-generator:
    post:
      operationId: generateMeme
      tags:
        - Tools
      summary: Gerador de memes
      description: |
        Cria memes usando modelos ou imagens personalizadas. Suporta dois modos: (1) Modo de modelo - envie corpo JSON com `templateId` e `textBoxes`. (2) Modo de imagem personalizada - envie multipart com `file` e `settings`.
      security:
        - bearerAuth: []
      requestBody:
        required: true
        content:
          multipart/form-data:
            schema:
              type: object
              properties:
                file:
                  type: string
                  format: binary
                  description: Custom image file (optional if using templateId)
                settings:
                  type: string
                  description: |
                    JSON string with options:
                    - `templateId` (string, optional) - ID of a meme template from /api/v1/meme-templates
                    - `textLayout` (string, default "top-bottom") - One of: top-bottom, top-only, bottom-only, center, side-by-side
                    - `textBoxes` (array of {id, text}) - Text content for each position
                    - `fontFamily` (string, default "anton") - One of: anton, arial-black, comic-sans, montserrat, bebas-neue, permanent-marker, roboto
                    - `fontSize` (number 8-200, optional) - Font size in pixels
                    - `textColor` (hex string, default "#ffffff") - Text color
                    - `strokeColor` (hex string, default "#000000") - Text outline color
                    - `textAlign` (string, default "center") - One of: left, center, right
                    - `allCaps` (boolean, default true) - Convert text to uppercase
          application/json:
            schema:
              type: object
              description: Template mode - send templateId and textBoxes as JSON body
              properties:
                templateId:
                  type: string
                  description: ID of a meme template
                textBoxes:
                  type: array
                  items:
                    type: object
                    properties:
                      id:
                        type: string
                      text:
                        type: string
      responses:
        '200':
          description: Processed meme image
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ToolResponse'
        '400':
          description: Invalid input or template not found
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Error'
        '401':
          description: Authentication required
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/UnauthorizedError'
        '422':
          description: Processing failed
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Error'
  /api/v1/meme-templates:
    get:
      operationId: listMemeTemplates
      tags:
        - Tools
      summary: Listar modelos de meme
      description: Retorna o manifesto completo dos modelos de meme disponíveis, incluindo as posições das caixas de texto.
      security:
        - bearerAuth: []
      responses:
        '200':
          description: Meme template manifest
          content:
            application/json:
              schema:
                type: object
                properties:
                  templates:
                    type: array
                    items:
                      type: object
                      properties:
                        id:
                          type: string
                        filename:
                          type: string
                        width:
                          type: integer
                        height:
                          type: integer
                        textBoxes:
                          type: array
                          items:
                            type: object
                            properties:
                              id:
                                type: string
                              x:
                                type: number
                              'y':
                                type: number
                              width:
                                type: number
                              height:
                                type: number
                              defaultText:
                                type: string
        '401':
          description: Authentication required
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/UnauthorizedError'
  /api/v1/meme-templates/full/{filename}:
    get:
      operationId: getMemeTemplateImage
      tags:
        - Tools
      summary: Obter imagem de modelo de meme
      description: Serve uma imagem de modelo de meme em tamanho completo.
      security:
        - bearerAuth: []
      parameters:
        - name: filename
          in: path
          required: true
          schema:
            type: string
          description: Template image filename
      responses:
        '200':
          description: Template image
          content:
            image/*:
              schema:
                type: string
                format: binary
        '400':
          description: Invalid filename
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Error'
        '404':
          description: Template not found
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Error'
  /api/v1/meme-templates/thumbs/{filename}:
    get:
      operationId: getMemeTemplateThumbnail
      tags:
        - Tools
      summary: Obter miniatura de modelo de meme
      description: Serve uma miniatura de um modelo de meme.
      security:
        - bearerAuth: []
      parameters:
        - name: filename
          in: path
          required: true
          schema:
            type: string
          description: Template thumbnail filename
      responses:
        '200':
          description: Thumbnail image
          content:
            image/*:
              schema:
                type: string
                format: binary
        '400':
          description: Invalid filename
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Error'
        '404':
          description: Thumbnail not found
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Error'
  /api/v1/meme-templates/fonts/{filename}:
    get:
      operationId: getMemeTemplateFont
      tags:
        - Tools
      summary: Obter fonte de modelo de meme
      description: Serve um arquivo de fonte usado pelo gerador de memes.
      security:
        - bearerAuth: []
      parameters:
        - name: filename
          in: path
          required: true
          schema:
            type: string
          description: Font filename (e.g. anton.ttf)
      responses:
        '200':
          description: Font file
          content:
            font/ttf:
              schema:
                type: string
                format: binary
        '400':
          description: Invalid filename
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Error'
        '404':
          description: Font not found
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Error'
  /api/v1/tools/image/compare:
    post:
      operationId: compareImages
      tags:
        - Tools
      summary: Comparar imagens
      description: Compara duas imagens e gera um relatório visual de diferenças.
      security:
        - bearerAuth: []
      requestBody:
        required: true
        content:
          multipart/form-data:
            schema:
              type: object
              required:
                - file1
                - file2
              properties:
                file1:
                  type: string
                  format: binary
                  description: First image to compare
                file2:
                  type: string
                  format: binary
                  description: Second image to compare
      responses:
        '200':
          description: Comparison result
          content:
            application/json:
              schema:
                allOf:
                  - $ref: '#/components/schemas/ToolResponse'
                  - type: object
                    properties:
                      similarity:
                        type: number
                        description: Similarity percentage (0-100)
                      dimensions:
                        type: object
                        properties:
                          width:
                            type: integer
                          height:
                            type: integer
        '400':
          description: Invalid input
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Error'
        '401':
          description: Authentication required
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/UnauthorizedError'
        '422':
          description: Processing failed
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Error'
  /api/v1/tools/image/erase-object:
    post:
      operationId: eraseObject
      tags:
        - Tools
      summary: Apagador de objetos
      description: Apaga objetos de uma imagem usando uma máscara. Áreas brancas na máscara indicam as regiões a apagar. Usa inpainting LaMa.
      security:
        - bearerAuth: []
      requestBody:
        required: true
        content:
          multipart/form-data:
            schema:
              type: object
              required:
                - image
                - mask
              properties:
                image:
                  type: string
                  format: binary
                  description: Source image file
                mask:
                  type: string
                  format: binary
                  description: Mask image (white areas will be erased)
                format:
                  type: string
                  enum:
                    - auto
                    - png
                    - jpg
                    - jpeg
                    - webp
                    - tiff
                    - gif
                    - avif
                    - heic
                    - heif
                    - jxl
                  default: auto
                  description: Output format
                quality:
                  type: integer
                  minimum: 1
                  maximum: 100
                  default: 95
                  description: Output quality
                clientJobId:
                  type: string
                  description: Client-provided job ID for SSE progress tracking
      responses:
        '202':
          description: Accepted for async processing. Track progress via SSE at /api/v1/jobs/{jobId}/progress.
          content:
            application/json:
              schema:
                type: object
                properties:
                  jobId:
                    type: string
                  async:
                    type: boolean
                    example: true
        '400':
          description: Invalid input
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Error'
        '401':
          description: Authentication required
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/UnauthorizedError'
        '422':
          description: Processing failed
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Error'
        '501':
          description: Feature not installed
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/FeatureNotInstalledError'
  /api/v1/tools/image/ai-canvas-expand:
    post:
      operationId: aiCanvasExpand
      tags:
        - Tools
      summary: Expansão de canvas com IA (experimental)
      description: |
        Expande o canvas de uma imagem usando outpainting com IA. Estende a imagem em qualquer
        direção gerando novo conteúdo que se integra de forma harmônica ao
        original. Usa outpainting baseado em LaMa. Requer o bundle de recurso ai-canvas-expand
        instalado.
      security:
        - bearerAuth: []
      requestBody:
        required: true
        content:
          multipart/form-data:
            schema:
              type: object
              required:
                - file
              properties:
                file:
                  type: string
                  format: binary
                  description: Image file to expand
                settings:
                  type: string
                  description: |
                    JSON string with options:
                    - `extendTop` (integer >= 0, default 0) - Pixels to extend upward
                    - `extendRight` (integer >= 0, default 0) - Pixels to extend rightward
                    - `extendBottom` (integer >= 0, default 0) - Pixels to extend downward
                    - `extendLeft` (integer >= 0, default 0) - Pixels to extend leftward
                    - `tier` (string, default "balanced") - One of: fast, balanced, high
                    - `format` (string, default "auto") - One of: auto, png, jpg, jpeg, webp, tiff, gif, avif, heic, heif, jxl
                    - `quality` (integer 1-100, default 95) - Output quality

                    At least one extend direction must be greater than 0.
                clientJobId:
                  type: string
                  description: Client-provided job ID for SSE progress tracking
      responses:
        '202':
          description: Accepted for async processing. Track progress via SSE at /api/v1/jobs/{jobId}/progress.
          content:
            application/json:
              schema:
                type: object
                properties:
                  jobId:
                    type: string
                  async:
                    type: boolean
                    example: true
        '400':
          description: Invalid input (or no extend direction > 0)
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Error'
        '401':
          description: Authentication required
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/UnauthorizedError'
        '422':
          description: Processing failed
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Error'
        '501':
          description: Feature not installed
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/FeatureNotInstalledError'
  /api/v1/tools/image/collage:
    post:
      operationId: createCollage
      tags:
        - Tools
      summary: Collage e Grade
      description: Combina várias imagens em uma colagem em grade.
      security:
        - bearerAuth: []
      requestBody:
        required: true
        content:
          multipart/form-data:
            schema:
              type: object
              required:
                - file
              properties:
                file:
                  type: array
                  items:
                    type: string
                    format: binary
                  description: Multiple image files to combine
                settings:
                  type: string
                  description: |
                    JSON string with options:
                    - `templateId` (string, required) - Template ID defining the grid layout
                    - `cells` (array, optional) - Per-cell configuration overrides
                    - `gap` (number 0-500, default 8) - Gap between images in pixels
                    - `cornerRadius` (number 0-500, default 0) - Corner rounding radius
                    - `backgroundColor` (string, default "#FFFFFF") - Background fill color
                    - `aspectRatio` (string, default "free") - Aspect ratio constraint
                    - `outputFormat` (string, default "png") - One of: png, jpeg, webp, avif, jxl
                    - `quality` (number 1-100, default 90) - Output quality
      responses:
        '200':
          description: Processed image
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ToolResponse'
        '400':
          description: Invalid input
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Error'
        '401':
          description: Authentication required
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/UnauthorizedError'
  /api/v1/tools/image/stitch:
    post:
      operationId: stitchImages
      tags:
        - Tools
      summary: Juntar Imagens
      description: Une várias imagens na horizontal, na vertical ou em um layout de grade.
      security:
        - bearerAuth: []
      requestBody:
        required: true
        content:
          multipart/form-data:
            schema:
              type: object
              required:
                - file
              properties:
                file:
                  type: array
                  items:
                    type: string
                    format: binary
                  description: Two or more image files to stitch together
                settings:
                  type: string
                  description: |
                    JSON string with options:
                    - `direction` (string, default "horizontal") - One of: horizontal, vertical, grid
                    - `gridColumns` (integer 2-100, default 2) - Columns when direction is grid
                    - `resizeMode` (string, default "fit") - One of: fit, original, stretch, crop
                    - `alignment` (string, default "center") - One of: start, center, end
                    - `gap` (number 0-1000, default 0) - Gap between images in pixels
                    - `border` (number 0-500, default 0) - Border width in pixels
                    - `cornerRadius` (number 0-500, default 0) - Corner radius in pixels
                    - `backgroundColor` (hex string, default "#FFFFFF") - Hex color for background and gap fill
                    - `format` (string, default "png") - One of: png, jpeg, webp, avif, jxl
                    - `quality` (number 1-100, default 90) - Output quality
      responses:
        '200':
          description: Stitched image
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ToolResponse'
        '400':
          description: Invalid input
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Error'
        '401':
          description: Authentication required
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/UnauthorizedError'
  /api/v1/tools/image/remove-gif-background:
    post:
      operationId: removeGifBackground
      tags:
        - Tools
      summary: Remover o fundo de uma imagem animada
      description: Remova o plano de fundo de um GIF, WebP ou APNG animado quadro a quadro usando AI (rembg). Executa localmente.
      security:
        - bearerAuth: []
      requestBody:
        required: true
        content:
          multipart/form-data:
            schema:
              type: object
              required:
                - file
              properties:
                file:
                  type: string
                  format: binary
                  description: Animated image file (GIF, animated WebP, or APNG)
                settings:
                  type: string
                  description: |
                    JSON string with options:
                    - `model` (string, optional) - AI model name
                    - `outputFormat` (string, optional) - One of: webp, apng, gif
                    - `backgroundType` (string, optional) - One of: transparent, color, gradient, blur, image
                    - `backgroundColor` (string, optional) - Hex color for the color background
                    - `gradientColor1` (string, optional) - First gradient color
                    - `gradientColor2` (string, optional) - Second gradient color
                    - `gradientAngle` (number, optional) - Gradient angle in degrees
                    - `blurIntensity` (number 0-100, optional) - Blur strength for the blur background
                    - `shadowEnabled` (boolean, optional) - Enable drop shadow
                    - `shadowOpacity` (number 0-100, optional) - Shadow opacity
                    - `edgeRefine` (number 0-3, optional) - Edge refinement level
                    - `decontaminate` (boolean, optional) - Remove background color spill
                backgroundImage:
                  type: string
                  format: binary
                  description: Background image, required when backgroundType is "image"
                clientJobId:
                  type: string
                  description: Client-provided job ID for SSE progress tracking
      responses:
        '202':
          description: Accepted for async processing. Track progress via SSE at /api/v1/jobs/{jobId}/progress.
          content:
            application/json:
              schema:
                type: object
                properties:
                  jobId:
                    type: string
                  async:
                    type: boolean
                    example: true
        '400':
          description: Invalid input (not animated, over the frame cap, or bad settings)
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Error'
        '401':
          description: Authentication required
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/UnauthorizedError'
        '501':
          description: Feature not installed
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/FeatureNotInstalledError'
  /api/v1/tools/image/remove-background:
    post:
      operationId: removeBackground
      tags:
        - Tools
      summary: Remover fundo
      description: Remove o fundo de uma imagem usando IA (rembg). Executa localmente.
      security:
        - bearerAuth: []
      requestBody:
        required: true
        content:
          multipart/form-data:
            schema:
              type: object
              required:
                - file
              properties:
                file:
                  type: string
                  format: binary
                  description: Image file to process
                settings:
                  type: string
                  description: |
                    JSON string with options:
                    - `model` (string, optional) - AI model name
                    - `backgroundType` (string, optional) - One of: transparent, color, gradient, blur, image
                    - `backgroundColor` (string, optional) - Hex color to replace removed background with
                    - `gradientColor1` (string, optional) - First gradient color
                    - `gradientColor2` (string, optional) - Second gradient color
                    - `gradientAngle` (number, optional) - Gradient angle in degrees
                    - `blurEnabled` (boolean, optional) - Enable background blur effect
                    - `blurIntensity` (number 0-100, optional) - Blur strength
                    - `shadowEnabled` (boolean, optional) - Enable drop shadow
                    - `shadowOpacity` (number 0-100, optional) - Shadow opacity
                clientJobId:
                  type: string
                  description: Client-provided job ID for SSE progress tracking
      responses:
        '202':
          description: Accepted for async processing. Track progress via SSE at /api/v1/jobs/{jobId}/progress.
          content:
            application/json:
              schema:
                type: object
                properties:
                  jobId:
                    type: string
                  async:
                    type: boolean
                    example: true
        '400':
          description: Invalid input
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Error'
        '401':
          description: Authentication required
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/UnauthorizedError'
        '422':
          description: Processing failed
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Error'
        '501':
          description: Feature not installed
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/FeatureNotInstalledError'
  /api/v1/tools/image/remove-background/effects:
    post:
      operationId: applyBackgroundEffects
      tags:
        - Tools
      summary: Aplicar efeitos de fundo
      description: |
        Aplica substituição ou efeitos de fundo a um resultado anterior de remoção de fundo. Requer um jobId de uma chamada anterior a POST /api/v1/tools/image/remove-background.
      security:
        - bearerAuth: []
      requestBody:
        required: true
        content:
          multipart/form-data:
            schema:
              type: object
              required:
                - settings
              properties:
                backgroundImage:
                  type: string
                  format: binary
                  description: Custom background image (required when backgroundType is "image")
                settings:
                  type: string
                  description: |
                    JSON string with options:
                    - `jobId` (string, required) - Job ID from the initial remove-background call
                    - `filename` (string, required) - Original filename
                    - `backgroundType` (string) - One of: transparent, color, gradient, blur, image
                    - `backgroundColor` (string) - Hex color for solid background
                    - `gradientColor1` (string) - First gradient color
                    - `gradientColor2` (string) - Second gradient color
                    - `gradientAngle` (number) - Gradient angle in degrees
                    - `blurEnabled` (boolean) - Enable background blur effect
                    - `blurIntensity` (number 0-100) - Blur strength
                    - `shadowEnabled` (boolean) - Enable drop shadow
                    - `shadowOpacity` (number 0-100) - Shadow opacity
      responses:
        '200':
          description: Image with applied effects
          content:
            application/json:
              schema:
                type: object
                properties:
                  jobId:
                    type: string
                  downloadUrl:
                    type: string
                  processedSize:
                    type: integer
        '400':
          description: Invalid input
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Error'
        '401':
          description: Authentication required
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/UnauthorizedError'
  /api/v1/tools/image/transparency-fixer:
    post:
      operationId: fixTransparency
      tags:
        - Tools
      summary: Corrigir falsa transparência
      description: |
        Corrige PNGs de "falsa transparência" que têm franjas, halos ou artefatos
        semitransparentes de uma remoção de fundo anterior. Usa BiRefNet HR-matting
        (2048x2048) para produzir um canal alpha limpo com processamento de defringe
        configurável. Faz fallback para birefnet-general e, em caso de OOM, para u2net. Requer
        o bundle de recurso background-removal instalado.
      security:
        - bearerAuth: []
      requestBody:
        required: true
        content:
          multipart/form-data:
            schema:
              type: object
              required:
                - file
              properties:
                file:
                  type: string
                  format: binary
                  description: PNG image with fake or damaged transparency
                settings:
                  type: string
                  description: |
                    JSON string with options:
                    - `defringe` (number 0-100, default 30) - Edge defringe strength to remove color contamination
                    - `outputFormat` (string, default "png") - One of: png, webp
                    - `removeWatermark` (boolean, default false) - Apply median filter to reduce watermark artifacts before matting
                clientJobId:
                  type: string
                  description: Client-provided job ID for SSE progress tracking
      responses:
        '202':
          description: Accepted for async processing. Track progress via SSE at /api/v1/jobs/{jobId}/progress.
          content:
            application/json:
              schema:
                type: object
                properties:
                  jobId:
                    type: string
                  async:
                    type: boolean
                    example: true
        '400':
          description: Invalid input
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Error'
        '401':
          description: Authentication required
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/UnauthorizedError'
        '422':
          description: Processing failed
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Error'
        '501':
          description: Feature not installed
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/FeatureNotInstalledError'
  /api/v1/tools/image/upscale:
    post:
      operationId: upscaleImage
      tags:
        - Tools
      summary: Ampliação de imagem
      description: Amplia uma imagem usando IA (Real-ESRGAN). Executa localmente.
      security:
        - bearerAuth: []
      requestBody:
        required: true
        content:
          multipart/form-data:
            schema:
              type: object
              required:
                - file
              properties:
                file:
                  type: string
                  format: binary
                  description: Image file to upscale
                settings:
                  type: string
                  description: |
                    JSON string with options:
                    - `scale` (number, default 2) - Upscale factor
                    - `model` (string, default "auto") - AI model name
                    - `faceEnhance` (boolean, default false) - Enable face enhancement
                    - `denoise` (number, default 0) - Denoise strength
                    - `format` (string, default "auto") - Output format (auto, png, jpg, jpeg, webp, tiff, gif, avif, heic, heif, jxl)
                    - `quality` (number, default 95) - Output quality (1-100)
                clientJobId:
                  type: string
                  description: Client-provided job ID for SSE progress tracking
      responses:
        '202':
          description: Accepted for async processing. Track progress via SSE at /api/v1/jobs/{jobId}/progress.
          content:
            application/json:
              schema:
                type: object
                properties:
                  jobId:
                    type: string
                  async:
                    type: boolean
                    example: true
        '400':
          description: Invalid input
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Error'
        '401':
          description: Authentication required
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/UnauthorizedError'
        '422':
          description: Processing failed
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Error'
        '501':
          description: Feature not installed
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/FeatureNotInstalledError'
  /api/v1/tools/image/blur-faces:
    post:
      operationId: blurFaces
      tags:
        - Tools
      summary: Desfocar Rostos e Dados Sensíveis
      description: Detecta e desfoca rostos em uma imagem por privacidade. Usa OpenCV. Executa localmente.
      security:
        - bearerAuth: []
      requestBody:
        required: true
        content:
          multipart/form-data:
            schema:
              type: object
              required:
                - file
              properties:
                file:
                  type: string
                  format: binary
                  description: Image file to process
                settings:
                  type: string
                  description: |
                    JSON string with options:
                    - `blurRadius` (number 1-100, default 30) - Blur strength radius
                    - `sensitivity` (number 0-1, default 0.5) - Face detection sensitivity
                clientJobId:
                  type: string
                  description: Client-provided job ID for SSE progress tracking
      responses:
        '202':
          description: Accepted for async processing. Track progress via SSE at /api/v1/jobs/{jobId}/progress.
          content:
            application/json:
              schema:
                type: object
                properties:
                  jobId:
                    type: string
                  async:
                    type: boolean
                    example: true
        '400':
          description: Invalid input
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Error'
        '401':
          description: Authentication required
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/UnauthorizedError'
        '422':
          description: Processing failed
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Error'
        '501':
          description: Feature not installed
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/FeatureNotInstalledError'
  /api/v1/tools/image/ocr:
    post:
      operationId: extractText
      tags:
        - Tools
      summary: OCR / extração de texto
      description: |-
        Extraia texto de uma imagem localmente. A camada Fast integrada usa
        Tesseract. Balanced e Best usam o tempo de execução RapidOCR assinado opcional
        com modelos PP-OCR ONNX fixados. Solicitações de nível explícito nunca silenciosamente
        fazer downgrade para outro nível.
      security:
        - bearerAuth: []
      requestBody:
        required: true
        content:
          multipart/form-data:
            schema:
              type: object
              required:
                - file
              properties:
                file:
                  type: string
                  format: binary
                  maxLength: 536870912
                  description: Image file to extract text from
                settings:
                  type: string
                  description: |
                    JSON string with options:
                    - `quality` (string, optional) - One of: fast, balanced, best. When both quality and engine are omitted, uses the best available tier in this order: best, balanced, fast. Korean never selects fast; it uses best, then balanced, or returns an accurate-runtime install/compatibility error
                    - `language` (string, default "auto") - One of: auto, en, de, fr, es, zh, ja, ko. Korean is unsupported by fast and the legacy tesseract alias
                    - `enhance` (boolean, optional) - Improve local contrast. Fast applies it directly; accurate tiers retain the variant only when calibrated scoring improves the result. Defaults to true for Best and false for Fast/Balanced
                    - `engine` (string, deprecated) - Compatibility alias: tesseract maps to fast; the legacy paddleocr value maps to balanced but does not load PaddlePaddle
                clientJobId:
                  type: string
      responses:
        '202':
          description: Accepted after validation and enqueueing. Track progress via SSE at /api/v1/jobs/{jobId}/progress; processing failures arrive in the terminal failed event.
          content:
            application/json:
              schema:
                type: object
                required:
                  - jobId
                  - async
                properties:
                  jobId:
                    type: string
                  progressJobId:
                    type: string
                  artifactJobId:
                    type: string
                  async:
                    type: boolean
                    const: true
        '400':
          description: Invalid input
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Error'
        '401':
          description: Authentication required
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/UnauthorizedError'
        '413':
          description: Encoded OCR input exceeds the hard 512 MiB safety limit
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Error'
        '501':
          description: Requested Balanced/Best runtime is not installed or is incompatible
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/FeatureNotInstalledError'
        '503':
          description: OCR could not be queued
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Error'
  /api/v1/tools/image/info:
    post:
      operationId: getImageInfo
      tags:
        - Tools
      summary: Informações da imagem
      description: Obtém metadados detalhados de uma imagem, incluindo dimensões, formato, espaço de cor e dados EXIF.
      security:
        - bearerAuth: []
      requestBody:
        required: true
        content:
          multipart/form-data:
            schema:
              type: object
              required:
                - file
              properties:
                file:
                  type: string
                  format: binary
                  description: Image file to inspect
      responses:
        '200':
          description: Image metadata
          content:
            application/json:
              schema:
                type: object
                properties:
                  width:
                    type: integer
                  height:
                    type: integer
                  filename:
                    type: string
                  format:
                    type: string
                  colorSpace:
                    type: string
                  channels:
                    type: integer
                  bitDepth:
                    type: string
                  density:
                    type:
                      - integer
                      - 'null'
                  hasAlpha:
                    type: boolean
                  fileSize:
                    type: integer
                  hasExif:
                    type: boolean
                  isProgressive:
                    type: boolean
                  orientation:
                    type:
                      - integer
                      - 'null'
                  hasProfile:
                    type: boolean
                  hasIcc:
                    type: boolean
                  hasXmp:
                    type: boolean
                  pages:
                    type: integer
                  histogram:
                    type: array
                    items:
                      type: object
                      properties:
                        channel:
                          type: string
                        min:
                          type: number
                        max:
                          type: number
                        mean:
                          type: number
                        stdev:
                          type: number
        '400':
          description: Invalid input
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Error'
        '401':
          description: Authentication required
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/UnauthorizedError'
        '422':
          description: Processing failed
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Error'
  /api/v1/tools/image/color-palette:
    post:
      operationId: extractColorPalette
      tags:
        - Tools
      summary: Paleta de cores
      description: Extrai as cores dominantes de uma imagem.
      security:
        - bearerAuth: []
      requestBody:
        required: true
        content:
          multipart/form-data:
            schema:
              type: object
              required:
                - file
              properties:
                file:
                  type: string
                  format: binary
                  description: Image file to analyze
      responses:
        '200':
          description: Dominant colors
          content:
            application/json:
              schema:
                type: object
                properties:
                  filename:
                    type: string
                  colors:
                    type: array
                    items:
                      type: string
                    description: Hex color strings
                  count:
                    type: integer
        '400':
          description: Invalid input
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Error'
        '401':
          description: Authentication required
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/UnauthorizedError'
  /api/v1/tools/image/barcode-read:
    post:
      operationId: readBarcode
      tags:
        - Tools
      summary: Leitor de código de barras
      description: Lê QR codes e códigos de barras de uma imagem.
      security:
        - bearerAuth: []
      requestBody:
        required: true
        content:
          multipart/form-data:
            schema:
              type: object
              required:
                - file
              properties:
                file:
                  type: string
                  format: binary
                  description: Image file containing barcode or QR code
                settings:
                  type: string
                  description: |
                    JSON string with options:
                    - `tryHarder` (boolean, default true) - Enable more aggressive barcode detection
      responses:
        '200':
          description: Decoded barcode data
          content:
            application/json:
              schema:
                type: object
                properties:
                  filename:
                    type: string
                  barcodes:
                    type: array
                    items:
                      type: object
                      properties:
                        type:
                          type: string
                        text:
                          type: string
                        position:
                          type: object
                          properties:
                            topLeft:
                              type: object
                              properties:
                                x:
                                  type: number
                                'y':
                                  type: number
                            topRight:
                              type: object
                              properties:
                                x:
                                  type: number
                                'y':
                                  type: number
                            bottomLeft:
                              type: object
                              properties:
                                x:
                                  type: number
                                'y':
                                  type: number
                            bottomRight:
                              type: object
                              properties:
                                x:
                                  type: number
                                'y':
                                  type: number
                  annotatedUrl:
                    type:
                      - string
                      - 'null'
                  previewUrl:
                    type:
                      - string
                      - 'null'
        '400':
          description: Invalid input
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Error'
        '401':
          description: Authentication required
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/UnauthorizedError'
        '422':
          description: Processing failed
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Error'
  /api/v1/tools/image/find-duplicates:
    post:
      operationId: findDuplicates
      tags:
        - Tools
      summary: Encontrar duplicatas
      description: Encontra imagens duplicadas em um conjunto usando hash perceptual.
      security:
        - bearerAuth: []
      requestBody:
        required: true
        content:
          multipart/form-data:
            schema:
              type: object
              required:
                - file
              properties:
                file:
                  type: array
                  items:
                    type: string
                    format: binary
                  description: Multiple image files to check for duplicates
                settings:
                  type: string
                  description: |
                    JSON string with options:
                    - `threshold` (number 0-20, default 8) - Hamming distance threshold for duplicate detection
      responses:
        '200':
          description: Duplicate groups
          content:
            application/json:
              schema:
                type: object
                properties:
                  totalImages:
                    type: integer
                  duplicateGroups:
                    type: array
                    items:
                      type: object
                      properties:
                        groupId:
                          type: integer
                        files:
                          type: array
                          items:
                            type: object
                            properties:
                              filename:
                                type: string
                              similarity:
                                type: number
                              width:
                                type: integer
                              height:
                                type: integer
                              fileSize:
                                type: integer
                              format:
                                type: string
                              isBest:
                                type: boolean
                              thumbnail:
                                type:
                                  - string
                                  - 'null'
                  uniqueImages:
                    type: integer
                  spaceSaveable:
                    type: integer
                  skippedFiles:
                    type: array
                    description: Files that could not be processed (present only when non-empty)
                    items:
                      type: object
                      properties:
                        filename:
                          type: string
                        reason:
                          type: string
        '400':
          description: Invalid input
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Error'
        '401':
          description: Authentication required
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/UnauthorizedError'
        '422':
          description: Processing failed
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Error'
  /api/v1/tools/image/qr-generate:
    post:
      operationId: generateQrCode
      tags:
        - Tools
      summary: Gerador de QR code
      description: Gera uma imagem de QR code a partir de texto. Esta ferramenta aceita um corpo JSON, não multipart.
      security:
        - bearerAuth: []
      requestBody:
        required: true
        content:
          application/json:
            schema:
              type: object
              required:
                - text
              properties:
                text:
                  type: string
                  maxLength: 2000
                  description: Text to encode in the QR code
                size:
                  type: integer
                  minimum: 100
                  maximum: 10000
                  default: 400
                  description: Image size in pixels
                errorCorrection:
                  type: string
                  enum:
                    - L
                    - M
                    - Q
                    - H
                  default: M
                foreground:
                  type: string
                  default: '#000000'
                  description: Foreground color (hex)
                background:
                  type: string
                  default: '#FFFFFF'
                  description: Background color (hex)
      responses:
        '200':
          description: Generated QR code
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ToolResponse'
        '400':
          description: Invalid input
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Error'
        '401':
          description: Authentication required
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/UnauthorizedError'
  /api/v1/tools/image/html-to-image:
    post:
      operationId: captureWebpage
      tags:
        - Tools
      summary: Capturar página web como imagem
      description: Captura uma página web como uma imagem de captura de tela de alta qualidade. Aceita um corpo JSON com uma URL e opções de captura.
      security:
        - bearerAuth: []
      requestBody:
        required: true
        content:
          application/json:
            schema:
              type: object
              required:
                - url
              properties:
                url:
                  type: string
                  format: uri
                  description: URL to capture (http/https only)
                format:
                  type: string
                  enum:
                    - jpg
                    - png
                    - webp
                  default: png
                quality:
                  type: integer
                  minimum: 1
                  maximum: 100
                  default: 90
                fullPage:
                  type: boolean
                  default: false
                devicePreset:
                  type: string
                  enum:
                    - desktop
                    - tablet
                    - mobile
                    - custom
                  default: desktop
                viewportWidth:
                  type: integer
                  minimum: 320
                  maximum: 3840
                  default: 1280
                viewportHeight:
                  type: integer
                  minimum: 320
                  maximum: 2160
                  default: 720
      responses:
        '200':
          description: Screenshot captured successfully
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ToolResponse'
        '400':
          description: Invalid URL or parameters
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Error'
        '401':
          description: Authentication required
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/UnauthorizedError'
        '503':
          description: Browser service unavailable
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Error'
        '504':
          description: Page load timeout
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Error'
  /api/v1/tools/image/strip-metadata/inspect:
    post:
      operationId: inspectMetadata
      tags:
        - Tools
      summary: Inspecionar metadados
      description: Visualiza todos os metadados (EXIF, GPS, ICC, XMP) de uma imagem sem removê-los.
      security:
        - bearerAuth: []
      requestBody:
        required: true
        content:
          multipart/form-data:
            schema:
              type: object
              required:
                - file
              properties:
                file:
                  type: string
                  format: binary
                  description: Image file to inspect
      responses:
        '200':
          description: Image metadata
          content:
            application/json:
              schema:
                type: object
                properties:
                  filename:
                    type: string
                  fileSize:
                    type: integer
                  exif:
                    type: object
                    description: EXIF metadata
                  exifError:
                    type: string
                    description: Present when EXIF parsing fails
                  gps:
                    type: object
                    description: GPS metadata
                  icc:
                    type: object
                    description: ICC profile metadata
                  xmp:
                    type: object
                    description: XMP metadata
        '400':
          description: Invalid input
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Error'
        '401':
          description: Authentication required
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/UnauthorizedError'
        '422':
          description: Failed to read image metadata
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Error'
  /api/v1/tools/image/edit-metadata:
    post:
      operationId: editMetadata
      tags:
        - Tools
      summary: Editar Metadados da Imagem
      description: Edita campos de metadados EXIF, IPTC, GPS e outros de uma imagem usando ExifTool.
      security:
        - bearerAuth: []
      requestBody:
        required: true
        content:
          multipart/form-data:
            schema:
              type: object
              required:
                - file
              properties:
                file:
                  type: string
                  format: binary
                  description: Image file to process
                settings:
                  type: string
                  description: |
                    JSON string with options:
                    - `title` (string) - Image title
                    - `author` (string) - Author name
                    - `artist` (string) - Artist or author name
                    - `copyright` (string) - Copyright notice
                    - `imageDescription` (string) - Image description
                    - `software` (string) - Software used
                    - `dateTime` (string) - Date/time string
                    - `dateTimeOriginal` (string) - Original capture date/time
                    - `clearGps` (boolean, default false) - Remove all GPS data
                    - `fieldsToRemove` (string[]) - Specific metadata fields to remove
                    - `gpsLatitude` (number -90 to 90) - GPS latitude
                    - `gpsLongitude` (number -180 to 180) - GPS longitude
                    - `gpsAltitude` (number) - GPS altitude in meters
                    - `keywords` (string[]) - Keywords or tags
                    - `keywordsMode` (string, default "add") - One of: add, set
                    - `dateShift` (string) - Shift dates by offset, e.g. "+2:00" or "-1:30"
                    - `setAllDates` (string) - Set all date fields to this value
                    - `iptcTitle` (string) - IPTC title
                    - `iptcHeadline` (string) - IPTC headline
                    - `iptcCity` (string) - IPTC city
                    - `iptcState` (string) - IPTC state or province
                    - `iptcCountry` (string) - IPTC country
      responses:
        '200':
          description: Image with updated metadata
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ToolResponse'
        '400':
          description: Invalid input
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Error'
        '401':
          description: Authentication required
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/UnauthorizedError'
  /api/v1/tools/image/edit-metadata/inspect:
    post:
      operationId: inspectMetadataExifTool
      tags:
        - Tools
      summary: Inspecionar metadados (ExifTool)
      description: Lê todos os metadados de uma imagem usando ExifTool. Retorna todos os campos EXIF, IPTC, XMP e GPS incorporados.
      security:
        - bearerAuth: []
      requestBody:
        required: true
        content:
          multipart/form-data:
            schema:
              type: object
              required:
                - file
              properties:
                file:
                  type: string
                  format: binary
                  description: Image file to inspect
      responses:
        '200':
          description: Full metadata object (keys vary by image)
          content:
            application/json:
              schema:
                type: object
                additionalProperties: true
        '400':
          description: Invalid input
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Error'
        '401':
          description: Authentication required
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/UnauthorizedError'
        '422':
          description: Failed to read image metadata
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Error'
  /api/v1/tools/{section}/{toolId}/batch:
    post:
      operationId: batchProcess
      tags:
        - Batch
      summary: Processar imagens em lote
      description: |
        Envia várias imagens por uma única ferramenta. Retorna um arquivo ZIP. A resposta inclui um cabeçalho X-Job-Id para acompanhar o progresso.
      security:
        - bearerAuth: []
      parameters:
        - name: section
          in: path
          required: true
          schema:
            type: string
          description: Tool section slug (image, video, audio, pdf, files)
        - name: toolId
          in: path
          required: true
          schema:
            type: string
          description: Tool identifier (e.g. resize, crop)
      requestBody:
        required: true
        content:
          multipart/form-data:
            schema:
              type: object
              required:
                - file
              properties:
                file:
                  type: array
                  items:
                    type: string
                    format: binary
                  description: Image files to process (one or more)
                settings:
                  type: string
                  description: JSON string of tool settings applied to all images
      responses:
        '200':
          description: ZIP archive of processed images
          headers:
            X-Job-Id:
              schema:
                type: string
              description: Job identifier for progress tracking
          content:
            application/zip:
              schema:
                type: string
                format: binary
        '400':
          description: Invalid input
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Error'
        '401':
          description: Authentication required
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/UnauthorizedError'
  /api/v1/pipeline/execute:
    post:
      operationId: executePipeline
      tags:
        - Pipelines
      summary: Executar um pipeline
      description: Passa uma imagem por uma cadeia de ferramentas. A definição do pipeline é enviada como uma string JSON no campo multipart `pipeline`.
      security:
        - bearerAuth: []
      requestBody:
        required: true
        content:
          multipart/form-data:
            schema:
              type: object
              required:
                - file
                - pipeline
              properties:
                file:
                  type: string
                  format: binary
                  description: Image file to process
                pipeline:
                  type: string
                  description: 'JSON string: { "steps": [{ "toolId": "resize", "settings": {...} }, ...] }'
      responses:
        '200':
          description: Pipeline result
          content:
            application/json:
              schema:
                type: object
                properties:
                  jobId:
                    type: string
                  downloadUrl:
                    type: string
                  originalSize:
                    type: integer
                  processedSize:
                    type: integer
                  stepsCompleted:
                    type: integer
                  steps:
                    type: array
                    items:
                      type: object
        '400':
          description: Invalid input
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Error'
        '401':
          description: Authentication required
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/UnauthorizedError'
        '422':
          description: Processing failed
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Error'
        '501':
          description: Feature not installed
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/FeatureNotInstalledError'
  /api/v1/pipeline/save:
    post:
      operationId: savePipeline
      tags:
        - Pipelines
      summary: Salvar um pipeline
      description: Persiste uma definição de pipeline nomeada com suas etapas de ferramentas para execução posterior. Use isto para salvar fluxos de trabalho reutilizáveis.
      security:
        - bearerAuth: []
      requestBody:
        required: true
        content:
          application/json:
            schema:
              type: object
              required:
                - name
                - steps
              properties:
                name:
                  type: string
                description:
                  type: string
                  description: Optional human-readable description of the pipeline
                steps:
                  type: array
                  items:
                    type: object
                    properties:
                      toolId:
                        type: string
                      settings:
                        type: object
      responses:
        '201':
          description: Pipeline saved
          content:
            application/json:
              schema:
                type: object
                properties:
                  id:
                    type: string
                  name:
                    type: string
                  description:
                    type: string
                    description: Human-readable description of the pipeline
                  steps:
                    type: array
                    items:
                      type: object
                  createdAt:
                    type: string
                    format: date-time
        '400':
          description: Invalid input
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Error'
        '401':
          description: Authentication required
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/UnauthorizedError'
  /api/v1/pipeline/list:
    get:
      operationId: listPipelines
      tags:
        - Pipelines
      summary: Listar pipelines salvos
      description: Recupera todas as definições de pipeline salvas do usuário atual. Retorna nome, etapas e data de criação de cada uma.
      security:
        - bearerAuth: []
      responses:
        '200':
          description: List of pipelines
          content:
            application/json:
              schema:
                type: object
                properties:
                  pipelines:
                    type: array
                    items:
                      type: object
                      properties:
                        id:
                          type: string
                        name:
                          type: string
                        description:
                          type: string
                          description: Human-readable description of the pipeline
                        steps:
                          type: array
                          items:
                            type: object
                        createdAt:
                          type: string
                          format: date-time
        '400':
          description: Invalid input
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Error'
        '401':
          description: Authentication required
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/UnauthorizedError'
  /api/v1/pipeline/{id}:
    delete:
      operationId: deletePipeline
      tags:
        - Pipelines
      summary: Excluir um pipeline
      description: Exclui permanentemente um pipeline salvo pelo seu ID. Apenas o dono do pipeline ou um admin pode excluí-lo.
      security:
        - bearerAuth: []
      parameters:
        - name: id
          in: path
          required: true
          description: Pipeline UUID
          schema:
            type: string
      responses:
        '200':
          description: Pipeline deleted
          content:
            application/json:
              schema:
                type: object
                properties:
                  ok:
                    type: boolean
        '400':
          description: Invalid input
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Error'
        '401':
          description: Authentication required
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/UnauthorizedError'
        '403':
          description: Not authorized to delete this pipeline
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ForbiddenError'
        '404':
          description: Pipeline not found
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Error'
  /api/v1/pipeline/tools:
    get:
      operationId: listPipelineTools
      tags:
        - Pipelines
      summary: Listar ferramentas compatíveis com pipelines
      description: Retorna todos os IDs de ferramentas que podem ser usados como etapas de pipeline.
      security: []
      responses:
        '200':
          description: Tool IDs
          content:
            application/json:
              schema:
                type: object
                properties:
                  toolIds:
                    type: array
                    items:
                      type: string
  /api/v1/pipeline/batch:
    post:
      operationId: batchPipeline
      tags:
        - Pipelines
      summary: Executar pipeline em vários arquivos
      description: Executa um pipeline em várias imagens. Retorna um arquivo ZIP com todos os resultados processados.
      security:
        - bearerAuth: []
      requestBody:
        required: true
        content:
          multipart/form-data:
            schema:
              type: object
              required:
                - file
                - pipeline
              properties:
                file:
                  type: array
                  items:
                    type: string
                    format: binary
                  description: Image files to process
                pipeline:
                  type: string
                  description: |
                    JSON string with pipeline definition:
                    - `steps` (array, required, 1-20 items) - Pipeline steps
                    - `steps[].toolId` (string, required) - Tool ID for this step
                    - `steps[].settings` (object, optional) - Tool-specific settings
                clientJobId:
                  type: string
                  description: Optional client-provided job ID for progress tracking via SSE
      responses:
        '200':
          description: ZIP file with processed images
          headers:
            X-Job-Id:
              schema:
                type: string
              description: Job ID for progress tracking
            X-File-Results:
              schema:
                type: string
              description: Percent-encoded JSON mapping of input index to output filename (decode with decodeURIComponent before JSON.parse)
          content:
            application/zip:
              schema:
                type: string
                format: binary
        '400':
          description: Invalid input
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Error'
        '401':
          description: Authentication required
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/UnauthorizedError'
  /api/v1/upload:
    post:
      operationId: uploadImage
      tags:
        - Files
      summary: Enviar uma imagem
      description: Envia um arquivo de imagem bruto para processamento posterior pelos endpoints de ferramentas. Retorna um jobId e metadados do arquivo.
      security:
        - bearerAuth: []
      requestBody:
        required: true
        content:
          multipart/form-data:
            schema:
              type: object
              required:
                - file
              properties:
                file:
                  type: string
                  format: binary
      responses:
        '200':
          description: Upload result
          content:
            application/json:
              schema:
                type: object
                properties:
                  jobId:
                    type: string
                  files:
                    type: array
                    items:
                      type: object
                      properties:
                        name:
                          type: string
                        size:
                          type: integer
                        format:
                          type: string
        '400':
          description: Invalid input
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Error'
        '401':
          description: Authentication required
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/UnauthorizedError'
  /api/v1/download/{jobId}/{filename}:
    get:
      operationId: downloadProcessedImage
      tags:
        - Files
      summary: Baixar uma imagem processada
      description: Baixa uma imagem processada pelo ID do job e nome do arquivo. Esta é a URL retornada em downloadUrl nas respostas das ferramentas.
      security: []
      parameters:
        - name: jobId
          in: path
          required: true
          description: Job identifier from a tool processing response
          schema:
            type: string
        - name: filename
          in: path
          required: true
          description: Output filename from the processing response
          schema:
            type: string
      responses:
        '200':
          description: Image binary
          content:
            image/*:
              schema:
                type: string
                format: binary
        '404':
          description: File not found
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Error'
  /api/v1/preview:
    post:
      operationId: generatePreview
      tags:
        - Files
      summary: Gerar preview de imagem
      description: Converte qualquer imagem (inclusive HEIC/HEIF) em um preview WebP, redimensionado para caber em 1200x1200 pixels.
      security: []
      requestBody:
        required: true
        content:
          multipart/form-data:
            schema:
              type: object
              required:
                - file
              properties:
                file:
                  type: string
                  format: binary
                  description: Image file to preview
      responses:
        '200':
          description: WebP preview image
          content:
            image/webp:
              schema:
                type: string
                format: binary
        '400':
          description: Invalid input
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Error'
  /api/v1/files:
    get:
      operationId: listFiles
      tags:
        - Files
      summary: Listar arquivos salvos
      description: Retorna a versão mais recente de cada arquivo (uma por cadeia de versões). Suporta paginação e busca.
      security:
        - bearerAuth: []
      parameters:
        - name: limit
          in: query
          description: Maximum number of files to return (default 50)
          schema:
            type: integer
            default: 50
        - name: offset
          in: query
          description: Number of files to skip for pagination (default 0)
          schema:
            type: integer
            default: 0
        - name: search
          in: query
          description: Filter files by original filename (substring match)
          schema:
            type: string
      responses:
        '200':
          description: List of saved files
          content:
            application/json:
              schema:
                type: object
                properties:
                  files:
                    type: array
                    items:
                      type: object
                      properties:
                        id:
                          type: string
                        filename:
                          type: string
                        size:
                          type: integer
                        contentType:
                          type: string
                        createdAt:
                          type: string
                          format: date-time
                  total:
                    type: integer
                    description: Total number of matching files (for pagination)
                  limit:
                    type: integer
                    description: Applied limit value
                  offset:
                    type: integer
                    description: Applied offset value
        '400':
          description: Invalid input
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Error'
        '401':
          description: Authentication required
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/UnauthorizedError'
    delete:
      operationId: bulkDeleteFiles
      tags:
        - Files
      summary: Excluir arquivos salvos em massa
      description: Exclui arquivos e suas cadeias de versões inteiras. Usuários não-admin só podem excluir os próprios arquivos.
      security:
        - bearerAuth: []
      requestBody:
        required: true
        content:
          application/json:
            schema:
              type: object
              required:
                - ids
              properties:
                ids:
                  type: array
                  items:
                    type: string
                  description: File IDs to delete (deletes entire version chain for each)
      responses:
        '200':
          description: Deletion result
          content:
            application/json:
              schema:
                type: object
                properties:
                  deleted:
                    type: integer
                    description: Total number of records deleted across all version chains
        '400':
          description: Invalid input
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Error'
        '401':
          description: Authentication required
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/UnauthorizedError'
  /api/v1/files/upload:
    post:
      operationId: saveFileToLibrary
      tags:
        - Files
      summary: Salvar arquivo na biblioteca
      description: Envia um ou mais arquivos de imagem diretamente para a biblioteca de arquivos para armazenamento persistente e controle de versões.
      security:
        - bearerAuth: []
      requestBody:
        required: true
        content:
          multipart/form-data:
            schema:
              type: object
              required:
                - file
              properties:
                file:
                  type: string
                  format: binary
      responses:
        '201':
          description: Files uploaded
          content:
            application/json:
              schema:
                type: object
                properties:
                  files:
                    type: array
                    items:
                      type: object
                      description: Full file object with all metadata
        '400':
          description: Invalid input
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Error'
        '401':
          description: Authentication required
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/UnauthorizedError'
  /api/v1/files/save-result:
    post:
      operationId: saveProcessingResult
      tags:
        - Files
      summary: Salvar um resultado de processamento na biblioteca
      description: Salva o resultado do processamento de uma ferramenta como uma nova versão de um arquivo existente. Cria uma cadeia de versões.
      security:
        - bearerAuth: []
      requestBody:
        required: true
        content:
          multipart/form-data:
            schema:
              type: object
              required:
                - file
                - parentId
              properties:
                file:
                  type: string
                  format: binary
                  description: The processed image file
                parentId:
                  type: string
                  description: ID of the parent file in the version chain
                toolId:
                  type: string
                  description: Tool that produced this result
      responses:
        '201':
          description: File saved
          content:
            application/json:
              schema:
                type: object
                properties:
                  file:
                    type: object
                    description: Full file object with all metadata
        '400':
          description: Invalid input
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Error'
        '401':
          description: Authentication required
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/UnauthorizedError'
        '404':
          description: Parent file not found
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Error'
  /api/v1/files/{id}:
    get:
      operationId: getFile
      tags:
        - Files
      summary: Obter metadados do arquivo e histórico de versões
      description: Recupera metadados e o histórico completo de versões de um arquivo salvo. Use isto para inspecionar os detalhes do arquivo antes de baixá-lo.
      security:
        - bearerAuth: []
      parameters:
        - name: id
          in: path
          required: true
          description: Saved file UUID
          schema:
            type: string
      responses:
        '200':
          description: File metadata with version history
          content:
            application/json:
              schema:
                type: object
                properties:
                  file:
                    type: object
                    properties:
                      id:
                        type: string
                      originalName:
                        type: string
                      mimeType:
                        type: string
                      size:
                        type: integer
                      width:
                        type: integer
                      height:
                        type: integer
                      version:
                        type: integer
                      parentId:
                        type:
                          - string
                          - 'null'
                      toolChain:
                        type: array
                        items:
                          type: string
                      createdAt:
                        type: string
                        format: date-time
                  versions:
                    type: array
                    items:
                      type: object
                    description: Full version chain of this file
        '401':
          description: Authentication required
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/UnauthorizedError'
        '404':
          description: File not found
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Error'
  /api/v1/files/{id}/download:
    get:
      operationId: downloadSavedFile
      tags:
        - Files
      summary: Baixar um arquivo salvo
      description: Baixa o conteúdo binário de um arquivo salvo da biblioteca pelo seu ID.
      security:
        - bearerAuth: []
      parameters:
        - name: id
          in: path
          required: true
          description: Saved file UUID
          schema:
            type: string
      responses:
        '200':
          description: Image binary
          content:
            image/*:
              schema:
                type: string
                format: binary
        '401':
          description: Authentication required
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/UnauthorizedError'
        '404':
          description: File not found
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Error'
  /api/v1/files/{id}/thumbnail:
    get:
      operationId: getFileThumbnail
      tags:
        - Files
      summary: Obter miniatura do arquivo
      description: Recupera uma miniatura JPEG de 300px de um arquivo salvo. Útil para visualizações em galeria e navegadores de arquivos.
      security:
        - bearerAuth: []
      parameters:
        - name: id
          in: path
          required: true
          description: Saved file UUID
          schema:
            type: string
      responses:
        '200':
          description: 300px JPEG thumbnail
          content:
            image/jpeg:
              schema:
                type: string
                format: binary
        '404':
          description: File not found
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Error'
        '422':
          description: Thumbnail generation failed
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Error'
  /api/v1/fetch-urls:
    post:
      operationId: fetchImageUrls
      tags:
        - Files
      summary: Buscar imagens de URLs
      description: |
        Busca uma ou mais imagens de URLs externas. Cada URL é validada contra ataques
        SSRF, tem tamanho limitado e é verificada como uma imagem válida. Retorna URLs de download para
        cada imagem obtida com sucesso. Limitada a 200 requisições por hora.
      security:
        - bearerAuth: []
      requestBody:
        required: true
        content:
          application/json:
            schema:
              type: object
              required:
                - urls
              properties:
                urls:
                  type: array
                  items:
                    type: string
                    format: uri
                  minItems: 1
                  maxItems: 50
                  description: URLs of images to fetch (1-50)
      responses:
        '200':
          description: Fetch results for each URL
          content:
            application/json:
              schema:
                type: object
                properties:
                  results:
                    type: array
                    items:
                      type: object
                      properties:
                        success:
                          type: boolean
                        url:
                          type: string
                        filename:
                          type: string
                        contentType:
                          type: string
                        size:
                          type: integer
                        width:
                          type: integer
                        height:
                          type: integer
                        downloadUrl:
                          type: string
                        previewUrl:
                          type:
                            - string
                            - 'null'
                        error:
                          type: string
                          description: Present only when success is false
        '400':
          description: Invalid input
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Error'
        '401':
          description: Authentication required
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/UnauthorizedError'
  /api/v1/config/locale:
    get:
      operationId: getLocaleConfig
      tags:
        - System
      summary: Obter locale padrão
      description: Retorna a configuração de locale padrão. Endpoint público.
      security: []
      responses:
        '200':
          description: Default locale
          content:
            application/json:
              schema:
                type: object
                properties:
                  defaultLocale:
                    type: string
                    example: en
  /api/auth/login:
    post:
      operationId: login
      tags:
        - Auth
      summary: Fazer login
      description: Autentica com nome de usuário e senha. Retorna um bearer token para requisições subsequentes da API.
      security: []
      requestBody:
        required: true
        content:
          application/json:
            schema:
              type: object
              required:
                - username
                - password
              properties:
                username:
                  type: string
                password:
                  type: string
                  format: password
      responses:
        '200':
          description: Login successful
          content:
            application/json:
              schema:
                type: object
                properties:
                  token:
                    type: string
                  user:
                    type: object
                    properties:
                      id:
                        type: string
                      username:
                        type: string
                      role:
                        type: string
                      mustChangePassword:
                        type: boolean
                      permissions:
                        type: array
                        items:
                          type: string
                      teamName:
                        type: string
                  expiresAt:
                    type: string
                    format: date-time
        '400':
          description: Invalid input
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Error'
        '401':
          description: Invalid credentials
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/UnauthorizedError'
  /api/auth/logout:
    post:
      operationId: logout
      tags:
        - Auth
      summary: Fazer logout
      description: Invalida o token de sessão atual. O token não pode ser usado em novas requisições após o logout.
      security:
        - bearerAuth: []
      responses:
        '200':
          description: Logged out
          content:
            application/json:
              schema:
                type: object
                properties:
                  ok:
                    type: boolean
        '401':
          description: Authentication required
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/UnauthorizedError'
  /api/auth/session:
    get:
      operationId: getSession
      tags:
        - Auth
      summary: Obter sessão atual
      description: Recupera os detalhes da sessão atual, incluindo perfil do usuário, função e permissões. Retorna usuário nulo quando não autenticado.
      security: []
      responses:
        '200':
          description: Session info
          content:
            application/json:
              schema:
                type: object
                properties:
                  user:
                    type: object
                    properties:
                      id:
                        type: string
                      username:
                        type: string
                      role:
                        type: string
                      mustChangePassword:
                        type: boolean
                      permissions:
                        type: array
                        items:
                          type: string
                  expiresAt:
                    type:
                      - string
                      - 'null'
                    format: date-time
  /api/auth/change-password:
    post:
      operationId: changePassword
      tags:
        - Auth
      summary: Alterar senha
      description: Altera a senha do usuário autenticado. Requer a senha atual para verificação.
      security:
        - bearerAuth: []
      requestBody:
        required: true
        content:
          application/json:
            schema:
              type: object
              required:
                - currentPassword
                - newPassword
              properties:
                currentPassword:
                  type: string
                  format: password
                newPassword:
                  type: string
                  format: password
      responses:
        '200':
          description: Password changed
          content:
            application/json:
              schema:
                type: object
                properties:
                  ok:
                    type: boolean
        '400':
          description: Invalid input
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Error'
        '401':
          description: Authentication required
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/UnauthorizedError'
  /api/auth/users:
    get:
      operationId: listUsers
      tags:
        - Auth
      summary: Listar usuários
      description: Requer a permissão users:manage.
      security:
        - bearerAuth: []
      responses:
        '200':
          description: List of users
          content:
            application/json:
              schema:
                type: object
                properties:
                  users:
                    type: array
                    items:
                      type: object
                      properties:
                        id:
                          type: string
                        username:
                          type: string
                        role:
                          type: string
                        team:
                          type: string
                        createdAt:
                          type: string
                          format: date-time
                  maxUsers:
                    type: integer
        '401':
          description: Authentication required
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/UnauthorizedError'
        '403':
          description: Insufficient permissions
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ForbiddenError'
  /api/auth/register:
    post:
      operationId: createUser
      tags:
        - Auth
      summary: Criar usuário
      description: |
        Requires users:manage permission. The assigned role must be within the actor's effective role authority, including hierarchy, permissions, tool access, and any API-key scope.
      security:
        - bearerAuth: []
      requestBody:
        required: true
        content:
          application/json:
            schema:
              type: object
              required:
                - username
                - password
              properties:
                username:
                  type: string
                password:
                  type: string
                  format: password
                role:
                  type: string
                  description: Built-in role (admin, editor, user) or custom role name
                  default: user
                team:
                  type: string
                  default: Default
      responses:
        '201':
          description: User created
          content:
            application/json:
              schema:
                type: object
                properties:
                  id:
                    type: string
                  username:
                    type: string
                  role:
                    type: string
                  team:
                    type: string
        '400':
          description: Invalid input
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Error'
        '401':
          description: Authentication required
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/UnauthorizedError'
        '403':
          description: Insufficient permissions
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ForbiddenError'
        '409':
          description: Username already exists
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ConflictError'
  /api/auth/users/{id}:
    put:
      operationId: updateUser
      tags:
        - Auth
      summary: Atualizar usuário
      description: |
        Requires users:manage permission and authority over the target's current role. Any newly assigned role must also be within the actor's effective hierarchy, permissions, tool access, and API-key scope.
      security:
        - bearerAuth: []
      parameters:
        - name: id
          in: path
          required: true
          description: User UUID
          schema:
            type: string
      requestBody:
        required: true
        content:
          application/json:
            schema:
              type: object
              properties:
                role:
                  type: string
                  description: Built-in role (admin, editor, user) or custom role name
                team:
                  type: string
      responses:
        '200':
          description: User updated
          content:
            application/json:
              schema:
                type: object
                properties:
                  ok:
                    type: boolean
        '400':
          description: Invalid input
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Error'
        '401':
          description: Authentication required
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/UnauthorizedError'
        '403':
          description: Insufficient permissions
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ForbiddenError'
    delete:
      operationId: deleteUser
      tags:
        - Auth
      summary: Excluir usuário
      description: |
        Requires users:manage permission and authority over the target's current role, including hierarchy, permissions, tool access, and any API-key scope. Self-deletion is not allowed.
      security:
        - bearerAuth: []
      parameters:
        - name: id
          in: path
          required: true
          description: User UUID
          schema:
            type: string
      responses:
        '200':
          description: User deleted
          content:
            application/json:
              schema:
                type: object
                properties:
                  ok:
                    type: boolean
        '401':
          description: Authentication required
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/UnauthorizedError'
        '403':
          description: Insufficient permissions
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ForbiddenError'
  /api/auth/users/{id}/reset-password:
    post:
      operationId: resetUserPassword
      tags:
        - Auth
      summary: Redefinir senha do usuário
      description: |
        Reset a local user's password. Requires users:manage permission and authority over the target's current role, including hierarchy, permissions, tool access, and any API-key scope.
      security:
        - bearerAuth: []
      parameters:
        - name: id
          in: path
          required: true
          description: User UUID
          schema:
            type: string
      requestBody:
        required: true
        content:
          application/json:
            schema:
              type: object
              required:
                - newPassword
              properties:
                newPassword:
                  type: string
                  format: password
      responses:
        '200':
          description: Password reset
          content:
            application/json:
              schema:
                type: object
                properties:
                  ok:
                    type: boolean
        '400':
          description: Invalid input
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Error'
        '401':
          description: Authentication required
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/UnauthorizedError'
        '403':
          description: Insufficient permissions
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ForbiddenError'
  /api/v1/api-keys:
    post:
      operationId: createApiKey
      tags:
        - API Keys
      summary: Criar uma API key
      description: |
        Gera uma nova API key. A key completa (com o prefixo si_) é retornada apenas uma vez. As keys são armazenadas com hashing scrypt.
      security:
        - bearerAuth: []
      requestBody:
        required: true
        content:
          application/json:
            schema:
              type: object
              properties:
                name:
                  type: string
                  maxLength: 100
                  default: Default API Key
                permissions:
                  type: array
                  items:
                    type: string
                  description: Scoped permissions for this key
                expiresAt:
                  type: string
                  format: date-time
                  description: Optional expiration date
      responses:
        '201':
          description: API key created
          content:
            application/json:
              schema:
                type: object
                properties:
                  id:
                    type: string
                  key:
                    type: string
                    description: Full key (only shown once)
                  name:
                    type: string
                  permissions:
                    type:
                      - array
                      - 'null'
                    items:
                      type: string
                  expiresAt:
                    type:
                      - string
                      - 'null'
                    format: date-time
                  createdAt:
                    type: string
                    format: date-time
        '400':
          description: Invalid input
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Error'
        '401':
          description: Authentication required
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/UnauthorizedError'
    get:
      operationId: listApiKeys
      tags:
        - API Keys
      summary: Listar API keys
      description: Retorna os metadados da key, mas não a key completa.
      security:
        - bearerAuth: []
      responses:
        '200':
          description: List of API keys
          content:
            application/json:
              schema:
                type: object
                properties:
                  apiKeys:
                    type: array
                    items:
                      type: object
                      properties:
                        id:
                          type: string
                        name:
                          type: string
                        permissions:
                          type:
                            - array
                            - 'null'
                          items:
                            type: string
                        createdAt:
                          type: string
                          format: date-time
                        lastUsedAt:
                          type:
                            - string
                            - 'null'
                          format: date-time
                        expiresAt:
                          type:
                            - string
                            - 'null'
                          format: date-time
        '401':
          description: Authentication required
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/UnauthorizedError'
  /api/v1/api-keys/{id}:
    delete:
      operationId: deleteApiKey
      tags:
        - API Keys
      summary: Excluir uma API key
      description: Revoga e exclui permanentemente uma API key. A key não pode ser usada para autenticação após a exclusão.
      security:
        - bearerAuth: []
      parameters:
        - name: id
          in: path
          required: true
          description: API key UUID
          schema:
            type: string
      responses:
        '200':
          description: API key deleted
          content:
            application/json:
              schema:
                type: object
                properties:
                  ok:
                    type: boolean
        '401':
          description: Authentication required
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/UnauthorizedError'
  /api/v1/settings:
    get:
      operationId: getSettings
      tags:
        - Settings
      summary: Obter todas as configurações
      description: Recupera todas as configurações do sistema como pares chave-valor. Requer autenticação.
      security:
        - bearerAuth: []
      responses:
        '200':
          description: All settings wrapped in a settings object
          content:
            application/json:
              schema:
                type: object
                properties:
                  settings:
                    type: object
                    additionalProperties:
                      type: string
        '401':
          description: Authentication required
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/UnauthorizedError'
    put:
      operationId: updateSettings
      tags:
        - Settings
      summary: Atualizar configurações
      description: Aceita um objeto JSON plano de pares chave-valor a definir. Requer a função admin.
      security:
        - bearerAuth: []
      requestBody:
        required: true
        content:
          application/json:
            schema:
              type: object
              additionalProperties:
                type: string
      responses:
        '200':
          description: Settings updated
          content:
            application/json:
              schema:
                type: object
                properties:
                  ok:
                    type: boolean
                  updatedCount:
                    type: integer
        '400':
          description: Invalid input
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Error'
        '401':
          description: Authentication required
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/UnauthorizedError'
        '403':
          description: Admin access required
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ForbiddenError'
  /api/v1/settings/{key}:
    get:
      operationId: getSetting
      tags:
        - Settings
      summary: Obter uma única configuração
      description: Recupera o valor de uma única configuração pelo nome da chave. Retorna o valor e o timestamp da última atualização.
      security:
        - bearerAuth: []
      parameters:
        - name: key
          in: path
          required: true
          description: Setting key name
          schema:
            type: string
      responses:
        '200':
          description: Setting value
          content:
            application/json:
              schema:
                type: object
                properties:
                  key:
                    type: string
                  value:
                    type: string
                  updatedAt:
                    type: string
                    format: date-time
        '401':
          description: Authentication required
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/UnauthorizedError'
        '404':
          description: Setting not found
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Error'
  /api/v1/teams:
    get:
      operationId: listTeams
      tags:
        - Teams
      summary: Listar equipes
      description: Requer a permissão teams:manage.
      security:
        - bearerAuth: []
      responses:
        '200':
          description: List of teams
          content:
            application/json:
              schema:
                type: object
                properties:
                  teams:
                    type: array
                    items:
                      type: object
                      properties:
                        id:
                          type: string
                        name:
                          type: string
                        memberCount:
                          type: integer
                        createdAt:
                          type: string
                          format: date-time
        '401':
          description: Authentication required
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/UnauthorizedError'
        '403':
          description: Insufficient permissions
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ForbiddenError'
    post:
      operationId: createTeam
      tags:
        - Teams
      summary: Criar uma equipe
      description: Requer a permissão teams:manage.
      security:
        - bearerAuth: []
      requestBody:
        required: true
        content:
          application/json:
            schema:
              type: object
              required:
                - name
              properties:
                name:
                  type: string
      responses:
        '201':
          description: Team created
          content:
            application/json:
              schema:
                type: object
                properties:
                  id:
                    type: string
                  name:
                    type: string
        '400':
          description: Invalid input
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Error'
        '401':
          description: Authentication required
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/UnauthorizedError'
        '403':
          description: Insufficient permissions
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ForbiddenError'
        '409':
          description: Team name already exists
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ConflictError'
  /api/v1/teams/{id}:
    put:
      operationId: updateTeam
      tags:
        - Teams
      summary: Renomear uma equipe
      description: Requer a permissão teams:manage.
      security:
        - bearerAuth: []
      parameters:
        - name: id
          in: path
          required: true
          description: Team UUID
          schema:
            type: string
      requestBody:
        required: true
        content:
          application/json:
            schema:
              type: object
              required:
                - name
              properties:
                name:
                  type: string
      responses:
        '200':
          description: Team updated
          content:
            application/json:
              schema:
                type: object
                properties:
                  ok:
                    type: boolean
        '401':
          description: Authentication required
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/UnauthorizedError'
        '403':
          description: Insufficient permissions
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ForbiddenError'
        '404':
          description: Team not found
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Error'
        '409':
          description: Team name already exists
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ConflictError'
    delete:
      operationId: deleteTeam
      tags:
        - Teams
      summary: Excluir uma equipe
      description: Requer a permissão teams:manage.
      security:
        - bearerAuth: []
      parameters:
        - name: id
          in: path
          required: true
          description: Team UUID
          schema:
            type: string
      responses:
        '200':
          description: Team deleted
          content:
            application/json:
              schema:
                type: object
                properties:
                  ok:
                    type: boolean
        '401':
          description: Authentication required
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/UnauthorizedError'
        '403':
          description: Insufficient permissions
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ForbiddenError'
        '404':
          description: Team not found
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Error'
  /api/v1/config/auth:
    get:
      operationId: getAuthConfig
      tags:
        - System
      summary: Configuração de autenticação
      description: Verifica se a autenticação está habilitada nesta instância. Endpoint público usado pelo frontend para determinar o fluxo de login.
      security: []
      responses:
        '200':
          description: Auth config
          content:
            application/json:
              schema:
                type: object
                properties:
                  authEnabled:
                    type: boolean
  /api/v1/jobs/{jobId}/progress:
    get:
      operationId: getJobProgress
      tags:
        - System
      summary: Progresso do job (SSE)
      description: Stream de Server-Sent Events para acompanhar tarefas de longa duração. Envia frames de arquivo único, lote e heartbeat e fecha imediatamente após um evento terminal da tarefa.
      security: []
      parameters:
        - name: jobId
          in: path
          required: true
          description: Job UUID from a tool processing or installation response
          schema:
            type: string
      responses:
        '200':
          description: |
            SSE stream of job progress frames. Single-file frames use `phase`, `percent`, optional `stage`, and terminal `result` or `error` fields. Batch frames use `status`, file counters, and `errors`. Heartbeat frames keep idle connections alive.
          content:
            text/event-stream:
              schema:
                oneOf:
                  - title: Single-file progress frame
                    type: object
                    required:
                      - jobId
                      - type
                      - phase
                      - percent
                    properties:
                      jobId:
                        type: string
                      type:
                        type: string
                        const: single
                      phase:
                        type: string
                        enum:
                          - processing
                          - complete
                          - failed
                      stage:
                        type: string
                      percent:
                        type: number
                        minimum: 0
                        maximum: 100
                      error:
                        type: string
                        description: Present on terminal failed frames
                      result:
                        type: object
                        additionalProperties: true
                        description: Present on terminal complete frames; contains the tool result
                  - title: Batch progress frame
                    type: object
                    required:
                      - jobId
                      - type
                      - status
                      - totalFiles
                      - completedFiles
                      - failedFiles
                      - errors
                    properties:
                      jobId:
                        type: string
                      type:
                        type: string
                        const: batch
                      status:
                        type: string
                        enum:
                          - processing
                          - completed
                          - failed
                      totalFiles:
                        type: integer
                        minimum: 0
                      completedFiles:
                        type: integer
                        minimum: 0
                      failedFiles:
                        type: integer
                        minimum: 0
                      currentFile:
                        type: string
                      errors:
                        type: array
                        items:
                          type: object
                          required:
                            - filename
                            - error
                          properties:
                            filename:
                              type: string
                            error:
                              type: string
                  - title: Heartbeat frame
                    type: object
                    required:
                      - type
                    properties:
                      type:
                        type: string
                        const: heartbeat
  /api/v1/admin/health:
    get:
      operationId: adminHealthCheck
      tags:
        - Admin
      summary: Verificação de saúde de admin
      description: |
        Diagnóstico completo do sistema, incluindo status do banco de dados, modo de armazenamento, estado
        da fila e disponibilidade de GPU. Requer a permissão system:health.
      security:
        - bearerAuth: []
      responses:
        '200':
          description: Detailed system health
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/AdminHealthResponse'
        '401':
          description: Authentication required
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/UnauthorizedError'
        '403':
          description: Insufficient permissions
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ForbiddenError'
  /api/v1/config/analytics:
    get:
      operationId: getAnalyticsConfig
      tags:
        - Analytics
      summary: Obter configuração de analytics
      description: |
        Retorna a configuração efetiva do provedor de analytics. O analytics é
        habilitado apenas quando tanto o bake em tempo de compilação quanto a configuração
        de instância analyticsEnabled o permitem. Quando desabilitado, as keys, o DSN e o
        instanceId ficam em branco. Endpoint público.
      security: []
      responses:
        '200':
          description: Analytics configuration
          content:
            application/json:
              schema:
                type: object
                properties:
                  enabled:
                    type: boolean
                    description: |
                      Effective analytics state. True only when both the
                      compile-time bake and the instance analyticsEnabled
                      setting are on. When false, the keys, DSN, and instanceId
                      are blank.
                  posthogApiKey:
                    type: string
                  posthogHost:
                    type: string
                  sentryDsn:
                    type: string
                  sentryDsnWeb:
                    type: string
                  posthogSampleRate:
                    type: number
                  instanceId:
                    type: string
  /api/v1/features:
    get:
      operationId: listFeatures
      tags:
        - Features
      summary: Listar bundles de recursos
      description: |
        Retorna todos os bundles de recursos de IA disponíveis e seu status de instalação.
        Em ambientes não-Docker, todos os bundles aparecem como instalados.
      security:
        - bearerAuth: []
      responses:
        '200':
          description: Feature bundle list
          content:
            application/json:
              schema:
                type: object
                properties:
                  bundles:
                    type: array
                    items:
                      type: object
                      properties:
                        id:
                          type: string
                        name:
                          type: string
                        description:
                          type: string
                          description: Human-readable description of the feature bundle
                        status:
                          type: string
                          enum:
                            - installed
                            - not_installed
                            - installing
                            - queued
                            - error
                        installedVersion:
                          type:
                            - string
                            - 'null'
                        estimatedSize:
                          type: string
                        downloadBytes:
                          type:
                            - integer
                            - 'null'
                          description: Compressed archive size for this host's architecture, when the manifest records it. Null in native (non-Docker) mode or when unmeasured.
                        installedBytes:
                          type:
                            - integer
                            - 'null'
                          description: Extracted on-disk size for this host's architecture, when the manifest records it.
                        compatibility:
                          type: string
                          enum:
                            - compatible
                            - incompatible
                            - invalid
                          description: OCR runtime compatibility; omitted for legacy bundles
                        compatibilityReason:
                          type:
                            - string
                            - 'null'
                          description: OCR runtime compatibility reason; omitted for legacy bundles
                        selectedTarget:
                          type:
                            - string
                            - 'null'
                          description: Selected OCR runtime target, such as linux-amd64-cpu-py312
                        missingDownloadBytes:
                          type:
                            - integer
                            - 'null'
                          description: Remaining OCR runtime download size for this host
                        requiredMemoryBytes:
                          type:
                            - integer
                            - 'null'
                          description: Minimum effective memory required by the selected OCR runtime; null for legacy bundles
                        effectiveMemoryBytes:
                          type:
                            - integer
                            - 'null'
                          description: Effective memory available to OCR (the configured container cgroup limit, otherwise host memory); null for legacy bundles
                        healthyGeneration:
                          type:
                            - string
                            - 'null'
                          description: Active immutable OCR generation identifier
                        availableQualities:
                          type: array
                          description: OCR quality tiers currently available on this host
                          items:
                            type: string
                            enum:
                              - fast
                              - balanced
                              - best
                        enablesTools:
                          type: array
                          items:
                            type: string
                        progress:
                          type:
                            - object
                            - 'null'
                          properties:
                            percent:
                              type: number
                            stage:
                              type: string
                        error:
                          type:
                            - string
                            - 'null'
        '401':
          description: Authentication required
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/UnauthorizedError'
  /api/v1/admin/features/{bundleId}/install:
    post:
      operationId: installFeature
      tags:
        - Features
      summary: Instalar um bundle de recurso
      description: |-
        Inicie a instalação assíncrona de um pacote de recursos de IA. Baixa modelos e
        configura o Python sidecar. Retorna um jobId para acompanhamento do progresso
        por meio do terminal SSE. Requer permissão features:manage.

        Para bundleId=ocr, a instalação é suportada nos contêineres oficiais Linux amd64 e arm64.
        Ele baixa o destino selecionado para este host, verifica a versão
        assinatura Ed25519 do índice e o artifact/modelo resume, faz testes de fumaça e
        geração imutável encenada e a ativa atomicamente. O OCR
        o tempo de execução usa CPU mesmo quando o contêiner tem acesso NVIDIA GPU. O
        o tempo de execução preciso requer pelo menos 4 GiB de memória efetiva (o
        limite cgroup do contêiner configurado, caso contrário, memória do host). Sistemas abaixo daquele assinado
        mínimo de compatibilidade são rejeitados antes do download com
        compatibilityReason=insufficient-memory. Fast Tesseract OCR não é
        sujeito a esta exigência.

        As instalações são serializadas no lado do servidor: uma solicitação feita enquanto outra
        a instalação está em execução está na fila (queued=true) em vez de ser rejeitada e
        inicia automaticamente quando a instalação em execução termina. Uma repetição
        solicitação para o mesmo pacote em trânsito é desduplicada e retorna o mesmo
        jobId.
      security:
        - bearerAuth: []
      parameters:
        - name: bundleId
          in: path
          required: true
          description: Feature bundle identifier
          schema:
            type: string
      responses:
        '202':
          description: Installation started or queued
          content:
            application/json:
              schema:
                type: object
                properties:
                  jobId:
                    type: string
                    description: Job ID for tracking installation progress via SSE
                  queued:
                    type: boolean
                    description: True when the install was queued behind another running install instead of starting immediately.
        '401':
          description: Authentication required
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/UnauthorizedError'
        '403':
          description: Insufficient permissions
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ForbiddenError'
        '404':
          description: Unknown bundle ID
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Error'
        '409':
          description: Bundle already installed or OCR runtime unsupported on this host
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ConflictError'
        '503':
          description: OCR runtime trust key is not configured in this image
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Error'
  /api/v1/admin/features/{bundleId}/uninstall:
    post:
      operationId: uninstallFeature
      tags:
        - Features
      summary: Desinstalar um bundle de recurso
      description: |
        Desinstala um bundle de recurso de IA e exclui seus arquivos de modelo (a menos que sejam compartilhados
        com outro bundle instalado). Requer a permissão features:manage.
      security:
        - bearerAuth: []
      parameters:
        - name: bundleId
          in: path
          required: true
          description: Feature bundle identifier
          schema:
            type: string
      responses:
        '200':
          description: Bundle uninstalled
          content:
            application/json:
              schema:
                type: object
                properties:
                  ok:
                    type: boolean
        '401':
          description: Authentication required
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/UnauthorizedError'
        '403':
          description: Insufficient permissions
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ForbiddenError'
        '404':
          description: Unknown bundle ID
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Error'
        '409':
          description: Bundle is not installed
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ConflictError'
  /api/v1/admin/features/disk-usage:
    get:
      operationId: getFeatureDiskUsage
      tags:
        - Features
      summary: Uso de disco dos modelos de IA
      description: |
        Retorna o espaço total em disco usado pelos arquivos de modelo de IA.
        Requer a permissão features:manage.
      security:
        - bearerAuth: []
      responses:
        '200':
          description: Disk usage
          content:
            application/json:
              schema:
                type: object
                properties:
                  totalBytes:
                    type: integer
                    description: Total bytes used by AI model files
        '401':
          description: Authentication required
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/UnauthorizedError'
        '403':
          description: Insufficient permissions
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ForbiddenError'
  /api/v1/audit-log:
    get:
      operationId: queryAuditLog
      tags:
        - Audit
      summary: Consultar log de auditoria
      description: |
        Log de auditoria paginado de ações administrativas. Suporta filtro por
        tipo de ação e intervalo de datas. Requer a permissão audit:read.
      security:
        - bearerAuth: []
      parameters:
        - name: page
          in: query
          description: Page number (default 1)
          schema:
            type: integer
            minimum: 1
            default: 1
        - name: limit
          in: query
          description: Results per page (1-100, default 50)
          schema:
            type: integer
            minimum: 1
            maximum: 100
            default: 50
        - name: action
          in: query
          description: Filter by action type
          schema:
            type: string
        - name: from
          in: query
          description: Filter entries after this ISO 8601 date
          schema:
            type: string
            format: date-time
        - name: to
          in: query
          description: Filter entries before this ISO 8601 date
          schema:
            type: string
            format: date-time
      responses:
        '200':
          description: Paginated audit log entries
          content:
            application/json:
              schema:
                type: object
                properties:
                  entries:
                    type: array
                    items:
                      type: object
                      properties:
                        id:
                          type: string
                        actorId:
                          type:
                            - string
                            - 'null'
                        actorUsername:
                          type: string
                        action:
                          type: string
                        targetType:
                          type:
                            - string
                            - 'null'
                        targetId:
                          type:
                            - string
                            - 'null'
                        details:
                          type:
                            - object
                            - 'null'
                        ipAddress:
                          type:
                            - string
                            - 'null'
                        requestId:
                          type:
                            - string
                            - 'null'
                        createdAt:
                          type: string
                          format: date-time
                  total:
                    type: integer
                    description: Total matching entries
                  page:
                    type: integer
                  limit:
                    type: integer
        '401':
          description: Authentication required
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/UnauthorizedError'
        '403':
          description: Insufficient permissions
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ForbiddenError'
  /api/v1/roles:
    get:
      operationId: listRoles
      tags:
        - Roles
      summary: Listar funções
      description: |
        Lista todas as funções (integradas e personalizadas) com suas permissões e
        contagens de usuários. Requer a permissão audit:read.
      security:
        - bearerAuth: []
      responses:
        '200':
          description: Role list
          content:
            application/json:
              schema:
                type: object
                properties:
                  roles:
                    type: array
                    items:
                      type: object
                      properties:
                        id:
                          type: string
                        name:
                          type: string
                        description:
                          type: string
                          description: Human-readable description of the role
                        permissions:
                          type: array
                          items:
                            type: string
                        isBuiltin:
                          type: boolean
                        userCount:
                          type: integer
                        createdAt:
                          type: string
                          format: date-time
                        updatedAt:
                          type: string
                          format: date-time
        '401':
          description: Authentication required
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/UnauthorizedError'
        '403':
          description: Insufficient permissions
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ForbiddenError'
    post:
      operationId: createRole
      tags:
        - Roles
      summary: Criar uma função personalizada
      description: |
        Create a new custom role with specified permissions. Role names must be
        2-30 characters, lowercase alphanumeric with hyphens and underscores.
        Requires security:manage permission. The new role's permissions and tool
        access must be contained by the actor's effective authority.
      security:
        - bearerAuth: []
      requestBody:
        required: true
        content:
          application/json:
            schema:
              type: object
              required:
                - name
                - permissions
              properties:
                name:
                  type: string
                  minLength: 2
                  maxLength: 30
                  pattern: ^[a-z0-9_-]+$
                  description: Lowercase role name
                description:
                  type: string
                  description: Human-readable description
                permissions:
                  type: array
                  items:
                    type: string
                  description: |
                    Permission strings. Valid values: tools:use, files:own, files:all,
                    apikeys:own, apikeys:all, pipelines:own, pipelines:all,
                    settings:read, settings:write, users:manage, teams:manage,
                    features:manage, system:health, audit:read, compliance:manage,
                    webhooks:manage, security:manage
      responses:
        '201':
          description: Role created
          content:
            application/json:
              schema:
                type: object
                properties:
                  id:
                    type: string
                  name:
                    type: string
                  description:
                    type: string
                    description: Human-readable description of the role
                  permissions:
                    type: array
                    items:
                      type: string
                  isBuiltin:
                    type: boolean
        '400':
          description: Validation error
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Error'
        '401':
          description: Authentication required
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/UnauthorizedError'
        '403':
          description: Insufficient permissions
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ForbiddenError'
        '409':
          description: Role name already exists
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ConflictError'
  /api/v1/roles/{id}:
    put:
      operationId: updateRole
      tags:
        - Roles
      summary: Atualizar uma função personalizada
      description: |
        Update name, description, or permissions of a custom role. Built-in
        roles cannot be modified. Requires security:manage permission and
        authority over the current role; the resulting permissions and tool
        access must remain contained by the actor's effective authority.
      security:
        - bearerAuth: []
      parameters:
        - name: id
          in: path
          required: true
          description: Role UUID
          schema:
            type: string
      requestBody:
        required: true
        content:
          application/json:
            schema:
              type: object
              properties:
                name:
                  type: string
                  minLength: 2
                  maxLength: 30
                description:
                  type: string
                  description: Updated description for the role
                permissions:
                  type: array
                  items:
                    type: string
      responses:
        '200':
          description: Role updated
          content:
            application/json:
              schema:
                type: object
                properties:
                  ok:
                    type: boolean
        '400':
          description: Validation error or cannot modify built-in role
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Error'
        '401':
          description: Authentication required
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/UnauthorizedError'
        '403':
          description: Insufficient permissions
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ForbiddenError'
        '404':
          description: Role not found
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Error'
        '409':
          description: Role name already exists
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ConflictError'
    delete:
      operationId: deleteRole
      tags:
        - Roles
      summary: Excluir uma função personalizada
      description: |
        Delete a custom role. Users assigned to this role are reassigned to the
        default "user" role. Built-in roles cannot be deleted. Requires
        security:manage permission, authority over the current role, and
        authority to assign the fallback role.
      security:
        - bearerAuth: []
      parameters:
        - name: id
          in: path
          required: true
          description: Role UUID
          schema:
            type: string
      responses:
        '200':
          description: Role deleted
          content:
            application/json:
              schema:
                type: object
                properties:
                  ok:
                    type: boolean
        '400':
          description: Cannot delete built-in role
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Error'
        '401':
          description: Authentication required
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/UnauthorizedError'
        '403':
          description: Insufficient permissions
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ForbiddenError'
        '404':
          description: Role not found
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Error'
  /api/v1/tools/video/convert-video:
    post:
      operationId: convertVideo
      tags:
        - Tools
      summary: Converter Vídeo
      description: Converte vídeos entre MP4, MOV, WebM, AVI e MKV.
      security:
        - bearerAuth: []
      requestBody:
        required: true
        content:
          multipart/form-data:
            schema:
              type: object
              required:
                - file
              properties:
                file:
                  type: string
                  format: binary
                  description: Video file to process
                settings:
                  type: string
                  description: |
                    JSON string with options:
                    - `format` (string, default "mp4") - One of: mp4, mov, webm, avi, mkv
                    - `quality` (string, default "balanced") - One of: high, balanced, small
                clientJobId:
                  type: string
                  description: Client-provided job ID for SSE progress tracking
      responses:
        '202':
          description: Accepted for async processing. Track progress via SSE at /api/v1/jobs/{jobId}/progress.
          content:
            application/json:
              schema:
                type: object
                properties:
                  jobId:
                    type: string
                  async:
                    type: boolean
                    example: true
        '400':
          description: Invalid input
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Error'
        '401':
          description: Authentication required
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/UnauthorizedError'
        '422':
          description: Processing failed
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Error'
  /api/v1/tools/video/compress-video:
    post:
      operationId: compressVideo
      tags:
        - Tools
      summary: Comprimir Vídeo
      description: Reduz o tamanho do arquivo de vídeo com controle de qualidade.
      security:
        - bearerAuth: []
      requestBody:
        required: true
        content:
          multipart/form-data:
            schema:
              type: object
              required:
                - file
              properties:
                file:
                  type: string
                  format: binary
                  description: Video file to process
                settings:
                  type: string
                  description: |
                    JSON string with options:
                    - `quality` (string, default "balanced") - One of: light, balanced, strong
                    - `resolution` (string, default "original") - One of: original, 1080p, 720p, 480p
                clientJobId:
                  type: string
                  description: Client-provided job ID for SSE progress tracking
      responses:
        '202':
          description: Accepted for async processing. Track progress via SSE at /api/v1/jobs/{jobId}/progress.
          content:
            application/json:
              schema:
                type: object
                properties:
                  jobId:
                    type: string
                  async:
                    type: boolean
                    example: true
        '400':
          description: Invalid input
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Error'
        '401':
          description: Authentication required
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/UnauthorizedError'
        '422':
          description: Processing failed
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Error'
  /api/v1/tools/video/trim-video:
    post:
      operationId: trimVideo
      tags:
        - Tools
      summary: Cortar Vídeo
      description: Extrai um trecho de um vídeo.
      security:
        - bearerAuth: []
      requestBody:
        required: true
        content:
          multipart/form-data:
            schema:
              type: object
              required:
                - file
              properties:
                file:
                  type: string
                  format: binary
                  description: Video file to process
                settings:
                  type: string
                  description: |
                    JSON string with options:
                    - `startS` (number >= 0, default 0) - Start time in seconds
                    - `endS` (number, required) - End time in seconds (must be after startS)
                    - `precise` (boolean, default false) - Re-encode for frame-accurate cuts instead of keyframe seek
      responses:
        '200':
          description: Processed video
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ToolResponse'
        '400':
          description: Invalid input
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Error'
        '401':
          description: Authentication required
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/UnauthorizedError'
  /api/v1/tools/video/mute-video:
    post:
      operationId: muteVideo
      tags:
        - Tools
      summary: Silenciar Vídeo
      description: Remove a trilha de áudio de um vídeo.
      security:
        - bearerAuth: []
      requestBody:
        required: true
        content:
          multipart/form-data:
            schema:
              type: object
              required:
                - file
              properties:
                file:
                  type: string
                  format: binary
                  description: Video file to process
      responses:
        '200':
          description: Processed video
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ToolResponse'
        '400':
          description: Invalid input
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Error'
        '401':
          description: Authentication required
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/UnauthorizedError'
  /api/v1/tools/video/video-to-gif:
    post:
      operationId: videoToGif
      tags:
        - Tools
      summary: Vídeo para GIF
      description: Transforma um trecho de vídeo em um GIF animado.
      security:
        - bearerAuth: []
      requestBody:
        required: true
        content:
          multipart/form-data:
            schema:
              type: object
              required:
                - file
              properties:
                file:
                  type: string
                  format: binary
                  description: Video file to process
                settings:
                  type: string
                  description: |
                    JSON string with options:
                    - `fps` (integer 1-30, default 12) - Output frame rate
                    - `width` (integer 64-1280, default 480) - Output width in pixels
                    - `startS` (number >= 0, default 0) - Start time in seconds
                    - `durationS` (number 0-60, default 5) - Duration in seconds
                clientJobId:
                  type: string
                  description: Client-provided job ID for SSE progress tracking
      responses:
        '202':
          description: Accepted for async processing. Track progress via SSE at /api/v1/jobs/{jobId}/progress.
          content:
            application/json:
              schema:
                type: object
                properties:
                  jobId:
                    type: string
                  async:
                    type: boolean
                    example: true
        '400':
          description: Invalid input
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Error'
        '401':
          description: Authentication required
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/UnauthorizedError'
        '422':
          description: Processing failed
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Error'
  /api/v1/tools/video/resize-video:
    post:
      operationId: resizeVideo
      tags:
        - Tools
      summary: Redimensionar Vídeo
      description: Escala um vídeo para uma nova resolução ou tamanho predefinido.
      security:
        - bearerAuth: []
      requestBody:
        required: true
        content:
          multipart/form-data:
            schema:
              type: object
              required:
                - file
              properties:
                file:
                  type: string
                  format: binary
                  description: Video file to process
                settings:
                  type: string
                  description: |
                    JSON string with options:
                    - `width` (integer 16-7680, optional) - Target width in pixels
                    - `height` (integer 16-4320, optional) - Target height in pixels
                    - `preset` (string, default "custom") - One of: custom, 2160p, 1440p, 1080p, 720p, 480p, 360p. When "custom", at least width or height is required
      responses:
        '200':
          description: Processed video
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ToolResponse'
        '400':
          description: Invalid input
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Error'
        '401':
          description: Authentication required
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/UnauthorizedError'
  /api/v1/tools/video/crop-video:
    post:
      operationId: cropVideo
      tags:
        - Tools
      summary: Recortar Vídeo
      description: Recorta uma região de um vídeo.
      security:
        - bearerAuth: []
      requestBody:
        required: true
        content:
          multipart/form-data:
            schema:
              type: object
              required:
                - file
              properties:
                file:
                  type: string
                  format: binary
                  description: Video file to process
                settings:
                  type: string
                  description: |
                    JSON string with options:
                    - `width` (integer >= 16, required) - Crop region width in pixels
                    - `height` (integer >= 16, required) - Crop region height in pixels
                    - `x` (integer >= 0, default 0) - Horizontal offset from top-left
                    - `y` (integer >= 0, default 0) - Vertical offset from top-left
      responses:
        '200':
          description: Processed video
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ToolResponse'
        '400':
          description: Invalid input
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Error'
        '401':
          description: Authentication required
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/UnauthorizedError'
  /api/v1/tools/video/rotate-video:
    post:
      operationId: rotateVideo
      tags:
        - Tools
      summary: Girar Vídeo
      description: Gira ou espelha um vídeo.
      security:
        - bearerAuth: []
      requestBody:
        required: true
        content:
          multipart/form-data:
            schema:
              type: object
              required:
                - file
              properties:
                file:
                  type: string
                  format: binary
                  description: Video file to process
                settings:
                  type: string
                  description: |
                    JSON string with options:
                    - `transform` (string, required) - One of: cw90, ccw90, 180, hflip, vflip
      responses:
        '200':
          description: Processed video
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ToolResponse'
        '400':
          description: Invalid input
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Error'
        '401':
          description: Authentication required
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/UnauthorizedError'
  /api/v1/tools/video/change-fps:
    post:
      operationId: changeFps
      tags:
        - Tools
      summary: Alterar FPS
      description: Altera a taxa de quadros de um vídeo.
      security:
        - bearerAuth: []
      requestBody:
        required: true
        content:
          multipart/form-data:
            schema:
              type: object
              required:
                - file
              properties:
                file:
                  type: string
                  format: binary
                  description: Video file to process
                settings:
                  type: string
                  description: |
                    JSON string with options:
                    - `fps` (number 1-120, default 30) - Target frame rate
      responses:
        '200':
          description: Processed video
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ToolResponse'
        '400':
          description: Invalid input
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Error'
        '401':
          description: Authentication required
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/UnauthorizedError'
  /api/v1/tools/video/video-color:
    post:
      operationId: videoColor
      tags:
        - Tools
      summary: Cor do Vídeo
      description: Ajusta brilho, contraste, saturação e gama.
      security:
        - bearerAuth: []
      requestBody:
        required: true
        content:
          multipart/form-data:
            schema:
              type: object
              required:
                - file
              properties:
                file:
                  type: string
                  format: binary
                  description: Video file to process
                settings:
                  type: string
                  description: |
                    JSON string with options:
                    - `brightness` (number -1 to 1, default 0) - Brightness adjustment
                    - `contrast` (number 0-4, default 1) - Contrast multiplier
                    - `saturation` (number 0-3, default 1) - Saturation multiplier
                    - `gamma` (number 0.1-10, default 1) - Gamma correction
      responses:
        '200':
          description: Processed video
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ToolResponse'
        '400':
          description: Invalid input
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Error'
        '401':
          description: Authentication required
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/UnauthorizedError'
  /api/v1/tools/video/video-speed:
    post:
      operationId: videoSpeed
      tags:
        - Tools
      summary: Velocidade do Vídeo
      description: Acelera ou desacelera um vídeo.
      security:
        - bearerAuth: []
      requestBody:
        required: true
        content:
          multipart/form-data:
            schema:
              type: object
              required:
                - file
              properties:
                file:
                  type: string
                  format: binary
                  description: Video file to process
                settings:
                  type: string
                  description: |
                    JSON string with options:
                    - `factor` (number 0.25-4, default 2) - Speed multiplier (2 = double speed, 0.5 = half speed)
                    - `keepPitch` (boolean, default true) - Preserve audio pitch when changing speed
      responses:
        '200':
          description: Processed video
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ToolResponse'
        '400':
          description: Invalid input
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Error'
        '401':
          description: Authentication required
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/UnauthorizedError'
  /api/v1/tools/video/reverse-video:
    post:
      operationId: reverseVideo
      tags:
        - Tools
      summary: Inverter Vídeo
      description: Reproduz um trecho de vídeo de trás para frente. Limitado a trechos de até 5 minutos.
      security:
        - bearerAuth: []
      requestBody:
        required: true
        content:
          multipart/form-data:
            schema:
              type: object
              required:
                - file
              properties:
                file:
                  type: string
                  format: binary
                  description: Video file to process
      responses:
        '200':
          description: Processed video
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ToolResponse'
        '400':
          description: Invalid input
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Error'
        '401':
          description: Authentication required
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/UnauthorizedError'
  /api/v1/tools/video/video-loudnorm:
    post:
      operationId: videoLoudnorm
      tags:
        - Tools
      summary: Normalizar Áudio do Vídeo
      description: Normaliza o volume do áudio do vídeo para o padrão de transmissão (EBU R128).
      security:
        - bearerAuth: []
      requestBody:
        required: true
        content:
          multipart/form-data:
            schema:
              type: object
              required:
                - file
              properties:
                file:
                  type: string
                  format: binary
                  description: Video file to process
      responses:
        '200':
          description: Processed video
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ToolResponse'
        '400':
          description: Invalid input
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Error'
        '401':
          description: Authentication required
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/UnauthorizedError'
  /api/v1/tools/video/aspect-pad:
    post:
      operationId: aspectPad
      tags:
        - Tools
      summary: Preenchimento de Proporção
      description: Adiciona barras de cor sólida para se ajustar a uma proporção-alvo.
      security:
        - bearerAuth: []
      requestBody:
        required: true
        content:
          multipart/form-data:
            schema:
              type: object
              required:
                - file
              properties:
                file:
                  type: string
                  format: binary
                  description: Video file to process
                settings:
                  type: string
                  description: |
                    JSON string with options:
                    - `target` (string, default "9:16") - One of: 16:9, 9:16, 1:1, 4:3, 3:4
                    - `color` (string, default "#000000") - Hex color for padding bars (e.g. "#000000")
      responses:
        '200':
          description: Processed video
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ToolResponse'
        '400':
          description: Invalid input
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Error'
        '401':
          description: Authentication required
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/UnauthorizedError'
  /api/v1/tools/video/blur-pad:
    post:
      operationId: blurPad
      tags:
        - Tools
      summary: Preenchimento com Desfoque
      description: Preenche as barras com uma cópia desfocada do vídeo.
      security:
        - bearerAuth: []
      requestBody:
        required: true
        content:
          multipart/form-data:
            schema:
              type: object
              required:
                - file
              properties:
                file:
                  type: string
                  format: binary
                  description: Video file to process
                settings:
                  type: string
                  description: |
                    JSON string with options:
                    - `target` (string, default "16:9") - One of: 16:9, 9:16, 1:1, 4:3, 3:4
                    - `blur` (number 2-50, default 20) - Gaussian blur sigma for the background
      responses:
        '200':
          description: Processed video
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ToolResponse'
        '400':
          description: Invalid input
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Error'
        '401':
          description: Authentication required
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/UnauthorizedError'
  /api/v1/tools/video/watermark-video:
    post:
      operationId: watermarkVideo
      tags:
        - Tools
      summary: Marca d'água em Vídeo
      description: Grava uma marca d'água de texto nos quadros do vídeo.
      security:
        - bearerAuth: []
      requestBody:
        required: true
        content:
          multipart/form-data:
            schema:
              type: object
              required:
                - file
              properties:
                file:
                  type: string
                  format: binary
                  description: Video file to process
                settings:
                  type: string
                  description: |
                    JSON string with options:
                    - `text` (string, required) - Watermark text (1-200 characters)
                    - `position` (string, default "br") - One of: tl, tc, tr, l, c, r, bl, bc, br
                    - `fontSize` (integer 8-120, default 36) - Font size in pixels
                    - `opacity` (number 0.05-1, default 0.5) - Watermark opacity
                    - `color` (string, default "#ffffff") - Hex color for the text (e.g. "#ffffff")
      responses:
        '200':
          description: Processed video
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ToolResponse'
        '400':
          description: Invalid input
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Error'
        '401':
          description: Authentication required
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/UnauthorizedError'
  /api/v1/tools/video/stabilize-video:
    post:
      operationId: stabilizeVideo
      tags:
        - Tools
      summary: Estabilizar Vídeo
      description: Reduz o tremor da câmera com estabilização em duas passagens.
      security:
        - bearerAuth: []
      requestBody:
        required: true
        content:
          multipart/form-data:
            schema:
              type: object
              required:
                - file
              properties:
                file:
                  type: string
                  format: binary
                  description: Video file to process
                settings:
                  type: string
                  description: |
                    JSON string with options:
                    - `smoothing` (integer 5-60, default 15) - Smoothing window size in frames
                clientJobId:
                  type: string
                  description: Client-provided job ID for SSE progress tracking
      responses:
        '202':
          description: Accepted for async processing. Track progress via SSE at /api/v1/jobs/{jobId}/progress.
          content:
            application/json:
              schema:
                type: object
                properties:
                  jobId:
                    type: string
                  async:
                    type: boolean
                    example: true
        '400':
          description: Invalid input
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Error'
        '401':
          description: Authentication required
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/UnauthorizedError'
        '422':
          description: Processing failed
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Error'
  /api/v1/tools/video/gif-to-video:
    post:
      operationId: gifToVideo
      tags:
        - Tools
      summary: GIF para Vídeo
      description: Converte um GIF animado em um vídeo MP4, WebM ou MOV.
      security:
        - bearerAuth: []
      requestBody:
        required: true
        content:
          multipart/form-data:
            schema:
              type: object
              required:
                - file
              properties:
                file:
                  type: string
                  format: binary
                  description: GIF file to convert
                settings:
                  type: string
                  description: |
                    JSON string with options:
                    - `format` (string, default "mp4") - One of: mp4, webm, mov
      responses:
        '200':
          description: Converted video
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ToolResponse'
        '400':
          description: Invalid input
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Error'
        '401':
          description: Authentication required
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/UnauthorizedError'
  /api/v1/tools/video/video-to-webp:
    post:
      operationId: videoToWebp
      tags:
        - Tools
      summary: Vídeo para WebP
      description: Converte um trecho de vídeo em uma imagem WebP animada.
      security:
        - bearerAuth: []
      requestBody:
        required: true
        content:
          multipart/form-data:
            schema:
              type: object
              required:
                - file
              properties:
                file:
                  type: string
                  format: binary
                  description: Video file to process
                settings:
                  type: string
                  description: |
                    JSON string with options:
                    - `fps` (integer 1-30, default 12) - Output frame rate
                    - `width` (integer 16-1920, default 480) - Output width in pixels
                    - `quality` (integer 1-100, default 75) - WebP compression quality
                    - `loop` (boolean, default true) - Loop the animation
      responses:
        '200':
          description: Converted animated WebP
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ToolResponse'
        '400':
          description: Invalid input
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Error'
        '401':
          description: Authentication required
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/UnauthorizedError'
  /api/v1/tools/video/video-to-frames:
    post:
      operationId: videoToFrames
      tags:
        - Tools
      summary: Vídeo para Quadros
      description: Extrai quadros de um vídeo como um ZIP de imagens.
      security:
        - bearerAuth: []
      requestBody:
        required: true
        content:
          multipart/form-data:
            schema:
              type: object
              required:
                - file
              properties:
                file:
                  type: string
                  format: binary
                  description: Video file to process
                settings:
                  type: string
                  description: |
                    JSON string with options:
                    - `mode` (string, default "all") - One of: all, nth, timestamps
                    - `n` (integer 2-1000, default 10) - Extract every Nth frame (used when mode is "nth")
                    - `timestamps` (string, default "") - Comma-separated timestamps in seconds (required when mode is "timestamps")
                    - `format` (string, default "png") - One of: png, jpg
      responses:
        '200':
          description: ZIP archive containing extracted frames
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ToolResponse'
        '400':
          description: Invalid input
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Error'
        '401':
          description: Authentication required
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/UnauthorizedError'
  /api/v1/tools/video/merge-videos:
    post:
      operationId: mergeVideos
      tags:
        - Tools
      summary: Mesclar Vídeos
      description: |
        Une vários trechos de vídeo em um único arquivo MP4. Todas as entradas são
        normalizadas para a resolução do primeiro vídeo e 30 fps.
      security:
        - bearerAuth: []
      requestBody:
        required: true
        content:
          multipart/form-data:
            schema:
              type: object
              required:
                - file
              properties:
                file:
                  type: array
                  items:
                    type: string
                    format: binary
                  description: Two or more video files to merge (up to 10)
                clientJobId:
                  type: string
                  description: Client-provided job ID for SSE progress tracking
      responses:
        '202':
          description: Accepted for async processing. Track progress via SSE at /api/v1/jobs/{jobId}/progress.
          content:
            application/json:
              schema:
                type: object
                properties:
                  jobId:
                    type: string
                  async:
                    type: boolean
                    example: true
        '400':
          description: Invalid input
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Error'
        '401':
          description: Authentication required
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/UnauthorizedError'
        '422':
          description: Processing failed
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Error'
  /api/v1/tools/video/replace-audio:
    post:
      operationId: replaceAudio
      tags:
        - Tools
      summary: Substituir Áudio
      description: Troca a trilha de áudio de um vídeo por outro arquivo.
      security:
        - bearerAuth: []
      requestBody:
        required: true
        content:
          multipart/form-data:
            schema:
              type: object
              required:
                - file
              properties:
                file:
                  type: array
                  items:
                    type: string
                    format: binary
                  description: A video file followed by an audio file (exactly 2 files)
      responses:
        '200':
          description: Processed video
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ToolResponse'
        '400':
          description: Invalid input
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Error'
        '401':
          description: Authentication required
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/UnauthorizedError'
  /api/v1/tools/video/burn-subtitles:
    post:
      operationId: burnSubtitles
      tags:
        - Tools
      summary: Gravar Legendas
      description: Renderiza legendas permanentemente nos quadros do vídeo.
      security:
        - bearerAuth: []
      requestBody:
        required: true
        content:
          multipart/form-data:
            schema:
              type: object
              required:
                - file
              properties:
                file:
                  type: array
                  items:
                    type: string
                    format: binary
                  description: A video file followed by a subtitle file (.srt, .vtt, .ass)
                settings:
                  type: string
                  description: |
                    JSON string with options:
                    - `fontSize` (integer 8-72, default 24) - Subtitle font size in pixels
                clientJobId:
                  type: string
                  description: Client-provided job ID for SSE progress tracking
      responses:
        '202':
          description: Accepted for async processing. Track progress via SSE at /api/v1/jobs/{jobId}/progress.
          content:
            application/json:
              schema:
                type: object
                properties:
                  jobId:
                    type: string
                  async:
                    type: boolean
                    example: true
        '400':
          description: Invalid input
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Error'
        '401':
          description: Authentication required
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/UnauthorizedError'
        '422':
          description: Processing failed
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Error'
  /api/v1/tools/video/embed-subtitles:
    post:
      operationId: embedSubtitles
      tags:
        - Tools
      summary: Incorporar Legendas
      description: Faz mux de uma trilha de legendas no contêiner do vídeo.
      security:
        - bearerAuth: []
      requestBody:
        required: true
        content:
          multipart/form-data:
            schema:
              type: object
              required:
                - file
              properties:
                file:
                  type: array
                  items:
                    type: string
                    format: binary
                  description: A video file followed by a subtitle file (.srt, .vtt, .ass)
                settings:
                  type: string
                  description: |
                    JSON string with options:
                    - `language` (string, default "eng") - ISO 639-2/B language code (3 lowercase letters, e.g. "eng", "fra", "deu")
      responses:
        '200':
          description: Processed video
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ToolResponse'
        '400':
          description: Invalid input
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Error'
        '401':
          description: Authentication required
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/UnauthorizedError'
  /api/v1/tools/video/extract-subtitles:
    post:
      operationId: extractSubtitles
      tags:
        - Tools
      summary: Extrair Legendas
      description: Extrai a trilha de legendas de um vídeo como um arquivo SRT.
      security:
        - bearerAuth: []
      requestBody:
        required: true
        content:
          multipart/form-data:
            schema:
              type: object
              required:
                - file
              properties:
                file:
                  type: string
                  format: binary
                  description: Video file with an embedded subtitle track
      responses:
        '200':
          description: Extracted subtitle file
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ToolResponse'
        '400':
          description: Invalid input
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Error'
        '401':
          description: Authentication required
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/UnauthorizedError'
  /api/v1/tools/video/images-to-video:
    post:
      operationId: imagesToVideo
      tags:
        - Tools
      summary: Imagens para Vídeo
      description: Transforma um conjunto de imagens em um vídeo de slideshow.
      security:
        - bearerAuth: []
      requestBody:
        required: true
        content:
          multipart/form-data:
            schema:
              type: object
              required:
                - file
              properties:
                file:
                  type: array
                  items:
                    type: string
                    format: binary
                  description: Two or more image files (up to 60)
                settings:
                  type: string
                  description: |
                    JSON string with options:
                    - `secondsPerImage` (number 0.5-10, default 2) - Display duration per image in seconds
                    - `resolution` (string, default "720p") - One of: 1080p, 720p, square
                    - `fps` (integer 10-60, default 30) - Output frame rate
      responses:
        '200':
          description: Generated slideshow video
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ToolResponse'
        '400':
          description: Invalid input
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Error'
        '401':
          description: Authentication required
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/UnauthorizedError'
  /api/v1/tools/video/video-metadata:
    post:
      operationId: videoMetadata
      tags:
        - Tools
      summary: Limpar Metadados de Vídeo
      description: Remove metadados de um vídeo e informa o que foi encontrado.
      security:
        - bearerAuth: []
      requestBody:
        required: true
        content:
          multipart/form-data:
            schema:
              type: object
              required:
                - file
              properties:
                file:
                  type: string
                  format: binary
                  description: Video file to process
      responses:
        '200':
          description: Processed video with metadata report
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ToolResponse'
        '400':
          description: Invalid input
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Error'
        '401':
          description: Authentication required
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/UnauthorizedError'
  /api/v1/tools/video/auto-subtitles:
    post:
      operationId: autoSubtitles
      tags:
        - Tools
      summary: Legendas Automáticas
      description: |
        Geração de legendas com IA a partir das trilhas de áudio do vídeo usando
        faster-whisper. Suporta detecção automática e 10 idiomas explícitos.
        Requer o bundle de recurso transcription instalado.
      security:
        - bearerAuth: []
      requestBody:
        required: true
        content:
          multipart/form-data:
            schema:
              type: object
              required:
                - file
              properties:
                file:
                  type: string
                  format: binary
                  description: Video file to transcribe
                settings:
                  type: string
                  description: |
                    JSON string with options:
                    - `language` (string, default "auto") - One of: auto, en, de, fr, es, zh, ja, ko, id, th, vi
                    - `format` (string, default "srt") - One of: srt, vtt
                clientJobId:
                  type: string
                  description: Client-provided job ID for SSE progress tracking
      responses:
        '202':
          description: Accepted for async processing. Track progress via SSE at /api/v1/jobs/{jobId}/progress.
          content:
            application/json:
              schema:
                type: object
                properties:
                  jobId:
                    type: string
                  async:
                    type: boolean
                    example: true
        '400':
          description: Invalid input
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Error'
        '401':
          description: Authentication required
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/UnauthorizedError'
        '422':
          description: Processing failed
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Error'
        '501':
          description: Feature not installed
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/FeatureNotInstalledError'
  /api/v1/tools/video/extract-audio:
    post:
      operationId: extractAudio
      tags:
        - Tools
      summary: Extrair Áudio
      description: Extrai a trilha de áudio de um vídeo.
      security:
        - bearerAuth: []
      requestBody:
        required: true
        content:
          multipart/form-data:
            schema:
              type: object
              required:
                - file
              properties:
                file:
                  type: string
                  format: binary
                  description: Video file to extract audio from
                settings:
                  type: string
                  description: |
                    JSON string with options:
                    - `format` (string, default "mp3") - One of: mp3, wav, m4a, ogg
      responses:
        '200':
          description: Extracted audio file
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ToolResponse'
        '400':
          description: Invalid input
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Error'
        '401':
          description: Authentication required
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/UnauthorizedError'
  /api/v1/tools/audio/convert-audio:
    post:
      operationId: convertAudio
      tags:
        - Tools
      summary: Converter Áudio
      description: Converte áudio entre MP3, WAV, OGG e outros.
      security:
        - bearerAuth: []
      requestBody:
        required: true
        content:
          multipart/form-data:
            schema:
              type: object
              required:
                - file
              properties:
                file:
                  type: string
                  format: binary
                  description: Audio file to process
                settings:
                  type: string
                  description: |
                    JSON string with options:
                    - `format` (string, default "mp3") - One of: mp3, wav, ogg, flac, m4a
                    - `bitrateKbps` (integer 32-320, default 192) - Output bitrate in kbps
      responses:
        '200':
          description: Processed audio
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ToolResponse'
        '400':
          description: Invalid input
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Error'
        '401':
          description: Authentication required
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/UnauthorizedError'
  /api/v1/tools/audio/trim-audio:
    post:
      operationId: trimAudio
      tags:
        - Tools
      summary: Cortar Áudio
      description: Extrai um trecho de um arquivo de áudio.
      security:
        - bearerAuth: []
      requestBody:
        required: true
        content:
          multipart/form-data:
            schema:
              type: object
              required:
                - file
              properties:
                file:
                  type: string
                  format: binary
                  description: Audio file to process
                settings:
                  type: string
                  description: |
                    JSON string with options:
                    - `startS` (number, min 0, default 0) - Start time in seconds
                    - `endS` (number, required) - End time in seconds (must be after start)
      responses:
        '200':
          description: Processed audio
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ToolResponse'
        '400':
          description: Invalid input
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Error'
        '401':
          description: Authentication required
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/UnauthorizedError'
  /api/v1/tools/audio/volume-adjust:
    post:
      operationId: volumeAdjust
      tags:
        - Tools
      summary: Ajustar Volume
      description: Aumenta ou diminui o volume do áudio por um ganho fixo.
      security:
        - bearerAuth: []
      requestBody:
        required: true
        content:
          multipart/form-data:
            schema:
              type: object
              required:
                - file
              properties:
                file:
                  type: string
                  format: binary
                  description: Audio file to process
                settings:
                  type: string
                  description: |
                    JSON string with options:
                    - `gainDb` (number -30 to 30, default 3) - Volume adjustment in decibels
      responses:
        '200':
          description: Processed audio
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ToolResponse'
        '400':
          description: Invalid input
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Error'
        '401':
          description: Authentication required
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/UnauthorizedError'
  /api/v1/tools/audio/normalize-audio:
    post:
      operationId: normalizeAudio
      tags:
        - Tools
      summary: Normalizar Áudio
      description: Uniformiza a intensidade sonora para os níveis do padrão de transmissão (EBU R128, -16 LUFS).
      security:
        - bearerAuth: []
      requestBody:
        required: true
        content:
          multipart/form-data:
            schema:
              type: object
              required:
                - file
              properties:
                file:
                  type: string
                  format: binary
                  description: Audio file to process
      responses:
        '200':
          description: Processed audio
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ToolResponse'
        '400':
          description: Invalid input
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Error'
        '401':
          description: Authentication required
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/UnauthorizedError'
  /api/v1/tools/audio/fade-audio:
    post:
      operationId: fadeAudio
      tags:
        - Tools
      summary: Fade de Áudio
      description: Adiciona efeitos de fade-in e fade-out ao áudio.
      security:
        - bearerAuth: []
      requestBody:
        required: true
        content:
          multipart/form-data:
            schema:
              type: object
              required:
                - file
              properties:
                file:
                  type: string
                  format: binary
                  description: Audio file to process
                settings:
                  type: string
                  description: |
                    JSON string with options:
                    - `fadeInS` (number 0-30, default 1) - Fade-in duration in seconds
                    - `fadeOutS` (number 0-30, default 1) - Fade-out duration in seconds
      responses:
        '200':
          description: Processed audio
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ToolResponse'
        '400':
          description: Invalid input
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Error'
        '401':
          description: Authentication required
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/UnauthorizedError'
  /api/v1/tools/audio/reverse-audio:
    post:
      operationId: reverseAudio
      tags:
        - Tools
      summary: Inverter Áudio
      description: Reproduz o áudio de trás para frente.
      security:
        - bearerAuth: []
      requestBody:
        required: true
        content:
          multipart/form-data:
            schema:
              type: object
              required:
                - file
              properties:
                file:
                  type: string
                  format: binary
                  description: Audio file to process
      responses:
        '200':
          description: Processed audio
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ToolResponse'
        '400':
          description: Invalid input
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Error'
        '401':
          description: Authentication required
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/UnauthorizedError'
  /api/v1/tools/audio/audio-speed:
    post:
      operationId: audioSpeed
      tags:
        - Tools
      summary: Velocidade do Áudio
      description: Acelera ou desacelera a reprodução do áudio.
      security:
        - bearerAuth: []
      requestBody:
        required: true
        content:
          multipart/form-data:
            schema:
              type: object
              required:
                - file
              properties:
                file:
                  type: string
                  format: binary
                  description: Audio file to process
                settings:
                  type: string
                  description: |
                    JSON string with options:
                    - `factor` (number 0.25-4, default 1.5) - Speed multiplier (0.25 = quarter speed, 4 = quadruple speed)
      responses:
        '200':
          description: Processed audio
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ToolResponse'
        '400':
          description: Invalid input
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Error'
        '401':
          description: Authentication required
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/UnauthorizedError'
  /api/v1/tools/audio/pitch-shift:
    post:
      operationId: pitchShift
      tags:
        - Tools
      summary: Alteração de Tom
      description: Sobe ou desce o tom em semitons sem alterar a velocidade.
      security:
        - bearerAuth: []
      requestBody:
        required: true
        content:
          multipart/form-data:
            schema:
              type: object
              required:
                - file
              properties:
                file:
                  type: string
                  format: binary
                  description: Audio file to process
                settings:
                  type: string
                  description: |
                    JSON string with options:
                    - `semitones` (integer -12 to 12, default 3) - Semitones to shift (must be nonzero)
      responses:
        '200':
          description: Processed audio
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ToolResponse'
        '400':
          description: Invalid input
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Error'
        '401':
          description: Authentication required
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/UnauthorizedError'
  /api/v1/tools/audio/audio-channels:
    post:
      operationId: audioChannels
      tags:
        - Tools
      summary: Canais de Áudio
      description: Converte entre mono e estéreo, ou troca os canais esquerdo e direito.
      security:
        - bearerAuth: []
      requestBody:
        required: true
        content:
          multipart/form-data:
            schema:
              type: object
              required:
                - file
              properties:
                file:
                  type: string
                  format: binary
                  description: Audio file to process
                settings:
                  type: string
                  description: |
                    JSON string with options:
                    - `mode` (string, required) - One of: stereo-to-mono, mono-to-stereo, swap
      responses:
        '200':
          description: Processed audio
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ToolResponse'
        '400':
          description: Invalid input
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Error'
        '401':
          description: Authentication required
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/UnauthorizedError'
  /api/v1/tools/audio/silence-removal:
    post:
      operationId: removeSilence
      tags:
        - Tools
      summary: Remoção de Silêncio
      description: Remove trechos de silêncio do áudio.
      security:
        - bearerAuth: []
      requestBody:
        required: true
        content:
          multipart/form-data:
            schema:
              type: object
              required:
                - file
              properties:
                file:
                  type: string
                  format: binary
                  description: Audio file to process
                settings:
                  type: string
                  description: |
                    JSON string with options:
                    - `thresholdDb` (number -80 to -20, default -50) - Silence threshold in dB
                    - `minSilenceS` (number 0.1-5, default 0.5) - Minimum silence duration in seconds to remove
      responses:
        '200':
          description: Processed audio
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ToolResponse'
        '400':
          description: Invalid input
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Error'
        '401':
          description: Authentication required
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/UnauthorizedError'
  /api/v1/tools/audio/noise-reduction:
    post:
      operationId: reduceNoise
      tags:
        - Tools
      summary: Redução de Ruído
      description: Reduz o ruído de fundo com denoising baseado em FFT.
      security:
        - bearerAuth: []
      requestBody:
        required: true
        content:
          multipart/form-data:
            schema:
              type: object
              required:
                - file
              properties:
                file:
                  type: string
                  format: binary
                  description: Audio file to process
                settings:
                  type: string
                  description: |
                    JSON string with options:
                    - `strength` (string, default "medium") - One of: light, medium, strong
      responses:
        '200':
          description: Processed audio
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ToolResponse'
        '400':
          description: Invalid input
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Error'
        '401':
          description: Authentication required
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/UnauthorizedError'
  /api/v1/tools/audio/merge-audio:
    post:
      operationId: mergeAudio
      tags:
        - Tools
      summary: Mesclar Áudio
      description: Combina vários arquivos de áudio em uma única trilha sequencial.
      security:
        - bearerAuth: []
      requestBody:
        required: true
        content:
          multipart/form-data:
            schema:
              type: object
              required:
                - file
              properties:
                file:
                  type: array
                  items:
                    type: string
                    format: binary
                  description: Two or more audio files to merge (max 10)
                settings:
                  type: string
                  description: |
                    JSON string with options:
                    - `format` (string, default "mp3") - One of: mp3, wav, flac, m4a
      responses:
        '200':
          description: Processed audio
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ToolResponse'
        '400':
          description: Invalid input
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Error'
        '401':
          description: Authentication required
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/UnauthorizedError'
  /api/v1/tools/audio/split-audio:
    post:
      operationId: splitAudio
      tags:
        - Tools
      summary: Dividir Áudio
      description: |
        Divide o áudio por tempo, em partes iguais ou por detecção de silêncio. Retorna um
        ToolResponse com um downloadUrl apontando para um arquivo ZIP de segmentos.
      security:
        - bearerAuth: []
      requestBody:
        required: true
        content:
          multipart/form-data:
            schema:
              type: object
              required:
                - file
              properties:
                file:
                  type: string
                  format: binary
                  description: Audio file to split
                settings:
                  type: string
                  description: |
                    JSON string with options:
                    - `mode` (string, default "time") - One of: time, parts, silence
                    - `segmentS` (number 1-3600, default 60) - Segment length in seconds (mode=time)
                    - `parts` (integer 2-20, default 2) - Number of equal parts (mode=parts)
                    - `thresholdDb` (number -80 to -20, default -40) - Silence threshold in dB (mode=silence)
                    - `minSilenceS` (number 0.1-10, default 0.3) - Minimum silence gap in seconds (mode=silence)
      responses:
        '200':
          description: Processed audio (downloadUrl points to a ZIP archive)
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ToolResponse'
        '400':
          description: Invalid input
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Error'
        '401':
          description: Authentication required
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/UnauthorizedError'
  /api/v1/tools/audio/ringtone-maker:
    post:
      operationId: ringtoneMaker
      tags:
        - Tools
      summary: Criador de Toques
      description: Cria um trecho de toque (.m4r) a partir de qualquer arquivo de áudio.
      security:
        - bearerAuth: []
      requestBody:
        required: true
        content:
          multipart/form-data:
            schema:
              type: object
              required:
                - file
              properties:
                file:
                  type: string
                  format: binary
                  description: Audio file to process
                settings:
                  type: string
                  description: |
                    JSON string with options:
                    - `startS` (number, min 0, default 0) - Start time in seconds
                    - `durationS` (number 1-30, default 30) - Clip duration in seconds
      responses:
        '200':
          description: Processed audio
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ToolResponse'
        '400':
          description: Invalid input
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Error'
        '401':
          description: Authentication required
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/UnauthorizedError'
  /api/v1/tools/audio/waveform-image:
    post:
      operationId: waveformImage
      tags:
        - Tools
      summary: Imagem de Forma de Onda
      description: Gera uma visualização de forma de onda como imagem PNG a partir de um arquivo de áudio.
      security:
        - bearerAuth: []
      requestBody:
        required: true
        content:
          multipart/form-data:
            schema:
              type: object
              required:
                - file
              properties:
                file:
                  type: string
                  format: binary
                  description: Audio file to process
                settings:
                  type: string
                  description: |
                    JSON string with options:
                    - `width` (integer 256-3840, default 1024) - Image width in pixels
                    - `height` (integer 64-1080, default 256) - Image height in pixels
                    - `color` (string, default "#4f46e5") - Waveform hex color (e.g. "#4f46e5")
      responses:
        '200':
          description: Processed image
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ToolResponse'
        '400':
          description: Invalid input
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Error'
        '401':
          description: Authentication required
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/UnauthorizedError'
  /api/v1/tools/audio/audio-metadata:
    post:
      operationId: audioMetadata
      tags:
        - Tools
      summary: Metadados de Áudio
      description: |
        Visualize, edite ou remova tags de áudio (ID3). A resposta inclui um
        objeto `metadata` com container, durationS, bitrateKbps e tags
        junto aos campos padrão do ToolResponse.
      security:
        - bearerAuth: []
      requestBody:
        required: true
        content:
          multipart/form-data:
            schema:
              type: object
              required:
                - file
              properties:
                file:
                  type: string
                  format: binary
                  description: Audio file to process
                settings:
                  type: string
                  description: |
                    JSON string with options:
                    - `strip` (boolean, default false) - Remove all existing metadata
                    - `title` (string, optional, max 500) - Set the title tag
                    - `artist` (string, optional, max 500) - Set the artist tag
                    - `album` (string, optional, max 500) - Set the album tag
      responses:
        '200':
          description: Processed audio with metadata
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ToolResponse'
        '400':
          description: Invalid input
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Error'
        '401':
          description: Authentication required
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/UnauthorizedError'
  /api/v1/tools/audio/transcribe-audio:
    post:
      operationId: transcribeAudio
      tags:
        - Tools
      summary: Transcrever Áudio
      description: |
        Transcrição de fala para texto com IA usando faster-whisper. Gera
        texto simples ou formatos de legenda SRT ou VTT. Requer o bundle de
        recurso transcription instalado.
      security:
        - bearerAuth: []
      requestBody:
        required: true
        content:
          multipart/form-data:
            schema:
              type: object
              required:
                - file
              properties:
                file:
                  type: string
                  format: binary
                  description: Audio file to transcribe
                settings:
                  type: string
                  description: |
                    JSON string with options:
                    - `language` (string, default "auto") - One of: auto, en, de, fr, es, zh, ja, ko, id, th, vi
                    - `outputFormat` (string, default "txt") - One of: txt, srt, vtt
                clientJobId:
                  type: string
                  description: Client-provided job ID for SSE progress tracking
      responses:
        '202':
          description: Accepted for async processing. Track progress via SSE at /api/v1/jobs/{jobId}/progress.
          content:
            application/json:
              schema:
                type: object
                properties:
                  jobId:
                    type: string
                  async:
                    type: boolean
                    example: true
        '400':
          description: Invalid input
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Error'
        '401':
          description: Authentication required
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/UnauthorizedError'
        '422':
          description: Processing failed
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Error'
        '501':
          description: Feature not installed
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/FeatureNotInstalledError'
  /api/v1/tools/pdf/merge-pdf:
    post:
      operationId: mergePdf
      tags:
        - Tools
      summary: Mesclar PDFs
      description: Combina vários PDFs em um só. Requer pelo menos dois arquivos.
      security:
        - bearerAuth: []
      requestBody:
        required: true
        content:
          multipart/form-data:
            schema:
              type: object
              required:
                - file
              properties:
                file:
                  type: array
                  items:
                    type: string
                    format: binary
                  description: Two or more PDF files to merge (max 20)
      responses:
        '200':
          description: Merged PDF
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ToolResponse'
        '400':
          description: Invalid input
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Error'
        '401':
          description: Authentication required
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/UnauthorizedError'
  /api/v1/tools/pdf/split-pdf:
    post:
      operationId: splitPdf
      tags:
        - Tools
      summary: Dividir PDF
      description: |
        Extrai páginas ou divide em partes. No modo "range" retorna um único PDF
        com as páginas selecionadas. No modo "every" divide em blocos de N páginas
        e retorna um arquivo ZIP contendo as partes.
      security:
        - bearerAuth: []
      requestBody:
        required: true
        content:
          multipart/form-data:
            schema:
              type: object
              required:
                - file
              properties:
                file:
                  type: string
                  format: binary
                  description: PDF file to split
                settings:
                  type: string
                  description: |
                    JSON string with options:
                    - `mode` (string, default "range") - One of: range, every
                    - `range` (string, optional) - Page range in qpdf syntax, e.g. "1-5,8,10-z" (required when mode is "range")
                    - `everyN` (integer 1-500, optional) - Split into chunks of N pages (required when mode is "every")
      responses:
        '200':
          description: Split PDF or ZIP archive of parts
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ToolResponse'
        '400':
          description: Invalid input
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Error'
        '401':
          description: Authentication required
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/UnauthorizedError'
  /api/v1/tools/pdf/compress-pdf:
    post:
      operationId: compressPdf
      tags:
        - Tools
      summary: Comprimir PDF
      description: |
        Reduz o tamanho do arquivo PDF fazendo downsampling das imagens incorporadas. No modo "quality"
        um controle de 1-100 define o DPI das imagens. No modo "targetSize" uma busca binária
        encontra o maior DPI que cabe no alvo. Se a compressão aumentasse
        o arquivo, os bytes originais são retornados inalterados.
      security:
        - bearerAuth: []
      requestBody:
        required: true
        content:
          multipart/form-data:
            schema:
              type: object
              required:
                - file
              properties:
                file:
                  type: string
                  format: binary
                  description: PDF file to compress
                settings:
                  type: string
                  description: |
                    JSON string with options:
                    - `mode` (string, default "quality") - One of: quality, targetSize
                    - `quality` (integer 1-100, optional) - Compression quality (higher = less compression). Used in quality mode
                    - `targetSizeKb` (number, optional) - Target file size in kilobytes. Used in targetSize mode
      responses:
        '200':
          description: Compressed PDF
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ToolResponse'
        '400':
          description: Invalid input
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Error'
        '401':
          description: Authentication required
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/UnauthorizedError'
  /api/v1/tools/pdf/rotate-pdf:
    post:
      operationId: rotatePdf
      tags:
        - Tools
      summary: Girar PDF
      description: Gira páginas de um PDF em 90, 180 ou 270 graus.
      security:
        - bearerAuth: []
      requestBody:
        required: true
        content:
          multipart/form-data:
            schema:
              type: object
              required:
                - file
              properties:
                file:
                  type: string
                  format: binary
                  description: PDF file to process
                settings:
                  type: string
                  description: |
                    JSON string with options:
                    - `angle` (integer, default 90) - One of: 90, 180, 270
                    - `range` (string, default "1-z") - Page range in qpdf syntax, e.g. "1-5,8" ("1-z" = all pages)
      responses:
        '200':
          description: Rotated PDF
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ToolResponse'
        '400':
          description: Invalid input
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Error'
        '401':
          description: Authentication required
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/UnauthorizedError'
  /api/v1/tools/pdf/extract-pages:
    post:
      operationId: extractPages
      tags:
        - Tools
      summary: Extrair páginas
      description: Extrai as páginas selecionadas para um novo PDF.
      security:
        - bearerAuth: []
      requestBody:
        required: true
        content:
          multipart/form-data:
            schema:
              type: object
              required:
                - file
              properties:
                file:
                  type: string
                  format: binary
                  description: PDF file to extract from
                settings:
                  type: string
                  description: |
                    JSON string with options:
                    - `range` (string, required) - Page range in qpdf syntax, e.g. "1-5,8,10-z"
      responses:
        '200':
          description: Extracted pages as a new PDF
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ToolResponse'
        '400':
          description: Invalid input
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Error'
        '401':
          description: Authentication required
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/UnauthorizedError'
  /api/v1/tools/pdf/remove-pages:
    post:
      operationId: removePages
      tags:
        - Tools
      summary: Remover páginas
      description: Exclui páginas específicas de um PDF. Não é possível remover todas as páginas.
      security:
        - bearerAuth: []
      requestBody:
        required: true
        content:
          multipart/form-data:
            schema:
              type: object
              required:
                - file
              properties:
                file:
                  type: string
                  format: binary
                  description: PDF file to process
                settings:
                  type: string
                  description: |
                    JSON string with options:
                    - `pages` (string, required) - Page range to remove in qpdf syntax, e.g. "3,5-7"
      responses:
        '200':
          description: PDF with pages removed
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ToolResponse'
        '400':
          description: Invalid input
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Error'
        '401':
          description: Authentication required
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/UnauthorizedError'
  /api/v1/tools/pdf/organize-pdf:
    post:
      operationId: organizePdf
      tags:
        - Tools
      summary: Organizar PDF
      description: Reordena páginas com uma ordem de páginas explícita.
      security:
        - bearerAuth: []
      requestBody:
        required: true
        content:
          multipart/form-data:
            schema:
              type: object
              required:
                - file
              properties:
                file:
                  type: string
                  format: binary
                  description: PDF file to reorganize
                settings:
                  type: string
                  description: |
                    JSON string with options:
                    - `order` (string, required) - Desired page order in qpdf syntax, e.g. "3,1,2,5-z"
      responses:
        '200':
          description: Reorganized PDF
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ToolResponse'
        '400':
          description: Invalid input
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Error'
        '401':
          description: Authentication required
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/UnauthorizedError'
  /api/v1/tools/pdf/protect-pdf:
    post:
      operationId: protectPdf
      tags:
        - Tools
      summary: Proteger PDF
      description: Adiciona proteção por senha com criptografia AES-256. As senhas são omitidas dos logs de auditoria.
      security:
        - bearerAuth: []
      requestBody:
        required: true
        content:
          multipart/form-data:
            schema:
              type: object
              required:
                - file
              properties:
                file:
                  type: string
                  format: binary
                  description: PDF file to protect
                settings:
                  type: string
                  description: |
                    JSON string with options:
                    - `userPassword` (string 1-256 chars, required) - Password required to open the PDF
                    - `ownerPassword` (string 1-256 chars, optional) - Owner password for permissions; defaults to userPassword if omitted
      responses:
        '200':
          description: Encrypted PDF
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ToolResponse'
        '400':
          description: Invalid input
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Error'
        '401':
          description: Authentication required
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/UnauthorizedError'
  /api/v1/tools/pdf/unlock-pdf:
    post:
      operationId: unlockPdf
      tags:
        - Tools
      summary: Desbloquear PDF
      description: Remove a proteção por senha de um PDF. A senha correta deve ser fornecida. As senhas são omitidas dos logs de auditoria.
      security:
        - bearerAuth: []
      requestBody:
        required: true
        content:
          multipart/form-data:
            schema:
              type: object
              required:
                - file
              properties:
                file:
                  type: string
                  format: binary
                  description: Password-protected PDF file
                settings:
                  type: string
                  description: |
                    JSON string with options:
                    - `password` (string 1-256 chars, required) - Password to decrypt the PDF
      responses:
        '200':
          description: Decrypted PDF
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ToolResponse'
        '400':
          description: Invalid input
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Error'
        '401':
          description: Authentication required
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/UnauthorizedError'
  /api/v1/tools/pdf/repair-pdf:
    post:
      operationId: repairPdf
      tags:
        - Tools
      summary: Reparar PDF
      description: Tenta reparar um PDF danificado ou corrompido. A validação estrutural é ignorada na entrada para permitir arquivos malformados.
      security:
        - bearerAuth: []
      requestBody:
        required: true
        content:
          multipart/form-data:
            schema:
              type: object
              required:
                - file
              properties:
                file:
                  type: string
                  format: binary
                  description: Damaged PDF file to repair
      responses:
        '200':
          description: Repaired PDF
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ToolResponse'
        '400':
          description: Invalid input
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Error'
        '401':
          description: Authentication required
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/UnauthorizedError'
  /api/v1/tools/pdf/linearize-pdf:
    post:
      operationId: linearizePdf
      tags:
        - Tools
      summary: Otimizar PDF para web
      description: Lineariza um PDF para visualização rápida na web (download progressivo).
      security:
        - bearerAuth: []
      requestBody:
        required: true
        content:
          multipart/form-data:
            schema:
              type: object
              required:
                - file
              properties:
                file:
                  type: string
                  format: binary
                  description: PDF file to linearize
      responses:
        '200':
          description: Linearized PDF
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ToolResponse'
        '400':
          description: Invalid input
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Error'
        '401':
          description: Authentication required
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/UnauthorizedError'
  /api/v1/tools/pdf/grayscale-pdf:
    post:
      operationId: grayscalePdf
      tags:
        - Tools
      summary: PDF em escala de cinza
      description: Converte todas as cores de um PDF para escala de cinza.
      security:
        - bearerAuth: []
      requestBody:
        required: true
        content:
          multipart/form-data:
            schema:
              type: object
              required:
                - file
              properties:
                file:
                  type: string
                  format: binary
                  description: PDF file to convert
      responses:
        '200':
          description: Grayscale PDF
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ToolResponse'
        '400':
          description: Invalid input
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Error'
        '401':
          description: Authentication required
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/UnauthorizedError'
  /api/v1/tools/pdf/pdfa-convert:
    post:
      operationId: pdfaConvert
      tags:
        - Tools
      summary: Conversor de PDF/A
      description: Converte um PDF para o formato de arquivamento PDF/A-2 para preservação de longo prazo.
      security:
        - bearerAuth: []
      requestBody:
        required: true
        content:
          multipart/form-data:
            schema:
              type: object
              required:
                - file
              properties:
                file:
                  type: string
                  format: binary
                  description: PDF file to convert
      responses:
        '200':
          description: PDF/A-2 compliant file
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ToolResponse'
        '400':
          description: Invalid input
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Error'
        '401':
          description: Authentication required
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/UnauthorizedError'
  /api/v1/tools/pdf/crop-pdf:
    post:
      operationId: cropPdf
      tags:
        - Tools
      summary: Recortar PDF
      description: Recorta todas as páginas com uma margem uniforme.
      security:
        - bearerAuth: []
      requestBody:
        required: true
        content:
          multipart/form-data:
            schema:
              type: object
              required:
                - file
              properties:
                file:
                  type: string
                  format: binary
                  description: PDF file to crop
                settings:
                  type: string
                  description: |
                    JSON string with options:
                    - `margin` (number 0-2000, default 20) - Uniform crop margin in points
      responses:
        '200':
          description: Cropped PDF
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ToolResponse'
        '400':
          description: Invalid input
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Error'
        '401':
          description: Authentication required
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/UnauthorizedError'
  /api/v1/tools/pdf/nup-pdf:
    post:
      operationId: nupPdf
      tags:
        - Tools
      summary: Páginas por Folha (N-up)
      description: Organiza várias páginas por folha (por exemplo, 2-up, 4-up).
      security:
        - bearerAuth: []
      requestBody:
        required: true
        content:
          multipart/form-data:
            schema:
              type: object
              required:
                - file
              properties:
                file:
                  type: string
                  format: binary
                  description: PDF file to process
                settings:
                  type: string
                  description: |
                    JSON string with options:
                    - `perSheet` (integer, default 2) - One of: 2, 3, 4, 8, 9, 12, 16
      responses:
        '200':
          description: N-up PDF
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ToolResponse'
        '400':
          description: Invalid input
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Error'
        '401':
          description: Authentication required
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/UnauthorizedError'
  /api/v1/tools/pdf/booklet-pdf:
    post:
      operationId: bookletPdf
      tags:
        - Tools
      summary: PDF em Livreto
      description: Organiza as páginas para dobrar em livreto (imposição para impressão frente e verso).
      security:
        - bearerAuth: []
      requestBody:
        required: true
        content:
          multipart/form-data:
            schema:
              type: object
              required:
                - file
              properties:
                file:
                  type: string
                  format: binary
                  description: PDF file to process
                settings:
                  type: string
                  description: |
                    JSON string with options:
                    - `perSheet` (integer, default 2) - One of: 2, 4, 6, 8
      responses:
        '200':
          description: Booklet-imposed PDF
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ToolResponse'
        '400':
          description: Invalid input
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Error'
        '401':
          description: Authentication required
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/UnauthorizedError'
  /api/v1/tools/pdf/watermark-pdf:
    post:
      operationId: watermarkPdf
      tags:
        - Tools
      summary: Marca d'água em PDF
      description: Adiciona uma marca d'água de texto a todas as páginas de um PDF.
      security:
        - bearerAuth: []
      requestBody:
        required: true
        content:
          multipart/form-data:
            schema:
              type: object
              required:
                - file
              properties:
                file:
                  type: string
                  format: binary
                  description: PDF file to watermark
                settings:
                  type: string
                  description: |
                    JSON string with options:
                    - `text` (string 1-200 chars, required) - Watermark text
                    - `position` (string, default "c") - One of: tl, tc, tr, l, c, r, bl, bc, br
                    - `fontSize` (integer 6-72, default 48) - Font size in points
                    - `opacity` (number 0.05-1, default 0.3) - Watermark opacity
                    - `rotation` (number -180 to 180, default 45) - Rotation angle in degrees
      responses:
        '200':
          description: Watermarked PDF
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ToolResponse'
        '400':
          description: Invalid input
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Error'
        '401':
          description: Authentication required
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/UnauthorizedError'
  /api/v1/tools/pdf/pdf-page-numbers:
    post:
      operationId: pdfPageNumbers
      tags:
        - Tools
      summary: Números de página do PDF
      description: Adiciona números de página ("Página N de M") a todas as páginas de um PDF.
      security:
        - bearerAuth: []
      requestBody:
        required: true
        content:
          multipart/form-data:
            schema:
              type: object
              required:
                - file
              properties:
                file:
                  type: string
                  format: binary
                  description: PDF file to process
                settings:
                  type: string
                  description: |
                    JSON string with options:
                    - `position` (string, default "bc") - One of: bl, bc, br, tl, tc, tr
                    - `fontSize` (integer 6-24, default 10) - Font size in points
      responses:
        '200':
          description: PDF with page numbers
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ToolResponse'
        '400':
          description: Invalid input
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Error'
        '401':
          description: Authentication required
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/UnauthorizedError'
  /api/v1/tools/files/convert-document:
    post:
      operationId: convertDocument
      tags:
        - Tools
      summary: Converter documento
      description: Converte entre os formatos Word, OpenDocument, RTF e texto simples.
      security:
        - bearerAuth: []
      requestBody:
        required: true
        content:
          multipart/form-data:
            schema:
              type: object
              required:
                - file
              properties:
                file:
                  type: string
                  format: binary
                  description: Document file to convert (.docx, .doc, .odt, .rtf, .txt)
                settings:
                  type: string
                  description: |
                    JSON string with options:
                    - `format` (string, required) - One of: docx, odt, rtf, txt
                clientJobId:
                  type: string
                  description: Client-provided job ID for SSE progress tracking
      responses:
        '202':
          description: Accepted for async processing. Track progress via SSE at /api/v1/jobs/{jobId}/progress.
          content:
            application/json:
              schema:
                type: object
                properties:
                  jobId:
                    type: string
                  async:
                    type: boolean
                    example: true
        '400':
          description: Invalid input
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Error'
        '401':
          description: Authentication required
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/UnauthorizedError'
  /api/v1/tools/files/convert-presentation:
    post:
      operationId: convertPresentation
      tags:
        - Tools
      summary: Converter apresentação
      description: Converte entre os formatos de apresentação PowerPoint e OpenDocument.
      security:
        - bearerAuth: []
      requestBody:
        required: true
        content:
          multipart/form-data:
            schema:
              type: object
              required:
                - file
              properties:
                file:
                  type: string
                  format: binary
                  description: Presentation file to convert (.pptx, .ppt, .odp)
                settings:
                  type: string
                  description: |
                    JSON string with options:
                    - `format` (string, required) - One of: pptx, odp
                clientJobId:
                  type: string
                  description: Client-provided job ID for SSE progress tracking
      responses:
        '202':
          description: Accepted for async processing. Track progress via SSE at /api/v1/jobs/{jobId}/progress.
          content:
            application/json:
              schema:
                type: object
                properties:
                  jobId:
                    type: string
                  async:
                    type: boolean
                    example: true
        '400':
          description: Invalid input
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Error'
        '401':
          description: Authentication required
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/UnauthorizedError'
  /api/v1/tools/files/convert-spreadsheet:
    post:
      operationId: convertSpreadsheet
      tags:
        - Tools
      summary: Converter planilha
      description: Converte entre os formatos Excel, OpenDocument e CSV. Planilhas com várias abas exportam a primeira aba para CSV.
      security:
        - bearerAuth: []
      requestBody:
        required: true
        content:
          multipart/form-data:
            schema:
              type: object
              required:
                - file
              properties:
                file:
                  type: string
                  format: binary
                  description: Spreadsheet file to convert (.xlsx, .xls, .ods, .csv)
                settings:
                  type: string
                  description: |
                    JSON string with options:
                    - `format` (string, required) - One of: xlsx, ods, csv
                clientJobId:
                  type: string
                  description: Client-provided job ID for SSE progress tracking
      responses:
        '202':
          description: Accepted for async processing. Track progress via SSE at /api/v1/jobs/{jobId}/progress.
          content:
            application/json:
              schema:
                type: object
                properties:
                  jobId:
                    type: string
                  async:
                    type: boolean
                    example: true
        '400':
          description: Invalid input
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Error'
        '401':
          description: Authentication required
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/UnauthorizedError'
  /api/v1/tools/files/excel-to-pdf:
    post:
      operationId: excelToPdf
      tags:
        - Tools
      summary: Excel para PDF
      description: Converte planilhas para PDF. Planilhas largas podem se dividir em várias páginas.
      security:
        - bearerAuth: []
      requestBody:
        required: true
        content:
          multipart/form-data:
            schema:
              type: object
              required:
                - file
              properties:
                file:
                  type: string
                  format: binary
                  description: Spreadsheet file to convert (.xlsx, .xls, .ods, .csv)
                clientJobId:
                  type: string
                  description: Client-provided job ID for SSE progress tracking
      responses:
        '202':
          description: Accepted for async processing. Track progress via SSE at /api/v1/jobs/{jobId}/progress.
          content:
            application/json:
              schema:
                type: object
                properties:
                  jobId:
                    type: string
                  async:
                    type: boolean
                    example: true
        '400':
          description: Invalid input
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Error'
        '401':
          description: Authentication required
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/UnauthorizedError'
  /api/v1/tools/files/word-to-pdf:
    post:
      operationId: wordToPdf
      tags:
        - Tools
      summary: Word para PDF
      description: Converte documentos Word para PDF.
      security:
        - bearerAuth: []
      requestBody:
        required: true
        content:
          multipart/form-data:
            schema:
              type: object
              required:
                - file
              properties:
                file:
                  type: string
                  format: binary
                  description: Word document to convert (.docx, .doc, .odt, .rtf, .txt)
                clientJobId:
                  type: string
                  description: Client-provided job ID for SSE progress tracking
      responses:
        '202':
          description: Accepted for async processing. Track progress via SSE at /api/v1/jobs/{jobId}/progress.
          content:
            application/json:
              schema:
                type: object
                properties:
                  jobId:
                    type: string
                  async:
                    type: boolean
                    example: true
        '400':
          description: Invalid input
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Error'
        '401':
          description: Authentication required
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/UnauthorizedError'
  /api/v1/tools/files/powerpoint-to-pdf:
    post:
      operationId: powerpointToPdf
      tags:
        - Tools
      summary: PowerPoint para PDF
      description: Converte apresentações para PDF.
      security:
        - bearerAuth: []
      requestBody:
        required: true
        content:
          multipart/form-data:
            schema:
              type: object
              required:
                - file
              properties:
                file:
                  type: string
                  format: binary
                  description: Presentation file to convert (.pptx, .ppt, .odp)
                clientJobId:
                  type: string
                  description: Client-provided job ID for SSE progress tracking
      responses:
        '202':
          description: Accepted for async processing. Track progress via SSE at /api/v1/jobs/{jobId}/progress.
          content:
            application/json:
              schema:
                type: object
                properties:
                  jobId:
                    type: string
                  async:
                    type: boolean
                    example: true
        '400':
          description: Invalid input
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Error'
        '401':
          description: Authentication required
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/UnauthorizedError'
  /api/v1/tools/files/html-to-pdf:
    post:
      operationId: htmlToPdf
      tags:
        - Tools
      summary: HTML para PDF
      description: Converte um arquivo HTML para PDF. Recursos remotos são desabilitados por privacidade.
      security:
        - bearerAuth: []
      requestBody:
        required: true
        content:
          multipart/form-data:
            schema:
              type: object
              required:
                - file
              properties:
                file:
                  type: string
                  format: binary
                  description: HTML file to convert (.html, .htm)
                clientJobId:
                  type: string
                  description: Client-provided job ID for SSE progress tracking
      responses:
        '202':
          description: Accepted for async processing. Track progress via SSE at /api/v1/jobs/{jobId}/progress.
          content:
            application/json:
              schema:
                type: object
                properties:
                  jobId:
                    type: string
                  async:
                    type: boolean
                    example: true
        '400':
          description: Invalid input
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Error'
        '401':
          description: Authentication required
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/UnauthorizedError'
  /api/v1/tools/files/markdown-to-docx:
    post:
      operationId: markdownToDocx
      tags:
        - Tools
      summary: Markdown para Word
      description: Converte um arquivo Markdown para um documento Word (DOCX).
      security:
        - bearerAuth: []
      requestBody:
        required: true
        content:
          multipart/form-data:
            schema:
              type: object
              required:
                - file
              properties:
                file:
                  type: string
                  format: binary
                  description: Markdown file to convert (.md, .markdown)
      responses:
        '200':
          description: Processed document
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ToolResponse'
        '400':
          description: Invalid input
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Error'
        '401':
          description: Authentication required
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/UnauthorizedError'
  /api/v1/tools/files/markdown-to-html:
    post:
      operationId: markdownToHtml
      tags:
        - Tools
      summary: Markdown para HTML
      description: Converte um arquivo Markdown para uma página HTML independente. Imagens remotas na origem são mantidas como estão na saída.
      security:
        - bearerAuth: []
      requestBody:
        required: true
        content:
          multipart/form-data:
            schema:
              type: object
              required:
                - file
              properties:
                file:
                  type: string
                  format: binary
                  description: Markdown file to convert (.md, .markdown)
      responses:
        '200':
          description: Processed document
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ToolResponse'
        '400':
          description: Invalid input
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Error'
        '401':
          description: Authentication required
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/UnauthorizedError'
  /api/v1/tools/files/markdown-to-pdf:
    post:
      operationId: markdownToPdf
      tags:
        - Tools
      summary: Markdown para PDF
      description: Converte um arquivo Markdown para um PDF estilizado. Recursos remotos são desabilitados por privacidade.
      security:
        - bearerAuth: []
      requestBody:
        required: true
        content:
          multipart/form-data:
            schema:
              type: object
              required:
                - file
              properties:
                file:
                  type: string
                  format: binary
                  description: Markdown file to convert (.md, .markdown)
                clientJobId:
                  type: string
                  description: Client-provided job ID for SSE progress tracking
      responses:
        '202':
          description: Accepted for async processing. Track progress via SSE at /api/v1/jobs/{jobId}/progress.
          content:
            application/json:
              schema:
                type: object
                properties:
                  jobId:
                    type: string
                  async:
                    type: boolean
                    example: true
        '400':
          description: Invalid input
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Error'
        '401':
          description: Authentication required
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/UnauthorizedError'
  /api/v1/tools/files/epub-convert:
    post:
      operationId: epubConvert
      tags:
        - Tools
      summary: Converter de EPUB
      description: Converte um EPUB para PDF, DOCX, HTML ou Markdown. Recursos remotos dentro do livro não são buscados.
      security:
        - bearerAuth: []
      requestBody:
        required: true
        content:
          multipart/form-data:
            schema:
              type: object
              required:
                - file
              properties:
                file:
                  type: string
                  format: binary
                  description: EPUB file to convert (.epub)
                settings:
                  type: string
                  description: |
                    JSON string with options:
                    - `format` (string, required) - One of: pdf, docx, html, md
                clientJobId:
                  type: string
                  description: Client-provided job ID for SSE progress tracking
      responses:
        '202':
          description: Accepted for async processing. Track progress via SSE at /api/v1/jobs/{jobId}/progress.
          content:
            application/json:
              schema:
                type: object
                properties:
                  jobId:
                    type: string
                  async:
                    type: boolean
                    example: true
        '400':
          description: Invalid input
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Error'
        '401':
          description: Authentication required
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/UnauthorizedError'
  /api/v1/tools/files/to-epub:
    post:
      operationId: toEpub
      tags:
        - Tools
      summary: Converter para EPUB
      description: Converte arquivos Word, Markdown, HTML ou texto simples para EPUB.
      security:
        - bearerAuth: []
      requestBody:
        required: true
        content:
          multipart/form-data:
            schema:
              type: object
              required:
                - file
              properties:
                file:
                  type: string
                  format: binary
                  description: Document file to convert (.docx, .md, .html, .txt)
                clientJobId:
                  type: string
                  description: Client-provided job ID for SSE progress tracking
      responses:
        '202':
          description: Accepted for async processing. Track progress via SSE at /api/v1/jobs/{jobId}/progress.
          content:
            application/json:
              schema:
                type: object
                properties:
                  jobId:
                    type: string
                  async:
                    type: boolean
                    example: true
        '400':
          description: Invalid input
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Error'
        '401':
          description: Authentication required
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/UnauthorizedError'
  /api/v1/tools/pdf/flatten-pdf:
    post:
      operationId: flattenPdf
      tags:
        - Tools
      summary: Achatar PDF
      description: Incorpora formulários e anotações ao conteúdo da página.
      security:
        - bearerAuth: []
      requestBody:
        required: true
        content:
          multipart/form-data:
            schema:
              type: object
              required:
                - file
              properties:
                file:
                  type: string
                  format: binary
                  description: PDF file to process (.pdf)
      responses:
        '200':
          description: Processed PDF
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ToolResponse'
        '400':
          description: Invalid input
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Error'
        '401':
          description: Authentication required
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/UnauthorizedError'
  /api/v1/tools/pdf/redact-pdf:
    post:
      operationId: redactPdf
      tags:
        - Tools
      summary: Redigir PDF
      description: Remove permanentemente ocorrências de texto (redação verdadeira e verificada).
      security:
        - bearerAuth: []
      requestBody:
        required: true
        content:
          multipart/form-data:
            schema:
              type: object
              required:
                - file
              properties:
                file:
                  type: string
                  format: binary
                  description: PDF file to process (.pdf)
                settings:
                  type: string
                  description: |
                    JSON string with options:
                    - `terms` (string[], required) - Text strings to redact (1-50 terms, each up to 200 characters)
                    - `caseSensitive` (boolean, default false) - Whether matching is case-sensitive
      responses:
        '200':
          description: Processed PDF
          content:
            application/json:
              schema:
                allOf:
                  - $ref: '#/components/schemas/ToolResponse'
                  - type: object
                    properties:
                      found:
                        type: integer
                        description: Number of redacted occurrences found
        '400':
          description: Invalid input
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Error'
        '401':
          description: Authentication required
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/UnauthorizedError'
  /api/v1/tools/pdf/pdf-to-text:
    post:
      operationId: pdfToText
      tags:
        - Tools
      summary: PDF para texto
      description: Extrai texto simples de um PDF.
      security:
        - bearerAuth: []
      requestBody:
        required: true
        content:
          multipart/form-data:
            schema:
              type: object
              required:
                - file
              properties:
                file:
                  type: string
                  format: binary
                  description: PDF file to process (.pdf)
      responses:
        '200':
          description: Extracted text file
          content:
            application/json:
              schema:
                allOf:
                  - $ref: '#/components/schemas/ToolResponse'
                  - type: object
                    properties:
                      chars:
                        type: integer
                        description: Number of characters extracted
        '400':
          description: Invalid input
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Error'
        '401':
          description: Authentication required
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/UnauthorizedError'
  /api/v1/tools/pdf/pdf-to-word:
    post:
      operationId: pdfToWord
      tags:
        - Tools
      summary: PDF para Word
      description: Melhor para PDFs baseados em texto; páginas digitalizadas precisam de OCR.
      security:
        - bearerAuth: []
      requestBody:
        required: true
        content:
          multipart/form-data:
            schema:
              type: object
              required:
                - file
              properties:
                file:
                  type: string
                  format: binary
                  description: PDF file to convert (.pdf)
                clientJobId:
                  type: string
                  description: Client-provided job ID for SSE progress tracking
      responses:
        '202':
          description: Accepted for async processing. Track progress via SSE at /api/v1/jobs/{jobId}/progress.
          content:
            application/json:
              schema:
                type: object
                properties:
                  jobId:
                    type: string
                  async:
                    type: boolean
                    example: true
        '400':
          description: Invalid input
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Error'
        '401':
          description: Authentication required
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/UnauthorizedError'
  /api/v1/tools/pdf/pdf-metadata:
    post:
      operationId: pdfMetadata
      tags:
        - Tools
      summary: Metadados do PDF
      description: Lê e grava os metadados do documento PDF.
      security:
        - bearerAuth: []
      requestBody:
        required: true
        content:
          multipart/form-data:
            schema:
              type: object
              required:
                - file
              properties:
                file:
                  type: string
                  format: binary
                  description: PDF file to process (.pdf)
                settings:
                  type: string
                  description: |
                    JSON string with options:
                    - `title` (string, optional, max 500) - Document title
                    - `author` (string, optional, max 500) - Document author
                    - `subject` (string, optional, max 500) - Document subject
                    - `keywords` (string, optional, max 500) - Document keywords
      responses:
        '200':
          description: Processed PDF with updated metadata
          content:
            application/json:
              schema:
                allOf:
                  - $ref: '#/components/schemas/ToolResponse'
                  - type: object
                    properties:
                      metadata:
                        type: object
                        description: The resulting PDF metadata fields
        '400':
          description: Invalid input
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Error'
        '401':
          description: Authentication required
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/UnauthorizedError'
  /api/v1/tools/pdf/ocr-pdf:
    post:
      operationId: ocrPdf
      tags:
        - Tools
      summary: OCR de PDF
      description: |-
        Extraia texto de documentos PDF digitalizados página por página. Fast usa o
        tempo de execução Tesseract integrado. Balanced e Best usam o sinal opcional
        Tempo de execução RapidOCR com modelos PP-OCR ONNX fixados. Solicitações de nível explícito
        nunca faça downgrade silenciosamente. Uma solicitação pode selecionar no máximo 50 páginas.
      security:
        - bearerAuth: []
      requestBody:
        required: true
        content:
          multipart/form-data:
            schema:
              type: object
              required:
                - file
              properties:
                file:
                  type: string
                  format: binary
                  maxLength: 536870912
                  description: PDF file to process (.pdf)
                settings:
                  type: string
                  description: |
                    JSON string with options:
                    - `quality` (string, optional) - One of: fast, balanced, best. When both quality and engine are omitted, uses the best available tier in this order: best, balanced, fast. Korean never selects fast; it uses best, then balanced, or returns an accurate-runtime install/compatibility error
                    - `language` (string, default "auto") - One of: auto, en, de, fr, es, zh, ja, ko. Korean is unsupported by fast and the legacy tesseract alias
                    - `pages` (string, default "all") - Page selection, e.g. "all", "1-3", "1,3,5"
                    - `enhance` (boolean, optional) - Improve local contrast. Fast applies it directly; accurate tiers retain the variant only when calibrated scoring improves the result. Defaults to true for Best and false for Fast/Balanced
                    - `engine` (string, deprecated) - Compatibility alias: tesseract maps to fast; the legacy paddleocr value maps to balanced but does not load PaddlePaddle
                clientJobId:
                  type: string
                  description: Client-provided job ID for SSE progress tracking
      responses:
        '202':
          description: Accepted for async processing. Track progress via SSE at /api/v1/jobs/{jobId}/progress.
          content:
            application/json:
              schema:
                type: object
                required:
                  - jobId
                  - async
                properties:
                  jobId:
                    type: string
                  progressJobId:
                    type: string
                  artifactJobId:
                    type: string
                  async:
                    type: boolean
                    const: true
        '400':
          description: Invalid input
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Error'
        '401':
          description: Authentication required
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/UnauthorizedError'
        '413':
          description: Encoded PDF exceeds the hard 512 MiB safety limit
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Error'
        '422':
          description: Processing failed
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Error'
        '501':
          description: Requested Balanced/Best runtime is not installed or is incompatible
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/FeatureNotInstalledError'
        '503':
          description: PDF OCR could not be queued
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Error'
  /api/v1/tools/files/chart-maker:
    post:
      operationId: chartMaker
      tags:
        - Tools
      summary: Criador de Gráficos
      description: Cria gráficos de barras, linhas ou pizza a partir de dados CSV ou JSON. Retorna uma imagem PNG.
      security:
        - bearerAuth: []
      requestBody:
        required: true
        content:
          multipart/form-data:
            schema:
              type: object
              required:
                - file
              properties:
                file:
                  type: string
                  format: binary
                  description: CSV or JSON data file
                settings:
                  type: string
                  description: |
                    JSON string with options:
                    - `kind` (string, default "bar") - One of: bar, line, pie
                    - `title` (string, optional) - Chart title (max 120 characters)
                    - `width` (integer 320-2048, default 960) - Chart width in pixels
                    - `height` (integer 240-1536, default 540) - Chart height in pixels
      responses:
        '200':
          description: Generated chart image
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ToolResponse'
        '400':
          description: Invalid input
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Error'
        '401':
          description: Authentication required
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/UnauthorizedError'
  /api/v1/tools/files/csv-excel:
    post:
      operationId: csvExcel
      tags:
        - Tools
      summary: CSV para Excel
      description: Converte entre CSV e Excel (XLSX), nos dois sentidos. Envie um arquivo CSV ou TSV para obter XLSX, ou envie um arquivo XLSX para obter CSV.
      security:
        - bearerAuth: []
      requestBody:
        required: true
        content:
          multipart/form-data:
            schema:
              type: object
              required:
                - file
              properties:
                file:
                  type: string
                  format: binary
                  description: CSV, TSV, or XLSX file to convert
                settings:
                  type: string
                  description: |
                    JSON string with options:
                    - `sheet` (integer, min 1, default 1) - Worksheet number to export when converting from XLSX
      responses:
        '200':
          description: Converted file
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ToolResponse'
        '400':
          description: Invalid input
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Error'
        '401':
          description: Authentication required
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/UnauthorizedError'
  /api/v1/tools/files/csv-json:
    post:
      operationId: csvJson
      tags:
        - Tools
      summary: CSV para JSON
      description: Converte entre CSV e JSON, nos dois sentidos. Envie um arquivo CSV ou TSV para obter JSON, ou envie um array JSON de objetos para obter CSV.
      security:
        - bearerAuth: []
      requestBody:
        required: true
        content:
          multipart/form-data:
            schema:
              type: object
              required:
                - file
              properties:
                file:
                  type: string
                  format: binary
                  description: CSV, TSV, or JSON file to convert
                settings:
                  type: string
                  description: |
                    JSON string with options:
                    - `pretty` (boolean, default true) - Pretty-print JSON output with indentation
      responses:
        '200':
          description: Converted file
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ToolResponse'
        '400':
          description: Invalid input
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Error'
        '401':
          description: Authentication required
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/UnauthorizedError'
  /api/v1/tools/files/json-xml:
    post:
      operationId: jsonXml
      tags:
        - Tools
      summary: JSON para XML
      description: Converte entre JSON e XML, nos dois sentidos. Envie um arquivo JSON para obter XML, ou envie um arquivo XML para obter JSON.
      security:
        - bearerAuth: []
      requestBody:
        required: true
        content:
          multipart/form-data:
            schema:
              type: object
              required:
                - file
              properties:
                file:
                  type: string
                  format: binary
                  description: JSON or XML file to convert
                settings:
                  type: string
                  description: |
                    JSON string with options:
                    - `pretty` (boolean, default true) - Pretty-print output with indentation
      responses:
        '200':
          description: Converted file
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ToolResponse'
        '400':
          description: Invalid input
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Error'
        '401':
          description: Authentication required
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/UnauthorizedError'
  /api/v1/tools/files/split-csv:
    post:
      operationId: splitCsv
      tags:
        - Tools
      summary: Dividir CSV
      description: Divide um arquivo CSV em arquivos menores por contagem de linhas. Retorna um arquivo ZIP contendo as partes.
      security:
        - bearerAuth: []
      requestBody:
        required: true
        content:
          multipart/form-data:
            schema:
              type: object
              required:
                - file
              properties:
                file:
                  type: string
                  format: binary
                  description: CSV or TSV file to split
                settings:
                  type: string
                  description: |
                    JSON string with options:
                    - `rowsPerFile` (integer 1-1000000, default 1000) - Number of data rows per output file
                    - `keepHeader` (boolean, default true) - Repeat the header row in each output file
      responses:
        '200':
          description: ZIP archive containing split CSV parts
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ToolResponse'
        '400':
          description: Invalid input
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Error'
        '401':
          description: Authentication required
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/UnauthorizedError'
  /api/v1/tools/files/merge-csvs:
    post:
      operationId: mergeCsvs
      tags:
        - Tools
      summary: Mesclar CSVs
      description: Combina vários arquivos CSV ou TSV com colunas correspondentes em um único arquivo mesclado. Todos os arquivos devem ter os mesmos cabeçalhos de coluna.
      security:
        - bearerAuth: []
      requestBody:
        required: true
        content:
          multipart/form-data:
            schema:
              type: object
              required:
                - file
              properties:
                file:
                  type: array
                  items:
                    type: string
                    format: binary
                  description: Two or more CSV or TSV files to merge (2-20 files)
      responses:
        '200':
          description: Merged CSV file
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ToolResponse'
        '400':
          description: Invalid input
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Error'
        '401':
          description: Authentication required
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/UnauthorizedError'
  /api/v1/tools/files/yaml-json:
    post:
      operationId: yamlJson
      tags:
        - Tools
      summary: Converter YAML / JSON
      description: Converte entre YAML e JSON, nos dois sentidos. Envie um arquivo YAML para obter JSON, ou envie um arquivo JSON para obter YAML.
      security:
        - bearerAuth: []
      requestBody:
        required: true
        content:
          multipart/form-data:
            schema:
              type: object
              required:
                - file
              properties:
                file:
                  type: string
                  format: binary
                  description: YAML (.yaml, .yml) or JSON file to convert
      responses:
        '200':
          description: Converted file
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ToolResponse'
        '400':
          description: Invalid input
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Error'
        '401':
          description: Authentication required
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/UnauthorizedError'
  /api/v1/tools/files/xml-to-csv:
    post:
      operationId: xmlToCsv
      tags:
        - Tools
      summary: XML para CSV
      description: Extrai elementos repetidos de um arquivo XML para uma tabela CSV. Encontra automaticamente o primeiro array de objetos na árvore XML.
      security:
        - bearerAuth: []
      requestBody:
        required: true
        content:
          multipart/form-data:
            schema:
              type: object
              required:
                - file
              properties:
                file:
                  type: string
                  format: binary
                  description: XML file to convert
      responses:
        '200':
          description: Extracted CSV file
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ToolResponse'
        '400':
          description: Invalid input
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Error'
        '401':
          description: Authentication required
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/UnauthorizedError'
  /api/v1/tools/files/create-zip:
    post:
      operationId: createZip
      tags:
        - Tools
      summary: Criar ZIP
      description: Agrupa vários arquivos em um único arquivo ZIP. Nomes de arquivo duplicados são deduplicados automaticamente.
      security:
        - bearerAuth: []
      requestBody:
        required: true
        content:
          multipart/form-data:
            schema:
              type: object
              required:
                - file
              properties:
                file:
                  type: array
                  items:
                    type: string
                    format: binary
                  description: Two or more files to bundle (2-50 files, any type)
      responses:
        '200':
          description: ZIP archive
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ToolResponse'
        '400':
          description: Invalid input
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Error'
        '401':
          description: Authentication required
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/UnauthorizedError'
  /api/v1/tools/files/extract-zip:
    post:
      operationId: extractZip
      tags:
        - Tools
      summary: Extrair ZIP
      description: Extrai arquivos de um ZIP com segurança e proteção contra bomba. Arquivos com um único item retornam o arquivo diretamente; arquivos com vários itens retornam um ZIP plano.
      security:
        - bearerAuth: []
      requestBody:
        required: true
        content:
          multipart/form-data:
            schema:
              type: object
              required:
                - file
              properties:
                file:
                  type: string
                  format: binary
                  description: ZIP archive to extract
      responses:
        '200':
          description: Extracted file or repackaged ZIP archive
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ToolResponse'
        '400':
          description: Invalid input
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Error'
        '401':
          description: Authentication required
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/UnauthorizedError'
  /api/v1/tools/image/background-replace:
    post:
      operationId: backgroundReplace
      tags:
        - Tools
      summary: Substituir fundo
      description: |
        Substituição de fundo com IA. Remove o fundo do objeto
        e o compõe sobre uma cor sólida ou gradiente. Requer o
        bundle de recurso background-removal instalado.
      security:
        - bearerAuth: []
      requestBody:
        required: true
        content:
          multipart/form-data:
            schema:
              type: object
              required:
                - file
              properties:
                file:
                  type: string
                  format: binary
                  description: Image file to process
                settings:
                  type: string
                  description: |
                    JSON string with options:
                    - `backgroundType` (string, default "color") - One of: color, gradient
                    - `color` (string, default "#ffffff") - Background hex color (when backgroundType is color)
                    - `gradientColor1` (string, optional) - First gradient hex color
                    - `gradientColor2` (string, optional) - Second gradient hex color
                    - `gradientAngle` (integer 0-360, default 180) - Gradient angle in degrees
                    - `feather` (integer 0-20, default 0) - Edge feathering radius
                    - `format` (string, default "png") - One of: png, webp
                clientJobId:
                  type: string
                  description: Client-provided job ID for SSE progress tracking
      responses:
        '202':
          description: Accepted for async processing. Track progress via SSE at /api/v1/jobs/{jobId}/progress.
          content:
            application/json:
              schema:
                type: object
                properties:
                  jobId:
                    type: string
                  async:
                    type: boolean
                    example: true
        '400':
          description: Invalid input
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Error'
        '401':
          description: Authentication required
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/UnauthorizedError'
        '422':
          description: Processing failed
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Error'
        '501':
          description: Feature not installed
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/FeatureNotInstalledError'
  /api/v1/tools/image/blur-background:
    post:
      operationId: blurBackground
      tags:
        - Tools
      summary: Desfocar fundo
      description: |
        Desfoque de fundo com IA. Remove o fundo, desfoca o
        original e compõe o objeto nítido por cima. Requer o
        bundle de recurso background-removal instalado.
      security:
        - bearerAuth: []
      requestBody:
        required: true
        content:
          multipart/form-data:
            schema:
              type: object
              required:
                - file
              properties:
                file:
                  type: string
                  format: binary
                  description: Image file to process
                settings:
                  type: string
                  description: |
                    JSON string with options:
                    - `intensity` (integer 1-100, default 50) - Blur intensity
                    - `feather` (integer 0-20, default 0) - Edge feathering radius
                    - `format` (string, default "png") - One of: png, webp
                clientJobId:
                  type: string
                  description: Client-provided job ID for SSE progress tracking
      responses:
        '202':
          description: Accepted for async processing. Track progress via SSE at /api/v1/jobs/{jobId}/progress.
          content:
            application/json:
              schema:
                type: object
                properties:
                  jobId:
                    type: string
                  async:
                    type: boolean
                    example: true
        '400':
          description: Invalid input
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Error'
        '401':
          description: Authentication required
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/UnauthorizedError'
        '422':
          description: Processing failed
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Error'
        '501':
          description: Feature not installed
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/FeatureNotInstalledError'
  /api/v1/tools/image/barcode-generate:
    post:
      operationId: generateBarcode
      tags:
        - Tools
      summary: Gerador de código de barras
      description: Gera uma imagem de código de barras a partir de texto. Esta ferramenta aceita um corpo JSON, não multipart.
      security:
        - bearerAuth: []
      requestBody:
        required: true
        content:
          application/json:
            schema:
              type: object
              required:
                - text
              properties:
                text:
                  type: string
                  minLength: 1
                  maxLength: 256
                  description: Text to encode in the barcode
                type:
                  type: string
                  enum:
                    - code128
                    - ean13
                    - upca
                    - code39
                    - itf14
                    - datamatrix
                  default: code128
                  description: Barcode format
                scale:
                  type: integer
                  minimum: 1
                  maximum: 8
                  default: 3
                  description: Image scale factor
                includeText:
                  type: boolean
                  default: true
                  description: Whether to render the text below the barcode
      responses:
        '200':
          description: Generated barcode
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ToolResponse'
        '400':
          description: Invalid input
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Error'
        '401':
          description: Authentication required
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/UnauthorizedError'
  /api/v1/tools/image/circle-crop:
    post:
      operationId: circleCrop
      tags:
        - Tools
      summary: Recorte circular
      description: Recorta uma imagem em um círculo centralizado com cantos transparentes, borda opcional e zoom/deslocamento ajustáveis.
      security:
        - bearerAuth: []
      requestBody:
        required: true
        content:
          multipart/form-data:
            schema:
              type: object
              required:
                - file
              properties:
                file:
                  type: string
                  format: binary
                  description: Image file to process
                settings:
                  type: string
                  description: |
                    JSON string with options:
                    - `zoom` (number 1-5, default 1) - Zoom factor (>=1 crops tighter)
                    - `offsetX` (number 0-1, default 0.5) - Horizontal center position
                    - `offsetY` (number 0-1, default 0.5) - Vertical center position
                    - `borderWidth` (integer 0-200, default 0) - Border width in pixels
                    - `borderColor` (string, default "#ffffff") - Border hex color
                    - `background` (string, default "transparent") - "transparent" or a hex color for corners
                    - `outputSize` (integer 16-4096, optional) - Final square dimension in pixels
      responses:
        '200':
          description: Processed image
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ToolResponse'
        '400':
          description: Invalid input
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Error'
        '401':
          description: Authentication required
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/UnauthorizedError'
  /api/v1/tools/image/rounded-crop:
    post:
      operationId: roundedCrop
      tags:
        - Tools
      summary: Rounded crop
      description: Crop an image to a rounded square or squircle with transparent corners, optional border, and adjustable zoom/offset.
      security:
        - bearerAuth: []
      requestBody:
        required: true
        content:
          multipart/form-data:
            schema:
              type: object
              required:
                - file
              properties:
                file:
                  type: string
                  format: binary
                  description: Image file to process
                settings:
                  type: string
                  description: |
                    JSON string with options:
                    - `shape` (string, default "rounded-square") - "rounded-square" or "squircle"
                    - `cornerRadius` (number 0-50, default 25) - Corner radius as a percent of the shorter side (rounded-square only)
                    - `zoom` (number 1-5, default 1) - Zoom factor (>=1 crops tighter)
                    - `offsetX` (number 0-1, default 0.5) - Horizontal center position
                    - `offsetY` (number 0-1, default 0.5) - Vertical center position
                    - `borderWidth` (integer 0-200, default 0) - Border width in pixels
                    - `borderColor` (string, default "#ffffff") - Border hex color
                    - `background` (string, default "transparent") - "transparent" or a hex color for corners
                    - `outputSize` (integer 16-4096, optional) - Final square dimension in pixels
      responses:
        '200':
          description: Processed image
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ToolResponse'
        '400':
          description: Invalid input
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Error'
        '401':
          description: Authentication required
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/UnauthorizedError'
  /api/v1/tools/image/duotone:
    post:
      operationId: duotone
      tags:
        - Tools
      summary: Duotone
      description: Aplica um efeito duotone de duas cores com cores personalizadas de sombra e realce.
      security:
        - bearerAuth: []
      requestBody:
        required: true
        content:
          multipart/form-data:
            schema:
              type: object
              required:
                - file
              properties:
                file:
                  type: string
                  format: binary
                  description: Image file to process
                settings:
                  type: string
                  description: |
                    JSON string with options:
                    - `shadow` (string, default "#1e3a8a") - Shadow hex color
                    - `highlight` (string, default "#fbbf24") - Highlight hex color
                    - `intensity` (integer 0-100, default 100) - Effect intensity (0 = original, 100 = full duotone)
      responses:
        '200':
          description: Processed image
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ToolResponse'
        '400':
          description: Invalid input
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Error'
        '401':
          description: Authentication required
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/UnauthorizedError'
  /api/v1/tools/image/image-pad:
    post:
      operationId: imagePad
      tags:
        - Tools
      summary: Preenchimento de imagem
      description: Preenche uma imagem até uma proporção-alvo com fundo de cor sólida, transparente ou desfocado.
      security:
        - bearerAuth: []
      requestBody:
        required: true
        content:
          multipart/form-data:
            schema:
              type: object
              required:
                - file
              properties:
                file:
                  type: string
                  format: binary
                  description: Image file to process
                settings:
                  type: string
                  description: |
                    JSON string with options:
                    - `target` (string, default "1:1") - One of: 16:9, 9:16, 1:1, 4:3, 3:4, custom
                    - `ratioW` (integer 1-100, default 1) - Custom ratio width (when target is custom)
                    - `ratioH` (integer 1-100, default 1) - Custom ratio height (when target is custom)
                    - `background` (string, default "color") - One of: color, transparent, blur
                    - `color` (string, default "#ffffff") - Background hex color (when background is color)
                    - `padding` (integer 0-50, default 0) - Extra padding as percentage of canvas
      responses:
        '200':
          description: Processed image
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ToolResponse'
        '400':
          description: Invalid input
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Error'
        '401':
          description: Authentication required
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/UnauthorizedError'
  /api/v1/tools/image/pixelate:
    post:
      operationId: pixelate
      tags:
        - Tools
      summary: Pixelizar
      description: Aplica um efeito de pixelização à imagem inteira ou a uma região retangular específica.
      security:
        - bearerAuth: []
      requestBody:
        required: true
        content:
          multipart/form-data:
            schema:
              type: object
              required:
                - file
              properties:
                file:
                  type: string
                  format: binary
                  description: Image file to process
                settings:
                  type: string
                  description: |
                    JSON string with options:
                    - `blockSize` (integer 2-128, default 12) - Pixel block size
                    - `region` (object, optional) - Restrict pixelation to a rectangle:
                      - `left` (integer >= 0) - Left offset in pixels
                      - `top` (integer >= 0) - Top offset in pixels
                      - `width` (integer >= 1) - Region width in pixels
                      - `height` (integer >= 1) - Region height in pixels
      responses:
        '200':
          description: Processed image
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ToolResponse'
        '400':
          description: Invalid input
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Error'
        '401':
          description: Authentication required
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/UnauthorizedError'
  /api/v1/tools/image/vignette:
    post:
      operationId: vignette
      tags:
        - Tools
      summary: Vinheta
      description: Adiciona um efeito de vinheta com intensidade, cor, raio, suavidade, arredondamento e posição do centro ajustáveis.
      security:
        - bearerAuth: []
      requestBody:
        required: true
        content:
          multipart/form-data:
            schema:
              type: object
              required:
                - file
              properties:
                file:
                  type: string
                  format: binary
                  description: Image file to process
                settings:
                  type: string
                  description: |
                    JSON string with options:
                    - `strength` (number 0.1-1, default 0.5) - Vignette opacity
                    - `color` (string, default "#000000") - Vignette hex color
                    - `radius` (integer 0-100, default 70) - Outer radius as percentage of half-diagonal
                    - `softness` (integer 0-100, default 50) - Feather softness (higher = more gradual)
                    - `roundness` (integer 0-100, default 100) - 100 = circle, 0 = ellipse matching image aspect ratio
                    - `centerX` (integer 0-100, default 50) - Horizontal center position as percentage
                    - `centerY` (integer 0-100, default 50) - Vertical center position as percentage
      responses:
        '200':
          description: Processed image
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ToolResponse'
        '400':
          description: Invalid input
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Error'
        '401':
          description: Authentication required
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/UnauthorizedError'
  /api/v1/tools/image/gif-webp:
    post:
      operationId: gifWebp
      tags:
        - Tools
      summary: Conversor GIF/WebP
      description: Converte GIF animado para WebP e vice-versa, preservando todos os quadros. Aceita apenas entradas .gif e .webp.
      security:
        - bearerAuth: []
      requestBody:
        required: true
        content:
          multipart/form-data:
            schema:
              type: object
              required:
                - file
              properties:
                file:
                  type: string
                  format: binary
                  description: GIF or WebP file to convert
                settings:
                  type: string
                  description: |
                    JSON string with options:
                    - `quality` (integer 1-100, default 80) - Output quality for WebP encoding
                    - `lossless` (boolean, default false) - Use lossless WebP compression
                    - `resizePercent` (integer 10-100, default 100) - Scale the output by percentage
      responses:
        '200':
          description: Converted animation
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ToolResponse'
        '400':
          description: Invalid input
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Error'
        '401':
          description: Authentication required
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/UnauthorizedError'
  /api/v1/tools/image/histogram:
    post:
      operationId: histogram
      tags:
        - Tools
      summary: Histograma
      description: |
        Gera um gráfico de histograma RGB a partir de uma imagem. Retorna uma imagem PNG de histograma
        mais estatísticas por canal e dados brutos de histograma de 256 bins no
        JSON da resposta.
      security:
        - bearerAuth: []
      requestBody:
        required: true
        content:
          multipart/form-data:
            schema:
              type: object
              required:
                - file
              properties:
                file:
                  type: string
                  format: binary
                  description: Image file to analyze
                settings:
                  type: string
                  description: |
                    JSON string with options:
                    - `scale` (string, default "linear") - One of: linear, log
      responses:
        '200':
          description: Histogram image and channel statistics
          content:
            application/json:
              schema:
                allOf:
                  - $ref: '#/components/schemas/ToolResponse'
                  - type: object
                    properties:
                      bins:
                        type: object
                        description: Raw 256-bin histogram arrays per channel
                        properties:
                          r:
                            type: array
                            items:
                              type: integer
                          g:
                            type: array
                            items:
                              type: integer
                          b:
                            type: array
                            items:
                              type: integer
                          lum:
                            type: array
                            items:
                              type: integer
                      stats:
                        type: object
                        description: Per-channel statistics
                        properties:
                          r:
                            type: object
                            properties:
                              mean:
                                type: integer
                              median:
                                type: integer
                              stdev:
                                type: number
                          g:
                            type: object
                            properties:
                              mean:
                                type: integer
                              median:
                                type: integer
                              stdev:
                                type: number
                          b:
                            type: object
                            properties:
                              mean:
                                type: integer
                              median:
                                type: integer
                              stdev:
                                type: number
                          lum:
                            type: object
                            properties:
                              mean:
                                type: integer
                              median:
                                type: integer
                              stdev:
                                type: number
                      mean:
                        type: object
                        description: Backward-compatible mean values
                        properties:
                          r:
                            type: integer
                          g:
                            type: integer
                          b:
                            type: integer
                      max:
                        type: object
                        description: Backward-compatible max bin values
                        properties:
                          r:
                            type: integer
                          g:
                            type: integer
                          b:
                            type: integer
        '400':
          description: Invalid input
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Error'
        '401':
          description: Authentication required
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/UnauthorizedError'
  /api/v1/tools/image/lqip-placeholder:
    post:
      operationId: lqipPlaceholder
      tags:
        - Tools
      summary: Placeholder LQIP
      description: |
        Gera um placeholder de imagem de baixa qualidade minúsculo. Retorna um pequeno arquivo
        de imagem mais um data URI base64, uma tag img HTML pronta para uso e um trecho
        CSS de background no JSON da resposta.
      security:
        - bearerAuth: []
      requestBody:
        required: true
        content:
          multipart/form-data:
            schema:
              type: object
              required:
                - file
              properties:
                file:
                  type: string
                  format: binary
                  description: Image file to process
                settings:
                  type: string
                  description: |
                    JSON string with options:
                    - `width` (integer 4-64, default 16) - Target width in pixels
                    - `blur` (number 0-20, default 2) - Blur radius (for blur strategy)
                    - `strategy` (string, default "blur") - One of: blur, pixelate, solid
                    - `format` (string, default "webp") - One of: webp, png, jpeg
                    - `quality` (integer 1-100, default 50) - Output quality
      responses:
        '200':
          description: LQIP placeholder image and data URI
          content:
            application/json:
              schema:
                allOf:
                  - $ref: '#/components/schemas/ToolResponse'
                  - type: object
                    properties:
                      dataUri:
                        type: string
                        description: Complete data URI (data:mime;base64,...)
                      width:
                        type: integer
                        description: Placeholder width in pixels
                      height:
                        type: integer
                        description: Placeholder height in pixels
                      bytes:
                        type: integer
                        description: Placeholder file size in bytes
                      strategy:
                        type: string
                        description: Strategy used (blur, pixelate, or solid)
                      html:
                        type: string
                        description: Ready-to-use HTML img tag
                      css:
                        type: string
                        description: Ready-to-use CSS background-image snippet
        '400':
          description: Invalid input
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Error'
        '401':
          description: Authentication required
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/UnauthorizedError'
  /api/v1/tools/image/sprite-sheet:
    post:
      operationId: spriteSheet
      tags:
        - Tools
      summary: Sprite sheet
      description: |
        Combina várias imagens em uma única grade de sprite sheet. Aceita
        de 2 a 64 imagens. Cada imagem é redimensionada para corresponder às
        dimensões da primeira imagem. Retorna a imagem do sprite sheet mais os
        metadados de coordenadas dos quadros.
      security:
        - bearerAuth: []
      requestBody:
        required: true
        content:
          multipart/form-data:
            schema:
              type: object
              required:
                - file
              properties:
                file:
                  type: array
                  items:
                    type: string
                    format: binary
                  description: Two or more image files to combine (max 64)
                settings:
                  type: string
                  description: |
                    JSON string with options:
                    - `columns` (integer 1-16, default 4) - Number of columns in the grid
                    - `padding` (integer 0-64, default 0) - Padding between cells in pixels
                    - `background` (string, default "#ffffff") - Background hex color
                    - `format` (string, default "png") - One of: png, webp, jpeg
                    - `quality` (integer 1-100, default 90) - Output quality
      responses:
        '200':
          description: Sprite sheet image with frame metadata
          content:
            application/json:
              schema:
                allOf:
                  - $ref: '#/components/schemas/ToolResponse'
                  - type: object
                    properties:
                      frames:
                        type: array
                        description: Per-frame coordinates in the sprite sheet
                        items:
                          type: object
                          properties:
                            index:
                              type: integer
                            left:
                              type: integer
                            top:
                              type: integer
                            width:
                              type: integer
                            height:
                              type: integer
                      cols:
                        type: integer
                        description: Number of columns
                      rows:
                        type: integer
                        description: Number of rows
                      cellWidth:
                        type: integer
                        description: Cell width in pixels
                      cellHeight:
                        type: integer
                        description: Cell height in pixels
                      canvasWidth:
                        type: integer
                        description: Total canvas width in pixels
                      canvasHeight:
                        type: integer
                        description: Total canvas height in pixels
        '400':
          description: Invalid input
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Error'
        '401':
          description: Authentication required
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/UnauthorizedError'
  /api/v1/tools/image/passport-photo:
    post:
      operationId: passportPhotoTool
      tags:
        - Tools
      summary: Foto de Passaporte
      description: |
        Endpoint de orientação para o fluxo de foto de passaporte em duas fases. Use
        `/api/v1/tools/image/passport-photo/analyze` para analisar uma imagem,
        depois `/api/v1/tools/image/passport-photo/generate` para criar a foto de
        passaporte final. Esta rota base retorna um erro de orientação 400 quando os
        bundles de recurso necessários estão instalados, ou 501 quando estão ausentes.
      security:
        - bearerAuth: []
      responses:
        '400':
          description: Use the analyze or generate endpoint
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Error'
        '401':
          description: Authentication required
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/UnauthorizedError'
        '501':
          description: Feature not installed
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/FeatureNotInstalledError'
  /api/v1/tools/pdf/sign-pdf:
    post:
      operationId: signPdfTool
      tags:
        - Tools
      summary: Assinar PDF
      description: Carimba uma ou mais imagens PNG de assinatura em um PDF usando posicionamentos de página normalizados.
      security:
        - bearerAuth: []
      requestBody:
        required: true
        content:
          multipart/form-data:
            schema:
              type: object
              required:
                - file
                - placements
                - sig0
              properties:
                file:
                  type: string
                  format: binary
                  description: PDF file to sign
                sig0:
                  type: string
                  format: binary
                  description: First signature image. Additional signatures use sig1, sig2, and so on.
                placements:
                  type: string
                  description: JSON array of placement objects with sig, page, x, y, w, and h. Coordinates are page fractions with top-left origin.
                clientJobId:
                  type: string
                  description: Client-provided job ID for SSE progress tracking
                fileId:
                  type: string
                  description: Optional file library ID to save the signed result as a new version
      responses:
        '200':
          description: Signed PDF
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ToolResponse'
        '202':
          description: Accepted for async processing. Track progress via SSE at /api/v1/jobs/{jobId}/progress.
          content:
            application/json:
              schema:
                type: object
                properties:
                  jobId:
                    type: string
                  async:
                    type: boolean
                    example: true
        '400':
          description: Invalid input or settings
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Error'
        '401':
          description: Authentication required
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/UnauthorizedError'
        '422':
          description: Processing failed
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Error'
  /api/v1/tools/image/jpg-to-png:
    post:
      operationId: jpgToPngTool
      tags:
        - Tools
      summary: JPG para PNG
      description: |
        Converte JPG para PNG
        Este é um endpoint dedicado de preset de conversão, com base em `convert`.
      security:
        - bearerAuth: []
      requestBody:
        required: true
        content:
          multipart/form-data:
            schema:
              type: object
              required:
                - file
              properties:
                file:
                  type: string
                  format: binary
                  description: JPG to PNG input file (.jpg, .jpeg)
                settings:
                  type: string
                  description: |
                    Dedicated JPG to PNG converter using the convert pipeline.
                    Optional settings: quality (number 1-100). The output format is locked by the endpoint.
                clientJobId:
                  type: string
                  description: Client-provided job ID for SSE progress tracking
      responses:
        '200':
          description: Processed file
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ToolResponse'
        '400':
          description: Invalid input or settings
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Error'
        '401':
          description: Authentication required
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/UnauthorizedError'
        '422':
          description: Processing failed
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Error'
  /api/v1/tools/image/png-to-jpg:
    post:
      operationId: pngToJpgTool
      tags:
        - Tools
      summary: PNG para JPG
      description: |
        Converte PNG para JPG
        Este é um endpoint dedicado de preset de conversão, com base em `convert`.
      security:
        - bearerAuth: []
      requestBody:
        required: true
        content:
          multipart/form-data:
            schema:
              type: object
              required:
                - file
              properties:
                file:
                  type: string
                  format: binary
                  description: PNG to JPG input file (.png)
                settings:
                  type: string
                  description: |
                    Dedicated PNG to JPG converter using the convert pipeline.
                    Optional settings: quality (number 1-100). The output format is locked by the endpoint.
                clientJobId:
                  type: string
                  description: Client-provided job ID for SSE progress tracking
      responses:
        '200':
          description: Processed file
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ToolResponse'
        '400':
          description: Invalid input or settings
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Error'
        '401':
          description: Authentication required
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/UnauthorizedError'
        '422':
          description: Processing failed
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Error'
  /api/v1/tools/image/jpg-to-webp:
    post:
      operationId: jpgToWebpTool
      tags:
        - Tools
      summary: JPG para WebP
      description: |
        Converte JPG para WebP
        Este é um endpoint dedicado de preset de conversão, com base em `convert`.
      security:
        - bearerAuth: []
      requestBody:
        required: true
        content:
          multipart/form-data:
            schema:
              type: object
              required:
                - file
              properties:
                file:
                  type: string
                  format: binary
                  description: JPG to WebP input file (.jpg, .jpeg)
                settings:
                  type: string
                  description: |
                    Dedicated JPG to WebP converter using the convert pipeline.
                    Optional settings: quality (number 1-100). The output format is locked by the endpoint.
                clientJobId:
                  type: string
                  description: Client-provided job ID for SSE progress tracking
      responses:
        '200':
          description: Processed file
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ToolResponse'
        '400':
          description: Invalid input or settings
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Error'
        '401':
          description: Authentication required
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/UnauthorizedError'
        '422':
          description: Processing failed
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Error'
  /api/v1/tools/image/png-to-webp:
    post:
      operationId: pngToWebpTool
      tags:
        - Tools
      summary: PNG para WebP
      description: |
        Converte PNG para WebP
        Este é um endpoint dedicado de preset de conversão, com base em `convert`.
      security:
        - bearerAuth: []
      requestBody:
        required: true
        content:
          multipart/form-data:
            schema:
              type: object
              required:
                - file
              properties:
                file:
                  type: string
                  format: binary
                  description: PNG to WebP input file (.png)
                settings:
                  type: string
                  description: |
                    Dedicated PNG to WebP converter using the convert pipeline.
                    Optional settings: quality (number 1-100). The output format is locked by the endpoint.
                clientJobId:
                  type: string
                  description: Client-provided job ID for SSE progress tracking
      responses:
        '200':
          description: Processed file
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ToolResponse'
        '400':
          description: Invalid input or settings
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Error'
        '401':
          description: Authentication required
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/UnauthorizedError'
        '422':
          description: Processing failed
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Error'
  /api/v1/tools/image/webp-to-jpg:
    post:
      operationId: webpToJpgTool
      tags:
        - Tools
      summary: WebP para JPG
      description: |
        Converte WebP para JPG
        Este é um endpoint dedicado de preset de conversão, com base em `convert`.
      security:
        - bearerAuth: []
      requestBody:
        required: true
        content:
          multipart/form-data:
            schema:
              type: object
              required:
                - file
              properties:
                file:
                  type: string
                  format: binary
                  description: WebP to JPG input file (.webp)
                settings:
                  type: string
                  description: |
                    Dedicated WebP to JPG converter using the convert pipeline.
                    Optional settings: quality (number 1-100). The output format is locked by the endpoint.
                clientJobId:
                  type: string
                  description: Client-provided job ID for SSE progress tracking
      responses:
        '200':
          description: Processed file
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ToolResponse'
        '400':
          description: Invalid input or settings
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Error'
        '401':
          description: Authentication required
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/UnauthorizedError'
        '422':
          description: Processing failed
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Error'
  /api/v1/tools/image/webp-to-png:
    post:
      operationId: webpToPngTool
      tags:
        - Tools
      summary: WebP para PNG
      description: |
        Converte WebP para PNG
        Este é um endpoint dedicado de preset de conversão, com base em `convert`.
      security:
        - bearerAuth: []
      requestBody:
        required: true
        content:
          multipart/form-data:
            schema:
              type: object
              required:
                - file
              properties:
                file:
                  type: string
                  format: binary
                  description: WebP to PNG input file (.webp)
                settings:
                  type: string
                  description: |
                    Dedicated WebP to PNG converter using the convert pipeline.
                    Optional settings: quality (number 1-100). The output format is locked by the endpoint.
                clientJobId:
                  type: string
                  description: Client-provided job ID for SSE progress tracking
      responses:
        '200':
          description: Processed file
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ToolResponse'
        '400':
          description: Invalid input or settings
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Error'
        '401':
          description: Authentication required
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/UnauthorizedError'
        '422':
          description: Processing failed
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Error'
  /api/v1/tools/image/jpg-to-avif:
    post:
      operationId: jpgToAvifTool
      tags:
        - Tools
      summary: JPG para AVIF
      description: |
        Converte JPG para AVIF
        Este é um endpoint dedicado de preset de conversão, com base em `convert`.
      security:
        - bearerAuth: []
      requestBody:
        required: true
        content:
          multipart/form-data:
            schema:
              type: object
              required:
                - file
              properties:
                file:
                  type: string
                  format: binary
                  description: JPG to AVIF input file (.jpg, .jpeg)
                settings:
                  type: string
                  description: |
                    Dedicated JPG to AVIF converter using the convert pipeline.
                    Optional settings: quality (number 1-100). The output format is locked by the endpoint.
                clientJobId:
                  type: string
                  description: Client-provided job ID for SSE progress tracking
      responses:
        '200':
          description: Processed file
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ToolResponse'
        '400':
          description: Invalid input or settings
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Error'
        '401':
          description: Authentication required
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/UnauthorizedError'
        '422':
          description: Processing failed
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Error'
  /api/v1/tools/image/png-to-avif:
    post:
      operationId: pngToAvifTool
      tags:
        - Tools
      summary: PNG para AVIF
      description: |
        Converte PNG para AVIF
        Este é um endpoint dedicado de preset de conversão, com base em `convert`.
      security:
        - bearerAuth: []
      requestBody:
        required: true
        content:
          multipart/form-data:
            schema:
              type: object
              required:
                - file
              properties:
                file:
                  type: string
                  format: binary
                  description: PNG to AVIF input file (.png)
                settings:
                  type: string
                  description: |
                    Dedicated PNG to AVIF converter using the convert pipeline.
                    Optional settings: quality (number 1-100). The output format is locked by the endpoint.
                clientJobId:
                  type: string
                  description: Client-provided job ID for SSE progress tracking
      responses:
        '200':
          description: Processed file
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ToolResponse'
        '400':
          description: Invalid input or settings
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Error'
        '401':
          description: Authentication required
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/UnauthorizedError'
        '422':
          description: Processing failed
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Error'
  /api/v1/tools/image/webp-to-avif:
    post:
      operationId: webpToAvifTool
      tags:
        - Tools
      summary: WebP para AVIF
      description: |
        Converte WebP para AVIF
        Este é um endpoint dedicado de preset de conversão, com base em `convert`.
      security:
        - bearerAuth: []
      requestBody:
        required: true
        content:
          multipart/form-data:
            schema:
              type: object
              required:
                - file
              properties:
                file:
                  type: string
                  format: binary
                  description: WebP to AVIF input file (.webp)
                settings:
                  type: string
                  description: |
                    Dedicated WebP to AVIF converter using the convert pipeline.
                    Optional settings: quality (number 1-100). The output format is locked by the endpoint.
                clientJobId:
                  type: string
                  description: Client-provided job ID for SSE progress tracking
      responses:
        '200':
          description: Processed file
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ToolResponse'
        '400':
          description: Invalid input or settings
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Error'
        '401':
          description: Authentication required
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/UnauthorizedError'
        '422':
          description: Processing failed
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Error'
  /api/v1/tools/image/heic-to-jpg:
    post:
      operationId: heicToJpgTool
      tags:
        - Tools
      summary: HEIC para JPG
      description: |
        Converte HEIC para JPG
        Este é um endpoint dedicado de preset de conversão, com base em `convert`.
      security:
        - bearerAuth: []
      requestBody:
        required: true
        content:
          multipart/form-data:
            schema:
              type: object
              required:
                - file
              properties:
                file:
                  type: string
                  format: binary
                  description: HEIC to JPG input file (.heic, .heif)
                settings:
                  type: string
                  description: |
                    Dedicated HEIC to JPG converter using the convert pipeline.
                    Optional settings: quality (number 1-100). The output format is locked by the endpoint.
                clientJobId:
                  type: string
                  description: Client-provided job ID for SSE progress tracking
      responses:
        '200':
          description: Processed file
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ToolResponse'
        '400':
          description: Invalid input or settings
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Error'
        '401':
          description: Authentication required
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/UnauthorizedError'
        '422':
          description: Processing failed
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Error'
  /api/v1/tools/image/heic-to-png:
    post:
      operationId: heicToPngTool
      tags:
        - Tools
      summary: HEIC para PNG
      description: |
        Converte HEIC para PNG
        Este é um endpoint dedicado de preset de conversão, com base em `convert`.
      security:
        - bearerAuth: []
      requestBody:
        required: true
        content:
          multipart/form-data:
            schema:
              type: object
              required:
                - file
              properties:
                file:
                  type: string
                  format: binary
                  description: HEIC to PNG input file (.heic, .heif)
                settings:
                  type: string
                  description: |
                    Dedicated HEIC to PNG converter using the convert pipeline.
                    Optional settings: quality (number 1-100). The output format is locked by the endpoint.
                clientJobId:
                  type: string
                  description: Client-provided job ID for SSE progress tracking
      responses:
        '200':
          description: Processed file
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ToolResponse'
        '400':
          description: Invalid input or settings
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Error'
        '401':
          description: Authentication required
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/UnauthorizedError'
        '422':
          description: Processing failed
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Error'
  /api/v1/tools/image/heic-to-avif:
    post:
      operationId: heicToAvifTool
      tags:
        - Tools
      summary: HEIC para AVIF
      description: |
        Converte HEIC para AVIF
        Este é um endpoint dedicado de preset de conversão, com base em `convert`.
      security:
        - bearerAuth: []
      requestBody:
        required: true
        content:
          multipart/form-data:
            schema:
              type: object
              required:
                - file
              properties:
                file:
                  type: string
                  format: binary
                  description: HEIC to AVIF input file (.heic, .heif)
                settings:
                  type: string
                  description: |
                    Dedicated HEIC to AVIF converter using the convert pipeline.
                    Optional settings: quality (number 1-100). The output format is locked by the endpoint.
                clientJobId:
                  type: string
                  description: Client-provided job ID for SSE progress tracking
      responses:
        '200':
          description: Processed file
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ToolResponse'
        '400':
          description: Invalid input or settings
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Error'
        '401':
          description: Authentication required
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/UnauthorizedError'
        '422':
          description: Processing failed
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Error'
  /api/v1/tools/image/jpg-to-gif:
    post:
      operationId: jpgToGifTool
      tags:
        - Tools
      summary: JPG para GIF
      description: |
        Converte JPG para GIF
        Este é um endpoint dedicado de preset de conversão, com base em `convert`.
      security:
        - bearerAuth: []
      requestBody:
        required: true
        content:
          multipart/form-data:
            schema:
              type: object
              required:
                - file
              properties:
                file:
                  type: string
                  format: binary
                  description: JPG to GIF input file (.jpg, .jpeg)
                settings:
                  type: string
                  description: |
                    Dedicated JPG to GIF converter using the convert pipeline.
                    Optional settings: quality (number 1-100). The output format is locked by the endpoint.
                clientJobId:
                  type: string
                  description: Client-provided job ID for SSE progress tracking
      responses:
        '200':
          description: Processed file
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ToolResponse'
        '400':
          description: Invalid input or settings
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Error'
        '401':
          description: Authentication required
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/UnauthorizedError'
        '422':
          description: Processing failed
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Error'
  /api/v1/tools/image/png-to-gif:
    post:
      operationId: pngToGifTool
      tags:
        - Tools
      summary: PNG para GIF
      description: |
        Converte PNG para GIF
        Este é um endpoint dedicado de preset de conversão, com base em `convert`.
      security:
        - bearerAuth: []
      requestBody:
        required: true
        content:
          multipart/form-data:
            schema:
              type: object
              required:
                - file
              properties:
                file:
                  type: string
                  format: binary
                  description: PNG to GIF input file (.png)
                settings:
                  type: string
                  description: |
                    Dedicated PNG to GIF converter using the convert pipeline.
                    Optional settings: quality (number 1-100). The output format is locked by the endpoint.
                clientJobId:
                  type: string
                  description: Client-provided job ID for SSE progress tracking
      responses:
        '200':
          description: Processed file
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ToolResponse'
        '400':
          description: Invalid input or settings
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Error'
        '401':
          description: Authentication required
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/UnauthorizedError'
        '422':
          description: Processing failed
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Error'
  /api/v1/tools/image/gif-to-jpg:
    post:
      operationId: gifToJpgTool
      tags:
        - Tools
      summary: GIF para JPG
      description: |
        Converte GIF para JPG
        Este é um endpoint dedicado de preset de conversão, com base em `convert`.
      security:
        - bearerAuth: []
      requestBody:
        required: true
        content:
          multipart/form-data:
            schema:
              type: object
              required:
                - file
              properties:
                file:
                  type: string
                  format: binary
                  description: GIF to JPG input file (.gif)
                settings:
                  type: string
                  description: |
                    Dedicated GIF to JPG converter using the convert pipeline.
                    Optional settings: quality (number 1-100). The output format is locked by the endpoint.
                clientJobId:
                  type: string
                  description: Client-provided job ID for SSE progress tracking
      responses:
        '200':
          description: Processed file
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ToolResponse'
        '400':
          description: Invalid input or settings
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Error'
        '401':
          description: Authentication required
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/UnauthorizedError'
        '422':
          description: Processing failed
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Error'
  /api/v1/tools/image/gif-to-png:
    post:
      operationId: gifToPngTool
      tags:
        - Tools
      summary: GIF para PNG
      description: |
        Converte GIF para PNG
        Este é um endpoint dedicado de preset de conversão, com base em `convert`.
      security:
        - bearerAuth: []
      requestBody:
        required: true
        content:
          multipart/form-data:
            schema:
              type: object
              required:
                - file
              properties:
                file:
                  type: string
                  format: binary
                  description: GIF to PNG input file (.gif)
                settings:
                  type: string
                  description: |
                    Dedicated GIF to PNG converter using the convert pipeline.
                    Optional settings: quality (number 1-100). The output format is locked by the endpoint.
                clientJobId:
                  type: string
                  description: Client-provided job ID for SSE progress tracking
      responses:
        '200':
          description: Processed file
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ToolResponse'
        '400':
          description: Invalid input or settings
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Error'
        '401':
          description: Authentication required
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/UnauthorizedError'
        '422':
          description: Processing failed
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Error'
  /api/v1/tools/image/webp-to-gif:
    post:
      operationId: webpToGifTool
      tags:
        - Tools
      summary: WebP para GIF
      description: |
        Converte WebP para GIF
        Este é um endpoint dedicado de preset de conversão, com base em `convert`.
      security:
        - bearerAuth: []
      requestBody:
        required: true
        content:
          multipart/form-data:
            schema:
              type: object
              required:
                - file
              properties:
                file:
                  type: string
                  format: binary
                  description: WebP to GIF input file (.webp)
                settings:
                  type: string
                  description: |
                    Dedicated WebP to GIF converter using the convert pipeline.
                    Optional settings: quality (number 1-100). The output format is locked by the endpoint.
                clientJobId:
                  type: string
                  description: Client-provided job ID for SSE progress tracking
      responses:
        '200':
          description: Processed file
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ToolResponse'
        '400':
          description: Invalid input or settings
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Error'
        '401':
          description: Authentication required
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/UnauthorizedError'
        '422':
          description: Processing failed
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Error'
  /api/v1/tools/image/jpg-to-tiff:
    post:
      operationId: jpgToTiffTool
      tags:
        - Tools
      summary: JPG para TIFF
      description: |
        Converte JPG para TIFF
        Este é um endpoint dedicado de preset de conversão, com base em `convert`.
      security:
        - bearerAuth: []
      requestBody:
        required: true
        content:
          multipart/form-data:
            schema:
              type: object
              required:
                - file
              properties:
                file:
                  type: string
                  format: binary
                  description: JPG to TIFF input file (.jpg, .jpeg)
                settings:
                  type: string
                  description: |
                    Dedicated JPG to TIFF converter using the convert pipeline.
                    Optional settings: quality (number 1-100). The output format is locked by the endpoint.
                clientJobId:
                  type: string
                  description: Client-provided job ID for SSE progress tracking
      responses:
        '200':
          description: Processed file
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ToolResponse'
        '400':
          description: Invalid input or settings
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Error'
        '401':
          description: Authentication required
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/UnauthorizedError'
        '422':
          description: Processing failed
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Error'
  /api/v1/tools/image/png-to-tiff:
    post:
      operationId: pngToTiffTool
      tags:
        - Tools
      summary: PNG para TIFF
      description: |
        Converte PNG para TIFF
        Este é um endpoint dedicado de preset de conversão, com base em `convert`.
      security:
        - bearerAuth: []
      requestBody:
        required: true
        content:
          multipart/form-data:
            schema:
              type: object
              required:
                - file
              properties:
                file:
                  type: string
                  format: binary
                  description: PNG to TIFF input file (.png)
                settings:
                  type: string
                  description: |
                    Dedicated PNG to TIFF converter using the convert pipeline.
                    Optional settings: quality (number 1-100). The output format is locked by the endpoint.
                clientJobId:
                  type: string
                  description: Client-provided job ID for SSE progress tracking
      responses:
        '200':
          description: Processed file
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ToolResponse'
        '400':
          description: Invalid input or settings
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Error'
        '401':
          description: Authentication required
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/UnauthorizedError'
        '422':
          description: Processing failed
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Error'
  /api/v1/tools/image/tiff-to-jpg:
    post:
      operationId: tiffToJpgTool
      tags:
        - Tools
      summary: TIFF para JPG
      description: |
        Converte TIFF para JPG
        Este é um endpoint dedicado de preset de conversão, com base em `convert`.
      security:
        - bearerAuth: []
      requestBody:
        required: true
        content:
          multipart/form-data:
            schema:
              type: object
              required:
                - file
              properties:
                file:
                  type: string
                  format: binary
                  description: TIFF to JPG input file (.tiff, .tif)
                settings:
                  type: string
                  description: |
                    Dedicated TIFF to JPG converter using the convert pipeline.
                    Optional settings: quality (number 1-100). The output format is locked by the endpoint.
                clientJobId:
                  type: string
                  description: Client-provided job ID for SSE progress tracking
      responses:
        '200':
          description: Processed file
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ToolResponse'
        '400':
          description: Invalid input or settings
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Error'
        '401':
          description: Authentication required
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/UnauthorizedError'
        '422':
          description: Processing failed
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Error'
  /api/v1/tools/image/tiff-to-png:
    post:
      operationId: tiffToPngTool
      tags:
        - Tools
      summary: TIFF para PNG
      description: |
        Converte TIFF para PNG
        Este é um endpoint dedicado de preset de conversão, com base em `convert`.
      security:
        - bearerAuth: []
      requestBody:
        required: true
        content:
          multipart/form-data:
            schema:
              type: object
              required:
                - file
              properties:
                file:
                  type: string
                  format: binary
                  description: TIFF to PNG input file (.tiff, .tif)
                settings:
                  type: string
                  description: |
                    Dedicated TIFF to PNG converter using the convert pipeline.
                    Optional settings: quality (number 1-100). The output format is locked by the endpoint.
                clientJobId:
                  type: string
                  description: Client-provided job ID for SSE progress tracking
      responses:
        '200':
          description: Processed file
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ToolResponse'
        '400':
          description: Invalid input or settings
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Error'
        '401':
          description: Authentication required
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/UnauthorizedError'
        '422':
          description: Processing failed
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Error'
  /api/v1/tools/image/psd-to-jpg:
    post:
      operationId: psdToJpgTool
      tags:
        - Tools
      summary: PSD para JPG
      description: |
        Converte PSD para JPG
        Este é um endpoint dedicado de preset de conversão, com base em `convert`.
      security:
        - bearerAuth: []
      requestBody:
        required: true
        content:
          multipart/form-data:
            schema:
              type: object
              required:
                - file
              properties:
                file:
                  type: string
                  format: binary
                  description: PSD to JPG input file (.psd)
                settings:
                  type: string
                  description: |
                    Dedicated PSD to JPG converter using the convert pipeline.
                    Optional settings: quality (number 1-100). The output format is locked by the endpoint.
                clientJobId:
                  type: string
                  description: Client-provided job ID for SSE progress tracking
      responses:
        '200':
          description: Processed file
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ToolResponse'
        '400':
          description: Invalid input or settings
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Error'
        '401':
          description: Authentication required
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/UnauthorizedError'
        '422':
          description: Processing failed
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Error'
  /api/v1/tools/image/psd-to-png:
    post:
      operationId: psdToPngTool
      tags:
        - Tools
      summary: PSD para PNG
      description: |
        Converte PSD para PNG
        Este é um endpoint dedicado de preset de conversão, com base em `convert`.
      security:
        - bearerAuth: []
      requestBody:
        required: true
        content:
          multipart/form-data:
            schema:
              type: object
              required:
                - file
              properties:
                file:
                  type: string
                  format: binary
                  description: PSD to PNG input file (.psd)
                settings:
                  type: string
                  description: |
                    Dedicated PSD to PNG converter using the convert pipeline.
                    Optional settings: quality (number 1-100). The output format is locked by the endpoint.
                clientJobId:
                  type: string
                  description: Client-provided job ID for SSE progress tracking
      responses:
        '200':
          description: Processed file
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ToolResponse'
        '400':
          description: Invalid input or settings
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Error'
        '401':
          description: Authentication required
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/UnauthorizedError'
        '422':
          description: Processing failed
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Error'
  /api/v1/tools/image/png-to-eps:
    post:
      operationId: pngToEpsTool
      tags:
        - Tools
      summary: PNG para EPS
      description: |
        Converte PNG para EPS
        Este é um endpoint dedicado de preset de conversão, com base em `convert`.
      security:
        - bearerAuth: []
      requestBody:
        required: true
        content:
          multipart/form-data:
            schema:
              type: object
              required:
                - file
              properties:
                file:
                  type: string
                  format: binary
                  description: PNG to EPS input file (.png)
                settings:
                  type: string
                  description: |
                    Dedicated PNG to EPS converter using the convert pipeline.
                    Optional settings: quality (number 1-100). The output format is locked by the endpoint.
                clientJobId:
                  type: string
                  description: Client-provided job ID for SSE progress tracking
      responses:
        '200':
          description: Processed file
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ToolResponse'
        '400':
          description: Invalid input or settings
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Error'
        '401':
          description: Authentication required
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/UnauthorizedError'
        '422':
          description: Processing failed
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Error'
  /api/v1/tools/image/jpg-to-eps:
    post:
      operationId: jpgToEpsTool
      tags:
        - Tools
      summary: JPG para EPS
      description: |
        Converte JPG para EPS
        Este é um endpoint dedicado de preset de conversão, com base em `convert`.
      security:
        - bearerAuth: []
      requestBody:
        required: true
        content:
          multipart/form-data:
            schema:
              type: object
              required:
                - file
              properties:
                file:
                  type: string
                  format: binary
                  description: JPG to EPS input file (.jpg, .jpeg)
                settings:
                  type: string
                  description: |
                    Dedicated JPG to EPS converter using the convert pipeline.
                    Optional settings: quality (number 1-100). The output format is locked by the endpoint.
                clientJobId:
                  type: string
                  description: Client-provided job ID for SSE progress tracking
      responses:
        '200':
          description: Processed file
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ToolResponse'
        '400':
          description: Invalid input or settings
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Error'
        '401':
          description: Authentication required
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/UnauthorizedError'
        '422':
          description: Processing failed
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Error'
  /api/v1/tools/image/eps-to-png:
    post:
      operationId: epsToPngTool
      tags:
        - Tools
      summary: EPS para PNG
      description: |
        Converte EPS para PNG
        Este é um endpoint dedicado de preset de conversão, com base em `convert`.
      security:
        - bearerAuth: []
      requestBody:
        required: true
        content:
          multipart/form-data:
            schema:
              type: object
              required:
                - file
              properties:
                file:
                  type: string
                  format: binary
                  description: EPS to PNG input file (.eps)
                settings:
                  type: string
                  description: |
                    Dedicated EPS to PNG converter using the convert pipeline.
                    Optional settings: quality (number 1-100). The output format is locked by the endpoint.
                clientJobId:
                  type: string
                  description: Client-provided job ID for SSE progress tracking
      responses:
        '200':
          description: Processed file
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ToolResponse'
        '400':
          description: Invalid input or settings
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Error'
        '401':
          description: Authentication required
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/UnauthorizedError'
        '422':
          description: Processing failed
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Error'
  /api/v1/tools/image/eps-to-jpg:
    post:
      operationId: epsToJpgTool
      tags:
        - Tools
      summary: EPS para JPG
      description: |
        Converte EPS para JPG
        Este é um endpoint dedicado de preset de conversão, com base em `convert`.
      security:
        - bearerAuth: []
      requestBody:
        required: true
        content:
          multipart/form-data:
            schema:
              type: object
              required:
                - file
              properties:
                file:
                  type: string
                  format: binary
                  description: EPS to JPG input file (.eps)
                settings:
                  type: string
                  description: |
                    Dedicated EPS to JPG converter using the convert pipeline.
                    Optional settings: quality (number 1-100). The output format is locked by the endpoint.
                clientJobId:
                  type: string
                  description: Client-provided job ID for SSE progress tracking
      responses:
        '200':
          description: Processed file
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ToolResponse'
        '400':
          description: Invalid input or settings
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Error'
        '401':
          description: Authentication required
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/UnauthorizedError'
        '422':
          description: Processing failed
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Error'
  /api/v1/tools/image/png-to-svg:
    post:
      operationId: pngToSvgTool
      tags:
        - Tools
      summary: PNG para SVG
      description: |
        Converte PNG para SVG
        Este é um endpoint dedicado de preset de conversão, com base em `vectorize`.
      security:
        - bearerAuth: []
      requestBody:
        required: true
        content:
          multipart/form-data:
            schema:
              type: object
              required:
                - file
              properties:
                file:
                  type: string
                  format: binary
                  description: PNG to SVG input file (.png)
                settings:
                  type: string
                  description: |
                    Dedicated PNG to SVG converter using the vectorize pipeline.
                    The output format is locked by the endpoint.
                clientJobId:
                  type: string
                  description: Client-provided job ID for SSE progress tracking
      responses:
        '200':
          description: Processed file
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ToolResponse'
        '400':
          description: Invalid input or settings
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Error'
        '401':
          description: Authentication required
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/UnauthorizedError'
        '422':
          description: Processing failed
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Error'
  /api/v1/tools/image/jpg-to-svg:
    post:
      operationId: jpgToSvgTool
      tags:
        - Tools
      summary: JPG para SVG
      description: |
        Converte JPG para SVG
        Este é um endpoint dedicado de preset de conversão, com base em `vectorize`.
      security:
        - bearerAuth: []
      requestBody:
        required: true
        content:
          multipart/form-data:
            schema:
              type: object
              required:
                - file
              properties:
                file:
                  type: string
                  format: binary
                  description: JPG to SVG input file (.jpg, .jpeg)
                settings:
                  type: string
                  description: |
                    Dedicated JPG to SVG converter using the vectorize pipeline.
                    The output format is locked by the endpoint.
                clientJobId:
                  type: string
                  description: Client-provided job ID for SSE progress tracking
      responses:
        '200':
          description: Processed file
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ToolResponse'
        '400':
          description: Invalid input or settings
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Error'
        '401':
          description: Authentication required
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/UnauthorizedError'
        '422':
          description: Processing failed
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Error'
  /api/v1/tools/image/tiff-to-svg:
    post:
      operationId: tiffToSvgTool
      tags:
        - Tools
      summary: TIFF para SVG
      description: |
        Converte TIFF para SVG
        Este é um endpoint dedicado de preset de conversão, com base em `vectorize`.
      security:
        - bearerAuth: []
      requestBody:
        required: true
        content:
          multipart/form-data:
            schema:
              type: object
              required:
                - file
              properties:
                file:
                  type: string
                  format: binary
                  description: TIFF to SVG input file (.tiff, .tif)
                settings:
                  type: string
                  description: |
                    Dedicated TIFF to SVG converter using the vectorize pipeline.
                    The output format is locked by the endpoint.
                clientJobId:
                  type: string
                  description: Client-provided job ID for SSE progress tracking
      responses:
        '200':
          description: Processed file
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ToolResponse'
        '400':
          description: Invalid input or settings
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Error'
        '401':
          description: Authentication required
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/UnauthorizedError'
        '422':
          description: Processing failed
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Error'
  /api/v1/tools/image/psd-to-svg:
    post:
      operationId: psdToSvgTool
      tags:
        - Tools
      summary: PSD para SVG
      description: |
        Converte PSD para SVG
        Este é um endpoint dedicado de preset de conversão, com base em `vectorize`.
      security:
        - bearerAuth: []
      requestBody:
        required: true
        content:
          multipart/form-data:
            schema:
              type: object
              required:
                - file
              properties:
                file:
                  type: string
                  format: binary
                  description: PSD to SVG input file (.psd)
                settings:
                  type: string
                  description: |
                    Dedicated PSD to SVG converter using the vectorize pipeline.
                    The output format is locked by the endpoint.
                clientJobId:
                  type: string
                  description: Client-provided job ID for SSE progress tracking
      responses:
        '200':
          description: Processed file
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ToolResponse'
        '400':
          description: Invalid input or settings
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Error'
        '401':
          description: Authentication required
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/UnauthorizedError'
        '422':
          description: Processing failed
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Error'
  /api/v1/tools/image/eps-to-svg:
    post:
      operationId: epsToSvgTool
      tags:
        - Tools
      summary: EPS para SVG
      description: |
        Converte EPS para SVG
        Este é um endpoint dedicado de preset de conversão, com base em `vectorize`.
      security:
        - bearerAuth: []
      requestBody:
        required: true
        content:
          multipart/form-data:
            schema:
              type: object
              required:
                - file
              properties:
                file:
                  type: string
                  format: binary
                  description: EPS to SVG input file (.eps)
                settings:
                  type: string
                  description: |
                    Dedicated EPS to SVG converter using the vectorize pipeline.
                    The output format is locked by the endpoint.
                clientJobId:
                  type: string
                  description: Client-provided job ID for SSE progress tracking
      responses:
        '200':
          description: Processed file
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ToolResponse'
        '400':
          description: Invalid input or settings
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Error'
        '401':
          description: Authentication required
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/UnauthorizedError'
        '422':
          description: Processing failed
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Error'
  /api/v1/tools/image/svg-to-png:
    post:
      operationId: svgToPngTool
      tags:
        - Tools
      summary: SVG para PNG
      description: |
        Converte SVG para PNG
        Este é um endpoint dedicado de preset de conversão, com base em `svg-to-raster`.
      security:
        - bearerAuth: []
      requestBody:
        required: true
        content:
          multipart/form-data:
            schema:
              type: object
              required:
                - file
              properties:
                file:
                  type: string
                  format: binary
                  description: SVG to PNG input file (.svg, .svgz)
                settings:
                  type: string
                  description: |
                    Dedicated SVG to PNG converter using the svg-to-raster pipeline.
                    Optional settings: quality (number 1-100). The output format is locked by the endpoint.
                clientJobId:
                  type: string
                  description: Client-provided job ID for SSE progress tracking
      responses:
        '200':
          description: Processed file
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ToolResponse'
        '400':
          description: Invalid input or settings
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Error'
        '401':
          description: Authentication required
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/UnauthorizedError'
        '422':
          description: Processing failed
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Error'
  /api/v1/tools/image/svg-to-jpg:
    post:
      operationId: svgToJpgTool
      tags:
        - Tools
      summary: SVG para JPG
      description: |
        Converte SVG para JPG
        Este é um endpoint dedicado de preset de conversão, com base em `svg-to-raster`.
      security:
        - bearerAuth: []
      requestBody:
        required: true
        content:
          multipart/form-data:
            schema:
              type: object
              required:
                - file
              properties:
                file:
                  type: string
                  format: binary
                  description: SVG to JPG input file (.svg, .svgz)
                settings:
                  type: string
                  description: |
                    Dedicated SVG to JPG converter using the svg-to-raster pipeline.
                    Optional settings: quality (number 1-100). The output format is locked by the endpoint.
                clientJobId:
                  type: string
                  description: Client-provided job ID for SSE progress tracking
      responses:
        '200':
          description: Processed file
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ToolResponse'
        '400':
          description: Invalid input or settings
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Error'
        '401':
          description: Authentication required
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/UnauthorizedError'
        '422':
          description: Processing failed
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Error'
  /api/v1/tools/image/jpg-to-pdf:
    post:
      operationId: jpgToPdfTool
      tags:
        - Tools
      summary: JPG para PDF
      description: |
        Converte JPG para PDF
        Este é um endpoint dedicado de preset de conversão, com base em `image-to-pdf`.
      security:
        - bearerAuth: []
      requestBody:
        required: true
        content:
          multipart/form-data:
            schema:
              type: object
              required:
                - file
              properties:
                file:
                  type: array
                  items:
                    type: string
                    format: binary
                  description: JPG to PDF input files (.jpg, .jpeg)
                settings:
                  type: string
                  description: |
                    Dedicated JPG to PDF converter using the image-to-pdf pipeline.
                    Optional settings: pageSize (A4, Letter, A3, A5), orientation (portrait or landscape), margin, targetSize, and collate.
                clientJobId:
                  type: string
                  description: Client-provided job ID for SSE progress tracking
      responses:
        '200':
          description: Processed file
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ToolResponse'
        '400':
          description: Invalid input or settings
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Error'
        '401':
          description: Authentication required
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/UnauthorizedError'
        '422':
          description: Processing failed
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Error'
  /api/v1/tools/image/png-to-pdf:
    post:
      operationId: pngToPdfTool
      tags:
        - Tools
      summary: PNG para PDF
      description: |
        Converte PNG para PDF
        Este é um endpoint dedicado de preset de conversão, com base em `image-to-pdf`.
      security:
        - bearerAuth: []
      requestBody:
        required: true
        content:
          multipart/form-data:
            schema:
              type: object
              required:
                - file
              properties:
                file:
                  type: array
                  items:
                    type: string
                    format: binary
                  description: PNG to PDF input files (.png)
                settings:
                  type: string
                  description: |
                    Dedicated PNG to PDF converter using the image-to-pdf pipeline.
                    Optional settings: pageSize (A4, Letter, A3, A5), orientation (portrait or landscape), margin, targetSize, and collate.
                clientJobId:
                  type: string
                  description: Client-provided job ID for SSE progress tracking
      responses:
        '200':
          description: Processed file
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ToolResponse'
        '400':
          description: Invalid input or settings
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Error'
        '401':
          description: Authentication required
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/UnauthorizedError'
        '422':
          description: Processing failed
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Error'
  /api/v1/tools/image/heic-to-pdf:
    post:
      operationId: heicToPdfTool
      tags:
        - Tools
      summary: HEIC para PDF
      description: |
        Converte HEIC para PDF
        Este é um endpoint dedicado de preset de conversão, com base em `image-to-pdf`.
      security:
        - bearerAuth: []
      requestBody:
        required: true
        content:
          multipart/form-data:
            schema:
              type: object
              required:
                - file
              properties:
                file:
                  type: array
                  items:
                    type: string
                    format: binary
                  description: HEIC to PDF input files (.heic, .heif)
                settings:
                  type: string
                  description: |
                    Dedicated HEIC to PDF converter using the image-to-pdf pipeline.
                    Optional settings: pageSize (A4, Letter, A3, A5), orientation (portrait or landscape), margin, targetSize, and collate.
                clientJobId:
                  type: string
                  description: Client-provided job ID for SSE progress tracking
      responses:
        '200':
          description: Processed file
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ToolResponse'
        '400':
          description: Invalid input or settings
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Error'
        '401':
          description: Authentication required
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/UnauthorizedError'
        '422':
          description: Processing failed
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Error'
  /api/v1/tools/image/tiff-to-pdf:
    post:
      operationId: tiffToPdfTool
      tags:
        - Tools
      summary: TIFF para PDF
      description: |
        Converte TIFF para PDF
        Este é um endpoint dedicado de preset de conversão, com base em `image-to-pdf`.
      security:
        - bearerAuth: []
      requestBody:
        required: true
        content:
          multipart/form-data:
            schema:
              type: object
              required:
                - file
              properties:
                file:
                  type: array
                  items:
                    type: string
                    format: binary
                  description: TIFF to PDF input files (.tiff, .tif)
                settings:
                  type: string
                  description: |
                    Dedicated TIFF to PDF converter using the image-to-pdf pipeline.
                    Optional settings: pageSize (A4, Letter, A3, A5), orientation (portrait or landscape), margin, targetSize, and collate.
                clientJobId:
                  type: string
                  description: Client-provided job ID for SSE progress tracking
      responses:
        '200':
          description: Processed file
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ToolResponse'
        '400':
          description: Invalid input or settings
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Error'
        '401':
          description: Authentication required
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/UnauthorizedError'
        '422':
          description: Processing failed
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Error'
  /api/v1/tools/image/webp-to-pdf:
    post:
      operationId: webpToPdfTool
      tags:
        - Tools
      summary: WebP para PDF
      description: |
        Converte WebP para PDF
        Este é um endpoint dedicado de preset de conversão, com base em `image-to-pdf`.
      security:
        - bearerAuth: []
      requestBody:
        required: true
        content:
          multipart/form-data:
            schema:
              type: object
              required:
                - file
              properties:
                file:
                  type: array
                  items:
                    type: string
                    format: binary
                  description: WebP to PDF input files (.webp)
                settings:
                  type: string
                  description: |
                    Dedicated WebP to PDF converter using the image-to-pdf pipeline.
                    Optional settings: pageSize (A4, Letter, A3, A5), orientation (portrait or landscape), margin, targetSize, and collate.
                clientJobId:
                  type: string
                  description: Client-provided job ID for SSE progress tracking
      responses:
        '200':
          description: Processed file
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ToolResponse'
        '400':
          description: Invalid input or settings
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Error'
        '401':
          description: Authentication required
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/UnauthorizedError'
        '422':
          description: Processing failed
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Error'
  /api/v1/tools/image/gif-to-pdf:
    post:
      operationId: gifToPdfTool
      tags:
        - Tools
      summary: GIF para PDF
      description: |
        Converte GIF para PDF
        Este é um endpoint dedicado de preset de conversão, com base em `image-to-pdf`.
      security:
        - bearerAuth: []
      requestBody:
        required: true
        content:
          multipart/form-data:
            schema:
              type: object
              required:
                - file
              properties:
                file:
                  type: array
                  items:
                    type: string
                    format: binary
                  description: GIF to PDF input files (.gif)
                settings:
                  type: string
                  description: |
                    Dedicated GIF to PDF converter using the image-to-pdf pipeline.
                    Optional settings: pageSize (A4, Letter, A3, A5), orientation (portrait or landscape), margin, targetSize, and collate.
                clientJobId:
                  type: string
                  description: Client-provided job ID for SSE progress tracking
      responses:
        '200':
          description: Processed file
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ToolResponse'
        '400':
          description: Invalid input or settings
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Error'
        '401':
          description: Authentication required
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/UnauthorizedError'
        '422':
          description: Processing failed
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Error'
  /api/v1/tools/image/eps-to-pdf:
    post:
      operationId: epsToPdfTool
      tags:
        - Tools
      summary: EPS para PDF
      description: |
        Converte EPS para PDF
        Este é um endpoint dedicado de preset de conversão, com base em `image-to-pdf`.
      security:
        - bearerAuth: []
      requestBody:
        required: true
        content:
          multipart/form-data:
            schema:
              type: object
              required:
                - file
              properties:
                file:
                  type: array
                  items:
                    type: string
                    format: binary
                  description: EPS to PDF input files (.eps)
                settings:
                  type: string
                  description: |
                    Dedicated EPS to PDF converter using the image-to-pdf pipeline.
                    Optional settings: pageSize (A4, Letter, A3, A5), orientation (portrait or landscape), margin, targetSize, and collate.
                clientJobId:
                  type: string
                  description: Client-provided job ID for SSE progress tracking
      responses:
        '200':
          description: Processed file
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ToolResponse'
        '400':
          description: Invalid input or settings
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Error'
        '401':
          description: Authentication required
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/UnauthorizedError'
        '422':
          description: Processing failed
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Error'
  /api/v1/tools/pdf/pdf-to-jpg:
    post:
      operationId: pdfToJpgTool
      tags:
        - Tools
      summary: PDF para JPG
      description: |
        Converte PDF para JPG
        Este é um endpoint dedicado de preset de conversão, com base em `pdf-to-image`.
      security:
        - bearerAuth: []
      requestBody:
        required: true
        content:
          multipart/form-data:
            schema:
              type: object
              required:
                - file
              properties:
                file:
                  type: string
                  format: binary
                  description: PDF to JPG input file (.pdf)
                settings:
                  type: string
                  description: |
                    Dedicated PDF to JPG converter using the pdf-to-image pipeline.
                    The output format is locked by the endpoint.
                clientJobId:
                  type: string
                  description: Client-provided job ID for SSE progress tracking
      responses:
        '200':
          description: Processed file
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ToolResponse'
        '400':
          description: Invalid input or settings
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Error'
        '401':
          description: Authentication required
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/UnauthorizedError'
        '422':
          description: Processing failed
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Error'
  /api/v1/tools/pdf/pdf-to-png:
    post:
      operationId: pdfToPngTool
      tags:
        - Tools
      summary: PDF para PNG
      description: |
        Converte PDF para PNG
        Este é um endpoint dedicado de preset de conversão, com base em `pdf-to-image`.
      security:
        - bearerAuth: []
      requestBody:
        required: true
        content:
          multipart/form-data:
            schema:
              type: object
              required:
                - file
              properties:
                file:
                  type: string
                  format: binary
                  description: PDF to PNG input file (.pdf)
                settings:
                  type: string
                  description: |
                    Dedicated PDF to PNG converter using the pdf-to-image pipeline.
                    The output format is locked by the endpoint.
                clientJobId:
                  type: string
                  description: Client-provided job ID for SSE progress tracking
      responses:
        '200':
          description: Processed file
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ToolResponse'
        '400':
          description: Invalid input or settings
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Error'
        '401':
          description: Authentication required
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/UnauthorizedError'
        '422':
          description: Processing failed
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Error'
  /api/v1/tools/pdf/pdf-to-tiff:
    post:
      operationId: pdfToTiffTool
      tags:
        - Tools
      summary: PDF para TIFF
      description: |
        Converte PDF para TIFF
        Este é um endpoint dedicado de preset de conversão, com base em `pdf-to-image`.
      security:
        - bearerAuth: []
      requestBody:
        required: true
        content:
          multipart/form-data:
            schema:
              type: object
              required:
                - file
              properties:
                file:
                  type: string
                  format: binary
                  description: PDF to TIFF input file (.pdf)
                settings:
                  type: string
                  description: |
                    Dedicated PDF to TIFF converter using the pdf-to-image pipeline.
                    The output format is locked by the endpoint.
                clientJobId:
                  type: string
                  description: Client-provided job ID for SSE progress tracking
      responses:
        '200':
          description: Processed file
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ToolResponse'
        '400':
          description: Invalid input or settings
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Error'
        '401':
          description: Authentication required
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/UnauthorizedError'
        '422':
          description: Processing failed
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Error'
  /api/v1/tools/video/mov-to-mp4:
    post:
      operationId: movToMp4Tool
      tags:
        - Tools
      summary: MOV para MP4
      description: |
        Converte MOV para MP4
        Este é um endpoint dedicado de preset de conversão, com base em `convert-video`.
      security:
        - bearerAuth: []
      requestBody:
        required: true
        content:
          multipart/form-data:
            schema:
              type: object
              required:
                - file
              properties:
                file:
                  type: string
                  format: binary
                  description: MOV to MP4 input file (.mov)
                settings:
                  type: string
                  description: |
                    Dedicated MOV to MP4 converter using the convert-video pipeline.
                    Optional settings: quality (high, balanced, small). The output format is locked by the endpoint.
                clientJobId:
                  type: string
                  description: Client-provided job ID for SSE progress tracking
      responses:
        '202':
          description: Accepted for async processing. Track progress via SSE at /api/v1/jobs/{jobId}/progress.
          content:
            application/json:
              schema:
                type: object
                properties:
                  jobId:
                    type: string
                  async:
                    type: boolean
                    example: true
        '400':
          description: Invalid input or settings
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Error'
        '401':
          description: Authentication required
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/UnauthorizedError'
        '422':
          description: Processing failed
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Error'
  /api/v1/tools/video/webm-to-mp4:
    post:
      operationId: webmToMp4Tool
      tags:
        - Tools
      summary: WEBM para MP4
      description: |
        Converte WEBM para MP4
        Este é um endpoint dedicado de preset de conversão, com base em `convert-video`.
      security:
        - bearerAuth: []
      requestBody:
        required: true
        content:
          multipart/form-data:
            schema:
              type: object
              required:
                - file
              properties:
                file:
                  type: string
                  format: binary
                  description: WEBM to MP4 input file (.webm)
                settings:
                  type: string
                  description: |
                    Dedicated WEBM to MP4 converter using the convert-video pipeline.
                    Optional settings: quality (high, balanced, small). The output format is locked by the endpoint.
                clientJobId:
                  type: string
                  description: Client-provided job ID for SSE progress tracking
      responses:
        '202':
          description: Accepted for async processing. Track progress via SSE at /api/v1/jobs/{jobId}/progress.
          content:
            application/json:
              schema:
                type: object
                properties:
                  jobId:
                    type: string
                  async:
                    type: boolean
                    example: true
        '400':
          description: Invalid input or settings
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Error'
        '401':
          description: Authentication required
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/UnauthorizedError'
        '422':
          description: Processing failed
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Error'
  /api/v1/tools/video/mkv-to-mp4:
    post:
      operationId: mkvToMp4Tool
      tags:
        - Tools
      summary: MKV para MP4
      description: |
        Converte MKV para MP4
        Este é um endpoint dedicado de preset de conversão, com base em `convert-video`.
      security:
        - bearerAuth: []
      requestBody:
        required: true
        content:
          multipart/form-data:
            schema:
              type: object
              required:
                - file
              properties:
                file:
                  type: string
                  format: binary
                  description: MKV to MP4 input file (.mkv)
                settings:
                  type: string
                  description: |
                    Dedicated MKV to MP4 converter using the convert-video pipeline.
                    Optional settings: quality (high, balanced, small). The output format is locked by the endpoint.
                clientJobId:
                  type: string
                  description: Client-provided job ID for SSE progress tracking
      responses:
        '202':
          description: Accepted for async processing. Track progress via SSE at /api/v1/jobs/{jobId}/progress.
          content:
            application/json:
              schema:
                type: object
                properties:
                  jobId:
                    type: string
                  async:
                    type: boolean
                    example: true
        '400':
          description: Invalid input or settings
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Error'
        '401':
          description: Authentication required
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/UnauthorizedError'
        '422':
          description: Processing failed
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Error'
  /api/v1/tools/video/avi-to-mp4:
    post:
      operationId: aviToMp4Tool
      tags:
        - Tools
      summary: AVI para MP4
      description: |
        Converte AVI para MP4
        Este é um endpoint dedicado de preset de conversão, com base em `convert-video`.
      security:
        - bearerAuth: []
      requestBody:
        required: true
        content:
          multipart/form-data:
            schema:
              type: object
              required:
                - file
              properties:
                file:
                  type: string
                  format: binary
                  description: AVI to MP4 input file (.avi)
                settings:
                  type: string
                  description: |
                    Dedicated AVI to MP4 converter using the convert-video pipeline.
                    Optional settings: quality (high, balanced, small). The output format is locked by the endpoint.
                clientJobId:
                  type: string
                  description: Client-provided job ID for SSE progress tracking
      responses:
        '202':
          description: Accepted for async processing. Track progress via SSE at /api/v1/jobs/{jobId}/progress.
          content:
            application/json:
              schema:
                type: object
                properties:
                  jobId:
                    type: string
                  async:
                    type: boolean
                    example: true
        '400':
          description: Invalid input or settings
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Error'
        '401':
          description: Authentication required
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/UnauthorizedError'
        '422':
          description: Processing failed
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Error'
  /api/v1/tools/video/mp4-to-mov:
    post:
      operationId: mp4ToMovTool
      tags:
        - Tools
      summary: MP4 para MOV
      description: |
        Converte MP4 para MOV
        Este é um endpoint dedicado de preset de conversão, com base em `convert-video`.
      security:
        - bearerAuth: []
      requestBody:
        required: true
        content:
          multipart/form-data:
            schema:
              type: object
              required:
                - file
              properties:
                file:
                  type: string
                  format: binary
                  description: MP4 to MOV input file (.mp4)
                settings:
                  type: string
                  description: |
                    Dedicated MP4 to MOV converter using the convert-video pipeline.
                    Optional settings: quality (high, balanced, small). The output format is locked by the endpoint.
                clientJobId:
                  type: string
                  description: Client-provided job ID for SSE progress tracking
      responses:
        '202':
          description: Accepted for async processing. Track progress via SSE at /api/v1/jobs/{jobId}/progress.
          content:
            application/json:
              schema:
                type: object
                properties:
                  jobId:
                    type: string
                  async:
                    type: boolean
                    example: true
        '400':
          description: Invalid input or settings
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Error'
        '401':
          description: Authentication required
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/UnauthorizedError'
        '422':
          description: Processing failed
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Error'
  /api/v1/tools/video/mp4-to-webm:
    post:
      operationId: mp4ToWebmTool
      tags:
        - Tools
      summary: MP4 para WEBM
      description: |
        Converte MP4 para WEBM
        Este é um endpoint dedicado de preset de conversão, com base em `convert-video`.
      security:
        - bearerAuth: []
      requestBody:
        required: true
        content:
          multipart/form-data:
            schema:
              type: object
              required:
                - file
              properties:
                file:
                  type: string
                  format: binary
                  description: MP4 to WEBM input file (.mp4)
                settings:
                  type: string
                  description: |
                    Dedicated MP4 to WEBM converter using the convert-video pipeline.
                    Optional settings: quality (high, balanced, small). The output format is locked by the endpoint.
                clientJobId:
                  type: string
                  description: Client-provided job ID for SSE progress tracking
      responses:
        '202':
          description: Accepted for async processing. Track progress via SSE at /api/v1/jobs/{jobId}/progress.
          content:
            application/json:
              schema:
                type: object
                properties:
                  jobId:
                    type: string
                  async:
                    type: boolean
                    example: true
        '400':
          description: Invalid input or settings
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Error'
        '401':
          description: Authentication required
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/UnauthorizedError'
        '422':
          description: Processing failed
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Error'
  /api/v1/tools/video/webm-to-mov:
    post:
      operationId: webmToMovTool
      tags:
        - Tools
      summary: WEBM para MOV
      description: |
        Converte WEBM para MOV
        Este é um endpoint dedicado de preset de conversão, com base em `convert-video`.
      security:
        - bearerAuth: []
      requestBody:
        required: true
        content:
          multipart/form-data:
            schema:
              type: object
              required:
                - file
              properties:
                file:
                  type: string
                  format: binary
                  description: WEBM to MOV input file (.webm)
                settings:
                  type: string
                  description: |
                    Dedicated WEBM to MOV converter using the convert-video pipeline.
                    Optional settings: quality (high, balanced, small). The output format is locked by the endpoint.
                clientJobId:
                  type: string
                  description: Client-provided job ID for SSE progress tracking
      responses:
        '202':
          description: Accepted for async processing. Track progress via SSE at /api/v1/jobs/{jobId}/progress.
          content:
            application/json:
              schema:
                type: object
                properties:
                  jobId:
                    type: string
                  async:
                    type: boolean
                    example: true
        '400':
          description: Invalid input or settings
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Error'
        '401':
          description: Authentication required
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/UnauthorizedError'
        '422':
          description: Processing failed
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Error'
  /api/v1/tools/video/mkv-to-mov:
    post:
      operationId: mkvToMovTool
      tags:
        - Tools
      summary: MKV para MOV
      description: |
        Converte MKV para MOV
        Este é um endpoint dedicado de preset de conversão, com base em `convert-video`.
      security:
        - bearerAuth: []
      requestBody:
        required: true
        content:
          multipart/form-data:
            schema:
              type: object
              required:
                - file
              properties:
                file:
                  type: string
                  format: binary
                  description: MKV to MOV input file (.mkv)
                settings:
                  type: string
                  description: |
                    Dedicated MKV to MOV converter using the convert-video pipeline.
                    Optional settings: quality (high, balanced, small). The output format is locked by the endpoint.
                clientJobId:
                  type: string
                  description: Client-provided job ID for SSE progress tracking
      responses:
        '202':
          description: Accepted for async processing. Track progress via SSE at /api/v1/jobs/{jobId}/progress.
          content:
            application/json:
              schema:
                type: object
                properties:
                  jobId:
                    type: string
                  async:
                    type: boolean
                    example: true
        '400':
          description: Invalid input or settings
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Error'
        '401':
          description: Authentication required
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/UnauthorizedError'
        '422':
          description: Processing failed
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Error'
  /api/v1/tools/video/avi-to-mov:
    post:
      operationId: aviToMovTool
      tags:
        - Tools
      summary: AVI para MOV
      description: |
        Converte AVI para MOV
        Este é um endpoint dedicado de preset de conversão, com base em `convert-video`.
      security:
        - bearerAuth: []
      requestBody:
        required: true
        content:
          multipart/form-data:
            schema:
              type: object
              required:
                - file
              properties:
                file:
                  type: string
                  format: binary
                  description: AVI to MOV input file (.avi)
                settings:
                  type: string
                  description: |
                    Dedicated AVI to MOV converter using the convert-video pipeline.
                    Optional settings: quality (high, balanced, small). The output format is locked by the endpoint.
                clientJobId:
                  type: string
                  description: Client-provided job ID for SSE progress tracking
      responses:
        '202':
          description: Accepted for async processing. Track progress via SSE at /api/v1/jobs/{jobId}/progress.
          content:
            application/json:
              schema:
                type: object
                properties:
                  jobId:
                    type: string
                  async:
                    type: boolean
                    example: true
        '400':
          description: Invalid input or settings
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Error'
        '401':
          description: Authentication required
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/UnauthorizedError'
        '422':
          description: Processing failed
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Error'
  /api/v1/tools/video/mp4-to-avi:
    post:
      operationId: mp4ToAviTool
      tags:
        - Tools
      summary: MP4 para AVI
      description: |
        Converte MP4 para AVI
        Este é um endpoint dedicado de preset de conversão, com base em `convert-video`.
      security:
        - bearerAuth: []
      requestBody:
        required: true
        content:
          multipart/form-data:
            schema:
              type: object
              required:
                - file
              properties:
                file:
                  type: string
                  format: binary
                  description: MP4 to AVI input file (.mp4)
                settings:
                  type: string
                  description: |
                    Dedicated MP4 to AVI converter using the convert-video pipeline.
                    Optional settings: quality (high, balanced, small). The output format is locked by the endpoint.
                clientJobId:
                  type: string
                  description: Client-provided job ID for SSE progress tracking
      responses:
        '202':
          description: Accepted for async processing. Track progress via SSE at /api/v1/jobs/{jobId}/progress.
          content:
            application/json:
              schema:
                type: object
                properties:
                  jobId:
                    type: string
                  async:
                    type: boolean
                    example: true
        '400':
          description: Invalid input or settings
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Error'
        '401':
          description: Authentication required
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/UnauthorizedError'
        '422':
          description: Processing failed
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Error'
  /api/v1/tools/video/mov-to-avi:
    post:
      operationId: movToAviTool
      tags:
        - Tools
      summary: MOV para AVI
      description: |
        Converte MOV para AVI
        Este é um endpoint dedicado de preset de conversão, com base em `convert-video`.
      security:
        - bearerAuth: []
      requestBody:
        required: true
        content:
          multipart/form-data:
            schema:
              type: object
              required:
                - file
              properties:
                file:
                  type: string
                  format: binary
                  description: MOV to AVI input file (.mov)
                settings:
                  type: string
                  description: |
                    Dedicated MOV to AVI converter using the convert-video pipeline.
                    Optional settings: quality (high, balanced, small). The output format is locked by the endpoint.
                clientJobId:
                  type: string
                  description: Client-provided job ID for SSE progress tracking
      responses:
        '202':
          description: Accepted for async processing. Track progress via SSE at /api/v1/jobs/{jobId}/progress.
          content:
            application/json:
              schema:
                type: object
                properties:
                  jobId:
                    type: string
                  async:
                    type: boolean
                    example: true
        '400':
          description: Invalid input or settings
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Error'
        '401':
          description: Authentication required
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/UnauthorizedError'
        '422':
          description: Processing failed
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Error'
  /api/v1/tools/video/mkv-to-avi:
    post:
      operationId: mkvToAviTool
      tags:
        - Tools
      summary: MKV para AVI
      description: |
        Converte MKV para AVI
        Este é um endpoint dedicado de preset de conversão, com base em `convert-video`.
      security:
        - bearerAuth: []
      requestBody:
        required: true
        content:
          multipart/form-data:
            schema:
              type: object
              required:
                - file
              properties:
                file:
                  type: string
                  format: binary
                  description: MKV to AVI input file (.mkv)
                settings:
                  type: string
                  description: |
                    Dedicated MKV to AVI converter using the convert-video pipeline.
                    Optional settings: quality (high, balanced, small). The output format is locked by the endpoint.
                clientJobId:
                  type: string
                  description: Client-provided job ID for SSE progress tracking
      responses:
        '202':
          description: Accepted for async processing. Track progress via SSE at /api/v1/jobs/{jobId}/progress.
          content:
            application/json:
              schema:
                type: object
                properties:
                  jobId:
                    type: string
                  async:
                    type: boolean
                    example: true
        '400':
          description: Invalid input or settings
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Error'
        '401':
          description: Authentication required
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/UnauthorizedError'
        '422':
          description: Processing failed
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Error'
  /api/v1/tools/video/avi-to-mkv:
    post:
      operationId: aviToMkvTool
      tags:
        - Tools
      summary: AVI para MKV
      description: |
        Converte AVI para MKV
        Este é um endpoint dedicado de preset de conversão, com base em `convert-video`.
      security:
        - bearerAuth: []
      requestBody:
        required: true
        content:
          multipart/form-data:
            schema:
              type: object
              required:
                - file
              properties:
                file:
                  type: string
                  format: binary
                  description: AVI to MKV input file (.avi)
                settings:
                  type: string
                  description: |
                    Dedicated AVI to MKV converter using the convert-video pipeline.
                    Optional settings: quality (high, balanced, small). The output format is locked by the endpoint.
                clientJobId:
                  type: string
                  description: Client-provided job ID for SSE progress tracking
      responses:
        '202':
          description: Accepted for async processing. Track progress via SSE at /api/v1/jobs/{jobId}/progress.
          content:
            application/json:
              schema:
                type: object
                properties:
                  jobId:
                    type: string
                  async:
                    type: boolean
                    example: true
        '400':
          description: Invalid input or settings
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Error'
        '401':
          description: Authentication required
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/UnauthorizedError'
        '422':
          description: Processing failed
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Error'
  /api/v1/tools/video/mp4-to-gif:
    post:
      operationId: mp4ToGifTool
      tags:
        - Tools
      summary: MP4 para GIF
      description: |
        Converte MP4 para GIF
        Este é um endpoint dedicado de preset de conversão, com base em `video-to-gif`.
      security:
        - bearerAuth: []
      requestBody:
        required: true
        content:
          multipart/form-data:
            schema:
              type: object
              required:
                - file
              properties:
                file:
                  type: string
                  format: binary
                  description: MP4 to GIF input file (.mp4)
                settings:
                  type: string
                  description: |
                    Dedicated MP4 to GIF converter using the video-to-gif pipeline.
                    The output format is locked by the endpoint.
                clientJobId:
                  type: string
                  description: Client-provided job ID for SSE progress tracking
      responses:
        '202':
          description: Accepted for async processing. Track progress via SSE at /api/v1/jobs/{jobId}/progress.
          content:
            application/json:
              schema:
                type: object
                properties:
                  jobId:
                    type: string
                  async:
                    type: boolean
                    example: true
        '400':
          description: Invalid input or settings
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Error'
        '401':
          description: Authentication required
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/UnauthorizedError'
        '422':
          description: Processing failed
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Error'
  /api/v1/tools/video/mov-to-gif:
    post:
      operationId: movToGifTool
      tags:
        - Tools
      summary: MOV para GIF
      description: |
        Converte MOV para GIF
        Este é um endpoint dedicado de preset de conversão, com base em `video-to-gif`.
      security:
        - bearerAuth: []
      requestBody:
        required: true
        content:
          multipart/form-data:
            schema:
              type: object
              required:
                - file
              properties:
                file:
                  type: string
                  format: binary
                  description: MOV to GIF input file (.mov)
                settings:
                  type: string
                  description: |
                    Dedicated MOV to GIF converter using the video-to-gif pipeline.
                    The output format is locked by the endpoint.
                clientJobId:
                  type: string
                  description: Client-provided job ID for SSE progress tracking
      responses:
        '202':
          description: Accepted for async processing. Track progress via SSE at /api/v1/jobs/{jobId}/progress.
          content:
            application/json:
              schema:
                type: object
                properties:
                  jobId:
                    type: string
                  async:
                    type: boolean
                    example: true
        '400':
          description: Invalid input or settings
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Error'
        '401':
          description: Authentication required
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/UnauthorizedError'
        '422':
          description: Processing failed
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Error'
  /api/v1/tools/video/mkv-to-gif:
    post:
      operationId: mkvToGifTool
      tags:
        - Tools
      summary: MKV para GIF
      description: |
        Converte MKV para GIF
        Este é um endpoint dedicado de preset de conversão, com base em `video-to-gif`.
      security:
        - bearerAuth: []
      requestBody:
        required: true
        content:
          multipart/form-data:
            schema:
              type: object
              required:
                - file
              properties:
                file:
                  type: string
                  format: binary
                  description: MKV to GIF input file (.mkv)
                settings:
                  type: string
                  description: |
                    Dedicated MKV to GIF converter using the video-to-gif pipeline.
                    The output format is locked by the endpoint.
                clientJobId:
                  type: string
                  description: Client-provided job ID for SSE progress tracking
      responses:
        '202':
          description: Accepted for async processing. Track progress via SSE at /api/v1/jobs/{jobId}/progress.
          content:
            application/json:
              schema:
                type: object
                properties:
                  jobId:
                    type: string
                  async:
                    type: boolean
                    example: true
        '400':
          description: Invalid input or settings
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Error'
        '401':
          description: Authentication required
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/UnauthorizedError'
        '422':
          description: Processing failed
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Error'
  /api/v1/tools/video/avi-to-gif:
    post:
      operationId: aviToGifTool
      tags:
        - Tools
      summary: AVI para GIF
      description: |
        Converte AVI para GIF
        Este é um endpoint dedicado de preset de conversão, com base em `video-to-gif`.
      security:
        - bearerAuth: []
      requestBody:
        required: true
        content:
          multipart/form-data:
            schema:
              type: object
              required:
                - file
              properties:
                file:
                  type: string
                  format: binary
                  description: AVI to GIF input file (.avi)
                settings:
                  type: string
                  description: |
                    Dedicated AVI to GIF converter using the video-to-gif pipeline.
                    The output format is locked by the endpoint.
                clientJobId:
                  type: string
                  description: Client-provided job ID for SSE progress tracking
      responses:
        '202':
          description: Accepted for async processing. Track progress via SSE at /api/v1/jobs/{jobId}/progress.
          content:
            application/json:
              schema:
                type: object
                properties:
                  jobId:
                    type: string
                  async:
                    type: boolean
                    example: true
        '400':
          description: Invalid input or settings
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Error'
        '401':
          description: Authentication required
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/UnauthorizedError'
        '422':
          description: Processing failed
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Error'
  /api/v1/tools/video/gif-to-mp4:
    post:
      operationId: gifToMp4Tool
      tags:
        - Tools
      summary: GIF para MP4
      description: |
        Converte GIF para MP4
        Este é um endpoint dedicado de preset de conversão, com base em `gif-to-video`.
      security:
        - bearerAuth: []
      requestBody:
        required: true
        content:
          multipart/form-data:
            schema:
              type: object
              required:
                - file
              properties:
                file:
                  type: string
                  format: binary
                  description: GIF to MP4 input file (.gif)
                settings:
                  type: string
                  description: |
                    Dedicated GIF to MP4 converter using the gif-to-video pipeline.
                    The output format is locked by the endpoint.
                clientJobId:
                  type: string
                  description: Client-provided job ID for SSE progress tracking
      responses:
        '200':
          description: Processed file
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ToolResponse'
        '400':
          description: Invalid input or settings
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Error'
        '401':
          description: Authentication required
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/UnauthorizedError'
        '422':
          description: Processing failed
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Error'
  /api/v1/tools/video/gif-to-webm:
    post:
      operationId: gifToWebmTool
      tags:
        - Tools
      summary: GIF para WEBM
      description: |
        Converte GIF para WEBM
        Este é um endpoint dedicado de preset de conversão, com base em `gif-to-video`.
      security:
        - bearerAuth: []
      requestBody:
        required: true
        content:
          multipart/form-data:
            schema:
              type: object
              required:
                - file
              properties:
                file:
                  type: string
                  format: binary
                  description: GIF to WEBM input file (.gif)
                settings:
                  type: string
                  description: |
                    Dedicated GIF to WEBM converter using the gif-to-video pipeline.
                    The output format is locked by the endpoint.
                clientJobId:
                  type: string
                  description: Client-provided job ID for SSE progress tracking
      responses:
        '200':
          description: Processed file
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ToolResponse'
        '400':
          description: Invalid input or settings
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Error'
        '401':
          description: Authentication required
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/UnauthorizedError'
        '422':
          description: Processing failed
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Error'
  /api/v1/tools/video/gif-to-mov:
    post:
      operationId: gifToMovTool
      tags:
        - Tools
      summary: GIF para MOV
      description: |
        Converte GIF para MOV
        Este é um endpoint dedicado de preset de conversão, com base em `gif-to-video`.
      security:
        - bearerAuth: []
      requestBody:
        required: true
        content:
          multipart/form-data:
            schema:
              type: object
              required:
                - file
              properties:
                file:
                  type: string
                  format: binary
                  description: GIF to MOV input file (.gif)
                settings:
                  type: string
                  description: |
                    Dedicated GIF to MOV converter using the gif-to-video pipeline.
                    The output format is locked by the endpoint.
                clientJobId:
                  type: string
                  description: Client-provided job ID for SSE progress tracking
      responses:
        '200':
          description: Processed file
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ToolResponse'
        '400':
          description: Invalid input or settings
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Error'
        '401':
          description: Authentication required
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/UnauthorizedError'
        '422':
          description: Processing failed
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Error'
  /api/v1/tools/video/mp4-to-mp3:
    post:
      operationId: mp4ToMp3Tool
      tags:
        - Tools
      summary: MP4 para MP3
      description: |
        Converte MP4 para MP3
        Este é um endpoint dedicado de preset de conversão, com base em `extract-audio`.
      security:
        - bearerAuth: []
      requestBody:
        required: true
        content:
          multipart/form-data:
            schema:
              type: object
              required:
                - file
              properties:
                file:
                  type: string
                  format: binary
                  description: MP4 to MP3 input file (.mp4)
                settings:
                  type: string
                  description: |
                    Dedicated MP4 to MP3 converter using the extract-audio pipeline.
                    The output format is locked by the endpoint.
                clientJobId:
                  type: string
                  description: Client-provided job ID for SSE progress tracking
      responses:
        '200':
          description: Processed file
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ToolResponse'
        '400':
          description: Invalid input or settings
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Error'
        '401':
          description: Authentication required
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/UnauthorizedError'
        '422':
          description: Processing failed
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Error'
  /api/v1/tools/video/mov-to-mp3:
    post:
      operationId: movToMp3Tool
      tags:
        - Tools
      summary: MOV para MP3
      description: |
        Converte MOV para MP3
        Este é um endpoint dedicado de preset de conversão, com base em `extract-audio`.
      security:
        - bearerAuth: []
      requestBody:
        required: true
        content:
          multipart/form-data:
            schema:
              type: object
              required:
                - file
              properties:
                file:
                  type: string
                  format: binary
                  description: MOV to MP3 input file (.mov)
                settings:
                  type: string
                  description: |
                    Dedicated MOV to MP3 converter using the extract-audio pipeline.
                    The output format is locked by the endpoint.
                clientJobId:
                  type: string
                  description: Client-provided job ID for SSE progress tracking
      responses:
        '200':
          description: Processed file
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ToolResponse'
        '400':
          description: Invalid input or settings
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Error'
        '401':
          description: Authentication required
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/UnauthorizedError'
        '422':
          description: Processing failed
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Error'
  /api/v1/tools/video/mkv-to-mp3:
    post:
      operationId: mkvToMp3Tool
      tags:
        - Tools
      summary: MKV para MP3
      description: |
        Converte MKV para MP3
        Este é um endpoint dedicado de preset de conversão, com base em `extract-audio`.
      security:
        - bearerAuth: []
      requestBody:
        required: true
        content:
          multipart/form-data:
            schema:
              type: object
              required:
                - file
              properties:
                file:
                  type: string
                  format: binary
                  description: MKV to MP3 input file (.mkv)
                settings:
                  type: string
                  description: |
                    Dedicated MKV to MP3 converter using the extract-audio pipeline.
                    The output format is locked by the endpoint.
                clientJobId:
                  type: string
                  description: Client-provided job ID for SSE progress tracking
      responses:
        '200':
          description: Processed file
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ToolResponse'
        '400':
          description: Invalid input or settings
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Error'
        '401':
          description: Authentication required
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/UnauthorizedError'
        '422':
          description: Processing failed
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Error'
  /api/v1/tools/video/webm-to-mp3:
    post:
      operationId: webmToMp3Tool
      tags:
        - Tools
      summary: WEBM para MP3
      description: |
        Converte WEBM para MP3
        Este é um endpoint dedicado de preset de conversão, com base em `extract-audio`.
      security:
        - bearerAuth: []
      requestBody:
        required: true
        content:
          multipart/form-data:
            schema:
              type: object
              required:
                - file
              properties:
                file:
                  type: string
                  format: binary
                  description: WEBM to MP3 input file (.webm)
                settings:
                  type: string
                  description: |
                    Dedicated WEBM to MP3 converter using the extract-audio pipeline.
                    The output format is locked by the endpoint.
                clientJobId:
                  type: string
                  description: Client-provided job ID for SSE progress tracking
      responses:
        '200':
          description: Processed file
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ToolResponse'
        '400':
          description: Invalid input or settings
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Error'
        '401':
          description: Authentication required
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/UnauthorizedError'
        '422':
          description: Processing failed
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Error'
  /api/v1/tools/video/avi-to-mp3:
    post:
      operationId: aviToMp3Tool
      tags:
        - Tools
      summary: AVI para MP3
      description: |
        Converte AVI para MP3
        Este é um endpoint dedicado de preset de conversão, com base em `extract-audio`.
      security:
        - bearerAuth: []
      requestBody:
        required: true
        content:
          multipart/form-data:
            schema:
              type: object
              required:
                - file
              properties:
                file:
                  type: string
                  format: binary
                  description: AVI to MP3 input file (.avi)
                settings:
                  type: string
                  description: |
                    Dedicated AVI to MP3 converter using the extract-audio pipeline.
                    The output format is locked by the endpoint.
                clientJobId:
                  type: string
                  description: Client-provided job ID for SSE progress tracking
      responses:
        '200':
          description: Processed file
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ToolResponse'
        '400':
          description: Invalid input or settings
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Error'
        '401':
          description: Authentication required
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/UnauthorizedError'
        '422':
          description: Processing failed
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Error'
  /api/v1/tools/video/mp4-to-wav:
    post:
      operationId: mp4ToWavTool
      tags:
        - Tools
      summary: MP4 para WAV
      description: |
        Converte MP4 para WAV
        Este é um endpoint dedicado de preset de conversão, com base em `extract-audio`.
      security:
        - bearerAuth: []
      requestBody:
        required: true
        content:
          multipart/form-data:
            schema:
              type: object
              required:
                - file
              properties:
                file:
                  type: string
                  format: binary
                  description: MP4 to WAV input file (.mp4)
                settings:
                  type: string
                  description: |
                    Dedicated MP4 to WAV converter using the extract-audio pipeline.
                    The output format is locked by the endpoint.
                clientJobId:
                  type: string
                  description: Client-provided job ID for SSE progress tracking
      responses:
        '200':
          description: Processed file
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ToolResponse'
        '400':
          description: Invalid input or settings
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Error'
        '401':
          description: Authentication required
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/UnauthorizedError'
        '422':
          description: Processing failed
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Error'
  /api/v1/tools/video/mov-to-wav:
    post:
      operationId: movToWavTool
      tags:
        - Tools
      summary: MOV para WAV
      description: |
        Converte MOV para WAV
        Este é um endpoint dedicado de preset de conversão, com base em `extract-audio`.
      security:
        - bearerAuth: []
      requestBody:
        required: true
        content:
          multipart/form-data:
            schema:
              type: object
              required:
                - file
              properties:
                file:
                  type: string
                  format: binary
                  description: MOV to WAV input file (.mov)
                settings:
                  type: string
                  description: |
                    Dedicated MOV to WAV converter using the extract-audio pipeline.
                    The output format is locked by the endpoint.
                clientJobId:
                  type: string
                  description: Client-provided job ID for SSE progress tracking
      responses:
        '200':
          description: Processed file
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ToolResponse'
        '400':
          description: Invalid input or settings
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Error'
        '401':
          description: Authentication required
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/UnauthorizedError'
        '422':
          description: Processing failed
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Error'
  /api/v1/tools/video/mp4-to-ogg:
    post:
      operationId: mp4ToOggTool
      tags:
        - Tools
      summary: MP4 para OGG
      description: |
        Converte MP4 para OGG
        Este é um endpoint dedicado de preset de conversão, com base em `extract-audio`.
      security:
        - bearerAuth: []
      requestBody:
        required: true
        content:
          multipart/form-data:
            schema:
              type: object
              required:
                - file
              properties:
                file:
                  type: string
                  format: binary
                  description: MP4 to OGG input file (.mp4)
                settings:
                  type: string
                  description: |
                    Dedicated MP4 to OGG converter using the extract-audio pipeline.
                    The output format is locked by the endpoint.
                clientJobId:
                  type: string
                  description: Client-provided job ID for SSE progress tracking
      responses:
        '200':
          description: Processed file
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ToolResponse'
        '400':
          description: Invalid input or settings
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Error'
        '401':
          description: Authentication required
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/UnauthorizedError'
        '422':
          description: Processing failed
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Error'
  /api/v1/tools/audio/m4a-to-mp3:
    post:
      operationId: m4aToMp3Tool
      tags:
        - Tools
      summary: M4A para MP3
      description: |
        Converte M4A para MP3
        Este é um endpoint dedicado de preset de conversão, com base em `convert-audio`.
      security:
        - bearerAuth: []
      requestBody:
        required: true
        content:
          multipart/form-data:
            schema:
              type: object
              required:
                - file
              properties:
                file:
                  type: string
                  format: binary
                  description: M4A to MP3 input file (.m4a)
                settings:
                  type: string
                  description: |
                    Dedicated M4A to MP3 converter using the convert-audio pipeline.
                    The output format is locked by the endpoint.
                clientJobId:
                  type: string
                  description: Client-provided job ID for SSE progress tracking
      responses:
        '200':
          description: Processed file
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ToolResponse'
        '400':
          description: Invalid input or settings
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Error'
        '401':
          description: Authentication required
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/UnauthorizedError'
        '422':
          description: Processing failed
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Error'
  /api/v1/tools/audio/m4a-to-wav:
    post:
      operationId: m4aToWavTool
      tags:
        - Tools
      summary: M4A para WAV
      description: |
        Converte M4A para WAV
        Este é um endpoint dedicado de preset de conversão, com base em `convert-audio`.
      security:
        - bearerAuth: []
      requestBody:
        required: true
        content:
          multipart/form-data:
            schema:
              type: object
              required:
                - file
              properties:
                file:
                  type: string
                  format: binary
                  description: M4A to WAV input file (.m4a)
                settings:
                  type: string
                  description: |
                    Dedicated M4A to WAV converter using the convert-audio pipeline.
                    The output format is locked by the endpoint.
                clientJobId:
                  type: string
                  description: Client-provided job ID for SSE progress tracking
      responses:
        '200':
          description: Processed file
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ToolResponse'
        '400':
          description: Invalid input or settings
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Error'
        '401':
          description: Authentication required
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/UnauthorizedError'
        '422':
          description: Processing failed
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Error'
  /api/v1/tools/audio/aac-to-mp3:
    post:
      operationId: aacToMp3Tool
      tags:
        - Tools
      summary: AAC para MP3
      description: |
        Converte AAC para MP3
        Este é um endpoint dedicado de preset de conversão, com base em `convert-audio`.
      security:
        - bearerAuth: []
      requestBody:
        required: true
        content:
          multipart/form-data:
            schema:
              type: object
              required:
                - file
              properties:
                file:
                  type: string
                  format: binary
                  description: AAC to MP3 input file (.aac)
                settings:
                  type: string
                  description: |
                    Dedicated AAC to MP3 converter using the convert-audio pipeline.
                    The output format is locked by the endpoint.
                clientJobId:
                  type: string
                  description: Client-provided job ID for SSE progress tracking
      responses:
        '200':
          description: Processed file
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ToolResponse'
        '400':
          description: Invalid input or settings
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Error'
        '401':
          description: Authentication required
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/UnauthorizedError'
        '422':
          description: Processing failed
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Error'
  /api/v1/tools/audio/aac-to-wav:
    post:
      operationId: aacToWavTool
      tags:
        - Tools
      summary: AAC para WAV
      description: |
        Converte AAC para WAV
        Este é um endpoint dedicado de preset de conversão, com base em `convert-audio`.
      security:
        - bearerAuth: []
      requestBody:
        required: true
        content:
          multipart/form-data:
            schema:
              type: object
              required:
                - file
              properties:
                file:
                  type: string
                  format: binary
                  description: AAC to WAV input file (.aac)
                settings:
                  type: string
                  description: |
                    Dedicated AAC to WAV converter using the convert-audio pipeline.
                    The output format is locked by the endpoint.
                clientJobId:
                  type: string
                  description: Client-provided job ID for SSE progress tracking
      responses:
        '200':
          description: Processed file
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ToolResponse'
        '400':
          description: Invalid input or settings
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Error'
        '401':
          description: Authentication required
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/UnauthorizedError'
        '422':
          description: Processing failed
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Error'
  /api/v1/tools/audio/aac-to-flac:
    post:
      operationId: aacToFlacTool
      tags:
        - Tools
      summary: AAC para FLAC
      description: |
        Converte AAC para FLAC
        Este é um endpoint dedicado de preset de conversão, com base em `convert-audio`.
      security:
        - bearerAuth: []
      requestBody:
        required: true
        content:
          multipart/form-data:
            schema:
              type: object
              required:
                - file
              properties:
                file:
                  type: string
                  format: binary
                  description: AAC to FLAC input file (.aac)
                settings:
                  type: string
                  description: |
                    Dedicated AAC to FLAC converter using the convert-audio pipeline.
                    The output format is locked by the endpoint.
                clientJobId:
                  type: string
                  description: Client-provided job ID for SSE progress tracking
      responses:
        '200':
          description: Processed file
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ToolResponse'
        '400':
          description: Invalid input or settings
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Error'
        '401':
          description: Authentication required
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/UnauthorizedError'
        '422':
          description: Processing failed
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Error'
  /api/v1/tools/audio/ogg-to-mp3:
    post:
      operationId: oggToMp3Tool
      tags:
        - Tools
      summary: OGG para MP3
      description: |
        Converte OGG para MP3
        Este é um endpoint dedicado de preset de conversão, com base em `convert-audio`.
      security:
        - bearerAuth: []
      requestBody:
        required: true
        content:
          multipart/form-data:
            schema:
              type: object
              required:
                - file
              properties:
                file:
                  type: string
                  format: binary
                  description: OGG to MP3 input file (.ogg)
                settings:
                  type: string
                  description: |
                    Dedicated OGG to MP3 converter using the convert-audio pipeline.
                    The output format is locked by the endpoint.
                clientJobId:
                  type: string
                  description: Client-provided job ID for SSE progress tracking
      responses:
        '200':
          description: Processed file
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ToolResponse'
        '400':
          description: Invalid input or settings
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Error'
        '401':
          description: Authentication required
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/UnauthorizedError'
        '422':
          description: Processing failed
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Error'
  /api/v1/tools/audio/ogg-to-wav:
    post:
      operationId: oggToWavTool
      tags:
        - Tools
      summary: OGG para WAV
      description: |
        Converte OGG para WAV
        Este é um endpoint dedicado de preset de conversão, com base em `convert-audio`.
      security:
        - bearerAuth: []
      requestBody:
        required: true
        content:
          multipart/form-data:
            schema:
              type: object
              required:
                - file
              properties:
                file:
                  type: string
                  format: binary
                  description: OGG to WAV input file (.ogg)
                settings:
                  type: string
                  description: |
                    Dedicated OGG to WAV converter using the convert-audio pipeline.
                    The output format is locked by the endpoint.
                clientJobId:
                  type: string
                  description: Client-provided job ID for SSE progress tracking
      responses:
        '200':
          description: Processed file
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ToolResponse'
        '400':
          description: Invalid input or settings
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Error'
        '401':
          description: Authentication required
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/UnauthorizedError'
        '422':
          description: Processing failed
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Error'
  /api/v1/tools/audio/wav-to-mp3:
    post:
      operationId: wavToMp3Tool
      tags:
        - Tools
      summary: WAV para MP3
      description: |
        Converte WAV para MP3
        Este é um endpoint dedicado de preset de conversão, com base em `convert-audio`.
      security:
        - bearerAuth: []
      requestBody:
        required: true
        content:
          multipart/form-data:
            schema:
              type: object
              required:
                - file
              properties:
                file:
                  type: string
                  format: binary
                  description: WAV to MP3 input file (.wav)
                settings:
                  type: string
                  description: |
                    Dedicated WAV to MP3 converter using the convert-audio pipeline.
                    The output format is locked by the endpoint.
                clientJobId:
                  type: string
                  description: Client-provided job ID for SSE progress tracking
      responses:
        '200':
          description: Processed file
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ToolResponse'
        '400':
          description: Invalid input or settings
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Error'
        '401':
          description: Authentication required
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/UnauthorizedError'
        '422':
          description: Processing failed
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Error'
  /api/v1/tools/audio/mp3-to-wav:
    post:
      operationId: mp3ToWavTool
      tags:
        - Tools
      summary: MP3 para WAV
      description: |
        Converte MP3 para WAV
        Este é um endpoint dedicado de preset de conversão, com base em `convert-audio`.
      security:
        - bearerAuth: []
      requestBody:
        required: true
        content:
          multipart/form-data:
            schema:
              type: object
              required:
                - file
              properties:
                file:
                  type: string
                  format: binary
                  description: MP3 to WAV input file (.mp3)
                settings:
                  type: string
                  description: |
                    Dedicated MP3 to WAV converter using the convert-audio pipeline.
                    The output format is locked by the endpoint.
                clientJobId:
                  type: string
                  description: Client-provided job ID for SSE progress tracking
      responses:
        '200':
          description: Processed file
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ToolResponse'
        '400':
          description: Invalid input or settings
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Error'
        '401':
          description: Authentication required
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/UnauthorizedError'
        '422':
          description: Processing failed
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Error'
  /api/v1/tools/audio/flac-to-mp3:
    post:
      operationId: flacToMp3Tool
      tags:
        - Tools
      summary: FLAC para MP3
      description: |
        Converte FLAC para MP3
        Este é um endpoint dedicado de preset de conversão, com base em `convert-audio`.
      security:
        - bearerAuth: []
      requestBody:
        required: true
        content:
          multipart/form-data:
            schema:
              type: object
              required:
                - file
              properties:
                file:
                  type: string
                  format: binary
                  description: FLAC to MP3 input file (.flac)
                settings:
                  type: string
                  description: |
                    Dedicated FLAC to MP3 converter using the convert-audio pipeline.
                    The output format is locked by the endpoint.
                clientJobId:
                  type: string
                  description: Client-provided job ID for SSE progress tracking
      responses:
        '200':
          description: Processed file
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ToolResponse'
        '400':
          description: Invalid input or settings
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Error'
        '401':
          description: Authentication required
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/UnauthorizedError'
        '422':
          description: Processing failed
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Error'
  /api/v1/tools/files/excel-to-csv:
    post:
      operationId: excelToCsvTool
      tags:
        - Tools
      summary: Excel para CSV
      description: |
        Converte Excel para CSV
        Este é um endpoint dedicado de preset de conversão, com base em `convert-spreadsheet`.
      security:
        - bearerAuth: []
      requestBody:
        required: true
        content:
          multipart/form-data:
            schema:
              type: object
              required:
                - file
              properties:
                file:
                  type: string
                  format: binary
                  description: Excel to CSV input file (.xlsx, .xls)
                settings:
                  type: string
                  description: |
                    Dedicated Excel to CSV converter using the convert-spreadsheet pipeline.
                    The output format is locked by the endpoint.
                clientJobId:
                  type: string
                  description: Client-provided job ID for SSE progress tracking
      responses:
        '202':
          description: Accepted for async processing. Track progress via SSE at /api/v1/jobs/{jobId}/progress.
          content:
            application/json:
              schema:
                type: object
                properties:
                  jobId:
                    type: string
                  async:
                    type: boolean
                    example: true
        '400':
          description: Invalid input or settings
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Error'
        '401':
          description: Authentication required
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/UnauthorizedError'
        '422':
          description: Processing failed
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Error'
  /api/v1/readyz:
    get:
      operationId: readinessCheck
      tags:
        - System
      summary: Verificação de prontidão
      description: Verifica PostgreSQL, Redis, espaço em disco local e alcançabilidade do S3 quando configurado. Endpoint público para load balancers e orquestradores.
      security: []
      responses:
        '200':
          description: Instance is ready to receive traffic
        '503':
          description: Instance is not ready to receive traffic
  /api/v1/jobs/{jobId}/cancel:
    post:
      operationId: cancelJob
      tags:
        - System
      summary: Cancelar job
      description: Solicita o cancelamento de um job na fila ou em execução.
      security:
        - bearerAuth: []
      parameters:
        - name: jobId
          in: path
          required: true
          schema:
            type: string
      responses:
        '200':
          description: Cancellation request result
        '401':
          description: Authentication required
  /api/v1/preferences:
    get:
      operationId: getPreferences
      tags:
        - Settings
      summary: Obter preferências do usuário
      description: Retorna o mapa de preferências por usuário do usuário atual.
      security:
        - bearerAuth: []
      responses:
        '200':
          description: Preference map
        '401':
          description: Authentication required
    put:
      operationId: updatePreferences
      tags:
        - Settings
      summary: Atualizar preferências do usuário
      description: Insere ou atualiza uma ou mais preferências do usuário atual.
      security:
        - bearerAuth: []
      requestBody:
        required: true
        content:
          application/json:
            schema:
              type: object
      responses:
        '200':
          description: Preferences updated
        '400':
          description: Invalid preferences body
        '401':
          description: Authentication required
  /api/auth/mfa/enroll:
    post:
      operationId: enrollMfa
      tags:
        - Auth
      summary: Iniciar cadastro de MFA
      description: Inicia o cadastro de MFA TOTP para o usuário atual. Requer o recurso enterprise MFA.
      security:
        - bearerAuth: []
      responses:
        '200':
          description: Enrollment URI and recovery codes
        '401':
          description: Authentication required
        '403':
          description: MFA feature not licensed
        '409':
          description: MFA enrollment already enabled or pending
  /api/auth/mfa/verify:
    post:
      operationId: verifyMfaEnrollment
      tags:
        - Auth
      summary: Verificar cadastro de MFA
      description: Confirma um cadastro de MFA pendente com um código TOTP.
      security:
        - bearerAuth: []
      requestBody:
        required: true
        content:
          application/json:
            schema:
              type: object
              required:
                - code
              properties:
                code:
                  type: string
      responses:
        '200':
          description: MFA enabled
        '400':
          description: No pending enrollment or invalid body
        '401':
          description: Invalid TOTP code
  /api/auth/mfa/complete:
    post:
      operationId: completeMfaLogin
      tags:
        - Auth
      summary: Concluir login com MFA
      description: Conclui um desafio de login pendente com um código TOTP ou de recuperação.
      security: []
      requestBody:
        required: true
        content:
          application/json:
            schema:
              type: object
              required:
                - mfaToken
                - code
              properties:
                mfaToken:
                  type: string
                code:
                  type: string
      responses:
        '200':
          description: Session token and user profile
        '400':
          description: Invalid request body
        '401':
          description: Invalid or expired MFA challenge
  /api/auth/mfa/disable:
    post:
      operationId: disableMfa
      tags:
        - Auth
      summary: Desativar MFA
      description: Desativa o MFA do usuário atual após verificar um código TOTP.
      security:
        - bearerAuth: []
      requestBody:
        required: true
        content:
          application/json:
            schema:
              type: object
              required:
                - code
              properties:
                code:
                  type: string
      responses:
        '200':
          description: MFA disabled
        '400':
          description: MFA is not enabled or body is invalid
        '401':
          description: Authentication required or invalid code
  /api/auth/users/{id}/mfa/reset:
    post:
      operationId: resetUserMfa
      tags:
        - Auth
      summary: Redefinir MFA do usuário
      description: |
        Reset a user's MFA enrollment. Requires users:manage permission and authority over the target's current role, including hierarchy, permissions, tool access, and any API-key scope.
      security:
        - bearerAuth: []
      parameters:
        - name: id
          in: path
          required: true
          schema:
            type: string
      responses:
        '200':
          description: MFA reset
        '401':
          description: Authentication required
        '403':
          description: Insufficient permissions
        '404':
          description: User not found
  /api/auth/oidc/login:
    get:
      operationId: oidcLogin
      tags:
        - Auth
      summary: Iniciar login OIDC
      description: Redireciona para o provedor OpenID Connect configurado.
      security: []
      responses:
        '302':
          description: Redirect to identity provider
        '500':
          description: OIDC provider discovery failed
  /api/auth/oidc/callback:
    get:
      operationId: oidcCallback
      tags:
        - Auth
      summary: Callback OIDC
      description: Conclui o fluxo de authorization code do OIDC e cria uma sessão.
      security: []
      responses:
        '302':
          description: Redirect to the application
  /api/auth/saml/metadata:
    get:
      operationId: samlMetadata
      tags:
        - Auth
      summary: Metadados SAML
      description: Retorna o XML de metadados do service provider SAML.
      security: []
      responses:
        '200':
          description: SAML metadata XML
  /api/auth/saml/login:
    get:
      operationId: samlLogin
      tags:
        - Auth
      summary: Iniciar login SAML
      description: Redireciona para o identity provider SAML configurado.
      security: []
      responses:
        '302':
          description: Redirect to identity provider
  /api/auth/saml/callback:
    post:
      operationId: samlCallback
      tags:
        - Auth
      summary: Callback SAML
      description: Endpoint do assertion consumer service para login SAML.
      security: []
      responses:
        '302':
          description: Redirect to the application
  /api/v1/files/{id}/preview:
    get:
      operationId: getFilePreview
      tags:
        - Files
      summary: Preview de arquivo
      description: Faz stream ou gera um preview compatível com o navegador para um PDF, documento de escritório, vídeo ou áudio salvo.
      security:
        - bearerAuth: []
      parameters:
        - name: id
          in: path
          required: true
          schema:
            type: string
      responses:
        '200':
          description: Preview binary
        '400':
          description: Preview not supported
        '401':
          description: Authentication required
        '404':
          description: File not found
        '422':
          description: Could not generate preview
  /api/v1/preview/generate:
    post:
      operationId: generatePreview
      tags:
        - Files
      summary: Gerar preview de upload
      description: Gera um preview MP4 ou MP3 sob demanda para um arquivo de mídia enviado.
      security: []
      requestBody:
        required: true
        content:
          multipart/form-data:
            schema:
              type: object
              required:
                - file
              properties:
                file:
                  type: string
                  format: binary
      responses:
        '200':
          description: Preview binary
        '400':
          description: Unsupported file type or missing file
        '422':
          description: Could not generate preview
  /api/v1/admin/log-level:
    get:
      operationId: getLogLevel
      tags:
        - Admin
      summary: Obter nível de log
      description: Lê o nível de log de runtime atual. Requer a permissão settings:write.
      security:
        - bearerAuth: []
      responses:
        '200':
          description: Current log level
        '403':
          description: Insufficient permissions
    post:
      operationId: setLogLevel
      tags:
        - Admin
      summary: Definir nível de log
      description: Altera o nível de log de runtime. Requer a permissão settings:write.
      security:
        - bearerAuth: []
      requestBody:
        required: true
        content:
          application/json:
            schema:
              type: object
              required:
                - level
              properties:
                level:
                  type: string
                  enum:
                    - fatal
                    - error
                    - warn
                    - info
                    - debug
                    - trace
                    - silent
      responses:
        '200':
          description: New log level
        '400':
          description: Invalid log level
        '403':
          description: Insufficient permissions
  /api/v1/metrics:
    get:
      operationId: getMetrics
      tags:
        - Admin
      summary: Métricas Prometheus
      description: Retorna métricas Prometheus. Requer a permissão system:health.
      security:
        - bearerAuth: []
      responses:
        '200':
          description: Prometheus text format
        '403':
          description: Insufficient permissions
  /api/v1/admin/support-bundle:
    get:
      operationId: getSupportBundle
      tags:
        - Admin
      summary: Pacote de suporte
      description: Baixa um ZIP de pacote de suporte de diagnóstico redigido. Requer a permissão system:health.
      security:
        - bearerAuth: []
      responses:
        '200':
          description: Support bundle ZIP
        '403':
          description: Insufficient permissions
  /api/v1/admin/usage:
    get:
      operationId: getAdminUsage
      tags:
        - Admin
      summary: Dados do painel de uso
      description: Retorna os dados locais do painel de uso. Requer a permissão audit:read.
      security:
        - bearerAuth: []
      parameters:
        - name: days
          in: query
          required: false
          schema:
            type: string
      responses:
        '200':
          description: Usage dashboard data
        '400':
          description: Invalid query parameters
        '403':
          description: Insufficient permissions
  /api/v1/admin/backup-status:
    get:
      operationId: getBackupStatus
      tags:
        - Admin
      summary: Status do backup
      description: Retorna os metadados e a atualidade do último backup. Requer a permissão system:health.
      security:
        - bearerAuth: []
      responses:
        '200':
          description: Backup status
        '403':
          description: Insufficient permissions
    post:
      operationId: setBackupStatus
      tags:
        - Admin
      summary: Registrar status do backup
      description: Registra um backup concluído. Requer a permissão system:health.
      security:
        - bearerAuth: []
      requestBody:
        required: true
        content:
          application/json:
            schema:
              type: object
              required:
                - type
              properties:
                type:
                  type: string
                sizeBytes:
                  type: integer
                notes:
                  type: string
      responses:
        '200':
          description: Backup status recorded
        '400':
          description: Invalid backup status
        '403':
          description: Insufficient permissions
  /api/v1/admin/features/import:
    post:
      operationId: importFeatureBundle
      tags:
        - Features
      summary: Importar bundle de recurso
      description: |-
        Importe um arquivo de pacote de IA legado ou importe um OCR off-line preciso
        release usando seu índice assinado e arquivo de plataforma correspondente. OCR
        a importação executa a mesma assinatura, hash, compatibilidade, extração,
        e smoke-test verifica como instalação on-line, incluindo a precisão
        mínimo de memória efetiva de 4 GiB do tempo de execução. Fast Tesseract OCR não é
        sujeito a esta exigência. Requer permissão features:manage.
      security:
        - bearerAuth: []
      requestBody:
        required: true
        content:
          multipart/form-data:
            schema:
              type: object
              oneOf:
                - required:
                    - file
                - required:
                    - index
                    - archive
              properties:
                file:
                  type: string
                  format: binary
                  description: One legacy AI bundle archive; mutually exclusive with index/archive
                index:
                  type: string
                  format: binary
                  description: Signed ocr-runtime-index.json from the SnapOtter release
                archive:
                  type: string
                  format: binary
                  description: OCR runtime archive for the current Linux amd64 or arm64 target; must follow index
      responses:
        '200':
          description: Bundle verified, imported, and activated
        '400':
          description: Invalid archive
        '403':
          description: Insufficient permissions
        '409':
          description: Import lock is held
        '507':
          description: Insufficient storage for the authenticated archive and expanded runtime
  /api/v1/enterprise/scim/token:
    post:
      operationId: createScimToken
      tags:
        - Enterprise
      summary: Criar token SCIM
      description: |
        Generate a global SCIM bearer token. Requires the enterprise SCIM feature and full effective built-in administrator authority; custom roles and API keys missing any administrator permission do not qualify. Legacy unversioned tokens are invalid and must be reissued as v2 tokens.
      security:
        - bearerAuth: []
      responses:
        '201':
          description: SCIM token created
        '403':
          description: SCIM feature not licensed or insufficient permissions
    delete:
      operationId: revokeScimToken
      tags:
        - Enterprise
      summary: Revogar token SCIM
      description: |
        Revoke the global SCIM bearer token. Requires the enterprise SCIM feature and full effective built-in administrator authority; custom roles and API keys missing any administrator permission do not qualify.
      security:
        - bearerAuth: []
      responses:
        '204':
          description: SCIM token revoked
        '403':
          description: SCIM feature not licensed or insufficient permissions
  /api/v1/scim/v2/ServiceProviderConfig:
    get:
      operationId: getScimServiceProviderConfig
      tags:
        - Enterprise
      summary: Configuração do service provider SCIM
      description: Endpoint público de descoberta SCIM.
      security: []
      responses:
        '200':
          description: SCIM service provider capabilities
  /api/v1/scim/v2/Schemas:
    get:
      operationId: getScimSchemas
      tags:
        - Enterprise
      summary: Schemas SCIM
      description: Endpoint público de descoberta de schemas SCIM.
      security: []
      responses:
        '200':
          description: SCIM schemas
  /api/v1/scim/v2/ResourceTypes:
    get:
      operationId: getScimResourceTypes
      tags:
        - Enterprise
      summary: Tipos de recurso SCIM
      description: Endpoint público de descoberta de tipos de recurso SCIM.
      security: []
      responses:
        '200':
          description: SCIM resource types
  /api/v1/scim/v2/Users:
    get:
      operationId: listScimUsers
      tags:
        - Enterprise
      summary: Listar usuários SCIM
      description: Lista usuários SCIM. Requer o bearer token SCIM.
      responses:
        '200':
          description: SCIM user list
    post:
      operationId: createScimUser
      tags:
        - Enterprise
      summary: Criar usuário SCIM
      description: Cria um usuário SCIM. Requer o bearer token SCIM.
      responses:
        '201':
          description: SCIM user created
  /api/v1/scim/v2/Users/{id}:
    get:
      operationId: getScimUser
      tags:
        - Enterprise
      summary: Obter usuário SCIM
      description: Obtém um usuário SCIM pelo ID. Requer o bearer token SCIM.
      parameters:
        - name: id
          in: path
          required: true
          schema:
            type: string
      responses:
        '200':
          description: SCIM user
    put:
      operationId: updateScimUser
      tags:
        - Enterprise
      summary: Atualizar usuário SCIM
      description: Substitui um usuário SCIM pelo ID. Requer o bearer token SCIM.
      parameters:
        - name: id
          in: path
          required: true
          schema:
            type: string
      responses:
        '200':
          description: SCIM user updated
    delete:
      operationId: deleteScimUser
      tags:
        - Enterprise
      summary: Excluir usuário SCIM
      description: Desativa (soft delete) um usuário SCIM pelo ID. Requer o bearer token SCIM.
      parameters:
        - name: id
          in: path
          required: true
          schema:
            type: string
      responses:
        '200':
          description: SCIM user deactivated
  /api/v1/scim/v2/Groups:
    get:
      operationId: listScimGroups
      tags:
        - Enterprise
      summary: Listar grupos SCIM
      description: Lista grupos SCIM. Requer o bearer token SCIM.
      responses:
        '200':
          description: SCIM group list
    post:
      operationId: createScimGroup
      tags:
        - Enterprise
      summary: Criar grupo SCIM
      description: Cria um grupo SCIM. Requer o bearer token SCIM.
      responses:
        '201':
          description: SCIM group created
  /api/v1/scim/v2/Groups/{id}:
    get:
      operationId: getScimGroup
      tags:
        - Enterprise
      summary: Obter grupo SCIM
      description: Obtém um grupo SCIM pelo ID. Requer o bearer token SCIM.
      parameters:
        - name: id
          in: path
          required: true
          schema:
            type: string
      responses:
        '200':
          description: SCIM group
    put:
      operationId: updateScimGroup
      tags:
        - Enterprise
      summary: Atualizar grupo SCIM
      description: Substitui um grupo SCIM pelo ID. Requer o bearer token SCIM.
      parameters:
        - name: id
          in: path
          required: true
          schema:
            type: string
      responses:
        '200':
          description: SCIM group updated
    delete:
      operationId: deleteScimGroup
      tags:
        - Enterprise
      summary: Excluir grupo SCIM
      description: Exclui um grupo SCIM pelo ID. Requer o bearer token SCIM.
      parameters:
        - name: id
          in: path
          required: true
          schema:
            type: string
      responses:
        '204':
          description: SCIM group deleted
  /api/v1/enterprise/audit/export:
    get:
      operationId: exportAuditLog
      tags:
        - Enterprise
      summary: Exportar log de auditoria
      description: Exporta entradas de auditoria como JSON ou CSV. Requer a permissão audit:read e o recurso enterprise de exportação de auditoria.
      security:
        - bearerAuth: []
      responses:
        '200':
          description: Audit export
        '400':
          description: Invalid query parameters
        '403':
          description: Feature not licensed or insufficient permissions
  /api/v1/enterprise/config/export:
    get:
      operationId: exportEnterpriseConfig
      tags:
        - Enterprise
      summary: Exportar configuração
      description: Exporta a configuração redigida da instância, funções personalizadas e equipes. Requer a permissão system:health.
      security:
        - bearerAuth: []
      responses:
        '200':
          description: Configuration export
        '403':
          description: Feature not licensed or insufficient permissions
  /api/v1/enterprise/config/import:
    post:
      operationId: importEnterpriseConfig
      tags:
        - Enterprise
      summary: Importar configuração
      description: |
        Import instance configuration, custom roles, and teams. Requires full effective built-in administrator authority; custom roles and API keys missing any administrator permission do not qualify.
      security:
        - bearerAuth: []
      responses:
        '200':
          description: Import result
        '400':
          description: Invalid import payload
        '403':
          description: Feature not licensed or insufficient permissions
  /api/v1/enterprise/ip-allowlist:
    get:
      operationId: getIpAllowlist
      tags:
        - Enterprise
      summary: Obter allowlist de IP
      description: Lê a allowlist de IP enterprise. Requer a permissão security:manage.
      security:
        - bearerAuth: []
      responses:
        '200':
          description: IP allowlist
        '403':
          description: Feature not licensed or insufficient permissions
    put:
      operationId: updateIpAllowlist
      tags:
        - Enterprise
      summary: Atualizar allowlist de IP
      description: Atualiza as entradas de allowlist CIDR com prevenção de auto-bloqueio. Requer a permissão security:manage.
      security:
        - bearerAuth: []
      responses:
        '200':
          description: IP allowlist updated
        '400':
          description: Invalid CIDR entries
        '403':
          description: Feature not licensed or insufficient permissions
  /api/v1/enterprise/legal-hold:
    get:
      operationId: listLegalHolds
      tags:
        - Enterprise
      summary: Listar retenções legais
      description: Lista usuários e equipes sob retenção legal. Requer a permissão compliance:manage.
      security:
        - bearerAuth: []
      responses:
        '200':
          description: Legal holds
        '403':
          description: Feature not licensed or insufficient permissions
    put:
      operationId: updateLegalHold
      tags:
        - Enterprise
      summary: Atualizar retenção legal
      description: Aplica ou libera uma retenção legal sobre um usuário ou equipe. Requer a permissão compliance:manage.
      security:
        - bearerAuth: []
      responses:
        '200':
          description: Legal hold updated
        '400':
          description: Invalid request body
        '403':
          description: Feature not licensed or insufficient permissions
  /api/v1/enterprise/siem/config:
    get:
      operationId: getSiemConfig
      tags:
        - Enterprise
      summary: Obter configuração de SIEM
      description: Lê a configuração de encaminhamento para SIEM. Requer a permissão webhooks:manage.
      security:
        - bearerAuth: []
      responses:
        '200':
          description: SIEM config
        '403':
          description: Feature not licensed or insufficient permissions
    put:
      operationId: updateSiemConfig
      tags:
        - Enterprise
      summary: Atualizar configuração de SIEM
      description: Atualiza a configuração de encaminhamento para SIEM. Requer a permissão webhooks:manage.
      security:
        - bearerAuth: []
      responses:
        '200':
          description: SIEM config updated
        '400':
          description: Invalid SIEM config
        '403':
          description: Feature not licensed or insufficient permissions
  /api/v1/enterprise/webhooks:
    get:
      operationId: listWebhooks
      tags:
        - Enterprise
      summary: Listar webhooks
      description: Lista os destinos de webhook. Requer a permissão webhooks:manage.
      security:
        - bearerAuth: []
      responses:
        '200':
          description: Webhook destinations
        '403':
          description: Feature not licensed or insufficient permissions
    post:
      operationId: createWebhook
      tags:
        - Enterprise
      summary: Criar webhook
      description: Cria um destino de webhook. Requer a permissão webhooks:manage.
      security:
        - bearerAuth: []
      responses:
        '201':
          description: Webhook created
        '400':
          description: Invalid webhook destination
        '403':
          description: Feature not licensed or insufficient permissions
  /api/v1/enterprise/webhooks/{index}:
    put:
      operationId: updateWebhook
      tags:
        - Enterprise
      summary: Atualizar webhook
      description: Atualiza um destino de webhook pelo índice. Requer a permissão webhooks:manage.
      security:
        - bearerAuth: []
      parameters:
        - name: index
          in: path
          required: true
          schema:
            type: string
      responses:
        '200':
          description: Webhook updated
        '400':
          description: Invalid index or destination
        '404':
          description: Webhook not found
    delete:
      operationId: deleteWebhook
      tags:
        - Enterprise
      summary: Excluir webhook
      description: Exclui um destino de webhook pelo índice. Requer a permissão webhooks:manage.
      security:
        - bearerAuth: []
      parameters:
        - name: index
          in: path
          required: true
          schema:
            type: string
      responses:
        '200':
          description: Webhook deleted
        '400':
          description: Invalid index
        '404':
          description: Webhook not found
  /api/v1/enterprise/webhooks/{index}/test:
    post:
      operationId: testWebhook
      tags:
        - Enterprise
      summary: Testar webhook
      description: Envia um payload de teste para um destino de webhook. Requer a permissão webhooks:manage.
      security:
        - bearerAuth: []
      parameters:
        - name: index
          in: path
          required: true
          schema:
            type: string
      responses:
        '200':
          description: Test result
        '400':
          description: Invalid index
        '404':
          description: Webhook not found
  /api/v1/enterprise/users/{id}/export:
    post:
      operationId: startGdprUserExport
      tags:
        - Enterprise
      summary: Iniciar exportação GDPR
      description: Inicia um job de exportação de dados de usuário GDPR. Requer a permissão compliance:manage.
      security:
        - bearerAuth: []
      parameters:
        - name: id
          in: path
          required: true
          schema:
            type: string
      responses:
        '202':
          description: Export job started
        '403':
          description: Feature not licensed or insufficient permissions
        '404':
          description: User not found
  /api/v1/enterprise/users/{id}/export/{jobId}:
    get:
      operationId: getGdprUserExport
      tags:
        - Enterprise
      summary: Obter status da exportação GDPR
      description: Lê o status e a URL de download de um job de exportação de usuário GDPR. Requer a permissão compliance:manage.
      security:
        - bearerAuth: []
      parameters:
        - name: id
          in: path
          required: true
          schema:
            type: string
        - name: jobId
          in: path
          required: true
          schema:
            type: string
      responses:
        '200':
          description: Export status
        '404':
          description: Export job not found
  /api/v1/enterprise/users/{id}/purge:
    delete:
      operationId: purgeEnterpriseUser
      tags:
        - Enterprise
      summary: Expurgar dados do usuário
      description: |
        Permanently purge a user's data after explicit confirmation. Requires compliance:manage permission and authority over the target's current role, including hierarchy, permissions, tool access, and any API-key scope.
      security:
        - bearerAuth: []
      parameters:
        - name: id
          in: path
          required: true
          schema:
            type: string
      responses:
        '200':
          description: User purged
        '400':
          description: Confirmation required
        '403':
          description: Feature not licensed, insufficient permission, or target role beyond actor authority
        '409':
          description: User or team is under legal hold
  /api/v1/enterprise/teams/{id}/purge:
    delete:
      operationId: purgeEnterpriseTeam
      tags:
        - Enterprise
      summary: Expurgar dados da equipe
      description: |
        Permanently purge a team's data after explicit confirmation. Requires compliance:manage permission and authority over every member's current role, including hierarchy, permissions, tool access, and any API-key scope. A team containing the actor cannot be purged.
      security:
        - bearerAuth: []
      parameters:
        - name: id
          in: path
          required: true
          schema:
            type: string
      responses:
        '200':
          description: Team purged
        '400':
          description: Confirmation required
        '403':
          description: Feature not licensed, insufficient permission, or a member role beyond actor authority
        '409':
          description: Team is under legal hold
  /api/v1/admin/version:
    get:
      operationId: getAdminVersion
      tags:
        - Admin
      summary: Metadados de versão
      description: Lê a versão do app, data de build, versão do Node e versão do schema. Requer a permissão system:health e o recurso enterprise de gerenciamento de upgrades.
      security:
        - bearerAuth: []
      responses:
        '200':
          description: Version metadata
        '403':
          description: Feature not licensed or insufficient permissions
  /api/v1/admin/migrations/pending:
    get:
      operationId: getPendingMigrations
      tags:
        - Admin
      summary: Status de migração
      description: Compara as migrações empacotadas com as migrações aplicadas. Requer a permissão system:health e o recurso enterprise de gerenciamento de upgrades.
      security:
        - bearerAuth: []
      responses:
        '200':
          description: Migration status
        '403':
          description: Feature not licensed or insufficient permissions
  /api/v1/admin/upgrade-check:
    get:
      operationId: getUpgradeCheck
      tags:
        - Admin
      summary: Prontidão para upgrade
      description: Executa verificações de prontidão para upgrade. Requer a permissão system:health e o recurso enterprise de gerenciamento de upgrades.
      security:
        - bearerAuth: []
      responses:
        '200':
          description: Upgrade readiness result
        '403':
          description: Feature not licensed or insufficient permissions
  /llms.txt:
    get:
      operationId: getLlmsTxt
      tags:
        - System
      summary: Resumo para LLM
      description: Resumo da API em texto simples amigável para LLM, gerado a partir da especificação OpenAPI.
      security: []
      responses:
        '200':
          description: Plain-text LLM summary
          content:
            text/plain:
              schema:
                type: string
  /llms-full.txt:
    get:
      operationId: getLlmsFullTxt
      tags:
        - System
      summary: Documentação completa da API para LLM
      description: Documentação completa da API em texto simples, gerada a partir da especificação OpenAPI.
      security: []
      responses:
        '200':
          description: Full plain-text API docs
          content:
            text/plain:
              schema:
                type: string
  /api/v1/openapi.yaml:
    get:
      operationId: getOpenApiYaml
      tags:
        - System
      summary: OpenAPI YAML
      description: Especificação OpenAPI 3.1 legível por máquina para esta instância do SnapOtter.
      security: []
      responses:
        '200':
          description: OpenAPI YAML document
          content:
            text/yaml:
              schema:
                type: string
  /api/v1/tools/popular:
    get:
      operationId: getPopularTools
      tags:
        - Tools
      summary: Ferramentas populares
      description: Retorna até 12 IDs de ferramentas populares, recorrendo a uma lista padrão curada quando os dados de uso são escassos.
      security: []
      responses:
        '200':
          description: Popular tool IDs
          content:
            application/json:
              schema:
                type: object
                properties:
                  tools:
                    type: array
                    items:
                      type: string
x-i18n:
  locale: pt-BR
  generator: scripts/i18n/adapters/api-spec.mjs
  entries:
    info.description:
      sourceHash: 0a7d56a602a9
      provenance: machine
      outputHash: f46e3bc4204b
    tags.Tools.description:
      sourceHash: 9c71553d06df
      provenance: machine
      outputHash: 1b77efc99f04
    tags.Batch.description:
      sourceHash: f9c99eda7653
      provenance: machine
      outputHash: c88f70a6a816
    tags.Pipelines.description:
      sourceHash: 18d60931b26d
      provenance: machine
      outputHash: ad5ee89d47fc
    tags.Files.description:
      sourceHash: c3b0e1590a61
      provenance: machine
      outputHash: 350f20c713af
    tags.Auth.description:
      sourceHash: 23f6a0fcf611
      provenance: machine
      outputHash: f4a26aca1763
    tags.API Keys.description:
      sourceHash: fb725a6a642a
      provenance: machine
      outputHash: 59eefc81f638
    tags.Settings.description:
      sourceHash: 25788b024fe2
      provenance: machine
      outputHash: 29ba74f1e183
    tags.Teams.description:
      sourceHash: 750d92c8bb9f
      provenance: machine
      outputHash: e89a6cc26aac
    tags.Roles.description:
      sourceHash: d031f589a884
      provenance: machine
      outputHash: 7672d9ab4a52
    tags.Audit.description:
      sourceHash: ed43b35a8636
      provenance: machine
      outputHash: ee320bfb83c5
    tags.Analytics.description:
      sourceHash: 36832a1b1df0
      provenance: machine
      outputHash: 7e64b80e7b09
    tags.Features.description:
      sourceHash: a6cdd16968b5
      provenance: machine
      outputHash: 643e05666c8d
    tags.Admin.description:
      sourceHash: c19c4b4f68c4
      provenance: machine
      outputHash: cb3cae5d3793
    tags.System.description:
      sourceHash: 91561bc11296
      provenance: machine
      outputHash: 93560c957075
    tags.Enterprise.description:
      sourceHash: 91b6b46b0dd1
      provenance: machine
      outputHash: a53e5a21fc0a
    paths./api/v1/health.get.summary:
      sourceHash: 14290e1d0681
      provenance: machine
      outputHash: 59d278f510e9
    paths./api/v1/health.get.description:
      sourceHash: 784e94eaa9df
      provenance: machine
      outputHash: d3092584765f
    paths./api/v1/tools/image/resize.post.summary:
      sourceHash: 6cc5dc48975b
      provenance: machine
      outputHash: 8175a4792066
    paths./api/v1/tools/image/resize.post.description:
      sourceHash: 3dd014df5258
      provenance: machine
      outputHash: a98cc6f86bb4
    paths./api/v1/tools/image/content-aware-resize.post.summary:
      sourceHash: 7f86a6451d78
      provenance: machine
      outputHash: 61e6180d29ed
    paths./api/v1/tools/image/content-aware-resize.post.description:
      sourceHash: fe6de26388f5
      provenance: machine
      outputHash: 0fd5cf646bcd
    paths./api/v1/tools/image/crop.post.summary:
      sourceHash: fb3dabcf0d02
      provenance: machine
      outputHash: d09caaa5410a
    paths./api/v1/tools/image/crop.post.description:
      sourceHash: db6c5c89d359
      provenance: machine
      outputHash: b157cf370486
    paths./api/v1/tools/image/rotate.post.summary:
      sourceHash: 816edfdfe65e
      provenance: machine
      outputHash: da1afe8da3c5
    paths./api/v1/tools/image/rotate.post.description:
      sourceHash: 5360b7d1a4c3
      provenance: machine
      outputHash: 661b98243a1c
    paths./api/v1/tools/image/convert.post.summary:
      sourceHash: 6ccca094d767
      provenance: machine
      outputHash: dd7052a01cb9
    paths./api/v1/tools/image/convert.post.description:
      sourceHash: 2298d8af460d
      provenance: machine
      outputHash: da4df04dbb74
    paths./api/v1/tools/image/compress.post.summary:
      sourceHash: a18573a37f49
      provenance: machine
      outputHash: a915a7ddabf1
    paths./api/v1/tools/image/compress.post.description:
      sourceHash: 01f63e9e928a
      provenance: machine
      outputHash: 7a720d8f7b52
    paths./api/v1/tools/image/strip-metadata.post.summary:
      sourceHash: 1b49ff96cee6
      provenance: machine
      outputHash: 962c724dd44b
    paths./api/v1/tools/image/strip-metadata.post.description:
      sourceHash: b5718bfd5335
      provenance: machine
      outputHash: 067143dfdb20
    paths./api/v1/tools/image/border.post.summary:
      sourceHash: 1d23e2771077
      provenance: machine
      outputHash: 8fdacbaed75e
    paths./api/v1/tools/image/border.post.description:
      sourceHash: 6d48679c08cd
      provenance: machine
      outputHash: 783f73229970
    paths./api/v1/tools/image/beautify.post.summary:
      sourceHash: 096b365107c5
      provenance: machine
      outputHash: 8b0328e3dc73
    paths./api/v1/tools/image/beautify.post.description:
      sourceHash: 7db7fb58263f
      provenance: machine
      outputHash: 8eade1c32211
    paths./api/v1/tools/image/adjust-colors.post.summary:
      sourceHash: a45b7d1e2535
      provenance: machine
      outputHash: c33bc8942c06
    paths./api/v1/tools/image/adjust-colors.post.description:
      sourceHash: 1fb36db32da7
      provenance: machine
      outputHash: f5d3cc577a56
    paths./api/v1/tools/image/sharpening.post.summary:
      sourceHash: d07b9f428bbf
      provenance: machine
      outputHash: 02f2a1bcad0e
    paths./api/v1/tools/image/sharpening.post.description:
      sourceHash: 8080cda0610a
      provenance: machine
      outputHash: fe42c1371e60
    paths./api/v1/tools/image/optimize-for-web.post.summary:
      sourceHash: 54245d072fef
      provenance: machine
      outputHash: 75f913817469
    paths./api/v1/tools/image/optimize-for-web.post.description:
      sourceHash: e846eaabbabc
      provenance: machine
      outputHash: 93bd3632c090
    paths./api/v1/tools/image/optimize-for-web/preview.post.summary:
      sourceHash: f50e6149b56c
      provenance: machine
      outputHash: 4b9eedb7410e
    paths./api/v1/tools/image/optimize-for-web/preview.post.description:
      sourceHash: 10a427197f6e
      provenance: machine
      outputHash: 08c67c5d08e4
    paths./api/v1/tools/image/image-enhancement.post.summary:
      sourceHash: 246a536c8207
      provenance: machine
      outputHash: 8838700d0376
    paths./api/v1/tools/image/image-enhancement.post.description:
      sourceHash: 17ec5e5e2b04
      provenance: machine
      outputHash: 7e02746c6dd8
    paths./api/v1/tools/image/image-enhancement/analyze.post.summary:
      sourceHash: 2697cf8b079f
      provenance: machine
      outputHash: bd2dc4b79331
    paths./api/v1/tools/image/image-enhancement/analyze.post.description:
      sourceHash: 7712bfd40c3a
      provenance: machine
      outputHash: f4d01e9313b3
    paths./api/v1/tools/image/noise-removal.post.summary:
      sourceHash: 9301fe47b563
      provenance: machine
      outputHash: dc2bb7066d43
    paths./api/v1/tools/image/noise-removal.post.description:
      sourceHash: e16aa743abcb
      provenance: machine
      outputHash: cdb76c931dca
    paths./api/v1/tools/image/red-eye-removal.post.summary:
      sourceHash: d9315c5a87b4
      provenance: machine
      outputHash: c942809a2742
    paths./api/v1/tools/image/red-eye-removal.post.description:
      sourceHash: 479c687988eb
      provenance: machine
      outputHash: '448805240899'
    paths./api/v1/tools/image/restore-photo.post.summary:
      sourceHash: 3ef425c0ba9b
      provenance: machine
      outputHash: 63d6eac3d277
    paths./api/v1/tools/image/restore-photo.post.description:
      sourceHash: 6e3b2de0372c
      provenance: machine
      outputHash: e80d270b5c2c
    paths./api/v1/tools/image/passport-photo/analyze.post.summary:
      sourceHash: 0a3c3687ff5f
      provenance: machine
      outputHash: 96ed952d6eed
    paths./api/v1/tools/image/passport-photo/analyze.post.description:
      sourceHash: 235c51cb497e
      provenance: machine
      outputHash: 6449160793f4
    paths./api/v1/tools/image/passport-photo/generate.post.summary:
      sourceHash: 029eecf0438e
      provenance: machine
      outputHash: '170145432e27'
    paths./api/v1/tools/image/passport-photo/generate.post.description:
      sourceHash: d0edd7c634d9
      provenance: machine
      outputHash: f580ca2fedcf
    paths./api/v1/tools/image/colorize.post.summary:
      sourceHash: 34df56e96234
      provenance: machine
      outputHash: 0a9e76f9aa9f
    paths./api/v1/tools/image/colorize.post.description:
      sourceHash: 04d7dc54396a
      provenance: machine
      outputHash: 0bec2258859f
    paths./api/v1/tools/image/enhance-faces.post.summary:
      sourceHash: 74f27131fec9
      provenance: machine
      outputHash: 6eacf3a14bb9
    paths./api/v1/tools/image/enhance-faces.post.description:
      sourceHash: beec2f7e8561
      provenance: machine
      outputHash: e8e84c86a22c
    paths./api/v1/tools/image/image-to-base64.post.summary:
      sourceHash: 440737c09f0d
      provenance: machine
      outputHash: c26fd815f28d
    paths./api/v1/tools/image/image-to-base64.post.description:
      sourceHash: 02a80d960185
      provenance: machine
      outputHash: 1bcb9c6454ed
    paths./api/v1/tools/image/watermark-text.post.summary:
      sourceHash: dfef3d1052de
      provenance: machine
      outputHash: 97ada96a6ac2
    paths./api/v1/tools/image/watermark-text.post.description:
      sourceHash: 158e444c0a77
      provenance: machine
      outputHash: 15c8a5159f2b
    paths./api/v1/tools/image/text-overlay.post.summary:
      sourceHash: 7311c0ffc340
      provenance: machine
      outputHash: 4613abe151f6
    paths./api/v1/tools/image/text-overlay.post.description:
      sourceHash: e5dd74752a49
      provenance: machine
      outputHash: fca251f3e896
    paths./api/v1/tools/image/replace-color.post.summary:
      sourceHash: 80e60e5fe0ed
      provenance: machine
      outputHash: 406710a9e246
    paths./api/v1/tools/image/replace-color.post.description:
      sourceHash: cbafc4d168c3
      provenance: machine
      outputHash: d561f2f59ca5
    paths./api/v1/tools/image/color-blindness.post.summary:
      sourceHash: 1ab73f30893d
      provenance: machine
      outputHash: 41ab8f42f49d
    paths./api/v1/tools/image/color-blindness.post.description:
      sourceHash: 1e362d094237
      provenance: machine
      outputHash: 863f5463d9e8
    paths./api/v1/tools/image/gif-tools.post.summary:
      sourceHash: 985d94d31548
      provenance: machine
      outputHash: f54c062306e5
    paths./api/v1/tools/image/gif-tools.post.description:
      sourceHash: fe7113c6fefd
      provenance: machine
      outputHash: 3757f0d13822
    paths./api/v1/tools/image/gif-tools/info.post.summary:
      sourceHash: e864c10b29f7
      provenance: machine
      outputHash: f0a2aefdecca
    paths./api/v1/tools/image/gif-tools/info.post.description:
      sourceHash: aaeba6b11dbf
      provenance: machine
      outputHash: 22511c9fa982
    paths./api/v1/tools/image/smart-crop.post.summary:
      sourceHash: 890848540d35
      provenance: machine
      outputHash: edb439b3278c
    paths./api/v1/tools/image/smart-crop.post.description:
      sourceHash: 0f00f334ba29
      provenance: machine
      outputHash: 8f1699133717
    paths./api/v1/tools/image/vectorize.post.summary:
      sourceHash: ffa9db773ada
      provenance: machine
      outputHash: d885baac27a3
    paths./api/v1/tools/image/vectorize.post.description:
      sourceHash: 3d5de38dc3f3
      provenance: machine
      outputHash: 962f62a369a0
    paths./api/v1/tools/image/svg-to-raster.post.summary:
      sourceHash: bccef23ad06b
      provenance: machine
      outputHash: a6d52999510f
    paths./api/v1/tools/image/svg-to-raster.post.description:
      sourceHash: 27a0b0dfe170
      provenance: machine
      outputHash: 03d34f7434b6
    paths./api/v1/tools/image/svg-to-raster/batch.post.summary:
      sourceHash: 340743c4e68b
      provenance: machine
      outputHash: 913283f5c06f
    paths./api/v1/tools/image/svg-to-raster/batch.post.description:
      sourceHash: a0069d315b7e
      provenance: machine
      outputHash: d4cb484d9206
    paths./api/v1/tools/image/image-to-pdf.post.summary:
      sourceHash: 966eba6e1412
      provenance: machine
      outputHash: 5aa8d35d7bca
    paths./api/v1/tools/image/image-to-pdf.post.description:
      sourceHash: f5a0819f56f7
      provenance: machine
      outputHash: b319c6024be5
    paths./api/v1/tools/pdf/pdf-to-image.post.summary:
      sourceHash: 4c3c2ae28b1f
      provenance: machine
      outputHash: 28f8ae1fafae
    paths./api/v1/tools/pdf/pdf-to-image.post.description:
      sourceHash: 4f0f5c643801
      provenance: machine
      outputHash: 3978f31eb95d
    paths./api/v1/tools/pdf/pdf-to-image/info.post.summary:
      sourceHash: d9398268b597
      provenance: machine
      outputHash: 3666e7d993da
    paths./api/v1/tools/pdf/pdf-to-image/info.post.description:
      sourceHash: 9ace4aaa3978
      provenance: machine
      outputHash: 86ffc43951de
    paths./api/v1/tools/pdf/pdf-to-image/preview.post.summary:
      sourceHash: 3bd369a401ba
      provenance: machine
      outputHash: 8a8d1bf310c3
    paths./api/v1/tools/pdf/pdf-to-image/preview.post.description:
      sourceHash: 90db92475b9a
      provenance: machine
      outputHash: 64255b0adc07
    paths./api/v1/tools/image/split.post.summary:
      sourceHash: 6a45e35cf509
      provenance: machine
      outputHash: 0fd5da0e6080
    paths./api/v1/tools/image/split.post.description:
      sourceHash: d7d4988765b3
      provenance: machine
      outputHash: abf0094fa596
    paths./api/v1/tools/image/bulk-rename.post.summary:
      sourceHash: 5489fe2b69b0
      provenance: machine
      outputHash: cbc49a2989c5
    paths./api/v1/tools/image/bulk-rename.post.description:
      sourceHash: 093bf3688b13
      provenance: machine
      outputHash: 7eddc1b18cad
    paths./api/v1/tools/image/favicon.post.summary:
      sourceHash: 7a587af97797
      provenance: machine
      outputHash: 34f0d1c6abf7
    paths./api/v1/tools/image/favicon.post.description:
      sourceHash: 5351a1c80471
      provenance: machine
      outputHash: b675dcf5f953
    paths./api/v1/tools/image/watermark-image.post.summary:
      sourceHash: 577e5e431f77
      provenance: machine
      outputHash: 9ae66b66bf4e
    paths./api/v1/tools/image/watermark-image.post.description:
      sourceHash: 4e103b8a7d17
      provenance: machine
      outputHash: b24e1cba5b86
    paths./api/v1/tools/image/compose.post.summary:
      sourceHash: 06718c2a3b98
      provenance: machine
      outputHash: 9c3fa9976a3b
    paths./api/v1/tools/image/compose.post.description:
      sourceHash: d48d40e77edf
      provenance: machine
      outputHash: 9b3612ea61b1
    paths./api/v1/tools/image/meme-generator.post.summary:
      sourceHash: a4e89614a5c1
      provenance: machine
      outputHash: 90a0a9820d9f
    paths./api/v1/tools/image/meme-generator.post.description:
      sourceHash: 7aa04984be62
      provenance: machine
      outputHash: 96ad3a461842
    paths./api/v1/meme-templates.get.summary:
      sourceHash: fd3392277306
      provenance: machine
      outputHash: 23f37621537e
    paths./api/v1/meme-templates.get.description:
      sourceHash: a147e348597c
      provenance: machine
      outputHash: 3fcbad21dc4a
    paths./api/v1/meme-templates/full/{filename}.get.summary:
      sourceHash: fd501704c913
      provenance: machine
      outputHash: 0dace3386f9e
    paths./api/v1/meme-templates/full/{filename}.get.description:
      sourceHash: 3bb962f4402c
      provenance: machine
      outputHash: 1b1a48261cb0
    paths./api/v1/meme-templates/thumbs/{filename}.get.summary:
      sourceHash: d2102190f5a2
      provenance: machine
      outputHash: baed1a82e39b
    paths./api/v1/meme-templates/thumbs/{filename}.get.description:
      sourceHash: 77c69805732d
      provenance: machine
      outputHash: 61a8904ae686
    paths./api/v1/meme-templates/fonts/{filename}.get.summary:
      sourceHash: 3627280f2609
      provenance: machine
      outputHash: 932161b00fbb
    paths./api/v1/meme-templates/fonts/{filename}.get.description:
      sourceHash: 717c6764f472
      provenance: machine
      outputHash: 32f9b526de33
    paths./api/v1/tools/image/compare.post.summary:
      sourceHash: 46e3612ffee1
      provenance: machine
      outputHash: 1ca508c93ab4
    paths./api/v1/tools/image/compare.post.description:
      sourceHash: ac21fee13ecc
      provenance: machine
      outputHash: ddaba39a26ac
    paths./api/v1/tools/image/erase-object.post.summary:
      sourceHash: 21f13ba2930d
      provenance: machine
      outputHash: 4a4471b47650
    paths./api/v1/tools/image/erase-object.post.description:
      sourceHash: 9d6614ed28c6
      provenance: machine
      outputHash: 41113e1a8392
    paths./api/v1/tools/image/ai-canvas-expand.post.summary:
      sourceHash: 990aeca723ff
      provenance: machine
      outputHash: 300f4139736f
    paths./api/v1/tools/image/ai-canvas-expand.post.description:
      sourceHash: 084de536af45
      provenance: machine
      outputHash: 80625ad48fa6
    paths./api/v1/tools/image/collage.post.summary:
      sourceHash: 6e5e905e8c16
      provenance: machine
      outputHash: 5f4e30151ddd
    paths./api/v1/tools/image/collage.post.description:
      sourceHash: fb0ba3d2cf44
      provenance: machine
      outputHash: 8e4a5cbc81a8
    paths./api/v1/tools/image/stitch.post.summary:
      sourceHash: d0e2b34fe22b
      provenance: machine
      outputHash: 3a166c1c0663
    paths./api/v1/tools/image/stitch.post.description:
      sourceHash: 914837a5d232
      provenance: machine
      outputHash: f8da4958af00
    paths./api/v1/tools/image/remove-gif-background.post.summary:
      sourceHash: a316267cf5d1
      provenance: machine
      outputHash: 127fac1a93b9
    paths./api/v1/tools/image/remove-gif-background.post.description:
      sourceHash: ef1d100f2345
      provenance: machine
      outputHash: ab7f450b2ff8
    paths./api/v1/tools/image/remove-background.post.summary:
      sourceHash: a8d1ab54b32c
      provenance: machine
      outputHash: 9a74cc03501d
    paths./api/v1/tools/image/remove-background.post.description:
      sourceHash: 3bb193c0a2f9
      provenance: machine
      outputHash: e268eaf71972
    paths./api/v1/tools/image/remove-background/effects.post.summary:
      sourceHash: 2194d6947f3f
      provenance: machine
      outputHash: f50b764642fe
    paths./api/v1/tools/image/remove-background/effects.post.description:
      sourceHash: 1e2ab8e3b1ae
      provenance: machine
      outputHash: 60a1aca8c5a0
    paths./api/v1/tools/image/transparency-fixer.post.summary:
      sourceHash: 0cf307cacb91
      provenance: machine
      outputHash: efd29a1d70fa
    paths./api/v1/tools/image/transparency-fixer.post.description:
      sourceHash: 67b2a3c1cb7b
      provenance: machine
      outputHash: 90462182fe76
    paths./api/v1/tools/image/upscale.post.summary:
      sourceHash: ce44856d9e64
      provenance: machine
      outputHash: 4aad6728b7e3
    paths./api/v1/tools/image/upscale.post.description:
      sourceHash: e603db60e711
      provenance: machine
      outputHash: b1479f2eea1b
    paths./api/v1/tools/image/blur-faces.post.summary:
      sourceHash: d9a7e1d9a145
      provenance: machine
      outputHash: 0a1338e0e4e7
    paths./api/v1/tools/image/blur-faces.post.description:
      sourceHash: 28790ae08ac7
      provenance: machine
      outputHash: 1db53ee14df9
    paths./api/v1/tools/image/ocr.post.summary:
      sourceHash: 0b508f19963b
      provenance: machine
      outputHash: 4143d8dd5f55
    paths./api/v1/tools/image/ocr.post.description:
      sourceHash: 7b74b30329a5
      provenance: machine
      outputHash: bf3ee7dc7483
    paths./api/v1/tools/image/info.post.summary:
      sourceHash: 1823e118aed5
      provenance: machine
      outputHash: 0998487efcca
    paths./api/v1/tools/image/info.post.description:
      sourceHash: 196d7190f16c
      provenance: machine
      outputHash: 320d13116a83
    paths./api/v1/tools/image/color-palette.post.summary:
      sourceHash: 81a9be1a8044
      provenance: machine
      outputHash: 9361c451e985
    paths./api/v1/tools/image/color-palette.post.description:
      sourceHash: d34d5ff2ade7
      provenance: machine
      outputHash: f03c2eaf1255
    paths./api/v1/tools/image/barcode-read.post.summary:
      sourceHash: b51dcac65420
      provenance: machine
      outputHash: 5337b0cce1ec
    paths./api/v1/tools/image/barcode-read.post.description:
      sourceHash: ae952218a233
      provenance: machine
      outputHash: a1b65e66491b
    paths./api/v1/tools/image/find-duplicates.post.summary:
      sourceHash: bd6e5d3e4d26
      provenance: machine
      outputHash: 26fceffe2cf0
    paths./api/v1/tools/image/find-duplicates.post.description:
      sourceHash: 61322792b4bd
      provenance: machine
      outputHash: 8afced369fb4
    paths./api/v1/tools/image/qr-generate.post.summary:
      sourceHash: 799653bb4ee1
      provenance: machine
      outputHash: 69f5a7a05bdb
    paths./api/v1/tools/image/qr-generate.post.description:
      sourceHash: ad4154c7b555
      provenance: machine
      outputHash: 1101fc89896e
    paths./api/v1/tools/image/html-to-image.post.summary:
      sourceHash: d4dcd8d52cfd
      provenance: machine
      outputHash: e0ac47d0bb32
    paths./api/v1/tools/image/html-to-image.post.description:
      sourceHash: adb3e27eb0e6
      provenance: machine
      outputHash: bf8457a966b9
    paths./api/v1/tools/image/strip-metadata/inspect.post.summary:
      sourceHash: 26c48e56ad62
      provenance: machine
      outputHash: 4915a2c6ee49
    paths./api/v1/tools/image/strip-metadata/inspect.post.description:
      sourceHash: a6a7d5c72088
      provenance: machine
      outputHash: bc2f20adfbe2
    paths./api/v1/tools/image/edit-metadata.post.summary:
      sourceHash: b2e22f0aa9d8
      provenance: machine
      outputHash: 613090af2db0
    paths./api/v1/tools/image/edit-metadata.post.description:
      sourceHash: 70d277ec5566
      provenance: machine
      outputHash: 1dca28fa2864
    paths./api/v1/tools/image/edit-metadata/inspect.post.summary:
      sourceHash: a9b31ad7470f
      provenance: machine
      outputHash: f8d5e4d8e59c
    paths./api/v1/tools/image/edit-metadata/inspect.post.description:
      sourceHash: 71173fd89d89
      provenance: machine
      outputHash: b4ce653ec279
    paths./api/v1/tools/{section}/{toolId}/batch.post.summary:
      sourceHash: c48b2b0a9212
      provenance: machine
      outputHash: 5f070ae8a284
    paths./api/v1/tools/{section}/{toolId}/batch.post.description:
      sourceHash: e2ddfe55094d
      provenance: machine
      outputHash: f5cc9e5300a5
    paths./api/v1/pipeline/execute.post.summary:
      sourceHash: 6357b47fa7a9
      provenance: machine
      outputHash: 6534c5588288
    paths./api/v1/pipeline/execute.post.description:
      sourceHash: ca3c9eb4acf1
      provenance: machine
      outputHash: 7afc2817cda5
    paths./api/v1/pipeline/save.post.summary:
      sourceHash: 8209ec3e0c9b
      provenance: machine
      outputHash: 486ce0f3bca3
    paths./api/v1/pipeline/save.post.description:
      sourceHash: a904ab4f3701
      provenance: machine
      outputHash: f93d9972188b
    paths./api/v1/pipeline/list.get.summary:
      sourceHash: c8d290eff4a7
      provenance: machine
      outputHash: a1b3969a5f60
    paths./api/v1/pipeline/list.get.description:
      sourceHash: d11170073568
      provenance: machine
      outputHash: 13d912b2f980
    paths./api/v1/pipeline/{id}.delete.summary:
      sourceHash: 0a798831c1be
      provenance: machine
      outputHash: e6ce99febfe0
    paths./api/v1/pipeline/{id}.delete.description:
      sourceHash: e3b43bf6d73d
      provenance: machine
      outputHash: 96960690d4a8
    paths./api/v1/pipeline/tools.get.summary:
      sourceHash: 79f28e6075f7
      provenance: machine
      outputHash: 54a7b0beb02d
    paths./api/v1/pipeline/tools.get.description:
      sourceHash: 6f341cc670d3
      provenance: machine
      outputHash: 670fef5dde69
    paths./api/v1/pipeline/batch.post.summary:
      sourceHash: 14abb819b562
      provenance: machine
      outputHash: 8a13dae95444
    paths./api/v1/pipeline/batch.post.description:
      sourceHash: 0c7b90acb5d8
      provenance: machine
      outputHash: c9d1f7422cac
    paths./api/v1/upload.post.summary:
      sourceHash: 25d0eba68bdc
      provenance: machine
      outputHash: f347c8d58e0d
    paths./api/v1/upload.post.description:
      sourceHash: a9e18800407e
      provenance: machine
      outputHash: e30e3c9dafb5
    paths./api/v1/download/{jobId}/{filename}.get.summary:
      sourceHash: 6246d8c07e4b
      provenance: machine
      outputHash: 5afbef1e5eb2
    paths./api/v1/download/{jobId}/{filename}.get.description:
      sourceHash: a09d603b069c
      provenance: machine
      outputHash: a39f5ab04c36
    paths./api/v1/preview.post.summary:
      sourceHash: 8266fbd3ff72
      provenance: machine
      outputHash: 6f72771be0eb
    paths./api/v1/preview.post.description:
      sourceHash: d5e04d49c594
      provenance: machine
      outputHash: deb1ad917b92
    paths./api/v1/files.get.summary:
      sourceHash: aefb51ff8aaa
      provenance: machine
      outputHash: 17875082812a
    paths./api/v1/files.get.description:
      sourceHash: 45f45ad625ed
      provenance: machine
      outputHash: 6dbe4e751c09
    paths./api/v1/files.delete.summary:
      sourceHash: 179bb72a6deb
      provenance: machine
      outputHash: f14f2003ea76
    paths./api/v1/files.delete.description:
      sourceHash: 1bfaa5c376bb
      provenance: machine
      outputHash: 93d3f519e90c
    paths./api/v1/files/upload.post.summary:
      sourceHash: b94c597782ee
      provenance: machine
      outputHash: 63789e10ea76
    paths./api/v1/files/upload.post.description:
      sourceHash: 8a070581c1e5
      provenance: machine
      outputHash: e614b6b9bf23
    paths./api/v1/files/save-result.post.summary:
      sourceHash: c35e320d9164
      provenance: machine
      outputHash: e10e7047b587
    paths./api/v1/files/save-result.post.description:
      sourceHash: 3c434713031e
      provenance: machine
      outputHash: ed282e2206ab
    paths./api/v1/files/{id}.get.summary:
      sourceHash: a09c1fb7e33a
      provenance: machine
      outputHash: d9b9e8ceae03
    paths./api/v1/files/{id}.get.description:
      sourceHash: 8193066e23c5
      provenance: machine
      outputHash: 86db76e6a531
    paths./api/v1/files/{id}/download.get.summary:
      sourceHash: c0043bd46394
      provenance: machine
      outputHash: 24fd18b91abc
    paths./api/v1/files/{id}/download.get.description:
      sourceHash: 96df2a599e22
      provenance: machine
      outputHash: 699368b6b933
    paths./api/v1/files/{id}/thumbnail.get.summary:
      sourceHash: 07714ad0c5f8
      provenance: machine
      outputHash: f679a2f71cc7
    paths./api/v1/files/{id}/thumbnail.get.description:
      sourceHash: 9b04bbf75cba
      provenance: machine
      outputHash: c14890d12137
    paths./api/v1/fetch-urls.post.summary:
      sourceHash: a00341e59161
      provenance: machine
      outputHash: 1a0f0b21a47b
    paths./api/v1/fetch-urls.post.description:
      sourceHash: ac474639261c
      provenance: machine
      outputHash: 09e36f846f7b
    paths./api/v1/config/locale.get.summary:
      sourceHash: 78de2255222d
      provenance: machine
      outputHash: 2ad4bef1d6eb
    paths./api/v1/config/locale.get.description:
      sourceHash: b9967e400d09
      provenance: machine
      outputHash: f96d2f736d9b
    paths./api/auth/login.post.summary:
      sourceHash: c189840cf7e2
      provenance: machine
      outputHash: 2fae3bc5164c
    paths./api/auth/login.post.description:
      sourceHash: 91ad9a828bf3
      provenance: machine
      outputHash: 52a498d6598b
    paths./api/auth/logout.post.summary:
      sourceHash: 49616145514e
      provenance: machine
      outputHash: 43afd54c89e3
    paths./api/auth/logout.post.description:
      sourceHash: 0b9ae300808d
      provenance: machine
      outputHash: 4734822ad8fe
    paths./api/auth/session.get.summary:
      sourceHash: f4f7376ca1ea
      provenance: machine
      outputHash: 730019b8917b
    paths./api/auth/session.get.description:
      sourceHash: 2dad822c46ea
      provenance: machine
      outputHash: 1e0108cb1572
    paths./api/auth/change-password.post.summary:
      sourceHash: 3f9c991f63a9
      provenance: machine
      outputHash: 32f9c37cfc7d
    paths./api/auth/change-password.post.description:
      sourceHash: 5190db8a04e7
      provenance: machine
      outputHash: 441b048974b6
    paths./api/auth/users.get.summary:
      sourceHash: '9029730603e0'
      provenance: machine
      outputHash: ab1e70bc87fa
    paths./api/auth/users.get.description:
      sourceHash: 34d5a11733bd
      provenance: machine
      outputHash: fdb8fb70948e
    paths./api/auth/register.post.summary:
      sourceHash: f06da128c342
      provenance: machine
      outputHash: '127384371311'
    paths./api/auth/users/{id}.put.summary:
      sourceHash: 602b8b1607b0
      provenance: machine
      outputHash: 7738a0af8d16
    paths./api/auth/users/{id}.delete.summary:
      sourceHash: 58363fd94240
      provenance: machine
      outputHash: 6d396d03df00
    paths./api/auth/users/{id}/reset-password.post.summary:
      sourceHash: aae899c2d3ad
      provenance: machine
      outputHash: a59c25c6b84d
    paths./api/v1/api-keys.get.summary:
      sourceHash: ad0527749f03
      provenance: machine
      outputHash: 37bd4c73a6ce
    paths./api/v1/api-keys.get.description:
      sourceHash: 470cee6220f0
      provenance: machine
      outputHash: cedf39b6bde9
    paths./api/v1/api-keys.post.summary:
      sourceHash: eb136082215a
      provenance: machine
      outputHash: 345affdf0a0b
    paths./api/v1/api-keys.post.description:
      sourceHash: 7568aec4ab9b
      provenance: machine
      outputHash: 5d4347adf21e
    paths./api/v1/api-keys/{id}.delete.summary:
      sourceHash: 9ebde15b7f02
      provenance: machine
      outputHash: 3009e1000fa7
    paths./api/v1/api-keys/{id}.delete.description:
      sourceHash: 46a57dbc68a5
      provenance: machine
      outputHash: 50be225c3646
    paths./api/v1/settings.get.summary:
      sourceHash: 8530e17de153
      provenance: machine
      outputHash: e6f093f09f4e
    paths./api/v1/settings.get.description:
      sourceHash: 65d8ff0f5873
      provenance: machine
      outputHash: c6fe75e15bc0
    paths./api/v1/settings.put.summary:
      sourceHash: 46ed69579c39
      provenance: machine
      outputHash: 78d8117d251c
    paths./api/v1/settings.put.description:
      sourceHash: 887f8facdd94
      provenance: machine
      outputHash: 327f6546fdab
    paths./api/v1/settings/{key}.get.summary:
      sourceHash: 186add054525
      provenance: machine
      outputHash: 14b11f7a54c5
    paths./api/v1/settings/{key}.get.description:
      sourceHash: 962db9ab06a3
      provenance: machine
      outputHash: 9881141d8b55
    paths./api/v1/teams.get.summary:
      sourceHash: 863546c57b78
      provenance: machine
      outputHash: 528370eea5c3
    paths./api/v1/teams.get.description:
      sourceHash: 88ce5170c6ed
      provenance: machine
      outputHash: 4e646bca444d
    paths./api/v1/teams.post.summary:
      sourceHash: 00766ad4b096
      provenance: machine
      outputHash: 02e121c913e2
    paths./api/v1/teams.post.description:
      sourceHash: 88ce5170c6ed
      provenance: machine
      outputHash: 4e646bca444d
    paths./api/v1/teams/{id}.put.summary:
      sourceHash: c5dd33827a5b
      provenance: machine
      outputHash: 270557337a40
    paths./api/v1/teams/{id}.put.description:
      sourceHash: 88ce5170c6ed
      provenance: machine
      outputHash: 4e646bca444d
    paths./api/v1/teams/{id}.delete.summary:
      sourceHash: b51488367ba6
      provenance: machine
      outputHash: 27b2e711e6a5
    paths./api/v1/teams/{id}.delete.description:
      sourceHash: 88ce5170c6ed
      provenance: machine
      outputHash: 4e646bca444d
    paths./api/v1/config/auth.get.summary:
      sourceHash: 13189298880a
      provenance: machine
      outputHash: 3f56822d9049
    paths./api/v1/config/auth.get.description:
      sourceHash: 404909083a24
      provenance: machine
      outputHash: f0f7113bc10a
    paths./api/v1/jobs/{jobId}/progress.get.summary:
      sourceHash: bcf19846aa43
      provenance: machine
      outputHash: e9e36feeec9f
    paths./api/v1/jobs/{jobId}/progress.get.description:
      sourceHash: 2df913c75d85
      provenance: human
      outputHash: cfb5327a040b
    paths./api/v1/admin/health.get.summary:
      sourceHash: 92f8575c9c94
      provenance: machine
      outputHash: 553cbd9c612f
    paths./api/v1/admin/health.get.description:
      sourceHash: 4abbba893ea4
      provenance: machine
      outputHash: 1421f877c1f1
    paths./api/v1/config/analytics.get.summary:
      sourceHash: b7fdcf112830
      provenance: machine
      outputHash: cec644d7a582
    paths./api/v1/config/analytics.get.description:
      sourceHash: 2447c239373f
      provenance: machine
      outputHash: b651e83a1341
    paths./api/v1/features.get.summary:
      sourceHash: e45ee3163f96
      provenance: machine
      outputHash: 4527ceb10930
    paths./api/v1/features.get.description:
      sourceHash: aa0b433ca9a9
      provenance: machine
      outputHash: 75b32e1ba0da
    paths./api/v1/admin/features/{bundleId}/install.post.summary:
      sourceHash: bebc6f7ea7c3
      provenance: machine
      outputHash: eff263b3c10a
    paths./api/v1/admin/features/{bundleId}/install.post.description:
      sourceHash: 6051af19be7e
      provenance: machine
      outputHash: 6dfa7a87e94f
    paths./api/v1/admin/features/{bundleId}/uninstall.post.summary:
      sourceHash: 160eaf179d4f
      provenance: machine
      outputHash: 59aa4d15e443
    paths./api/v1/admin/features/{bundleId}/uninstall.post.description:
      sourceHash: 35bdb459a1a7
      provenance: machine
      outputHash: 2545a68b7db5
    paths./api/v1/admin/features/disk-usage.get.summary:
      sourceHash: 54dc8991b9a7
      provenance: machine
      outputHash: 3da61ea367c5
    paths./api/v1/admin/features/disk-usage.get.description:
      sourceHash: a78fac026ea3
      provenance: machine
      outputHash: 101edcf5ebd5
    paths./api/v1/audit-log.get.summary:
      sourceHash: 05536545727c
      provenance: machine
      outputHash: c800d34d3bf2
    paths./api/v1/audit-log.get.description:
      sourceHash: b7950dcf7e49
      provenance: machine
      outputHash: ce390d12022f
    paths./api/v1/roles.get.summary:
      sourceHash: 833a75e86b93
      provenance: machine
      outputHash: 4e5b80d7c857
    paths./api/v1/roles.get.description:
      sourceHash: 9ccdec8630d3
      provenance: machine
      outputHash: 25f28d1ec2cf
    paths./api/v1/roles.post.summary:
      sourceHash: 6dd06b2529d4
      provenance: machine
      outputHash: 1defc74bb77e
    paths./api/v1/roles/{id}.put.summary:
      sourceHash: 6c0cda1bc967
      provenance: machine
      outputHash: 67042dd5db3c
    paths./api/v1/roles/{id}.delete.summary:
      sourceHash: d5a3ab01055a
      provenance: machine
      outputHash: 835fbe82053f
    paths./api/v1/tools/video/convert-video.post.summary:
      sourceHash: b4defe1d6906
      provenance: machine
      outputHash: 3031379a7e4d
    paths./api/v1/tools/video/convert-video.post.description:
      sourceHash: 00ab19fbffe4
      provenance: machine
      outputHash: 2951b2a984cb
    paths./api/v1/tools/video/compress-video.post.summary:
      sourceHash: 8af3d4b27a4b
      provenance: machine
      outputHash: 0a1de7efd8cd
    paths./api/v1/tools/video/compress-video.post.description:
      sourceHash: 7f517e885483
      provenance: machine
      outputHash: e914b898ce0d
    paths./api/v1/tools/video/trim-video.post.summary:
      sourceHash: af6ed946e090
      provenance: machine
      outputHash: 5fd5b9aaac35
    paths./api/v1/tools/video/trim-video.post.description:
      sourceHash: c01dd24a19dd
      provenance: machine
      outputHash: 90654d063f05
    paths./api/v1/tools/video/mute-video.post.summary:
      sourceHash: 48ed61cb4802
      provenance: machine
      outputHash: 6c566dc517b7
    paths./api/v1/tools/video/mute-video.post.description:
      sourceHash: 76898fc22f53
      provenance: machine
      outputHash: 10c77533d06c
    paths./api/v1/tools/video/video-to-gif.post.summary:
      sourceHash: 4abd5ab74c35
      provenance: machine
      outputHash: 3179c7d1e6a4
    paths./api/v1/tools/video/video-to-gif.post.description:
      sourceHash: d2d195459149
      provenance: machine
      outputHash: 1f0004ece916
    paths./api/v1/tools/video/resize-video.post.summary:
      sourceHash: 99fde7cc129d
      provenance: machine
      outputHash: 40073f83247f
    paths./api/v1/tools/video/resize-video.post.description:
      sourceHash: ffe090ccf55b
      provenance: machine
      outputHash: 603b5402b426
    paths./api/v1/tools/video/crop-video.post.summary:
      sourceHash: f042b21b17c0
      provenance: machine
      outputHash: faf7ed436e62
    paths./api/v1/tools/video/crop-video.post.description:
      sourceHash: a70528f0d600
      provenance: machine
      outputHash: 5e718984e537
    paths./api/v1/tools/video/rotate-video.post.summary:
      sourceHash: a1d5d2488fd5
      provenance: machine
      outputHash: 3d6e13da1d4a
    paths./api/v1/tools/video/rotate-video.post.description:
      sourceHash: 0858085b737b
      provenance: machine
      outputHash: 8df2266b227a
    paths./api/v1/tools/video/change-fps.post.summary:
      sourceHash: bc48dbea9413
      provenance: machine
      outputHash: 60a00009ecb6
    paths./api/v1/tools/video/change-fps.post.description:
      sourceHash: 20c9ae3f7c23
      provenance: machine
      outputHash: 1ffea2912411
    paths./api/v1/tools/video/video-color.post.summary:
      sourceHash: 6005cc99c426
      provenance: machine
      outputHash: 40c866e483aa
    paths./api/v1/tools/video/video-color.post.description:
      sourceHash: f8ce7dae4f04
      provenance: machine
      outputHash: 417391c92e56
    paths./api/v1/tools/video/video-speed.post.summary:
      sourceHash: 4439901f7d61
      provenance: machine
      outputHash: 0441c48f3c5f
    paths./api/v1/tools/video/video-speed.post.description:
      sourceHash: 7a4b64e60829
      provenance: machine
      outputHash: a01afb0aa919
    paths./api/v1/tools/video/reverse-video.post.summary:
      sourceHash: 4f0d7b5ea9ae
      provenance: machine
      outputHash: 6208940c5282
    paths./api/v1/tools/video/reverse-video.post.description:
      sourceHash: 77abcb48d823
      provenance: machine
      outputHash: 9fff1f645454
    paths./api/v1/tools/video/video-loudnorm.post.summary:
      sourceHash: e502f3536632
      provenance: machine
      outputHash: 21e626a2398e
    paths./api/v1/tools/video/video-loudnorm.post.description:
      sourceHash: 019f6147f722
      provenance: machine
      outputHash: a337cfe27df6
    paths./api/v1/tools/video/aspect-pad.post.summary:
      sourceHash: aa7c02eeacf1
      provenance: machine
      outputHash: 987c5d4fdee2
    paths./api/v1/tools/video/aspect-pad.post.description:
      sourceHash: 288b33cc62de
      provenance: machine
      outputHash: 75a81c219744
    paths./api/v1/tools/video/blur-pad.post.summary:
      sourceHash: 84af9c217d52
      provenance: machine
      outputHash: a3efefe0f9d5
    paths./api/v1/tools/video/blur-pad.post.description:
      sourceHash: d49a3fab5e02
      provenance: machine
      outputHash: a51410c0085f
    paths./api/v1/tools/video/watermark-video.post.summary:
      sourceHash: bbbe197c46c5
      provenance: machine
      outputHash: 740e67b91cfa
    paths./api/v1/tools/video/watermark-video.post.description:
      sourceHash: d914078985d7
      provenance: machine
      outputHash: 15e112ecb08b
    paths./api/v1/tools/video/stabilize-video.post.summary:
      sourceHash: f76615a74aac
      provenance: machine
      outputHash: 45687ed21207
    paths./api/v1/tools/video/stabilize-video.post.description:
      sourceHash: 42e5e8a954ae
      provenance: machine
      outputHash: 246552249f26
    paths./api/v1/tools/video/gif-to-video.post.summary:
      sourceHash: 766d5d54badb
      provenance: machine
      outputHash: 3cf4223a267a
    paths./api/v1/tools/video/gif-to-video.post.description:
      sourceHash: 22a13cbfe672
      provenance: machine
      outputHash: a7d792839535
    paths./api/v1/tools/video/video-to-webp.post.summary:
      sourceHash: 0c4028ecac61
      provenance: machine
      outputHash: ec091bedb1c1
    paths./api/v1/tools/video/video-to-webp.post.description:
      sourceHash: 48fa0306cbe8
      provenance: machine
      outputHash: 491cfc457883
    paths./api/v1/tools/video/video-to-frames.post.summary:
      sourceHash: 8941a5926866
      provenance: machine
      outputHash: 8735076d5945
    paths./api/v1/tools/video/video-to-frames.post.description:
      sourceHash: 907e19fcf6a2
      provenance: machine
      outputHash: d19559007734
    paths./api/v1/tools/video/merge-videos.post.summary:
      sourceHash: afbd2e62e1dd
      provenance: machine
      outputHash: b46a84f2139c
    paths./api/v1/tools/video/merge-videos.post.description:
      sourceHash: 30e77c05314f
      provenance: machine
      outputHash: 135ccfd188f3
    paths./api/v1/tools/video/replace-audio.post.summary:
      sourceHash: 0cd096774105
      provenance: machine
      outputHash: 87c382f3acad
    paths./api/v1/tools/video/replace-audio.post.description:
      sourceHash: f9aef92c2ad2
      provenance: machine
      outputHash: c345847b1fe3
    paths./api/v1/tools/video/burn-subtitles.post.summary:
      sourceHash: 53e23068613b
      provenance: machine
      outputHash: 643e0de8ab4b
    paths./api/v1/tools/video/burn-subtitles.post.description:
      sourceHash: a501f3253970
      provenance: machine
      outputHash: 59c917e9b1b0
    paths./api/v1/tools/video/embed-subtitles.post.summary:
      sourceHash: 55a0c6104e9e
      provenance: machine
      outputHash: ff1edf2bca2e
    paths./api/v1/tools/video/embed-subtitles.post.description:
      sourceHash: 9ed219a50b61
      provenance: machine
      outputHash: 35a1e3768ba7
    paths./api/v1/tools/video/extract-subtitles.post.summary:
      sourceHash: 0ae3dd9f7012
      provenance: machine
      outputHash: e38c49f123fd
    paths./api/v1/tools/video/extract-subtitles.post.description:
      sourceHash: 4daf70632777
      provenance: machine
      outputHash: 37c31b4c99ea
    paths./api/v1/tools/video/images-to-video.post.summary:
      sourceHash: feb4b6d19902
      provenance: machine
      outputHash: b31b7ed86ab0
    paths./api/v1/tools/video/images-to-video.post.description:
      sourceHash: c7ec30bbfec8
      provenance: machine
      outputHash: 1c57d4760a13
    paths./api/v1/tools/video/video-metadata.post.summary:
      sourceHash: f2902a6112ff
      provenance: machine
      outputHash: ed1a81daad4e
    paths./api/v1/tools/video/video-metadata.post.description:
      sourceHash: 25b0eb0d1f64
      provenance: machine
      outputHash: f768ac6070e9
    paths./api/v1/tools/video/auto-subtitles.post.summary:
      sourceHash: 61b5c42d8cd0
      provenance: machine
      outputHash: 1e1cb681b413
    paths./api/v1/tools/video/auto-subtitles.post.description:
      sourceHash: fe628b19b3dc
      provenance: machine
      outputHash: 4c9bb48765cb
    paths./api/v1/tools/video/extract-audio.post.summary:
      sourceHash: ad7f2c51f215
      provenance: machine
      outputHash: 9bd854582b3c
    paths./api/v1/tools/video/extract-audio.post.description:
      sourceHash: 97425b1da99f
      provenance: machine
      outputHash: e1690a767984
    paths./api/v1/tools/audio/convert-audio.post.summary:
      sourceHash: 295e4a5939e1
      provenance: machine
      outputHash: f600377181d5
    paths./api/v1/tools/audio/convert-audio.post.description:
      sourceHash: db7c32326757
      provenance: machine
      outputHash: caa3e9fc265f
    paths./api/v1/tools/audio/trim-audio.post.summary:
      sourceHash: 6fdc83d79136
      provenance: machine
      outputHash: 89a5f322e902
    paths./api/v1/tools/audio/trim-audio.post.description:
      sourceHash: 1150da04e398
      provenance: machine
      outputHash: df966bbfb137
    paths./api/v1/tools/audio/volume-adjust.post.summary:
      sourceHash: 04257764f127
      provenance: machine
      outputHash: 46dc895e96c0
    paths./api/v1/tools/audio/volume-adjust.post.description:
      sourceHash: 18308a469600
      provenance: machine
      outputHash: 968fde198717
    paths./api/v1/tools/audio/normalize-audio.post.summary:
      sourceHash: f5298327c322
      provenance: machine
      outputHash: 212fc75a6321
    paths./api/v1/tools/audio/normalize-audio.post.description:
      sourceHash: d9993db45ccb
      provenance: machine
      outputHash: 7459cd5b30bc
    paths./api/v1/tools/audio/fade-audio.post.summary:
      sourceHash: fc7e035a63d5
      provenance: machine
      outputHash: e9fe4f3c3ff0
    paths./api/v1/tools/audio/fade-audio.post.description:
      sourceHash: a85ba1a120e5
      provenance: machine
      outputHash: d528616b4dd9
    paths./api/v1/tools/audio/reverse-audio.post.summary:
      sourceHash: b2eddae3a717
      provenance: machine
      outputHash: e4ab9999369f
    paths./api/v1/tools/audio/reverse-audio.post.description:
      sourceHash: 218f9b33f5eb
      provenance: machine
      outputHash: ba54c3d9acf3
    paths./api/v1/tools/audio/audio-speed.post.summary:
      sourceHash: aee2a47692bd
      provenance: machine
      outputHash: 7983da6d7a7d
    paths./api/v1/tools/audio/audio-speed.post.description:
      sourceHash: 51fe7745adbd
      provenance: machine
      outputHash: fddcef875928
    paths./api/v1/tools/audio/pitch-shift.post.summary:
      sourceHash: b26d4482b076
      provenance: machine
      outputHash: 1956d5c936fa
    paths./api/v1/tools/audio/pitch-shift.post.description:
      sourceHash: 04cf48eca03e
      provenance: machine
      outputHash: 5a097c61b88a
    paths./api/v1/tools/audio/audio-channels.post.summary:
      sourceHash: 409c74651939
      provenance: machine
      outputHash: 1a51e102342b
    paths./api/v1/tools/audio/audio-channels.post.description:
      sourceHash: 0974271a6730
      provenance: machine
      outputHash: 396a06a11bc1
    paths./api/v1/tools/audio/silence-removal.post.summary:
      sourceHash: dc27812cd572
      provenance: machine
      outputHash: 44b148638cb3
    paths./api/v1/tools/audio/silence-removal.post.description:
      sourceHash: 29d4e890a62d
      provenance: machine
      outputHash: d985d36b9263
    paths./api/v1/tools/audio/noise-reduction.post.summary:
      sourceHash: 0bcf30a006cb
      provenance: machine
      outputHash: cece2316b6a5
    paths./api/v1/tools/audio/noise-reduction.post.description:
      sourceHash: 032c91b97641
      provenance: machine
      outputHash: 0eb9c283cd25
    paths./api/v1/tools/audio/merge-audio.post.summary:
      sourceHash: 780965910dff
      provenance: machine
      outputHash: 9a451d0c701b
    paths./api/v1/tools/audio/merge-audio.post.description:
      sourceHash: 1397edf49056
      provenance: machine
      outputHash: f21ec1d82105
    paths./api/v1/tools/audio/split-audio.post.summary:
      sourceHash: e59c513c0ba2
      provenance: machine
      outputHash: 9b5d9ce9ed23
    paths./api/v1/tools/audio/split-audio.post.description:
      sourceHash: 763e0458c032
      provenance: machine
      outputHash: f55036762540
    paths./api/v1/tools/audio/ringtone-maker.post.summary:
      sourceHash: f0e142322ff9
      provenance: machine
      outputHash: 0225bd762624
    paths./api/v1/tools/audio/ringtone-maker.post.description:
      sourceHash: c301dcbbedc1
      provenance: machine
      outputHash: b8d779200151
    paths./api/v1/tools/audio/waveform-image.post.summary:
      sourceHash: 0e373ca45197
      provenance: machine
      outputHash: 29e5e9b73cfd
    paths./api/v1/tools/audio/waveform-image.post.description:
      sourceHash: 70f63b279b5d
      provenance: machine
      outputHash: 7c5d5dc29739
    paths./api/v1/tools/audio/audio-metadata.post.summary:
      sourceHash: 55d1a5a0e65d
      provenance: machine
      outputHash: d2c44b61bffd
    paths./api/v1/tools/audio/audio-metadata.post.description:
      sourceHash: 5c799135421c
      provenance: machine
      outputHash: a4c07996c6f7
    paths./api/v1/tools/audio/transcribe-audio.post.summary:
      sourceHash: a723c84c3ab0
      provenance: machine
      outputHash: b9013b731de3
    paths./api/v1/tools/audio/transcribe-audio.post.description:
      sourceHash: 2967ad141d14
      provenance: machine
      outputHash: 78e1c5aaafc4
    paths./api/v1/tools/pdf/merge-pdf.post.summary:
      sourceHash: 43d1539b7596
      provenance: machine
      outputHash: f0e11d032607
    paths./api/v1/tools/pdf/merge-pdf.post.description:
      sourceHash: 728388cddb8a
      provenance: machine
      outputHash: 62ad657eb2bc
    paths./api/v1/tools/pdf/split-pdf.post.summary:
      sourceHash: 4f1b846cb144
      provenance: machine
      outputHash: d1cf6832ccc8
    paths./api/v1/tools/pdf/split-pdf.post.description:
      sourceHash: fbe9d9c35006
      provenance: machine
      outputHash: 487334008fdd
    paths./api/v1/tools/pdf/compress-pdf.post.summary:
      sourceHash: 932c3c48a4de
      provenance: machine
      outputHash: eedac386ada3
    paths./api/v1/tools/pdf/compress-pdf.post.description:
      sourceHash: 89f23fb6b3c8
      provenance: machine
      outputHash: 1105f204bc02
    paths./api/v1/tools/pdf/rotate-pdf.post.summary:
      sourceHash: 1eccabbce838
      provenance: machine
      outputHash: 8468e376622f
    paths./api/v1/tools/pdf/rotate-pdf.post.description:
      sourceHash: 7c561cc7ed48
      provenance: machine
      outputHash: f060197f35dd
    paths./api/v1/tools/pdf/extract-pages.post.summary:
      sourceHash: 0247b9f3e6cf
      provenance: machine
      outputHash: 0c6c9c948cc3
    paths./api/v1/tools/pdf/extract-pages.post.description:
      sourceHash: c07eea0c017f
      provenance: machine
      outputHash: 999071461b66
    paths./api/v1/tools/pdf/remove-pages.post.summary:
      sourceHash: 670a5909f883
      provenance: machine
      outputHash: 4b10ae44058f
    paths./api/v1/tools/pdf/remove-pages.post.description:
      sourceHash: 6231b16e7bea
      provenance: machine
      outputHash: b787f58e0e50
    paths./api/v1/tools/pdf/organize-pdf.post.summary:
      sourceHash: d5a641ebd93a
      provenance: machine
      outputHash: 55fa7add3939
    paths./api/v1/tools/pdf/organize-pdf.post.description:
      sourceHash: ac32f48665e7
      provenance: machine
      outputHash: cae4579e15ba
    paths./api/v1/tools/pdf/protect-pdf.post.summary:
      sourceHash: dae6c607e963
      provenance: machine
      outputHash: bd06e23edde5
    paths./api/v1/tools/pdf/protect-pdf.post.description:
      sourceHash: 600a3be47970
      provenance: machine
      outputHash: 117cc61d5319
    paths./api/v1/tools/pdf/unlock-pdf.post.summary:
      sourceHash: 893947d82ded
      provenance: machine
      outputHash: b048712e69aa
    paths./api/v1/tools/pdf/unlock-pdf.post.description:
      sourceHash: 1d199055f386
      provenance: machine
      outputHash: 0386e2602571
    paths./api/v1/tools/pdf/repair-pdf.post.summary:
      sourceHash: 43faa052d4cf
      provenance: machine
      outputHash: e34a078dfccc
    paths./api/v1/tools/pdf/repair-pdf.post.description:
      sourceHash: 77f71d364df4
      provenance: machine
      outputHash: 3ece98667714
    paths./api/v1/tools/pdf/linearize-pdf.post.summary:
      sourceHash: 75fa522e3fe3
      provenance: machine
      outputHash: c5d1980255f7
    paths./api/v1/tools/pdf/linearize-pdf.post.description:
      sourceHash: 200f45aa4a91
      provenance: machine
      outputHash: 9ae27a0db6ac
    paths./api/v1/tools/pdf/grayscale-pdf.post.summary:
      sourceHash: 58b0007c3425
      provenance: machine
      outputHash: e8d0fde24a3e
    paths./api/v1/tools/pdf/grayscale-pdf.post.description:
      sourceHash: 7b7e2b4857e6
      provenance: machine
      outputHash: ec64f79cb1c1
    paths./api/v1/tools/pdf/pdfa-convert.post.summary:
      sourceHash: 9971b16fe624
      provenance: machine
      outputHash: 35ddf1359713
    paths./api/v1/tools/pdf/pdfa-convert.post.description:
      sourceHash: 3c8ad47aa904
      provenance: machine
      outputHash: 41f5b58f8d5e
    paths./api/v1/tools/pdf/crop-pdf.post.summary:
      sourceHash: dc4bdbce825f
      provenance: machine
      outputHash: d00194f776de
    paths./api/v1/tools/pdf/crop-pdf.post.description:
      sourceHash: 94eb9575aecc
      provenance: machine
      outputHash: d04e8c15f7c4
    paths./api/v1/tools/pdf/nup-pdf.post.summary:
      sourceHash: dac1b895452a
      provenance: machine
      outputHash: ba2f198194cb
    paths./api/v1/tools/pdf/nup-pdf.post.description:
      sourceHash: 5e2a264dafb0
      provenance: machine
      outputHash: 94f351d6c70e
    paths./api/v1/tools/pdf/booklet-pdf.post.summary:
      sourceHash: a1fdff0bc065
      provenance: machine
      outputHash: c1d29614db90
    paths./api/v1/tools/pdf/booklet-pdf.post.description:
      sourceHash: 0ce5f88843c3
      provenance: machine
      outputHash: 4ad5f5e2d395
    paths./api/v1/tools/pdf/watermark-pdf.post.summary:
      sourceHash: 903ce98c712a
      provenance: machine
      outputHash: 3e1e7eb3aabd
    paths./api/v1/tools/pdf/watermark-pdf.post.description:
      sourceHash: c15a57a06624
      provenance: machine
      outputHash: 452ca1d4253f
    paths./api/v1/tools/pdf/pdf-page-numbers.post.summary:
      sourceHash: 44eb045097be
      provenance: machine
      outputHash: 184aee87e54a
    paths./api/v1/tools/pdf/pdf-page-numbers.post.description:
      sourceHash: 30ce839cac96
      provenance: machine
      outputHash: 4cda52851239
    paths./api/v1/tools/files/convert-document.post.summary:
      sourceHash: 51556213858a
      provenance: machine
      outputHash: f55f2ef5c857
    paths./api/v1/tools/files/convert-document.post.description:
      sourceHash: 8a61bf0f3459
      provenance: machine
      outputHash: dea8858bd050
    paths./api/v1/tools/files/convert-presentation.post.summary:
      sourceHash: 5e2293e30835
      provenance: machine
      outputHash: 62313cc598d4
    paths./api/v1/tools/files/convert-presentation.post.description:
      sourceHash: c24f1d70d630
      provenance: machine
      outputHash: 78fdcabe033c
    paths./api/v1/tools/files/convert-spreadsheet.post.summary:
      sourceHash: 670b7aa81c49
      provenance: machine
      outputHash: 371f187d3953
    paths./api/v1/tools/files/convert-spreadsheet.post.description:
      sourceHash: 1c254c7db8bc
      provenance: machine
      outputHash: 5da37d8301fa
    paths./api/v1/tools/files/excel-to-pdf.post.summary:
      sourceHash: edba3432df77
      provenance: machine
      outputHash: 13ff1251056c
    paths./api/v1/tools/files/excel-to-pdf.post.description:
      sourceHash: c2e4aaa88cb7
      provenance: machine
      outputHash: 8249b29dc04d
    paths./api/v1/tools/files/word-to-pdf.post.summary:
      sourceHash: e6cb1150e762
      provenance: machine
      outputHash: a706fb4afab8
    paths./api/v1/tools/files/word-to-pdf.post.description:
      sourceHash: eca3c027bc10
      provenance: machine
      outputHash: fd5accf39803
    paths./api/v1/tools/files/powerpoint-to-pdf.post.summary:
      sourceHash: 799dd29fe97a
      provenance: machine
      outputHash: a8462ad732dc
    paths./api/v1/tools/files/powerpoint-to-pdf.post.description:
      sourceHash: cdc18f172040
      provenance: machine
      outputHash: 908682baad70
    paths./api/v1/tools/files/html-to-pdf.post.summary:
      sourceHash: e03b0e6dda95
      provenance: machine
      outputHash: b6f92deea198
    paths./api/v1/tools/files/html-to-pdf.post.description:
      sourceHash: e189b21c7bed
      provenance: machine
      outputHash: 3ea26ddbb957
    paths./api/v1/tools/files/markdown-to-docx.post.summary:
      sourceHash: 1162393274fa
      provenance: machine
      outputHash: ad32f054b55e
    paths./api/v1/tools/files/markdown-to-docx.post.description:
      sourceHash: 2bc0fd718613
      provenance: machine
      outputHash: ec8eb1d41be2
    paths./api/v1/tools/files/markdown-to-html.post.summary:
      sourceHash: e5e6e736b649
      provenance: machine
      outputHash: c021fd496f06
    paths./api/v1/tools/files/markdown-to-html.post.description:
      sourceHash: 4f6f706c371e
      provenance: machine
      outputHash: 339cca35e2f1
    paths./api/v1/tools/files/markdown-to-pdf.post.summary:
      sourceHash: 82712b1fc2b5
      provenance: machine
      outputHash: 54c0d837e6c6
    paths./api/v1/tools/files/markdown-to-pdf.post.description:
      sourceHash: f588e3d2847d
      provenance: machine
      outputHash: de75fb59d90b
    paths./api/v1/tools/files/epub-convert.post.summary:
      sourceHash: dcee1b93b5d5
      provenance: machine
      outputHash: fe443513a0d3
    paths./api/v1/tools/files/epub-convert.post.description:
      sourceHash: 5a42072194d3
      provenance: machine
      outputHash: be8b9b2d8d46
    paths./api/v1/tools/files/to-epub.post.summary:
      sourceHash: 542d3fb468be
      provenance: machine
      outputHash: 843c588ea1bb
    paths./api/v1/tools/files/to-epub.post.description:
      sourceHash: 1e0b8c39ef9d
      provenance: machine
      outputHash: 7a9c9a941ae2
    paths./api/v1/tools/pdf/flatten-pdf.post.summary:
      sourceHash: 86c838d2d497
      provenance: machine
      outputHash: 7e22e1e9a5e9
    paths./api/v1/tools/pdf/flatten-pdf.post.description:
      sourceHash: cb0b0bdeaaa5
      provenance: machine
      outputHash: 5edc31d97674
    paths./api/v1/tools/pdf/redact-pdf.post.summary:
      sourceHash: 1437d196213c
      provenance: machine
      outputHash: 3c46186f5627
    paths./api/v1/tools/pdf/redact-pdf.post.description:
      sourceHash: c2652c3da04a
      provenance: machine
      outputHash: f009524c851c
    paths./api/v1/tools/pdf/pdf-to-text.post.summary:
      sourceHash: f1a719f99a31
      provenance: machine
      outputHash: 2c96ed0a68bd
    paths./api/v1/tools/pdf/pdf-to-text.post.description:
      sourceHash: f107a4082454
      provenance: machine
      outputHash: 60c85fcfe7e9
    paths./api/v1/tools/pdf/pdf-to-word.post.summary:
      sourceHash: 283553b063ad
      provenance: machine
      outputHash: 5e0f43677aa2
    paths./api/v1/tools/pdf/pdf-to-word.post.description:
      sourceHash: 324b6f3d35f7
      provenance: machine
      outputHash: 0da922e5e9c5
    paths./api/v1/tools/pdf/pdf-metadata.post.summary:
      sourceHash: 7aa0fbdbeac6
      provenance: machine
      outputHash: 4c1853f015ee
    paths./api/v1/tools/pdf/pdf-metadata.post.description:
      sourceHash: 4267623afc19
      provenance: machine
      outputHash: 860d26c318dc
    paths./api/v1/tools/pdf/ocr-pdf.post.summary:
      sourceHash: 2d9dfe74b2d1
      provenance: machine
      outputHash: 0303d41d5976
    paths./api/v1/tools/pdf/ocr-pdf.post.description:
      sourceHash: fbed951c39df
      provenance: machine
      outputHash: d08ca179dcc4
    paths./api/v1/tools/files/chart-maker.post.summary:
      sourceHash: dbcca76ccbf5
      provenance: machine
      outputHash: 96287e43b732
    paths./api/v1/tools/files/chart-maker.post.description:
      sourceHash: e4adb80dab58
      provenance: machine
      outputHash: dc0255d3ca87
    paths./api/v1/tools/files/csv-excel.post.summary:
      sourceHash: c53386a6d40d
      provenance: machine
      outputHash: 4e78eab3193b
    paths./api/v1/tools/files/csv-excel.post.description:
      sourceHash: b567a5eb0ee5
      provenance: machine
      outputHash: a916b995a97b
    paths./api/v1/tools/files/csv-json.post.summary:
      sourceHash: 3ed0542f5ca4
      provenance: machine
      outputHash: 12c1c90a8af1
    paths./api/v1/tools/files/csv-json.post.description:
      sourceHash: 6ed58f7ca5ac
      provenance: machine
      outputHash: 70aef334114f
    paths./api/v1/tools/files/json-xml.post.summary:
      sourceHash: 0e374572749d
      provenance: machine
      outputHash: 24b47e730082
    paths./api/v1/tools/files/json-xml.post.description:
      sourceHash: bbb56d23a274
      provenance: machine
      outputHash: 99a535fd6b3b
    paths./api/v1/tools/files/split-csv.post.summary:
      sourceHash: 06c1cfb6cb2c
      provenance: machine
      outputHash: 4da3e47f9567
    paths./api/v1/tools/files/split-csv.post.description:
      sourceHash: 1181658ae239
      provenance: machine
      outputHash: 304216f1ac01
    paths./api/v1/tools/files/merge-csvs.post.summary:
      sourceHash: 0042c1153e55
      provenance: machine
      outputHash: dec5a55ed68c
    paths./api/v1/tools/files/merge-csvs.post.description:
      sourceHash: 4956c801900a
      provenance: machine
      outputHash: f02be7f98656
    paths./api/v1/tools/files/yaml-json.post.summary:
      sourceHash: cd3e06c3d51d
      provenance: machine
      outputHash: 677b487602d8
    paths./api/v1/tools/files/yaml-json.post.description:
      sourceHash: a7fa089ac00a
      provenance: machine
      outputHash: e61afdef2a99
    paths./api/v1/tools/files/xml-to-csv.post.summary:
      sourceHash: 909195e97a63
      provenance: machine
      outputHash: 5f8468946013
    paths./api/v1/tools/files/xml-to-csv.post.description:
      sourceHash: 156ea77c8a62
      provenance: machine
      outputHash: 21d5dac90c54
    paths./api/v1/tools/files/create-zip.post.summary:
      sourceHash: 8463eb4799ce
      provenance: machine
      outputHash: 03aa2e08605d
    paths./api/v1/tools/files/create-zip.post.description:
      sourceHash: 26c97508b498
      provenance: machine
      outputHash: 01915308b49a
    paths./api/v1/tools/files/extract-zip.post.summary:
      sourceHash: aa7dad519142
      provenance: machine
      outputHash: c4b11a85b002
    paths./api/v1/tools/files/extract-zip.post.description:
      sourceHash: 5b025b124d8b
      provenance: machine
      outputHash: 3c597d383605
    paths./api/v1/tools/image/background-replace.post.summary:
      sourceHash: 971cacdad5ce
      provenance: machine
      outputHash: b6ec62124d47
    paths./api/v1/tools/image/background-replace.post.description:
      sourceHash: 5f7a0c0dbbc7
      provenance: machine
      outputHash: 808af1798e85
    paths./api/v1/tools/image/blur-background.post.summary:
      sourceHash: 2383b0d9c928
      provenance: machine
      outputHash: 7a25da1d468e
    paths./api/v1/tools/image/blur-background.post.description:
      sourceHash: f3afcae74767
      provenance: machine
      outputHash: d1fe9cfb253b
    paths./api/v1/tools/image/barcode-generate.post.summary:
      sourceHash: 4717ee594681
      provenance: machine
      outputHash: bbc9487cbb91
    paths./api/v1/tools/image/barcode-generate.post.description:
      sourceHash: e174f8923d5d
      provenance: machine
      outputHash: 51ee9b0c0ff3
    paths./api/v1/tools/image/circle-crop.post.summary:
      sourceHash: d76473270976
      provenance: machine
      outputHash: 91363d141fb4
    paths./api/v1/tools/image/circle-crop.post.description:
      sourceHash: 3e4d4819d594
      provenance: machine
      outputHash: 78e23b1e9cb4
    paths./api/v1/tools/image/duotone.post.summary:
      sourceHash: 27e78e7e8cca
      provenance: machine
      outputHash: 27e78e7e8cca
    paths./api/v1/tools/image/duotone.post.description:
      sourceHash: f3d5b592526a
      provenance: machine
      outputHash: 4be5a7065e01
    paths./api/v1/tools/image/image-pad.post.summary:
      sourceHash: 7cbc7ca5de5b
      provenance: machine
      outputHash: bac5c5106690
    paths./api/v1/tools/image/image-pad.post.description:
      sourceHash: f222b64a52bc
      provenance: machine
      outputHash: 280794d6c6fc
    paths./api/v1/tools/image/pixelate.post.summary:
      sourceHash: f4982f86e732
      provenance: machine
      outputHash: 178e7d0da195
    paths./api/v1/tools/image/pixelate.post.description:
      sourceHash: c47f345410f8
      provenance: machine
      outputHash: 78be7bf972ef
    paths./api/v1/tools/image/vignette.post.summary:
      sourceHash: f841c2080740
      provenance: machine
      outputHash: d4ceee12866b
    paths./api/v1/tools/image/vignette.post.description:
      sourceHash: 9e1e807e89e0
      provenance: machine
      outputHash: c02dcad5ecac
    paths./api/v1/tools/image/gif-webp.post.summary:
      sourceHash: 337e539d70a4
      provenance: machine
      outputHash: 188329f5512c
    paths./api/v1/tools/image/gif-webp.post.description:
      sourceHash: f397405d0b9c
      provenance: machine
      outputHash: 69334214c68a
    paths./api/v1/tools/image/histogram.post.summary:
      sourceHash: b32dc489fba8
      provenance: machine
      outputHash: d2af6c22e16f
    paths./api/v1/tools/image/histogram.post.description:
      sourceHash: f98c4a1d1bf0
      provenance: machine
      outputHash: 569422b2a3f3
    paths./api/v1/tools/image/lqip-placeholder.post.summary:
      sourceHash: 878e3f3518db
      provenance: machine
      outputHash: bdc05e069e40
    paths./api/v1/tools/image/lqip-placeholder.post.description:
      sourceHash: 2100f7e08142
      provenance: machine
      outputHash: b81227a73e50
    paths./api/v1/tools/image/sprite-sheet.post.summary:
      sourceHash: f1c0772c4599
      provenance: machine
      outputHash: f1c0772c4599
    paths./api/v1/tools/image/sprite-sheet.post.description:
      sourceHash: 4ef141ec92ed
      provenance: machine
      outputHash: bbe17915cd37
    paths./api/v1/tools/image/passport-photo.post.summary:
      sourceHash: 535e5bf9e5e6
      provenance: machine
      outputHash: 80f4fe26e3b9
    paths./api/v1/tools/image/passport-photo.post.description:
      sourceHash: 7d3a81dbb05c
      provenance: machine
      outputHash: 5f001253bc9b
    paths./api/v1/tools/pdf/sign-pdf.post.summary:
      sourceHash: 1bf93e215df9
      provenance: machine
      outputHash: 9e207f9d79de
    paths./api/v1/tools/pdf/sign-pdf.post.description:
      sourceHash: d66e168d79ba
      provenance: machine
      outputHash: 135a2a75ac99
    paths./api/v1/tools/image/jpg-to-png.post.summary:
      sourceHash: 2b455c24e59e
      provenance: machine
      outputHash: 71c02a412164
    paths./api/v1/tools/image/jpg-to-png.post.description:
      sourceHash: 1d504faac90f
      provenance: machine
      outputHash: 72da2aa2f103
    paths./api/v1/tools/image/png-to-jpg.post.summary:
      sourceHash: 9e2eef671853
      provenance: machine
      outputHash: 8002f42c1493
    paths./api/v1/tools/image/png-to-jpg.post.description:
      sourceHash: 12d6c8d6aa01
      provenance: machine
      outputHash: cd24a57bd82b
    paths./api/v1/tools/image/jpg-to-webp.post.summary:
      sourceHash: 1e67451d3358
      provenance: machine
      outputHash: 787cda40142a
    paths./api/v1/tools/image/jpg-to-webp.post.description:
      sourceHash: 0e7e4b18daa5
      provenance: machine
      outputHash: 2a088c85b682
    paths./api/v1/tools/image/png-to-webp.post.summary:
      sourceHash: 6c9410143dd5
      provenance: machine
      outputHash: 80b3be090533
    paths./api/v1/tools/image/png-to-webp.post.description:
      sourceHash: 7505ddaf944b
      provenance: machine
      outputHash: d434753150b0
    paths./api/v1/tools/image/webp-to-jpg.post.summary:
      sourceHash: 670a9097dec8
      provenance: machine
      outputHash: 204594d6f993
    paths./api/v1/tools/image/webp-to-jpg.post.description:
      sourceHash: 2b7701fe02eb
      provenance: machine
      outputHash: 59b54e0c8e22
    paths./api/v1/tools/image/webp-to-png.post.summary:
      sourceHash: 781a17cb5ec0
      provenance: machine
      outputHash: 7d2f63e7d3b7
    paths./api/v1/tools/image/webp-to-png.post.description:
      sourceHash: 78645c123b20
      provenance: machine
      outputHash: cd0702025830
    paths./api/v1/tools/image/jpg-to-avif.post.summary:
      sourceHash: 1c50ea7bdfce
      provenance: machine
      outputHash: 9c4c588cc788
    paths./api/v1/tools/image/jpg-to-avif.post.description:
      sourceHash: fbc8f6ae407c
      provenance: machine
      outputHash: 2779e51d55d7
    paths./api/v1/tools/image/png-to-avif.post.summary:
      sourceHash: c3ba0668ee1f
      provenance: machine
      outputHash: eac42700580a
    paths./api/v1/tools/image/png-to-avif.post.description:
      sourceHash: d0dcdde42555
      provenance: machine
      outputHash: f1aa50cdb23f
    paths./api/v1/tools/image/webp-to-avif.post.summary:
      sourceHash: 690ea37f961e
      provenance: machine
      outputHash: 96deb699262f
    paths./api/v1/tools/image/webp-to-avif.post.description:
      sourceHash: bcae3fc230b8
      provenance: machine
      outputHash: 0d7a3ab50afa
    paths./api/v1/tools/image/heic-to-jpg.post.summary:
      sourceHash: 0013ad2f7e97
      provenance: machine
      outputHash: fe53fc73ae3b
    paths./api/v1/tools/image/heic-to-jpg.post.description:
      sourceHash: deb0641a6bfd
      provenance: machine
      outputHash: 6652fdf22b51
    paths./api/v1/tools/image/heic-to-png.post.summary:
      sourceHash: a191f1708025
      provenance: machine
      outputHash: 67a70c4a50e1
    paths./api/v1/tools/image/heic-to-png.post.description:
      sourceHash: 583345b636d6
      provenance: machine
      outputHash: 5e08a1a88096
    paths./api/v1/tools/image/heic-to-avif.post.summary:
      sourceHash: a03031e5c80d
      provenance: machine
      outputHash: 8a5f92fbb097
    paths./api/v1/tools/image/heic-to-avif.post.description:
      sourceHash: 6812ebf99c0b
      provenance: machine
      outputHash: c6bc31de7494
    paths./api/v1/tools/image/jpg-to-gif.post.summary:
      sourceHash: 46b3bcd3a38f
      provenance: machine
      outputHash: 76edfe65e840
    paths./api/v1/tools/image/jpg-to-gif.post.description:
      sourceHash: 365097fb68a9
      provenance: machine
      outputHash: 0758d9f5443e
    paths./api/v1/tools/image/png-to-gif.post.summary:
      sourceHash: e4de0e11bf3d
      provenance: machine
      outputHash: 722c4aab4f9b
    paths./api/v1/tools/image/png-to-gif.post.description:
      sourceHash: 89620988c5bd
      provenance: machine
      outputHash: 8e61eb4440b9
    paths./api/v1/tools/image/gif-to-jpg.post.summary:
      sourceHash: 016ce94121b8
      provenance: machine
      outputHash: 5eb787f898e9
    paths./api/v1/tools/image/gif-to-jpg.post.description:
      sourceHash: d3c99d325441
      provenance: machine
      outputHash: e536d9aee955
    paths./api/v1/tools/image/gif-to-png.post.summary:
      sourceHash: 8f3239971851
      provenance: machine
      outputHash: fc42b46817a8
    paths./api/v1/tools/image/gif-to-png.post.description:
      sourceHash: 6b363c6c5b6a
      provenance: machine
      outputHash: c6d8f5624790
    paths./api/v1/tools/image/webp-to-gif.post.summary:
      sourceHash: db331b95e19b
      provenance: machine
      outputHash: cf2ae281124e
    paths./api/v1/tools/image/webp-to-gif.post.description:
      sourceHash: 9295d8ac8c09
      provenance: machine
      outputHash: 928fa0a0e317
    paths./api/v1/tools/image/jpg-to-tiff.post.summary:
      sourceHash: 86ae950de6a3
      provenance: machine
      outputHash: 4c77cfaa9cd4
    paths./api/v1/tools/image/jpg-to-tiff.post.description:
      sourceHash: 4f0ac37f2e06
      provenance: machine
      outputHash: 24a8935b096f
    paths./api/v1/tools/image/png-to-tiff.post.summary:
      sourceHash: 3fa124cbaa4a
      provenance: machine
      outputHash: f6599e50e390
    paths./api/v1/tools/image/png-to-tiff.post.description:
      sourceHash: 5cf97e597ee8
      provenance: machine
      outputHash: 57a082b1b772
    paths./api/v1/tools/image/tiff-to-jpg.post.summary:
      sourceHash: c803f62926b8
      provenance: machine
      outputHash: 7eee455d79d3
    paths./api/v1/tools/image/tiff-to-jpg.post.description:
      sourceHash: cf928b0513e5
      provenance: machine
      outputHash: 09e5dcc8190d
    paths./api/v1/tools/image/tiff-to-png.post.summary:
      sourceHash: 73c313976bb4
      provenance: machine
      outputHash: '889850720825'
    paths./api/v1/tools/image/tiff-to-png.post.description:
      sourceHash: 52f065d2f324
      provenance: machine
      outputHash: 6780048bdb55
    paths./api/v1/tools/image/psd-to-jpg.post.summary:
      sourceHash: b9d50a4fd5a9
      provenance: machine
      outputHash: e79c5e535285
    paths./api/v1/tools/image/psd-to-jpg.post.description:
      sourceHash: a9897ebb6e8c
      provenance: machine
      outputHash: b43f4a973012
    paths./api/v1/tools/image/psd-to-png.post.summary:
      sourceHash: af36a6f53221
      provenance: machine
      outputHash: 07de6d4de95a
    paths./api/v1/tools/image/psd-to-png.post.description:
      sourceHash: b15171b20160
      provenance: machine
      outputHash: 9f1c9f5620ed
    paths./api/v1/tools/image/png-to-eps.post.summary:
      sourceHash: 33ce1b37d63f
      provenance: machine
      outputHash: d6abe338e4cd
    paths./api/v1/tools/image/png-to-eps.post.description:
      sourceHash: 31d8bfe76c0d
      provenance: machine
      outputHash: 529d640f7d53
    paths./api/v1/tools/image/jpg-to-eps.post.summary:
      sourceHash: e8bde3997aaf
      provenance: machine
      outputHash: 0bf5df13e659
    paths./api/v1/tools/image/jpg-to-eps.post.description:
      sourceHash: 62cce0cb73ba
      provenance: machine
      outputHash: 4383f53d9661
    paths./api/v1/tools/image/eps-to-png.post.summary:
      sourceHash: be7ed58f6b7e
      provenance: machine
      outputHash: e694eef39664
    paths./api/v1/tools/image/eps-to-png.post.description:
      sourceHash: e9157e4e334b
      provenance: machine
      outputHash: b2e7fd924693
    paths./api/v1/tools/image/eps-to-jpg.post.summary:
      sourceHash: c05512db4202
      provenance: machine
      outputHash: 06ac67b36251
    paths./api/v1/tools/image/eps-to-jpg.post.description:
      sourceHash: b582104358cd
      provenance: machine
      outputHash: 4560ed2ece8d
    paths./api/v1/tools/image/png-to-svg.post.summary:
      sourceHash: 651353e8e22c
      provenance: machine
      outputHash: f83ee8b229af
    paths./api/v1/tools/image/png-to-svg.post.description:
      sourceHash: 5cbb3b81f383
      provenance: machine
      outputHash: 8739291803c5
    paths./api/v1/tools/image/jpg-to-svg.post.summary:
      sourceHash: 46f10c1c31b4
      provenance: machine
      outputHash: d7871a6869ca
    paths./api/v1/tools/image/jpg-to-svg.post.description:
      sourceHash: ea475d6542cb
      provenance: machine
      outputHash: a7edbcf43883
    paths./api/v1/tools/image/tiff-to-svg.post.summary:
      sourceHash: 52f9db13bb53
      provenance: machine
      outputHash: bae319fa2951
    paths./api/v1/tools/image/tiff-to-svg.post.description:
      sourceHash: 26ac4eed4312
      provenance: machine
      outputHash: dd6b0036dd19
    paths./api/v1/tools/image/psd-to-svg.post.summary:
      sourceHash: 654d378a3882
      provenance: machine
      outputHash: 4aeda604ec90
    paths./api/v1/tools/image/psd-to-svg.post.description:
      sourceHash: 990b5ce49f61
      provenance: machine
      outputHash: 39fd354e146a
    paths./api/v1/tools/image/eps-to-svg.post.summary:
      sourceHash: 36137967b026
      provenance: machine
      outputHash: e8855edcfd4e
    paths./api/v1/tools/image/eps-to-svg.post.description:
      sourceHash: 8c2914626a16
      provenance: machine
      outputHash: 6da0b3e5e1ac
    paths./api/v1/tools/image/svg-to-png.post.summary:
      sourceHash: 1f1eb20e97fe
      provenance: machine
      outputHash: 7134b741109e
    paths./api/v1/tools/image/svg-to-png.post.description:
      sourceHash: 5338d3a81ff7
      provenance: machine
      outputHash: 0dc59e912518
    paths./api/v1/tools/image/svg-to-jpg.post.summary:
      sourceHash: 8e51faf94ff1
      provenance: machine
      outputHash: 4e88c04c6171
    paths./api/v1/tools/image/svg-to-jpg.post.description:
      sourceHash: 23241e664642
      provenance: machine
      outputHash: 144b547e5481
    paths./api/v1/tools/image/jpg-to-pdf.post.summary:
      sourceHash: 480104dd2622
      provenance: machine
      outputHash: 34670e459d26
    paths./api/v1/tools/image/jpg-to-pdf.post.description:
      sourceHash: daffc3441cf6
      provenance: machine
      outputHash: 515a4321556e
    paths./api/v1/tools/image/png-to-pdf.post.summary:
      sourceHash: 7cab3612203f
      provenance: machine
      outputHash: 99f5eb56b6b8
    paths./api/v1/tools/image/png-to-pdf.post.description:
      sourceHash: 135646c47b37
      provenance: machine
      outputHash: b132eb40a103
    paths./api/v1/tools/image/heic-to-pdf.post.summary:
      sourceHash: d9a32701511d
      provenance: machine
      outputHash: bf86621f699c
    paths./api/v1/tools/image/heic-to-pdf.post.description:
      sourceHash: cb1a4bc6ad48
      provenance: machine
      outputHash: 4577ef0f5acb
    paths./api/v1/tools/image/tiff-to-pdf.post.summary:
      sourceHash: 76458bef5bb7
      provenance: machine
      outputHash: 6875a49bf8d3
    paths./api/v1/tools/image/tiff-to-pdf.post.description:
      sourceHash: f26e932da894
      provenance: machine
      outputHash: e7894fa22f7a
    paths./api/v1/tools/image/webp-to-pdf.post.summary:
      sourceHash: fdbcd78acb8e
      provenance: machine
      outputHash: e282db1d9789
    paths./api/v1/tools/image/webp-to-pdf.post.description:
      sourceHash: 02d3842d170f
      provenance: machine
      outputHash: afac57c14cfa
    paths./api/v1/tools/image/gif-to-pdf.post.summary:
      sourceHash: 62a5f26018fc
      provenance: machine
      outputHash: df4ca50a3b8e
    paths./api/v1/tools/image/gif-to-pdf.post.description:
      sourceHash: e1d4ce05a711
      provenance: machine
      outputHash: 4811b554fb6b
    paths./api/v1/tools/image/eps-to-pdf.post.summary:
      sourceHash: b183686e176a
      provenance: machine
      outputHash: f13d040b1ed9
    paths./api/v1/tools/image/eps-to-pdf.post.description:
      sourceHash: 797845fc1a13
      provenance: machine
      outputHash: 0a5875cbb551
    paths./api/v1/tools/pdf/pdf-to-jpg.post.summary:
      sourceHash: f84d053c3344
      provenance: machine
      outputHash: 447066736bed
    paths./api/v1/tools/pdf/pdf-to-jpg.post.description:
      sourceHash: 294e7071a0cd
      provenance: machine
      outputHash: 0b61c3926138
    paths./api/v1/tools/pdf/pdf-to-png.post.summary:
      sourceHash: 18362545fcb8
      provenance: machine
      outputHash: 1f012a7abe38
    paths./api/v1/tools/pdf/pdf-to-png.post.description:
      sourceHash: 879e2d2c287b
      provenance: machine
      outputHash: 84e580878bcb
    paths./api/v1/tools/pdf/pdf-to-tiff.post.summary:
      sourceHash: cd0065e23473
      provenance: machine
      outputHash: c173a51a2698
    paths./api/v1/tools/pdf/pdf-to-tiff.post.description:
      sourceHash: 4e46452ab02e
      provenance: machine
      outputHash: 07cf4f17d18d
    paths./api/v1/tools/video/mov-to-mp4.post.summary:
      sourceHash: b88833009ff9
      provenance: machine
      outputHash: 18de6287049a
    paths./api/v1/tools/video/mov-to-mp4.post.description:
      sourceHash: a248339c26d1
      provenance: machine
      outputHash: a2418561abb7
    paths./api/v1/tools/video/webm-to-mp4.post.summary:
      sourceHash: 66e6dae5210e
      provenance: machine
      outputHash: 6dafb202ad34
    paths./api/v1/tools/video/webm-to-mp4.post.description:
      sourceHash: a9977ca09125
      provenance: machine
      outputHash: acc76d6bb17d
    paths./api/v1/tools/video/mkv-to-mp4.post.summary:
      sourceHash: cffe53cd9489
      provenance: machine
      outputHash: 7118114032cf
    paths./api/v1/tools/video/mkv-to-mp4.post.description:
      sourceHash: d4c9bfc05b71
      provenance: machine
      outputHash: 06015bef1b08
    paths./api/v1/tools/video/avi-to-mp4.post.summary:
      sourceHash: a5ed5b5782d3
      provenance: machine
      outputHash: f6402dc3fcb5
    paths./api/v1/tools/video/avi-to-mp4.post.description:
      sourceHash: 59f0c2a1860b
      provenance: machine
      outputHash: 9b30bce1b17c
    paths./api/v1/tools/video/mp4-to-mov.post.summary:
      sourceHash: a20af2c4b1fb
      provenance: machine
      outputHash: 86e4d188bbff
    paths./api/v1/tools/video/mp4-to-mov.post.description:
      sourceHash: 8d99e8459b46
      provenance: machine
      outputHash: 5e0fa2349a71
    paths./api/v1/tools/video/mp4-to-webm.post.summary:
      sourceHash: a1b67df8d74e
      provenance: machine
      outputHash: 13e9701c8310
    paths./api/v1/tools/video/mp4-to-webm.post.description:
      sourceHash: 2d0fec602c82
      provenance: machine
      outputHash: 4ce02c821a24
    paths./api/v1/tools/video/webm-to-mov.post.summary:
      sourceHash: 60df7d3b385b
      provenance: machine
      outputHash: f9c93db6603b
    paths./api/v1/tools/video/webm-to-mov.post.description:
      sourceHash: 6982fc1cb933
      provenance: machine
      outputHash: 1fe096ca3ec8
    paths./api/v1/tools/video/mkv-to-mov.post.summary:
      sourceHash: dc04291270e6
      provenance: machine
      outputHash: e73eb950a272
    paths./api/v1/tools/video/mkv-to-mov.post.description:
      sourceHash: 71c9c1b99374
      provenance: machine
      outputHash: 351ed340c6c3
    paths./api/v1/tools/video/avi-to-mov.post.summary:
      sourceHash: 449c58ca9486
      provenance: machine
      outputHash: 334c37e1d34e
    paths./api/v1/tools/video/avi-to-mov.post.description:
      sourceHash: 62b1fd2e8e0c
      provenance: machine
      outputHash: 3eb0f2a48059
    paths./api/v1/tools/video/mp4-to-avi.post.summary:
      sourceHash: 700763fbdb4a
      provenance: machine
      outputHash: d8fdbd66a1f7
    paths./api/v1/tools/video/mp4-to-avi.post.description:
      sourceHash: da2c6f405f96
      provenance: machine
      outputHash: c75e82cf359b
    paths./api/v1/tools/video/mov-to-avi.post.summary:
      sourceHash: 6cc35a24d9fa
      provenance: machine
      outputHash: 21dcebfa4195
    paths./api/v1/tools/video/mov-to-avi.post.description:
      sourceHash: e1dd8989a812
      provenance: machine
      outputHash: 8da27bc2b735
    paths./api/v1/tools/video/mkv-to-avi.post.summary:
      sourceHash: 993ee5fab472
      provenance: machine
      outputHash: 53cf6541aa08
    paths./api/v1/tools/video/mkv-to-avi.post.description:
      sourceHash: f1b9b4ced057
      provenance: machine
      outputHash: 3d9fb7e5e019
    paths./api/v1/tools/video/avi-to-mkv.post.summary:
      sourceHash: 3393bba23656
      provenance: machine
      outputHash: 26d6c0ec3559
    paths./api/v1/tools/video/avi-to-mkv.post.description:
      sourceHash: ce821c05956b
      provenance: machine
      outputHash: 779ce4a25c37
    paths./api/v1/tools/video/mp4-to-gif.post.summary:
      sourceHash: 62a7b6e94a53
      provenance: machine
      outputHash: 5dba4d12faec
    paths./api/v1/tools/video/mp4-to-gif.post.description:
      sourceHash: eb95a94fc0e0
      provenance: machine
      outputHash: 4a8d9be0bf54
    paths./api/v1/tools/video/mov-to-gif.post.summary:
      sourceHash: c0e8c3dcfdfd
      provenance: machine
      outputHash: 1cc54039fa7a
    paths./api/v1/tools/video/mov-to-gif.post.description:
      sourceHash: f0f7c24adb4f
      provenance: machine
      outputHash: 248aac1ba598
    paths./api/v1/tools/video/mkv-to-gif.post.summary:
      sourceHash: c19834912c0e
      provenance: machine
      outputHash: aac0a6f2762b
    paths./api/v1/tools/video/mkv-to-gif.post.description:
      sourceHash: 591062eb518f
      provenance: machine
      outputHash: 11c24723f3ab
    paths./api/v1/tools/video/avi-to-gif.post.summary:
      sourceHash: b6286a84b91d
      provenance: machine
      outputHash: 7215ca57ea40
    paths./api/v1/tools/video/avi-to-gif.post.description:
      sourceHash: dd44bdc0686d
      provenance: machine
      outputHash: 7fc590ed778d
    paths./api/v1/tools/video/gif-to-mp4.post.summary:
      sourceHash: 9c2d0df2d1e9
      provenance: machine
      outputHash: 40f9eec3f5fe
    paths./api/v1/tools/video/gif-to-mp4.post.description:
      sourceHash: 96914944fdc5
      provenance: machine
      outputHash: 16c4472fe96d
    paths./api/v1/tools/video/gif-to-webm.post.summary:
      sourceHash: f2b694034e10
      provenance: machine
      outputHash: b647256ff686
    paths./api/v1/tools/video/gif-to-webm.post.description:
      sourceHash: 221c2fee1775
      provenance: machine
      outputHash: 48a7356ee8cf
    paths./api/v1/tools/video/gif-to-mov.post.summary:
      sourceHash: 45c5f35f45f3
      provenance: machine
      outputHash: 65db304cad54
    paths./api/v1/tools/video/gif-to-mov.post.description:
      sourceHash: 57996ae8e8cb
      provenance: machine
      outputHash: 7283b85a3390
    paths./api/v1/tools/video/mp4-to-mp3.post.summary:
      sourceHash: 69e107b048a8
      provenance: machine
      outputHash: 9d10585efb6f
    paths./api/v1/tools/video/mp4-to-mp3.post.description:
      sourceHash: 40de83aa6ed2
      provenance: machine
      outputHash: 61619e4ffa7c
    paths./api/v1/tools/video/mov-to-mp3.post.summary:
      sourceHash: e149b039deaf
      provenance: machine
      outputHash: c6988b147012
    paths./api/v1/tools/video/mov-to-mp3.post.description:
      sourceHash: 27f1a1f36a88
      provenance: machine
      outputHash: 1d87af8d965c
    paths./api/v1/tools/video/mkv-to-mp3.post.summary:
      sourceHash: a017e66660ad
      provenance: machine
      outputHash: 7816b8935f8e
    paths./api/v1/tools/video/mkv-to-mp3.post.description:
      sourceHash: 12cb145afcd0
      provenance: machine
      outputHash: cd798e92ea0d
    paths./api/v1/tools/video/webm-to-mp3.post.summary:
      sourceHash: 9fbcc26dd542
      provenance: machine
      outputHash: 81cd65f009b4
    paths./api/v1/tools/video/webm-to-mp3.post.description:
      sourceHash: 5213b45f3c7d
      provenance: machine
      outputHash: 020148d3fbbb
    paths./api/v1/tools/video/avi-to-mp3.post.summary:
      sourceHash: 5f4d7167967b
      provenance: machine
      outputHash: 941e95814116
    paths./api/v1/tools/video/avi-to-mp3.post.description:
      sourceHash: dea6a15cea91
      provenance: machine
      outputHash: 03dbdd63ceae
    paths./api/v1/tools/video/mp4-to-wav.post.summary:
      sourceHash: b6769af66349
      provenance: machine
      outputHash: 52142fcbbc2f
    paths./api/v1/tools/video/mp4-to-wav.post.description:
      sourceHash: 5b9126a6bb99
      provenance: machine
      outputHash: 6b070c1f1115
    paths./api/v1/tools/video/mov-to-wav.post.summary:
      sourceHash: 4194226fdf68
      provenance: machine
      outputHash: a57ddfb7904b
    paths./api/v1/tools/video/mov-to-wav.post.description:
      sourceHash: fe4925d7babd
      provenance: machine
      outputHash: 4a9f6eb197e3
    paths./api/v1/tools/video/mp4-to-ogg.post.summary:
      sourceHash: dcb33be4e615
      provenance: machine
      outputHash: a1340b795231
    paths./api/v1/tools/video/mp4-to-ogg.post.description:
      sourceHash: d8be42b38707
      provenance: machine
      outputHash: f0852e81df59
    paths./api/v1/tools/audio/m4a-to-mp3.post.summary:
      sourceHash: e4f86f589bd4
      provenance: machine
      outputHash: 7f830563534a
    paths./api/v1/tools/audio/m4a-to-mp3.post.description:
      sourceHash: ad144ccdc1d4
      provenance: machine
      outputHash: cc56d5c4d7a6
    paths./api/v1/tools/audio/m4a-to-wav.post.summary:
      sourceHash: 5e8aa1c62150
      provenance: machine
      outputHash: 7351fcb43a98
    paths./api/v1/tools/audio/m4a-to-wav.post.description:
      sourceHash: dab8ed1c0668
      provenance: machine
      outputHash: 858dcebe3897
    paths./api/v1/tools/audio/aac-to-mp3.post.summary:
      sourceHash: dd7d2c46e335
      provenance: machine
      outputHash: 4989918254cd
    paths./api/v1/tools/audio/aac-to-mp3.post.description:
      sourceHash: 4187ef8fb789
      provenance: machine
      outputHash: 360d05266f03
    paths./api/v1/tools/audio/aac-to-wav.post.summary:
      sourceHash: 6485f4d30fd0
      provenance: machine
      outputHash: 8776e3501ce0
    paths./api/v1/tools/audio/aac-to-wav.post.description:
      sourceHash: c2221db94cf4
      provenance: machine
      outputHash: 8a87dbdfd4be
    paths./api/v1/tools/audio/aac-to-flac.post.summary:
      sourceHash: 9dc0a9886938
      provenance: machine
      outputHash: d981ceb2b67f
    paths./api/v1/tools/audio/aac-to-flac.post.description:
      sourceHash: 945f63a2be18
      provenance: machine
      outputHash: c3c23d4cc21e
    paths./api/v1/tools/audio/ogg-to-mp3.post.summary:
      sourceHash: c3cff86b4282
      provenance: machine
      outputHash: 7fbefd6ec8b7
    paths./api/v1/tools/audio/ogg-to-mp3.post.description:
      sourceHash: 8889a760d19f
      provenance: machine
      outputHash: 2da33e26c3a0
    paths./api/v1/tools/audio/ogg-to-wav.post.summary:
      sourceHash: 85fbfd8ee338
      provenance: machine
      outputHash: 29d9984b76a8
    paths./api/v1/tools/audio/ogg-to-wav.post.description:
      sourceHash: 99156f0db542
      provenance: machine
      outputHash: ef7e6e06ee03
    paths./api/v1/tools/audio/wav-to-mp3.post.summary:
      sourceHash: d8344c7d753c
      provenance: machine
      outputHash: 0a8e3dd67030
    paths./api/v1/tools/audio/wav-to-mp3.post.description:
      sourceHash: 7f83d006baf8
      provenance: machine
      outputHash: 8876ee6434f0
    paths./api/v1/tools/audio/mp3-to-wav.post.summary:
      sourceHash: a7de59ab62b9
      provenance: machine
      outputHash: 867e336c3842
    paths./api/v1/tools/audio/mp3-to-wav.post.description:
      sourceHash: c19d5fba1478
      provenance: machine
      outputHash: 573e2594042f
    paths./api/v1/tools/audio/flac-to-mp3.post.summary:
      sourceHash: da353684dd2b
      provenance: machine
      outputHash: 8d076018908e
    paths./api/v1/tools/audio/flac-to-mp3.post.description:
      sourceHash: e83797072608
      provenance: machine
      outputHash: 1ca98ab05d38
    paths./api/v1/tools/files/excel-to-csv.post.summary:
      sourceHash: 8610a88e662a
      provenance: machine
      outputHash: 72198adddc62
    paths./api/v1/tools/files/excel-to-csv.post.description:
      sourceHash: c5b6027afaa5
      provenance: machine
      outputHash: 1375bde88d0c
    paths./api/v1/readyz.get.summary:
      sourceHash: 2f70aef72373
      provenance: machine
      outputHash: 07899bd4c2d7
    paths./api/v1/readyz.get.description:
      sourceHash: 1600aad09ab9
      provenance: machine
      outputHash: b6307067dd39
    paths./api/v1/jobs/{jobId}/cancel.post.summary:
      sourceHash: a5032d26d61d
      provenance: machine
      outputHash: 891b0a0f8622
    paths./api/v1/jobs/{jobId}/cancel.post.description:
      sourceHash: c828ad6eed72
      provenance: machine
      outputHash: 1dcfa85eab42
    paths./api/v1/preferences.get.summary:
      sourceHash: 14eccfecf549
      provenance: machine
      outputHash: 8663d1623731
    paths./api/v1/preferences.get.description:
      sourceHash: be05d444bc85
      provenance: machine
      outputHash: 956e70423e43
    paths./api/v1/preferences.put.summary:
      sourceHash: 2649ca8f6a25
      provenance: machine
      outputHash: da6454729aa7
    paths./api/v1/preferences.put.description:
      sourceHash: ac325d43a780
      provenance: machine
      outputHash: 460de4669dde
    paths./api/auth/mfa/enroll.post.summary:
      sourceHash: c138aec8b79b
      provenance: machine
      outputHash: 302bd3930a7b
    paths./api/auth/mfa/enroll.post.description:
      sourceHash: e00560362017
      provenance: machine
      outputHash: 5efe59d64d3c
    paths./api/auth/mfa/verify.post.summary:
      sourceHash: 12b670ebf850
      provenance: machine
      outputHash: 3575ef2f2688
    paths./api/auth/mfa/verify.post.description:
      sourceHash: ae628486beb5
      provenance: machine
      outputHash: 397cc36e21f9
    paths./api/auth/mfa/complete.post.summary:
      sourceHash: 16f65adb8582
      provenance: machine
      outputHash: 720ebf6da174
    paths./api/auth/mfa/complete.post.description:
      sourceHash: 3dc67142d5da
      provenance: machine
      outputHash: 4d501085b026
    paths./api/auth/mfa/disable.post.summary:
      sourceHash: 8adb8274a7c6
      provenance: machine
      outputHash: f117dca64063
    paths./api/auth/mfa/disable.post.description:
      sourceHash: 7980f5f0d9f0
      provenance: machine
      outputHash: cf55ccb9947f
    paths./api/auth/users/{id}/mfa/reset.post.summary:
      sourceHash: c765204eb72c
      provenance: machine
      outputHash: 6a278a5509e3
    paths./api/auth/oidc/login.get.summary:
      sourceHash: bac046b3ef24
      provenance: machine
      outputHash: 99056b85af64
    paths./api/auth/oidc/login.get.description:
      sourceHash: e48289d1e534
      provenance: machine
      outputHash: 00f0c145848e
    paths./api/auth/oidc/callback.get.summary:
      sourceHash: 396eda750d3c
      provenance: machine
      outputHash: a7bfd94f666e
    paths./api/auth/oidc/callback.get.description:
      sourceHash: e9437a06c530
      provenance: machine
      outputHash: 5d68a88ef5d1
    paths./api/auth/saml/metadata.get.summary:
      sourceHash: d6b4c80ec1e4
      provenance: machine
      outputHash: 9c3453520e82
    paths./api/auth/saml/metadata.get.description:
      sourceHash: 0b08445f03af
      provenance: machine
      outputHash: 11f1af669abd
    paths./api/auth/saml/login.get.summary:
      sourceHash: 6de21463c7cb
      provenance: machine
      outputHash: d904d33eefa2
    paths./api/auth/saml/login.get.description:
      sourceHash: eca8bcf506ea
      provenance: machine
      outputHash: bfb672ee5649
    paths./api/auth/saml/callback.post.summary:
      sourceHash: 2a95ad454bd5
      provenance: machine
      outputHash: 28ade6f02ec6
    paths./api/auth/saml/callback.post.description:
      sourceHash: a03dcebde897
      provenance: machine
      outputHash: c9cfe40763d0
    paths./api/v1/files/{id}/preview.get.summary:
      sourceHash: 71d50aa39313
      provenance: machine
      outputHash: 2e19d97c51ba
    paths./api/v1/files/{id}/preview.get.description:
      sourceHash: fb3edf8df6cf
      provenance: machine
      outputHash: 6d608c7261bd
    paths./api/v1/preview/generate.post.summary:
      sourceHash: cff28fa8e380
      provenance: machine
      outputHash: 6ea25711e4ea
    paths./api/v1/preview/generate.post.description:
      sourceHash: 56d484d0c85b
      provenance: machine
      outputHash: b3ec31bf3104
    paths./api/v1/admin/log-level.get.summary:
      sourceHash: f9fa3ff842cd
      provenance: machine
      outputHash: 970855b0e117
    paths./api/v1/admin/log-level.get.description:
      sourceHash: 277cddb1b284
      provenance: machine
      outputHash: 3596ebd3b760
    paths./api/v1/admin/log-level.post.summary:
      sourceHash: e9ac46f76c49
      provenance: machine
      outputHash: bc727aba33be
    paths./api/v1/admin/log-level.post.description:
      sourceHash: 5f12391622b9
      provenance: machine
      outputHash: 30fdc907cb45
    paths./api/v1/metrics.get.summary:
      sourceHash: 05be9e156194
      provenance: machine
      outputHash: a875050fb371
    paths./api/v1/metrics.get.description:
      sourceHash: e5723b8ba79e
      provenance: machine
      outputHash: 3fb2e04cc401
    paths./api/v1/admin/support-bundle.get.summary:
      sourceHash: 3fc31382eaf7
      provenance: machine
      outputHash: b599aa777048
    paths./api/v1/admin/support-bundle.get.description:
      sourceHash: c04e47b20915
      provenance: machine
      outputHash: c5a4495cd6f5
    paths./api/v1/admin/usage.get.summary:
      sourceHash: 2f5017840735
      provenance: machine
      outputHash: 7b00300e7885
    paths./api/v1/admin/usage.get.description:
      sourceHash: 5ba73241349d
      provenance: machine
      outputHash: 241f0138b7cf
    paths./api/v1/admin/backup-status.get.summary:
      sourceHash: cea8a0d690fc
      provenance: machine
      outputHash: 71c3c843b999
    paths./api/v1/admin/backup-status.get.description:
      sourceHash: e42e4494117a
      provenance: machine
      outputHash: 755e038a1984
    paths./api/v1/admin/backup-status.post.summary:
      sourceHash: 1a7ed3984596
      provenance: machine
      outputHash: 2e86e1b41176
    paths./api/v1/admin/backup-status.post.description:
      sourceHash: 84b506533dbf
      provenance: machine
      outputHash: e7b6c20fffec
    paths./api/v1/admin/features/import.post.summary:
      sourceHash: 31b34aebf83b
      provenance: machine
      outputHash: 71302de53c1e
    paths./api/v1/admin/features/import.post.description:
      sourceHash: 229fcb03f525
      provenance: machine
      outputHash: 085ece9c6636
    paths./api/v1/enterprise/scim/token.post.summary:
      sourceHash: e550c4aaeecf
      provenance: machine
      outputHash: 92db7f92e032
    paths./api/v1/enterprise/scim/token.delete.summary:
      sourceHash: e57912d50189
      provenance: machine
      outputHash: c7831758ca93
    paths./api/v1/scim/v2/ServiceProviderConfig.get.summary:
      sourceHash: 6d9bb054fb7b
      provenance: machine
      outputHash: a8acee3071a4
    paths./api/v1/scim/v2/ServiceProviderConfig.get.description:
      sourceHash: a111048aed19
      provenance: machine
      outputHash: e3135f603bab
    paths./api/v1/scim/v2/Schemas.get.summary:
      sourceHash: c04171c8ce40
      provenance: machine
      outputHash: 3cba8ccb5a2e
    paths./api/v1/scim/v2/Schemas.get.description:
      sourceHash: 74cf47ef6038
      provenance: machine
      outputHash: 5613f5969064
    paths./api/v1/scim/v2/ResourceTypes.get.summary:
      sourceHash: b3f61558b888
      provenance: machine
      outputHash: '820200784708'
    paths./api/v1/scim/v2/ResourceTypes.get.description:
      sourceHash: d3935183ed14
      provenance: machine
      outputHash: 116ab4b85b02
    paths./api/v1/scim/v2/Users.get.summary:
      sourceHash: 06aa223783d0
      provenance: machine
      outputHash: 636fee06e11b
    paths./api/v1/scim/v2/Users.get.description:
      sourceHash: dccfb3116fb5
      provenance: machine
      outputHash: 7584be6c7ac6
    paths./api/v1/scim/v2/Users.post.summary:
      sourceHash: ba4ce4467898
      provenance: machine
      outputHash: 1207cd508b05
    paths./api/v1/scim/v2/Users.post.description:
      sourceHash: ab9d5413dfe0
      provenance: machine
      outputHash: 740d2e05976f
    paths./api/v1/scim/v2/Users/{id}.get.summary:
      sourceHash: 06a26f48a1d7
      provenance: machine
      outputHash: 1e93b8b03c53
    paths./api/v1/scim/v2/Users/{id}.get.description:
      sourceHash: f643db8015e1
      provenance: machine
      outputHash: d43700ba2027
    paths./api/v1/scim/v2/Users/{id}.put.summary:
      sourceHash: acfe35ddd973
      provenance: machine
      outputHash: 7a4015c7e6d3
    paths./api/v1/scim/v2/Users/{id}.put.description:
      sourceHash: 7051119f905f
      provenance: machine
      outputHash: 1fa385472889
    paths./api/v1/scim/v2/Users/{id}.delete.summary:
      sourceHash: 11c8da919810
      provenance: machine
      outputHash: f0610683dd1a
    paths./api/v1/scim/v2/Users/{id}.delete.description:
      sourceHash: f8a3ee46d088
      provenance: machine
      outputHash: e1a4a69afe45
    paths./api/v1/scim/v2/Groups.get.summary:
      sourceHash: 7f5cfe4da60d
      provenance: machine
      outputHash: 777bf611098e
    paths./api/v1/scim/v2/Groups.get.description:
      sourceHash: b3dea6667317
      provenance: machine
      outputHash: 4b8e6df41503
    paths./api/v1/scim/v2/Groups.post.summary:
      sourceHash: 2477896fba1c
      provenance: machine
      outputHash: 4730b02cbbcd
    paths./api/v1/scim/v2/Groups.post.description:
      sourceHash: fbb117796dfb
      provenance: machine
      outputHash: 2926e17a8448
    paths./api/v1/scim/v2/Groups/{id}.get.summary:
      sourceHash: cf7956970f8e
      provenance: machine
      outputHash: c28b77e9385a
    paths./api/v1/scim/v2/Groups/{id}.get.description:
      sourceHash: 7aeb5a733198
      provenance: machine
      outputHash: 9241753508c7
    paths./api/v1/scim/v2/Groups/{id}.put.summary:
      sourceHash: 5878fbd31c27
      provenance: machine
      outputHash: 4538001d3767
    paths./api/v1/scim/v2/Groups/{id}.put.description:
      sourceHash: 0b41d735866c
      provenance: machine
      outputHash: a87b3be4e6d6
    paths./api/v1/scim/v2/Groups/{id}.delete.summary:
      sourceHash: 37212ab76d6b
      provenance: machine
      outputHash: 6204cffd1aa4
    paths./api/v1/scim/v2/Groups/{id}.delete.description:
      sourceHash: faf5572bb482
      provenance: machine
      outputHash: 4e64566a647f
    paths./api/v1/enterprise/audit/export.get.summary:
      sourceHash: bb1ef74df6a7
      provenance: machine
      outputHash: d043515c3f85
    paths./api/v1/enterprise/audit/export.get.description:
      sourceHash: 78723fd7a551
      provenance: machine
      outputHash: 5a138c5b8c9c
    paths./api/v1/enterprise/config/export.get.summary:
      sourceHash: 0657bced852f
      provenance: machine
      outputHash: a29248d9ba03
    paths./api/v1/enterprise/config/export.get.description:
      sourceHash: 8e3ce9c435f5
      provenance: machine
      outputHash: 240b4bd3ef81
    paths./api/v1/enterprise/config/import.post.summary:
      sourceHash: 8a507f40b80f
      provenance: machine
      outputHash: 79e7be0dfa9f
    paths./api/v1/enterprise/ip-allowlist.get.summary:
      sourceHash: 8756df674b27
      provenance: machine
      outputHash: 821d43e49db3
    paths./api/v1/enterprise/ip-allowlist.get.description:
      sourceHash: 42fe8d5e6555
      provenance: machine
      outputHash: 1ca4245b1039
    paths./api/v1/enterprise/ip-allowlist.put.summary:
      sourceHash: e0d442c15809
      provenance: machine
      outputHash: b2aa05547555
    paths./api/v1/enterprise/ip-allowlist.put.description:
      sourceHash: 0cb040540d15
      provenance: machine
      outputHash: 47498364fc83
    paths./api/v1/enterprise/legal-hold.get.summary:
      sourceHash: a3b33d97f1b3
      provenance: machine
      outputHash: 722dd6ff66a0
    paths./api/v1/enterprise/legal-hold.get.description:
      sourceHash: 2b51807e41b2
      provenance: machine
      outputHash: 633e778c4f74
    paths./api/v1/enterprise/legal-hold.put.summary:
      sourceHash: f0f8a1f6bd23
      provenance: machine
      outputHash: 1cb8d60fc542
    paths./api/v1/enterprise/legal-hold.put.description:
      sourceHash: 2f22192732ff
      provenance: machine
      outputHash: d05f917c9972
    paths./api/v1/enterprise/siem/config.get.summary:
      sourceHash: 7ac4058ce316
      provenance: machine
      outputHash: f71c7ec29d6f
    paths./api/v1/enterprise/siem/config.get.description:
      sourceHash: b4c92171b7fb
      provenance: machine
      outputHash: 951f5b6a03eb
    paths./api/v1/enterprise/siem/config.put.summary:
      sourceHash: b236d6669c61
      provenance: machine
      outputHash: 6be28050d129
    paths./api/v1/enterprise/siem/config.put.description:
      sourceHash: 94cec3b103c8
      provenance: machine
      outputHash: 8560b3ca10c8
    paths./api/v1/enterprise/webhooks.get.summary:
      sourceHash: a43cfb1a8c96
      provenance: machine
      outputHash: e7d4eabf9694
    paths./api/v1/enterprise/webhooks.get.description:
      sourceHash: 292a38feca49
      provenance: machine
      outputHash: fb51b20afe8d
    paths./api/v1/enterprise/webhooks.post.summary:
      sourceHash: 4a2b33ad2c1f
      provenance: machine
      outputHash: 918750aa6b34
    paths./api/v1/enterprise/webhooks.post.description:
      sourceHash: 2cc3c4b18327
      provenance: machine
      outputHash: 37493b3ac4dc
    paths./api/v1/enterprise/webhooks/{index}.put.summary:
      sourceHash: c5be5387d7be
      provenance: machine
      outputHash: eef81cd7f15c
    paths./api/v1/enterprise/webhooks/{index}.put.description:
      sourceHash: 28806bb5b6ce
      provenance: machine
      outputHash: 3c6f1fae78c0
    paths./api/v1/enterprise/webhooks/{index}.delete.summary:
      sourceHash: 60f85e57e4a7
      provenance: machine
      outputHash: 0981acf14c57
    paths./api/v1/enterprise/webhooks/{index}.delete.description:
      sourceHash: 97b9800c59a2
      provenance: machine
      outputHash: 8dbbabf9b226
    paths./api/v1/enterprise/webhooks/{index}/test.post.summary:
      sourceHash: 333e8368ac5c
      provenance: machine
      outputHash: 002713a1ef12
    paths./api/v1/enterprise/webhooks/{index}/test.post.description:
      sourceHash: 5626cb4c037d
      provenance: machine
      outputHash: a1584f6001b5
    paths./api/v1/enterprise/users/{id}/export.post.summary:
      sourceHash: 75556a6714fc
      provenance: machine
      outputHash: 91d880c5fe8e
    paths./api/v1/enterprise/users/{id}/export.post.description:
      sourceHash: 4f90b2a09ef8
      provenance: machine
      outputHash: f1ddf20c7f23
    paths./api/v1/enterprise/users/{id}/export/{jobId}.get.summary:
      sourceHash: 7fa624472044
      provenance: machine
      outputHash: 2eeb3a119dbe
    paths./api/v1/enterprise/users/{id}/export/{jobId}.get.description:
      sourceHash: fb51267dcbf7
      provenance: machine
      outputHash: dc34ac7b67c0
    paths./api/v1/enterprise/users/{id}/purge.delete.summary:
      sourceHash: 09c40d574cc0
      provenance: machine
      outputHash: 9411fe4291a6
    paths./api/v1/enterprise/teams/{id}/purge.delete.summary:
      sourceHash: e5f557c6db8b
      provenance: machine
      outputHash: 89d3c380c9a3
    paths./api/v1/admin/version.get.summary:
      sourceHash: 1fa5f3f5fa4a
      provenance: machine
      outputHash: aacc13fc45d8
    paths./api/v1/admin/version.get.description:
      sourceHash: b72b0bfc24e6
      provenance: machine
      outputHash: 67e2c6643d61
    paths./api/v1/admin/migrations/pending.get.summary:
      sourceHash: dd3d4aea6207
      provenance: machine
      outputHash: 346efbc3145f
    paths./api/v1/admin/migrations/pending.get.description:
      sourceHash: 434f121758d2
      provenance: machine
      outputHash: e2c9b7f99f7d
    paths./api/v1/admin/upgrade-check.get.summary:
      sourceHash: abc755f5b011
      provenance: machine
      outputHash: d463ba1b1739
    paths./api/v1/admin/upgrade-check.get.description:
      sourceHash: a21396abfd74
      provenance: machine
      outputHash: 4f4fdba56622
    paths./llms.txt.get.summary:
      sourceHash: ba31286a938b
      provenance: machine
      outputHash: b666b48c1a67
    paths./llms.txt.get.description:
      sourceHash: 5f69094320cb
      provenance: machine
      outputHash: bce81d168577
    paths./llms-full.txt.get.summary:
      sourceHash: 6f12d5cecc9d
      provenance: machine
      outputHash: 4d7ab1aac163
    paths./llms-full.txt.get.description:
      sourceHash: a135dfc6aac1
      provenance: machine
      outputHash: 81fbe2750d31
    paths./api/v1/openapi.yaml.get.summary:
      sourceHash: 9ee7dc77d462
      provenance: machine
      outputHash: 9ee7dc77d462
    paths./api/v1/openapi.yaml.get.description:
      sourceHash: 10ceb656a256
      provenance: machine
      outputHash: d62de08f89bb
    paths./api/v1/tools/popular.get.summary:
      sourceHash: de2139ffd4c3
      provenance: machine
      outputHash: 1262297f8189
    paths./api/v1/tools/popular.get.description:
      sourceHash: 40c9637d843b
      provenance: machine
      outputHash: 9b288a5af64c
