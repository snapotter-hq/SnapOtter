openapi: 3.1.0
info:
  title: SnapOtter API
  version: 2.0.0
  description: |
    SnapOtter の REST API。image、video、audio、PDF、ドキュメントにわたる 200+ のツールを備えた、セルフホスト型のファイル処理基盤です。

    ## 認証

    ほとんどのエンドポイントでは、次の2つのいずれかの方法による認証が必要です。

    1. **セッションクッキー** - ユーザー名とパスワードで `POST /api/auth/login` を呼び出します。レスポンスには `token` フィールドが含まれます。以降のリクエストでは、それを `Authorization: Bearer <token>` として渡してください。

    2. **API キー** - 設定 UI または `POST /api/v1/api-keys` からキーを生成します。キーには `si_` という接頭辞が付きます。`Authorization: Bearer si_...` として渡してください。

    鍵アイコンが付いたエンドポイントには認証が必要です。管理者専用のエンドポイントは、その説明に明記されています。
  license:
    name: AGPL-3.0
    url: https://github.com/snapotter-hq/snapotter/blob/main/LICENSE
servers:
  - url: /
    description: Current instance
tags:
  - name: Tools
    description: 画像、動画、音声、PDF、ファイルにまたがるファイル処理ツール。それぞれ multipart によるファイルアップロードを受け付け、ダウンロード URL を返します。
  - name: Batch
    description: 1 回のリクエストで複数のファイルを 1 つのツールで処理します。
  - name: Pipelines
    description: 複数のツールを連結して再利用可能なワークフローにします。
  - name: Files
    description: 処理済みファイルのアップロード、ダウンロード、管理を行います。
  - name: Auth
    description: ログイン、ログアウト、セッション管理、ユーザー管理。
  - name: API Keys
    description: プログラムからのアクセス用に API キーを作成、管理します。
  - name: Settings
    description: システム全体の設定（書き込みは管理者のみ）。
  - name: Teams
    description: ユーザーをチームに編成します。
  - name: Roles
    description: きめ細かな権限を持つカスタムロールの管理。
  - name: Audit
    description: 管理操作を追跡するための監査ログ。
  - name: Analytics
    description: アナリティクスの設定とユーザーの同意。
  - name: Features
    description: AI 機能バンドルのインストールと管理。
  - name: Admin
    description: 管理用のヘルス診断。
  - name: System
    description: ヘルスチェック、設定、ジョブ進捗。
  - name: Enterprise
    description: ライセンスで制御されるエンタープライズ管理、コンプライアンス、SCIM、統合 API。
components:
  securitySchemes:
    bearerAuth:
      type: http
      scheme: bearer
      description: Session token from login or API key (prefixed with si_)
  responses:
    RateLimited:
      description: Too many requests. The client has exceeded the rate limit.
      headers:
        Retry-After:
          schema:
            type: integer
          description: Seconds until the rate limit window resets
        X-RateLimit-Limit:
          schema:
            type: integer
          description: Maximum requests per time window
        X-RateLimit-Remaining:
          schema:
            type: integer
          description: Remaining requests in current window
      content:
        application/json:
          schema:
            type: object
            properties:
              error:
                type: string
                example: Rate limit exceeded
  schemas:
    Error:
      type: object
      required:
        - error
      properties:
        error:
          type: string
          example: Invalid image format
        details:
          type: string
          description: Additional error details (e.g. Zod validation errors)
    ToolResponse:
      type: object
      properties:
        jobId:
          type: string
          description: Unique job identifier
        downloadUrl:
          type: string
          description: URL to download the processed image
          example: /api/v1/download/abc123/output.png
        originalSize:
          type: integer
          description: Original file size in bytes
        processedSize:
          type: integer
          description: Processed file size in bytes
        previewUrl:
          type: string
          description: URL to a WebP preview (present when output is not browser-previewable, e.g. TIFF, HEIF)
        savedFileId:
          type: string
          description: ID of the auto-saved file in the library (present when fileId was provided in the request)
    UnauthorizedError:
      type: object
      properties:
        statusCode:
          type: integer
          example: 401
        error:
          type: string
          example: Unauthorized
        message:
          type: string
          example: Authentication required
    ForbiddenError:
      type: object
      properties:
        statusCode:
          type: integer
          example: 403
        error:
          type: string
          example: Forbidden
        message:
          type: string
          example: Insufficient permissions
    ConflictError:
      type: object
      properties:
        statusCode:
          type: integer
          example: 409
        error:
          type: string
          example: Conflict
        message:
          type: string
          example: Resource already exists
    FeatureNotInstalledError:
      type: object
      properties:
        error:
          type: string
          example: Feature not installed
        code:
          type: string
          enum:
            - FEATURE_NOT_INSTALLED
            - FEATURE_INCOMPATIBLE
          example: FEATURE_NOT_INSTALLED
        feature:
          type: string
          description: Feature bundle identifier
        featureName:
          type: string
          description: Human-readable feature name
        estimatedSize:
          type: string
          description: Estimated download size
        requestedQuality:
          type: string
          enum:
            - fast
            - balanced
            - best
          description: OCR tier that could not be honored; Fast is reported when Korean is explicitly requested with Fast or the legacy Tesseract alias
        compatibilityReason:
          type: string
          description: Machine-readable runtime compatibility reason, when available
        guidance:
          type: string
          description: Actionable recovery guidance, when available
    HealthResponse:
      type: object
      properties:
        status:
          type: string
          enum:
            - healthy
            - unhealthy
        version:
          type: string
    AdminHealthResponse:
      type: object
      properties:
        status:
          type: string
          enum:
            - healthy
            - degraded
        version:
          type: string
        uptime:
          type: string
        storage:
          type: object
          properties:
            mode:
              type: string
            available:
              type: string
        database:
          type: string
          enum:
            - ok
            - error
        queue:
          type: object
          properties:
            active:
              type: integer
            pending:
              type: integer
        ai:
          type: object
          properties:
            gpu:
              type: boolean
security:
  - bearerAuth: []
paths:
  /api/v1/health:
    get:
      operationId: healthCheck
      tags:
        - System
      summary: ヘルスチェック
      description: サーバーのヘルス状態を返します。Docker HEALTHCHECK で使用されます。パブリックエンドポイントです。
      security: []
      responses:
        '200':
          description: Server health
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/HealthResponse'
        '503':
          description: Database unavailable
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/HealthResponse'
  /api/v1/tools/image/resize:
    post:
      operationId: resizeImage
      tags:
        - Tools
      summary: リサイズ
      description: 画像を特定の寸法または割合でリサイズします。
      security:
        - bearerAuth: []
      requestBody:
        required: true
        content:
          multipart/form-data:
            schema:
              type: object
              required:
                - file
              properties:
                file:
                  type: string
                  format: binary
                  description: Image file to process
                settings:
                  type: string
                  description: |
                    JSON string with options:
                    - `width` (number, optional) - Target width in pixels
                    - `height` (number, optional) - Target height in pixels
                    - `fit` (string, default "contain") - One of: contain, cover, fill, inside, outside
                    - `withoutEnlargement` (boolean, default false) - Prevent upscaling
                    - `percentage` (number, optional) - Scale by percentage instead of fixed dimensions
      responses:
        '200':
          description: Processed image
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ToolResponse'
        '400':
          description: Invalid input
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Error'
        '401':
          description: Authentication required
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/UnauthorizedError'
  /api/v1/tools/image/content-aware-resize:
    post:
      operationId: contentAwareResize
      tags:
        - Tools
      summary: コンテンツ対応リサイズ
      description: シームカービングを用いて画像をリサイズし、重要な特徴を保ちながら賢くコンテンツを削除または挿入します。
      security:
        - bearerAuth: []
      requestBody:
        required: true
        content:
          multipart/form-data:
            schema:
              type: object
              required:
                - file
              properties:
                file:
                  type: string
                  format: binary
                  description: Image file to process
                settings:
                  type: string
                  description: |
                    JSON string with options:
                    - `width` (number, optional) - Target width in pixels
                    - `height` (number, optional) - Target height in pixels
                    - `protectFaces` (boolean, default false) - Detect and protect faces from distortion
                    - `blurRadius` (number 0-20, default 4) - Blur radius for energy map
                    - `sobelThreshold` (number 1-20, default 2) - Edge detection threshold
                    - `square` (boolean, default false) - Crop to square aspect ratio

                    At least one of `width`, `height`, or `square` must be provided.
      responses:
        '200':
          description: Processed image
          content:
            application/json:
              schema:
                allOf:
                  - $ref: '#/components/schemas/ToolResponse'
                  - type: object
                    properties:
                      width:
                        type: integer
                      height:
                        type: integer
        '400':
          description: Invalid input
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Error'
        '401':
          description: Authentication required
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/UnauthorizedError'
  /api/v1/tools/image/crop:
    post:
      operationId: cropImage
      tags:
        - Tools
      summary: 切り抜き
      description: 画像を特定の領域に切り抜きます。
      security:
        - bearerAuth: []
      requestBody:
        required: true
        content:
          multipart/form-data:
            schema:
              type: object
              required:
                - file
              properties:
                file:
                  type: string
                  format: binary
                  description: Image file to process
                settings:
                  type: string
                  description: |
                    JSON string with options:
                    - `left` (number, required) - Left offset in pixels (min 0)
                    - `top` (number, required) - Top offset in pixels (min 0)
                    - `width` (number, required) - Width of crop region in pixels
                    - `height` (number, required) - Height of crop region in pixels
                    - `unit` (string, optional) - One of: px, percent
      responses:
        '200':
          description: Processed image
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ToolResponse'
        '400':
          description: Invalid input
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Error'
        '401':
          description: Authentication required
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/UnauthorizedError'
  /api/v1/tools/image/rotate:
    post:
      operationId: rotateImage
      tags:
        - Tools
      summary: 回転と反転
      description: 画像を角度で回転させる、または水平方向/垂直方向に反転します。
      security:
        - bearerAuth: []
      requestBody:
        required: true
        content:
          multipart/form-data:
            schema:
              type: object
              required:
                - file
              properties:
                file:
                  type: string
                  format: binary
                  description: Image file to process
                settings:
                  type: string
                  description: |
                    JSON string with options:
                    - `angle` (number, default 0) - Rotation angle in degrees
                    - `horizontal` (boolean, default false) - Flip horizontally
                    - `vertical` (boolean, default false) - Flip vertically
      responses:
        '200':
          description: Processed image
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ToolResponse'
        '400':
          description: Invalid input
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Error'
        '401':
          description: Authentication required
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/UnauthorizedError'
  /api/v1/tools/image/convert:
    post:
      operationId: convertFormat
      tags:
        - Tools
      summary: フォーマット変換
      description: 画像を別のフォーマットに変換します。
      security:
        - bearerAuth: []
      requestBody:
        required: true
        content:
          multipart/form-data:
            schema:
              type: object
              required:
                - file
              properties:
                file:
                  type: string
                  format: binary
                  description: Image file to process
                settings:
                  type: string
                  description: |
                    JSON string with options:
                    - `format` (string, required) - One of: jpg, png, webp, avif, tiff, gif, heic, heif, jxl, bmp, ico, jp2, qoi, psd
                    - `quality` (number 1-100, optional) - Output quality
      responses:
        '200':
          description: Processed image
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ToolResponse'
        '400':
          description: Invalid input
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Error'
        '401':
          description: Authentication required
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/UnauthorizedError'
  /api/v1/tools/image/compress:
    post:
      operationId: compressImage
      tags:
        - Tools
      summary: 圧縮
      description: 品質レベルまたは目標サイズによって画像のファイルサイズを削減します。
      security:
        - bearerAuth: []
      requestBody:
        required: true
        content:
          multipart/form-data:
            schema:
              type: object
              required:
                - file
              properties:
                file:
                  type: string
                  format: binary
                  description: Image file to process
                settings:
                  type: string
                  description: |
                    JSON string with options:
                    - `mode` (string, default "quality") - One of: quality, targetSize
                    - `quality` (number 1-100, optional) - Compression quality level
                    - `targetSizeKb` (number, optional) - Target file size in kilobytes
      responses:
        '200':
          description: Processed image
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ToolResponse'
        '400':
          description: Invalid input
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Error'
        '401':
          description: Authentication required
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/UnauthorizedError'
  /api/v1/tools/image/strip-metadata:
    post:
      operationId: stripMetadata
      tags:
        - Tools
      summary: メタデータ削除
      description: 画像から EXIF、GPS、ICC、XMP のメタデータを削除します。
      security:
        - bearerAuth: []
      requestBody:
        required: true
        content:
          multipart/form-data:
            schema:
              type: object
              required:
                - file
              properties:
                file:
                  type: string
                  format: binary
                  description: Image file to process
                settings:
                  type: string
                  description: |
                    JSON string with options:
                    - `stripExif` (boolean, default false) - Remove EXIF data
                    - `stripGps` (boolean, default false) - Remove GPS data
                    - `stripIcc` (boolean, default false) - Remove ICC profile
                    - `stripXmp` (boolean, default false) - Remove XMP data
                    - `stripAll` (boolean, default true) - Remove all metadata
      responses:
        '200':
          description: Processed image
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ToolResponse'
        '400':
          description: Invalid input
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Error'
        '401':
          description: Authentication required
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/UnauthorizedError'
  /api/v1/tools/image/border:
    post:
      operationId: addBorder
      tags:
        - Tools
      summary: 枠線とフレーム
      description: 画像に枠線、角丸、余白、影を追加します。
      security:
        - bearerAuth: []
      requestBody:
        required: true
        content:
          multipart/form-data:
            schema:
              type: object
              required:
                - file
              properties:
                file:
                  type: string
                  format: binary
                  description: Image file to process
                settings:
                  type: string
                  description: |
                    JSON string with options:
                    - `borderWidth` (number 0-2000, default 10) - Border thickness in pixels
                    - `borderColor` (hex string, default "#000000") - Border color
                    - `padding` (number 0-200, default 0) - Inner padding in pixels
                    - `paddingColor` (hex string, default "#FFFFFF") - Padding fill color
                    - `cornerRadius` (number 0-2000, default 0) - Corner rounding radius
                    - `shadow` (boolean, default false) - Enable drop shadow
                    - `shadowBlur` (number 1-200, default 15) - Drop shadow blur radius
                    - `shadowOffsetX` (number -50 to 50, default 0) - Shadow horizontal offset
                    - `shadowOffsetY` (number -50 to 50, default 5) - Shadow vertical offset
                    - `shadowColor` (hex string, default "#000000") - Drop shadow color
                    - `shadowOpacity` (number 0-100, default 40) - Shadow opacity percentage
      responses:
        '200':
          description: Processed image
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ToolResponse'
        '400':
          description: Invalid input
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Error'
        '401':
          description: Authentication required
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/UnauthorizedError'
  /api/v1/tools/image/beautify:
    post:
      operationId: beautifyScreenshot
      tags:
        - Tools
      summary: スクリーンショットの装飾
      description: |
        スクリーンショットにグラデーション背景、デバイスフレーム、影、透かし、ソーシャルメディア向けサイズを追加します。単色、線形グラデーション、放射状グラデーション、画像、透明の背景に対応します。macOS、Windows、ブラウザー、iPhone、MacBook、iPad のデバイスフレームを含みます。Twitter、LinkedIn、Instagram、Facebook、Product Hunt 向けのソーシャルメディアプリセットを備えます。
      security:
        - bearerAuth: []
      requestBody:
        required: true
        content:
          multipart/form-data:
            schema:
              type: object
              required:
                - file
              properties:
                file:
                  type: string
                  format: binary
                  description: Screenshot or image file to beautify
                backgroundImage:
                  type: string
                  format: binary
                  description: Optional image for image backgrounds (used when backgroundType is "image")
                settings:
                  type: string
                  description: |
                    JSON string with options:
                    - `backgroundType` (string, default "linear-gradient") -- One of: solid, linear-gradient, radial-gradient, image, transparent
                    - `backgroundColor` (hex string, default "#667eea") -- Background color (for solid backgrounds)
                    - `gradientStops` (array, default [{color:"#667eea",position:0},{color:"#764ba2",position:100}]) -- Array of {color, position} objects for gradient backgrounds
                    - `gradientAngle` (number 0-360, default 135) -- Gradient angle in degrees
                    - `padding` (number 0-256, default 64) -- Space between screenshot and canvas edge in pixels
                    - `borderRadius` (number 0-64, default 12) -- Corner rounding radius for the screenshot
                    - `shadowPreset` (string, default "subtle") -- One of: none, subtle, medium, dramatic, custom
                    - `shadowBlur` (number 0-100, default 20) -- Shadow blur radius (used with custom shadow preset)
                    - `shadowOffsetX` (number -50 to 50, default 0) -- Shadow horizontal offset
                    - `shadowOffsetY` (number -50 to 50, default 10) -- Shadow vertical offset
                    - `shadowColor` (hex string, default "#000000") -- Shadow color
                    - `shadowOpacity` (number 0-100, default 30) -- Shadow opacity percentage
                    - `frame` (string, default "none") -- One of: none, macos-light, macos-dark, windows-light, windows-dark, browser-light, browser-dark, iphone, iphone-dark, macbook, macbook-dark, ipad, ipad-dark
                    - `frameTitle` (string, optional) -- Title text shown in window title bars
                    - `socialPreset` (string, default "none") -- One of: none, twitter, linkedin, instagram-square, instagram-story, facebook, producthunt
                    - `watermarkText` (string, optional) -- Watermark text to overlay
                    - `watermarkPosition` (string, default "bottom-right") -- One of: top-left, top-right, bottom-left, bottom-right, center
                    - `watermarkOpacity` (number 0-100, default 50) -- Watermark opacity percentage
                    - `outputFormat` (string, default "png") -- One of: png, jpeg, webp
      responses:
        '200':
          description: Beautified image
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ToolResponse'
        '400':
          description: Invalid input
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Error'
        '401':
          description: Authentication required
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/UnauthorizedError'
  /api/v1/tools/image/adjust-colors:
    post:
      operationId: adjustColors
      tags:
        - Tools
      summary: 色調整
      description: |
        統合された色調整ツール。明るさ、コントラスト、露出、彩度、色温度、色合い、色相、シャープネス、カラーチャンネル、エフェクトを 1 回の処理で調整します。
      security:
        - bearerAuth: []
      requestBody:
        required: true
        content:
          multipart/form-data:
            schema:
              type: object
              required:
                - file
              properties:
                file:
                  type: string
                  format: binary
                  description: Image file to process
                settings:
                  type: string
                  description: |
                    JSON string with options:
                    - `brightness` (number -100 to 100, default 0) - Brightness adjustment
                    - `contrast` (number -100 to 100, default 0) - Contrast adjustment
                    - `exposure` (number -100 to 100, default 0) - Exposure adjustment (gamma)
                    - `saturation` (number -100 to 100, default 0) - Color saturation
                    - `temperature` (number -100 to 100, default 0) - Color temperature (cool to warm)
                    - `tint` (number -100 to 100, default 0) - Tint shift (green to magenta)
                    - `hue` (number -180 to 180, default 0) - Hue rotation in degrees
                    - `sharpness` (number 0 to 100, default 0) - Sharpness enhancement
                    - `red` (number 0-200, default 100) - Red channel multiplier
                    - `green` (number 0-200, default 100) - Green channel multiplier
                    - `blue` (number 0-200, default 100) - Blue channel multiplier
                    - `effect` (string, default "none") - One of: none, grayscale, sepia, invert
      responses:
        '200':
          description: Processed image
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ToolResponse'
        '400':
          description: Invalid input
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Error'
        '401':
          description: Authentication required
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/UnauthorizedError'
  /api/v1/tools/image/sharpening:
    post:
      operationId: sharpenImage
      tags:
        - Tools
      summary: シャープ化
      description: 適応的、アンシャープマスク、ハイパスの各手法で画像をシャープ化します。ノイズ低減はオプションです。
      security:
        - bearerAuth: []
      requestBody:
        required: true
        content:
          multipart/form-data:
            schema:
              type: object
              required:
                - file
              properties:
                file:
                  type: string
                  format: binary
                  description: Image file to process
                settings:
                  type: string
                  description: |
                    JSON string with options:
                    - `method` (string, default "adaptive") - One of: adaptive, unsharp-mask, high-pass
                    - `sigma` (number 0.5-10, default 1.0) - Gaussian sigma (adaptive)
                    - `m1` (number 0-10, default 1.0) - Flat area sharpening (adaptive)
                    - `m2` (number 0-20, default 3.0) - Jagged area sharpening (adaptive)
                    - `x1` (number 0-10, default 2.0) - Flat/jagged threshold (adaptive)
                    - `y2` (number 0-50, default 12) - Maximum brightening (adaptive)
                    - `y3` (number 0-50, default 20) - Maximum darkening (adaptive)
                    - `amount` (number 0-1000, default 100) - Sharpen amount (unsharp-mask)
                    - `radius` (number 0.1-5, default 1.0) - Blur radius (unsharp-mask)
                    - `threshold` (number 0-255, default 0) - Luminance threshold (unsharp-mask)
                    - `strength` (number 0-100, default 50) - Blend strength (high-pass)
                    - `kernelSize` (integer, default 3) - One of: 3, 5 (high-pass)
                    - `denoise` (string, default "off") - One of: off, light, medium, strong
      responses:
        '200':
          description: Processed image
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ToolResponse'
        '400':
          description: Invalid input
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Error'
        '401':
          description: Authentication required
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/UnauthorizedError'
  /api/v1/tools/image/optimize-for-web:
    post:
      operationId: optimizeForWeb
      tags:
        - Tools
      summary: Web 向け最適化
      description: 最新フォーマットへの変換、品質調整、必要に応じたリサイズにより、Web 配信向けに画像を最適化します。
      security:
        - bearerAuth: []
      requestBody:
        required: true
        content:
          multipart/form-data:
            schema:
              type: object
              required:
                - file
              properties:
                file:
                  type: string
                  format: binary
                  description: Image file to process
                settings:
                  type: string
                  description: |
                    JSON string with options:
                    - `format` (string, default "webp") - One of: webp, jpeg, avif, png, jxl
                    - `quality` (number 1-100, default 80) - Output quality
                    - `maxWidth` (number, optional) - Maximum width in pixels
                    - `maxHeight` (number, optional) - Maximum height in pixels
                    - `progressive` (boolean, default true) - Enable progressive encoding
                    - `stripMetadata` (boolean, default true) - Remove metadata
      responses:
        '200':
          description: Processed image
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ToolResponse'
        '400':
          description: Invalid input
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Error'
        '401':
          description: Authentication required
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/UnauthorizedError'
  /api/v1/tools/image/optimize-for-web/preview:
    post:
      operationId: optimizeForWebPreview
      tags:
        - Tools
      summary: Web 向け最適化（プレビュー）
      description: |
        パラメーターをリアルタイムに調整するための軽量プレビューエンドポイント。最適化された画像バイナリを（JSON ではなく）直接返します。サイズ情報はレスポンスヘッダーに含まれます。ワークスペースの作成や結果の保存は行いません。
      security:
        - bearerAuth: []
      requestBody:
        required: true
        content:
          multipart/form-data:
            schema:
              type: object
              required:
                - file
              properties:
                file:
                  type: string
                  format: binary
                  description: Image file to preview
                settings:
                  type: string
                  description: Same settings as the main optimize-for-web endpoint
      responses:
        '200':
          description: Optimized image binary
          headers:
            X-Original-Size:
              schema:
                type: string
              description: Original file size in bytes
            X-Processed-Size:
              schema:
                type: string
              description: Processed file size in bytes
            X-Output-Filename:
              schema:
                type: string
              description: Suggested output filename
          content:
            image/*:
              schema:
                type: string
                format: binary
        '400':
          description: Invalid input
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Error'
        '422':
          description: Processing failed
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Error'
  /api/v1/tools/image/image-enhancement:
    post:
      operationId: enhanceImage
      tags:
        - Tools
      summary: 画像補正
      description: |
        AI による自動画像補正。画像を分析し、選択したシーンモードに基づいて露出、コントラスト、ホワイトバランス、彩度、シャープネス、ノイズの補正を適用します。
      security:
        - bearerAuth: []
      requestBody:
        required: true
        content:
          multipart/form-data:
            schema:
              type: object
              required:
                - file
              properties:
                file:
                  type: string
                  format: binary
                  description: Image file to process
                settings:
                  type: string
                  description: |
                    JSON string with options:
                    - `mode` (string, default "auto") - One of: auto, portrait, landscape, low-light, food, document
                    - `intensity` (number 0-100, default 50) - Overall enhancement intensity
                    - `corrections` (object, optional) - Toggle individual corrections:
                      - `exposure` (boolean, default true)
                      - `contrast` (boolean, default true)
                      - `whiteBalance` (boolean, default true)
                      - `saturation` (boolean, default true)
                      - `sharpness` (boolean, default true)
                      - `denoise` (boolean, default true)
                    - `deepEnhance` (boolean, default false) - Use AI noise removal for deeper enhancement (requires noise-removal feature)
      responses:
        '200':
          description: Processed image
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ToolResponse'
        '400':
          description: Invalid input
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Error'
        '401':
          description: Authentication required
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/UnauthorizedError'
  /api/v1/tools/image/image-enhancement/analyze:
    post:
      operationId: analyzeImageEnhancement
      tags:
        - Tools
      summary: 補正のための画像分析
      description: |
        画像を分析し、適用せずに推奨される補正を返します。どのような補正が行われるかをプレビューするのに便利です。
      security:
        - bearerAuth: []
      requestBody:
        required: true
        content:
          multipart/form-data:
            schema:
              type: object
              required:
                - file
              properties:
                file:
                  type: string
                  format: binary
                  description: Image file to analyze
      responses:
        '200':
          description: Image analysis with suggested corrections
          content:
            application/json:
              schema:
                type: object
                properties:
                  corrections:
                    type: object
                    description: Suggested correction values
                  metrics:
                    type: object
                    description: Image quality metrics
        '400':
          description: Invalid input
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Error'
        '422':
          description: Analysis failed
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Error'
  /api/v1/tools/image/noise-removal:
    post:
      operationId: removeNoise
      tags:
        - Tools
      summary: ノイズ除去
      description: |
        Python サイドカーを用いた AI ノイズ除去。クイックプレビューから最高品質まで複数の品質段階に対応します。noise-removal 機能バンドルのインストールが必要です。
      security:
        - bearerAuth: []
      requestBody:
        required: true
        content:
          multipart/form-data:
            schema:
              type: object
              required:
                - file
              properties:
                file:
                  type: string
                  format: binary
                  description: Image file to process
                settings:
                  type: string
                  description: |
                    JSON string with options:
                    - `tier` (string, default "balanced") - One of: quick, balanced, quality, maximum
                    - `strength` (number, default 50) - Denoising strength
                    - `detailPreservation` (number, default 50) - Detail preservation level
                    - `colorNoise` (number, default 30) - Color noise reduction
                    - `format` (string, default "original") - One of: original, png, jpeg, webp, avif, jxl
                    - `quality` (number, default 90) - Output quality (1-100)
                clientJobId:
                  type: string
                  description: Client-provided job ID for SSE progress tracking
      responses:
        '202':
          description: Accepted for async processing. Track progress via SSE at /api/v1/jobs/{jobId}/progress.
          content:
            application/json:
              schema:
                type: object
                properties:
                  jobId:
                    type: string
                  async:
                    type: boolean
                    example: true
        '400':
          description: Invalid input
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Error'
        '401':
          description: Authentication required
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/UnauthorizedError'
        '422':
          description: Processing failed
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Error'
        '501':
          description: Feature not installed
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/FeatureNotInstalledError'
  /api/v1/tools/image/red-eye-removal:
    post:
      operationId: removeRedEye
      tags:
        - Tools
      summary: 赤目除去
      description: |
        AI による赤目の検出と除去。顔を検出し、写真の赤目の写り込みを補正します。red-eye-removal 機能バンドルのインストールが必要です。
      security:
        - bearerAuth: []
      requestBody:
        required: true
        content:
          multipart/form-data:
            schema:
              type: object
              required:
                - file
              properties:
                file:
                  type: string
                  format: binary
                  description: Image file to process
                settings:
                  type: string
                  description: |
                    JSON string with options:
                    - `sensitivity` (number 0-100, default 50) - Detection sensitivity
                    - `strength` (number 0-100, default 70) - Correction strength
                    - `format` (string, optional) - Output format
                    - `quality` (number 1-100, default 90) - Output quality
                clientJobId:
                  type: string
                  description: Client-provided job ID for SSE progress tracking
      responses:
        '202':
          description: Accepted for async processing. Track progress via SSE at /api/v1/jobs/{jobId}/progress.
          content:
            application/json:
              schema:
                type: object
                properties:
                  jobId:
                    type: string
                  async:
                    type: boolean
                    example: true
        '400':
          description: Invalid input
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Error'
        '401':
          description: Authentication required
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/UnauthorizedError'
        '422':
          description: Processing failed
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Error'
        '501':
          description: Feature not installed
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/FeatureNotInstalledError'
  /api/v1/tools/image/restore-photo:
    post:
      operationId: restorePhoto
      tags:
        - Tools
      summary: 写真の復元
      description: |
        AI による写真復元パイプライン。傷を修復し、顔を補正し、ノイズを除去し、必要に応じて古い写真や傷んだ写真をカラー化します。photo-restoration 機能バンドルのインストールが必要です。
      security:
        - bearerAuth: []
      requestBody:
        required: true
        content:
          multipart/form-data:
            schema:
              type: object
              required:
                - file
              properties:
                file:
                  type: string
                  format: binary
                  description: Image file to process
                settings:
                  type: string
                  description: |
                    JSON string with options:
                    - `scratchRemoval` (boolean, default true) - Enable scratch repair
                    - `faceEnhancement` (boolean, default true) - Enhance detected faces
                    - `fidelity` (number 0-1, default 0.7) - Face enhancement fidelity
                    - `denoise` (boolean, default true) - Enable denoising
                    - `denoiseStrength` (number 0-100, default 25) - Denoising strength
                    - `colorize` (boolean, default false) - Colorize grayscale photos
                    - `colorizeStrength` (number 0-100, default 85) - Colorization strength
                clientJobId:
                  type: string
                  description: Client-provided job ID for SSE progress tracking
      responses:
        '202':
          description: Accepted for async processing. Track progress via SSE at /api/v1/jobs/{jobId}/progress.
          content:
            application/json:
              schema:
                type: object
                properties:
                  jobId:
                    type: string
                  async:
                    type: boolean
                    example: true
        '400':
          description: Invalid input
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Error'
        '401':
          description: Authentication required
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/UnauthorizedError'
        '422':
          description: Processing failed
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Error'
        '501':
          description: Feature not installed
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/FeatureNotInstalledError'
  /api/v1/tools/image/passport-photo/analyze:
    post:
      operationId: analyzePassportPhoto
      tags:
        - Tools
      summary: 証明写真 - 分析
      description: |
        証明写真生成のフェーズ 1。顔のランドマークを検出し、背景を除去します。generate フェーズでクライアントが使用するランドマークデータと base64 エンコードされたプレビューを返します。passport-photo 機能バンドルのインストールが必要です。
      security:
        - bearerAuth: []
      requestBody:
        required: true
        content:
          multipart/form-data:
            schema:
              type: object
              required:
                - file
              properties:
                file:
                  type: string
                  format: binary
                  description: Photo with a clear, front-facing face
                clientJobId:
                  type: string
                  description: Client-provided job ID for SSE progress tracking
      responses:
        '200':
          description: Face analysis and background removal result
          content:
            application/json:
              schema:
                type: object
                properties:
                  jobId:
                    type: string
                    description: Job ID to pass to the generate endpoint
                  filename:
                    type: string
                  preview:
                    type: string
                    description: Base64-encoded PNG preview of the background-removed image
                  previewWidth:
                    type: integer
                  previewHeight:
                    type: integer
                  landmarks:
                    type: object
                    description: Normalized face landmark coordinates (0-1)
                    properties:
                      leftEye:
                        type: object
                        properties:
                          x:
                            type: number
                          'y':
                            type: number
                      rightEye:
                        type: object
                        properties:
                          x:
                            type: number
                          'y':
                            type: number
                      eyeCenter:
                        type: object
                        properties:
                          x:
                            type: number
                          'y':
                            type: number
                      chin:
                        type: object
                        properties:
                          x:
                            type: number
                          'y':
                            type: number
                      forehead:
                        type: object
                        properties:
                          x:
                            type: number
                          'y':
                            type: number
                      crown:
                        type: object
                        properties:
                          x:
                            type: number
                          'y':
                            type: number
                      nose:
                        type: object
                        properties:
                          x:
                            type: number
                          'y':
                            type: number
                      faceCenterX:
                        type: number
                  imageWidth:
                    type: integer
                  imageHeight:
                    type: integer
        '400':
          description: Invalid input
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Error'
        '401':
          description: Authentication required
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/UnauthorizedError'
        '422':
          description: No face detected or analysis failed
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Error'
        '501':
          description: Feature not installed
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/FeatureNotInstalledError'
  /api/v1/tools/image/passport-photo/generate:
    post:
      operationId: generatePassportPhoto
      tags:
        - Tools
      summary: 証明写真 - 生成
      description: |
        証明写真生成のフェーズ 2。analyze フェーズの jobId とランドマークを用いて、指定した国/書類の規格に合わせて写真を切り抜き、リサイズし、並べます。応答は高速です（AI の再実行なし）。
      security:
        - bearerAuth: []
      requestBody:
        required: true
        content:
          application/json:
            schema:
              type: object
              required:
                - jobId
                - filename
                - countryCode
                - landmarks
                - imageWidth
                - imageHeight
              properties:
                jobId:
                  type: string
                  description: Job ID from the analyze phase
                filename:
                  type: string
                  description: Original filename from analyze phase
                countryCode:
                  type: string
                  description: ISO country code for passport specification
                documentType:
                  type: string
                  default: passport
                  description: Document type (passport, visa, etc.)
                bgColor:
                  type: string
                  default: '#FFFFFF'
                  description: Background color (hex)
                printLayout:
                  type: string
                  default: none
                  description: Print sheet layout (none, 4x6, A4, letter)
                maxFileSizeKb:
                  type: number
                  default: 0
                  description: Max file size in KB (0 = no limit)
                dpi:
                  type: number
                  minimum: 72
                  maximum: 1200
                  default: 300
                  description: Output DPI
                customWidthMm:
                  type: number
                  description: Custom photo width in mm (overrides country spec)
                customHeightMm:
                  type: number
                  description: Custom photo height in mm (overrides country spec)
                zoom:
                  type: number
                  minimum: 0.5
                  maximum: 3
                  default: 1
                  description: Zoom level (>1 = tighter crop)
                adjustX:
                  type: number
                  default: 0
                  description: Horizontal position adjustment
                adjustY:
                  type: number
                  default: 0
                  description: Vertical position adjustment
                landmarks:
                  type: object
                  description: Face landmarks from analyze phase
                imageWidth:
                  type: integer
                  description: Original image width from analyze phase
                imageHeight:
                  type: integer
                  description: Original image height from analyze phase
      responses:
        '200':
          description: Generated passport photo
          content:
            application/json:
              schema:
                type: object
                properties:
                  jobId:
                    type: string
                  downloadUrl:
                    type: string
                  dimensions:
                    type: object
                    properties:
                      widthMm:
                        type: number
                      heightMm:
                        type: number
                      widthPx:
                        type: integer
                      heightPx:
                        type: integer
                      dpi:
                        type: integer
                  spec:
                    type: object
                    properties:
                      country:
                        type: string
                      countryCode:
                        type: string
                      documentType:
                        type: string
                      documentLabel:
                        type: string
                  printDownloadUrl:
                    type: string
                    description: URL for print sheet download (if printLayout was specified)
        '400':
          description: Invalid settings or unknown country code
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Error'
        '401':
          description: Authentication required
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/UnauthorizedError'
        '422':
          description: Generation failed
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Error'
  /api/v1/tools/image/colorize:
    post:
      operationId: colorizeImage
      tags:
        - Tools
      summary: カラー化
      description: |
        AI による写真のカラー化。DDColor（OpenCV DNN フォールバック付き）を用いて白黒またはグレースケールの写真をフルカラーに変換します。colorize 機能バンドルのインストールが必要です。
      security:
        - bearerAuth: []
      requestBody:
        required: true
        content:
          multipart/form-data:
            schema:
              type: object
              required:
                - file
              properties:
                file:
                  type: string
                  format: binary
                  description: Grayscale or B&W image file
                settings:
                  type: string
                  description: |
                    JSON string with options:
                    - `intensity` (number 0-1, default 1.0) - Colorization intensity
                    - `model` (string, default "auto") - One of: auto, ddcolor, opencv
                clientJobId:
                  type: string
                  description: Client-provided job ID for SSE progress tracking
      responses:
        '202':
          description: Accepted for async processing. Track progress via SSE at /api/v1/jobs/{jobId}/progress.
          content:
            application/json:
              schema:
                type: object
                properties:
                  jobId:
                    type: string
                  async:
                    type: boolean
                    example: true
        '400':
          description: Invalid input
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Error'
        '401':
          description: Authentication required
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/UnauthorizedError'
        '422':
          description: Processing failed
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Error'
        '501':
          description: Feature not installed
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/FeatureNotInstalledError'
  /api/v1/tools/image/enhance-faces:
    post:
      operationId: enhanceFaces
      tags:
        - Tools
      summary: 顔の補正
      description: |
        GFPGAN または CodeFormer を用いた AI による顔の補正。画像内の顔を検出し、顔のディテール、肌の質感、鮮明さを向上させます。enhance-faces 機能バンドルのインストールが必要です。
      security:
        - bearerAuth: []
      requestBody:
        required: true
        content:
          multipart/form-data:
            schema:
              type: object
              required:
                - file
              properties:
                file:
                  type: string
                  format: binary
                  description: Image file containing faces
                settings:
                  type: string
                  description: |
                    JSON string with options:
                    - `model` (string, default "auto") - One of: auto, gfpgan, codeformer
                    - `strength` (number 0-1, default 0.8) - Enhancement strength
                    - `onlyCenterFace` (boolean, default false) - Only enhance the center face
                    - `sensitivity` (number 0-1, default 0.5) - Face detection sensitivity
                clientJobId:
                  type: string
                  description: Client-provided job ID for SSE progress tracking
      responses:
        '202':
          description: Accepted for async processing. Track progress via SSE at /api/v1/jobs/{jobId}/progress.
          content:
            application/json:
              schema:
                type: object
                properties:
                  jobId:
                    type: string
                  async:
                    type: boolean
                    example: true
        '400':
          description: Invalid input
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Error'
        '401':
          description: Authentication required
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/UnauthorizedError'
        '422':
          description: Processing failed
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Error'
        '501':
          description: Feature not installed
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/FeatureNotInstalledError'
  /api/v1/tools/image/image-to-base64:
    post:
      operationId: imageToBase64
      tags:
        - Tools
      summary: 画像から Base64
      description: |
        1 つ以上の画像を data URI 付きの Base64 エンコード文字列に変換します。フォーマット変換とリサイズをオプションで行えます。各ファイルについて生の Base64 文字列とそのまま使える data URI の両方を返します。
      security:
        - bearerAuth: []
      requestBody:
        required: true
        content:
          multipart/form-data:
            schema:
              type: object
              required:
                - file
              properties:
                file:
                  type: array
                  items:
                    type: string
                    format: binary
                  description: One or more image files to convert
                settings:
                  type: string
                  description: |
                    JSON string with options:
                    - `outputFormat` (string, default "original") - One of: original, jpeg, png, webp, avif, jxl
                    - `quality` (integer 1-100, default 80) - Output quality for lossy formats
                    - `maxWidth` (integer, default 0) - Max width in pixels (0 = no limit)
                    - `maxHeight` (integer, default 0) - Max height in pixels (0 = no limit)
      responses:
        '200':
          description: Base64-encoded results
          content:
            application/json:
              schema:
                type: object
                properties:
                  results:
                    type: array
                    items:
                      type: object
                      properties:
                        filename:
                          type: string
                        mimeType:
                          type: string
                        width:
                          type: integer
                        height:
                          type: integer
                        originalSize:
                          type: integer
                          description: Original file size in bytes
                        encodedSize:
                          type: integer
                          description: Base64 string size in bytes
                        overheadPercent:
                          type: number
                          description: Size overhead of Base64 encoding
                        base64:
                          type: string
                          description: Raw Base64-encoded image data
                        dataUri:
                          type: string
                          description: Complete data URI (data:mime;base64,...)
                  errors:
                    type: array
                    items:
                      type: object
                      properties:
                        filename:
                          type: string
                        error:
                          type: string
        '400':
          description: Invalid input
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Error'
        '401':
          description: Authentication required
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/UnauthorizedError'
  /api/v1/tools/image/watermark-text:
    post:
      operationId: addTextWatermark
      tags:
        - Tools
      summary: テキスト透かし
      description: 画像にテキストの透かしを追加します。
      security:
        - bearerAuth: []
      requestBody:
        required: true
        content:
          multipart/form-data:
            schema:
              type: object
              required:
                - file
              properties:
                file:
                  type: string
                  format: binary
                  description: Image file to process
                settings:
                  type: string
                  description: |
                    JSON string with options:
                    - `text` (string 1-500, required) - Watermark text
                    - `fontSize` (number 8-1000, default 48) - Font size in pixels
                    - `color` (hex string, default "#000000") - Text color
                    - `opacity` (number 0-100, default 50) - Opacity percentage
                    - `position` (string, default "center") - One of: center, top-left, top-right, bottom-left, bottom-right, tiled
                    - `rotation` (number -360 to 360, default 0) - Text rotation angle
      responses:
        '200':
          description: Processed image
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ToolResponse'
        '400':
          description: Invalid input
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Error'
        '401':
          description: Authentication required
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/UnauthorizedError'
  /api/v1/tools/image/text-overlay:
    post:
      operationId: addTextOverlay
      tags:
        - Tools
      summary: テキストオーバーレイ
      description: 背景ボックス（オプション）付きのスタイル付きテキストオーバーレイを追加します。
      security:
        - bearerAuth: []
      requestBody:
        required: true
        content:
          multipart/form-data:
            schema:
              type: object
              required:
                - file
              properties:
                file:
                  type: string
                  format: binary
                  description: Image file to process
                settings:
                  type: string
                  description: |
                    JSON string with options:
                    - `text` (string 1-500, required) - Overlay text
                    - `fontSize` (number 8-200, default 48) - Font size in pixels
                    - `color` (hex string, default "#FFFFFF") - Text color
                    - `position` (string, default "bottom") - One of: top, center, bottom
                    - `backgroundBox` (boolean, default false) - Show background box behind text
                    - `backgroundColor` (hex string, default "#000000") - Background box color
                    - `shadow` (boolean, default true) - Add text shadow
      responses:
        '200':
          description: Processed image
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ToolResponse'
        '400':
          description: Invalid input
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Error'
        '401':
          description: Authentication required
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/UnauthorizedError'
  /api/v1/tools/image/replace-color:
    post:
      operationId: replaceColor
      tags:
        - Tools
      summary: 色の置換
      description: 画像内の特定の色を別の色または透明に置き換えます。
      security:
        - bearerAuth: []
      requestBody:
        required: true
        content:
          multipart/form-data:
            schema:
              type: object
              required:
                - file
              properties:
                file:
                  type: string
                  format: binary
                  description: Image file to process
                settings:
                  type: string
                  description: |
                    JSON string with options:
                    - `sourceColor` (hex string, default "#FF0000") - Color to replace
                    - `targetColor` (hex string, default "#00FF00") - Replacement color
                    - `makeTransparent` (boolean, default false) - Make matched pixels transparent instead
                    - `tolerance` (number 0-255, default 30) - Color matching tolerance
      responses:
        '200':
          description: Processed image
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ToolResponse'
        '400':
          description: Invalid input
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Error'
        '401':
          description: Authentication required
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/UnauthorizedError'
  /api/v1/tools/image/color-blindness:
    post:
      operationId: simulateColorBlindness
      tags:
        - Tools
      summary: 色覚多様性シミュレーション
      description: さまざまな種類の色覚特性を持つ人に画像がどう見えるかをシミュレートします。
      security:
        - bearerAuth: []
      requestBody:
        required: true
        content:
          multipart/form-data:
            schema:
              type: object
              required:
                - file
              properties:
                file:
                  type: string
                  format: binary
                  description: Image file to process
                settings:
                  type: string
                  description: |
                    JSON string with options:
                    - `simulationType` (enum, default "deuteranomaly") -- Type of color vision deficiency to simulate. One of: protanopia, deuteranopia, tritanopia, protanomaly, deuteranomaly, tritanomaly, achromatopsia, blueConeMonochromacy
      responses:
        '200':
          description: Processed image
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ToolResponse'
        '400':
          description: Invalid input
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Error'
        '401':
          description: Authentication required
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/UnauthorizedError'
  /api/v1/tools/image/gif-tools:
    post:
      operationId: processGif
      tags:
        - Tools
      summary: GIF ツール
      description: アニメーション GIF のリサイズ、最適化、速度調整、逆再生、フレーム抽出、回転を行います。
      security:
        - bearerAuth: []
      requestBody:
        required: true
        content:
          multipart/form-data:
            schema:
              type: object
              required:
                - file
              properties:
                file:
                  type: string
                  format: binary
                  description: GIF file to process
                settings:
                  type: string
                  description: |
                    JSON string with options:
                    - `mode` (string, default "resize") - One of: resize, optimize, speed, reverse, extract, rotate
                    - `width` (number 1-16384, optional) - Target width in pixels (resize mode)
                    - `height` (number 1-16384, optional) - Target height in pixels (resize mode)
                    - `percentage` (number 1-500, optional) - Scale by percentage (resize mode)
                    - `colors` (number 2-256, default 256) - Color palette size (optimize mode)
                    - `dither` (number 0-1, default 1.0) - Dither amount (optimize mode)
                    - `effort` (number 1-10, default 7) - Compression effort (optimize mode)
                    - `speedFactor` (number 0.1-10, default 1.0) - Speed multiplier (speed mode)
                    - `extractMode` (string, default "single") - One of: single, range, all (extract mode)
                    - `frameNumber` (number, default 0) - Frame index to extract (extract/single mode)
                    - `frameStart` (number, default 0) - Start frame index (extract/range mode)
                    - `frameEnd` (number, optional) - End frame index (extract/range mode)
                    - `extractFormat` (string, default "png") - One of: png, webp (extract mode)
                    - `angle` (number, optional) - Rotation angle, one of: 90, 180, 270 (rotate mode)
                    - `flipH` (boolean, default false) - Flip horizontally (rotate mode)
                    - `flipV` (boolean, default false) - Flip vertically (rotate mode)
                    - `loop` (number 0-100, default 0) - Loop count, 0 = infinite
      responses:
        '200':
          description: Processed image
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ToolResponse'
        '400':
          description: Invalid input
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Error'
        '401':
          description: Authentication required
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/UnauthorizedError'
  /api/v1/tools/image/gif-tools/info:
    post:
      operationId: getGifInfo
      tags:
        - Tools
      summary: GIF 情報
      description: 寸法、フレーム数、遅延、再生時間を含むアニメーション GIF のメタデータを取得します。
      security: []
      requestBody:
        required: true
        content:
          multipart/form-data:
            schema:
              type: object
              required:
                - file
              properties:
                file:
                  type: string
                  format: binary
                  description: GIF file to inspect
      responses:
        '200':
          description: GIF metadata
          content:
            application/json:
              schema:
                type: object
                properties:
                  width:
                    type: integer
                  height:
                    type: integer
                  pages:
                    type: integer
                    description: Number of frames
                  delay:
                    type: array
                    items:
                      type: integer
                    description: Per-frame delay in milliseconds
                  loop:
                    type: integer
                    description: Loop count (0 = infinite)
                  fileSize:
                    type: integer
                  duration:
                    type: integer
                    description: Total duration in milliseconds
        '400':
          description: Invalid input
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Error'
  /api/v1/tools/image/smart-crop:
    post:
      operationId: smartCrop
      tags:
        - Tools
      summary: スマート切り抜き
      description: 被写体フォーカス、顔フォーカス、自動トリミングの 3 モードを備えたスマート切り抜き。
      security:
        - bearerAuth: []
      requestBody:
        required: true
        content:
          multipart/form-data:
            schema:
              type: object
              required:
                - file
              properties:
                file:
                  type: string
                  format: binary
                  description: Image file to process
                settings:
                  type: string
                  description: |
                    JSON string with options:
                    - `mode` (string, default "subject") - One of: subject, face, trim, attention (alias for subject), content (alias for trim)
                    - `strategy` (string, default "attention") - One of: attention, entropy (subject mode)
                    - `width` (integer, optional) - Target width in pixels
                    - `height` (integer, optional) - Target height in pixels
                    - `padding` (integer 0-50, default 0) - Padding percentage around focus area
                    - `facePreset` (string, default "head-shoulders") - One of: closeup, head-shoulders, upper-body, half-body (face mode)
                    - `sensitivity` (number 0-1, default 0.5) - Face detection sensitivity (face mode)
                    - `threshold` (integer 0-255, default 30) - Trim tolerance (trim mode)
                    - `padToSquare` (boolean, default false) - Pad to square after trimming (trim mode)
                    - `padColor` (string, default "#ffffff") - Hex color for padding (trim mode)
                    - `targetSize` (integer, optional) - Target size for padded output (trim mode)
                    - `quality` (integer 1-100, optional) - Output quality
      responses:
        '200':
          description: Processed image
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ToolResponse'
        '400':
          description: Invalid input
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Error'
        '401':
          description: Authentication required
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/UnauthorizedError'
  /api/v1/tools/image/vectorize:
    post:
      operationId: vectorizeImage
      tags:
        - Tools
      summary: 画像から SVG
      description: potrace（白黒）または VTracer（カラー）を用いてラスター画像を SVG ベクター形式に変換します。
      security:
        - bearerAuth: []
      requestBody:
        required: true
        content:
          multipart/form-data:
            schema:
              type: object
              required:
                - file
              properties:
                file:
                  type: string
                  format: binary
                  description: Image file to process
                settings:
                  type: string
                  description: |
                    JSON string with options:
                    - `colorMode` (string, default "bw") - One of: bw, color
                    - `threshold` (number 0-255, default 128) - B&W binarization threshold
                    - `colorPrecision` (number 1-16, default 6) - Color bits per channel
                    - `layerDifference` (number 1-128, default 6) - Color gradient step
                    - `filterSpeckle` (number 1-256, default 4) - Noise filter size
                    - `pathMode` (string, default "spline") - One of: none, polygon, spline
                    - `cornerThreshold` (number 0-180, default 60) - Corner detection angle
                    - `invert` (boolean, default false) - Invert colors before tracing
      responses:
        '200':
          description: Processed image (downloadUrl points to .svg file)
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ToolResponse'
        '400':
          description: Invalid input
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Error'
        '401':
          description: Authentication required
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/UnauthorizedError'
  /api/v1/tools/image/svg-to-raster:
    post:
      operationId: svgToRaster
      tags:
        - Tools
      summary: SVG からラスター
      description: SVG ファイルを任意のスケールと DPI でラスター画像フォーマットに変換します。
      security:
        - bearerAuth: []
      requestBody:
        required: true
        content:
          multipart/form-data:
            schema:
              type: object
              required:
                - file
              properties:
                file:
                  type: string
                  format: binary
                  description: SVG file to convert
                settings:
                  type: string
                  description: |
                    JSON string with options:
                    - `width` (number 1-65536, optional) - Output width in pixels
                    - `height` (number 1-65536, optional) - Output height in pixels
                    - `dpi` (number 36-2400, default 300) - Render density for SVG rasterization
                    - `quality` (number 1-100, default 90) - Output quality for lossy formats
                    - `backgroundColor` (hex string, default "#00000000") - Background color
                    - `outputFormat` (string, default "png") - One of: png, jpg, webp, avif, tiff, gif, heif, jxl
      responses:
        '200':
          description: Processed image
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ToolResponse'
        '400':
          description: Invalid input
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Error'
        '401':
          description: Authentication required
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/UnauthorizedError'
  /api/v1/tools/image/svg-to-raster/batch:
    post:
      operationId: svgToRasterBatch
      tags:
        - Tools
      summary: SVG からラスター（バッチ）
      description: 複数の SVG ファイルをラスター画像に変換します。ZIP アーカイブを返します。
      security:
        - bearerAuth: []
      requestBody:
        required: true
        content:
          multipart/form-data:
            schema:
              type: object
              required:
                - file
              properties:
                file:
                  type: array
                  items:
                    type: string
                    format: binary
                  description: SVG files to convert
                settings:
                  type: string
                  description: Same settings as single-file endpoint
                clientJobId:
                  type: string
                  description: Optional client-generated job ID for progress tracking
      responses:
        '200':
          description: ZIP archive containing processed images
          content:
            application/zip:
              schema:
                type: string
                format: binary
        '400':
          description: Invalid input
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Error'
        '401':
          description: Authentication required
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/UnauthorizedError'
        '422':
          description: All files failed processing
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Error'
  /api/v1/tools/image/image-to-pdf:
    post:
      operationId: imageToPdf
      tags:
        - Tools
      summary: 画像から PDF
      description: 1 つ以上の画像を PDF ドキュメントに変換します。
      security:
        - bearerAuth: []
      requestBody:
        required: true
        content:
          multipart/form-data:
            schema:
              type: object
              required:
                - file
              properties:
                file:
                  type: array
                  items:
                    type: string
                    format: binary
                  description: One or more image files to include in the PDF
                settings:
                  type: string
                  description: |
                    JSON string with options:
                    - `pageSize` (string, default "A4") - One of: A4, Letter, A3, A5
                    - `orientation` (string, default "portrait") - One of: portrait, landscape
                    - `margin` (number 0-500, default 20) - Page margin in points
                    - `targetSize` (object, optional) - Target output file size
                      - `value` (number, required) - Positive number (decimals allowed)
                      - `unit` (string, required) - One of: KB, MB
                      Minimum effective target is 50KB. When set, images are compressed as JPEG with quality auto-tuned to meet the target.
                    - `collate` (boolean, default true) - When true, all images go into one PDF. When false, each image becomes a separate PDF and the result is a ZIP.
      responses:
        '200':
          description: Generated PDF (downloadUrl points to .pdf file)
          content:
            application/json:
              schema:
                allOf:
                  - $ref: '#/components/schemas/ToolResponse'
                  - type: object
                    properties:
                      pages:
                        type: integer
                        description: Number of images/pages in the PDF
                      compression:
                        type: object
                        description: Present only when targetSize was specified
                        properties:
                          targetRequested:
                            type: integer
                            description: Target size in bytes
                          targetMet:
                            type: boolean
                            description: Whether the target was achieved
                          jpegQuality:
                            type: integer
                            description: Final JPEG quality used (10-95)
        '400':
          description: Invalid input
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Error'
        '401':
          description: Authentication required
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/UnauthorizedError'
        '422':
          description: Processing failed
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Error'
  /api/v1/tools/pdf/pdf-to-image:
    post:
      operationId: pdfToImage
      tags:
        - Tools
      summary: PDF から画像
      description: PDF のページを画像に変換します。各ページの個別ダウンロードと全ページの ZIP を返します。
      security:
        - bearerAuth: []
      requestBody:
        required: true
        content:
          multipart/form-data:
            schema:
              type: object
              required:
                - file
              properties:
                file:
                  type: string
                  format: binary
                  description: PDF file to convert
                settings:
                  type: string
                  description: |
                    JSON string with options:
                    - `format` (string, default "png") - Output format: png, jpg, webp, avif, tiff, gif, heic, heif, jxl
                    - `dpi` (number 36-2400, default 150) - Resolution in dots per inch
                    - `quality` (number 1-100, default 85) - Output quality
                    - `colorMode` (string, default "color") - One of: color, grayscale, bw
                    - `pages` (string, default "all") - Page selection, e.g. "all", "1-3", "1,3,5"
      responses:
        '200':
          description: Converted pages
          content:
            application/json:
              schema:
                type: object
                properties:
                  jobId:
                    type: string
                  pageCount:
                    type: integer
                    description: Total pages in the PDF
                  selectedPages:
                    type: array
                    items:
                      type: integer
                  format:
                    type: string
                  pages:
                    type: array
                    items:
                      type: object
                      properties:
                        page:
                          type: integer
                        downloadUrl:
                          type: string
                        size:
                          type: integer
                  zipUrl:
                    type: string
                    description: URL to download all pages as ZIP
                  zipSize:
                    type: integer
        '400':
          description: Invalid input
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Error'
        '401':
          description: Authentication required
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/UnauthorizedError'
  /api/v1/tools/pdf/pdf-to-image/info:
    post:
      operationId: getPdfInfo
      tags:
        - Tools
      summary: PDF ページ数
      description: PDF ファイルのページ数を取得します。
      security: []
      requestBody:
        required: true
        content:
          multipart/form-data:
            schema:
              type: object
              required:
                - file
              properties:
                file:
                  type: string
                  format: binary
                  description: PDF file to inspect
      responses:
        '200':
          description: PDF info
          content:
            application/json:
              schema:
                type: object
                properties:
                  pageCount:
                    type: integer
        '400':
          description: Invalid input
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Error'
  /api/v1/tools/pdf/pdf-to-image/preview:
    post:
      operationId: previewPdfPages
      tags:
        - Tools
      summary: PDF ページのサムネイル
      description: PDF の各ページのサムネイルプレビューを生成します（最大 200 ページ、幅 300px の JPEG）。
      security: []
      requestBody:
        required: true
        content:
          multipart/form-data:
            schema:
              type: object
              required:
                - file
              properties:
                file:
                  type: string
                  format: binary
                  description: PDF file to preview
      responses:
        '200':
          description: Page thumbnails
          content:
            application/json:
              schema:
                type: object
                properties:
                  pageCount:
                    type: integer
                  thumbnails:
                    type: array
                    items:
                      type: object
                      properties:
                        page:
                          type: integer
                        dataUrl:
                          type: string
                          description: Base64-encoded JPEG data URL
                        width:
                          type: integer
                        height:
                          type: integer
        '400':
          description: Invalid input
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Error'
  /api/v1/tools/image/split:
    post:
      operationId: splitImage
      tags:
        - Tools
      summary: 画像の分割
      description: 画像をタイルのグリッドに分割します。ZIP ファイルを返します。
      security:
        - bearerAuth: []
      requestBody:
        required: true
        content:
          multipart/form-data:
            schema:
              type: object
              required:
                - file
              properties:
                file:
                  type: string
                  format: binary
                  description: Image file to split
                settings:
                  type: string
                  description: |
                    JSON string with options:
                    - `columns` (number 1-100, default 3) - Number of columns
                    - `rows` (number 1-100, default 3) - Number of rows
                    - `tileWidth` (number, min 10, optional) - Fixed tile width in pixels
                    - `tileHeight` (number, min 10, optional) - Fixed tile height in pixels
                    - `outputFormat` (string, default "original") - One of: original, png, jpg, webp, avif, jxl
                    - `quality` (number 1-100, default 90) - Output quality
      responses:
        '200':
          description: ZIP archive with results
          content:
            application/zip:
              schema:
                type: string
                format: binary
        '400':
          description: Invalid input
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Error'
        '401':
          description: Authentication required
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/UnauthorizedError'
  /api/v1/tools/image/bulk-rename:
    post:
      operationId: bulkRename
      tags:
        - Tools
      summary: 一括リネーム
      description: パターンテンプレートを用いて複数の画像をリネームします。{{index}}、{{padded}}、{{original}} のトークンに対応します。ZIP ファイルを返します。
      security:
        - bearerAuth: []
      requestBody:
        required: true
        content:
          multipart/form-data:
            schema:
              type: object
              required:
                - file
              properties:
                file:
                  type: array
                  items:
                    type: string
                    format: binary
                  description: Multiple image files to rename
                settings:
                  type: string
                  description: |
                    JSON string with options:
                    - `pattern` (string 1-1000, default "image-{{index}}") - Naming pattern template
                    - `startIndex` (number, default 1) - Starting index number
      responses:
        '200':
          description: ZIP archive with results
          content:
            application/zip:
              schema:
                type: string
                format: binary
        '400':
          description: Invalid input
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Error'
        '401':
          description: Authentication required
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/UnauthorizedError'
  /api/v1/tools/image/favicon:
    post:
      operationId: generateFavicon
      tags:
        - Tools
      summary: ファビコン生成
      description: 画像から完全なファビコンセット（16px から 512px の PNG、ICO、manifest.json）を生成します。ZIP ファイルを返します。
      security:
        - bearerAuth: []
      requestBody:
        required: true
        content:
          multipart/form-data:
            schema:
              type: object
              required:
                - file
              properties:
                file:
                  type: string
                  format: binary
                  description: Image file to use as favicon source
      responses:
        '200':
          description: ZIP archive with results
          content:
            application/zip:
              schema:
                type: string
                format: binary
        '400':
          description: Invalid input
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Error'
        '401':
          description: Authentication required
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/UnauthorizedError'
  /api/v1/tools/image/watermark-image:
    post:
      operationId: addImageWatermark
      tags:
        - Tools
      summary: 画像透かし
      description: 画像の透かしオーバーレイを追加します。
      security:
        - bearerAuth: []
      requestBody:
        required: true
        content:
          multipart/form-data:
            schema:
              type: object
              required:
                - file
                - watermark
              properties:
                file:
                  type: string
                  format: binary
                  description: Main image file
                watermark:
                  type: string
                  format: binary
                  description: Watermark overlay image
                settings:
                  type: string
                  description: |
                    JSON string with options:
                    - `position` (string, default "bottom-right") - One of: center, top-left, top-right, bottom-left, bottom-right
                    - `opacity` (number 0-100, default 50) - Watermark opacity percentage
                    - `scale` (number 1-100, default 25) - Watermark size as percentage of base image
      responses:
        '200':
          description: Processed image
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ToolResponse'
        '400':
          description: Invalid input
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Error'
        '401':
          description: Authentication required
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/UnauthorizedError'
  /api/v1/tools/image/compose:
    post:
      operationId: composeImages
      tags:
        - Tools
      summary: 画像の合成
      description: ブレンドモードでオーバーレイ画像をベース画像に合成します。
      security:
        - bearerAuth: []
      requestBody:
        required: true
        content:
          multipart/form-data:
            schema:
              type: object
              required:
                - file
                - overlay
              properties:
                file:
                  type: string
                  format: binary
                  description: Base image file
                overlay:
                  type: string
                  format: binary
                  description: Overlay image file
                settings:
                  type: string
                  description: |
                    JSON string with options:
                    - `x` (number, default 0) - Horizontal offset of overlay
                    - `y` (number, default 0) - Vertical offset of overlay
                    - `opacity` (number 0-100, default 100) - Overlay opacity percentage
                    - `blendMode` (string, default "over") - One of: over, multiply, screen, overlay, darken, lighten, hard-light, soft-light, difference, exclusion
      responses:
        '200':
          description: Processed image
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ToolResponse'
        '400':
          description: Invalid input
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Error'
        '401':
          description: Authentication required
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/UnauthorizedError'
  /api/v1/tools/image/meme-generator:
    post:
      operationId: generateMeme
      tags:
        - Tools
      summary: ミーム生成
      description: |
        テンプレートまたはカスタム画像を用いてミームを作成します。2 つのモードに対応します。(1) テンプレートモード - `templateId` と `textBoxes` を含む JSON ボディを送信します。(2) カスタム画像モード - `file` と `settings` を含む multipart を送信します。
      security:
        - bearerAuth: []
      requestBody:
        required: true
        content:
          multipart/form-data:
            schema:
              type: object
              properties:
                file:
                  type: string
                  format: binary
                  description: Custom image file (optional if using templateId)
                settings:
                  type: string
                  description: |
                    JSON string with options:
                    - `templateId` (string, optional) - ID of a meme template from /api/v1/meme-templates
                    - `textLayout` (string, default "top-bottom") - One of: top-bottom, top-only, bottom-only, center, side-by-side
                    - `textBoxes` (array of {id, text}) - Text content for each position
                    - `fontFamily` (string, default "anton") - One of: anton, arial-black, comic-sans, montserrat, bebas-neue, permanent-marker, roboto
                    - `fontSize` (number 8-200, optional) - Font size in pixels
                    - `textColor` (hex string, default "#ffffff") - Text color
                    - `strokeColor` (hex string, default "#000000") - Text outline color
                    - `textAlign` (string, default "center") - One of: left, center, right
                    - `allCaps` (boolean, default true) - Convert text to uppercase
          application/json:
            schema:
              type: object
              description: Template mode - send templateId and textBoxes as JSON body
              properties:
                templateId:
                  type: string
                  description: ID of a meme template
                textBoxes:
                  type: array
                  items:
                    type: object
                    properties:
                      id:
                        type: string
                      text:
                        type: string
      responses:
        '200':
          description: Processed meme image
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ToolResponse'
        '400':
          description: Invalid input or template not found
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Error'
        '401':
          description: Authentication required
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/UnauthorizedError'
        '422':
          description: Processing failed
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Error'
  /api/v1/meme-templates:
    get:
      operationId: listMemeTemplates
      tags:
        - Tools
      summary: ミームテンプレートの一覧
      description: テキストボックスの位置を含む、利用可能なミームテンプレートの完全なマニフェストを返します。
      security:
        - bearerAuth: []
      responses:
        '200':
          description: Meme template manifest
          content:
            application/json:
              schema:
                type: object
                properties:
                  templates:
                    type: array
                    items:
                      type: object
                      properties:
                        id:
                          type: string
                        filename:
                          type: string
                        width:
                          type: integer
                        height:
                          type: integer
                        textBoxes:
                          type: array
                          items:
                            type: object
                            properties:
                              id:
                                type: string
                              x:
                                type: number
                              'y':
                                type: number
                              width:
                                type: number
                              height:
                                type: number
                              defaultText:
                                type: string
        '401':
          description: Authentication required
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/UnauthorizedError'
  /api/v1/meme-templates/full/{filename}:
    get:
      operationId: getMemeTemplateImage
      tags:
        - Tools
      summary: ミームテンプレート画像の取得
      description: フルサイズのミームテンプレート画像を配信します。
      security:
        - bearerAuth: []
      parameters:
        - name: filename
          in: path
          required: true
          schema:
            type: string
          description: Template image filename
      responses:
        '200':
          description: Template image
          content:
            image/*:
              schema:
                type: string
                format: binary
        '400':
          description: Invalid filename
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Error'
        '404':
          description: Template not found
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Error'
  /api/v1/meme-templates/thumbs/{filename}:
    get:
      operationId: getMemeTemplateThumbnail
      tags:
        - Tools
      summary: ミームテンプレートのサムネイル取得
      description: ミームテンプレートのサムネイルを配信します。
      security:
        - bearerAuth: []
      parameters:
        - name: filename
          in: path
          required: true
          schema:
            type: string
          description: Template thumbnail filename
      responses:
        '200':
          description: Thumbnail image
          content:
            image/*:
              schema:
                type: string
                format: binary
        '400':
          description: Invalid filename
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Error'
        '404':
          description: Thumbnail not found
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Error'
  /api/v1/meme-templates/fonts/{filename}:
    get:
      operationId: getMemeTemplateFont
      tags:
        - Tools
      summary: ミームテンプレートフォントの取得
      description: ミーム生成で使用されるフォントファイルを配信します。
      security:
        - bearerAuth: []
      parameters:
        - name: filename
          in: path
          required: true
          schema:
            type: string
          description: Font filename (e.g. anton.ttf)
      responses:
        '200':
          description: Font file
          content:
            font/ttf:
              schema:
                type: string
                format: binary
        '400':
          description: Invalid filename
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Error'
        '404':
          description: Font not found
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Error'
  /api/v1/tools/image/compare:
    post:
      operationId: compareImages
      tags:
        - Tools
      summary: 画像の比較
      description: 2 つの画像を比較し、視覚的な差分レポートを生成します。
      security:
        - bearerAuth: []
      requestBody:
        required: true
        content:
          multipart/form-data:
            schema:
              type: object
              required:
                - file1
                - file2
              properties:
                file1:
                  type: string
                  format: binary
                  description: First image to compare
                file2:
                  type: string
                  format: binary
                  description: Second image to compare
      responses:
        '200':
          description: Comparison result
          content:
            application/json:
              schema:
                allOf:
                  - $ref: '#/components/schemas/ToolResponse'
                  - type: object
                    properties:
                      similarity:
                        type: number
                        description: Similarity percentage (0-100)
                      dimensions:
                        type: object
                        properties:
                          width:
                            type: integer
                          height:
                            type: integer
        '400':
          description: Invalid input
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Error'
        '401':
          description: Authentication required
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/UnauthorizedError'
        '422':
          description: Processing failed
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Error'
  /api/v1/tools/image/erase-object:
    post:
      operationId: eraseObject
      tags:
        - Tools
      summary: オブジェクト消去
      description: マスクを用いて画像からオブジェクトを消去します。マスクの白い領域が消去する範囲を示します。LaMa インペインティングを使用します。
      security:
        - bearerAuth: []
      requestBody:
        required: true
        content:
          multipart/form-data:
            schema:
              type: object
              required:
                - image
                - mask
              properties:
                image:
                  type: string
                  format: binary
                  description: Source image file
                mask:
                  type: string
                  format: binary
                  description: Mask image (white areas will be erased)
                format:
                  type: string
                  enum:
                    - auto
                    - png
                    - jpg
                    - jpeg
                    - webp
                    - tiff
                    - gif
                    - avif
                    - heic
                    - heif
                    - jxl
                  default: auto
                  description: Output format
                quality:
                  type: integer
                  minimum: 1
                  maximum: 100
                  default: 95
                  description: Output quality
                clientJobId:
                  type: string
                  description: Client-provided job ID for SSE progress tracking
      responses:
        '202':
          description: Accepted for async processing. Track progress via SSE at /api/v1/jobs/{jobId}/progress.
          content:
            application/json:
              schema:
                type: object
                properties:
                  jobId:
                    type: string
                  async:
                    type: boolean
                    example: true
        '400':
          description: Invalid input
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Error'
        '401':
          description: Authentication required
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/UnauthorizedError'
        '422':
          description: Processing failed
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Error'
        '501':
          description: Feature not installed
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/FeatureNotInstalledError'
  /api/v1/tools/image/ai-canvas-expand:
    post:
      operationId: aiCanvasExpand
      tags:
        - Tools
      summary: AI キャンバス拡張（実験的）
      description: |
        AI アウトペインティングを用いて画像キャンバスを拡張します。任意の方向に画像を延長し、元の画像となめらかに馴染む新しいコンテンツを生成します。LaMa ベースのアウトペインティングを使用します。ai-canvas-expand 機能バンドルのインストールが必要です。
      security:
        - bearerAuth: []
      requestBody:
        required: true
        content:
          multipart/form-data:
            schema:
              type: object
              required:
                - file
              properties:
                file:
                  type: string
                  format: binary
                  description: Image file to expand
                settings:
                  type: string
                  description: |
                    JSON string with options:
                    - `extendTop` (integer >= 0, default 0) - Pixels to extend upward
                    - `extendRight` (integer >= 0, default 0) - Pixels to extend rightward
                    - `extendBottom` (integer >= 0, default 0) - Pixels to extend downward
                    - `extendLeft` (integer >= 0, default 0) - Pixels to extend leftward
                    - `tier` (string, default "balanced") - One of: fast, balanced, high
                    - `format` (string, default "auto") - One of: auto, png, jpg, jpeg, webp, tiff, gif, avif, heic, heif, jxl
                    - `quality` (integer 1-100, default 95) - Output quality

                    At least one extend direction must be greater than 0.
                clientJobId:
                  type: string
                  description: Client-provided job ID for SSE progress tracking
      responses:
        '202':
          description: Accepted for async processing. Track progress via SSE at /api/v1/jobs/{jobId}/progress.
          content:
            application/json:
              schema:
                type: object
                properties:
                  jobId:
                    type: string
                  async:
                    type: boolean
                    example: true
        '400':
          description: Invalid input (or no extend direction > 0)
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Error'
        '401':
          description: Authentication required
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/UnauthorizedError'
        '422':
          description: Processing failed
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Error'
        '501':
          description: Feature not installed
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/FeatureNotInstalledError'
  /api/v1/tools/image/collage:
    post:
      operationId: createCollage
      tags:
        - Tools
      summary: コラージュ / グリッド
      description: 複数の画像をグリッドコラージュにまとめます。
      security:
        - bearerAuth: []
      requestBody:
        required: true
        content:
          multipart/form-data:
            schema:
              type: object
              required:
                - file
              properties:
                file:
                  type: array
                  items:
                    type: string
                    format: binary
                  description: Multiple image files to combine
                settings:
                  type: string
                  description: |
                    JSON string with options:
                    - `templateId` (string, required) - Template ID defining the grid layout
                    - `cells` (array, optional) - Per-cell configuration overrides
                    - `gap` (number 0-500, default 8) - Gap between images in pixels
                    - `cornerRadius` (number 0-500, default 0) - Corner rounding radius
                    - `backgroundColor` (string, default "#FFFFFF") - Background fill color
                    - `aspectRatio` (string, default "free") - Aspect ratio constraint
                    - `outputFormat` (string, default "png") - One of: png, jpeg, webp, avif, jxl
                    - `quality` (number 1-100, default 90) - Output quality
      responses:
        '200':
          description: Processed image
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ToolResponse'
        '400':
          description: Invalid input
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Error'
        '401':
          description: Authentication required
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/UnauthorizedError'
  /api/v1/tools/image/stitch:
    post:
      operationId: stitchImages
      tags:
        - Tools
      summary: 画像の連結
      description: 複数の画像を水平方向、垂直方向、またはグリッドレイアウトで連結します。
      security:
        - bearerAuth: []
      requestBody:
        required: true
        content:
          multipart/form-data:
            schema:
              type: object
              required:
                - file
              properties:
                file:
                  type: array
                  items:
                    type: string
                    format: binary
                  description: Two or more image files to stitch together
                settings:
                  type: string
                  description: |
                    JSON string with options:
                    - `direction` (string, default "horizontal") - One of: horizontal, vertical, grid
                    - `gridColumns` (integer 2-100, default 2) - Columns when direction is grid
                    - `resizeMode` (string, default "fit") - One of: fit, original, stretch, crop
                    - `alignment` (string, default "center") - One of: start, center, end
                    - `gap` (number 0-1000, default 0) - Gap between images in pixels
                    - `border` (number 0-500, default 0) - Border width in pixels
                    - `cornerRadius` (number 0-500, default 0) - Corner radius in pixels
                    - `backgroundColor` (hex string, default "#FFFFFF") - Hex color for background and gap fill
                    - `format` (string, default "png") - One of: png, jpeg, webp, avif, jxl
                    - `quality` (number 1-100, default 90) - Output quality
      responses:
        '200':
          description: Stitched image
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ToolResponse'
        '400':
          description: Invalid input
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Error'
        '401':
          description: Authentication required
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/UnauthorizedError'
  /api/v1/tools/image/remove-gif-background:
    post:
      operationId: removeGifBackground
      tags:
        - Tools
      summary: アニメーション画像から背景を削除する
      description: AI (rembg) を使用して、アニメーション化された GIF、WebP、または APNG からフレームごとに背景を削除します。ローカルで実行されます。
      security:
        - bearerAuth: []
      requestBody:
        required: true
        content:
          multipart/form-data:
            schema:
              type: object
              required:
                - file
              properties:
                file:
                  type: string
                  format: binary
                  description: Animated image file (GIF, animated WebP, or APNG)
                settings:
                  type: string
                  description: |
                    JSON string with options:
                    - `model` (string, optional) - AI model name
                    - `outputFormat` (string, optional) - One of: webp, apng, gif
                    - `backgroundType` (string, optional) - One of: transparent, color, gradient, blur, image
                    - `backgroundColor` (string, optional) - Hex color for the color background
                    - `gradientColor1` (string, optional) - First gradient color
                    - `gradientColor2` (string, optional) - Second gradient color
                    - `gradientAngle` (number, optional) - Gradient angle in degrees
                    - `blurIntensity` (number 0-100, optional) - Blur strength for the blur background
                    - `shadowEnabled` (boolean, optional) - Enable drop shadow
                    - `shadowOpacity` (number 0-100, optional) - Shadow opacity
                    - `edgeRefine` (number 0-3, optional) - Edge refinement level
                    - `decontaminate` (boolean, optional) - Remove background color spill
                backgroundImage:
                  type: string
                  format: binary
                  description: Background image, required when backgroundType is "image"
                clientJobId:
                  type: string
                  description: Client-provided job ID for SSE progress tracking
      responses:
        '202':
          description: Accepted for async processing. Track progress via SSE at /api/v1/jobs/{jobId}/progress.
          content:
            application/json:
              schema:
                type: object
                properties:
                  jobId:
                    type: string
                  async:
                    type: boolean
                    example: true
        '400':
          description: Invalid input (not animated, over the frame cap, or bad settings)
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Error'
        '401':
          description: Authentication required
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/UnauthorizedError'
        '501':
          description: Feature not installed
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/FeatureNotInstalledError'
  /api/v1/tools/image/remove-background:
    post:
      operationId: removeBackground
      tags:
        - Tools
      summary: 背景の除去
      description: AI（rembg）を用いて画像から背景を除去します。ローカルで実行されます。
      security:
        - bearerAuth: []
      requestBody:
        required: true
        content:
          multipart/form-data:
            schema:
              type: object
              required:
                - file
              properties:
                file:
                  type: string
                  format: binary
                  description: Image file to process
                settings:
                  type: string
                  description: |
                    JSON string with options:
                    - `model` (string, optional) - AI model name
                    - `backgroundType` (string, optional) - One of: transparent, color, gradient, blur, image
                    - `backgroundColor` (string, optional) - Hex color to replace removed background with
                    - `gradientColor1` (string, optional) - First gradient color
                    - `gradientColor2` (string, optional) - Second gradient color
                    - `gradientAngle` (number, optional) - Gradient angle in degrees
                    - `blurEnabled` (boolean, optional) - Enable background blur effect
                    - `blurIntensity` (number 0-100, optional) - Blur strength
                    - `shadowEnabled` (boolean, optional) - Enable drop shadow
                    - `shadowOpacity` (number 0-100, optional) - Shadow opacity
                clientJobId:
                  type: string
                  description: Client-provided job ID for SSE progress tracking
      responses:
        '202':
          description: Accepted for async processing. Track progress via SSE at /api/v1/jobs/{jobId}/progress.
          content:
            application/json:
              schema:
                type: object
                properties:
                  jobId:
                    type: string
                  async:
                    type: boolean
                    example: true
        '400':
          description: Invalid input
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Error'
        '401':
          description: Authentication required
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/UnauthorizedError'
        '422':
          description: Processing failed
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Error'
        '501':
          description: Feature not installed
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/FeatureNotInstalledError'
  /api/v1/tools/image/remove-background/effects:
    post:
      operationId: applyBackgroundEffects
      tags:
        - Tools
      summary: 背景エフェクトの適用
      description: |
        以前に処理した remove-background の結果に、背景の置換またはエフェクトを適用します。事前の POST /api/v1/tools/image/remove-background 呼び出しで得た jobId が必要です。
      security:
        - bearerAuth: []
      requestBody:
        required: true
        content:
          multipart/form-data:
            schema:
              type: object
              required:
                - settings
              properties:
                backgroundImage:
                  type: string
                  format: binary
                  description: Custom background image (required when backgroundType is "image")
                settings:
                  type: string
                  description: |
                    JSON string with options:
                    - `jobId` (string, required) - Job ID from the initial remove-background call
                    - `filename` (string, required) - Original filename
                    - `backgroundType` (string) - One of: transparent, color, gradient, blur, image
                    - `backgroundColor` (string) - Hex color for solid background
                    - `gradientColor1` (string) - First gradient color
                    - `gradientColor2` (string) - Second gradient color
                    - `gradientAngle` (number) - Gradient angle in degrees
                    - `blurEnabled` (boolean) - Enable background blur effect
                    - `blurIntensity` (number 0-100) - Blur strength
                    - `shadowEnabled` (boolean) - Enable drop shadow
                    - `shadowOpacity` (number 0-100) - Shadow opacity
      responses:
        '200':
          description: Image with applied effects
          content:
            application/json:
              schema:
                type: object
                properties:
                  jobId:
                    type: string
                  downloadUrl:
                    type: string
                  processedSize:
                    type: integer
        '400':
          description: Invalid input
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Error'
        '401':
          description: Authentication required
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/UnauthorizedError'
  /api/v1/tools/image/transparency-fixer:
    post:
      operationId: fixTransparency
      tags:
        - Tools
      summary: 偽の透明を修正
      description: |
        以前の背景除去で生じた縁取り、ハロー、半透明のアーティファクトがある「偽の透明」PNG を修正します。BiRefNet HR マッティング（2048x2048）を用いて、設定可能なデフリンジ処理で綺麗なアルファチャンネルを生成します。メモリ不足時は birefnet-general、次いで u2net にフォールバックします。background-removal 機能バンドルのインストールが必要です。
      security:
        - bearerAuth: []
      requestBody:
        required: true
        content:
          multipart/form-data:
            schema:
              type: object
              required:
                - file
              properties:
                file:
                  type: string
                  format: binary
                  description: PNG image with fake or damaged transparency
                settings:
                  type: string
                  description: |
                    JSON string with options:
                    - `defringe` (number 0-100, default 30) - Edge defringe strength to remove color contamination
                    - `outputFormat` (string, default "png") - One of: png, webp
                    - `removeWatermark` (boolean, default false) - Apply median filter to reduce watermark artifacts before matting
                clientJobId:
                  type: string
                  description: Client-provided job ID for SSE progress tracking
      responses:
        '202':
          description: Accepted for async processing. Track progress via SSE at /api/v1/jobs/{jobId}/progress.
          content:
            application/json:
              schema:
                type: object
                properties:
                  jobId:
                    type: string
                  async:
                    type: boolean
                    example: true
        '400':
          description: Invalid input
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Error'
        '401':
          description: Authentication required
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/UnauthorizedError'
        '422':
          description: Processing failed
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Error'
        '501':
          description: Feature not installed
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/FeatureNotInstalledError'
  /api/v1/tools/image/upscale:
    post:
      operationId: upscaleImage
      tags:
        - Tools
      summary: 画像の拡大
      description: AI（Real-ESRGAN）を用いて画像を拡大します。ローカルで実行されます。
      security:
        - bearerAuth: []
      requestBody:
        required: true
        content:
          multipart/form-data:
            schema:
              type: object
              required:
                - file
              properties:
                file:
                  type: string
                  format: binary
                  description: Image file to upscale
                settings:
                  type: string
                  description: |
                    JSON string with options:
                    - `scale` (number, default 2) - Upscale factor
                    - `model` (string, default "auto") - AI model name
                    - `faceEnhance` (boolean, default false) - Enable face enhancement
                    - `denoise` (number, default 0) - Denoise strength
                    - `format` (string, default "auto") - Output format (auto, png, jpg, jpeg, webp, tiff, gif, avif, heic, heif, jxl)
                    - `quality` (number, default 95) - Output quality (1-100)
                clientJobId:
                  type: string
                  description: Client-provided job ID for SSE progress tracking
      responses:
        '202':
          description: Accepted for async processing. Track progress via SSE at /api/v1/jobs/{jobId}/progress.
          content:
            application/json:
              schema:
                type: object
                properties:
                  jobId:
                    type: string
                  async:
                    type: boolean
                    example: true
        '400':
          description: Invalid input
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Error'
        '401':
          description: Authentication required
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/UnauthorizedError'
        '422':
          description: Processing failed
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Error'
        '501':
          description: Feature not installed
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/FeatureNotInstalledError'
  /api/v1/tools/image/blur-faces:
    post:
      operationId: blurFaces
      tags:
        - Tools
      summary: 顔のぼかし
      description: プライバシー保護のため画像内の顔を検出してぼかします。OpenCV を使用します。ローカルで実行されます。
      security:
        - bearerAuth: []
      requestBody:
        required: true
        content:
          multipart/form-data:
            schema:
              type: object
              required:
                - file
              properties:
                file:
                  type: string
                  format: binary
                  description: Image file to process
                settings:
                  type: string
                  description: |
                    JSON string with options:
                    - `blurRadius` (number 1-100, default 30) - Blur strength radius
                    - `sensitivity` (number 0-1, default 0.5) - Face detection sensitivity
                clientJobId:
                  type: string
                  description: Client-provided job ID for SSE progress tracking
      responses:
        '202':
          description: Accepted for async processing. Track progress via SSE at /api/v1/jobs/{jobId}/progress.
          content:
            application/json:
              schema:
                type: object
                properties:
                  jobId:
                    type: string
                  async:
                    type: boolean
                    example: true
        '400':
          description: Invalid input
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Error'
        '401':
          description: Authentication required
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/UnauthorizedError'
        '422':
          description: Processing failed
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Error'
        '501':
          description: Feature not installed
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/FeatureNotInstalledError'
  /api/v1/tools/image/ocr:
    post:
      operationId: extractText
      tags:
        - Tools
      summary: OCR / テキスト抽出
      description: |-
        画像からローカルでテキストを抽出します。組み込みの Fast 層は、
        Tesseract。 Balanced および Best は、オプションの署名付き RapidOCR ランタイムを使用します
        固定された PP-OCR ONNX モデルを使用します。明示的な階層リクエストがサイレントに行われることはありません
        別のティアにダウングレードします。
      security:
        - bearerAuth: []
      requestBody:
        required: true
        content:
          multipart/form-data:
            schema:
              type: object
              required:
                - file
              properties:
                file:
                  type: string
                  format: binary
                  maxLength: 536870912
                  description: Image file to extract text from
                settings:
                  type: string
                  description: |
                    JSON string with options:
                    - `quality` (string, optional) - One of: fast, balanced, best. When both quality and engine are omitted, uses the best available tier in this order: best, balanced, fast. Korean never selects fast; it uses best, then balanced, or returns an accurate-runtime install/compatibility error
                    - `language` (string, default "auto") - One of: auto, en, de, fr, es, zh, ja, ko. Korean is unsupported by fast and the legacy tesseract alias
                    - `enhance` (boolean, optional) - Improve local contrast. Fast applies it directly; accurate tiers retain the variant only when calibrated scoring improves the result. Defaults to true for Best and false for Fast/Balanced
                    - `engine` (string, deprecated) - Compatibility alias: tesseract maps to fast; the legacy paddleocr value maps to balanced but does not load PaddlePaddle
                clientJobId:
                  type: string
      responses:
        '202':
          description: Accepted after validation and enqueueing. Track progress via SSE at /api/v1/jobs/{jobId}/progress; processing failures arrive in the terminal failed event.
          content:
            application/json:
              schema:
                type: object
                required:
                  - jobId
                  - async
                properties:
                  jobId:
                    type: string
                  progressJobId:
                    type: string
                  artifactJobId:
                    type: string
                  async:
                    type: boolean
                    const: true
        '400':
          description: Invalid input
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Error'
        '401':
          description: Authentication required
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/UnauthorizedError'
        '413':
          description: Encoded OCR input exceeds the hard 512 MiB safety limit
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Error'
        '501':
          description: Requested Balanced/Best runtime is not installed or is incompatible
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/FeatureNotInstalledError'
        '503':
          description: OCR could not be queued
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Error'
  /api/v1/tools/image/info:
    post:
      operationId: getImageInfo
      tags:
        - Tools
      summary: 画像情報
      description: 寸法、フォーマット、色空間、EXIF データを含む画像の詳細なメタデータを取得します。
      security:
        - bearerAuth: []
      requestBody:
        required: true
        content:
          multipart/form-data:
            schema:
              type: object
              required:
                - file
              properties:
                file:
                  type: string
                  format: binary
                  description: Image file to inspect
      responses:
        '200':
          description: Image metadata
          content:
            application/json:
              schema:
                type: object
                properties:
                  width:
                    type: integer
                  height:
                    type: integer
                  filename:
                    type: string
                  format:
                    type: string
                  colorSpace:
                    type: string
                  channels:
                    type: integer
                  bitDepth:
                    type: string
                  density:
                    type:
                      - integer
                      - 'null'
                  hasAlpha:
                    type: boolean
                  fileSize:
                    type: integer
                  hasExif:
                    type: boolean
                  isProgressive:
                    type: boolean
                  orientation:
                    type:
                      - integer
                      - 'null'
                  hasProfile:
                    type: boolean
                  hasIcc:
                    type: boolean
                  hasXmp:
                    type: boolean
                  pages:
                    type: integer
                  histogram:
                    type: array
                    items:
                      type: object
                      properties:
                        channel:
                          type: string
                        min:
                          type: number
                        max:
                          type: number
                        mean:
                          type: number
                        stdev:
                          type: number
        '400':
          description: Invalid input
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Error'
        '401':
          description: Authentication required
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/UnauthorizedError'
        '422':
          description: Processing failed
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Error'
  /api/v1/tools/image/color-palette:
    post:
      operationId: extractColorPalette
      tags:
        - Tools
      summary: カラーパレット
      description: 画像から主要な色を抽出します。
      security:
        - bearerAuth: []
      requestBody:
        required: true
        content:
          multipart/form-data:
            schema:
              type: object
              required:
                - file
              properties:
                file:
                  type: string
                  format: binary
                  description: Image file to analyze
      responses:
        '200':
          description: Dominant colors
          content:
            application/json:
              schema:
                type: object
                properties:
                  filename:
                    type: string
                  colors:
                    type: array
                    items:
                      type: string
                    description: Hex color strings
                  count:
                    type: integer
        '400':
          description: Invalid input
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Error'
        '401':
          description: Authentication required
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/UnauthorizedError'
  /api/v1/tools/image/barcode-read:
    post:
      operationId: readBarcode
      tags:
        - Tools
      summary: バーコード読み取り
      description: 画像から QR コードとバーコードを読み取ります。
      security:
        - bearerAuth: []
      requestBody:
        required: true
        content:
          multipart/form-data:
            schema:
              type: object
              required:
                - file
              properties:
                file:
                  type: string
                  format: binary
                  description: Image file containing barcode or QR code
                settings:
                  type: string
                  description: |
                    JSON string with options:
                    - `tryHarder` (boolean, default true) - Enable more aggressive barcode detection
      responses:
        '200':
          description: Decoded barcode data
          content:
            application/json:
              schema:
                type: object
                properties:
                  filename:
                    type: string
                  barcodes:
                    type: array
                    items:
                      type: object
                      properties:
                        type:
                          type: string
                        text:
                          type: string
                        position:
                          type: object
                          properties:
                            topLeft:
                              type: object
                              properties:
                                x:
                                  type: number
                                'y':
                                  type: number
                            topRight:
                              type: object
                              properties:
                                x:
                                  type: number
                                'y':
                                  type: number
                            bottomLeft:
                              type: object
                              properties:
                                x:
                                  type: number
                                'y':
                                  type: number
                            bottomRight:
                              type: object
                              properties:
                                x:
                                  type: number
                                'y':
                                  type: number
                  annotatedUrl:
                    type:
                      - string
                      - 'null'
                  previewUrl:
                    type:
                      - string
                      - 'null'
        '400':
          description: Invalid input
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Error'
        '401':
          description: Authentication required
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/UnauthorizedError'
        '422':
          description: Processing failed
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Error'
  /api/v1/tools/image/find-duplicates:
    post:
      operationId: findDuplicates
      tags:
        - Tools
      summary: 重複の検出
      description: 知覚ハッシュを用いて画像セット内の重複画像を検出します。
      security:
        - bearerAuth: []
      requestBody:
        required: true
        content:
          multipart/form-data:
            schema:
              type: object
              required:
                - file
              properties:
                file:
                  type: array
                  items:
                    type: string
                    format: binary
                  description: Multiple image files to check for duplicates
                settings:
                  type: string
                  description: |
                    JSON string with options:
                    - `threshold` (number 0-20, default 8) - Hamming distance threshold for duplicate detection
      responses:
        '200':
          description: Duplicate groups
          content:
            application/json:
              schema:
                type: object
                properties:
                  totalImages:
                    type: integer
                  duplicateGroups:
                    type: array
                    items:
                      type: object
                      properties:
                        groupId:
                          type: integer
                        files:
                          type: array
                          items:
                            type: object
                            properties:
                              filename:
                                type: string
                              similarity:
                                type: number
                              width:
                                type: integer
                              height:
                                type: integer
                              fileSize:
                                type: integer
                              format:
                                type: string
                              isBest:
                                type: boolean
                              thumbnail:
                                type:
                                  - string
                                  - 'null'
                  uniqueImages:
                    type: integer
                  spaceSaveable:
                    type: integer
                  skippedFiles:
                    type: array
                    description: Files that could not be processed (present only when non-empty)
                    items:
                      type: object
                      properties:
                        filename:
                          type: string
                        reason:
                          type: string
        '400':
          description: Invalid input
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Error'
        '401':
          description: Authentication required
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/UnauthorizedError'
        '422':
          description: Processing failed
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Error'
  /api/v1/tools/image/qr-generate:
    post:
      operationId: generateQrCode
      tags:
        - Tools
      summary: QR コード生成
      description: テキストから QR コード画像を生成します。このツールは multipart ではなく JSON ボディを受け付けます。
      security:
        - bearerAuth: []
      requestBody:
        required: true
        content:
          application/json:
            schema:
              type: object
              required:
                - text
              properties:
                text:
                  type: string
                  maxLength: 2000
                  description: Text to encode in the QR code
                size:
                  type: integer
                  minimum: 100
                  maximum: 10000
                  default: 400
                  description: Image size in pixels
                errorCorrection:
                  type: string
                  enum:
                    - L
                    - M
                    - Q
                    - H
                  default: M
                foreground:
                  type: string
                  default: '#000000'
                  description: Foreground color (hex)
                background:
                  type: string
                  default: '#FFFFFF'
                  description: Background color (hex)
      responses:
        '200':
          description: Generated QR code
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ToolResponse'
        '400':
          description: Invalid input
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Error'
        '401':
          description: Authentication required
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/UnauthorizedError'
  /api/v1/tools/image/html-to-image:
    post:
      operationId: captureWebpage
      tags:
        - Tools
      summary: Web ページを画像として取得
      description: Web ページを高品質なスクリーンショット画像として取得します。URL と取得オプションを含む JSON ボディを受け付けます。
      security:
        - bearerAuth: []
      requestBody:
        required: true
        content:
          application/json:
            schema:
              type: object
              required:
                - url
              properties:
                url:
                  type: string
                  format: uri
                  description: URL to capture (http/https only)
                format:
                  type: string
                  enum:
                    - jpg
                    - png
                    - webp
                  default: png
                quality:
                  type: integer
                  minimum: 1
                  maximum: 100
                  default: 90
                fullPage:
                  type: boolean
                  default: false
                devicePreset:
                  type: string
                  enum:
                    - desktop
                    - tablet
                    - mobile
                    - custom
                  default: desktop
                viewportWidth:
                  type: integer
                  minimum: 320
                  maximum: 3840
                  default: 1280
                viewportHeight:
                  type: integer
                  minimum: 320
                  maximum: 2160
                  default: 720
      responses:
        '200':
          description: Screenshot captured successfully
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ToolResponse'
        '400':
          description: Invalid URL or parameters
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Error'
        '401':
          description: Authentication required
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/UnauthorizedError'
        '503':
          description: Browser service unavailable
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Error'
        '504':
          description: Page load timeout
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Error'
  /api/v1/tools/image/strip-metadata/inspect:
    post:
      operationId: inspectMetadata
      tags:
        - Tools
      summary: メタデータの確認
      description: 画像内のすべてのメタデータ（EXIF、GPS、ICC、XMP）を削除せずに表示します。
      security:
        - bearerAuth: []
      requestBody:
        required: true
        content:
          multipart/form-data:
            schema:
              type: object
              required:
                - file
              properties:
                file:
                  type: string
                  format: binary
                  description: Image file to inspect
      responses:
        '200':
          description: Image metadata
          content:
            application/json:
              schema:
                type: object
                properties:
                  filename:
                    type: string
                  fileSize:
                    type: integer
                  exif:
                    type: object
                    description: EXIF metadata
                  exifError:
                    type: string
                    description: Present when EXIF parsing fails
                  gps:
                    type: object
                    description: GPS metadata
                  icc:
                    type: object
                    description: ICC profile metadata
                  xmp:
                    type: object
                    description: XMP metadata
        '400':
          description: Invalid input
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Error'
        '401':
          description: Authentication required
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/UnauthorizedError'
        '422':
          description: Failed to read image metadata
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Error'
  /api/v1/tools/image/edit-metadata:
    post:
      operationId: editMetadata
      tags:
        - Tools
      summary: メタデータの編集
      description: ExifTool を用いて画像内の EXIF、IPTC、GPS などのメタデータフィールドを編集します。
      security:
        - bearerAuth: []
      requestBody:
        required: true
        content:
          multipart/form-data:
            schema:
              type: object
              required:
                - file
              properties:
                file:
                  type: string
                  format: binary
                  description: Image file to process
                settings:
                  type: string
                  description: |
                    JSON string with options:
                    - `title` (string) - Image title
                    - `author` (string) - Author name
                    - `artist` (string) - Artist or author name
                    - `copyright` (string) - Copyright notice
                    - `imageDescription` (string) - Image description
                    - `software` (string) - Software used
                    - `dateTime` (string) - Date/time string
                    - `dateTimeOriginal` (string) - Original capture date/time
                    - `clearGps` (boolean, default false) - Remove all GPS data
                    - `fieldsToRemove` (string[]) - Specific metadata fields to remove
                    - `gpsLatitude` (number -90 to 90) - GPS latitude
                    - `gpsLongitude` (number -180 to 180) - GPS longitude
                    - `gpsAltitude` (number) - GPS altitude in meters
                    - `keywords` (string[]) - Keywords or tags
                    - `keywordsMode` (string, default "add") - One of: add, set
                    - `dateShift` (string) - Shift dates by offset, e.g. "+2:00" or "-1:30"
                    - `setAllDates` (string) - Set all date fields to this value
                    - `iptcTitle` (string) - IPTC title
                    - `iptcHeadline` (string) - IPTC headline
                    - `iptcCity` (string) - IPTC city
                    - `iptcState` (string) - IPTC state or province
                    - `iptcCountry` (string) - IPTC country
      responses:
        '200':
          description: Image with updated metadata
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ToolResponse'
        '400':
          description: Invalid input
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Error'
        '401':
          description: Authentication required
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/UnauthorizedError'
  /api/v1/tools/image/edit-metadata/inspect:
    post:
      operationId: inspectMetadataExifTool
      tags:
        - Tools
      summary: メタデータの確認（ExifTool）
      description: ExifTool を用いて画像からすべてのメタデータを読み取ります。埋め込まれたすべての EXIF、IPTC、XMP、GPS フィールドを返します。
      security:
        - bearerAuth: []
      requestBody:
        required: true
        content:
          multipart/form-data:
            schema:
              type: object
              required:
                - file
              properties:
                file:
                  type: string
                  format: binary
                  description: Image file to inspect
      responses:
        '200':
          description: Full metadata object (keys vary by image)
          content:
            application/json:
              schema:
                type: object
                additionalProperties: true
        '400':
          description: Invalid input
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Error'
        '401':
          description: Authentication required
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/UnauthorizedError'
        '422':
          description: Failed to read image metadata
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Error'
  /api/v1/tools/{section}/{toolId}/batch:
    post:
      operationId: batchProcess
      tags:
        - Batch
      summary: 画像のバッチ処理
      description: |
        複数の画像を単一のツールで処理します。ZIP ファイルを返します。レスポンスには進捗追跡用の X-Job-Id ヘッダーが含まれます。
      security:
        - bearerAuth: []
      parameters:
        - name: section
          in: path
          required: true
          schema:
            type: string
          description: Tool section slug (image, video, audio, pdf, files)
        - name: toolId
          in: path
          required: true
          schema:
            type: string
          description: Tool identifier (e.g. resize, crop)
      requestBody:
        required: true
        content:
          multipart/form-data:
            schema:
              type: object
              required:
                - file
              properties:
                file:
                  type: array
                  items:
                    type: string
                    format: binary
                  description: Image files to process (one or more)
                settings:
                  type: string
                  description: JSON string of tool settings applied to all images
      responses:
        '200':
          description: ZIP archive of processed images
          headers:
            X-Job-Id:
              schema:
                type: string
              description: Job identifier for progress tracking
          content:
            application/zip:
              schema:
                type: string
                format: binary
        '400':
          description: Invalid input
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Error'
        '401':
          description: Authentication required
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/UnauthorizedError'
  /api/v1/pipeline/execute:
    post:
      operationId: executePipeline
      tags:
        - Pipelines
      summary: パイプラインの実行
      description: 画像をツールのチェーンに通します。パイプライン定義は multipart の `pipeline` フィールドに JSON 文字列として送信します。
      security:
        - bearerAuth: []
      requestBody:
        required: true
        content:
          multipart/form-data:
            schema:
              type: object
              required:
                - file
                - pipeline
              properties:
                file:
                  type: string
                  format: binary
                  description: Image file to process
                pipeline:
                  type: string
                  description: 'JSON string: { "steps": [{ "toolId": "resize", "settings": {...} }, ...] }'
      responses:
        '200':
          description: Pipeline result
          content:
            application/json:
              schema:
                type: object
                properties:
                  jobId:
                    type: string
                  downloadUrl:
                    type: string
                  originalSize:
                    type: integer
                  processedSize:
                    type: integer
                  stepsCompleted:
                    type: integer
                  steps:
                    type: array
                    items:
                      type: object
        '400':
          description: Invalid input
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Error'
        '401':
          description: Authentication required
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/UnauthorizedError'
        '422':
          description: Processing failed
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Error'
        '501':
          description: Feature not installed
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/FeatureNotInstalledError'
  /api/v1/pipeline/save:
    post:
      operationId: savePipeline
      tags:
        - Pipelines
      summary: パイプラインの保存
      description: 名前付きパイプライン定義をそのツールステップとともに保存し、後で実行できるようにします。再利用可能なワークフローの保存に使用します。
      security:
        - bearerAuth: []
      requestBody:
        required: true
        content:
          application/json:
            schema:
              type: object
              required:
                - name
                - steps
              properties:
                name:
                  type: string
                description:
                  type: string
                  description: Optional human-readable description of the pipeline
                steps:
                  type: array
                  items:
                    type: object
                    properties:
                      toolId:
                        type: string
                      settings:
                        type: object
      responses:
        '201':
          description: Pipeline saved
          content:
            application/json:
              schema:
                type: object
                properties:
                  id:
                    type: string
                  name:
                    type: string
                  description:
                    type: string
                    description: Human-readable description of the pipeline
                  steps:
                    type: array
                    items:
                      type: object
                  createdAt:
                    type: string
                    format: date-time
        '400':
          description: Invalid input
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Error'
        '401':
          description: Authentication required
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/UnauthorizedError'
  /api/v1/pipeline/list:
    get:
      operationId: listPipelines
      tags:
        - Pipelines
      summary: 保存済みパイプラインの一覧
      description: 現在のユーザーの保存済みパイプライン定義をすべて取得します。それぞれの名前、ステップ、作成日を返します。
      security:
        - bearerAuth: []
      responses:
        '200':
          description: List of pipelines
          content:
            application/json:
              schema:
                type: object
                properties:
                  pipelines:
                    type: array
                    items:
                      type: object
                      properties:
                        id:
                          type: string
                        name:
                          type: string
                        description:
                          type: string
                          description: Human-readable description of the pipeline
                        steps:
                          type: array
                          items:
                            type: object
                        createdAt:
                          type: string
                          format: date-time
        '400':
          description: Invalid input
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Error'
        '401':
          description: Authentication required
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/UnauthorizedError'
  /api/v1/pipeline/{id}:
    delete:
      operationId: deletePipeline
      tags:
        - Pipelines
      summary: パイプラインの削除
      description: 保存済みパイプラインを ID で完全に削除します。削除できるのはパイプラインの所有者または管理者のみです。
      security:
        - bearerAuth: []
      parameters:
        - name: id
          in: path
          required: true
          description: Pipeline UUID
          schema:
            type: string
      responses:
        '200':
          description: Pipeline deleted
          content:
            application/json:
              schema:
                type: object
                properties:
                  ok:
                    type: boolean
        '400':
          description: Invalid input
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Error'
        '401':
          description: Authentication required
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/UnauthorizedError'
        '403':
          description: Not authorized to delete this pipeline
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ForbiddenError'
        '404':
          description: Pipeline not found
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Error'
  /api/v1/pipeline/tools:
    get:
      operationId: listPipelineTools
      tags:
        - Pipelines
      summary: パイプライン対応ツールの一覧
      description: パイプラインステップとして使用できるすべてのツール ID を返します。
      security: []
      responses:
        '200':
          description: Tool IDs
          content:
            application/json:
              schema:
                type: object
                properties:
                  toolIds:
                    type: array
                    items:
                      type: string
  /api/v1/pipeline/batch:
    post:
      operationId: batchPipeline
      tags:
        - Pipelines
      summary: 複数ファイルでのパイプライン実行
      description: 複数の画像にわたってパイプラインを実行します。すべての処理結果を含む ZIP ファイルを返します。
      security:
        - bearerAuth: []
      requestBody:
        required: true
        content:
          multipart/form-data:
            schema:
              type: object
              required:
                - file
                - pipeline
              properties:
                file:
                  type: array
                  items:
                    type: string
                    format: binary
                  description: Image files to process
                pipeline:
                  type: string
                  description: |
                    JSON string with pipeline definition:
                    - `steps` (array, required, 1-20 items) - Pipeline steps
                    - `steps[].toolId` (string, required) - Tool ID for this step
                    - `steps[].settings` (object, optional) - Tool-specific settings
                clientJobId:
                  type: string
                  description: Optional client-provided job ID for progress tracking via SSE
      responses:
        '200':
          description: ZIP file with processed images
          headers:
            X-Job-Id:
              schema:
                type: string
              description: Job ID for progress tracking
            X-File-Results:
              schema:
                type: string
              description: Percent-encoded JSON mapping of input index to output filename (decode with decodeURIComponent before JSON.parse)
          content:
            application/zip:
              schema:
                type: string
                format: binary
        '400':
          description: Invalid input
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Error'
        '401':
          description: Authentication required
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/UnauthorizedError'
  /api/v1/upload:
    post:
      operationId: uploadImage
      tags:
        - Files
      summary: 画像のアップロード
      description: 後でツールエンドポイントで処理するために生の画像ファイルをアップロードします。jobId とファイルメタデータを返します。
      security:
        - bearerAuth: []
      requestBody:
        required: true
        content:
          multipart/form-data:
            schema:
              type: object
              required:
                - file
              properties:
                file:
                  type: string
                  format: binary
      responses:
        '200':
          description: Upload result
          content:
            application/json:
              schema:
                type: object
                properties:
                  jobId:
                    type: string
                  files:
                    type: array
                    items:
                      type: object
                      properties:
                        name:
                          type: string
                        size:
                          type: integer
                        format:
                          type: string
        '400':
          description: Invalid input
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Error'
        '401':
          description: Authentication required
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/UnauthorizedError'
  /api/v1/download/{jobId}/{filename}:
    get:
      operationId: downloadProcessedImage
      tags:
        - Files
      summary: 処理済み画像のダウンロード
      description: ジョブ ID とファイル名で処理済み画像をダウンロードします。これはツールのレスポンスの downloadUrl で返される URL です。
      security: []
      parameters:
        - name: jobId
          in: path
          required: true
          description: Job identifier from a tool processing response
          schema:
            type: string
        - name: filename
          in: path
          required: true
          description: Output filename from the processing response
          schema:
            type: string
      responses:
        '200':
          description: Image binary
          content:
            image/*:
              schema:
                type: string
                format: binary
        '404':
          description: File not found
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Error'
  /api/v1/preview:
    post:
      operationId: generatePreview
      tags:
        - Files
      summary: 画像プレビューの生成
      description: 任意の画像（HEIC/HEIF を含む）を WebP プレビューに変換し、1200x1200 ピクセル以内に収まるようリサイズします。
      security: []
      requestBody:
        required: true
        content:
          multipart/form-data:
            schema:
              type: object
              required:
                - file
              properties:
                file:
                  type: string
                  format: binary
                  description: Image file to preview
      responses:
        '200':
          description: WebP preview image
          content:
            image/webp:
              schema:
                type: string
                format: binary
        '400':
          description: Invalid input
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Error'
  /api/v1/files:
    get:
      operationId: listFiles
      tags:
        - Files
      summary: 保存済みファイルの一覧
      description: 各ファイルの最新バージョン（バージョンチェーンごとに 1 件）を返します。ページネーションと検索に対応します。
      security:
        - bearerAuth: []
      parameters:
        - name: limit
          in: query
          description: Maximum number of files to return (default 50)
          schema:
            type: integer
            default: 50
        - name: offset
          in: query
          description: Number of files to skip for pagination (default 0)
          schema:
            type: integer
            default: 0
        - name: search
          in: query
          description: Filter files by original filename (substring match)
          schema:
            type: string
      responses:
        '200':
          description: List of saved files
          content:
            application/json:
              schema:
                type: object
                properties:
                  files:
                    type: array
                    items:
                      type: object
                      properties:
                        id:
                          type: string
                        filename:
                          type: string
                        size:
                          type: integer
                        contentType:
                          type: string
                        createdAt:
                          type: string
                          format: date-time
                  total:
                    type: integer
                    description: Total number of matching files (for pagination)
                  limit:
                    type: integer
                    description: Applied limit value
                  offset:
                    type: integer
                    description: Applied offset value
        '400':
          description: Invalid input
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Error'
        '401':
          description: Authentication required
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/UnauthorizedError'
    delete:
      operationId: bulkDeleteFiles
      tags:
        - Files
      summary: 保存済みファイルの一括削除
      description: ファイルとそのバージョンチェーン全体を削除します。管理者以外のユーザーは自分のファイルのみ削除できます。
      security:
        - bearerAuth: []
      requestBody:
        required: true
        content:
          application/json:
            schema:
              type: object
              required:
                - ids
              properties:
                ids:
                  type: array
                  items:
                    type: string
                  description: File IDs to delete (deletes entire version chain for each)
      responses:
        '200':
          description: Deletion result
          content:
            application/json:
              schema:
                type: object
                properties:
                  deleted:
                    type: integer
                    description: Total number of records deleted across all version chains
        '400':
          description: Invalid input
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Error'
        '401':
          description: Authentication required
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/UnauthorizedError'
  /api/v1/files/upload:
    post:
      operationId: saveFileToLibrary
      tags:
        - Files
      summary: ライブラリへのファイル保存
      description: 永続的な保存とバージョン追跡のため、1 つ以上の画像ファイルをファイルライブラリに直接アップロードします。
      security:
        - bearerAuth: []
      requestBody:
        required: true
        content:
          multipart/form-data:
            schema:
              type: object
              required:
                - file
              properties:
                file:
                  type: string
                  format: binary
      responses:
        '201':
          description: Files uploaded
          content:
            application/json:
              schema:
                type: object
                properties:
                  files:
                    type: array
                    items:
                      type: object
                      description: Full file object with all metadata
        '400':
          description: Invalid input
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Error'
        '401':
          description: Authentication required
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/UnauthorizedError'
  /api/v1/files/save-result:
    post:
      operationId: saveProcessingResult
      tags:
        - Files
      summary: 処理結果をライブラリに保存
      description: ツールの処理結果を既存ファイルの新しいバージョンとして保存します。バージョンチェーンを作成します。
      security:
        - bearerAuth: []
      requestBody:
        required: true
        content:
          multipart/form-data:
            schema:
              type: object
              required:
                - file
                - parentId
              properties:
                file:
                  type: string
                  format: binary
                  description: The processed image file
                parentId:
                  type: string
                  description: ID of the parent file in the version chain
                toolId:
                  type: string
                  description: Tool that produced this result
      responses:
        '201':
          description: File saved
          content:
            application/json:
              schema:
                type: object
                properties:
                  file:
                    type: object
                    description: Full file object with all metadata
        '400':
          description: Invalid input
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Error'
        '401':
          description: Authentication required
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/UnauthorizedError'
        '404':
          description: Parent file not found
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Error'
  /api/v1/files/{id}:
    get:
      operationId: getFile
      tags:
        - Files
      summary: ファイルのメタデータとバージョン履歴の取得
      description: 保存済みファイルのメタデータと完全なバージョン履歴を取得します。ダウンロード前にファイルの詳細を確認するのに使用します。
      security:
        - bearerAuth: []
      parameters:
        - name: id
          in: path
          required: true
          description: Saved file UUID
          schema:
            type: string
      responses:
        '200':
          description: File metadata with version history
          content:
            application/json:
              schema:
                type: object
                properties:
                  file:
                    type: object
                    properties:
                      id:
                        type: string
                      originalName:
                        type: string
                      mimeType:
                        type: string
                      size:
                        type: integer
                      width:
                        type: integer
                      height:
                        type: integer
                      version:
                        type: integer
                      parentId:
                        type:
                          - string
                          - 'null'
                      toolChain:
                        type: array
                        items:
                          type: string
                      createdAt:
                        type: string
                        format: date-time
                  versions:
                    type: array
                    items:
                      type: object
                    description: Full version chain of this file
        '401':
          description: Authentication required
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/UnauthorizedError'
        '404':
          description: File not found
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Error'
  /api/v1/files/{id}/download:
    get:
      operationId: downloadSavedFile
      tags:
        - Files
      summary: 保存済みファイルのダウンロード
      description: ライブラリ内の保存済みファイルのバイナリコンテンツを ID でダウンロードします。
      security:
        - bearerAuth: []
      parameters:
        - name: id
          in: path
          required: true
          description: Saved file UUID
          schema:
            type: string
      responses:
        '200':
          description: Image binary
          content:
            image/*:
              schema:
                type: string
                format: binary
        '401':
          description: Authentication required
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/UnauthorizedError'
        '404':
          description: File not found
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Error'
  /api/v1/files/{id}/thumbnail:
    get:
      operationId: getFileThumbnail
      tags:
        - Files
      summary: ファイルサムネイルの取得
      description: 保存済みファイルの 300px JPEG サムネイルを取得します。ギャラリー表示やファイルブラウザーに便利です。
      security:
        - bearerAuth: []
      parameters:
        - name: id
          in: path
          required: true
          description: Saved file UUID
          schema:
            type: string
      responses:
        '200':
          description: 300px JPEG thumbnail
          content:
            image/jpeg:
              schema:
                type: string
                format: binary
        '404':
          description: File not found
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Error'
        '422':
          description: Thumbnail generation failed
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Error'
  /api/v1/fetch-urls:
    post:
      operationId: fetchImageUrls
      tags:
        - Files
      summary: URL から画像を取得
      description: |
        1 つ以上の外部 URL から画像を取得します。各 URL は SSRF 攻撃に対して検証され、サイズが制限され、有効な画像であることが確認されます。取得に成功した各画像のダウンロード URL を返します。レート制限は 1 時間あたり 200 リクエストです。
      security:
        - bearerAuth: []
      requestBody:
        required: true
        content:
          application/json:
            schema:
              type: object
              required:
                - urls
              properties:
                urls:
                  type: array
                  items:
                    type: string
                    format: uri
                  minItems: 1
                  maxItems: 50
                  description: URLs of images to fetch (1-50)
      responses:
        '200':
          description: Fetch results for each URL
          content:
            application/json:
              schema:
                type: object
                properties:
                  results:
                    type: array
                    items:
                      type: object
                      properties:
                        success:
                          type: boolean
                        url:
                          type: string
                        filename:
                          type: string
                        contentType:
                          type: string
                        size:
                          type: integer
                        width:
                          type: integer
                        height:
                          type: integer
                        downloadUrl:
                          type: string
                        previewUrl:
                          type:
                            - string
                            - 'null'
                        error:
                          type: string
                          description: Present only when success is false
        '400':
          description: Invalid input
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Error'
        '401':
          description: Authentication required
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/UnauthorizedError'
  /api/v1/config/locale:
    get:
      operationId: getLocaleConfig
      tags:
        - System
      summary: デフォルトロケールの取得
      description: デフォルトロケール設定を返します。パブリックエンドポイントです。
      security: []
      responses:
        '200':
          description: Default locale
          content:
            application/json:
              schema:
                type: object
                properties:
                  defaultLocale:
                    type: string
                    example: en
  /api/auth/login:
    post:
      operationId: login
      tags:
        - Auth
      summary: ログイン
      description: ユーザー名とパスワードで認証します。以降の API リクエスト用のベアラートークンを返します。
      security: []
      requestBody:
        required: true
        content:
          application/json:
            schema:
              type: object
              required:
                - username
                - password
              properties:
                username:
                  type: string
                password:
                  type: string
                  format: password
      responses:
        '200':
          description: Login successful
          content:
            application/json:
              schema:
                type: object
                properties:
                  token:
                    type: string
                  user:
                    type: object
                    properties:
                      id:
                        type: string
                      username:
                        type: string
                      role:
                        type: string
                      mustChangePassword:
                        type: boolean
                      permissions:
                        type: array
                        items:
                          type: string
                      teamName:
                        type: string
                  expiresAt:
                    type: string
                    format: date-time
        '400':
          description: Invalid input
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Error'
        '401':
          description: Invalid credentials
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/UnauthorizedError'
  /api/auth/logout:
    post:
      operationId: logout
      tags:
        - Auth
      summary: ログアウト
      description: 現在のセッショントークンを無効化します。ログアウト後、そのトークンは以降のリクエストに使用できません。
      security:
        - bearerAuth: []
      responses:
        '200':
          description: Logged out
          content:
            application/json:
              schema:
                type: object
                properties:
                  ok:
                    type: boolean
        '401':
          description: Authentication required
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/UnauthorizedError'
  /api/auth/session:
    get:
      operationId: getSession
      tags:
        - Auth
      summary: 現在のセッションの取得
      description: ユーザープロフィール、ロール、権限を含む現在のセッションの詳細を取得します。認証されていない場合は user が null で返されます。
      security: []
      responses:
        '200':
          description: Session info
          content:
            application/json:
              schema:
                type: object
                properties:
                  user:
                    type: object
                    properties:
                      id:
                        type: string
                      username:
                        type: string
                      role:
                        type: string
                      mustChangePassword:
                        type: boolean
                      permissions:
                        type: array
                        items:
                          type: string
                  expiresAt:
                    type:
                      - string
                      - 'null'
                    format: date-time
  /api/auth/change-password:
    post:
      operationId: changePassword
      tags:
        - Auth
      summary: パスワードの変更
      description: 認証済みユーザーのパスワードを変更します。確認のため現在のパスワードが必要です。
      security:
        - bearerAuth: []
      requestBody:
        required: true
        content:
          application/json:
            schema:
              type: object
              required:
                - currentPassword
                - newPassword
              properties:
                currentPassword:
                  type: string
                  format: password
                newPassword:
                  type: string
                  format: password
      responses:
        '200':
          description: Password changed
          content:
            application/json:
              schema:
                type: object
                properties:
                  ok:
                    type: boolean
        '400':
          description: Invalid input
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Error'
        '401':
          description: Authentication required
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/UnauthorizedError'
  /api/auth/users:
    get:
      operationId: listUsers
      tags:
        - Auth
      summary: ユーザーの一覧
      description: users:manage 権限が必要です。
      security:
        - bearerAuth: []
      responses:
        '200':
          description: List of users
          content:
            application/json:
              schema:
                type: object
                properties:
                  users:
                    type: array
                    items:
                      type: object
                      properties:
                        id:
                          type: string
                        username:
                          type: string
                        role:
                          type: string
                        team:
                          type: string
                        createdAt:
                          type: string
                          format: date-time
                  maxUsers:
                    type: integer
        '401':
          description: Authentication required
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/UnauthorizedError'
        '403':
          description: Insufficient permissions
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ForbiddenError'
  /api/auth/register:
    post:
      operationId: createUser
      tags:
        - Auth
      summary: ユーザーの作成
      description: users:manage 権限が必要です。
      security:
        - bearerAuth: []
      requestBody:
        required: true
        content:
          application/json:
            schema:
              type: object
              required:
                - username
                - password
              properties:
                username:
                  type: string
                password:
                  type: string
                  format: password
                role:
                  type: string
                  description: Built-in role (admin, editor, user) or custom role name
                  default: user
                team:
                  type: string
                  default: Default
      responses:
        '201':
          description: User created
          content:
            application/json:
              schema:
                type: object
                properties:
                  id:
                    type: string
                  username:
                    type: string
                  role:
                    type: string
                  team:
                    type: string
        '400':
          description: Invalid input
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Error'
        '401':
          description: Authentication required
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/UnauthorizedError'
        '403':
          description: Insufficient permissions
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ForbiddenError'
        '409':
          description: Username already exists
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ConflictError'
  /api/auth/users/{id}:
    put:
      operationId: updateUser
      tags:
        - Auth
      summary: ユーザーの更新
      description: users:manage 権限が必要です。
      security:
        - bearerAuth: []
      parameters:
        - name: id
          in: path
          required: true
          description: User UUID
          schema:
            type: string
      requestBody:
        required: true
        content:
          application/json:
            schema:
              type: object
              properties:
                role:
                  type: string
                  description: Built-in role (admin, editor, user) or custom role name
                team:
                  type: string
      responses:
        '200':
          description: User updated
          content:
            application/json:
              schema:
                type: object
                properties:
                  ok:
                    type: boolean
        '400':
          description: Invalid input
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Error'
        '401':
          description: Authentication required
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/UnauthorizedError'
        '403':
          description: Insufficient permissions
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ForbiddenError'
    delete:
      operationId: deleteUser
      tags:
        - Auth
      summary: ユーザーの削除
      description: users:manage 権限が必要です。
      security:
        - bearerAuth: []
      parameters:
        - name: id
          in: path
          required: true
          description: User UUID
          schema:
            type: string
      responses:
        '200':
          description: User deleted
          content:
            application/json:
              schema:
                type: object
                properties:
                  ok:
                    type: boolean
        '401':
          description: Authentication required
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/UnauthorizedError'
        '403':
          description: Insufficient permissions
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ForbiddenError'
  /api/auth/users/{id}/reset-password:
    post:
      operationId: resetUserPassword
      tags:
        - Auth
      summary: ユーザーパスワードのリセット
      description: users:manage 権限が必要です。
      security:
        - bearerAuth: []
      parameters:
        - name: id
          in: path
          required: true
          description: User UUID
          schema:
            type: string
      requestBody:
        required: true
        content:
          application/json:
            schema:
              type: object
              required:
                - newPassword
              properties:
                newPassword:
                  type: string
                  format: password
      responses:
        '200':
          description: Password reset
          content:
            application/json:
              schema:
                type: object
                properties:
                  ok:
                    type: boolean
        '400':
          description: Invalid input
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Error'
        '401':
          description: Authentication required
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/UnauthorizedError'
        '403':
          description: Insufficient permissions
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ForbiddenError'
  /api/v1/api-keys:
    post:
      operationId: createApiKey
      tags:
        - API Keys
      summary: API キーの作成
      description: |
        新しい API キーを生成します。完全なキー（si_ のプレフィックス付き）は 1 回だけ返されます。キーは scrypt ハッシュで保存されます。
      security:
        - bearerAuth: []
      requestBody:
        required: true
        content:
          application/json:
            schema:
              type: object
              properties:
                name:
                  type: string
                  maxLength: 100
                  default: Default API Key
                permissions:
                  type: array
                  items:
                    type: string
                  description: Scoped permissions for this key
                expiresAt:
                  type: string
                  format: date-time
                  description: Optional expiration date
      responses:
        '201':
          description: API key created
          content:
            application/json:
              schema:
                type: object
                properties:
                  id:
                    type: string
                  key:
                    type: string
                    description: Full key (only shown once)
                  name:
                    type: string
                  permissions:
                    type:
                      - array
                      - 'null'
                    items:
                      type: string
                  expiresAt:
                    type:
                      - string
                      - 'null'
                    format: date-time
                  createdAt:
                    type: string
                    format: date-time
        '400':
          description: Invalid input
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Error'
        '401':
          description: Authentication required
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/UnauthorizedError'
    get:
      operationId: listApiKeys
      tags:
        - API Keys
      summary: API キーの一覧
      description: キーのメタデータを返しますが、完全なキーは返しません。
      security:
        - bearerAuth: []
      responses:
        '200':
          description: List of API keys
          content:
            application/json:
              schema:
                type: object
                properties:
                  apiKeys:
                    type: array
                    items:
                      type: object
                      properties:
                        id:
                          type: string
                        name:
                          type: string
                        permissions:
                          type:
                            - array
                            - 'null'
                          items:
                            type: string
                        createdAt:
                          type: string
                          format: date-time
                        lastUsedAt:
                          type:
                            - string
                            - 'null'
                          format: date-time
                        expiresAt:
                          type:
                            - string
                            - 'null'
                          format: date-time
        '401':
          description: Authentication required
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/UnauthorizedError'
  /api/v1/api-keys/{id}:
    delete:
      operationId: deleteApiKey
      tags:
        - API Keys
      summary: API キーの削除
      description: API キーを完全に失効させ、削除します。削除後、そのキーは認証に使用できません。
      security:
        - bearerAuth: []
      parameters:
        - name: id
          in: path
          required: true
          description: API key UUID
          schema:
            type: string
      responses:
        '200':
          description: API key deleted
          content:
            application/json:
              schema:
                type: object
                properties:
                  ok:
                    type: boolean
        '401':
          description: Authentication required
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/UnauthorizedError'
  /api/v1/settings:
    get:
      operationId: getSettings
      tags:
        - Settings
      summary: すべての設定の取得
      description: すべてのシステム設定をキーと値のペアとして取得します。認証が必要です。
      security:
        - bearerAuth: []
      responses:
        '200':
          description: All settings wrapped in a settings object
          content:
            application/json:
              schema:
                type: object
                properties:
                  settings:
                    type: object
                    additionalProperties:
                      type: string
        '401':
          description: Authentication required
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/UnauthorizedError'
    put:
      operationId: updateSettings
      tags:
        - Settings
      summary: 設定の更新
      description: 設定するキーと値のペアのフラットな JSON オブジェクトを受け付けます。管理者ロールが必要です。
      security:
        - bearerAuth: []
      requestBody:
        required: true
        content:
          application/json:
            schema:
              type: object
              additionalProperties:
                type: string
      responses:
        '200':
          description: Settings updated
          content:
            application/json:
              schema:
                type: object
                properties:
                  ok:
                    type: boolean
                  updatedCount:
                    type: integer
        '400':
          description: Invalid input
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Error'
        '401':
          description: Authentication required
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/UnauthorizedError'
        '403':
          description: Admin access required
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ForbiddenError'
  /api/v1/settings/{key}:
    get:
      operationId: getSetting
      tags:
        - Settings
      summary: 単一設定の取得
      description: キー名で単一の設定値を取得します。値と最終更新タイムスタンプを返します。
      security:
        - bearerAuth: []
      parameters:
        - name: key
          in: path
          required: true
          description: Setting key name
          schema:
            type: string
      responses:
        '200':
          description: Setting value
          content:
            application/json:
              schema:
                type: object
                properties:
                  key:
                    type: string
                  value:
                    type: string
                  updatedAt:
                    type: string
                    format: date-time
        '401':
          description: Authentication required
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/UnauthorizedError'
        '404':
          description: Setting not found
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Error'
  /api/v1/teams:
    get:
      operationId: listTeams
      tags:
        - Teams
      summary: チームの一覧
      description: teams:manage 権限が必要です。
      security:
        - bearerAuth: []
      responses:
        '200':
          description: List of teams
          content:
            application/json:
              schema:
                type: object
                properties:
                  teams:
                    type: array
                    items:
                      type: object
                      properties:
                        id:
                          type: string
                        name:
                          type: string
                        memberCount:
                          type: integer
                        createdAt:
                          type: string
                          format: date-time
        '401':
          description: Authentication required
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/UnauthorizedError'
        '403':
          description: Insufficient permissions
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ForbiddenError'
    post:
      operationId: createTeam
      tags:
        - Teams
      summary: チームの作成
      description: teams:manage 権限が必要です。
      security:
        - bearerAuth: []
      requestBody:
        required: true
        content:
          application/json:
            schema:
              type: object
              required:
                - name
              properties:
                name:
                  type: string
      responses:
        '201':
          description: Team created
          content:
            application/json:
              schema:
                type: object
                properties:
                  id:
                    type: string
                  name:
                    type: string
        '400':
          description: Invalid input
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Error'
        '401':
          description: Authentication required
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/UnauthorizedError'
        '403':
          description: Insufficient permissions
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ForbiddenError'
        '409':
          description: Team name already exists
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ConflictError'
  /api/v1/teams/{id}:
    put:
      operationId: updateTeam
      tags:
        - Teams
      summary: チームの名前変更
      description: teams:manage 権限が必要です。
      security:
        - bearerAuth: []
      parameters:
        - name: id
          in: path
          required: true
          description: Team UUID
          schema:
            type: string
      requestBody:
        required: true
        content:
          application/json:
            schema:
              type: object
              required:
                - name
              properties:
                name:
                  type: string
      responses:
        '200':
          description: Team updated
          content:
            application/json:
              schema:
                type: object
                properties:
                  ok:
                    type: boolean
        '401':
          description: Authentication required
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/UnauthorizedError'
        '403':
          description: Insufficient permissions
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ForbiddenError'
        '404':
          description: Team not found
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Error'
        '409':
          description: Team name already exists
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ConflictError'
    delete:
      operationId: deleteTeam
      tags:
        - Teams
      summary: チームの削除
      description: teams:manage 権限が必要です。
      security:
        - bearerAuth: []
      parameters:
        - name: id
          in: path
          required: true
          description: Team UUID
          schema:
            type: string
      responses:
        '200':
          description: Team deleted
          content:
            application/json:
              schema:
                type: object
                properties:
                  ok:
                    type: boolean
        '401':
          description: Authentication required
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/UnauthorizedError'
        '403':
          description: Insufficient permissions
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ForbiddenError'
        '404':
          description: Team not found
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Error'
  /api/v1/config/auth:
    get:
      operationId: getAuthConfig
      tags:
        - System
      summary: 認証の設定
      description: このインスタンスで認証が有効かどうかを確認します。ログインフローを判断するためにフロントエンドが使用するパブリックエンドポイントです。
      security: []
      responses:
        '200':
          description: Auth config
          content:
            application/json:
              schema:
                type: object
                properties:
                  authEnabled:
                    type: boolean
  /api/v1/jobs/{jobId}/progress:
    get:
      operationId: getJobProgress
      tags:
        - System
      summary: ジョブ進捗（SSE）
      description: 長時間実行ジョブを追跡するための Server-Sent Events ストリームです。単一ファイル、バッチ、ハートビートの各フレームを送信し、ジョブの終端イベント直後に閉じます。
      security: []
      parameters:
        - name: jobId
          in: path
          required: true
          description: Job UUID from a tool processing or installation response
          schema:
            type: string
      responses:
        '200':
          description: |
            SSE stream of job progress frames. Single-file frames use `phase`, `percent`, optional `stage`, and terminal `result` or `error` fields. Batch frames use `status`, file counters, and `errors`. Heartbeat frames keep idle connections alive.
          content:
            text/event-stream:
              schema:
                oneOf:
                  - title: Single-file progress frame
                    type: object
                    required:
                      - jobId
                      - type
                      - phase
                      - percent
                    properties:
                      jobId:
                        type: string
                      type:
                        type: string
                        const: single
                      phase:
                        type: string
                        enum:
                          - processing
                          - complete
                          - failed
                      stage:
                        type: string
                      percent:
                        type: number
                        minimum: 0
                        maximum: 100
                      error:
                        type: string
                        description: Present on terminal failed frames
                      result:
                        type: object
                        additionalProperties: true
                        description: Present on terminal complete frames; contains the tool result
                  - title: Batch progress frame
                    type: object
                    required:
                      - jobId
                      - type
                      - status
                      - totalFiles
                      - completedFiles
                      - failedFiles
                      - errors
                    properties:
                      jobId:
                        type: string
                      type:
                        type: string
                        const: batch
                      status:
                        type: string
                        enum:
                          - processing
                          - completed
                          - failed
                      totalFiles:
                        type: integer
                        minimum: 0
                      completedFiles:
                        type: integer
                        minimum: 0
                      failedFiles:
                        type: integer
                        minimum: 0
                      currentFile:
                        type: string
                      errors:
                        type: array
                        items:
                          type: object
                          required:
                            - filename
                            - error
                          properties:
                            filename:
                              type: string
                            error:
                              type: string
                  - title: Heartbeat frame
                    type: object
                    required:
                      - type
                    properties:
                      type:
                        type: string
                        const: heartbeat
  /api/v1/admin/health:
    get:
      operationId: adminHealthCheck
      tags:
        - Admin
      summary: 管理ヘルスチェック
      description: |
        データベースの状態、ストレージモード、キューの状態、GPU の可用性を含む完全なシステム診断。system:health 権限が必要です。
      security:
        - bearerAuth: []
      responses:
        '200':
          description: Detailed system health
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/AdminHealthResponse'
        '401':
          description: Authentication required
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/UnauthorizedError'
        '403':
          description: Insufficient permissions
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ForbiddenError'
  /api/v1/config/analytics:
    get:
      operationId: getAnalyticsConfig
      tags:
        - Analytics
      summary: アナリティクス設定の取得
      description: |
        有効なアナリティクスプロバイダーの設定を返します。アナリティクスは、コンパイル時のバンドルとインスタンスの analyticsEnabled 設定の両方が許可した場合にのみ有効になります。無効な場合、キー、DSN、instanceId は空になります。パブリックエンドポイントです。
      security: []
      responses:
        '200':
          description: Analytics configuration
          content:
            application/json:
              schema:
                type: object
                properties:
                  enabled:
                    type: boolean
                    description: |
                      Effective analytics state. True only when both the
                      compile-time bake and the instance analyticsEnabled
                      setting are on. When false, the keys, DSN, and instanceId
                      are blank.
                  posthogApiKey:
                    type: string
                  posthogHost:
                    type: string
                  sentryDsn:
                    type: string
                  sentryDsnWeb:
                    type: string
                  posthogSampleRate:
                    type: number
                  instanceId:
                    type: string
  /api/v1/features:
    get:
      operationId: listFeatures
      tags:
        - Features
      summary: 機能バンドルの一覧
      description: |
        利用可能なすべての AI 機能バンドルとそのインストール状態を返します。Docker 以外の環境では、すべてのバンドルがインストール済みとして表示されます。
      security:
        - bearerAuth: []
      responses:
        '200':
          description: Feature bundle list
          content:
            application/json:
              schema:
                type: object
                properties:
                  bundles:
                    type: array
                    items:
                      type: object
                      properties:
                        id:
                          type: string
                        name:
                          type: string
                        description:
                          type: string
                          description: Human-readable description of the feature bundle
                        status:
                          type: string
                          enum:
                            - installed
                            - not_installed
                            - installing
                            - queued
                            - error
                        installedVersion:
                          type:
                            - string
                            - 'null'
                        estimatedSize:
                          type: string
                        downloadBytes:
                          type:
                            - integer
                            - 'null'
                          description: Compressed archive size for this host's architecture, when the manifest records it. Null in native (non-Docker) mode or when unmeasured.
                        installedBytes:
                          type:
                            - integer
                            - 'null'
                          description: Extracted on-disk size for this host's architecture, when the manifest records it.
                        compatibility:
                          type: string
                          enum:
                            - compatible
                            - incompatible
                            - invalid
                          description: OCR runtime compatibility; omitted for legacy bundles
                        compatibilityReason:
                          type:
                            - string
                            - 'null'
                          description: OCR runtime compatibility reason; omitted for legacy bundles
                        selectedTarget:
                          type:
                            - string
                            - 'null'
                          description: Selected OCR runtime target, such as linux-amd64-cpu-py312
                        missingDownloadBytes:
                          type:
                            - integer
                            - 'null'
                          description: Remaining OCR runtime download size for this host
                        requiredMemoryBytes:
                          type:
                            - integer
                            - 'null'
                          description: Minimum effective memory required by the selected OCR runtime; null for legacy bundles
                        effectiveMemoryBytes:
                          type:
                            - integer
                            - 'null'
                          description: Effective memory available to OCR (the configured container cgroup limit, otherwise host memory); null for legacy bundles
                        healthyGeneration:
                          type:
                            - string
                            - 'null'
                          description: Active immutable OCR generation identifier
                        availableQualities:
                          type: array
                          description: OCR quality tiers currently available on this host
                          items:
                            type: string
                            enum:
                              - fast
                              - balanced
                              - best
                        enablesTools:
                          type: array
                          items:
                            type: string
                        progress:
                          type:
                            - object
                            - 'null'
                          properties:
                            percent:
                              type: number
                            stage:
                              type: string
                        error:
                          type:
                            - string
                            - 'null'
        '401':
          description: Authentication required
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/UnauthorizedError'
  /api/v1/admin/features/{bundleId}/install:
    post:
      operationId: installFeature
      tags:
        - Features
      summary: 機能バンドルのインストール
      description: |-
        AI 機能バンドルの非同期インストールを開始します。 モデルをダウンロードし、
        Python sidecar を構成します。 進行状況を追跡するために jobId を返します。
        SSE エンドポイント経由。 features:manage 権限が必要です。

        bundleId=ocr の場合、インストールは公式の Linux amd64 および arm64 コンテナーでサポートされています。
        このホスト用に選択されたターゲットをダウンロードし、リリースを確認します
        インデックスの Ed25519 シグネチャと artifact/モデル ダイジェスト、スモーク テスト
        不変生成を段階的に実行し、それをアトミックにアクティブ化します。 OCR
        ランタイムは、コンテナーに NVIDIA GPU アクセス権がある場合でも、CPU を使用します。 の
        正確なランタイムには、少なくとも 4 GiB の有効メモリが必要です (
        構成されたコンテナー cgroup 制限、それ以外の場合はホスト メモリ)。 署名された以下のシステム
        最低限の互換性はダウンロード前に拒否されます
        compatibilityReason=insufficient-memory。 Fast Tesseract OCR ではありません
        この要件の対象となります。

        インストールはサーバー側でシリアル化されます。リクエストが行われている間に別のリクエストが行われます。
        実行中のインストールは拒否されるのではなくキューに入れられます (queued=true)。
        実行中のインストールが終了すると、自動的に開始されます。 繰り返し
        同じインフライトバンドルに対するリクエストは重複排除され、同じものを返します。
        jobId。
      security:
        - bearerAuth: []
      parameters:
        - name: bundleId
          in: path
          required: true
          description: Feature bundle identifier
          schema:
            type: string
      responses:
        '202':
          description: Installation started or queued
          content:
            application/json:
              schema:
                type: object
                properties:
                  jobId:
                    type: string
                    description: Job ID for tracking installation progress via SSE
                  queued:
                    type: boolean
                    description: True when the install was queued behind another running install instead of starting immediately.
        '401':
          description: Authentication required
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/UnauthorizedError'
        '403':
          description: Insufficient permissions
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ForbiddenError'
        '404':
          description: Unknown bundle ID
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Error'
        '409':
          description: Bundle already installed or OCR runtime unsupported on this host
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ConflictError'
        '503':
          description: OCR runtime trust key is not configured in this image
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Error'
  /api/v1/admin/features/{bundleId}/uninstall:
    post:
      operationId: uninstallFeature
      tags:
        - Features
      summary: 機能バンドルのアンインストール
      description: |
        AI 機能バンドルをアンインストールし、そのモデルファイルを削除します（他のインストール済みバンドルと共有されている場合を除く）。features:manage 権限が必要です。
      security:
        - bearerAuth: []
      parameters:
        - name: bundleId
          in: path
          required: true
          description: Feature bundle identifier
          schema:
            type: string
      responses:
        '200':
          description: Bundle uninstalled
          content:
            application/json:
              schema:
                type: object
                properties:
                  ok:
                    type: boolean
        '401':
          description: Authentication required
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/UnauthorizedError'
        '403':
          description: Insufficient permissions
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ForbiddenError'
        '404':
          description: Unknown bundle ID
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Error'
        '409':
          description: Bundle is not installed
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ConflictError'
  /api/v1/admin/features/disk-usage:
    get:
      operationId: getFeatureDiskUsage
      tags:
        - Features
      summary: AI モデルのディスク使用量
      description: |
        AI モデルファイルが使用しているディスク容量の合計を返します。
        features:manage 権限が必要です。
      security:
        - bearerAuth: []
      responses:
        '200':
          description: Disk usage
          content:
            application/json:
              schema:
                type: object
                properties:
                  totalBytes:
                    type: integer
                    description: Total bytes used by AI model files
        '401':
          description: Authentication required
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/UnauthorizedError'
        '403':
          description: Insufficient permissions
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ForbiddenError'
  /api/v1/audit-log:
    get:
      operationId: queryAuditLog
      tags:
        - Audit
      summary: 監査ログの照会
      description: |
        管理操作のページネーション付き監査ログ。操作の種類と日付範囲によるフィルタリングに対応します。audit:read 権限が必要です。
      security:
        - bearerAuth: []
      parameters:
        - name: page
          in: query
          description: Page number (default 1)
          schema:
            type: integer
            minimum: 1
            default: 1
        - name: limit
          in: query
          description: Results per page (1-100, default 50)
          schema:
            type: integer
            minimum: 1
            maximum: 100
            default: 50
        - name: action
          in: query
          description: Filter by action type
          schema:
            type: string
        - name: from
          in: query
          description: Filter entries after this ISO 8601 date
          schema:
            type: string
            format: date-time
        - name: to
          in: query
          description: Filter entries before this ISO 8601 date
          schema:
            type: string
            format: date-time
      responses:
        '200':
          description: Paginated audit log entries
          content:
            application/json:
              schema:
                type: object
                properties:
                  entries:
                    type: array
                    items:
                      type: object
                      properties:
                        id:
                          type: string
                        actorId:
                          type:
                            - string
                            - 'null'
                        actorUsername:
                          type: string
                        action:
                          type: string
                        targetType:
                          type:
                            - string
                            - 'null'
                        targetId:
                          type:
                            - string
                            - 'null'
                        details:
                          type:
                            - object
                            - 'null'
                        ipAddress:
                          type:
                            - string
                            - 'null'
                        requestId:
                          type:
                            - string
                            - 'null'
                        createdAt:
                          type: string
                          format: date-time
                  total:
                    type: integer
                    description: Total matching entries
                  page:
                    type: integer
                  limit:
                    type: integer
        '401':
          description: Authentication required
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/UnauthorizedError'
        '403':
          description: Insufficient permissions
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ForbiddenError'
  /api/v1/roles:
    get:
      operationId: listRoles
      tags:
        - Roles
      summary: ロールの一覧
      description: |
        すべてのロール（組み込みおよびカスタム）を、その権限とユーザー数とともに一覧表示します。audit:read 権限が必要です。
      security:
        - bearerAuth: []
      responses:
        '200':
          description: Role list
          content:
            application/json:
              schema:
                type: object
                properties:
                  roles:
                    type: array
                    items:
                      type: object
                      properties:
                        id:
                          type: string
                        name:
                          type: string
                        description:
                          type: string
                          description: Human-readable description of the role
                        permissions:
                          type: array
                          items:
                            type: string
                        isBuiltin:
                          type: boolean
                        userCount:
                          type: integer
                        createdAt:
                          type: string
                          format: date-time
                        updatedAt:
                          type: string
                          format: date-time
        '401':
          description: Authentication required
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/UnauthorizedError'
        '403':
          description: Insufficient permissions
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ForbiddenError'
    post:
      operationId: createRole
      tags:
        - Roles
      summary: カスタムロールの作成
      description: |
        指定した権限を持つ新しいカスタムロールを作成します。ロール名は 2〜30 文字で、小文字の英数字にハイフンとアンダースコアを使用できます。security:manage 権限が必要です。
      security:
        - bearerAuth: []
      requestBody:
        required: true
        content:
          application/json:
            schema:
              type: object
              required:
                - name
                - permissions
              properties:
                name:
                  type: string
                  minLength: 2
                  maxLength: 30
                  pattern: ^[a-z0-9_-]+$
                  description: Lowercase role name
                description:
                  type: string
                  description: Human-readable description
                permissions:
                  type: array
                  items:
                    type: string
                  description: |
                    Permission strings. Valid values: tools:use, files:own, files:all,
                    apikeys:own, apikeys:all, pipelines:own, pipelines:all,
                    settings:read, settings:write, users:manage, teams:manage,
                    features:manage, system:health, audit:read, security:manage
      responses:
        '201':
          description: Role created
          content:
            application/json:
              schema:
                type: object
                properties:
                  id:
                    type: string
                  name:
                    type: string
                  description:
                    type: string
                    description: Human-readable description of the role
                  permissions:
                    type: array
                    items:
                      type: string
                  isBuiltin:
                    type: boolean
        '400':
          description: Validation error
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Error'
        '401':
          description: Authentication required
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/UnauthorizedError'
        '403':
          description: Insufficient permissions
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ForbiddenError'
        '409':
          description: Role name already exists
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ConflictError'
  /api/v1/roles/{id}:
    put:
      operationId: updateRole
      tags:
        - Roles
      summary: カスタムロールの更新
      description: |
        カスタムロールの名前、説明、権限を更新します。組み込みロールは変更できません。security:manage 権限が必要です。
      security:
        - bearerAuth: []
      parameters:
        - name: id
          in: path
          required: true
          description: Role UUID
          schema:
            type: string
      requestBody:
        required: true
        content:
          application/json:
            schema:
              type: object
              properties:
                name:
                  type: string
                  minLength: 2
                  maxLength: 30
                description:
                  type: string
                  description: Updated description for the role
                permissions:
                  type: array
                  items:
                    type: string
      responses:
        '200':
          description: Role updated
          content:
            application/json:
              schema:
                type: object
                properties:
                  ok:
                    type: boolean
        '400':
          description: Validation error or cannot modify built-in role
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Error'
        '401':
          description: Authentication required
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/UnauthorizedError'
        '403':
          description: Insufficient permissions
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ForbiddenError'
        '404':
          description: Role not found
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Error'
        '409':
          description: Role name already exists
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ConflictError'
    delete:
      operationId: deleteRole
      tags:
        - Roles
      summary: カスタムロールの削除
      description: |
        カスタムロールを削除します。このロールが割り当てられているユーザーはデフォルトの「user」ロールに再割り当てされます。組み込みロールは削除できません。security:manage 権限が必要です。
      security:
        - bearerAuth: []
      parameters:
        - name: id
          in: path
          required: true
          description: Role UUID
          schema:
            type: string
      responses:
        '200':
          description: Role deleted
          content:
            application/json:
              schema:
                type: object
                properties:
                  ok:
                    type: boolean
        '400':
          description: Cannot delete built-in role
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Error'
        '401':
          description: Authentication required
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/UnauthorizedError'
        '403':
          description: Insufficient permissions
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ForbiddenError'
        '404':
          description: Role not found
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Error'
  /api/v1/tools/video/convert-video:
    post:
      operationId: convertVideo
      tags:
        - Tools
      summary: 動画の変換
      description: MP4、MOV、WebM、AVI、MKV の間で動画を変換します。
      security:
        - bearerAuth: []
      requestBody:
        required: true
        content:
          multipart/form-data:
            schema:
              type: object
              required:
                - file
              properties:
                file:
                  type: string
                  format: binary
                  description: Video file to process
                settings:
                  type: string
                  description: |
                    JSON string with options:
                    - `format` (string, default "mp4") - One of: mp4, mov, webm, avi, mkv
                    - `quality` (string, default "balanced") - One of: high, balanced, small
                clientJobId:
                  type: string
                  description: Client-provided job ID for SSE progress tracking
      responses:
        '202':
          description: Accepted for async processing. Track progress via SSE at /api/v1/jobs/{jobId}/progress.
          content:
            application/json:
              schema:
                type: object
                properties:
                  jobId:
                    type: string
                  async:
                    type: boolean
                    example: true
        '400':
          description: Invalid input
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Error'
        '401':
          description: Authentication required
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/UnauthorizedError'
        '422':
          description: Processing failed
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Error'
  /api/v1/tools/video/compress-video:
    post:
      operationId: compressVideo
      tags:
        - Tools
      summary: 動画の圧縮
      description: 品質を制御しながら動画のファイルサイズを縮小します。
      security:
        - bearerAuth: []
      requestBody:
        required: true
        content:
          multipart/form-data:
            schema:
              type: object
              required:
                - file
              properties:
                file:
                  type: string
                  format: binary
                  description: Video file to process
                settings:
                  type: string
                  description: |
                    JSON string with options:
                    - `quality` (string, default "balanced") - One of: light, balanced, strong
                    - `resolution` (string, default "original") - One of: original, 1080p, 720p, 480p
                clientJobId:
                  type: string
                  description: Client-provided job ID for SSE progress tracking
      responses:
        '202':
          description: Accepted for async processing. Track progress via SSE at /api/v1/jobs/{jobId}/progress.
          content:
            application/json:
              schema:
                type: object
                properties:
                  jobId:
                    type: string
                  async:
                    type: boolean
                    example: true
        '400':
          description: Invalid input
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Error'
        '401':
          description: Authentication required
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/UnauthorizedError'
        '422':
          description: Processing failed
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Error'
  /api/v1/tools/video/trim-video:
    post:
      operationId: trimVideo
      tags:
        - Tools
      summary: 動画のトリミング
      description: 動画からクリップを切り出します。
      security:
        - bearerAuth: []
      requestBody:
        required: true
        content:
          multipart/form-data:
            schema:
              type: object
              required:
                - file
              properties:
                file:
                  type: string
                  format: binary
                  description: Video file to process
                settings:
                  type: string
                  description: |
                    JSON string with options:
                    - `startS` (number >= 0, default 0) - Start time in seconds
                    - `endS` (number, required) - End time in seconds (must be after startS)
                    - `precise` (boolean, default false) - Re-encode for frame-accurate cuts instead of keyframe seek
      responses:
        '200':
          description: Processed video
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ToolResponse'
        '400':
          description: Invalid input
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Error'
        '401':
          description: Authentication required
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/UnauthorizedError'
  /api/v1/tools/video/mute-video:
    post:
      operationId: muteVideo
      tags:
        - Tools
      summary: 動画のミュート
      description: 動画から音声トラックを削除します。
      security:
        - bearerAuth: []
      requestBody:
        required: true
        content:
          multipart/form-data:
            schema:
              type: object
              required:
                - file
              properties:
                file:
                  type: string
                  format: binary
                  description: Video file to process
      responses:
        '200':
          description: Processed video
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ToolResponse'
        '400':
          description: Invalid input
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Error'
        '401':
          description: Authentication required
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/UnauthorizedError'
  /api/v1/tools/video/video-to-gif:
    post:
      operationId: videoToGif
      tags:
        - Tools
      summary: 動画から GIF
      description: 動画クリップをアニメーション GIF に変換します。
      security:
        - bearerAuth: []
      requestBody:
        required: true
        content:
          multipart/form-data:
            schema:
              type: object
              required:
                - file
              properties:
                file:
                  type: string
                  format: binary
                  description: Video file to process
                settings:
                  type: string
                  description: |
                    JSON string with options:
                    - `fps` (integer 1-30, default 12) - Output frame rate
                    - `width` (integer 64-1280, default 480) - Output width in pixels
                    - `startS` (number >= 0, default 0) - Start time in seconds
                    - `durationS` (number 0-60, default 5) - Duration in seconds
                clientJobId:
                  type: string
                  description: Client-provided job ID for SSE progress tracking
      responses:
        '202':
          description: Accepted for async processing. Track progress via SSE at /api/v1/jobs/{jobId}/progress.
          content:
            application/json:
              schema:
                type: object
                properties:
                  jobId:
                    type: string
                  async:
                    type: boolean
                    example: true
        '400':
          description: Invalid input
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Error'
        '401':
          description: Authentication required
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/UnauthorizedError'
        '422':
          description: Processing failed
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Error'
  /api/v1/tools/video/resize-video:
    post:
      operationId: resizeVideo
      tags:
        - Tools
      summary: 動画のリサイズ
      description: 動画を新しい解像度またはプリセットサイズにスケールします。
      security:
        - bearerAuth: []
      requestBody:
        required: true
        content:
          multipart/form-data:
            schema:
              type: object
              required:
                - file
              properties:
                file:
                  type: string
                  format: binary
                  description: Video file to process
                settings:
                  type: string
                  description: |
                    JSON string with options:
                    - `width` (integer 16-7680, optional) - Target width in pixels
                    - `height` (integer 16-4320, optional) - Target height in pixels
                    - `preset` (string, default "custom") - One of: custom, 2160p, 1440p, 1080p, 720p, 480p, 360p. When "custom", at least width or height is required
      responses:
        '200':
          description: Processed video
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ToolResponse'
        '400':
          description: Invalid input
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Error'
        '401':
          description: Authentication required
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/UnauthorizedError'
  /api/v1/tools/video/crop-video:
    post:
      operationId: cropVideo
      tags:
        - Tools
      summary: 動画の切り抜き
      description: 動画から領域を切り抜きます。
      security:
        - bearerAuth: []
      requestBody:
        required: true
        content:
          multipart/form-data:
            schema:
              type: object
              required:
                - file
              properties:
                file:
                  type: string
                  format: binary
                  description: Video file to process
                settings:
                  type: string
                  description: |
                    JSON string with options:
                    - `width` (integer >= 16, required) - Crop region width in pixels
                    - `height` (integer >= 16, required) - Crop region height in pixels
                    - `x` (integer >= 0, default 0) - Horizontal offset from top-left
                    - `y` (integer >= 0, default 0) - Vertical offset from top-left
      responses:
        '200':
          description: Processed video
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ToolResponse'
        '400':
          description: Invalid input
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Error'
        '401':
          description: Authentication required
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/UnauthorizedError'
  /api/v1/tools/video/rotate-video:
    post:
      operationId: rotateVideo
      tags:
        - Tools
      summary: 動画の回転
      description: 動画を回転または反転します。
      security:
        - bearerAuth: []
      requestBody:
        required: true
        content:
          multipart/form-data:
            schema:
              type: object
              required:
                - file
              properties:
                file:
                  type: string
                  format: binary
                  description: Video file to process
                settings:
                  type: string
                  description: |
                    JSON string with options:
                    - `transform` (string, required) - One of: cw90, ccw90, 180, hflip, vflip
      responses:
        '200':
          description: Processed video
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ToolResponse'
        '400':
          description: Invalid input
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Error'
        '401':
          description: Authentication required
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/UnauthorizedError'
  /api/v1/tools/video/change-fps:
    post:
      operationId: changeFps
      tags:
        - Tools
      summary: FPS の変更
      description: 動画のフレームレートを変更します。
      security:
        - bearerAuth: []
      requestBody:
        required: true
        content:
          multipart/form-data:
            schema:
              type: object
              required:
                - file
              properties:
                file:
                  type: string
                  format: binary
                  description: Video file to process
                settings:
                  type: string
                  description: |
                    JSON string with options:
                    - `fps` (number 1-120, default 30) - Target frame rate
      responses:
        '200':
          description: Processed video
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ToolResponse'
        '400':
          description: Invalid input
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Error'
        '401':
          description: Authentication required
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/UnauthorizedError'
  /api/v1/tools/video/video-color:
    post:
      operationId: videoColor
      tags:
        - Tools
      summary: 動画のカラー
      description: 明るさ、コントラスト、彩度、ガンマを調整します。
      security:
        - bearerAuth: []
      requestBody:
        required: true
        content:
          multipart/form-data:
            schema:
              type: object
              required:
                - file
              properties:
                file:
                  type: string
                  format: binary
                  description: Video file to process
                settings:
                  type: string
                  description: |
                    JSON string with options:
                    - `brightness` (number -1 to 1, default 0) - Brightness adjustment
                    - `contrast` (number 0-4, default 1) - Contrast multiplier
                    - `saturation` (number 0-3, default 1) - Saturation multiplier
                    - `gamma` (number 0.1-10, default 1) - Gamma correction
      responses:
        '200':
          description: Processed video
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ToolResponse'
        '400':
          description: Invalid input
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Error'
        '401':
          description: Authentication required
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/UnauthorizedError'
  /api/v1/tools/video/video-speed:
    post:
      operationId: videoSpeed
      tags:
        - Tools
      summary: 動画の速度
      description: 動画を速くまたは遅くします。
      security:
        - bearerAuth: []
      requestBody:
        required: true
        content:
          multipart/form-data:
            schema:
              type: object
              required:
                - file
              properties:
                file:
                  type: string
                  format: binary
                  description: Video file to process
                settings:
                  type: string
                  description: |
                    JSON string with options:
                    - `factor` (number 0.25-4, default 2) - Speed multiplier (2 = double speed, 0.5 = half speed)
                    - `keepPitch` (boolean, default true) - Preserve audio pitch when changing speed
      responses:
        '200':
          description: Processed video
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ToolResponse'
        '400':
          description: Invalid input
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Error'
        '401':
          description: Authentication required
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/UnauthorizedError'
  /api/v1/tools/video/reverse-video:
    post:
      operationId: reverseVideo
      tags:
        - Tools
      summary: 動画の逆再生
      description: 動画クリップを逆再生します。最大 5 分のクリップまでに制限されます。
      security:
        - bearerAuth: []
      requestBody:
        required: true
        content:
          multipart/form-data:
            schema:
              type: object
              required:
                - file
              properties:
                file:
                  type: string
                  format: binary
                  description: Video file to process
      responses:
        '200':
          description: Processed video
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ToolResponse'
        '400':
          description: Invalid input
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Error'
        '401':
          description: Authentication required
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/UnauthorizedError'
  /api/v1/tools/video/video-loudnorm:
    post:
      operationId: videoLoudnorm
      tags:
        - Tools
      summary: 音声の正規化
      description: 動画の音声音量を放送標準（EBU R128）に正規化します。
      security:
        - bearerAuth: []
      requestBody:
        required: true
        content:
          multipart/form-data:
            schema:
              type: object
              required:
                - file
              properties:
                file:
                  type: string
                  format: binary
                  description: Video file to process
      responses:
        '200':
          description: Processed video
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ToolResponse'
        '400':
          description: Invalid input
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Error'
        '401':
          description: Authentication required
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/UnauthorizedError'
  /api/v1/tools/video/aspect-pad:
    post:
      operationId: aspectPad
      tags:
        - Tools
      summary: アスペクトパディング
      description: 目標のアスペクト比に合わせるため単色のバーを追加します。
      security:
        - bearerAuth: []
      requestBody:
        required: true
        content:
          multipart/form-data:
            schema:
              type: object
              required:
                - file
              properties:
                file:
                  type: string
                  format: binary
                  description: Video file to process
                settings:
                  type: string
                  description: |
                    JSON string with options:
                    - `target` (string, default "9:16") - One of: 16:9, 9:16, 1:1, 4:3, 3:4
                    - `color` (string, default "#000000") - Hex color for padding bars (e.g. "#000000")
      responses:
        '200':
          description: Processed video
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ToolResponse'
        '400':
          description: Invalid input
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Error'
        '401':
          description: Authentication required
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/UnauthorizedError'
  /api/v1/tools/video/blur-pad:
    post:
      operationId: blurPad
      tags:
        - Tools
      summary: ぼかしパディング
      description: バーを動画のぼかしたコピーで埋めます。
      security:
        - bearerAuth: []
      requestBody:
        required: true
        content:
          multipart/form-data:
            schema:
              type: object
              required:
                - file
              properties:
                file:
                  type: string
                  format: binary
                  description: Video file to process
                settings:
                  type: string
                  description: |
                    JSON string with options:
                    - `target` (string, default "16:9") - One of: 16:9, 9:16, 1:1, 4:3, 3:4
                    - `blur` (number 2-50, default 20) - Gaussian blur sigma for the background
      responses:
        '200':
          description: Processed video
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ToolResponse'
        '400':
          description: Invalid input
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Error'
        '401':
          description: Authentication required
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/UnauthorizedError'
  /api/v1/tools/video/watermark-video:
    post:
      operationId: watermarkVideo
      tags:
        - Tools
      summary: 動画の透かし
      description: 動画フレームにテキストの透かしを焼き込みます。
      security:
        - bearerAuth: []
      requestBody:
        required: true
        content:
          multipart/form-data:
            schema:
              type: object
              required:
                - file
              properties:
                file:
                  type: string
                  format: binary
                  description: Video file to process
                settings:
                  type: string
                  description: |
                    JSON string with options:
                    - `text` (string, required) - Watermark text (1-200 characters)
                    - `position` (string, default "br") - One of: tl, tc, tr, l, c, r, bl, bc, br
                    - `fontSize` (integer 8-120, default 36) - Font size in pixels
                    - `opacity` (number 0.05-1, default 0.5) - Watermark opacity
                    - `color` (string, default "#ffffff") - Hex color for the text (e.g. "#ffffff")
      responses:
        '200':
          description: Processed video
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ToolResponse'
        '400':
          description: Invalid input
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Error'
        '401':
          description: Authentication required
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/UnauthorizedError'
  /api/v1/tools/video/stabilize-video:
    post:
      operationId: stabilizeVideo
      tags:
        - Tools
      summary: 動画の手ブレ補正
      description: 2 パスの手ブレ補正でカメラの揺れを軽減します。
      security:
        - bearerAuth: []
      requestBody:
        required: true
        content:
          multipart/form-data:
            schema:
              type: object
              required:
                - file
              properties:
                file:
                  type: string
                  format: binary
                  description: Video file to process
                settings:
                  type: string
                  description: |
                    JSON string with options:
                    - `smoothing` (integer 5-60, default 15) - Smoothing window size in frames
                clientJobId:
                  type: string
                  description: Client-provided job ID for SSE progress tracking
      responses:
        '202':
          description: Accepted for async processing. Track progress via SSE at /api/v1/jobs/{jobId}/progress.
          content:
            application/json:
              schema:
                type: object
                properties:
                  jobId:
                    type: string
                  async:
                    type: boolean
                    example: true
        '400':
          description: Invalid input
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Error'
        '401':
          description: Authentication required
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/UnauthorizedError'
        '422':
          description: Processing failed
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Error'
  /api/v1/tools/video/gif-to-video:
    post:
      operationId: gifToVideo
      tags:
        - Tools
      summary: GIF から動画
      description: アニメーション GIF を MP4、WebM、MOV の動画に変換します。
      security:
        - bearerAuth: []
      requestBody:
        required: true
        content:
          multipart/form-data:
            schema:
              type: object
              required:
                - file
              properties:
                file:
                  type: string
                  format: binary
                  description: GIF file to convert
                settings:
                  type: string
                  description: |
                    JSON string with options:
                    - `format` (string, default "mp4") - One of: mp4, webm, mov
      responses:
        '200':
          description: Converted video
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ToolResponse'
        '400':
          description: Invalid input
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Error'
        '401':
          description: Authentication required
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/UnauthorizedError'
  /api/v1/tools/video/video-to-webp:
    post:
      operationId: videoToWebp
      tags:
        - Tools
      summary: 動画から WebP
      description: 動画クリップをアニメーション WebP 画像に変換します。
      security:
        - bearerAuth: []
      requestBody:
        required: true
        content:
          multipart/form-data:
            schema:
              type: object
              required:
                - file
              properties:
                file:
                  type: string
                  format: binary
                  description: Video file to process
                settings:
                  type: string
                  description: |
                    JSON string with options:
                    - `fps` (integer 1-30, default 12) - Output frame rate
                    - `width` (integer 16-1920, default 480) - Output width in pixels
                    - `quality` (integer 1-100, default 75) - WebP compression quality
                    - `loop` (boolean, default true) - Loop the animation
      responses:
        '200':
          description: Converted animated WebP
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ToolResponse'
        '400':
          description: Invalid input
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Error'
        '401':
          description: Authentication required
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/UnauthorizedError'
  /api/v1/tools/video/video-to-frames:
    post:
      operationId: videoToFrames
      tags:
        - Tools
      summary: 動画からフレーム
      description: 動画からフレームを画像の ZIP として抽出します。
      security:
        - bearerAuth: []
      requestBody:
        required: true
        content:
          multipart/form-data:
            schema:
              type: object
              required:
                - file
              properties:
                file:
                  type: string
                  format: binary
                  description: Video file to process
                settings:
                  type: string
                  description: |
                    JSON string with options:
                    - `mode` (string, default "all") - One of: all, nth, timestamps
                    - `n` (integer 2-1000, default 10) - Extract every Nth frame (used when mode is "nth")
                    - `timestamps` (string, default "") - Comma-separated timestamps in seconds (required when mode is "timestamps")
                    - `format` (string, default "png") - One of: png, jpg
      responses:
        '200':
          description: ZIP archive containing extracted frames
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ToolResponse'
        '400':
          description: Invalid input
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Error'
        '401':
          description: Authentication required
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/UnauthorizedError'
  /api/v1/tools/video/merge-videos:
    post:
      operationId: mergeVideos
      tags:
        - Tools
      summary: 動画の結合
      description: |
        複数の動画クリップを 1 つの MP4 ファイルに結合します。すべての入力は最初の動画の解像度と 30 fps に正規化されます。
      security:
        - bearerAuth: []
      requestBody:
        required: true
        content:
          multipart/form-data:
            schema:
              type: object
              required:
                - file
              properties:
                file:
                  type: array
                  items:
                    type: string
                    format: binary
                  description: Two or more video files to merge (up to 10)
                clientJobId:
                  type: string
                  description: Client-provided job ID for SSE progress tracking
      responses:
        '202':
          description: Accepted for async processing. Track progress via SSE at /api/v1/jobs/{jobId}/progress.
          content:
            application/json:
              schema:
                type: object
                properties:
                  jobId:
                    type: string
                  async:
                    type: boolean
                    example: true
        '400':
          description: Invalid input
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Error'
        '401':
          description: Authentication required
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/UnauthorizedError'
        '422':
          description: Processing failed
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Error'
  /api/v1/tools/video/replace-audio:
    post:
      operationId: replaceAudio
      tags:
        - Tools
      summary: 音声の置換
      description: 動画の音声トラックを別のファイルに差し替えます。
      security:
        - bearerAuth: []
      requestBody:
        required: true
        content:
          multipart/form-data:
            schema:
              type: object
              required:
                - file
              properties:
                file:
                  type: array
                  items:
                    type: string
                    format: binary
                  description: A video file followed by an audio file (exactly 2 files)
      responses:
        '200':
          description: Processed video
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ToolResponse'
        '400':
          description: Invalid input
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Error'
        '401':
          description: Authentication required
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/UnauthorizedError'
  /api/v1/tools/video/burn-subtitles:
    post:
      operationId: burnSubtitles
      tags:
        - Tools
      summary: 字幕の焼き込み
      description: 字幕を動画フレームに恒久的に描画します。
      security:
        - bearerAuth: []
      requestBody:
        required: true
        content:
          multipart/form-data:
            schema:
              type: object
              required:
                - file
              properties:
                file:
                  type: array
                  items:
                    type: string
                    format: binary
                  description: A video file followed by a subtitle file (.srt, .vtt, .ass)
                settings:
                  type: string
                  description: |
                    JSON string with options:
                    - `fontSize` (integer 8-72, default 24) - Subtitle font size in pixels
                clientJobId:
                  type: string
                  description: Client-provided job ID for SSE progress tracking
      responses:
        '202':
          description: Accepted for async processing. Track progress via SSE at /api/v1/jobs/{jobId}/progress.
          content:
            application/json:
              schema:
                type: object
                properties:
                  jobId:
                    type: string
                  async:
                    type: boolean
                    example: true
        '400':
          description: Invalid input
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Error'
        '401':
          description: Authentication required
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/UnauthorizedError'
        '422':
          description: Processing failed
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Error'
  /api/v1/tools/video/embed-subtitles:
    post:
      operationId: embedSubtitles
      tags:
        - Tools
      summary: 字幕の埋め込み
      description: 字幕トラックを動画コンテナーに多重化します。
      security:
        - bearerAuth: []
      requestBody:
        required: true
        content:
          multipart/form-data:
            schema:
              type: object
              required:
                - file
              properties:
                file:
                  type: array
                  items:
                    type: string
                    format: binary
                  description: A video file followed by a subtitle file (.srt, .vtt, .ass)
                settings:
                  type: string
                  description: |
                    JSON string with options:
                    - `language` (string, default "eng") - ISO 639-2/B language code (3 lowercase letters, e.g. "eng", "fra", "deu")
      responses:
        '200':
          description: Processed video
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ToolResponse'
        '400':
          description: Invalid input
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Error'
        '401':
          description: Authentication required
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/UnauthorizedError'
  /api/v1/tools/video/extract-subtitles:
    post:
      operationId: extractSubtitles
      tags:
        - Tools
      summary: 字幕の抽出
      description: 動画から字幕トラックを SRT ファイルとして取り出します。
      security:
        - bearerAuth: []
      requestBody:
        required: true
        content:
          multipart/form-data:
            schema:
              type: object
              required:
                - file
              properties:
                file:
                  type: string
                  format: binary
                  description: Video file with an embedded subtitle track
      responses:
        '200':
          description: Extracted subtitle file
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ToolResponse'
        '400':
          description: Invalid input
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Error'
        '401':
          description: Authentication required
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/UnauthorizedError'
  /api/v1/tools/video/images-to-video:
    post:
      operationId: imagesToVideo
      tags:
        - Tools
      summary: 画像から動画
      description: 画像のセットをスライドショー動画にします。
      security:
        - bearerAuth: []
      requestBody:
        required: true
        content:
          multipart/form-data:
            schema:
              type: object
              required:
                - file
              properties:
                file:
                  type: array
                  items:
                    type: string
                    format: binary
                  description: Two or more image files (up to 60)
                settings:
                  type: string
                  description: |
                    JSON string with options:
                    - `secondsPerImage` (number 0.5-10, default 2) - Display duration per image in seconds
                    - `resolution` (string, default "720p") - One of: 1080p, 720p, square
                    - `fps` (integer 10-60, default 30) - Output frame rate
      responses:
        '200':
          description: Generated slideshow video
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ToolResponse'
        '400':
          description: Invalid input
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Error'
        '401':
          description: Authentication required
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/UnauthorizedError'
  /api/v1/tools/video/video-metadata:
    post:
      operationId: videoMetadata
      tags:
        - Tools
      summary: 動画メタデータのクリーンアップ
      description: 動画からメタデータを削除し、見つかった内容を報告します。
      security:
        - bearerAuth: []
      requestBody:
        required: true
        content:
          multipart/form-data:
            schema:
              type: object
              required:
                - file
              properties:
                file:
                  type: string
                  format: binary
                  description: Video file to process
      responses:
        '200':
          description: Processed video with metadata report
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ToolResponse'
        '400':
          description: Invalid input
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Error'
        '401':
          description: Authentication required
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/UnauthorizedError'
  /api/v1/tools/video/auto-subtitles:
    post:
      operationId: autoSubtitles
      tags:
        - Tools
      summary: 自動字幕
      description: |
        faster-whisper を用いて動画の音声トラックから AI で字幕を生成します。自動検出と 10 の明示的な言語に対応します。transcription 機能バンドルのインストールが必要です。
      security:
        - bearerAuth: []
      requestBody:
        required: true
        content:
          multipart/form-data:
            schema:
              type: object
              required:
                - file
              properties:
                file:
                  type: string
                  format: binary
                  description: Video file to transcribe
                settings:
                  type: string
                  description: |
                    JSON string with options:
                    - `language` (string, default "auto") - One of: auto, en, de, fr, es, zh, ja, ko, id, th, vi
                    - `format` (string, default "srt") - One of: srt, vtt
                clientJobId:
                  type: string
                  description: Client-provided job ID for SSE progress tracking
      responses:
        '202':
          description: Accepted for async processing. Track progress via SSE at /api/v1/jobs/{jobId}/progress.
          content:
            application/json:
              schema:
                type: object
                properties:
                  jobId:
                    type: string
                  async:
                    type: boolean
                    example: true
        '400':
          description: Invalid input
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Error'
        '401':
          description: Authentication required
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/UnauthorizedError'
        '422':
          description: Processing failed
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Error'
        '501':
          description: Feature not installed
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/FeatureNotInstalledError'
  /api/v1/tools/video/extract-audio:
    post:
      operationId: extractAudio
      tags:
        - Tools
      summary: 音声の抽出
      description: 動画から音声トラックを取り出します。
      security:
        - bearerAuth: []
      requestBody:
        required: true
        content:
          multipart/form-data:
            schema:
              type: object
              required:
                - file
              properties:
                file:
                  type: string
                  format: binary
                  description: Video file to extract audio from
                settings:
                  type: string
                  description: |
                    JSON string with options:
                    - `format` (string, default "mp3") - One of: mp3, wav, m4a, ogg
      responses:
        '200':
          description: Extracted audio file
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ToolResponse'
        '400':
          description: Invalid input
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Error'
        '401':
          description: Authentication required
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/UnauthorizedError'
  /api/v1/tools/audio/convert-audio:
    post:
      operationId: convertAudio
      tags:
        - Tools
      summary: 音声の変換
      description: MP3、WAV、OGG などの間で音声を変換します。
      security:
        - bearerAuth: []
      requestBody:
        required: true
        content:
          multipart/form-data:
            schema:
              type: object
              required:
                - file
              properties:
                file:
                  type: string
                  format: binary
                  description: Audio file to process
                settings:
                  type: string
                  description: |
                    JSON string with options:
                    - `format` (string, default "mp3") - One of: mp3, wav, ogg, flac, m4a
                    - `bitrateKbps` (integer 32-320, default 192) - Output bitrate in kbps
      responses:
        '200':
          description: Processed audio
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ToolResponse'
        '400':
          description: Invalid input
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Error'
        '401':
          description: Authentication required
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/UnauthorizedError'
  /api/v1/tools/audio/trim-audio:
    post:
      operationId: trimAudio
      tags:
        - Tools
      summary: 音声のトリミング
      description: 音声ファイルから一部分を切り出します。
      security:
        - bearerAuth: []
      requestBody:
        required: true
        content:
          multipart/form-data:
            schema:
              type: object
              required:
                - file
              properties:
                file:
                  type: string
                  format: binary
                  description: Audio file to process
                settings:
                  type: string
                  description: |
                    JSON string with options:
                    - `startS` (number, min 0, default 0) - Start time in seconds
                    - `endS` (number, required) - End time in seconds (must be after start)
      responses:
        '200':
          description: Processed audio
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ToolResponse'
        '400':
          description: Invalid input
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Error'
        '401':
          description: Authentication required
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/UnauthorizedError'
  /api/v1/tools/audio/volume-adjust:
    post:
      operationId: volumeAdjust
      tags:
        - Tools
      summary: 音量の調整
      description: 音声の音量を一定のゲインで上げ下げします。
      security:
        - bearerAuth: []
      requestBody:
        required: true
        content:
          multipart/form-data:
            schema:
              type: object
              required:
                - file
              properties:
                file:
                  type: string
                  format: binary
                  description: Audio file to process
                settings:
                  type: string
                  description: |
                    JSON string with options:
                    - `gainDb` (number -30 to 30, default 3) - Volume adjustment in decibels
      responses:
        '200':
          description: Processed audio
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ToolResponse'
        '400':
          description: Invalid input
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Error'
        '401':
          description: Authentication required
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/UnauthorizedError'
  /api/v1/tools/audio/normalize-audio:
    post:
      operationId: normalizeAudio
      tags:
        - Tools
      summary: 音声の正規化
      description: ラウドネスを放送標準レベル（EBU R128、-16 LUFS）に均します。
      security:
        - bearerAuth: []
      requestBody:
        required: true
        content:
          multipart/form-data:
            schema:
              type: object
              required:
                - file
              properties:
                file:
                  type: string
                  format: binary
                  description: Audio file to process
      responses:
        '200':
          description: Processed audio
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ToolResponse'
        '400':
          description: Invalid input
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Error'
        '401':
          description: Authentication required
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/UnauthorizedError'
  /api/v1/tools/audio/fade-audio:
    post:
      operationId: fadeAudio
      tags:
        - Tools
      summary: 音声のフェード
      description: 音声にフェードインとフェードアウトの効果を加えます。
      security:
        - bearerAuth: []
      requestBody:
        required: true
        content:
          multipart/form-data:
            schema:
              type: object
              required:
                - file
              properties:
                file:
                  type: string
                  format: binary
                  description: Audio file to process
                settings:
                  type: string
                  description: |
                    JSON string with options:
                    - `fadeInS` (number 0-30, default 1) - Fade-in duration in seconds
                    - `fadeOutS` (number 0-30, default 1) - Fade-out duration in seconds
      responses:
        '200':
          description: Processed audio
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ToolResponse'
        '400':
          description: Invalid input
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Error'
        '401':
          description: Authentication required
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/UnauthorizedError'
  /api/v1/tools/audio/reverse-audio:
    post:
      operationId: reverseAudio
      tags:
        - Tools
      summary: 音声の逆再生
      description: 音声を逆再生します。
      security:
        - bearerAuth: []
      requestBody:
        required: true
        content:
          multipart/form-data:
            schema:
              type: object
              required:
                - file
              properties:
                file:
                  type: string
                  format: binary
                  description: Audio file to process
      responses:
        '200':
          description: Processed audio
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ToolResponse'
        '400':
          description: Invalid input
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Error'
        '401':
          description: Authentication required
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/UnauthorizedError'
  /api/v1/tools/audio/audio-speed:
    post:
      operationId: audioSpeed
      tags:
        - Tools
      summary: 音声の速度
      description: 音声の再生を速くまたは遅くします。
      security:
        - bearerAuth: []
      requestBody:
        required: true
        content:
          multipart/form-data:
            schema:
              type: object
              required:
                - file
              properties:
                file:
                  type: string
                  format: binary
                  description: Audio file to process
                settings:
                  type: string
                  description: |
                    JSON string with options:
                    - `factor` (number 0.25-4, default 1.5) - Speed multiplier (0.25 = quarter speed, 4 = quadruple speed)
      responses:
        '200':
          description: Processed audio
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ToolResponse'
        '400':
          description: Invalid input
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Error'
        '401':
          description: Authentication required
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/UnauthorizedError'
  /api/v1/tools/audio/pitch-shift:
    post:
      operationId: pitchShift
      tags:
        - Tools
      summary: ピッチシフト
      description: 速度を変えずに半音単位でピッチを上げ下げします。
      security:
        - bearerAuth: []
      requestBody:
        required: true
        content:
          multipart/form-data:
            schema:
              type: object
              required:
                - file
              properties:
                file:
                  type: string
                  format: binary
                  description: Audio file to process
                settings:
                  type: string
                  description: |
                    JSON string with options:
                    - `semitones` (integer -12 to 12, default 3) - Semitones to shift (must be nonzero)
      responses:
        '200':
          description: Processed audio
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ToolResponse'
        '400':
          description: Invalid input
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Error'
        '401':
          description: Authentication required
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/UnauthorizedError'
  /api/v1/tools/audio/audio-channels:
    post:
      operationId: audioChannels
      tags:
        - Tools
      summary: 音声チャンネル
      description: モノラルとステレオの間で変換する、または左右のチャンネルを入れ替えます。
      security:
        - bearerAuth: []
      requestBody:
        required: true
        content:
          multipart/form-data:
            schema:
              type: object
              required:
                - file
              properties:
                file:
                  type: string
                  format: binary
                  description: Audio file to process
                settings:
                  type: string
                  description: |
                    JSON string with options:
                    - `mode` (string, required) - One of: stereo-to-mono, mono-to-stereo, swap
      responses:
        '200':
          description: Processed audio
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ToolResponse'
        '400':
          description: Invalid input
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Error'
        '401':
          description: Authentication required
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/UnauthorizedError'
  /api/v1/tools/audio/silence-removal:
    post:
      operationId: removeSilence
      tags:
        - Tools
      summary: 無音の除去
      description: 音声から無音部分を取り除きます。
      security:
        - bearerAuth: []
      requestBody:
        required: true
        content:
          multipart/form-data:
            schema:
              type: object
              required:
                - file
              properties:
                file:
                  type: string
                  format: binary
                  description: Audio file to process
                settings:
                  type: string
                  description: |
                    JSON string with options:
                    - `thresholdDb` (number -80 to -20, default -50) - Silence threshold in dB
                    - `minSilenceS` (number 0.1-5, default 0.5) - Minimum silence duration in seconds to remove
      responses:
        '200':
          description: Processed audio
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ToolResponse'
        '400':
          description: Invalid input
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Error'
        '401':
          description: Authentication required
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/UnauthorizedError'
  /api/v1/tools/audio/noise-reduction:
    post:
      operationId: reduceNoise
      tags:
        - Tools
      summary: ノイズ低減
      description: FFT ベースのノイズ除去で背景ノイズを軽減します。
      security:
        - bearerAuth: []
      requestBody:
        required: true
        content:
          multipart/form-data:
            schema:
              type: object
              required:
                - file
              properties:
                file:
                  type: string
                  format: binary
                  description: Audio file to process
                settings:
                  type: string
                  description: |
                    JSON string with options:
                    - `strength` (string, default "medium") - One of: light, medium, strong
      responses:
        '200':
          description: Processed audio
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ToolResponse'
        '400':
          description: Invalid input
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Error'
        '401':
          description: Authentication required
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/UnauthorizedError'
  /api/v1/tools/audio/merge-audio:
    post:
      operationId: mergeAudio
      tags:
        - Tools
      summary: 音声の結合
      description: 複数の音声ファイルを 1 つの連続したトラックに結合します。
      security:
        - bearerAuth: []
      requestBody:
        required: true
        content:
          multipart/form-data:
            schema:
              type: object
              required:
                - file
              properties:
                file:
                  type: array
                  items:
                    type: string
                    format: binary
                  description: Two or more audio files to merge (max 10)
                settings:
                  type: string
                  description: |
                    JSON string with options:
                    - `format` (string, default "mp3") - One of: mp3, wav, flac, m4a
      responses:
        '200':
          description: Processed audio
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ToolResponse'
        '400':
          description: Invalid input
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Error'
        '401':
          description: Authentication required
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/UnauthorizedError'
  /api/v1/tools/audio/split-audio:
    post:
      operationId: splitAudio
      tags:
        - Tools
      summary: 音声の分割
      description: |
        時間、均等分割、または無音検出によって音声を分割します。セグメントの ZIP アーカイブを指す downloadUrl を含む ToolResponse を返します。
      security:
        - bearerAuth: []
      requestBody:
        required: true
        content:
          multipart/form-data:
            schema:
              type: object
              required:
                - file
              properties:
                file:
                  type: string
                  format: binary
                  description: Audio file to split
                settings:
                  type: string
                  description: |
                    JSON string with options:
                    - `mode` (string, default "time") - One of: time, parts, silence
                    - `segmentS` (number 1-3600, default 60) - Segment length in seconds (mode=time)
                    - `parts` (integer 2-20, default 2) - Number of equal parts (mode=parts)
                    - `thresholdDb` (number -80 to -20, default -40) - Silence threshold in dB (mode=silence)
                    - `minSilenceS` (number 0.1-10, default 0.3) - Minimum silence gap in seconds (mode=silence)
      responses:
        '200':
          description: Processed audio (downloadUrl points to a ZIP archive)
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ToolResponse'
        '400':
          description: Invalid input
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Error'
        '401':
          description: Authentication required
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/UnauthorizedError'
  /api/v1/tools/audio/ringtone-maker:
    post:
      operationId: ringtoneMaker
      tags:
        - Tools
      summary: 着信音メーカー
      description: 任意の音声ファイルから着信音クリップ（.m4r）を作成します。
      security:
        - bearerAuth: []
      requestBody:
        required: true
        content:
          multipart/form-data:
            schema:
              type: object
              required:
                - file
              properties:
                file:
                  type: string
                  format: binary
                  description: Audio file to process
                settings:
                  type: string
                  description: |
                    JSON string with options:
                    - `startS` (number, min 0, default 0) - Start time in seconds
                    - `durationS` (number 1-30, default 30) - Clip duration in seconds
      responses:
        '200':
          description: Processed audio
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ToolResponse'
        '400':
          description: Invalid input
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Error'
        '401':
          description: Authentication required
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/UnauthorizedError'
  /api/v1/tools/audio/waveform-image:
    post:
      operationId: waveformImage
      tags:
        - Tools
      summary: 波形画像
      description: 音声ファイルから波形の可視化を PNG 画像として生成します。
      security:
        - bearerAuth: []
      requestBody:
        required: true
        content:
          multipart/form-data:
            schema:
              type: object
              required:
                - file
              properties:
                file:
                  type: string
                  format: binary
                  description: Audio file to process
                settings:
                  type: string
                  description: |
                    JSON string with options:
                    - `width` (integer 256-3840, default 1024) - Image width in pixels
                    - `height` (integer 64-1080, default 256) - Image height in pixels
                    - `color` (string, default "#4f46e5") - Waveform hex color (e.g. "#4f46e5")
      responses:
        '200':
          description: Processed image
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ToolResponse'
        '400':
          description: Invalid input
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Error'
        '401':
          description: Authentication required
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/UnauthorizedError'
  /api/v1/tools/audio/audio-metadata:
    post:
      operationId: audioMetadata
      tags:
        - Tools
      summary: 音声メタデータ
      description: |
        音声タグ（ID3）を表示、編集、または削除します。レスポンスには、標準の ToolResponse フィールドに加えて container、durationS、bitrateKbps、tags を含む `metadata` オブジェクトが含まれます。
      security:
        - bearerAuth: []
      requestBody:
        required: true
        content:
          multipart/form-data:
            schema:
              type: object
              required:
                - file
              properties:
                file:
                  type: string
                  format: binary
                  description: Audio file to process
                settings:
                  type: string
                  description: |
                    JSON string with options:
                    - `strip` (boolean, default false) - Remove all existing metadata
                    - `title` (string, optional, max 500) - Set the title tag
                    - `artist` (string, optional, max 500) - Set the artist tag
                    - `album` (string, optional, max 500) - Set the album tag
      responses:
        '200':
          description: Processed audio with metadata
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ToolResponse'
        '400':
          description: Invalid input
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Error'
        '401':
          description: Authentication required
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/UnauthorizedError'
  /api/v1/tools/audio/transcribe-audio:
    post:
      operationId: transcribeAudio
      tags:
        - Tools
      summary: 音声の文字起こし
      description: |
        faster-whisper を用いた AI による音声からテキストへの文字起こし。プレーンテキスト、SRT、VTT の字幕形式で出力します。transcription 機能バンドルのインストールが必要です。
      security:
        - bearerAuth: []
      requestBody:
        required: true
        content:
          multipart/form-data:
            schema:
              type: object
              required:
                - file
              properties:
                file:
                  type: string
                  format: binary
                  description: Audio file to transcribe
                settings:
                  type: string
                  description: |
                    JSON string with options:
                    - `language` (string, default "auto") - One of: auto, en, de, fr, es, zh, ja, ko, id, th, vi
                    - `outputFormat` (string, default "txt") - One of: txt, srt, vtt
                clientJobId:
                  type: string
                  description: Client-provided job ID for SSE progress tracking
      responses:
        '202':
          description: Accepted for async processing. Track progress via SSE at /api/v1/jobs/{jobId}/progress.
          content:
            application/json:
              schema:
                type: object
                properties:
                  jobId:
                    type: string
                  async:
                    type: boolean
                    example: true
        '400':
          description: Invalid input
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Error'
        '401':
          description: Authentication required
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/UnauthorizedError'
        '422':
          description: Processing failed
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Error'
        '501':
          description: Feature not installed
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/FeatureNotInstalledError'
  /api/v1/tools/pdf/merge-pdf:
    post:
      operationId: mergePdf
      tags:
        - Tools
      summary: PDF の結合
      description: 複数の PDF を 1 つに結合します。少なくとも 2 つのファイルが必要です。
      security:
        - bearerAuth: []
      requestBody:
        required: true
        content:
          multipart/form-data:
            schema:
              type: object
              required:
                - file
              properties:
                file:
                  type: array
                  items:
                    type: string
                    format: binary
                  description: Two or more PDF files to merge (max 20)
      responses:
        '200':
          description: Merged PDF
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ToolResponse'
        '400':
          description: Invalid input
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Error'
        '401':
          description: Authentication required
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/UnauthorizedError'
  /api/v1/tools/pdf/split-pdf:
    post:
      operationId: splitPdf
      tags:
        - Tools
      summary: PDF の分割
      description: |
        ページを抽出する、またはパーツに分割します。「range」モードでは選択したページを含む単一の PDF を返します。「every」モードでは N ページのチャンクに分割し、パーツを含む ZIP アーカイブを返します。
      security:
        - bearerAuth: []
      requestBody:
        required: true
        content:
          multipart/form-data:
            schema:
              type: object
              required:
                - file
              properties:
                file:
                  type: string
                  format: binary
                  description: PDF file to split
                settings:
                  type: string
                  description: |
                    JSON string with options:
                    - `mode` (string, default "range") - One of: range, every
                    - `range` (string, optional) - Page range in qpdf syntax, e.g. "1-5,8,10-z" (required when mode is "range")
                    - `everyN` (integer 1-500, optional) - Split into chunks of N pages (required when mode is "every")
      responses:
        '200':
          description: Split PDF or ZIP archive of parts
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ToolResponse'
        '400':
          description: Invalid input
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Error'
        '401':
          description: Authentication required
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/UnauthorizedError'
  /api/v1/tools/pdf/compress-pdf:
    post:
      operationId: compressPdf
      tags:
        - Tools
      summary: PDF の圧縮
      description: |
        埋め込み画像をダウンサンプリングして PDF のファイルサイズを縮小します。「quality」モードでは 1〜100 のスライダーで画像 DPI を制御します。「targetSize」モードでは二分探索で目標に収まる最高の DPI を見つけます。圧縮によってファイルが大きくなる場合は、元のバイトをそのまま返します。
      security:
        - bearerAuth: []
      requestBody:
        required: true
        content:
          multipart/form-data:
            schema:
              type: object
              required:
                - file
              properties:
                file:
                  type: string
                  format: binary
                  description: PDF file to compress
                settings:
                  type: string
                  description: |
                    JSON string with options:
                    - `mode` (string, default "quality") - One of: quality, targetSize
                    - `quality` (integer 1-100, optional) - Compression quality (higher = less compression). Used in quality mode
                    - `targetSizeKb` (number, optional) - Target file size in kilobytes. Used in targetSize mode
      responses:
        '200':
          description: Compressed PDF
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ToolResponse'
        '400':
          description: Invalid input
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Error'
        '401':
          description: Authentication required
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/UnauthorizedError'
  /api/v1/tools/pdf/rotate-pdf:
    post:
      operationId: rotatePdf
      tags:
        - Tools
      summary: PDF の回転
      description: PDF のページを 90、180、270 度回転します。
      security:
        - bearerAuth: []
      requestBody:
        required: true
        content:
          multipart/form-data:
            schema:
              type: object
              required:
                - file
              properties:
                file:
                  type: string
                  format: binary
                  description: PDF file to process
                settings:
                  type: string
                  description: |
                    JSON string with options:
                    - `angle` (integer, default 90) - One of: 90, 180, 270
                    - `range` (string, default "1-z") - Page range in qpdf syntax, e.g. "1-5,8" ("1-z" = all pages)
      responses:
        '200':
          description: Rotated PDF
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ToolResponse'
        '400':
          description: Invalid input
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Error'
        '401':
          description: Authentication required
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/UnauthorizedError'
  /api/v1/tools/pdf/extract-pages:
    post:
      operationId: extractPages
      tags:
        - Tools
      summary: ページの抽出
      description: 選択したページを新しい PDF に取り出します。
      security:
        - bearerAuth: []
      requestBody:
        required: true
        content:
          multipart/form-data:
            schema:
              type: object
              required:
                - file
              properties:
                file:
                  type: string
                  format: binary
                  description: PDF file to extract from
                settings:
                  type: string
                  description: |
                    JSON string with options:
                    - `range` (string, required) - Page range in qpdf syntax, e.g. "1-5,8,10-z"
      responses:
        '200':
          description: Extracted pages as a new PDF
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ToolResponse'
        '400':
          description: Invalid input
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Error'
        '401':
          description: Authentication required
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/UnauthorizedError'
  /api/v1/tools/pdf/remove-pages:
    post:
      operationId: removePages
      tags:
        - Tools
      summary: ページの削除
      description: PDF から特定のページを削除します。すべてのページを削除することはできません。
      security:
        - bearerAuth: []
      requestBody:
        required: true
        content:
          multipart/form-data:
            schema:
              type: object
              required:
                - file
              properties:
                file:
                  type: string
                  format: binary
                  description: PDF file to process
                settings:
                  type: string
                  description: |
                    JSON string with options:
                    - `pages` (string, required) - Page range to remove in qpdf syntax, e.g. "3,5-7"
      responses:
        '200':
          description: PDF with pages removed
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ToolResponse'
        '400':
          description: Invalid input
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Error'
        '401':
          description: Authentication required
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/UnauthorizedError'
  /api/v1/tools/pdf/organize-pdf:
    post:
      operationId: organizePdf
      tags:
        - Tools
      summary: PDF の整理
      description: 明示的なページ順でページを並べ替えます。
      security:
        - bearerAuth: []
      requestBody:
        required: true
        content:
          multipart/form-data:
            schema:
              type: object
              required:
                - file
              properties:
                file:
                  type: string
                  format: binary
                  description: PDF file to reorganize
                settings:
                  type: string
                  description: |
                    JSON string with options:
                    - `order` (string, required) - Desired page order in qpdf syntax, e.g. "3,1,2,5-z"
      responses:
        '200':
          description: Reorganized PDF
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ToolResponse'
        '400':
          description: Invalid input
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Error'
        '401':
          description: Authentication required
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/UnauthorizedError'
  /api/v1/tools/pdf/protect-pdf:
    post:
      operationId: protectPdf
      tags:
        - Tools
      summary: PDF の保護
      description: AES-256 暗号化によるパスワード保護を追加します。パスワードは監査ログから編集除去されます。
      security:
        - bearerAuth: []
      requestBody:
        required: true
        content:
          multipart/form-data:
            schema:
              type: object
              required:
                - file
              properties:
                file:
                  type: string
                  format: binary
                  description: PDF file to protect
                settings:
                  type: string
                  description: |
                    JSON string with options:
                    - `userPassword` (string 1-256 chars, required) - Password required to open the PDF
                    - `ownerPassword` (string 1-256 chars, optional) - Owner password for permissions; defaults to userPassword if omitted
      responses:
        '200':
          description: Encrypted PDF
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ToolResponse'
        '400':
          description: Invalid input
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Error'
        '401':
          description: Authentication required
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/UnauthorizedError'
  /api/v1/tools/pdf/unlock-pdf:
    post:
      operationId: unlockPdf
      tags:
        - Tools
      summary: PDF のロック解除
      description: PDF からパスワード保護を解除します。正しいパスワードを指定する必要があります。パスワードは監査ログから編集除去されます。
      security:
        - bearerAuth: []
      requestBody:
        required: true
        content:
          multipart/form-data:
            schema:
              type: object
              required:
                - file
              properties:
                file:
                  type: string
                  format: binary
                  description: Password-protected PDF file
                settings:
                  type: string
                  description: |
                    JSON string with options:
                    - `password` (string 1-256 chars, required) - Password to decrypt the PDF
      responses:
        '200':
          description: Decrypted PDF
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ToolResponse'
        '400':
          description: Invalid input
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Error'
        '401':
          description: Authentication required
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/UnauthorizedError'
  /api/v1/tools/pdf/repair-pdf:
    post:
      operationId: repairPdf
      tags:
        - Tools
      summary: PDF の修復
      description: 破損または壊れた PDF の修復を試みます。不正なファイルを許容するため、入力に対する構造検証はスキップされます。
      security:
        - bearerAuth: []
      requestBody:
        required: true
        content:
          multipart/form-data:
            schema:
              type: object
              required:
                - file
              properties:
                file:
                  type: string
                  format: binary
                  description: Damaged PDF file to repair
      responses:
        '200':
          description: Repaired PDF
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ToolResponse'
        '400':
          description: Invalid input
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Error'
        '401':
          description: Authentication required
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/UnauthorizedError'
  /api/v1/tools/pdf/linearize-pdf:
    post:
      operationId: linearizePdf
      tags:
        - Tools
      summary: PDF の Web 最適化
      description: 高速な Web 表示（プログレッシブダウンロード）のため PDF をリニアライズします。
      security:
        - bearerAuth: []
      requestBody:
        required: true
        content:
          multipart/form-data:
            schema:
              type: object
              required:
                - file
              properties:
                file:
                  type: string
                  format: binary
                  description: PDF file to linearize
      responses:
        '200':
          description: Linearized PDF
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ToolResponse'
        '400':
          description: Invalid input
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Error'
        '401':
          description: Authentication required
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/UnauthorizedError'
  /api/v1/tools/pdf/grayscale-pdf:
    post:
      operationId: grayscalePdf
      tags:
        - Tools
      summary: PDF のグレースケール化
      description: PDF 内のすべての色をグレースケールに変換します。
      security:
        - bearerAuth: []
      requestBody:
        required: true
        content:
          multipart/form-data:
            schema:
              type: object
              required:
                - file
              properties:
                file:
                  type: string
                  format: binary
                  description: PDF file to convert
      responses:
        '200':
          description: Grayscale PDF
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ToolResponse'
        '400':
          description: Invalid input
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Error'
        '401':
          description: Authentication required
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/UnauthorizedError'
  /api/v1/tools/pdf/pdfa-convert:
    post:
      operationId: pdfaConvert
      tags:
        - Tools
      summary: PDF/A 変換
      description: 長期保存のため PDF をアーカイブ用 PDF/A-2 形式に変換します。
      security:
        - bearerAuth: []
      requestBody:
        required: true
        content:
          multipart/form-data:
            schema:
              type: object
              required:
                - file
              properties:
                file:
                  type: string
                  format: binary
                  description: PDF file to convert
      responses:
        '200':
          description: PDF/A-2 compliant file
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ToolResponse'
        '400':
          description: Invalid input
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Error'
        '401':
          description: Authentication required
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/UnauthorizedError'
  /api/v1/tools/pdf/crop-pdf:
    post:
      operationId: cropPdf
      tags:
        - Tools
      summary: PDF の切り抜き
      description: すべてのページを均一な余白で切り抜きます。
      security:
        - bearerAuth: []
      requestBody:
        required: true
        content:
          multipart/form-data:
            schema:
              type: object
              required:
                - file
              properties:
                file:
                  type: string
                  format: binary
                  description: PDF file to crop
                settings:
                  type: string
                  description: |
                    JSON string with options:
                    - `margin` (number 0-2000, default 20) - Uniform crop margin in points
      responses:
        '200':
          description: Cropped PDF
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ToolResponse'
        '400':
          description: Invalid input
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Error'
        '401':
          description: Authentication required
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/UnauthorizedError'
  /api/v1/tools/pdf/nup-pdf:
    post:
      operationId: nupPdf
      tags:
        - Tools
      summary: PDF の N-up
      description: '1 枚のシートに複数ページを配置します（例: 2-up、4-up）。'
      security:
        - bearerAuth: []
      requestBody:
        required: true
        content:
          multipart/form-data:
            schema:
              type: object
              required:
                - file
              properties:
                file:
                  type: string
                  format: binary
                  description: PDF file to process
                settings:
                  type: string
                  description: |
                    JSON string with options:
                    - `perSheet` (integer, default 2) - One of: 2, 3, 4, 8, 9, 12, 16
      responses:
        '200':
          description: N-up PDF
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ToolResponse'
        '400':
          description: Invalid input
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Error'
        '401':
          description: Authentication required
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/UnauthorizedError'
  /api/v1/tools/pdf/booklet-pdf:
    post:
      operationId: bookletPdf
      tags:
        - Tools
      summary: PDF の小冊子
      description: 折り畳んで小冊子にするためにページを配置します（両面印刷用の面付け）。
      security:
        - bearerAuth: []
      requestBody:
        required: true
        content:
          multipart/form-data:
            schema:
              type: object
              required:
                - file
              properties:
                file:
                  type: string
                  format: binary
                  description: PDF file to process
                settings:
                  type: string
                  description: |
                    JSON string with options:
                    - `perSheet` (integer, default 2) - One of: 2, 4, 6, 8
      responses:
        '200':
          description: Booklet-imposed PDF
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ToolResponse'
        '400':
          description: Invalid input
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Error'
        '401':
          description: Authentication required
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/UnauthorizedError'
  /api/v1/tools/pdf/watermark-pdf:
    post:
      operationId: watermarkPdf
      tags:
        - Tools
      summary: PDF の透かし
      description: PDF のすべてのページにテキストの透かしを追加します。
      security:
        - bearerAuth: []
      requestBody:
        required: true
        content:
          multipart/form-data:
            schema:
              type: object
              required:
                - file
              properties:
                file:
                  type: string
                  format: binary
                  description: PDF file to watermark
                settings:
                  type: string
                  description: |
                    JSON string with options:
                    - `text` (string 1-200 chars, required) - Watermark text
                    - `position` (string, default "c") - One of: tl, tc, tr, l, c, r, bl, bc, br
                    - `fontSize` (integer 6-72, default 48) - Font size in points
                    - `opacity` (number 0.05-1, default 0.3) - Watermark opacity
                    - `rotation` (number -180 to 180, default 45) - Rotation angle in degrees
      responses:
        '200':
          description: Watermarked PDF
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ToolResponse'
        '400':
          description: Invalid input
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Error'
        '401':
          description: Authentication required
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/UnauthorizedError'
  /api/v1/tools/pdf/pdf-page-numbers:
    post:
      operationId: pdfPageNumbers
      tags:
        - Tools
      summary: PDF のページ番号
      description: PDF のすべてのページにページ番号（「Page N of M」）を追加します。
      security:
        - bearerAuth: []
      requestBody:
        required: true
        content:
          multipart/form-data:
            schema:
              type: object
              required:
                - file
              properties:
                file:
                  type: string
                  format: binary
                  description: PDF file to process
                settings:
                  type: string
                  description: |
                    JSON string with options:
                    - `position` (string, default "bc") - One of: bl, bc, br, tl, tc, tr
                    - `fontSize` (integer 6-24, default 10) - Font size in points
      responses:
        '200':
          description: PDF with page numbers
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ToolResponse'
        '400':
          description: Invalid input
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Error'
        '401':
          description: Authentication required
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/UnauthorizedError'
  /api/v1/tools/files/convert-document:
    post:
      operationId: convertDocument
      tags:
        - Tools
      summary: ドキュメントの変換
      description: Word、OpenDocument、RTF、プレーンテキストの各形式の間で変換します。
      security:
        - bearerAuth: []
      requestBody:
        required: true
        content:
          multipart/form-data:
            schema:
              type: object
              required:
                - file
              properties:
                file:
                  type: string
                  format: binary
                  description: Document file to convert (.docx, .doc, .odt, .rtf, .txt)
                settings:
                  type: string
                  description: |
                    JSON string with options:
                    - `format` (string, required) - One of: docx, odt, rtf, txt
                clientJobId:
                  type: string
                  description: Client-provided job ID for SSE progress tracking
      responses:
        '202':
          description: Accepted for async processing. Track progress via SSE at /api/v1/jobs/{jobId}/progress.
          content:
            application/json:
              schema:
                type: object
                properties:
                  jobId:
                    type: string
                  async:
                    type: boolean
                    example: true
        '400':
          description: Invalid input
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Error'
        '401':
          description: Authentication required
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/UnauthorizedError'
  /api/v1/tools/files/convert-presentation:
    post:
      operationId: convertPresentation
      tags:
        - Tools
      summary: プレゼンテーションの変換
      description: PowerPoint と OpenDocument のプレゼンテーション形式の間で変換します。
      security:
        - bearerAuth: []
      requestBody:
        required: true
        content:
          multipart/form-data:
            schema:
              type: object
              required:
                - file
              properties:
                file:
                  type: string
                  format: binary
                  description: Presentation file to convert (.pptx, .ppt, .odp)
                settings:
                  type: string
                  description: |
                    JSON string with options:
                    - `format` (string, required) - One of: pptx, odp
                clientJobId:
                  type: string
                  description: Client-provided job ID for SSE progress tracking
      responses:
        '202':
          description: Accepted for async processing. Track progress via SSE at /api/v1/jobs/{jobId}/progress.
          content:
            application/json:
              schema:
                type: object
                properties:
                  jobId:
                    type: string
                  async:
                    type: boolean
                    example: true
        '400':
          description: Invalid input
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Error'
        '401':
          description: Authentication required
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/UnauthorizedError'
  /api/v1/tools/files/convert-spreadsheet:
    post:
      operationId: convertSpreadsheet
      tags:
        - Tools
      summary: スプレッドシートの変換
      description: Excel、OpenDocument、CSV の各形式の間で変換します。複数シートのワークブックは最初のシートを CSV にエクスポートします。
      security:
        - bearerAuth: []
      requestBody:
        required: true
        content:
          multipart/form-data:
            schema:
              type: object
              required:
                - file
              properties:
                file:
                  type: string
                  format: binary
                  description: Spreadsheet file to convert (.xlsx, .xls, .ods, .csv)
                settings:
                  type: string
                  description: |
                    JSON string with options:
                    - `format` (string, required) - One of: xlsx, ods, csv
                clientJobId:
                  type: string
                  description: Client-provided job ID for SSE progress tracking
      responses:
        '202':
          description: Accepted for async processing. Track progress via SSE at /api/v1/jobs/{jobId}/progress.
          content:
            application/json:
              schema:
                type: object
                properties:
                  jobId:
                    type: string
                  async:
                    type: boolean
                    example: true
        '400':
          description: Invalid input
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Error'
        '401':
          description: Authentication required
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/UnauthorizedError'
  /api/v1/tools/files/excel-to-pdf:
    post:
      operationId: excelToPdf
      tags:
        - Tools
      summary: Excel から PDF
      description: スプレッドシートを PDF に変換します。幅の広いシートは複数ページに分割されることがあります。
      security:
        - bearerAuth: []
      requestBody:
        required: true
        content:
          multipart/form-data:
            schema:
              type: object
              required:
                - file
              properties:
                file:
                  type: string
                  format: binary
                  description: Spreadsheet file to convert (.xlsx, .xls, .ods, .csv)
                clientJobId:
                  type: string
                  description: Client-provided job ID for SSE progress tracking
      responses:
        '202':
          description: Accepted for async processing. Track progress via SSE at /api/v1/jobs/{jobId}/progress.
          content:
            application/json:
              schema:
                type: object
                properties:
                  jobId:
                    type: string
                  async:
                    type: boolean
                    example: true
        '400':
          description: Invalid input
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Error'
        '401':
          description: Authentication required
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/UnauthorizedError'
  /api/v1/tools/files/word-to-pdf:
    post:
      operationId: wordToPdf
      tags:
        - Tools
      summary: Word から PDF
      description: Word ドキュメントを PDF に変換します。
      security:
        - bearerAuth: []
      requestBody:
        required: true
        content:
          multipart/form-data:
            schema:
              type: object
              required:
                - file
              properties:
                file:
                  type: string
                  format: binary
                  description: Word document to convert (.docx, .doc, .odt, .rtf, .txt)
                clientJobId:
                  type: string
                  description: Client-provided job ID for SSE progress tracking
      responses:
        '202':
          description: Accepted for async processing. Track progress via SSE at /api/v1/jobs/{jobId}/progress.
          content:
            application/json:
              schema:
                type: object
                properties:
                  jobId:
                    type: string
                  async:
                    type: boolean
                    example: true
        '400':
          description: Invalid input
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Error'
        '401':
          description: Authentication required
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/UnauthorizedError'
  /api/v1/tools/files/powerpoint-to-pdf:
    post:
      operationId: powerpointToPdf
      tags:
        - Tools
      summary: PowerPoint から PDF
      description: プレゼンテーションを PDF に変換します。
      security:
        - bearerAuth: []
      requestBody:
        required: true
        content:
          multipart/form-data:
            schema:
              type: object
              required:
                - file
              properties:
                file:
                  type: string
                  format: binary
                  description: Presentation file to convert (.pptx, .ppt, .odp)
                clientJobId:
                  type: string
                  description: Client-provided job ID for SSE progress tracking
      responses:
        '202':
          description: Accepted for async processing. Track progress via SSE at /api/v1/jobs/{jobId}/progress.
          content:
            application/json:
              schema:
                type: object
                properties:
                  jobId:
                    type: string
                  async:
                    type: boolean
                    example: true
        '400':
          description: Invalid input
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Error'
        '401':
          description: Authentication required
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/UnauthorizedError'
  /api/v1/tools/files/html-to-pdf:
    post:
      operationId: htmlToPdf
      tags:
        - Tools
      summary: HTML から PDF
      description: HTML ファイルを PDF に変換します。プライバシー保護のためリモートリソースは無効化されます。
      security:
        - bearerAuth: []
      requestBody:
        required: true
        content:
          multipart/form-data:
            schema:
              type: object
              required:
                - file
              properties:
                file:
                  type: string
                  format: binary
                  description: HTML file to convert (.html, .htm)
                clientJobId:
                  type: string
                  description: Client-provided job ID for SSE progress tracking
      responses:
        '202':
          description: Accepted for async processing. Track progress via SSE at /api/v1/jobs/{jobId}/progress.
          content:
            application/json:
              schema:
                type: object
                properties:
                  jobId:
                    type: string
                  async:
                    type: boolean
                    example: true
        '400':
          description: Invalid input
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Error'
        '401':
          description: Authentication required
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/UnauthorizedError'
  /api/v1/tools/files/markdown-to-docx:
    post:
      operationId: markdownToDocx
      tags:
        - Tools
      summary: Markdown から Word
      description: Markdown ファイルを Word ドキュメント（DOCX）に変換します。
      security:
        - bearerAuth: []
      requestBody:
        required: true
        content:
          multipart/form-data:
            schema:
              type: object
              required:
                - file
              properties:
                file:
                  type: string
                  format: binary
                  description: Markdown file to convert (.md, .markdown)
      responses:
        '200':
          description: Processed document
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ToolResponse'
        '400':
          description: Invalid input
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Error'
        '401':
          description: Authentication required
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/UnauthorizedError'
  /api/v1/tools/files/markdown-to-html:
    post:
      operationId: markdownToHtml
      tags:
        - Tools
      summary: Markdown から HTML
      description: Markdown ファイルを単独の HTML ページに変換します。ソース内のリモート画像は出力でそのまま残されます。
      security:
        - bearerAuth: []
      requestBody:
        required: true
        content:
          multipart/form-data:
            schema:
              type: object
              required:
                - file
              properties:
                file:
                  type: string
                  format: binary
                  description: Markdown file to convert (.md, .markdown)
      responses:
        '200':
          description: Processed document
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ToolResponse'
        '400':
          description: Invalid input
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Error'
        '401':
          description: Authentication required
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/UnauthorizedError'
  /api/v1/tools/files/markdown-to-pdf:
    post:
      operationId: markdownToPdf
      tags:
        - Tools
      summary: Markdown から PDF
      description: Markdown ファイルをスタイル付きの PDF に変換します。プライバシー保護のためリモートリソースは無効化されます。
      security:
        - bearerAuth: []
      requestBody:
        required: true
        content:
          multipart/form-data:
            schema:
              type: object
              required:
                - file
              properties:
                file:
                  type: string
                  format: binary
                  description: Markdown file to convert (.md, .markdown)
                clientJobId:
                  type: string
                  description: Client-provided job ID for SSE progress tracking
      responses:
        '202':
          description: Accepted for async processing. Track progress via SSE at /api/v1/jobs/{jobId}/progress.
          content:
            application/json:
              schema:
                type: object
                properties:
                  jobId:
                    type: string
                  async:
                    type: boolean
                    example: true
        '400':
          description: Invalid input
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Error'
        '401':
          description: Authentication required
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/UnauthorizedError'
  /api/v1/tools/files/epub-convert:
    post:
      operationId: epubConvert
      tags:
        - Tools
      summary: EPUB の変換
      description: EPUB を PDF、DOCX、HTML、または Markdown に変換します。書籍内のリモートリソースは取得されません。
      security:
        - bearerAuth: []
      requestBody:
        required: true
        content:
          multipart/form-data:
            schema:
              type: object
              required:
                - file
              properties:
                file:
                  type: string
                  format: binary
                  description: EPUB file to convert (.epub)
                settings:
                  type: string
                  description: |
                    JSON string with options:
                    - `format` (string, required) - One of: pdf, docx, html, md
                clientJobId:
                  type: string
                  description: Client-provided job ID for SSE progress tracking
      responses:
        '202':
          description: Accepted for async processing. Track progress via SSE at /api/v1/jobs/{jobId}/progress.
          content:
            application/json:
              schema:
                type: object
                properties:
                  jobId:
                    type: string
                  async:
                    type: boolean
                    example: true
        '400':
          description: Invalid input
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Error'
        '401':
          description: Authentication required
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/UnauthorizedError'
  /api/v1/tools/files/to-epub:
    post:
      operationId: toEpub
      tags:
        - Tools
      summary: EPUB への変換
      description: Word、Markdown、HTML、プレーンテキストのファイルを EPUB に変換します。
      security:
        - bearerAuth: []
      requestBody:
        required: true
        content:
          multipart/form-data:
            schema:
              type: object
              required:
                - file
              properties:
                file:
                  type: string
                  format: binary
                  description: Document file to convert (.docx, .md, .html, .txt)
                clientJobId:
                  type: string
                  description: Client-provided job ID for SSE progress tracking
      responses:
        '202':
          description: Accepted for async processing. Track progress via SSE at /api/v1/jobs/{jobId}/progress.
          content:
            application/json:
              schema:
                type: object
                properties:
                  jobId:
                    type: string
                  async:
                    type: boolean
                    example: true
        '400':
          description: Invalid input
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Error'
        '401':
          description: Authentication required
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/UnauthorizedError'
  /api/v1/tools/pdf/flatten-pdf:
    post:
      operationId: flattenPdf
      tags:
        - Tools
      summary: PDF のフラット化
      description: フォームと注釈をページコンテンツに焼き込みます。
      security:
        - bearerAuth: []
      requestBody:
        required: true
        content:
          multipart/form-data:
            schema:
              type: object
              required:
                - file
              properties:
                file:
                  type: string
                  format: binary
                  description: PDF file to process (.pdf)
      responses:
        '200':
          description: Processed PDF
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ToolResponse'
        '400':
          description: Invalid input
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Error'
        '401':
          description: Authentication required
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/UnauthorizedError'
  /api/v1/tools/pdf/redact-pdf:
    post:
      operationId: redactPdf
      tags:
        - Tools
      summary: PDF の墨消し
      description: テキストの出現箇所を恒久的に削除します（検証済みの真の墨消し）。
      security:
        - bearerAuth: []
      requestBody:
        required: true
        content:
          multipart/form-data:
            schema:
              type: object
              required:
                - file
              properties:
                file:
                  type: string
                  format: binary
                  description: PDF file to process (.pdf)
                settings:
                  type: string
                  description: |
                    JSON string with options:
                    - `terms` (string[], required) - Text strings to redact (1-50 terms, each up to 200 characters)
                    - `caseSensitive` (boolean, default false) - Whether matching is case-sensitive
      responses:
        '200':
          description: Processed PDF
          content:
            application/json:
              schema:
                allOf:
                  - $ref: '#/components/schemas/ToolResponse'
                  - type: object
                    properties:
                      found:
                        type: integer
                        description: Number of redacted occurrences found
        '400':
          description: Invalid input
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Error'
        '401':
          description: Authentication required
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/UnauthorizedError'
  /api/v1/tools/pdf/pdf-to-text:
    post:
      operationId: pdfToText
      tags:
        - Tools
      summary: PDF からテキスト
      description: PDF からプレーンテキストを抽出します。
      security:
        - bearerAuth: []
      requestBody:
        required: true
        content:
          multipart/form-data:
            schema:
              type: object
              required:
                - file
              properties:
                file:
                  type: string
                  format: binary
                  description: PDF file to process (.pdf)
      responses:
        '200':
          description: Extracted text file
          content:
            application/json:
              schema:
                allOf:
                  - $ref: '#/components/schemas/ToolResponse'
                  - type: object
                    properties:
                      chars:
                        type: integer
                        description: Number of characters extracted
        '400':
          description: Invalid input
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Error'
        '401':
          description: Authentication required
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/UnauthorizedError'
  /api/v1/tools/pdf/pdf-to-word:
    post:
      operationId: pdfToWord
      tags:
        - Tools
      summary: PDF から Word
      description: テキストベースの PDF に最適です。スキャンされたページには OCR が必要です。
      security:
        - bearerAuth: []
      requestBody:
        required: true
        content:
          multipart/form-data:
            schema:
              type: object
              required:
                - file
              properties:
                file:
                  type: string
                  format: binary
                  description: PDF file to convert (.pdf)
                clientJobId:
                  type: string
                  description: Client-provided job ID for SSE progress tracking
      responses:
        '202':
          description: Accepted for async processing. Track progress via SSE at /api/v1/jobs/{jobId}/progress.
          content:
            application/json:
              schema:
                type: object
                properties:
                  jobId:
                    type: string
                  async:
                    type: boolean
                    example: true
        '400':
          description: Invalid input
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Error'
        '401':
          description: Authentication required
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/UnauthorizedError'
  /api/v1/tools/pdf/pdf-metadata:
    post:
      operationId: pdfMetadata
      tags:
        - Tools
      summary: PDF メタデータ
      description: PDF ドキュメントのメタデータを読み書きします。
      security:
        - bearerAuth: []
      requestBody:
        required: true
        content:
          multipart/form-data:
            schema:
              type: object
              required:
                - file
              properties:
                file:
                  type: string
                  format: binary
                  description: PDF file to process (.pdf)
                settings:
                  type: string
                  description: |
                    JSON string with options:
                    - `title` (string, optional, max 500) - Document title
                    - `author` (string, optional, max 500) - Document author
                    - `subject` (string, optional, max 500) - Document subject
                    - `keywords` (string, optional, max 500) - Document keywords
      responses:
        '200':
          description: Processed PDF with updated metadata
          content:
            application/json:
              schema:
                allOf:
                  - $ref: '#/components/schemas/ToolResponse'
                  - type: object
                    properties:
                      metadata:
                        type: object
                        description: The resulting PDF metadata fields
        '400':
          description: Invalid input
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Error'
        '401':
          description: Authentication required
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/UnauthorizedError'
  /api/v1/tools/pdf/ocr-pdf:
    post:
      operationId: ocrPdf
      tags:
        - Tools
      summary: PDF OCR
      description: |-
        スキャンした PDF ドキュメントからページごとにテキストを抽出します。 Fast は、
        組み込みの Tesseract ランタイム。 Balanced および Best は、オプションの署名付き
        固定された PP-OCR ONNX モデルを使用した RapidOCR ランタイム。 明示的な階層リクエスト
        決して黙ってダウングレードしないでください。 リクエストでは最大 50 ページを選択できます。
      security:
        - bearerAuth: []
      requestBody:
        required: true
        content:
          multipart/form-data:
            schema:
              type: object
              required:
                - file
              properties:
                file:
                  type: string
                  format: binary
                  maxLength: 536870912
                  description: PDF file to process (.pdf)
                settings:
                  type: string
                  description: |
                    JSON string with options:
                    - `quality` (string, optional) - One of: fast, balanced, best. When both quality and engine are omitted, uses the best available tier in this order: best, balanced, fast. Korean never selects fast; it uses best, then balanced, or returns an accurate-runtime install/compatibility error
                    - `language` (string, default "auto") - One of: auto, en, de, fr, es, zh, ja, ko. Korean is unsupported by fast and the legacy tesseract alias
                    - `pages` (string, default "all") - Page selection, e.g. "all", "1-3", "1,3,5"
                    - `enhance` (boolean, optional) - Improve local contrast. Fast applies it directly; accurate tiers retain the variant only when calibrated scoring improves the result. Defaults to true for Best and false for Fast/Balanced
                    - `engine` (string, deprecated) - Compatibility alias: tesseract maps to fast; the legacy paddleocr value maps to balanced but does not load PaddlePaddle
                clientJobId:
                  type: string
                  description: Client-provided job ID for SSE progress tracking
      responses:
        '202':
          description: Accepted for async processing. Track progress via SSE at /api/v1/jobs/{jobId}/progress.
          content:
            application/json:
              schema:
                type: object
                required:
                  - jobId
                  - async
                properties:
                  jobId:
                    type: string
                  progressJobId:
                    type: string
                  artifactJobId:
                    type: string
                  async:
                    type: boolean
                    const: true
        '400':
          description: Invalid input
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Error'
        '401':
          description: Authentication required
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/UnauthorizedError'
        '413':
          description: Encoded PDF exceeds the hard 512 MiB safety limit
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Error'
        '422':
          description: Processing failed
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Error'
        '501':
          description: Requested Balanced/Best runtime is not installed or is incompatible
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/FeatureNotInstalledError'
        '503':
          description: PDF OCR could not be queued
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Error'
  /api/v1/tools/files/chart-maker:
    post:
      operationId: chartMaker
      tags:
        - Tools
      summary: チャートメーカー
      description: CSV または JSON データから棒グラフ、折れ線グラフ、円グラフを作成します。PNG 画像を返します。
      security:
        - bearerAuth: []
      requestBody:
        required: true
        content:
          multipart/form-data:
            schema:
              type: object
              required:
                - file
              properties:
                file:
                  type: string
                  format: binary
                  description: CSV or JSON data file
                settings:
                  type: string
                  description: |
                    JSON string with options:
                    - `kind` (string, default "bar") - One of: bar, line, pie
                    - `title` (string, optional) - Chart title (max 120 characters)
                    - `width` (integer 320-2048, default 960) - Chart width in pixels
                    - `height` (integer 240-1536, default 540) - Chart height in pixels
      responses:
        '200':
          description: Generated chart image
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ToolResponse'
        '400':
          description: Invalid input
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Error'
        '401':
          description: Authentication required
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/UnauthorizedError'
  /api/v1/tools/files/csv-excel:
    post:
      operationId: csvExcel
      tags:
        - Tools
      summary: CSV から Excel
      description: CSV と Excel（XLSX）の間で双方向に変換します。CSV または TSV ファイルをアップロードすると XLSX が、XLSX ファイルをアップロードすると CSV が得られます。
      security:
        - bearerAuth: []
      requestBody:
        required: true
        content:
          multipart/form-data:
            schema:
              type: object
              required:
                - file
              properties:
                file:
                  type: string
                  format: binary
                  description: CSV, TSV, or XLSX file to convert
                settings:
                  type: string
                  description: |
                    JSON string with options:
                    - `sheet` (integer, min 1, default 1) - Worksheet number to export when converting from XLSX
      responses:
        '200':
          description: Converted file
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ToolResponse'
        '400':
          description: Invalid input
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Error'
        '401':
          description: Authentication required
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/UnauthorizedError'
  /api/v1/tools/files/csv-json:
    post:
      operationId: csvJson
      tags:
        - Tools
      summary: CSV から JSON
      description: CSV と JSON の間で双方向に変換します。CSV または TSV ファイルをアップロードすると JSON が、オブジェクトの JSON 配列をアップロードすると CSV が得られます。
      security:
        - bearerAuth: []
      requestBody:
        required: true
        content:
          multipart/form-data:
            schema:
              type: object
              required:
                - file
              properties:
                file:
                  type: string
                  format: binary
                  description: CSV, TSV, or JSON file to convert
                settings:
                  type: string
                  description: |
                    JSON string with options:
                    - `pretty` (boolean, default true) - Pretty-print JSON output with indentation
      responses:
        '200':
          description: Converted file
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ToolResponse'
        '400':
          description: Invalid input
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Error'
        '401':
          description: Authentication required
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/UnauthorizedError'
  /api/v1/tools/files/json-xml:
    post:
      operationId: jsonXml
      tags:
        - Tools
      summary: JSON から XML
      description: JSON と XML の間で双方向に変換します。JSON ファイルをアップロードすると XML が、XML ファイルをアップロードすると JSON が得られます。
      security:
        - bearerAuth: []
      requestBody:
        required: true
        content:
          multipart/form-data:
            schema:
              type: object
              required:
                - file
              properties:
                file:
                  type: string
                  format: binary
                  description: JSON or XML file to convert
                settings:
                  type: string
                  description: |
                    JSON string with options:
                    - `pretty` (boolean, default true) - Pretty-print output with indentation
      responses:
        '200':
          description: Converted file
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ToolResponse'
        '400':
          description: Invalid input
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Error'
        '401':
          description: Authentication required
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/UnauthorizedError'
  /api/v1/tools/files/split-csv:
    post:
      operationId: splitCsv
      tags:
        - Tools
      summary: CSV の分割
      description: CSV ファイルを行数によって小さなファイルに分割します。パーツを含む ZIP アーカイブを返します。
      security:
        - bearerAuth: []
      requestBody:
        required: true
        content:
          multipart/form-data:
            schema:
              type: object
              required:
                - file
              properties:
                file:
                  type: string
                  format: binary
                  description: CSV or TSV file to split
                settings:
                  type: string
                  description: |
                    JSON string with options:
                    - `rowsPerFile` (integer 1-1000000, default 1000) - Number of data rows per output file
                    - `keepHeader` (boolean, default true) - Repeat the header row in each output file
      responses:
        '200':
          description: ZIP archive containing split CSV parts
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ToolResponse'
        '400':
          description: Invalid input
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Error'
        '401':
          description: Authentication required
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/UnauthorizedError'
  /api/v1/tools/files/merge-csvs:
    post:
      operationId: mergeCsvs
      tags:
        - Tools
      summary: CSV の結合
      description: 一致する列を持つ複数の CSV または TSV ファイルを 1 つの結合ファイルにまとめます。すべてのファイルは同じ列ヘッダーを持つ必要があります。
      security:
        - bearerAuth: []
      requestBody:
        required: true
        content:
          multipart/form-data:
            schema:
              type: object
              required:
                - file
              properties:
                file:
                  type: array
                  items:
                    type: string
                    format: binary
                  description: Two or more CSV or TSV files to merge (2-20 files)
      responses:
        '200':
          description: Merged CSV file
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ToolResponse'
        '400':
          description: Invalid input
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Error'
        '401':
          description: Authentication required
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/UnauthorizedError'
  /api/v1/tools/files/yaml-json:
    post:
      operationId: yamlJson
      tags:
        - Tools
      summary: YAML / JSON
      description: YAML と JSON の間で双方向に変換します。YAML ファイルをアップロードすると JSON が、JSON ファイルをアップロードすると YAML が得られます。
      security:
        - bearerAuth: []
      requestBody:
        required: true
        content:
          multipart/form-data:
            schema:
              type: object
              required:
                - file
              properties:
                file:
                  type: string
                  format: binary
                  description: YAML (.yaml, .yml) or JSON file to convert
      responses:
        '200':
          description: Converted file
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ToolResponse'
        '400':
          description: Invalid input
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Error'
        '401':
          description: Authentication required
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/UnauthorizedError'
  /api/v1/tools/files/xml-to-csv:
    post:
      operationId: xmlToCsv
      tags:
        - Tools
      summary: XML から CSV
      description: XML ファイルから繰り返し要素を CSV テーブルに抽出します。XML ツリー内で最初のオブジェクト配列を自動的に見つけます。
      security:
        - bearerAuth: []
      requestBody:
        required: true
        content:
          multipart/form-data:
            schema:
              type: object
              required:
                - file
              properties:
                file:
                  type: string
                  format: binary
                  description: XML file to convert
      responses:
        '200':
          description: Extracted CSV file
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ToolResponse'
        '400':
          description: Invalid input
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Error'
        '401':
          description: Authentication required
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/UnauthorizedError'
  /api/v1/tools/files/create-zip:
    post:
      operationId: createZip
      tags:
        - Tools
      summary: ZIP の作成
      description: 複数のファイルを 1 つの ZIP アーカイブにまとめます。重複するファイル名は自動的に重複排除されます。
      security:
        - bearerAuth: []
      requestBody:
        required: true
        content:
          multipart/form-data:
            schema:
              type: object
              required:
                - file
              properties:
                file:
                  type: array
                  items:
                    type: string
                    format: binary
                  description: Two or more files to bundle (2-50 files, any type)
      responses:
        '200':
          description: ZIP archive
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ToolResponse'
        '400':
          description: Invalid input
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Error'
        '401':
          description: Authentication required
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/UnauthorizedError'
  /api/v1/tools/files/extract-zip:
    post:
      operationId: extractZip
      tags:
        - Tools
      summary: ZIP の展開
      description: ZIP アーカイブからファイルを安全に展開します（ボム対策付き）。単一ファイルのアーカイブはそのファイルを直接返し、複数ファイルのアーカイブはフラットな ZIP を返します。
      security:
        - bearerAuth: []
      requestBody:
        required: true
        content:
          multipart/form-data:
            schema:
              type: object
              required:
                - file
              properties:
                file:
                  type: string
                  format: binary
                  description: ZIP archive to extract
      responses:
        '200':
          description: Extracted file or repackaged ZIP archive
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ToolResponse'
        '400':
          description: Invalid input
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Error'
        '401':
          description: Authentication required
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/UnauthorizedError'
  /api/v1/tools/image/background-replace:
    post:
      operationId: backgroundReplace
      tags:
        - Tools
      summary: 背景の置換
      description: |
        AI による背景の置換。被写体の背景を除去し、単色またはグラデーションに合成します。background-removal 機能バンドルのインストールが必要です。
      security:
        - bearerAuth: []
      requestBody:
        required: true
        content:
          multipart/form-data:
            schema:
              type: object
              required:
                - file
              properties:
                file:
                  type: string
                  format: binary
                  description: Image file to process
                settings:
                  type: string
                  description: |
                    JSON string with options:
                    - `backgroundType` (string, default "color") - One of: color, gradient
                    - `color` (string, default "#ffffff") - Background hex color (when backgroundType is color)
                    - `gradientColor1` (string, optional) - First gradient hex color
                    - `gradientColor2` (string, optional) - Second gradient hex color
                    - `gradientAngle` (integer 0-360, default 180) - Gradient angle in degrees
                    - `feather` (integer 0-20, default 0) - Edge feathering radius
                    - `format` (string, default "png") - One of: png, webp
                clientJobId:
                  type: string
                  description: Client-provided job ID for SSE progress tracking
      responses:
        '202':
          description: Accepted for async processing. Track progress via SSE at /api/v1/jobs/{jobId}/progress.
          content:
            application/json:
              schema:
                type: object
                properties:
                  jobId:
                    type: string
                  async:
                    type: boolean
                    example: true
        '400':
          description: Invalid input
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Error'
        '401':
          description: Authentication required
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/UnauthorizedError'
        '422':
          description: Processing failed
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Error'
        '501':
          description: Feature not installed
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/FeatureNotInstalledError'
  /api/v1/tools/image/blur-background:
    post:
      operationId: blurBackground
      tags:
        - Tools
      summary: 背景のぼかし
      description: |
        AI による背景のぼかし。背景を除去し、元画像をぼかし、鮮明な被写体を上に合成します。background-removal 機能バンドルのインストールが必要です。
      security:
        - bearerAuth: []
      requestBody:
        required: true
        content:
          multipart/form-data:
            schema:
              type: object
              required:
                - file
              properties:
                file:
                  type: string
                  format: binary
                  description: Image file to process
                settings:
                  type: string
                  description: |
                    JSON string with options:
                    - `intensity` (integer 1-100, default 50) - Blur intensity
                    - `feather` (integer 0-20, default 0) - Edge feathering radius
                    - `format` (string, default "png") - One of: png, webp
                clientJobId:
                  type: string
                  description: Client-provided job ID for SSE progress tracking
      responses:
        '202':
          description: Accepted for async processing. Track progress via SSE at /api/v1/jobs/{jobId}/progress.
          content:
            application/json:
              schema:
                type: object
                properties:
                  jobId:
                    type: string
                  async:
                    type: boolean
                    example: true
        '400':
          description: Invalid input
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Error'
        '401':
          description: Authentication required
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/UnauthorizedError'
        '422':
          description: Processing failed
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Error'
        '501':
          description: Feature not installed
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/FeatureNotInstalledError'
  /api/v1/tools/image/barcode-generate:
    post:
      operationId: generateBarcode
      tags:
        - Tools
      summary: バーコード生成
      description: テキストからバーコード画像を生成します。このツールは multipart ではなく JSON ボディを受け付けます。
      security:
        - bearerAuth: []
      requestBody:
        required: true
        content:
          application/json:
            schema:
              type: object
              required:
                - text
              properties:
                text:
                  type: string
                  minLength: 1
                  maxLength: 256
                  description: Text to encode in the barcode
                type:
                  type: string
                  enum:
                    - code128
                    - ean13
                    - upca
                    - code39
                    - itf14
                    - datamatrix
                  default: code128
                  description: Barcode format
                scale:
                  type: integer
                  minimum: 1
                  maximum: 8
                  default: 3
                  description: Image scale factor
                includeText:
                  type: boolean
                  default: true
                  description: Whether to render the text below the barcode
      responses:
        '200':
          description: Generated barcode
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ToolResponse'
        '400':
          description: Invalid input
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Error'
        '401':
          description: Authentication required
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/UnauthorizedError'
  /api/v1/tools/image/circle-crop:
    post:
      operationId: circleCrop
      tags:
        - Tools
      summary: 円形切り抜き
      description: 画像を中央の円形に切り抜き、角を透明にします。枠線（オプション）と調整可能なズーム/オフセットに対応します。
      security:
        - bearerAuth: []
      requestBody:
        required: true
        content:
          multipart/form-data:
            schema:
              type: object
              required:
                - file
              properties:
                file:
                  type: string
                  format: binary
                  description: Image file to process
                settings:
                  type: string
                  description: |
                    JSON string with options:
                    - `zoom` (number 1-5, default 1) - Zoom factor (>=1 crops tighter)
                    - `offsetX` (number 0-1, default 0.5) - Horizontal center position
                    - `offsetY` (number 0-1, default 0.5) - Vertical center position
                    - `borderWidth` (integer 0-200, default 0) - Border width in pixels
                    - `borderColor` (string, default "#ffffff") - Border hex color
                    - `background` (string, default "transparent") - "transparent" or a hex color for corners
                    - `outputSize` (integer 16-4096, optional) - Final square dimension in pixels
      responses:
        '200':
          description: Processed image
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ToolResponse'
        '400':
          description: Invalid input
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Error'
        '401':
          description: Authentication required
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/UnauthorizedError'
  /api/v1/tools/image/duotone:
    post:
      operationId: duotone
      tags:
        - Tools
      summary: デュオトーン
      description: カスタムのシャドウ色とハイライト色で 2 色のデュオトーン効果を適用します。
      security:
        - bearerAuth: []
      requestBody:
        required: true
        content:
          multipart/form-data:
            schema:
              type: object
              required:
                - file
              properties:
                file:
                  type: string
                  format: binary
                  description: Image file to process
                settings:
                  type: string
                  description: |
                    JSON string with options:
                    - `shadow` (string, default "#1e3a8a") - Shadow hex color
                    - `highlight` (string, default "#fbbf24") - Highlight hex color
                    - `intensity` (integer 0-100, default 100) - Effect intensity (0 = original, 100 = full duotone)
      responses:
        '200':
          description: Processed image
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ToolResponse'
        '400':
          description: Invalid input
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Error'
        '401':
          description: Authentication required
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/UnauthorizedError'
  /api/v1/tools/image/image-pad:
    post:
      operationId: imagePad
      tags:
        - Tools
      summary: 画像のパディング
      description: 単色、透明、またはぼかし背景で画像を目標のアスペクト比にパディングします。
      security:
        - bearerAuth: []
      requestBody:
        required: true
        content:
          multipart/form-data:
            schema:
              type: object
              required:
                - file
              properties:
                file:
                  type: string
                  format: binary
                  description: Image file to process
                settings:
                  type: string
                  description: |
                    JSON string with options:
                    - `target` (string, default "1:1") - One of: 16:9, 9:16, 1:1, 4:3, 3:4, custom
                    - `ratioW` (integer 1-100, default 1) - Custom ratio width (when target is custom)
                    - `ratioH` (integer 1-100, default 1) - Custom ratio height (when target is custom)
                    - `background` (string, default "color") - One of: color, transparent, blur
                    - `color` (string, default "#ffffff") - Background hex color (when background is color)
                    - `padding` (integer 0-50, default 0) - Extra padding as percentage of canvas
      responses:
        '200':
          description: Processed image
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ToolResponse'
        '400':
          description: Invalid input
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Error'
        '401':
          description: Authentication required
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/UnauthorizedError'
  /api/v1/tools/image/pixelate:
    post:
      operationId: pixelate
      tags:
        - Tools
      summary: モザイク化
      description: 画像全体または特定の矩形領域にモザイク効果を適用します。
      security:
        - bearerAuth: []
      requestBody:
        required: true
        content:
          multipart/form-data:
            schema:
              type: object
              required:
                - file
              properties:
                file:
                  type: string
                  format: binary
                  description: Image file to process
                settings:
                  type: string
                  description: |
                    JSON string with options:
                    - `blockSize` (integer 2-128, default 12) - Pixel block size
                    - `region` (object, optional) - Restrict pixelation to a rectangle:
                      - `left` (integer >= 0) - Left offset in pixels
                      - `top` (integer >= 0) - Top offset in pixels
                      - `width` (integer >= 1) - Region width in pixels
                      - `height` (integer >= 1) - Region height in pixels
      responses:
        '200':
          description: Processed image
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ToolResponse'
        '400':
          description: Invalid input
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Error'
        '401':
          description: Authentication required
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/UnauthorizedError'
  /api/v1/tools/image/vignette:
    post:
      operationId: vignette
      tags:
        - Tools
      summary: ビネット
      description: 強さ、色、半径、柔らかさ、丸み、中心位置を調整できるビネット効果を追加します。
      security:
        - bearerAuth: []
      requestBody:
        required: true
        content:
          multipart/form-data:
            schema:
              type: object
              required:
                - file
              properties:
                file:
                  type: string
                  format: binary
                  description: Image file to process
                settings:
                  type: string
                  description: |
                    JSON string with options:
                    - `strength` (number 0.1-1, default 0.5) - Vignette opacity
                    - `color` (string, default "#000000") - Vignette hex color
                    - `radius` (integer 0-100, default 70) - Outer radius as percentage of half-diagonal
                    - `softness` (integer 0-100, default 50) - Feather softness (higher = more gradual)
                    - `roundness` (integer 0-100, default 100) - 100 = circle, 0 = ellipse matching image aspect ratio
                    - `centerX` (integer 0-100, default 50) - Horizontal center position as percentage
                    - `centerY` (integer 0-100, default 50) - Vertical center position as percentage
      responses:
        '200':
          description: Processed image
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ToolResponse'
        '400':
          description: Invalid input
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Error'
        '401':
          description: Authentication required
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/UnauthorizedError'
  /api/v1/tools/image/gif-webp:
    post:
      operationId: gifWebp
      tags:
        - Tools
      summary: GIF/WebP 変換
      description: アニメーション GIF と WebP を相互に変換し、すべてのフレームを保持します。.gif と .webp の入力のみを受け付けます。
      security:
        - bearerAuth: []
      requestBody:
        required: true
        content:
          multipart/form-data:
            schema:
              type: object
              required:
                - file
              properties:
                file:
                  type: string
                  format: binary
                  description: GIF or WebP file to convert
                settings:
                  type: string
                  description: |
                    JSON string with options:
                    - `quality` (integer 1-100, default 80) - Output quality for WebP encoding
                    - `lossless` (boolean, default false) - Use lossless WebP compression
                    - `resizePercent` (integer 10-100, default 100) - Scale the output by percentage
      responses:
        '200':
          description: Converted animation
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ToolResponse'
        '400':
          description: Invalid input
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Error'
        '401':
          description: Authentication required
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/UnauthorizedError'
  /api/v1/tools/image/histogram:
    post:
      operationId: histogram
      tags:
        - Tools
      summary: ヒストグラム
      description: |
        画像から RGB ヒストグラムチャートを生成します。PNG ヒストグラム画像に加えて、チャンネルごとの統計と生の 256 ビンヒストグラムデータをレスポンス JSON で返します。
      security:
        - bearerAuth: []
      requestBody:
        required: true
        content:
          multipart/form-data:
            schema:
              type: object
              required:
                - file
              properties:
                file:
                  type: string
                  format: binary
                  description: Image file to analyze
                settings:
                  type: string
                  description: |
                    JSON string with options:
                    - `scale` (string, default "linear") - One of: linear, log
      responses:
        '200':
          description: Histogram image and channel statistics
          content:
            application/json:
              schema:
                allOf:
                  - $ref: '#/components/schemas/ToolResponse'
                  - type: object
                    properties:
                      bins:
                        type: object
                        description: Raw 256-bin histogram arrays per channel
                        properties:
                          r:
                            type: array
                            items:
                              type: integer
                          g:
                            type: array
                            items:
                              type: integer
                          b:
                            type: array
                            items:
                              type: integer
                          lum:
                            type: array
                            items:
                              type: integer
                      stats:
                        type: object
                        description: Per-channel statistics
                        properties:
                          r:
                            type: object
                            properties:
                              mean:
                                type: integer
                              median:
                                type: integer
                              stdev:
                                type: number
                          g:
                            type: object
                            properties:
                              mean:
                                type: integer
                              median:
                                type: integer
                              stdev:
                                type: number
                          b:
                            type: object
                            properties:
                              mean:
                                type: integer
                              median:
                                type: integer
                              stdev:
                                type: number
                          lum:
                            type: object
                            properties:
                              mean:
                                type: integer
                              median:
                                type: integer
                              stdev:
                                type: number
                      mean:
                        type: object
                        description: Backward-compatible mean values
                        properties:
                          r:
                            type: integer
                          g:
                            type: integer
                          b:
                            type: integer
                      max:
                        type: object
                        description: Backward-compatible max bin values
                        properties:
                          r:
                            type: integer
                          g:
                            type: integer
                          b:
                            type: integer
        '400':
          description: Invalid input
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Error'
        '401':
          description: Authentication required
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/UnauthorizedError'
  /api/v1/tools/image/lqip-placeholder:
    post:
      operationId: lqipPlaceholder
      tags:
        - Tools
      summary: LQIP プレースホルダー
      description: |
        低品質の小さな画像プレースホルダーを生成します。小さな画像ファイルに加えて、base64 data URI、そのまま使える HTML img タグ、CSS background スニペットをレスポンス JSON で返します。
      security:
        - bearerAuth: []
      requestBody:
        required: true
        content:
          multipart/form-data:
            schema:
              type: object
              required:
                - file
              properties:
                file:
                  type: string
                  format: binary
                  description: Image file to process
                settings:
                  type: string
                  description: |
                    JSON string with options:
                    - `width` (integer 4-64, default 16) - Target width in pixels
                    - `blur` (number 0-20, default 2) - Blur radius (for blur strategy)
                    - `strategy` (string, default "blur") - One of: blur, pixelate, solid
                    - `format` (string, default "webp") - One of: webp, png, jpeg
                    - `quality` (integer 1-100, default 50) - Output quality
      responses:
        '200':
          description: LQIP placeholder image and data URI
          content:
            application/json:
              schema:
                allOf:
                  - $ref: '#/components/schemas/ToolResponse'
                  - type: object
                    properties:
                      dataUri:
                        type: string
                        description: Complete data URI (data:mime;base64,...)
                      width:
                        type: integer
                        description: Placeholder width in pixels
                      height:
                        type: integer
                        description: Placeholder height in pixels
                      bytes:
                        type: integer
                        description: Placeholder file size in bytes
                      strategy:
                        type: string
                        description: Strategy used (blur, pixelate, or solid)
                      html:
                        type: string
                        description: Ready-to-use HTML img tag
                      css:
                        type: string
                        description: Ready-to-use CSS background-image snippet
        '400':
          description: Invalid input
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Error'
        '401':
          description: Authentication required
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/UnauthorizedError'
  /api/v1/tools/image/sprite-sheet:
    post:
      operationId: spriteSheet
      tags:
        - Tools
      summary: スプライトシート
      description: |
        複数の画像を 1 つのスプライトシートグリッドにまとめます。2〜64 枚の画像を受け付けます。各画像は最初の画像の寸法に合わせてリサイズされます。スプライトシート画像とフレーム座標のメタデータを返します。
      security:
        - bearerAuth: []
      requestBody:
        required: true
        content:
          multipart/form-data:
            schema:
              type: object
              required:
                - file
              properties:
                file:
                  type: array
                  items:
                    type: string
                    format: binary
                  description: Two or more image files to combine (max 64)
                settings:
                  type: string
                  description: |
                    JSON string with options:
                    - `columns` (integer 1-16, default 4) - Number of columns in the grid
                    - `padding` (integer 0-64, default 0) - Padding between cells in pixels
                    - `background` (string, default "#ffffff") - Background hex color
                    - `format` (string, default "png") - One of: png, webp, jpeg
                    - `quality` (integer 1-100, default 90) - Output quality
      responses:
        '200':
          description: Sprite sheet image with frame metadata
          content:
            application/json:
              schema:
                allOf:
                  - $ref: '#/components/schemas/ToolResponse'
                  - type: object
                    properties:
                      frames:
                        type: array
                        description: Per-frame coordinates in the sprite sheet
                        items:
                          type: object
                          properties:
                            index:
                              type: integer
                            left:
                              type: integer
                            top:
                              type: integer
                            width:
                              type: integer
                            height:
                              type: integer
                      cols:
                        type: integer
                        description: Number of columns
                      rows:
                        type: integer
                        description: Number of rows
                      cellWidth:
                        type: integer
                        description: Cell width in pixels
                      cellHeight:
                        type: integer
                        description: Cell height in pixels
                      canvasWidth:
                        type: integer
                        description: Total canvas width in pixels
                      canvasHeight:
                        type: integer
                        description: Total canvas height in pixels
        '400':
          description: Invalid input
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Error'
        '401':
          description: Authentication required
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/UnauthorizedError'
  /api/v1/tools/image/passport-photo:
    post:
      operationId: passportPhotoTool
      tags:
        - Tools
      summary: 証明写真
      description: |
        2 フェーズの証明写真フロー用のガイダンスエンドポイント。`/api/v1/tools/image/passport-photo/analyze` で画像を分析し、次に `/api/v1/tools/image/passport-photo/generate` で最終的な証明写真を作成します。このベースルートは、必要な機能バンドルがインストールされている場合は 400 のガイダンスエラーを、不足している場合は 501 を返します。
      security:
        - bearerAuth: []
      responses:
        '400':
          description: Use the analyze or generate endpoint
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Error'
        '401':
          description: Authentication required
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/UnauthorizedError'
        '501':
          description: Feature not installed
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/FeatureNotInstalledError'
  /api/v1/tools/pdf/sign-pdf:
    post:
      operationId: signPdfTool
      tags:
        - Tools
      summary: PDF への署名
      description: 正規化されたページ配置を用いて、1 つ以上の署名 PNG 画像を PDF に押印します。
      security:
        - bearerAuth: []
      requestBody:
        required: true
        content:
          multipart/form-data:
            schema:
              type: object
              required:
                - file
                - placements
                - sig0
              properties:
                file:
                  type: string
                  format: binary
                  description: PDF file to sign
                sig0:
                  type: string
                  format: binary
                  description: First signature image. Additional signatures use sig1, sig2, and so on.
                placements:
                  type: string
                  description: JSON array of placement objects with sig, page, x, y, w, and h. Coordinates are page fractions with top-left origin.
                clientJobId:
                  type: string
                  description: Client-provided job ID for SSE progress tracking
                fileId:
                  type: string
                  description: Optional file library ID to save the signed result as a new version
      responses:
        '200':
          description: Signed PDF
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ToolResponse'
        '202':
          description: Accepted for async processing. Track progress via SSE at /api/v1/jobs/{jobId}/progress.
          content:
            application/json:
              schema:
                type: object
                properties:
                  jobId:
                    type: string
                  async:
                    type: boolean
                    example: true
        '400':
          description: Invalid input or settings
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Error'
        '401':
          description: Authentication required
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/UnauthorizedError'
        '422':
          description: Processing failed
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Error'
  /api/v1/tools/image/jpg-to-png:
    post:
      operationId: jpgToPngTool
      tags:
        - Tools
      summary: JPG から PNG
      description: |
        JPG を PNG に変換
        これは `convert` に基づく専用の変換プリセットエンドポイントです。
      security:
        - bearerAuth: []
      requestBody:
        required: true
        content:
          multipart/form-data:
            schema:
              type: object
              required:
                - file
              properties:
                file:
                  type: string
                  format: binary
                  description: JPG to PNG input file (.jpg, .jpeg)
                settings:
                  type: string
                  description: |
                    Dedicated JPG to PNG converter using the convert pipeline.
                    Optional settings: quality (number 1-100). The output format is locked by the endpoint.
                clientJobId:
                  type: string
                  description: Client-provided job ID for SSE progress tracking
      responses:
        '200':
          description: Processed file
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ToolResponse'
        '400':
          description: Invalid input or settings
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Error'
        '401':
          description: Authentication required
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/UnauthorizedError'
        '422':
          description: Processing failed
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Error'
  /api/v1/tools/image/png-to-jpg:
    post:
      operationId: pngToJpgTool
      tags:
        - Tools
      summary: PNG から JPG
      description: |
        PNG を JPG に変換
        これは `convert` に基づく専用の変換プリセットエンドポイントです。
      security:
        - bearerAuth: []
      requestBody:
        required: true
        content:
          multipart/form-data:
            schema:
              type: object
              required:
                - file
              properties:
                file:
                  type: string
                  format: binary
                  description: PNG to JPG input file (.png)
                settings:
                  type: string
                  description: |
                    Dedicated PNG to JPG converter using the convert pipeline.
                    Optional settings: quality (number 1-100). The output format is locked by the endpoint.
                clientJobId:
                  type: string
                  description: Client-provided job ID for SSE progress tracking
      responses:
        '200':
          description: Processed file
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ToolResponse'
        '400':
          description: Invalid input or settings
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Error'
        '401':
          description: Authentication required
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/UnauthorizedError'
        '422':
          description: Processing failed
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Error'
  /api/v1/tools/image/jpg-to-webp:
    post:
      operationId: jpgToWebpTool
      tags:
        - Tools
      summary: JPG から WebP
      description: |
        JPG を WebP に変換
        これは `convert` に基づく専用の変換プリセットエンドポイントです。
      security:
        - bearerAuth: []
      requestBody:
        required: true
        content:
          multipart/form-data:
            schema:
              type: object
              required:
                - file
              properties:
                file:
                  type: string
                  format: binary
                  description: JPG to WebP input file (.jpg, .jpeg)
                settings:
                  type: string
                  description: |
                    Dedicated JPG to WebP converter using the convert pipeline.
                    Optional settings: quality (number 1-100). The output format is locked by the endpoint.
                clientJobId:
                  type: string
                  description: Client-provided job ID for SSE progress tracking
      responses:
        '200':
          description: Processed file
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ToolResponse'
        '400':
          description: Invalid input or settings
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Error'
        '401':
          description: Authentication required
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/UnauthorizedError'
        '422':
          description: Processing failed
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Error'
  /api/v1/tools/image/png-to-webp:
    post:
      operationId: pngToWebpTool
      tags:
        - Tools
      summary: PNG から WebP
      description: |
        PNG を WebP に変換
        これは `convert` に基づく専用の変換プリセットエンドポイントです。
      security:
        - bearerAuth: []
      requestBody:
        required: true
        content:
          multipart/form-data:
            schema:
              type: object
              required:
                - file
              properties:
                file:
                  type: string
                  format: binary
                  description: PNG to WebP input file (.png)
                settings:
                  type: string
                  description: |
                    Dedicated PNG to WebP converter using the convert pipeline.
                    Optional settings: quality (number 1-100). The output format is locked by the endpoint.
                clientJobId:
                  type: string
                  description: Client-provided job ID for SSE progress tracking
      responses:
        '200':
          description: Processed file
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ToolResponse'
        '400':
          description: Invalid input or settings
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Error'
        '401':
          description: Authentication required
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/UnauthorizedError'
        '422':
          description: Processing failed
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Error'
  /api/v1/tools/image/webp-to-jpg:
    post:
      operationId: webpToJpgTool
      tags:
        - Tools
      summary: WebP から JPG
      description: |
        WebP を JPG に変換
        これは `convert` に基づく専用の変換プリセットエンドポイントです。
      security:
        - bearerAuth: []
      requestBody:
        required: true
        content:
          multipart/form-data:
            schema:
              type: object
              required:
                - file
              properties:
                file:
                  type: string
                  format: binary
                  description: WebP to JPG input file (.webp)
                settings:
                  type: string
                  description: |
                    Dedicated WebP to JPG converter using the convert pipeline.
                    Optional settings: quality (number 1-100). The output format is locked by the endpoint.
                clientJobId:
                  type: string
                  description: Client-provided job ID for SSE progress tracking
      responses:
        '200':
          description: Processed file
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ToolResponse'
        '400':
          description: Invalid input or settings
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Error'
        '401':
          description: Authentication required
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/UnauthorizedError'
        '422':
          description: Processing failed
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Error'
  /api/v1/tools/image/webp-to-png:
    post:
      operationId: webpToPngTool
      tags:
        - Tools
      summary: WebP から PNG
      description: |
        WebP を PNG に変換
        これは `convert` に基づく専用の変換プリセットエンドポイントです。
      security:
        - bearerAuth: []
      requestBody:
        required: true
        content:
          multipart/form-data:
            schema:
              type: object
              required:
                - file
              properties:
                file:
                  type: string
                  format: binary
                  description: WebP to PNG input file (.webp)
                settings:
                  type: string
                  description: |
                    Dedicated WebP to PNG converter using the convert pipeline.
                    Optional settings: quality (number 1-100). The output format is locked by the endpoint.
                clientJobId:
                  type: string
                  description: Client-provided job ID for SSE progress tracking
      responses:
        '200':
          description: Processed file
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ToolResponse'
        '400':
          description: Invalid input or settings
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Error'
        '401':
          description: Authentication required
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/UnauthorizedError'
        '422':
          description: Processing failed
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Error'
  /api/v1/tools/image/jpg-to-avif:
    post:
      operationId: jpgToAvifTool
      tags:
        - Tools
      summary: JPG から AVIF
      description: |
        JPG を AVIF に変換
        これは `convert` に基づく専用の変換プリセットエンドポイントです。
      security:
        - bearerAuth: []
      requestBody:
        required: true
        content:
          multipart/form-data:
            schema:
              type: object
              required:
                - file
              properties:
                file:
                  type: string
                  format: binary
                  description: JPG to AVIF input file (.jpg, .jpeg)
                settings:
                  type: string
                  description: |
                    Dedicated JPG to AVIF converter using the convert pipeline.
                    Optional settings: quality (number 1-100). The output format is locked by the endpoint.
                clientJobId:
                  type: string
                  description: Client-provided job ID for SSE progress tracking
      responses:
        '200':
          description: Processed file
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ToolResponse'
        '400':
          description: Invalid input or settings
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Error'
        '401':
          description: Authentication required
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/UnauthorizedError'
        '422':
          description: Processing failed
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Error'
  /api/v1/tools/image/png-to-avif:
    post:
      operationId: pngToAvifTool
      tags:
        - Tools
      summary: PNG から AVIF
      description: |
        PNG を AVIF に変換
        これは `convert` に基づく専用の変換プリセットエンドポイントです。
      security:
        - bearerAuth: []
      requestBody:
        required: true
        content:
          multipart/form-data:
            schema:
              type: object
              required:
                - file
              properties:
                file:
                  type: string
                  format: binary
                  description: PNG to AVIF input file (.png)
                settings:
                  type: string
                  description: |
                    Dedicated PNG to AVIF converter using the convert pipeline.
                    Optional settings: quality (number 1-100). The output format is locked by the endpoint.
                clientJobId:
                  type: string
                  description: Client-provided job ID for SSE progress tracking
      responses:
        '200':
          description: Processed file
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ToolResponse'
        '400':
          description: Invalid input or settings
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Error'
        '401':
          description: Authentication required
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/UnauthorizedError'
        '422':
          description: Processing failed
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Error'
  /api/v1/tools/image/webp-to-avif:
    post:
      operationId: webpToAvifTool
      tags:
        - Tools
      summary: WebP から AVIF
      description: |
        WebP を AVIF に変換
        これは `convert` に基づく専用の変換プリセットエンドポイントです。
      security:
        - bearerAuth: []
      requestBody:
        required: true
        content:
          multipart/form-data:
            schema:
              type: object
              required:
                - file
              properties:
                file:
                  type: string
                  format: binary
                  description: WebP to AVIF input file (.webp)
                settings:
                  type: string
                  description: |
                    Dedicated WebP to AVIF converter using the convert pipeline.
                    Optional settings: quality (number 1-100). The output format is locked by the endpoint.
                clientJobId:
                  type: string
                  description: Client-provided job ID for SSE progress tracking
      responses:
        '200':
          description: Processed file
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ToolResponse'
        '400':
          description: Invalid input or settings
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Error'
        '401':
          description: Authentication required
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/UnauthorizedError'
        '422':
          description: Processing failed
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Error'
  /api/v1/tools/image/heic-to-jpg:
    post:
      operationId: heicToJpgTool
      tags:
        - Tools
      summary: HEIC から JPG
      description: |
        HEIC を JPG に変換
        これは `convert` に基づく専用の変換プリセットエンドポイントです。
      security:
        - bearerAuth: []
      requestBody:
        required: true
        content:
          multipart/form-data:
            schema:
              type: object
              required:
                - file
              properties:
                file:
                  type: string
                  format: binary
                  description: HEIC to JPG input file (.heic, .heif)
                settings:
                  type: string
                  description: |
                    Dedicated HEIC to JPG converter using the convert pipeline.
                    Optional settings: quality (number 1-100). The output format is locked by the endpoint.
                clientJobId:
                  type: string
                  description: Client-provided job ID for SSE progress tracking
      responses:
        '200':
          description: Processed file
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ToolResponse'
        '400':
          description: Invalid input or settings
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Error'
        '401':
          description: Authentication required
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/UnauthorizedError'
        '422':
          description: Processing failed
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Error'
  /api/v1/tools/image/heic-to-png:
    post:
      operationId: heicToPngTool
      tags:
        - Tools
      summary: HEIC から PNG
      description: |
        HEIC を PNG に変換
        これは `convert` に基づく専用の変換プリセットエンドポイントです。
      security:
        - bearerAuth: []
      requestBody:
        required: true
        content:
          multipart/form-data:
            schema:
              type: object
              required:
                - file
              properties:
                file:
                  type: string
                  format: binary
                  description: HEIC to PNG input file (.heic, .heif)
                settings:
                  type: string
                  description: |
                    Dedicated HEIC to PNG converter using the convert pipeline.
                    Optional settings: quality (number 1-100). The output format is locked by the endpoint.
                clientJobId:
                  type: string
                  description: Client-provided job ID for SSE progress tracking
      responses:
        '200':
          description: Processed file
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ToolResponse'
        '400':
          description: Invalid input or settings
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Error'
        '401':
          description: Authentication required
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/UnauthorizedError'
        '422':
          description: Processing failed
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Error'
  /api/v1/tools/image/heic-to-avif:
    post:
      operationId: heicToAvifTool
      tags:
        - Tools
      summary: HEIC から AVIF
      description: |
        HEIC を AVIF に変換
        これは `convert` に基づく専用の変換プリセットエンドポイントです。
      security:
        - bearerAuth: []
      requestBody:
        required: true
        content:
          multipart/form-data:
            schema:
              type: object
              required:
                - file
              properties:
                file:
                  type: string
                  format: binary
                  description: HEIC to AVIF input file (.heic, .heif)
                settings:
                  type: string
                  description: |
                    Dedicated HEIC to AVIF converter using the convert pipeline.
                    Optional settings: quality (number 1-100). The output format is locked by the endpoint.
                clientJobId:
                  type: string
                  description: Client-provided job ID for SSE progress tracking
      responses:
        '200':
          description: Processed file
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ToolResponse'
        '400':
          description: Invalid input or settings
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Error'
        '401':
          description: Authentication required
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/UnauthorizedError'
        '422':
          description: Processing failed
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Error'
  /api/v1/tools/image/jpg-to-gif:
    post:
      operationId: jpgToGifTool
      tags:
        - Tools
      summary: JPG から GIF
      description: |
        JPG を GIF に変換
        これは `convert` に基づく専用の変換プリセットエンドポイントです。
      security:
        - bearerAuth: []
      requestBody:
        required: true
        content:
          multipart/form-data:
            schema:
              type: object
              required:
                - file
              properties:
                file:
                  type: string
                  format: binary
                  description: JPG to GIF input file (.jpg, .jpeg)
                settings:
                  type: string
                  description: |
                    Dedicated JPG to GIF converter using the convert pipeline.
                    Optional settings: quality (number 1-100). The output format is locked by the endpoint.
                clientJobId:
                  type: string
                  description: Client-provided job ID for SSE progress tracking
      responses:
        '200':
          description: Processed file
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ToolResponse'
        '400':
          description: Invalid input or settings
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Error'
        '401':
          description: Authentication required
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/UnauthorizedError'
        '422':
          description: Processing failed
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Error'
  /api/v1/tools/image/png-to-gif:
    post:
      operationId: pngToGifTool
      tags:
        - Tools
      summary: PNG から GIF
      description: |
        PNG を GIF に変換
        これは `convert` に基づく専用の変換プリセットエンドポイントです。
      security:
        - bearerAuth: []
      requestBody:
        required: true
        content:
          multipart/form-data:
            schema:
              type: object
              required:
                - file
              properties:
                file:
                  type: string
                  format: binary
                  description: PNG to GIF input file (.png)
                settings:
                  type: string
                  description: |
                    Dedicated PNG to GIF converter using the convert pipeline.
                    Optional settings: quality (number 1-100). The output format is locked by the endpoint.
                clientJobId:
                  type: string
                  description: Client-provided job ID for SSE progress tracking
      responses:
        '200':
          description: Processed file
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ToolResponse'
        '400':
          description: Invalid input or settings
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Error'
        '401':
          description: Authentication required
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/UnauthorizedError'
        '422':
          description: Processing failed
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Error'
  /api/v1/tools/image/gif-to-jpg:
    post:
      operationId: gifToJpgTool
      tags:
        - Tools
      summary: GIF から JPG
      description: |
        GIF を JPG に変換
        これは `convert` に基づく専用の変換プリセットエンドポイントです。
      security:
        - bearerAuth: []
      requestBody:
        required: true
        content:
          multipart/form-data:
            schema:
              type: object
              required:
                - file
              properties:
                file:
                  type: string
                  format: binary
                  description: GIF to JPG input file (.gif)
                settings:
                  type: string
                  description: |
                    Dedicated GIF to JPG converter using the convert pipeline.
                    Optional settings: quality (number 1-100). The output format is locked by the endpoint.
                clientJobId:
                  type: string
                  description: Client-provided job ID for SSE progress tracking
      responses:
        '200':
          description: Processed file
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ToolResponse'
        '400':
          description: Invalid input or settings
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Error'
        '401':
          description: Authentication required
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/UnauthorizedError'
        '422':
          description: Processing failed
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Error'
  /api/v1/tools/image/gif-to-png:
    post:
      operationId: gifToPngTool
      tags:
        - Tools
      summary: GIF から PNG
      description: |
        GIF を PNG に変換
        これは `convert` に基づく専用の変換プリセットエンドポイントです。
      security:
        - bearerAuth: []
      requestBody:
        required: true
        content:
          multipart/form-data:
            schema:
              type: object
              required:
                - file
              properties:
                file:
                  type: string
                  format: binary
                  description: GIF to PNG input file (.gif)
                settings:
                  type: string
                  description: |
                    Dedicated GIF to PNG converter using the convert pipeline.
                    Optional settings: quality (number 1-100). The output format is locked by the endpoint.
                clientJobId:
                  type: string
                  description: Client-provided job ID for SSE progress tracking
      responses:
        '200':
          description: Processed file
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ToolResponse'
        '400':
          description: Invalid input or settings
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Error'
        '401':
          description: Authentication required
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/UnauthorizedError'
        '422':
          description: Processing failed
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Error'
  /api/v1/tools/image/webp-to-gif:
    post:
      operationId: webpToGifTool
      tags:
        - Tools
      summary: WebP から GIF
      description: |
        WebP を GIF に変換
        これは `convert` に基づく専用の変換プリセットエンドポイントです。
      security:
        - bearerAuth: []
      requestBody:
        required: true
        content:
          multipart/form-data:
            schema:
              type: object
              required:
                - file
              properties:
                file:
                  type: string
                  format: binary
                  description: WebP to GIF input file (.webp)
                settings:
                  type: string
                  description: |
                    Dedicated WebP to GIF converter using the convert pipeline.
                    Optional settings: quality (number 1-100). The output format is locked by the endpoint.
                clientJobId:
                  type: string
                  description: Client-provided job ID for SSE progress tracking
      responses:
        '200':
          description: Processed file
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ToolResponse'
        '400':
          description: Invalid input or settings
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Error'
        '401':
          description: Authentication required
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/UnauthorizedError'
        '422':
          description: Processing failed
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Error'
  /api/v1/tools/image/jpg-to-tiff:
    post:
      operationId: jpgToTiffTool
      tags:
        - Tools
      summary: JPG から TIFF
      description: |
        JPG を TIFF に変換
        これは `convert` に基づく専用の変換プリセットエンドポイントです。
      security:
        - bearerAuth: []
      requestBody:
        required: true
        content:
          multipart/form-data:
            schema:
              type: object
              required:
                - file
              properties:
                file:
                  type: string
                  format: binary
                  description: JPG to TIFF input file (.jpg, .jpeg)
                settings:
                  type: string
                  description: |
                    Dedicated JPG to TIFF converter using the convert pipeline.
                    Optional settings: quality (number 1-100). The output format is locked by the endpoint.
                clientJobId:
                  type: string
                  description: Client-provided job ID for SSE progress tracking
      responses:
        '200':
          description: Processed file
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ToolResponse'
        '400':
          description: Invalid input or settings
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Error'
        '401':
          description: Authentication required
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/UnauthorizedError'
        '422':
          description: Processing failed
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Error'
  /api/v1/tools/image/png-to-tiff:
    post:
      operationId: pngToTiffTool
      tags:
        - Tools
      summary: PNG から TIFF
      description: |
        PNG を TIFF に変換
        これは `convert` に基づく専用の変換プリセットエンドポイントです。
      security:
        - bearerAuth: []
      requestBody:
        required: true
        content:
          multipart/form-data:
            schema:
              type: object
              required:
                - file
              properties:
                file:
                  type: string
                  format: binary
                  description: PNG to TIFF input file (.png)
                settings:
                  type: string
                  description: |
                    Dedicated PNG to TIFF converter using the convert pipeline.
                    Optional settings: quality (number 1-100). The output format is locked by the endpoint.
                clientJobId:
                  type: string
                  description: Client-provided job ID for SSE progress tracking
      responses:
        '200':
          description: Processed file
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ToolResponse'
        '400':
          description: Invalid input or settings
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Error'
        '401':
          description: Authentication required
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/UnauthorizedError'
        '422':
          description: Processing failed
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Error'
  /api/v1/tools/image/tiff-to-jpg:
    post:
      operationId: tiffToJpgTool
      tags:
        - Tools
      summary: TIFF から JPG
      description: |
        TIFF を JPG に変換
        これは `convert` に基づく専用の変換プリセットエンドポイントです。
      security:
        - bearerAuth: []
      requestBody:
        required: true
        content:
          multipart/form-data:
            schema:
              type: object
              required:
                - file
              properties:
                file:
                  type: string
                  format: binary
                  description: TIFF to JPG input file (.tiff, .tif)
                settings:
                  type: string
                  description: |
                    Dedicated TIFF to JPG converter using the convert pipeline.
                    Optional settings: quality (number 1-100). The output format is locked by the endpoint.
                clientJobId:
                  type: string
                  description: Client-provided job ID for SSE progress tracking
      responses:
        '200':
          description: Processed file
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ToolResponse'
        '400':
          description: Invalid input or settings
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Error'
        '401':
          description: Authentication required
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/UnauthorizedError'
        '422':
          description: Processing failed
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Error'
  /api/v1/tools/image/tiff-to-png:
    post:
      operationId: tiffToPngTool
      tags:
        - Tools
      summary: TIFF から PNG
      description: |
        TIFF を PNG に変換
        これは `convert` に基づく専用の変換プリセットエンドポイントです。
      security:
        - bearerAuth: []
      requestBody:
        required: true
        content:
          multipart/form-data:
            schema:
              type: object
              required:
                - file
              properties:
                file:
                  type: string
                  format: binary
                  description: TIFF to PNG input file (.tiff, .tif)
                settings:
                  type: string
                  description: |
                    Dedicated TIFF to PNG converter using the convert pipeline.
                    Optional settings: quality (number 1-100). The output format is locked by the endpoint.
                clientJobId:
                  type: string
                  description: Client-provided job ID for SSE progress tracking
      responses:
        '200':
          description: Processed file
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ToolResponse'
        '400':
          description: Invalid input or settings
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Error'
        '401':
          description: Authentication required
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/UnauthorizedError'
        '422':
          description: Processing failed
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Error'
  /api/v1/tools/image/psd-to-jpg:
    post:
      operationId: psdToJpgTool
      tags:
        - Tools
      summary: PSD から JPG
      description: |
        PSD を JPG に変換
        これは `convert` に基づく専用の変換プリセットエンドポイントです。
      security:
        - bearerAuth: []
      requestBody:
        required: true
        content:
          multipart/form-data:
            schema:
              type: object
              required:
                - file
              properties:
                file:
                  type: string
                  format: binary
                  description: PSD to JPG input file (.psd)
                settings:
                  type: string
                  description: |
                    Dedicated PSD to JPG converter using the convert pipeline.
                    Optional settings: quality (number 1-100). The output format is locked by the endpoint.
                clientJobId:
                  type: string
                  description: Client-provided job ID for SSE progress tracking
      responses:
        '200':
          description: Processed file
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ToolResponse'
        '400':
          description: Invalid input or settings
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Error'
        '401':
          description: Authentication required
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/UnauthorizedError'
        '422':
          description: Processing failed
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Error'
  /api/v1/tools/image/psd-to-png:
    post:
      operationId: psdToPngTool
      tags:
        - Tools
      summary: PSD から PNG
      description: |
        PSD を PNG に変換
        これは `convert` に基づく専用の変換プリセットエンドポイントです。
      security:
        - bearerAuth: []
      requestBody:
        required: true
        content:
          multipart/form-data:
            schema:
              type: object
              required:
                - file
              properties:
                file:
                  type: string
                  format: binary
                  description: PSD to PNG input file (.psd)
                settings:
                  type: string
                  description: |
                    Dedicated PSD to PNG converter using the convert pipeline.
                    Optional settings: quality (number 1-100). The output format is locked by the endpoint.
                clientJobId:
                  type: string
                  description: Client-provided job ID for SSE progress tracking
      responses:
        '200':
          description: Processed file
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ToolResponse'
        '400':
          description: Invalid input or settings
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Error'
        '401':
          description: Authentication required
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/UnauthorizedError'
        '422':
          description: Processing failed
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Error'
  /api/v1/tools/image/png-to-eps:
    post:
      operationId: pngToEpsTool
      tags:
        - Tools
      summary: PNG から EPS
      description: |
        PNG を EPS に変換
        これは `convert` に基づく専用の変換プリセットエンドポイントです。
      security:
        - bearerAuth: []
      requestBody:
        required: true
        content:
          multipart/form-data:
            schema:
              type: object
              required:
                - file
              properties:
                file:
                  type: string
                  format: binary
                  description: PNG to EPS input file (.png)
                settings:
                  type: string
                  description: |
                    Dedicated PNG to EPS converter using the convert pipeline.
                    Optional settings: quality (number 1-100). The output format is locked by the endpoint.
                clientJobId:
                  type: string
                  description: Client-provided job ID for SSE progress tracking
      responses:
        '200':
          description: Processed file
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ToolResponse'
        '400':
          description: Invalid input or settings
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Error'
        '401':
          description: Authentication required
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/UnauthorizedError'
        '422':
          description: Processing failed
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Error'
  /api/v1/tools/image/jpg-to-eps:
    post:
      operationId: jpgToEpsTool
      tags:
        - Tools
      summary: JPG から EPS
      description: |
        JPG を EPS に変換
        これは `convert` に基づく専用の変換プリセットエンドポイントです。
      security:
        - bearerAuth: []
      requestBody:
        required: true
        content:
          multipart/form-data:
            schema:
              type: object
              required:
                - file
              properties:
                file:
                  type: string
                  format: binary
                  description: JPG to EPS input file (.jpg, .jpeg)
                settings:
                  type: string
                  description: |
                    Dedicated JPG to EPS converter using the convert pipeline.
                    Optional settings: quality (number 1-100). The output format is locked by the endpoint.
                clientJobId:
                  type: string
                  description: Client-provided job ID for SSE progress tracking
      responses:
        '200':
          description: Processed file
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ToolResponse'
        '400':
          description: Invalid input or settings
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Error'
        '401':
          description: Authentication required
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/UnauthorizedError'
        '422':
          description: Processing failed
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Error'
  /api/v1/tools/image/eps-to-png:
    post:
      operationId: epsToPngTool
      tags:
        - Tools
      summary: EPS から PNG
      description: |
        EPS を PNG に変換
        これは `convert` に基づく専用の変換プリセットエンドポイントです。
      security:
        - bearerAuth: []
      requestBody:
        required: true
        content:
          multipart/form-data:
            schema:
              type: object
              required:
                - file
              properties:
                file:
                  type: string
                  format: binary
                  description: EPS to PNG input file (.eps)
                settings:
                  type: string
                  description: |
                    Dedicated EPS to PNG converter using the convert pipeline.
                    Optional settings: quality (number 1-100). The output format is locked by the endpoint.
                clientJobId:
                  type: string
                  description: Client-provided job ID for SSE progress tracking
      responses:
        '200':
          description: Processed file
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ToolResponse'
        '400':
          description: Invalid input or settings
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Error'
        '401':
          description: Authentication required
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/UnauthorizedError'
        '422':
          description: Processing failed
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Error'
  /api/v1/tools/image/eps-to-jpg:
    post:
      operationId: epsToJpgTool
      tags:
        - Tools
      summary: EPS から JPG
      description: |
        EPS を JPG に変換
        これは `convert` に基づく専用の変換プリセットエンドポイントです。
      security:
        - bearerAuth: []
      requestBody:
        required: true
        content:
          multipart/form-data:
            schema:
              type: object
              required:
                - file
              properties:
                file:
                  type: string
                  format: binary
                  description: EPS to JPG input file (.eps)
                settings:
                  type: string
                  description: |
                    Dedicated EPS to JPG converter using the convert pipeline.
                    Optional settings: quality (number 1-100). The output format is locked by the endpoint.
                clientJobId:
                  type: string
                  description: Client-provided job ID for SSE progress tracking
      responses:
        '200':
          description: Processed file
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ToolResponse'
        '400':
          description: Invalid input or settings
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Error'
        '401':
          description: Authentication required
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/UnauthorizedError'
        '422':
          description: Processing failed
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Error'
  /api/v1/tools/image/png-to-svg:
    post:
      operationId: pngToSvgTool
      tags:
        - Tools
      summary: PNG から SVG
      description: |
        PNG を SVG に変換
        これは `vectorize` に基づく専用の変換プリセットエンドポイントです。
      security:
        - bearerAuth: []
      requestBody:
        required: true
        content:
          multipart/form-data:
            schema:
              type: object
              required:
                - file
              properties:
                file:
                  type: string
                  format: binary
                  description: PNG to SVG input file (.png)
                settings:
                  type: string
                  description: |
                    Dedicated PNG to SVG converter using the vectorize pipeline.
                    The output format is locked by the endpoint.
                clientJobId:
                  type: string
                  description: Client-provided job ID for SSE progress tracking
      responses:
        '200':
          description: Processed file
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ToolResponse'
        '400':
          description: Invalid input or settings
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Error'
        '401':
          description: Authentication required
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/UnauthorizedError'
        '422':
          description: Processing failed
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Error'
  /api/v1/tools/image/jpg-to-svg:
    post:
      operationId: jpgToSvgTool
      tags:
        - Tools
      summary: JPG から SVG
      description: |
        JPG を SVG に変換
        これは `vectorize` に基づく専用の変換プリセットエンドポイントです。
      security:
        - bearerAuth: []
      requestBody:
        required: true
        content:
          multipart/form-data:
            schema:
              type: object
              required:
                - file
              properties:
                file:
                  type: string
                  format: binary
                  description: JPG to SVG input file (.jpg, .jpeg)
                settings:
                  type: string
                  description: |
                    Dedicated JPG to SVG converter using the vectorize pipeline.
                    The output format is locked by the endpoint.
                clientJobId:
                  type: string
                  description: Client-provided job ID for SSE progress tracking
      responses:
        '200':
          description: Processed file
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ToolResponse'
        '400':
          description: Invalid input or settings
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Error'
        '401':
          description: Authentication required
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/UnauthorizedError'
        '422':
          description: Processing failed
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Error'
  /api/v1/tools/image/tiff-to-svg:
    post:
      operationId: tiffToSvgTool
      tags:
        - Tools
      summary: TIFF から SVG
      description: |
        TIFF を SVG に変換
        これは `vectorize` に基づく専用の変換プリセットエンドポイントです。
      security:
        - bearerAuth: []
      requestBody:
        required: true
        content:
          multipart/form-data:
            schema:
              type: object
              required:
                - file
              properties:
                file:
                  type: string
                  format: binary
                  description: TIFF to SVG input file (.tiff, .tif)
                settings:
                  type: string
                  description: |
                    Dedicated TIFF to SVG converter using the vectorize pipeline.
                    The output format is locked by the endpoint.
                clientJobId:
                  type: string
                  description: Client-provided job ID for SSE progress tracking
      responses:
        '200':
          description: Processed file
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ToolResponse'
        '400':
          description: Invalid input or settings
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Error'
        '401':
          description: Authentication required
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/UnauthorizedError'
        '422':
          description: Processing failed
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Error'
  /api/v1/tools/image/psd-to-svg:
    post:
      operationId: psdToSvgTool
      tags:
        - Tools
      summary: PSD から SVG
      description: |
        PSD を SVG に変換
        これは `vectorize` に基づく専用の変換プリセットエンドポイントです。
      security:
        - bearerAuth: []
      requestBody:
        required: true
        content:
          multipart/form-data:
            schema:
              type: object
              required:
                - file
              properties:
                file:
                  type: string
                  format: binary
                  description: PSD to SVG input file (.psd)
                settings:
                  type: string
                  description: |
                    Dedicated PSD to SVG converter using the vectorize pipeline.
                    The output format is locked by the endpoint.
                clientJobId:
                  type: string
                  description: Client-provided job ID for SSE progress tracking
      responses:
        '200':
          description: Processed file
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ToolResponse'
        '400':
          description: Invalid input or settings
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Error'
        '401':
          description: Authentication required
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/UnauthorizedError'
        '422':
          description: Processing failed
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Error'
  /api/v1/tools/image/eps-to-svg:
    post:
      operationId: epsToSvgTool
      tags:
        - Tools
      summary: EPS から SVG
      description: |
        EPS を SVG に変換
        これは `vectorize` に基づく専用の変換プリセットエンドポイントです。
      security:
        - bearerAuth: []
      requestBody:
        required: true
        content:
          multipart/form-data:
            schema:
              type: object
              required:
                - file
              properties:
                file:
                  type: string
                  format: binary
                  description: EPS to SVG input file (.eps)
                settings:
                  type: string
                  description: |
                    Dedicated EPS to SVG converter using the vectorize pipeline.
                    The output format is locked by the endpoint.
                clientJobId:
                  type: string
                  description: Client-provided job ID for SSE progress tracking
      responses:
        '200':
          description: Processed file
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ToolResponse'
        '400':
          description: Invalid input or settings
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Error'
        '401':
          description: Authentication required
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/UnauthorizedError'
        '422':
          description: Processing failed
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Error'
  /api/v1/tools/image/svg-to-png:
    post:
      operationId: svgToPngTool
      tags:
        - Tools
      summary: SVG から PNG
      description: |
        SVG を PNG に変換
        これは `svg-to-raster` に基づく専用の変換プリセットエンドポイントです。
      security:
        - bearerAuth: []
      requestBody:
        required: true
        content:
          multipart/form-data:
            schema:
              type: object
              required:
                - file
              properties:
                file:
                  type: string
                  format: binary
                  description: SVG to PNG input file (.svg, .svgz)
                settings:
                  type: string
                  description: |
                    Dedicated SVG to PNG converter using the svg-to-raster pipeline.
                    Optional settings: quality (number 1-100). The output format is locked by the endpoint.
                clientJobId:
                  type: string
                  description: Client-provided job ID for SSE progress tracking
      responses:
        '200':
          description: Processed file
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ToolResponse'
        '400':
          description: Invalid input or settings
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Error'
        '401':
          description: Authentication required
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/UnauthorizedError'
        '422':
          description: Processing failed
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Error'
  /api/v1/tools/image/svg-to-jpg:
    post:
      operationId: svgToJpgTool
      tags:
        - Tools
      summary: SVG から JPG
      description: |
        SVG を JPG に変換
        これは `svg-to-raster` に基づく専用の変換プリセットエンドポイントです。
      security:
        - bearerAuth: []
      requestBody:
        required: true
        content:
          multipart/form-data:
            schema:
              type: object
              required:
                - file
              properties:
                file:
                  type: string
                  format: binary
                  description: SVG to JPG input file (.svg, .svgz)
                settings:
                  type: string
                  description: |
                    Dedicated SVG to JPG converter using the svg-to-raster pipeline.
                    Optional settings: quality (number 1-100). The output format is locked by the endpoint.
                clientJobId:
                  type: string
                  description: Client-provided job ID for SSE progress tracking
      responses:
        '200':
          description: Processed file
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ToolResponse'
        '400':
          description: Invalid input or settings
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Error'
        '401':
          description: Authentication required
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/UnauthorizedError'
        '422':
          description: Processing failed
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Error'
  /api/v1/tools/image/jpg-to-pdf:
    post:
      operationId: jpgToPdfTool
      tags:
        - Tools
      summary: JPG から PDF
      description: |
        JPG を PDF に変換
        これは `image-to-pdf` に基づく専用の変換プリセットエンドポイントです。
      security:
        - bearerAuth: []
      requestBody:
        required: true
        content:
          multipart/form-data:
            schema:
              type: object
              required:
                - file
              properties:
                file:
                  type: array
                  items:
                    type: string
                    format: binary
                  description: JPG to PDF input files (.jpg, .jpeg)
                settings:
                  type: string
                  description: |
                    Dedicated JPG to PDF converter using the image-to-pdf pipeline.
                    Optional settings: pageSize (A4, Letter, A3, A5), orientation (portrait or landscape), margin, targetSize, and collate.
                clientJobId:
                  type: string
                  description: Client-provided job ID for SSE progress tracking
      responses:
        '200':
          description: Processed file
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ToolResponse'
        '400':
          description: Invalid input or settings
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Error'
        '401':
          description: Authentication required
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/UnauthorizedError'
        '422':
          description: Processing failed
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Error'
  /api/v1/tools/image/png-to-pdf:
    post:
      operationId: pngToPdfTool
      tags:
        - Tools
      summary: PNG から PDF
      description: |
        PNG を PDF に変換
        これは `image-to-pdf` に基づく専用の変換プリセットエンドポイントです。
      security:
        - bearerAuth: []
      requestBody:
        required: true
        content:
          multipart/form-data:
            schema:
              type: object
              required:
                - file
              properties:
                file:
                  type: array
                  items:
                    type: string
                    format: binary
                  description: PNG to PDF input files (.png)
                settings:
                  type: string
                  description: |
                    Dedicated PNG to PDF converter using the image-to-pdf pipeline.
                    Optional settings: pageSize (A4, Letter, A3, A5), orientation (portrait or landscape), margin, targetSize, and collate.
                clientJobId:
                  type: string
                  description: Client-provided job ID for SSE progress tracking
      responses:
        '200':
          description: Processed file
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ToolResponse'
        '400':
          description: Invalid input or settings
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Error'
        '401':
          description: Authentication required
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/UnauthorizedError'
        '422':
          description: Processing failed
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Error'
  /api/v1/tools/image/heic-to-pdf:
    post:
      operationId: heicToPdfTool
      tags:
        - Tools
      summary: HEIC から PDF
      description: |
        HEIC を PDF に変換
        これは `image-to-pdf` に基づく専用の変換プリセットエンドポイントです。
      security:
        - bearerAuth: []
      requestBody:
        required: true
        content:
          multipart/form-data:
            schema:
              type: object
              required:
                - file
              properties:
                file:
                  type: array
                  items:
                    type: string
                    format: binary
                  description: HEIC to PDF input files (.heic, .heif)
                settings:
                  type: string
                  description: |
                    Dedicated HEIC to PDF converter using the image-to-pdf pipeline.
                    Optional settings: pageSize (A4, Letter, A3, A5), orientation (portrait or landscape), margin, targetSize, and collate.
                clientJobId:
                  type: string
                  description: Client-provided job ID for SSE progress tracking
      responses:
        '200':
          description: Processed file
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ToolResponse'
        '400':
          description: Invalid input or settings
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Error'
        '401':
          description: Authentication required
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/UnauthorizedError'
        '422':
          description: Processing failed
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Error'
  /api/v1/tools/image/tiff-to-pdf:
    post:
      operationId: tiffToPdfTool
      tags:
        - Tools
      summary: TIFF から PDF
      description: |
        TIFF を PDF に変換
        これは `image-to-pdf` に基づく専用の変換プリセットエンドポイントです。
      security:
        - bearerAuth: []
      requestBody:
        required: true
        content:
          multipart/form-data:
            schema:
              type: object
              required:
                - file
              properties:
                file:
                  type: array
                  items:
                    type: string
                    format: binary
                  description: TIFF to PDF input files (.tiff, .tif)
                settings:
                  type: string
                  description: |
                    Dedicated TIFF to PDF converter using the image-to-pdf pipeline.
                    Optional settings: pageSize (A4, Letter, A3, A5), orientation (portrait or landscape), margin, targetSize, and collate.
                clientJobId:
                  type: string
                  description: Client-provided job ID for SSE progress tracking
      responses:
        '200':
          description: Processed file
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ToolResponse'
        '400':
          description: Invalid input or settings
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Error'
        '401':
          description: Authentication required
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/UnauthorizedError'
        '422':
          description: Processing failed
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Error'
  /api/v1/tools/image/webp-to-pdf:
    post:
      operationId: webpToPdfTool
      tags:
        - Tools
      summary: WebP から PDF
      description: |
        WebP を PDF に変換
        これは `image-to-pdf` に基づく専用の変換プリセットエンドポイントです。
      security:
        - bearerAuth: []
      requestBody:
        required: true
        content:
          multipart/form-data:
            schema:
              type: object
              required:
                - file
              properties:
                file:
                  type: array
                  items:
                    type: string
                    format: binary
                  description: WebP to PDF input files (.webp)
                settings:
                  type: string
                  description: |
                    Dedicated WebP to PDF converter using the image-to-pdf pipeline.
                    Optional settings: pageSize (A4, Letter, A3, A5), orientation (portrait or landscape), margin, targetSize, and collate.
                clientJobId:
                  type: string
                  description: Client-provided job ID for SSE progress tracking
      responses:
        '200':
          description: Processed file
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ToolResponse'
        '400':
          description: Invalid input or settings
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Error'
        '401':
          description: Authentication required
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/UnauthorizedError'
        '422':
          description: Processing failed
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Error'
  /api/v1/tools/image/gif-to-pdf:
    post:
      operationId: gifToPdfTool
      tags:
        - Tools
      summary: GIF から PDF
      description: |
        GIF を PDF に変換
        これは `image-to-pdf` に基づく専用の変換プリセットエンドポイントです。
      security:
        - bearerAuth: []
      requestBody:
        required: true
        content:
          multipart/form-data:
            schema:
              type: object
              required:
                - file
              properties:
                file:
                  type: array
                  items:
                    type: string
                    format: binary
                  description: GIF to PDF input files (.gif)
                settings:
                  type: string
                  description: |
                    Dedicated GIF to PDF converter using the image-to-pdf pipeline.
                    Optional settings: pageSize (A4, Letter, A3, A5), orientation (portrait or landscape), margin, targetSize, and collate.
                clientJobId:
                  type: string
                  description: Client-provided job ID for SSE progress tracking
      responses:
        '200':
          description: Processed file
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ToolResponse'
        '400':
          description: Invalid input or settings
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Error'
        '401':
          description: Authentication required
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/UnauthorizedError'
        '422':
          description: Processing failed
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Error'
  /api/v1/tools/image/eps-to-pdf:
    post:
      operationId: epsToPdfTool
      tags:
        - Tools
      summary: EPS から PDF
      description: |
        EPS を PDF に変換
        これは `image-to-pdf` に基づく専用の変換プリセットエンドポイントです。
      security:
        - bearerAuth: []
      requestBody:
        required: true
        content:
          multipart/form-data:
            schema:
              type: object
              required:
                - file
              properties:
                file:
                  type: array
                  items:
                    type: string
                    format: binary
                  description: EPS to PDF input files (.eps)
                settings:
                  type: string
                  description: |
                    Dedicated EPS to PDF converter using the image-to-pdf pipeline.
                    Optional settings: pageSize (A4, Letter, A3, A5), orientation (portrait or landscape), margin, targetSize, and collate.
                clientJobId:
                  type: string
                  description: Client-provided job ID for SSE progress tracking
      responses:
        '200':
          description: Processed file
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ToolResponse'
        '400':
          description: Invalid input or settings
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Error'
        '401':
          description: Authentication required
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/UnauthorizedError'
        '422':
          description: Processing failed
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Error'
  /api/v1/tools/pdf/pdf-to-jpg:
    post:
      operationId: pdfToJpgTool
      tags:
        - Tools
      summary: PDF から JPG
      description: |
        PDF を JPG に変換
        これは `pdf-to-image` に基づく専用の変換プリセットエンドポイントです。
      security:
        - bearerAuth: []
      requestBody:
        required: true
        content:
          multipart/form-data:
            schema:
              type: object
              required:
                - file
              properties:
                file:
                  type: string
                  format: binary
                  description: PDF to JPG input file (.pdf)
                settings:
                  type: string
                  description: |
                    Dedicated PDF to JPG converter using the pdf-to-image pipeline.
                    The output format is locked by the endpoint.
                clientJobId:
                  type: string
                  description: Client-provided job ID for SSE progress tracking
      responses:
        '200':
          description: Processed file
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ToolResponse'
        '400':
          description: Invalid input or settings
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Error'
        '401':
          description: Authentication required
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/UnauthorizedError'
        '422':
          description: Processing failed
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Error'
  /api/v1/tools/pdf/pdf-to-png:
    post:
      operationId: pdfToPngTool
      tags:
        - Tools
      summary: PDF から PNG
      description: |
        PDF を PNG に変換
        これは `pdf-to-image` に基づく専用の変換プリセットエンドポイントです。
      security:
        - bearerAuth: []
      requestBody:
        required: true
        content:
          multipart/form-data:
            schema:
              type: object
              required:
                - file
              properties:
                file:
                  type: string
                  format: binary
                  description: PDF to PNG input file (.pdf)
                settings:
                  type: string
                  description: |
                    Dedicated PDF to PNG converter using the pdf-to-image pipeline.
                    The output format is locked by the endpoint.
                clientJobId:
                  type: string
                  description: Client-provided job ID for SSE progress tracking
      responses:
        '200':
          description: Processed file
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ToolResponse'
        '400':
          description: Invalid input or settings
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Error'
        '401':
          description: Authentication required
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/UnauthorizedError'
        '422':
          description: Processing failed
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Error'
  /api/v1/tools/pdf/pdf-to-tiff:
    post:
      operationId: pdfToTiffTool
      tags:
        - Tools
      summary: PDF から TIFF
      description: |
        PDF を TIFF に変換
        これは `pdf-to-image` に基づく専用の変換プリセットエンドポイントです。
      security:
        - bearerAuth: []
      requestBody:
        required: true
        content:
          multipart/form-data:
            schema:
              type: object
              required:
                - file
              properties:
                file:
                  type: string
                  format: binary
                  description: PDF to TIFF input file (.pdf)
                settings:
                  type: string
                  description: |
                    Dedicated PDF to TIFF converter using the pdf-to-image pipeline.
                    The output format is locked by the endpoint.
                clientJobId:
                  type: string
                  description: Client-provided job ID for SSE progress tracking
      responses:
        '200':
          description: Processed file
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ToolResponse'
        '400':
          description: Invalid input or settings
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Error'
        '401':
          description: Authentication required
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/UnauthorizedError'
        '422':
          description: Processing failed
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Error'
  /api/v1/tools/video/mov-to-mp4:
    post:
      operationId: movToMp4Tool
      tags:
        - Tools
      summary: MOV から MP4
      description: |
        MOV を MP4 に変換
        これは `convert-video` に基づく専用の変換プリセットエンドポイントです。
      security:
        - bearerAuth: []
      requestBody:
        required: true
        content:
          multipart/form-data:
            schema:
              type: object
              required:
                - file
              properties:
                file:
                  type: string
                  format: binary
                  description: MOV to MP4 input file (.mov)
                settings:
                  type: string
                  description: |
                    Dedicated MOV to MP4 converter using the convert-video pipeline.
                    Optional settings: quality (high, balanced, small). The output format is locked by the endpoint.
                clientJobId:
                  type: string
                  description: Client-provided job ID for SSE progress tracking
      responses:
        '202':
          description: Accepted for async processing. Track progress via SSE at /api/v1/jobs/{jobId}/progress.
          content:
            application/json:
              schema:
                type: object
                properties:
                  jobId:
                    type: string
                  async:
                    type: boolean
                    example: true
        '400':
          description: Invalid input or settings
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Error'
        '401':
          description: Authentication required
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/UnauthorizedError'
        '422':
          description: Processing failed
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Error'
  /api/v1/tools/video/webm-to-mp4:
    post:
      operationId: webmToMp4Tool
      tags:
        - Tools
      summary: WEBM から MP4
      description: |
        WEBM を MP4 に変換
        これは `convert-video` に基づく専用の変換プリセットエンドポイントです。
      security:
        - bearerAuth: []
      requestBody:
        required: true
        content:
          multipart/form-data:
            schema:
              type: object
              required:
                - file
              properties:
                file:
                  type: string
                  format: binary
                  description: WEBM to MP4 input file (.webm)
                settings:
                  type: string
                  description: |
                    Dedicated WEBM to MP4 converter using the convert-video pipeline.
                    Optional settings: quality (high, balanced, small). The output format is locked by the endpoint.
                clientJobId:
                  type: string
                  description: Client-provided job ID for SSE progress tracking
      responses:
        '202':
          description: Accepted for async processing. Track progress via SSE at /api/v1/jobs/{jobId}/progress.
          content:
            application/json:
              schema:
                type: object
                properties:
                  jobId:
                    type: string
                  async:
                    type: boolean
                    example: true
        '400':
          description: Invalid input or settings
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Error'
        '401':
          description: Authentication required
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/UnauthorizedError'
        '422':
          description: Processing failed
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Error'
  /api/v1/tools/video/mkv-to-mp4:
    post:
      operationId: mkvToMp4Tool
      tags:
        - Tools
      summary: MKV から MP4
      description: |
        MKV を MP4 に変換
        これは `convert-video` に基づく専用の変換プリセットエンドポイントです。
      security:
        - bearerAuth: []
      requestBody:
        required: true
        content:
          multipart/form-data:
            schema:
              type: object
              required:
                - file
              properties:
                file:
                  type: string
                  format: binary
                  description: MKV to MP4 input file (.mkv)
                settings:
                  type: string
                  description: |
                    Dedicated MKV to MP4 converter using the convert-video pipeline.
                    Optional settings: quality (high, balanced, small). The output format is locked by the endpoint.
                clientJobId:
                  type: string
                  description: Client-provided job ID for SSE progress tracking
      responses:
        '202':
          description: Accepted for async processing. Track progress via SSE at /api/v1/jobs/{jobId}/progress.
          content:
            application/json:
              schema:
                type: object
                properties:
                  jobId:
                    type: string
                  async:
                    type: boolean
                    example: true
        '400':
          description: Invalid input or settings
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Error'
        '401':
          description: Authentication required
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/UnauthorizedError'
        '422':
          description: Processing failed
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Error'
  /api/v1/tools/video/avi-to-mp4:
    post:
      operationId: aviToMp4Tool
      tags:
        - Tools
      summary: AVI から MP4
      description: |
        AVI を MP4 に変換
        これは `convert-video` に基づく専用の変換プリセットエンドポイントです。
      security:
        - bearerAuth: []
      requestBody:
        required: true
        content:
          multipart/form-data:
            schema:
              type: object
              required:
                - file
              properties:
                file:
                  type: string
                  format: binary
                  description: AVI to MP4 input file (.avi)
                settings:
                  type: string
                  description: |
                    Dedicated AVI to MP4 converter using the convert-video pipeline.
                    Optional settings: quality (high, balanced, small). The output format is locked by the endpoint.
                clientJobId:
                  type: string
                  description: Client-provided job ID for SSE progress tracking
      responses:
        '202':
          description: Accepted for async processing. Track progress via SSE at /api/v1/jobs/{jobId}/progress.
          content:
            application/json:
              schema:
                type: object
                properties:
                  jobId:
                    type: string
                  async:
                    type: boolean
                    example: true
        '400':
          description: Invalid input or settings
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Error'
        '401':
          description: Authentication required
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/UnauthorizedError'
        '422':
          description: Processing failed
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Error'
  /api/v1/tools/video/mp4-to-mov:
    post:
      operationId: mp4ToMovTool
      tags:
        - Tools
      summary: MP4 から MOV
      description: |
        MP4 を MOV に変換
        これは `convert-video` に基づく専用の変換プリセットエンドポイントです。
      security:
        - bearerAuth: []
      requestBody:
        required: true
        content:
          multipart/form-data:
            schema:
              type: object
              required:
                - file
              properties:
                file:
                  type: string
                  format: binary
                  description: MP4 to MOV input file (.mp4)
                settings:
                  type: string
                  description: |
                    Dedicated MP4 to MOV converter using the convert-video pipeline.
                    Optional settings: quality (high, balanced, small). The output format is locked by the endpoint.
                clientJobId:
                  type: string
                  description: Client-provided job ID for SSE progress tracking
      responses:
        '202':
          description: Accepted for async processing. Track progress via SSE at /api/v1/jobs/{jobId}/progress.
          content:
            application/json:
              schema:
                type: object
                properties:
                  jobId:
                    type: string
                  async:
                    type: boolean
                    example: true
        '400':
          description: Invalid input or settings
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Error'
        '401':
          description: Authentication required
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/UnauthorizedError'
        '422':
          description: Processing failed
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Error'
  /api/v1/tools/video/mp4-to-webm:
    post:
      operationId: mp4ToWebmTool
      tags:
        - Tools
      summary: MP4 から WEBM
      description: |
        MP4 を WEBM に変換
        これは `convert-video` に基づく専用の変換プリセットエンドポイントです。
      security:
        - bearerAuth: []
      requestBody:
        required: true
        content:
          multipart/form-data:
            schema:
              type: object
              required:
                - file
              properties:
                file:
                  type: string
                  format: binary
                  description: MP4 to WEBM input file (.mp4)
                settings:
                  type: string
                  description: |
                    Dedicated MP4 to WEBM converter using the convert-video pipeline.
                    Optional settings: quality (high, balanced, small). The output format is locked by the endpoint.
                clientJobId:
                  type: string
                  description: Client-provided job ID for SSE progress tracking
      responses:
        '202':
          description: Accepted for async processing. Track progress via SSE at /api/v1/jobs/{jobId}/progress.
          content:
            application/json:
              schema:
                type: object
                properties:
                  jobId:
                    type: string
                  async:
                    type: boolean
                    example: true
        '400':
          description: Invalid input or settings
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Error'
        '401':
          description: Authentication required
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/UnauthorizedError'
        '422':
          description: Processing failed
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Error'
  /api/v1/tools/video/webm-to-mov:
    post:
      operationId: webmToMovTool
      tags:
        - Tools
      summary: WEBM から MOV
      description: |
        WEBM を MOV に変換
        これは `convert-video` に基づく専用の変換プリセットエンドポイントです。
      security:
        - bearerAuth: []
      requestBody:
        required: true
        content:
          multipart/form-data:
            schema:
              type: object
              required:
                - file
              properties:
                file:
                  type: string
                  format: binary
                  description: WEBM to MOV input file (.webm)
                settings:
                  type: string
                  description: |
                    Dedicated WEBM to MOV converter using the convert-video pipeline.
                    Optional settings: quality (high, balanced, small). The output format is locked by the endpoint.
                clientJobId:
                  type: string
                  description: Client-provided job ID for SSE progress tracking
      responses:
        '202':
          description: Accepted for async processing. Track progress via SSE at /api/v1/jobs/{jobId}/progress.
          content:
            application/json:
              schema:
                type: object
                properties:
                  jobId:
                    type: string
                  async:
                    type: boolean
                    example: true
        '400':
          description: Invalid input or settings
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Error'
        '401':
          description: Authentication required
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/UnauthorizedError'
        '422':
          description: Processing failed
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Error'
  /api/v1/tools/video/mkv-to-mov:
    post:
      operationId: mkvToMovTool
      tags:
        - Tools
      summary: MKV から MOV
      description: |
        MKV を MOV に変換
        これは `convert-video` に基づく専用の変換プリセットエンドポイントです。
      security:
        - bearerAuth: []
      requestBody:
        required: true
        content:
          multipart/form-data:
            schema:
              type: object
              required:
                - file
              properties:
                file:
                  type: string
                  format: binary
                  description: MKV to MOV input file (.mkv)
                settings:
                  type: string
                  description: |
                    Dedicated MKV to MOV converter using the convert-video pipeline.
                    Optional settings: quality (high, balanced, small). The output format is locked by the endpoint.
                clientJobId:
                  type: string
                  description: Client-provided job ID for SSE progress tracking
      responses:
        '202':
          description: Accepted for async processing. Track progress via SSE at /api/v1/jobs/{jobId}/progress.
          content:
            application/json:
              schema:
                type: object
                properties:
                  jobId:
                    type: string
                  async:
                    type: boolean
                    example: true
        '400':
          description: Invalid input or settings
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Error'
        '401':
          description: Authentication required
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/UnauthorizedError'
        '422':
          description: Processing failed
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Error'
  /api/v1/tools/video/avi-to-mov:
    post:
      operationId: aviToMovTool
      tags:
        - Tools
      summary: AVI から MOV
      description: |
        AVI を MOV に変換
        これは `convert-video` に基づく専用の変換プリセットエンドポイントです。
      security:
        - bearerAuth: []
      requestBody:
        required: true
        content:
          multipart/form-data:
            schema:
              type: object
              required:
                - file
              properties:
                file:
                  type: string
                  format: binary
                  description: AVI to MOV input file (.avi)
                settings:
                  type: string
                  description: |
                    Dedicated AVI to MOV converter using the convert-video pipeline.
                    Optional settings: quality (high, balanced, small). The output format is locked by the endpoint.
                clientJobId:
                  type: string
                  description: Client-provided job ID for SSE progress tracking
      responses:
        '202':
          description: Accepted for async processing. Track progress via SSE at /api/v1/jobs/{jobId}/progress.
          content:
            application/json:
              schema:
                type: object
                properties:
                  jobId:
                    type: string
                  async:
                    type: boolean
                    example: true
        '400':
          description: Invalid input or settings
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Error'
        '401':
          description: Authentication required
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/UnauthorizedError'
        '422':
          description: Processing failed
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Error'
  /api/v1/tools/video/mp4-to-avi:
    post:
      operationId: mp4ToAviTool
      tags:
        - Tools
      summary: MP4 から AVI
      description: |
        MP4 を AVI に変換
        これは `convert-video` に基づく専用の変換プリセットエンドポイントです。
      security:
        - bearerAuth: []
      requestBody:
        required: true
        content:
          multipart/form-data:
            schema:
              type: object
              required:
                - file
              properties:
                file:
                  type: string
                  format: binary
                  description: MP4 to AVI input file (.mp4)
                settings:
                  type: string
                  description: |
                    Dedicated MP4 to AVI converter using the convert-video pipeline.
                    Optional settings: quality (high, balanced, small). The output format is locked by the endpoint.
                clientJobId:
                  type: string
                  description: Client-provided job ID for SSE progress tracking
      responses:
        '202':
          description: Accepted for async processing. Track progress via SSE at /api/v1/jobs/{jobId}/progress.
          content:
            application/json:
              schema:
                type: object
                properties:
                  jobId:
                    type: string
                  async:
                    type: boolean
                    example: true
        '400':
          description: Invalid input or settings
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Error'
        '401':
          description: Authentication required
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/UnauthorizedError'
        '422':
          description: Processing failed
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Error'
  /api/v1/tools/video/mov-to-avi:
    post:
      operationId: movToAviTool
      tags:
        - Tools
      summary: MOV から AVI
      description: |
        MOV を AVI に変換
        これは `convert-video` に基づく専用の変換プリセットエンドポイントです。
      security:
        - bearerAuth: []
      requestBody:
        required: true
        content:
          multipart/form-data:
            schema:
              type: object
              required:
                - file
              properties:
                file:
                  type: string
                  format: binary
                  description: MOV to AVI input file (.mov)
                settings:
                  type: string
                  description: |
                    Dedicated MOV to AVI converter using the convert-video pipeline.
                    Optional settings: quality (high, balanced, small). The output format is locked by the endpoint.
                clientJobId:
                  type: string
                  description: Client-provided job ID for SSE progress tracking
      responses:
        '202':
          description: Accepted for async processing. Track progress via SSE at /api/v1/jobs/{jobId}/progress.
          content:
            application/json:
              schema:
                type: object
                properties:
                  jobId:
                    type: string
                  async:
                    type: boolean
                    example: true
        '400':
          description: Invalid input or settings
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Error'
        '401':
          description: Authentication required
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/UnauthorizedError'
        '422':
          description: Processing failed
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Error'
  /api/v1/tools/video/mkv-to-avi:
    post:
      operationId: mkvToAviTool
      tags:
        - Tools
      summary: MKV から AVI
      description: |
        MKV を AVI に変換
        これは `convert-video` に基づく専用の変換プリセットエンドポイントです。
      security:
        - bearerAuth: []
      requestBody:
        required: true
        content:
          multipart/form-data:
            schema:
              type: object
              required:
                - file
              properties:
                file:
                  type: string
                  format: binary
                  description: MKV to AVI input file (.mkv)
                settings:
                  type: string
                  description: |
                    Dedicated MKV to AVI converter using the convert-video pipeline.
                    Optional settings: quality (high, balanced, small). The output format is locked by the endpoint.
                clientJobId:
                  type: string
                  description: Client-provided job ID for SSE progress tracking
      responses:
        '202':
          description: Accepted for async processing. Track progress via SSE at /api/v1/jobs/{jobId}/progress.
          content:
            application/json:
              schema:
                type: object
                properties:
                  jobId:
                    type: string
                  async:
                    type: boolean
                    example: true
        '400':
          description: Invalid input or settings
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Error'
        '401':
          description: Authentication required
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/UnauthorizedError'
        '422':
          description: Processing failed
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Error'
  /api/v1/tools/video/avi-to-mkv:
    post:
      operationId: aviToMkvTool
      tags:
        - Tools
      summary: AVI から MKV
      description: |
        AVI を MKV に変換
        これは `convert-video` に基づく専用の変換プリセットエンドポイントです。
      security:
        - bearerAuth: []
      requestBody:
        required: true
        content:
          multipart/form-data:
            schema:
              type: object
              required:
                - file
              properties:
                file:
                  type: string
                  format: binary
                  description: AVI to MKV input file (.avi)
                settings:
                  type: string
                  description: |
                    Dedicated AVI to MKV converter using the convert-video pipeline.
                    Optional settings: quality (high, balanced, small). The output format is locked by the endpoint.
                clientJobId:
                  type: string
                  description: Client-provided job ID for SSE progress tracking
      responses:
        '202':
          description: Accepted for async processing. Track progress via SSE at /api/v1/jobs/{jobId}/progress.
          content:
            application/json:
              schema:
                type: object
                properties:
                  jobId:
                    type: string
                  async:
                    type: boolean
                    example: true
        '400':
          description: Invalid input or settings
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Error'
        '401':
          description: Authentication required
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/UnauthorizedError'
        '422':
          description: Processing failed
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Error'
  /api/v1/tools/video/mp4-to-gif:
    post:
      operationId: mp4ToGifTool
      tags:
        - Tools
      summary: MP4 から GIF
      description: |
        MP4 を GIF に変換
        これは `video-to-gif` に基づく専用の変換プリセットエンドポイントです。
      security:
        - bearerAuth: []
      requestBody:
        required: true
        content:
          multipart/form-data:
            schema:
              type: object
              required:
                - file
              properties:
                file:
                  type: string
                  format: binary
                  description: MP4 to GIF input file (.mp4)
                settings:
                  type: string
                  description: |
                    Dedicated MP4 to GIF converter using the video-to-gif pipeline.
                    The output format is locked by the endpoint.
                clientJobId:
                  type: string
                  description: Client-provided job ID for SSE progress tracking
      responses:
        '202':
          description: Accepted for async processing. Track progress via SSE at /api/v1/jobs/{jobId}/progress.
          content:
            application/json:
              schema:
                type: object
                properties:
                  jobId:
                    type: string
                  async:
                    type: boolean
                    example: true
        '400':
          description: Invalid input or settings
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Error'
        '401':
          description: Authentication required
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/UnauthorizedError'
        '422':
          description: Processing failed
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Error'
  /api/v1/tools/video/mov-to-gif:
    post:
      operationId: movToGifTool
      tags:
        - Tools
      summary: MOV から GIF
      description: |
        MOV を GIF に変換
        これは `video-to-gif` に基づく専用の変換プリセットエンドポイントです。
      security:
        - bearerAuth: []
      requestBody:
        required: true
        content:
          multipart/form-data:
            schema:
              type: object
              required:
                - file
              properties:
                file:
                  type: string
                  format: binary
                  description: MOV to GIF input file (.mov)
                settings:
                  type: string
                  description: |
                    Dedicated MOV to GIF converter using the video-to-gif pipeline.
                    The output format is locked by the endpoint.
                clientJobId:
                  type: string
                  description: Client-provided job ID for SSE progress tracking
      responses:
        '202':
          description: Accepted for async processing. Track progress via SSE at /api/v1/jobs/{jobId}/progress.
          content:
            application/json:
              schema:
                type: object
                properties:
                  jobId:
                    type: string
                  async:
                    type: boolean
                    example: true
        '400':
          description: Invalid input or settings
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Error'
        '401':
          description: Authentication required
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/UnauthorizedError'
        '422':
          description: Processing failed
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Error'
  /api/v1/tools/video/mkv-to-gif:
    post:
      operationId: mkvToGifTool
      tags:
        - Tools
      summary: MKV から GIF
      description: |
        MKV を GIF に変換
        これは `video-to-gif` に基づく専用の変換プリセットエンドポイントです。
      security:
        - bearerAuth: []
      requestBody:
        required: true
        content:
          multipart/form-data:
            schema:
              type: object
              required:
                - file
              properties:
                file:
                  type: string
                  format: binary
                  description: MKV to GIF input file (.mkv)
                settings:
                  type: string
                  description: |
                    Dedicated MKV to GIF converter using the video-to-gif pipeline.
                    The output format is locked by the endpoint.
                clientJobId:
                  type: string
                  description: Client-provided job ID for SSE progress tracking
      responses:
        '202':
          description: Accepted for async processing. Track progress via SSE at /api/v1/jobs/{jobId}/progress.
          content:
            application/json:
              schema:
                type: object
                properties:
                  jobId:
                    type: string
                  async:
                    type: boolean
                    example: true
        '400':
          description: Invalid input or settings
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Error'
        '401':
          description: Authentication required
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/UnauthorizedError'
        '422':
          description: Processing failed
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Error'
  /api/v1/tools/video/avi-to-gif:
    post:
      operationId: aviToGifTool
      tags:
        - Tools
      summary: AVI から GIF
      description: |
        AVI を GIF に変換
        これは `video-to-gif` に基づく専用の変換プリセットエンドポイントです。
      security:
        - bearerAuth: []
      requestBody:
        required: true
        content:
          multipart/form-data:
            schema:
              type: object
              required:
                - file
              properties:
                file:
                  type: string
                  format: binary
                  description: AVI to GIF input file (.avi)
                settings:
                  type: string
                  description: |
                    Dedicated AVI to GIF converter using the video-to-gif pipeline.
                    The output format is locked by the endpoint.
                clientJobId:
                  type: string
                  description: Client-provided job ID for SSE progress tracking
      responses:
        '202':
          description: Accepted for async processing. Track progress via SSE at /api/v1/jobs/{jobId}/progress.
          content:
            application/json:
              schema:
                type: object
                properties:
                  jobId:
                    type: string
                  async:
                    type: boolean
                    example: true
        '400':
          description: Invalid input or settings
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Error'
        '401':
          description: Authentication required
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/UnauthorizedError'
        '422':
          description: Processing failed
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Error'
  /api/v1/tools/video/gif-to-mp4:
    post:
      operationId: gifToMp4Tool
      tags:
        - Tools
      summary: GIF から MP4
      description: |
        GIF を MP4 に変換
        これは `gif-to-video` に基づく専用の変換プリセットエンドポイントです。
      security:
        - bearerAuth: []
      requestBody:
        required: true
        content:
          multipart/form-data:
            schema:
              type: object
              required:
                - file
              properties:
                file:
                  type: string
                  format: binary
                  description: GIF to MP4 input file (.gif)
                settings:
                  type: string
                  description: |
                    Dedicated GIF to MP4 converter using the gif-to-video pipeline.
                    The output format is locked by the endpoint.
                clientJobId:
                  type: string
                  description: Client-provided job ID for SSE progress tracking
      responses:
        '200':
          description: Processed file
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ToolResponse'
        '400':
          description: Invalid input or settings
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Error'
        '401':
          description: Authentication required
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/UnauthorizedError'
        '422':
          description: Processing failed
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Error'
  /api/v1/tools/video/gif-to-webm:
    post:
      operationId: gifToWebmTool
      tags:
        - Tools
      summary: GIF から WEBM
      description: |
        GIF を WEBM に変換
        これは `gif-to-video` に基づく専用の変換プリセットエンドポイントです。
      security:
        - bearerAuth: []
      requestBody:
        required: true
        content:
          multipart/form-data:
            schema:
              type: object
              required:
                - file
              properties:
                file:
                  type: string
                  format: binary
                  description: GIF to WEBM input file (.gif)
                settings:
                  type: string
                  description: |
                    Dedicated GIF to WEBM converter using the gif-to-video pipeline.
                    The output format is locked by the endpoint.
                clientJobId:
                  type: string
                  description: Client-provided job ID for SSE progress tracking
      responses:
        '200':
          description: Processed file
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ToolResponse'
        '400':
          description: Invalid input or settings
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Error'
        '401':
          description: Authentication required
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/UnauthorizedError'
        '422':
          description: Processing failed
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Error'
  /api/v1/tools/video/gif-to-mov:
    post:
      operationId: gifToMovTool
      tags:
        - Tools
      summary: GIF から MOV
      description: |
        GIF を MOV に変換
        これは `gif-to-video` に基づく専用の変換プリセットエンドポイントです。
      security:
        - bearerAuth: []
      requestBody:
        required: true
        content:
          multipart/form-data:
            schema:
              type: object
              required:
                - file
              properties:
                file:
                  type: string
                  format: binary
                  description: GIF to MOV input file (.gif)
                settings:
                  type: string
                  description: |
                    Dedicated GIF to MOV converter using the gif-to-video pipeline.
                    The output format is locked by the endpoint.
                clientJobId:
                  type: string
                  description: Client-provided job ID for SSE progress tracking
      responses:
        '200':
          description: Processed file
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ToolResponse'
        '400':
          description: Invalid input or settings
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Error'
        '401':
          description: Authentication required
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/UnauthorizedError'
        '422':
          description: Processing failed
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Error'
  /api/v1/tools/video/mp4-to-mp3:
    post:
      operationId: mp4ToMp3Tool
      tags:
        - Tools
      summary: MP4 から MP3
      description: |
        MP4 を MP3 に変換
        これは `extract-audio` に基づく専用の変換プリセットエンドポイントです。
      security:
        - bearerAuth: []
      requestBody:
        required: true
        content:
          multipart/form-data:
            schema:
              type: object
              required:
                - file
              properties:
                file:
                  type: string
                  format: binary
                  description: MP4 to MP3 input file (.mp4)
                settings:
                  type: string
                  description: |
                    Dedicated MP4 to MP3 converter using the extract-audio pipeline.
                    The output format is locked by the endpoint.
                clientJobId:
                  type: string
                  description: Client-provided job ID for SSE progress tracking
      responses:
        '200':
          description: Processed file
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ToolResponse'
        '400':
          description: Invalid input or settings
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Error'
        '401':
          description: Authentication required
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/UnauthorizedError'
        '422':
          description: Processing failed
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Error'
  /api/v1/tools/video/mov-to-mp3:
    post:
      operationId: movToMp3Tool
      tags:
        - Tools
      summary: MOV から MP3
      description: |
        MOV を MP3 に変換
        これは `extract-audio` に基づく専用の変換プリセットエンドポイントです。
      security:
        - bearerAuth: []
      requestBody:
        required: true
        content:
          multipart/form-data:
            schema:
              type: object
              required:
                - file
              properties:
                file:
                  type: string
                  format: binary
                  description: MOV to MP3 input file (.mov)
                settings:
                  type: string
                  description: |
                    Dedicated MOV to MP3 converter using the extract-audio pipeline.
                    The output format is locked by the endpoint.
                clientJobId:
                  type: string
                  description: Client-provided job ID for SSE progress tracking
      responses:
        '200':
          description: Processed file
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ToolResponse'
        '400':
          description: Invalid input or settings
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Error'
        '401':
          description: Authentication required
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/UnauthorizedError'
        '422':
          description: Processing failed
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Error'
  /api/v1/tools/video/mkv-to-mp3:
    post:
      operationId: mkvToMp3Tool
      tags:
        - Tools
      summary: MKV から MP3
      description: |
        MKV を MP3 に変換
        これは `extract-audio` に基づく専用の変換プリセットエンドポイントです。
      security:
        - bearerAuth: []
      requestBody:
        required: true
        content:
          multipart/form-data:
            schema:
              type: object
              required:
                - file
              properties:
                file:
                  type: string
                  format: binary
                  description: MKV to MP3 input file (.mkv)
                settings:
                  type: string
                  description: |
                    Dedicated MKV to MP3 converter using the extract-audio pipeline.
                    The output format is locked by the endpoint.
                clientJobId:
                  type: string
                  description: Client-provided job ID for SSE progress tracking
      responses:
        '200':
          description: Processed file
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ToolResponse'
        '400':
          description: Invalid input or settings
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Error'
        '401':
          description: Authentication required
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/UnauthorizedError'
        '422':
          description: Processing failed
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Error'
  /api/v1/tools/video/webm-to-mp3:
    post:
      operationId: webmToMp3Tool
      tags:
        - Tools
      summary: WEBM から MP3
      description: |
        WEBM を MP3 に変換
        これは `extract-audio` に基づく専用の変換プリセットエンドポイントです。
      security:
        - bearerAuth: []
      requestBody:
        required: true
        content:
          multipart/form-data:
            schema:
              type: object
              required:
                - file
              properties:
                file:
                  type: string
                  format: binary
                  description: WEBM to MP3 input file (.webm)
                settings:
                  type: string
                  description: |
                    Dedicated WEBM to MP3 converter using the extract-audio pipeline.
                    The output format is locked by the endpoint.
                clientJobId:
                  type: string
                  description: Client-provided job ID for SSE progress tracking
      responses:
        '200':
          description: Processed file
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ToolResponse'
        '400':
          description: Invalid input or settings
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Error'
        '401':
          description: Authentication required
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/UnauthorizedError'
        '422':
          description: Processing failed
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Error'
  /api/v1/tools/video/avi-to-mp3:
    post:
      operationId: aviToMp3Tool
      tags:
        - Tools
      summary: AVI から MP3
      description: |
        AVI を MP3 に変換
        これは `extract-audio` に基づく専用の変換プリセットエンドポイントです。
      security:
        - bearerAuth: []
      requestBody:
        required: true
        content:
          multipart/form-data:
            schema:
              type: object
              required:
                - file
              properties:
                file:
                  type: string
                  format: binary
                  description: AVI to MP3 input file (.avi)
                settings:
                  type: string
                  description: |
                    Dedicated AVI to MP3 converter using the extract-audio pipeline.
                    The output format is locked by the endpoint.
                clientJobId:
                  type: string
                  description: Client-provided job ID for SSE progress tracking
      responses:
        '200':
          description: Processed file
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ToolResponse'
        '400':
          description: Invalid input or settings
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Error'
        '401':
          description: Authentication required
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/UnauthorizedError'
        '422':
          description: Processing failed
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Error'
  /api/v1/tools/video/mp4-to-wav:
    post:
      operationId: mp4ToWavTool
      tags:
        - Tools
      summary: MP4 から WAV
      description: |
        MP4 を WAV に変換
        これは `extract-audio` に基づく専用の変換プリセットエンドポイントです。
      security:
        - bearerAuth: []
      requestBody:
        required: true
        content:
          multipart/form-data:
            schema:
              type: object
              required:
                - file
              properties:
                file:
                  type: string
                  format: binary
                  description: MP4 to WAV input file (.mp4)
                settings:
                  type: string
                  description: |
                    Dedicated MP4 to WAV converter using the extract-audio pipeline.
                    The output format is locked by the endpoint.
                clientJobId:
                  type: string
                  description: Client-provided job ID for SSE progress tracking
      responses:
        '200':
          description: Processed file
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ToolResponse'
        '400':
          description: Invalid input or settings
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Error'
        '401':
          description: Authentication required
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/UnauthorizedError'
        '422':
          description: Processing failed
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Error'
  /api/v1/tools/video/mov-to-wav:
    post:
      operationId: movToWavTool
      tags:
        - Tools
      summary: MOV から WAV
      description: |
        MOV を WAV に変換
        これは `extract-audio` に基づく専用の変換プリセットエンドポイントです。
      security:
        - bearerAuth: []
      requestBody:
        required: true
        content:
          multipart/form-data:
            schema:
              type: object
              required:
                - file
              properties:
                file:
                  type: string
                  format: binary
                  description: MOV to WAV input file (.mov)
                settings:
                  type: string
                  description: |
                    Dedicated MOV to WAV converter using the extract-audio pipeline.
                    The output format is locked by the endpoint.
                clientJobId:
                  type: string
                  description: Client-provided job ID for SSE progress tracking
      responses:
        '200':
          description: Processed file
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ToolResponse'
        '400':
          description: Invalid input or settings
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Error'
        '401':
          description: Authentication required
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/UnauthorizedError'
        '422':
          description: Processing failed
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Error'
  /api/v1/tools/video/mp4-to-ogg:
    post:
      operationId: mp4ToOggTool
      tags:
        - Tools
      summary: MP4 から OGG
      description: |
        MP4 を OGG に変換
        これは `extract-audio` に基づく専用の変換プリセットエンドポイントです。
      security:
        - bearerAuth: []
      requestBody:
        required: true
        content:
          multipart/form-data:
            schema:
              type: object
              required:
                - file
              properties:
                file:
                  type: string
                  format: binary
                  description: MP4 to OGG input file (.mp4)
                settings:
                  type: string
                  description: |
                    Dedicated MP4 to OGG converter using the extract-audio pipeline.
                    The output format is locked by the endpoint.
                clientJobId:
                  type: string
                  description: Client-provided job ID for SSE progress tracking
      responses:
        '200':
          description: Processed file
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ToolResponse'
        '400':
          description: Invalid input or settings
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Error'
        '401':
          description: Authentication required
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/UnauthorizedError'
        '422':
          description: Processing failed
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Error'
  /api/v1/tools/audio/m4a-to-mp3:
    post:
      operationId: m4aToMp3Tool
      tags:
        - Tools
      summary: M4A から MP3
      description: |
        M4A を MP3 に変換
        これは `convert-audio` に基づく専用の変換プリセットエンドポイントです。
      security:
        - bearerAuth: []
      requestBody:
        required: true
        content:
          multipart/form-data:
            schema:
              type: object
              required:
                - file
              properties:
                file:
                  type: string
                  format: binary
                  description: M4A to MP3 input file (.m4a)
                settings:
                  type: string
                  description: |
                    Dedicated M4A to MP3 converter using the convert-audio pipeline.
                    The output format is locked by the endpoint.
                clientJobId:
                  type: string
                  description: Client-provided job ID for SSE progress tracking
      responses:
        '200':
          description: Processed file
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ToolResponse'
        '400':
          description: Invalid input or settings
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Error'
        '401':
          description: Authentication required
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/UnauthorizedError'
        '422':
          description: Processing failed
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Error'
  /api/v1/tools/audio/m4a-to-wav:
    post:
      operationId: m4aToWavTool
      tags:
        - Tools
      summary: M4A から WAV
      description: |
        M4A を WAV に変換
        これは `convert-audio` に基づく専用の変換プリセットエンドポイントです。
      security:
        - bearerAuth: []
      requestBody:
        required: true
        content:
          multipart/form-data:
            schema:
              type: object
              required:
                - file
              properties:
                file:
                  type: string
                  format: binary
                  description: M4A to WAV input file (.m4a)
                settings:
                  type: string
                  description: |
                    Dedicated M4A to WAV converter using the convert-audio pipeline.
                    The output format is locked by the endpoint.
                clientJobId:
                  type: string
                  description: Client-provided job ID for SSE progress tracking
      responses:
        '200':
          description: Processed file
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ToolResponse'
        '400':
          description: Invalid input or settings
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Error'
        '401':
          description: Authentication required
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/UnauthorizedError'
        '422':
          description: Processing failed
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Error'
  /api/v1/tools/audio/aac-to-mp3:
    post:
      operationId: aacToMp3Tool
      tags:
        - Tools
      summary: AAC から MP3
      description: |
        AAC を MP3 に変換
        これは `convert-audio` に基づく専用の変換プリセットエンドポイントです。
      security:
        - bearerAuth: []
      requestBody:
        required: true
        content:
          multipart/form-data:
            schema:
              type: object
              required:
                - file
              properties:
                file:
                  type: string
                  format: binary
                  description: AAC to MP3 input file (.aac)
                settings:
                  type: string
                  description: |
                    Dedicated AAC to MP3 converter using the convert-audio pipeline.
                    The output format is locked by the endpoint.
                clientJobId:
                  type: string
                  description: Client-provided job ID for SSE progress tracking
      responses:
        '200':
          description: Processed file
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ToolResponse'
        '400':
          description: Invalid input or settings
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Error'
        '401':
          description: Authentication required
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/UnauthorizedError'
        '422':
          description: Processing failed
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Error'
  /api/v1/tools/audio/aac-to-wav:
    post:
      operationId: aacToWavTool
      tags:
        - Tools
      summary: AAC から WAV
      description: |
        AAC を WAV に変換
        これは `convert-audio` に基づく専用の変換プリセットエンドポイントです。
      security:
        - bearerAuth: []
      requestBody:
        required: true
        content:
          multipart/form-data:
            schema:
              type: object
              required:
                - file
              properties:
                file:
                  type: string
                  format: binary
                  description: AAC to WAV input file (.aac)
                settings:
                  type: string
                  description: |
                    Dedicated AAC to WAV converter using the convert-audio pipeline.
                    The output format is locked by the endpoint.
                clientJobId:
                  type: string
                  description: Client-provided job ID for SSE progress tracking
      responses:
        '200':
          description: Processed file
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ToolResponse'
        '400':
          description: Invalid input or settings
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Error'
        '401':
          description: Authentication required
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/UnauthorizedError'
        '422':
          description: Processing failed
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Error'
  /api/v1/tools/audio/aac-to-flac:
    post:
      operationId: aacToFlacTool
      tags:
        - Tools
      summary: AAC から FLAC
      description: |
        AAC を FLAC に変換
        これは `convert-audio` に基づく専用の変換プリセットエンドポイントです。
      security:
        - bearerAuth: []
      requestBody:
        required: true
        content:
          multipart/form-data:
            schema:
              type: object
              required:
                - file
              properties:
                file:
                  type: string
                  format: binary
                  description: AAC to FLAC input file (.aac)
                settings:
                  type: string
                  description: |
                    Dedicated AAC to FLAC converter using the convert-audio pipeline.
                    The output format is locked by the endpoint.
                clientJobId:
                  type: string
                  description: Client-provided job ID for SSE progress tracking
      responses:
        '200':
          description: Processed file
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ToolResponse'
        '400':
          description: Invalid input or settings
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Error'
        '401':
          description: Authentication required
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/UnauthorizedError'
        '422':
          description: Processing failed
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Error'
  /api/v1/tools/audio/ogg-to-mp3:
    post:
      operationId: oggToMp3Tool
      tags:
        - Tools
      summary: OGG から MP3
      description: |
        OGG を MP3 に変換
        これは `convert-audio` に基づく専用の変換プリセットエンドポイントです。
      security:
        - bearerAuth: []
      requestBody:
        required: true
        content:
          multipart/form-data:
            schema:
              type: object
              required:
                - file
              properties:
                file:
                  type: string
                  format: binary
                  description: OGG to MP3 input file (.ogg)
                settings:
                  type: string
                  description: |
                    Dedicated OGG to MP3 converter using the convert-audio pipeline.
                    The output format is locked by the endpoint.
                clientJobId:
                  type: string
                  description: Client-provided job ID for SSE progress tracking
      responses:
        '200':
          description: Processed file
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ToolResponse'
        '400':
          description: Invalid input or settings
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Error'
        '401':
          description: Authentication required
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/UnauthorizedError'
        '422':
          description: Processing failed
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Error'
  /api/v1/tools/audio/ogg-to-wav:
    post:
      operationId: oggToWavTool
      tags:
        - Tools
      summary: OGG から WAV
      description: |
        OGG を WAV に変換
        これは `convert-audio` に基づく専用の変換プリセットエンドポイントです。
      security:
        - bearerAuth: []
      requestBody:
        required: true
        content:
          multipart/form-data:
            schema:
              type: object
              required:
                - file
              properties:
                file:
                  type: string
                  format: binary
                  description: OGG to WAV input file (.ogg)
                settings:
                  type: string
                  description: |
                    Dedicated OGG to WAV converter using the convert-audio pipeline.
                    The output format is locked by the endpoint.
                clientJobId:
                  type: string
                  description: Client-provided job ID for SSE progress tracking
      responses:
        '200':
          description: Processed file
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ToolResponse'
        '400':
          description: Invalid input or settings
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Error'
        '401':
          description: Authentication required
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/UnauthorizedError'
        '422':
          description: Processing failed
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Error'
  /api/v1/tools/audio/wav-to-mp3:
    post:
      operationId: wavToMp3Tool
      tags:
        - Tools
      summary: WAV から MP3
      description: |
        WAV を MP3 に変換
        これは `convert-audio` に基づく専用の変換プリセットエンドポイントです。
      security:
        - bearerAuth: []
      requestBody:
        required: true
        content:
          multipart/form-data:
            schema:
              type: object
              required:
                - file
              properties:
                file:
                  type: string
                  format: binary
                  description: WAV to MP3 input file (.wav)
                settings:
                  type: string
                  description: |
                    Dedicated WAV to MP3 converter using the convert-audio pipeline.
                    The output format is locked by the endpoint.
                clientJobId:
                  type: string
                  description: Client-provided job ID for SSE progress tracking
      responses:
        '200':
          description: Processed file
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ToolResponse'
        '400':
          description: Invalid input or settings
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Error'
        '401':
          description: Authentication required
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/UnauthorizedError'
        '422':
          description: Processing failed
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Error'
  /api/v1/tools/audio/mp3-to-wav:
    post:
      operationId: mp3ToWavTool
      tags:
        - Tools
      summary: MP3 から WAV
      description: |
        MP3 を WAV に変換
        これは `convert-audio` に基づく専用の変換プリセットエンドポイントです。
      security:
        - bearerAuth: []
      requestBody:
        required: true
        content:
          multipart/form-data:
            schema:
              type: object
              required:
                - file
              properties:
                file:
                  type: string
                  format: binary
                  description: MP3 to WAV input file (.mp3)
                settings:
                  type: string
                  description: |
                    Dedicated MP3 to WAV converter using the convert-audio pipeline.
                    The output format is locked by the endpoint.
                clientJobId:
                  type: string
                  description: Client-provided job ID for SSE progress tracking
      responses:
        '200':
          description: Processed file
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ToolResponse'
        '400':
          description: Invalid input or settings
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Error'
        '401':
          description: Authentication required
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/UnauthorizedError'
        '422':
          description: Processing failed
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Error'
  /api/v1/tools/audio/flac-to-mp3:
    post:
      operationId: flacToMp3Tool
      tags:
        - Tools
      summary: FLAC から MP3
      description: |
        FLAC を MP3 に変換
        これは `convert-audio` に基づく専用の変換プリセットエンドポイントです。
      security:
        - bearerAuth: []
      requestBody:
        required: true
        content:
          multipart/form-data:
            schema:
              type: object
              required:
                - file
              properties:
                file:
                  type: string
                  format: binary
                  description: FLAC to MP3 input file (.flac)
                settings:
                  type: string
                  description: |
                    Dedicated FLAC to MP3 converter using the convert-audio pipeline.
                    The output format is locked by the endpoint.
                clientJobId:
                  type: string
                  description: Client-provided job ID for SSE progress tracking
      responses:
        '200':
          description: Processed file
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ToolResponse'
        '400':
          description: Invalid input or settings
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Error'
        '401':
          description: Authentication required
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/UnauthorizedError'
        '422':
          description: Processing failed
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Error'
  /api/v1/tools/files/excel-to-csv:
    post:
      operationId: excelToCsvTool
      tags:
        - Tools
      summary: Excel から CSV
      description: |
        Excel を CSV に変換
        これは `convert-spreadsheet` に基づく専用の変換プリセットエンドポイントです。
      security:
        - bearerAuth: []
      requestBody:
        required: true
        content:
          multipart/form-data:
            schema:
              type: object
              required:
                - file
              properties:
                file:
                  type: string
                  format: binary
                  description: Excel to CSV input file (.xlsx, .xls)
                settings:
                  type: string
                  description: |
                    Dedicated Excel to CSV converter using the convert-spreadsheet pipeline.
                    The output format is locked by the endpoint.
                clientJobId:
                  type: string
                  description: Client-provided job ID for SSE progress tracking
      responses:
        '202':
          description: Accepted for async processing. Track progress via SSE at /api/v1/jobs/{jobId}/progress.
          content:
            application/json:
              schema:
                type: object
                properties:
                  jobId:
                    type: string
                  async:
                    type: boolean
                    example: true
        '400':
          description: Invalid input or settings
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Error'
        '401':
          description: Authentication required
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/UnauthorizedError'
        '422':
          description: Processing failed
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Error'
  /api/v1/readyz:
    get:
      operationId: readinessCheck
      tags:
        - System
      summary: レディネスチェック
      description: PostgreSQL、Redis、ローカルディスク容量を確認し、設定されている場合は S3 の到達性も確認します。ロードバランサーやオーケストレーター向けのパブリックエンドポイントです。
      security: []
      responses:
        '200':
          description: Instance is ready to receive traffic
        '503':
          description: Instance is not ready to receive traffic
  /api/v1/jobs/{jobId}/cancel:
    post:
      operationId: cancelJob
      tags:
        - System
      summary: ジョブのキャンセル
      description: キュー内または実行中のジョブのキャンセルを要求します。
      security:
        - bearerAuth: []
      parameters:
        - name: jobId
          in: path
          required: true
          schema:
            type: string
      responses:
        '200':
          description: Cancellation request result
        '401':
          description: Authentication required
  /api/v1/preferences:
    get:
      operationId: getPreferences
      tags:
        - Settings
      summary: ユーザー設定の取得
      description: 現在のユーザーのユーザーごとの設定マップを返します。
      security:
        - bearerAuth: []
      responses:
        '200':
          description: Preference map
        '401':
          description: Authentication required
    put:
      operationId: updatePreferences
      tags:
        - Settings
      summary: ユーザー設定の更新
      description: 現在のユーザーの 1 つ以上の設定をアップサートします。
      security:
        - bearerAuth: []
      requestBody:
        required: true
        content:
          application/json:
            schema:
              type: object
      responses:
        '200':
          description: Preferences updated
        '400':
          description: Invalid preferences body
        '401':
          description: Authentication required
  /api/auth/mfa/enroll:
    post:
      operationId: enrollMfa
      tags:
        - Auth
      summary: MFA 登録の開始
      description: 現在のユーザーの TOTP MFA 登録を開始します。エンタープライズ MFA 機能が必要です。
      security:
        - bearerAuth: []
      responses:
        '200':
          description: Enrollment URI and recovery codes
        '401':
          description: Authentication required
        '403':
          description: MFA feature not licensed
        '409':
          description: MFA enrollment already enabled or pending
  /api/auth/mfa/verify:
    post:
      operationId: verifyMfaEnrollment
      tags:
        - Auth
      summary: MFA 登録の確認
      description: 保留中の MFA 登録を TOTP コードで確定します。
      security:
        - bearerAuth: []
      requestBody:
        required: true
        content:
          application/json:
            schema:
              type: object
              required:
                - code
              properties:
                code:
                  type: string
      responses:
        '200':
          description: MFA enabled
        '400':
          description: No pending enrollment or invalid body
        '401':
          description: Invalid TOTP code
  /api/auth/mfa/complete:
    post:
      operationId: completeMfaLogin
      tags:
        - Auth
      summary: MFA ログインの完了
      description: 保留中のログインチャレンジを TOTP コードまたはリカバリコードで完了します。
      security: []
      requestBody:
        required: true
        content:
          application/json:
            schema:
              type: object
              required:
                - mfaToken
                - code
              properties:
                mfaToken:
                  type: string
                code:
                  type: string
      responses:
        '200':
          description: Session token and user profile
        '400':
          description: Invalid request body
        '401':
          description: Invalid or expired MFA challenge
  /api/auth/mfa/disable:
    post:
      operationId: disableMfa
      tags:
        - Auth
      summary: MFA の無効化
      description: TOTP コードを確認した上で現在のユーザーの MFA を無効化します。
      security:
        - bearerAuth: []
      requestBody:
        required: true
        content:
          application/json:
            schema:
              type: object
              required:
                - code
              properties:
                code:
                  type: string
      responses:
        '200':
          description: MFA disabled
        '400':
          description: MFA is not enabled or body is invalid
        '401':
          description: Authentication required or invalid code
  /api/auth/users/{id}/mfa/reset:
    post:
      operationId: resetUserMfa
      tags:
        - Auth
      summary: ユーザー MFA のリセット
      description: ユーザーの MFA 登録を管理者がリセットします。users:manage 権限が必要です。
      security:
        - bearerAuth: []
      parameters:
        - name: id
          in: path
          required: true
          schema:
            type: string
      responses:
        '200':
          description: MFA reset
        '401':
          description: Authentication required
        '403':
          description: Insufficient permissions
        '404':
          description: User not found
  /api/auth/oidc/login:
    get:
      operationId: oidcLogin
      tags:
        - Auth
      summary: OIDC ログインの開始
      description: 設定された OpenID Connect プロバイダーにリダイレクトします。
      security: []
      responses:
        '302':
          description: Redirect to identity provider
        '500':
          description: OIDC provider discovery failed
  /api/auth/oidc/callback:
    get:
      operationId: oidcCallback
      tags:
        - Auth
      summary: OIDC コールバック
      description: OIDC 認可コードフローを完了し、セッションを作成します。
      security: []
      responses:
        '302':
          description: Redirect to the application
  /api/auth/saml/metadata:
    get:
      operationId: samlMetadata
      tags:
        - Auth
      summary: SAML メタデータ
      description: SAML サービスプロバイダーのメタデータ XML を返します。
      security: []
      responses:
        '200':
          description: SAML metadata XML
  /api/auth/saml/login:
    get:
      operationId: samlLogin
      tags:
        - Auth
      summary: SAML ログインの開始
      description: 設定された SAML アイデンティティプロバイダーにリダイレクトします。
      security: []
      responses:
        '302':
          description: Redirect to identity provider
  /api/auth/saml/callback:
    post:
      operationId: samlCallback
      tags:
        - Auth
      summary: SAML コールバック
      description: SAML ログインのアサーションコンシューマーサービスエンドポイント。
      security: []
      responses:
        '302':
          description: Redirect to the application
  /api/v1/files/{id}/preview:
    get:
      operationId: getFilePreview
      tags:
        - Files
      summary: ファイルプレビュー
      description: 保存済みの PDF、Office ドキュメント、動画、音声ファイルについて、ブラウザー互換のプレビューをストリーミングまたは生成します。
      security:
        - bearerAuth: []
      parameters:
        - name: id
          in: path
          required: true
          schema:
            type: string
      responses:
        '200':
          description: Preview binary
        '400':
          description: Preview not supported
        '401':
          description: Authentication required
        '404':
          description: File not found
        '422':
          description: Could not generate preview
  /api/v1/preview/generate:
    post:
      operationId: generatePreview
      tags:
        - Files
      summary: アップロードプレビューの生成
      description: アップロードされたメディアファイルについて、オンデマンドで MP4 または MP3 のプレビューを生成します。
      security: []
      requestBody:
        required: true
        content:
          multipart/form-data:
            schema:
              type: object
              required:
                - file
              properties:
                file:
                  type: string
                  format: binary
      responses:
        '200':
          description: Preview binary
        '400':
          description: Unsupported file type or missing file
        '422':
          description: Could not generate preview
  /api/v1/admin/log-level:
    get:
      operationId: getLogLevel
      tags:
        - Admin
      summary: ログレベルの取得
      description: 現在のランタイムログレベルを読み取ります。settings:write 権限が必要です。
      security:
        - bearerAuth: []
      responses:
        '200':
          description: Current log level
        '403':
          description: Insufficient permissions
    post:
      operationId: setLogLevel
      tags:
        - Admin
      summary: ログレベルの設定
      description: ランタイムログレベルを変更します。settings:write 権限が必要です。
      security:
        - bearerAuth: []
      requestBody:
        required: true
        content:
          application/json:
            schema:
              type: object
              required:
                - level
              properties:
                level:
                  type: string
                  enum:
                    - fatal
                    - error
                    - warn
                    - info
                    - debug
                    - trace
                    - silent
      responses:
        '200':
          description: New log level
        '400':
          description: Invalid log level
        '403':
          description: Insufficient permissions
  /api/v1/metrics:
    get:
      operationId: getMetrics
      tags:
        - Admin
      summary: Prometheus メトリクス
      description: Prometheus メトリクスを返します。system:health 権限が必要です。
      security:
        - bearerAuth: []
      responses:
        '200':
          description: Prometheus text format
        '403':
          description: Insufficient permissions
  /api/v1/admin/support-bundle:
    get:
      operationId: getSupportBundle
      tags:
        - Admin
      summary: サポートバンドル
      description: 編集除去済みの診断サポートバンドル ZIP をダウンロードします。system:health 権限が必要です。
      security:
        - bearerAuth: []
      responses:
        '200':
          description: Support bundle ZIP
        '403':
          description: Insufficient permissions
  /api/v1/admin/usage:
    get:
      operationId: getAdminUsage
      tags:
        - Admin
      summary: 使用状況ダッシュボードのデータ
      description: ローカルの使用状況ダッシュボードデータを返します。audit:read 権限が必要です。
      security:
        - bearerAuth: []
      parameters:
        - name: days
          in: query
          required: false
          schema:
            type: string
      responses:
        '200':
          description: Usage dashboard data
        '400':
          description: Invalid query parameters
        '403':
          description: Insufficient permissions
  /api/v1/admin/backup-status:
    get:
      operationId: getBackupStatus
      tags:
        - Admin
      summary: バックアップ状態
      description: 直近のバックアップのメタデータと鮮度を返します。system:health 権限が必要です。
      security:
        - bearerAuth: []
      responses:
        '200':
          description: Backup status
        '403':
          description: Insufficient permissions
    post:
      operationId: setBackupStatus
      tags:
        - Admin
      summary: バックアップ状態の記録
      description: 完了したバックアップを記録します。system:health 権限が必要です。
      security:
        - bearerAuth: []
      requestBody:
        required: true
        content:
          application/json:
            schema:
              type: object
              required:
                - type
              properties:
                type:
                  type: string
                sizeBytes:
                  type: integer
                notes:
                  type: string
      responses:
        '200':
          description: Backup status recorded
        '400':
          description: Invalid backup status
        '403':
          description: Insufficient permissions
  /api/v1/admin/features/import:
    post:
      operationId: importFeatureBundle
      tags:
        - Features
      summary: 機能バンドルのインポート
      description: |-
        従来の AI バンドル アーカイブをインポートするか、オフラインで正確な OCR をインポートします
        署名付きインデックスと一致するプラットフォーム アーカイブを使用してリリースします。 OCR
        import は同じ署名、ハッシュ、互換性、抽出を実行します。
        および smoke-test は、正確なインストールを含むオンライン インストールとしてチェックします。
        ランタイムの 4 GiB 有効メモリの最小値。 Fast Tesseract OCR ではありません
        この要件の対象となります。 features:manage 権限が必要です。
      security:
        - bearerAuth: []
      requestBody:
        required: true
        content:
          multipart/form-data:
            schema:
              type: object
              oneOf:
                - required:
                    - file
                - required:
                    - index
                    - archive
              properties:
                file:
                  type: string
                  format: binary
                  description: One legacy AI bundle archive; mutually exclusive with index/archive
                index:
                  type: string
                  format: binary
                  description: Signed ocr-runtime-index.json from the SnapOtter release
                archive:
                  type: string
                  format: binary
                  description: OCR runtime archive for the current Linux amd64 or arm64 target; must follow index
      responses:
        '200':
          description: Bundle verified, imported, and activated
        '400':
          description: Invalid archive
        '403':
          description: Insufficient permissions
        '409':
          description: Import lock is held
        '507':
          description: Insufficient storage for the authenticated archive and expanded runtime
  /api/v1/enterprise/scim/token:
    post:
      operationId: createScimToken
      tags:
        - Enterprise
      summary: SCIM トークンの作成
      description: SCIM ベアラートークンを生成します。users:manage 権限とエンタープライズ SCIM 機能が必要です。
      security:
        - bearerAuth: []
      responses:
        '201':
          description: SCIM token created
        '403':
          description: SCIM feature not licensed or insufficient permissions
    delete:
      operationId: revokeScimToken
      tags:
        - Enterprise
      summary: SCIM トークンの失効
      description: 現在の SCIM ベアラートークンを失効させます。users:manage 権限とエンタープライズ SCIM 機能が必要です。
      security:
        - bearerAuth: []
      responses:
        '204':
          description: SCIM token revoked
        '403':
          description: SCIM feature not licensed or insufficient permissions
  /api/v1/scim/v2/ServiceProviderConfig:
    get:
      operationId: getScimServiceProviderConfig
      tags:
        - Enterprise
      summary: SCIM サービスプロバイダー設定
      description: パブリックな SCIM ディスカバリーエンドポイント。
      security: []
      responses:
        '200':
          description: SCIM service provider capabilities
  /api/v1/scim/v2/Schemas:
    get:
      operationId: getScimSchemas
      tags:
        - Enterprise
      summary: SCIM スキーマ
      description: パブリックな SCIM スキーマディスカバリーエンドポイント。
      security: []
      responses:
        '200':
          description: SCIM schemas
  /api/v1/scim/v2/ResourceTypes:
    get:
      operationId: getScimResourceTypes
      tags:
        - Enterprise
      summary: SCIM リソースタイプ
      description: パブリックな SCIM リソースタイプディスカバリーエンドポイント。
      security: []
      responses:
        '200':
          description: SCIM resource types
  /api/v1/scim/v2/Users:
    get:
      operationId: listScimUsers
      tags:
        - Enterprise
      summary: SCIM ユーザーの一覧
      description: SCIM ユーザーを一覧表示します。SCIM ベアラートークンが必要です。
      responses:
        '200':
          description: SCIM user list
    post:
      operationId: createScimUser
      tags:
        - Enterprise
      summary: SCIM ユーザーの作成
      description: SCIM ユーザーを作成します。SCIM ベアラートークンが必要です。
      responses:
        '201':
          description: SCIM user created
  /api/v1/scim/v2/Users/{id}:
    get:
      operationId: getScimUser
      tags:
        - Enterprise
      summary: SCIM ユーザーの取得
      description: SCIM ユーザーを ID で取得します。SCIM ベアラートークンが必要です。
      parameters:
        - name: id
          in: path
          required: true
          schema:
            type: string
      responses:
        '200':
          description: SCIM user
    put:
      operationId: updateScimUser
      tags:
        - Enterprise
      summary: SCIM ユーザーの更新
      description: SCIM ユーザーを ID で置き換えます。SCIM ベアラートークンが必要です。
      parameters:
        - name: id
          in: path
          required: true
          schema:
            type: string
      responses:
        '200':
          description: SCIM user updated
    delete:
      operationId: deleteScimUser
      tags:
        - Enterprise
      summary: SCIM ユーザーの削除
      description: SCIM ユーザーを ID でソフト無効化します。SCIM ベアラートークンが必要です。
      parameters:
        - name: id
          in: path
          required: true
          schema:
            type: string
      responses:
        '200':
          description: SCIM user deactivated
  /api/v1/scim/v2/Groups:
    get:
      operationId: listScimGroups
      tags:
        - Enterprise
      summary: SCIM グループの一覧
      description: SCIM グループを一覧表示します。SCIM ベアラートークンが必要です。
      responses:
        '200':
          description: SCIM group list
    post:
      operationId: createScimGroup
      tags:
        - Enterprise
      summary: SCIM グループの作成
      description: SCIM グループを作成します。SCIM ベアラートークンが必要です。
      responses:
        '201':
          description: SCIM group created
  /api/v1/scim/v2/Groups/{id}:
    get:
      operationId: getScimGroup
      tags:
        - Enterprise
      summary: SCIM グループの取得
      description: SCIM グループを ID で取得します。SCIM ベアラートークンが必要です。
      parameters:
        - name: id
          in: path
          required: true
          schema:
            type: string
      responses:
        '200':
          description: SCIM group
    put:
      operationId: updateScimGroup
      tags:
        - Enterprise
      summary: SCIM グループの更新
      description: SCIM グループを ID で置き換えます。SCIM ベアラートークンが必要です。
      parameters:
        - name: id
          in: path
          required: true
          schema:
            type: string
      responses:
        '200':
          description: SCIM group updated
    delete:
      operationId: deleteScimGroup
      tags:
        - Enterprise
      summary: SCIM グループの削除
      description: SCIM グループを ID で削除します。SCIM ベアラートークンが必要です。
      parameters:
        - name: id
          in: path
          required: true
          schema:
            type: string
      responses:
        '204':
          description: SCIM group deleted
  /api/v1/enterprise/audit/export:
    get:
      operationId: exportAuditLog
      tags:
        - Enterprise
      summary: 監査ログのエクスポート
      description: 監査エントリを JSON または CSV としてエクスポートします。audit:read 権限とエンタープライズ監査エクスポート機能が必要です。
      security:
        - bearerAuth: []
      responses:
        '200':
          description: Audit export
        '400':
          description: Invalid query parameters
        '403':
          description: Feature not licensed or insufficient permissions
  /api/v1/enterprise/config/export:
    get:
      operationId: exportEnterpriseConfig
      tags:
        - Enterprise
      summary: 設定のエクスポート
      description: 編集除去済みのインスタンス設定、カスタムロール、チームをエクスポートします。system:health 権限が必要です。
      security:
        - bearerAuth: []
      responses:
        '200':
          description: Configuration export
        '403':
          description: Feature not licensed or insufficient permissions
  /api/v1/enterprise/config/import:
    post:
      operationId: importEnterpriseConfig
      tags:
        - Enterprise
      summary: 設定のインポート
      description: インスタンス設定、カスタムロール、チームをインポートします。system:health 権限が必要です。
      security:
        - bearerAuth: []
      responses:
        '200':
          description: Import result
        '400':
          description: Invalid import payload
        '403':
          description: Feature not licensed or insufficient permissions
  /api/v1/enterprise/ip-allowlist:
    get:
      operationId: getIpAllowlist
      tags:
        - Enterprise
      summary: IP 許可リストの取得
      description: エンタープライズ IP 許可リストを読み取ります。security:manage 権限が必要です。
      security:
        - bearerAuth: []
      responses:
        '200':
          description: IP allowlist
        '403':
          description: Feature not licensed or insufficient permissions
    put:
      operationId: updateIpAllowlist
      tags:
        - Enterprise
      summary: IP 許可リストの更新
      description: 自己ロックアウト防止付きで CIDR 許可リストのエントリを更新します。security:manage 権限が必要です。
      security:
        - bearerAuth: []
      responses:
        '200':
          description: IP allowlist updated
        '400':
          description: Invalid CIDR entries
        '403':
          description: Feature not licensed or insufficient permissions
  /api/v1/enterprise/legal-hold:
    get:
      operationId: listLegalHolds
      tags:
        - Enterprise
      summary: リーガルホールドの一覧
      description: リーガルホールド下のユーザーとチームを一覧表示します。compliance:manage 権限が必要です。
      security:
        - bearerAuth: []
      responses:
        '200':
          description: Legal holds
        '403':
          description: Feature not licensed or insufficient permissions
    put:
      operationId: updateLegalHold
      tags:
        - Enterprise
      summary: リーガルホールドの更新
      description: ユーザーまたはチームにリーガルホールドを適用または解除します。compliance:manage 権限が必要です。
      security:
        - bearerAuth: []
      responses:
        '200':
          description: Legal hold updated
        '400':
          description: Invalid request body
        '403':
          description: Feature not licensed or insufficient permissions
  /api/v1/enterprise/siem/config:
    get:
      operationId: getSiemConfig
      tags:
        - Enterprise
      summary: SIEM 設定の取得
      description: SIEM 転送の設定を読み取ります。webhooks:manage 権限が必要です。
      security:
        - bearerAuth: []
      responses:
        '200':
          description: SIEM config
        '403':
          description: Feature not licensed or insufficient permissions
    put:
      operationId: updateSiemConfig
      tags:
        - Enterprise
      summary: SIEM 設定の更新
      description: SIEM 転送の設定を更新します。webhooks:manage 権限が必要です。
      security:
        - bearerAuth: []
      responses:
        '200':
          description: SIEM config updated
        '400':
          description: Invalid SIEM config
        '403':
          description: Feature not licensed or insufficient permissions
  /api/v1/enterprise/webhooks:
    get:
      operationId: listWebhooks
      tags:
        - Enterprise
      summary: Webhook の一覧
      description: Webhook の宛先を一覧表示します。webhooks:manage 権限が必要です。
      security:
        - bearerAuth: []
      responses:
        '200':
          description: Webhook destinations
        '403':
          description: Feature not licensed or insufficient permissions
    post:
      operationId: createWebhook
      tags:
        - Enterprise
      summary: Webhook の作成
      description: Webhook の宛先を作成します。webhooks:manage 権限が必要です。
      security:
        - bearerAuth: []
      responses:
        '201':
          description: Webhook created
        '400':
          description: Invalid webhook destination
        '403':
          description: Feature not licensed or insufficient permissions
  /api/v1/enterprise/webhooks/{index}:
    put:
      operationId: updateWebhook
      tags:
        - Enterprise
      summary: Webhook の更新
      description: インデックスで Webhook の宛先を更新します。webhooks:manage 権限が必要です。
      security:
        - bearerAuth: []
      parameters:
        - name: index
          in: path
          required: true
          schema:
            type: string
      responses:
        '200':
          description: Webhook updated
        '400':
          description: Invalid index or destination
        '404':
          description: Webhook not found
    delete:
      operationId: deleteWebhook
      tags:
        - Enterprise
      summary: Webhook の削除
      description: インデックスで Webhook の宛先を削除します。webhooks:manage 権限が必要です。
      security:
        - bearerAuth: []
      parameters:
        - name: index
          in: path
          required: true
          schema:
            type: string
      responses:
        '200':
          description: Webhook deleted
        '400':
          description: Invalid index
        '404':
          description: Webhook not found
  /api/v1/enterprise/webhooks/{index}/test:
    post:
      operationId: testWebhook
      tags:
        - Enterprise
      summary: Webhook のテスト
      description: Webhook の宛先にテストペイロードを送信します。webhooks:manage 権限が必要です。
      security:
        - bearerAuth: []
      parameters:
        - name: index
          in: path
          required: true
          schema:
            type: string
      responses:
        '200':
          description: Test result
        '400':
          description: Invalid index
        '404':
          description: Webhook not found
  /api/v1/enterprise/users/{id}/export:
    post:
      operationId: startGdprUserExport
      tags:
        - Enterprise
      summary: GDPR エクスポートの開始
      description: GDPR のユーザーデータエクスポートジョブを開始します。compliance:manage 権限が必要です。
      security:
        - bearerAuth: []
      parameters:
        - name: id
          in: path
          required: true
          schema:
            type: string
      responses:
        '202':
          description: Export job started
        '403':
          description: Feature not licensed or insufficient permissions
        '404':
          description: User not found
  /api/v1/enterprise/users/{id}/export/{jobId}:
    get:
      operationId: getGdprUserExport
      tags:
        - Enterprise
      summary: GDPR エクスポート状態の取得
      description: GDPR ユーザーエクスポートジョブの状態とダウンロード URL を読み取ります。compliance:manage 権限が必要です。
      security:
        - bearerAuth: []
      parameters:
        - name: id
          in: path
          required: true
          schema:
            type: string
        - name: jobId
          in: path
          required: true
          schema:
            type: string
      responses:
        '200':
          description: Export status
        '404':
          description: Export job not found
  /api/v1/enterprise/users/{id}/purge:
    delete:
      operationId: purgeEnterpriseUser
      tags:
        - Enterprise
      summary: ユーザーデータの完全削除
      description: 明示的な確認の後、ユーザーのデータを恒久的に完全削除します。compliance:manage 権限が必要です。
      security:
        - bearerAuth: []
      parameters:
        - name: id
          in: path
          required: true
          schema:
            type: string
      responses:
        '200':
          description: User purged
        '400':
          description: Confirmation required
        '409':
          description: User or team is under legal hold
  /api/v1/enterprise/teams/{id}/purge:
    delete:
      operationId: purgeEnterpriseTeam
      tags:
        - Enterprise
      summary: チームデータの完全削除
      description: 明示的な確認の後、チームのデータを恒久的に完全削除します。compliance:manage 権限が必要です。
      security:
        - bearerAuth: []
      parameters:
        - name: id
          in: path
          required: true
          schema:
            type: string
      responses:
        '200':
          description: Team purged
        '400':
          description: Confirmation required
        '409':
          description: Team is under legal hold
  /api/v1/admin/version:
    get:
      operationId: getAdminVersion
      tags:
        - Admin
      summary: バージョンメタデータ
      description: アプリのバージョン、ビルド日、Node のバージョン、スキーマバージョンを読み取ります。system:health 権限とエンタープライズのアップグレード管理機能が必要です。
      security:
        - bearerAuth: []
      responses:
        '200':
          description: Version metadata
        '403':
          description: Feature not licensed or insufficient permissions
  /api/v1/admin/migrations/pending:
    get:
      operationId: getPendingMigrations
      tags:
        - Admin
      summary: マイグレーション状態
      description: パッケージ化されたマイグレーションと適用済みマイグレーションを比較します。system:health 権限とエンタープライズのアップグレード管理機能が必要です。
      security:
        - bearerAuth: []
      responses:
        '200':
          description: Migration status
        '403':
          description: Feature not licensed or insufficient permissions
  /api/v1/admin/upgrade-check:
    get:
      operationId: getUpgradeCheck
      tags:
        - Admin
      summary: アップグレード準備状況
      description: アップグレードの準備状況チェックを実行します。system:health 権限とエンタープライズのアップグレード管理機能が必要です。
      security:
        - bearerAuth: []
      responses:
        '200':
          description: Upgrade readiness result
        '403':
          description: Feature not licensed or insufficient permissions
  /llms.txt:
    get:
      operationId: getLlmsTxt
      tags:
        - System
      summary: LLM 概要
      description: OpenAPI 仕様から生成した、LLM 向けのプレーンテキスト API 概要。
      security: []
      responses:
        '200':
          description: Plain-text LLM summary
          content:
            text/plain:
              schema:
                type: string
  /llms-full.txt:
    get:
      operationId: getLlmsFullTxt
      tags:
        - System
      summary: 完全な LLM API ドキュメント
      description: OpenAPI 仕様から生成した、完全なプレーンテキスト API ドキュメント。
      security: []
      responses:
        '200':
          description: Full plain-text API docs
          content:
            text/plain:
              schema:
                type: string
  /api/v1/openapi.yaml:
    get:
      operationId: getOpenApiYaml
      tags:
        - System
      summary: OpenAPI YAML
      description: この SnapOtter インスタンス向けの機械可読な OpenAPI 3.1 仕様。
      security: []
      responses:
        '200':
          description: OpenAPI YAML document
          content:
            text/yaml:
              schema:
                type: string
  /api/v1/tools/popular:
    get:
      operationId: getPopularTools
      tags:
        - Tools
      summary: 人気のツール
      description: 人気のツール ID を最大 12 件返します。使用状況データが乏しい場合は厳選したデフォルトリストにフォールバックします。
      security: []
      responses:
        '200':
          description: Popular tool IDs
          content:
            application/json:
              schema:
                type: object
                properties:
                  tools:
                    type: array
                    items:
                      type: string
x-i18n:
  locale: ja
  generator: scripts/i18n/adapters/api-spec.mjs
  entries:
    info.description:
      sourceHash: 0a7d56a602a9
      provenance: machine
      outputHash: 37d81c2053a1
    tags.Tools.description:
      sourceHash: 9c71553d06df
      provenance: machine
      outputHash: f7e07b8200c3
    tags.Batch.description:
      sourceHash: f9c99eda7653
      provenance: machine
      outputHash: 982478ffabf2
    tags.Pipelines.description:
      sourceHash: 18d60931b26d
      provenance: machine
      outputHash: 9bfc44882340
    tags.Files.description:
      sourceHash: c3b0e1590a61
      provenance: machine
      outputHash: d2acf7e6a2c7
    tags.Auth.description:
      sourceHash: 23f6a0fcf611
      provenance: machine
      outputHash: 0192a4fb5fbd
    tags.API Keys.description:
      sourceHash: fb725a6a642a
      provenance: machine
      outputHash: 95d41ba6e9fc
    tags.Settings.description:
      sourceHash: 25788b024fe2
      provenance: machine
      outputHash: d95165261cdd
    tags.Teams.description:
      sourceHash: 750d92c8bb9f
      provenance: machine
      outputHash: e1debb399d47
    tags.Roles.description:
      sourceHash: d031f589a884
      provenance: machine
      outputHash: bb13af6aad58
    tags.Audit.description:
      sourceHash: ed43b35a8636
      provenance: machine
      outputHash: 4d705e320371
    tags.Analytics.description:
      sourceHash: 36832a1b1df0
      provenance: machine
      outputHash: e9470ae981fa
    tags.Features.description:
      sourceHash: a6cdd16968b5
      provenance: machine
      outputHash: f18680eff7a9
    tags.Admin.description:
      sourceHash: c19c4b4f68c4
      provenance: machine
      outputHash: de536d6b37ae
    tags.System.description:
      sourceHash: 91561bc11296
      provenance: machine
      outputHash: 7cf2aed5e3d8
    tags.Enterprise.description:
      sourceHash: 91b6b46b0dd1
      provenance: machine
      outputHash: c949599f0153
    paths./api/v1/health.get.summary:
      sourceHash: 14290e1d0681
      provenance: machine
      outputHash: 09809b84c834
    paths./api/v1/health.get.description:
      sourceHash: 784e94eaa9df
      provenance: machine
      outputHash: 354511038ea4
    paths./api/v1/tools/image/resize.post.summary:
      sourceHash: 2956e06ac065
      provenance: machine
      outputHash: 235b48d5a237
    paths./api/v1/tools/image/resize.post.description:
      sourceHash: 3dd014df5258
      provenance: machine
      outputHash: 08e071786913
    paths./api/v1/tools/image/content-aware-resize.post.summary:
      sourceHash: 7f86a6451d78
      provenance: machine
      outputHash: 4061bcd54b24
    paths./api/v1/tools/image/content-aware-resize.post.description:
      sourceHash: fe6de26388f5
      provenance: machine
      outputHash: ade55f6b6d70
    paths./api/v1/tools/image/crop.post.summary:
      sourceHash: 98caf7c43c6f
      provenance: machine
      outputHash: 631432f8ff93
    paths./api/v1/tools/image/crop.post.description:
      sourceHash: db6c5c89d359
      provenance: machine
      outputHash: 0184ef3b1cad
    paths./api/v1/tools/image/rotate.post.summary:
      sourceHash: c98856efbb08
      provenance: machine
      outputHash: 0e7a3a26966b
    paths./api/v1/tools/image/rotate.post.description:
      sourceHash: 5360b7d1a4c3
      provenance: machine
      outputHash: 73e5b2e9bc7b
    paths./api/v1/tools/image/convert.post.summary:
      sourceHash: 3c2ee9bc3ec8
      provenance: machine
      outputHash: 1751296cdb97
    paths./api/v1/tools/image/convert.post.description:
      sourceHash: 2298d8af460d
      provenance: machine
      outputHash: 47d4a02fc026
    paths./api/v1/tools/image/compress.post.summary:
      sourceHash: 033fc0445b90
      provenance: machine
      outputHash: 91b7b6dd76ba
    paths./api/v1/tools/image/compress.post.description:
      sourceHash: 01f63e9e928a
      provenance: machine
      outputHash: 62dbcc6f81db
    paths./api/v1/tools/image/strip-metadata.post.summary:
      sourceHash: 013af4252599
      provenance: machine
      outputHash: 142ad18cb08e
    paths./api/v1/tools/image/strip-metadata.post.description:
      sourceHash: b5718bfd5335
      provenance: machine
      outputHash: 7ed91fa4d168
    paths./api/v1/tools/image/border.post.summary:
      sourceHash: 1d23e2771077
      provenance: machine
      outputHash: d07aa0c2cea2
    paths./api/v1/tools/image/border.post.description:
      sourceHash: 6d48679c08cd
      provenance: machine
      outputHash: be426ace83c8
    paths./api/v1/tools/image/beautify.post.summary:
      sourceHash: 096b365107c5
      provenance: machine
      outputHash: 3651cc29df57
    paths./api/v1/tools/image/beautify.post.description:
      sourceHash: 7db7fb58263f
      provenance: machine
      outputHash: 80ecfb56c6dd
    paths./api/v1/tools/image/adjust-colors.post.summary:
      sourceHash: a45b7d1e2535
      provenance: machine
      outputHash: 236a8f552dae
    paths./api/v1/tools/image/adjust-colors.post.description:
      sourceHash: 1fb36db32da7
      provenance: machine
      outputHash: 2a00cf714a44
    paths./api/v1/tools/image/sharpening.post.summary:
      sourceHash: 73fb568dcab6
      provenance: machine
      outputHash: 691ae659fb67
    paths./api/v1/tools/image/sharpening.post.description:
      sourceHash: 8080cda0610a
      provenance: machine
      outputHash: d12cafd54415
    paths./api/v1/tools/image/optimize-for-web.post.summary:
      sourceHash: 54245d072fef
      provenance: machine
      outputHash: 2a8041f76ec5
    paths./api/v1/tools/image/optimize-for-web.post.description:
      sourceHash: e846eaabbabc
      provenance: machine
      outputHash: 86f940184c6d
    paths./api/v1/tools/image/optimize-for-web/preview.post.summary:
      sourceHash: f50e6149b56c
      provenance: machine
      outputHash: e8af6e1fbc5b
    paths./api/v1/tools/image/optimize-for-web/preview.post.description:
      sourceHash: 10a427197f6e
      provenance: machine
      outputHash: 3a27df475734
    paths./api/v1/tools/image/image-enhancement.post.summary:
      sourceHash: 246a536c8207
      provenance: machine
      outputHash: 94f04a0b98f4
    paths./api/v1/tools/image/image-enhancement.post.description:
      sourceHash: 17ec5e5e2b04
      provenance: machine
      outputHash: 78cd2fab0c17
    paths./api/v1/tools/image/image-enhancement/analyze.post.summary:
      sourceHash: 2697cf8b079f
      provenance: machine
      outputHash: 446a22316c09
    paths./api/v1/tools/image/image-enhancement/analyze.post.description:
      sourceHash: 7712bfd40c3a
      provenance: machine
      outputHash: 2674ef68cb62
    paths./api/v1/tools/image/noise-removal.post.summary:
      sourceHash: 9301fe47b563
      provenance: machine
      outputHash: 4106e72adc2d
    paths./api/v1/tools/image/noise-removal.post.description:
      sourceHash: e16aa743abcb
      provenance: machine
      outputHash: af432793023f
    paths./api/v1/tools/image/red-eye-removal.post.summary:
      sourceHash: d9315c5a87b4
      provenance: machine
      outputHash: 2e35f5210308
    paths./api/v1/tools/image/red-eye-removal.post.description:
      sourceHash: 479c687988eb
      provenance: machine
      outputHash: 2f9eed1f81c9
    paths./api/v1/tools/image/restore-photo.post.summary:
      sourceHash: 3ef425c0ba9b
      provenance: machine
      outputHash: f04bec951714
    paths./api/v1/tools/image/restore-photo.post.description:
      sourceHash: 6e3b2de0372c
      provenance: machine
      outputHash: 103dd81e0a62
    paths./api/v1/tools/image/passport-photo/analyze.post.summary:
      sourceHash: 0a3c3687ff5f
      provenance: machine
      outputHash: 57fcc5bfdf54
    paths./api/v1/tools/image/passport-photo/analyze.post.description:
      sourceHash: 235c51cb497e
      provenance: machine
      outputHash: 792ea248ac50
    paths./api/v1/tools/image/passport-photo/generate.post.summary:
      sourceHash: 029eecf0438e
      provenance: machine
      outputHash: 6496716be7d6
    paths./api/v1/tools/image/passport-photo/generate.post.description:
      sourceHash: d0edd7c634d9
      provenance: machine
      outputHash: debacc4de381
    paths./api/v1/tools/image/colorize.post.summary:
      sourceHash: 34df56e96234
      provenance: machine
      outputHash: a9be2baa75be
    paths./api/v1/tools/image/colorize.post.description:
      sourceHash: 04d7dc54396a
      provenance: machine
      outputHash: f4970e04f60e
    paths./api/v1/tools/image/enhance-faces.post.summary:
      sourceHash: 74f27131fec9
      provenance: machine
      outputHash: 98b6544adfd9
    paths./api/v1/tools/image/enhance-faces.post.description:
      sourceHash: beec2f7e8561
      provenance: machine
      outputHash: 437aae38a790
    paths./api/v1/tools/image/image-to-base64.post.summary:
      sourceHash: 440737c09f0d
      provenance: machine
      outputHash: f6d5f2870387
    paths./api/v1/tools/image/image-to-base64.post.description:
      sourceHash: 02a80d960185
      provenance: machine
      outputHash: c6ddfb0e2210
    paths./api/v1/tools/image/watermark-text.post.summary:
      sourceHash: dfef3d1052de
      provenance: machine
      outputHash: faf5048a8f31
    paths./api/v1/tools/image/watermark-text.post.description:
      sourceHash: 158e444c0a77
      provenance: machine
      outputHash: 5435eada5f31
    paths./api/v1/tools/image/text-overlay.post.summary:
      sourceHash: 7311c0ffc340
      provenance: machine
      outputHash: c755eb0e66da
    paths./api/v1/tools/image/text-overlay.post.description:
      sourceHash: e5dd74752a49
      provenance: machine
      outputHash: ed2301c1f694
    paths./api/v1/tools/image/replace-color.post.summary:
      sourceHash: 80e60e5fe0ed
      provenance: machine
      outputHash: 9e7c8f269e6e
    paths./api/v1/tools/image/replace-color.post.description:
      sourceHash: cbafc4d168c3
      provenance: machine
      outputHash: 53404aff203d
    paths./api/v1/tools/image/color-blindness.post.summary:
      sourceHash: 1ab73f30893d
      provenance: machine
      outputHash: 161bcf02058d
    paths./api/v1/tools/image/color-blindness.post.description:
      sourceHash: 1e362d094237
      provenance: machine
      outputHash: 8e62042871e7
    paths./api/v1/tools/image/gif-tools.post.summary:
      sourceHash: 985d94d31548
      provenance: machine
      outputHash: 5de599c50adf
    paths./api/v1/tools/image/gif-tools.post.description:
      sourceHash: fe7113c6fefd
      provenance: machine
      outputHash: c69feb20d83f
    paths./api/v1/tools/image/gif-tools/info.post.summary:
      sourceHash: e864c10b29f7
      provenance: machine
      outputHash: e3b9f72693b8
    paths./api/v1/tools/image/gif-tools/info.post.description:
      sourceHash: aaeba6b11dbf
      provenance: machine
      outputHash: bb8db8586a07
    paths./api/v1/tools/image/smart-crop.post.summary:
      sourceHash: 890848540d35
      provenance: machine
      outputHash: f6048f285147
    paths./api/v1/tools/image/smart-crop.post.description:
      sourceHash: 0f00f334ba29
      provenance: machine
      outputHash: 56ddb3d3c783
    paths./api/v1/tools/image/vectorize.post.summary:
      sourceHash: ffa9db773ada
      provenance: machine
      outputHash: 9fc7fe13f347
    paths./api/v1/tools/image/vectorize.post.description:
      sourceHash: 3d5de38dc3f3
      provenance: machine
      outputHash: 685c683e42b6
    paths./api/v1/tools/image/svg-to-raster.post.summary:
      sourceHash: bccef23ad06b
      provenance: machine
      outputHash: c1a78eed3842
    paths./api/v1/tools/image/svg-to-raster.post.description:
      sourceHash: 27a0b0dfe170
      provenance: machine
      outputHash: a55878c92e56
    paths./api/v1/tools/image/svg-to-raster/batch.post.summary:
      sourceHash: 340743c4e68b
      provenance: machine
      outputHash: a42a70de7299
    paths./api/v1/tools/image/svg-to-raster/batch.post.description:
      sourceHash: a0069d315b7e
      provenance: machine
      outputHash: 079cbd81a2a7
    paths./api/v1/tools/image/image-to-pdf.post.summary:
      sourceHash: 966eba6e1412
      provenance: machine
      outputHash: 296e6b7c26ab
    paths./api/v1/tools/image/image-to-pdf.post.description:
      sourceHash: f5a0819f56f7
      provenance: machine
      outputHash: ba458e6a338f
    paths./api/v1/tools/pdf/pdf-to-image.post.summary:
      sourceHash: 4c3c2ae28b1f
      provenance: machine
      outputHash: b9c5f1b3b39e
    paths./api/v1/tools/pdf/pdf-to-image.post.description:
      sourceHash: 4f0f5c643801
      provenance: machine
      outputHash: 6857793d9996
    paths./api/v1/tools/pdf/pdf-to-image/info.post.summary:
      sourceHash: d9398268b597
      provenance: machine
      outputHash: 265d444f60dc
    paths./api/v1/tools/pdf/pdf-to-image/info.post.description:
      sourceHash: 9ace4aaa3978
      provenance: machine
      outputHash: bdd20cf25492
    paths./api/v1/tools/pdf/pdf-to-image/preview.post.summary:
      sourceHash: 3bd369a401ba
      provenance: machine
      outputHash: 54c09fd7d5ec
    paths./api/v1/tools/pdf/pdf-to-image/preview.post.description:
      sourceHash: 90db92475b9a
      provenance: machine
      outputHash: 376ec5771218
    paths./api/v1/tools/image/split.post.summary:
      sourceHash: 21b17b422f71
      provenance: machine
      outputHash: 118c459ea0d7
    paths./api/v1/tools/image/split.post.description:
      sourceHash: d7d4988765b3
      provenance: machine
      outputHash: 7fb333c009b1
    paths./api/v1/tools/image/bulk-rename.post.summary:
      sourceHash: 5489fe2b69b0
      provenance: machine
      outputHash: 9763d648f5e5
    paths./api/v1/tools/image/bulk-rename.post.description:
      sourceHash: 093bf3688b13
      provenance: machine
      outputHash: 89b2bab7828d
    paths./api/v1/tools/image/favicon.post.summary:
      sourceHash: 7a587af97797
      provenance: machine
      outputHash: 2847bd1f1106
    paths./api/v1/tools/image/favicon.post.description:
      sourceHash: 5351a1c80471
      provenance: machine
      outputHash: cc11aaa70ab6
    paths./api/v1/tools/image/watermark-image.post.summary:
      sourceHash: 577e5e431f77
      provenance: machine
      outputHash: 34b1c0f9f839
    paths./api/v1/tools/image/watermark-image.post.description:
      sourceHash: 4e103b8a7d17
      provenance: machine
      outputHash: a62f6c73fc3f
    paths./api/v1/tools/image/compose.post.summary:
      sourceHash: 06718c2a3b98
      provenance: machine
      outputHash: 0872e3f9d8ea
    paths./api/v1/tools/image/compose.post.description:
      sourceHash: d48d40e77edf
      provenance: machine
      outputHash: 014edb5c7d56
    paths./api/v1/tools/image/meme-generator.post.summary:
      sourceHash: a4e89614a5c1
      provenance: machine
      outputHash: fe2b20c6cf2a
    paths./api/v1/tools/image/meme-generator.post.description:
      sourceHash: 7aa04984be62
      provenance: machine
      outputHash: 12be09016e40
    paths./api/v1/meme-templates.get.summary:
      sourceHash: fd3392277306
      provenance: machine
      outputHash: d22b27b567d4
    paths./api/v1/meme-templates.get.description:
      sourceHash: a147e348597c
      provenance: machine
      outputHash: 62046466852f
    paths./api/v1/meme-templates/full/{filename}.get.summary:
      sourceHash: fd501704c913
      provenance: machine
      outputHash: abdf507acdad
    paths./api/v1/meme-templates/full/{filename}.get.description:
      sourceHash: 3bb962f4402c
      provenance: machine
      outputHash: f2bb87a4d244
    paths./api/v1/meme-templates/thumbs/{filename}.get.summary:
      sourceHash: d2102190f5a2
      provenance: machine
      outputHash: cf66872fd365
    paths./api/v1/meme-templates/thumbs/{filename}.get.description:
      sourceHash: 77c69805732d
      provenance: machine
      outputHash: 912024b0cd1d
    paths./api/v1/meme-templates/fonts/{filename}.get.summary:
      sourceHash: 3627280f2609
      provenance: machine
      outputHash: 11b99d1e9d3f
    paths./api/v1/meme-templates/fonts/{filename}.get.description:
      sourceHash: 717c6764f472
      provenance: machine
      outputHash: 37565d43215a
    paths./api/v1/tools/image/compare.post.summary:
      sourceHash: 46e3612ffee1
      provenance: machine
      outputHash: d6714d313b07
    paths./api/v1/tools/image/compare.post.description:
      sourceHash: ac21fee13ecc
      provenance: machine
      outputHash: 4c9f0edca2cc
    paths./api/v1/tools/image/erase-object.post.summary:
      sourceHash: 21f13ba2930d
      provenance: machine
      outputHash: 7def85137264
    paths./api/v1/tools/image/erase-object.post.description:
      sourceHash: 9d6614ed28c6
      provenance: machine
      outputHash: f07a7851a38a
    paths./api/v1/tools/image/ai-canvas-expand.post.summary:
      sourceHash: 990aeca723ff
      provenance: machine
      outputHash: f4ab03de2030
    paths./api/v1/tools/image/ai-canvas-expand.post.description:
      sourceHash: 084de536af45
      provenance: machine
      outputHash: 02a3ad46d84a
    paths./api/v1/tools/image/collage.post.summary:
      sourceHash: 792f730c3eb6
      provenance: machine
      outputHash: bd76cda1f02d
    paths./api/v1/tools/image/collage.post.description:
      sourceHash: fb0ba3d2cf44
      provenance: machine
      outputHash: 59f5b9c618be
    paths./api/v1/tools/image/stitch.post.summary:
      sourceHash: dd874f58b1aa
      provenance: machine
      outputHash: 596ae97aca7c
    paths./api/v1/tools/image/stitch.post.description:
      sourceHash: 914837a5d232
      provenance: machine
      outputHash: 947f180fe871
    paths./api/v1/tools/image/remove-gif-background.post.summary:
      sourceHash: a316267cf5d1
      provenance: machine
      outputHash: eff1fc9d4fa5
    paths./api/v1/tools/image/remove-gif-background.post.description:
      sourceHash: ef1d100f2345
      provenance: machine
      outputHash: 09ca7b6f7be9
    paths./api/v1/tools/image/remove-background.post.summary:
      sourceHash: a8d1ab54b32c
      provenance: machine
      outputHash: f78755f74562
    paths./api/v1/tools/image/remove-background.post.description:
      sourceHash: 3bb193c0a2f9
      provenance: machine
      outputHash: ebff9fd1a582
    paths./api/v1/tools/image/remove-background/effects.post.summary:
      sourceHash: 2194d6947f3f
      provenance: machine
      outputHash: 4404ff59a913
    paths./api/v1/tools/image/remove-background/effects.post.description:
      sourceHash: 1e2ab8e3b1ae
      provenance: machine
      outputHash: eec057161b9f
    paths./api/v1/tools/image/transparency-fixer.post.summary:
      sourceHash: 0cf307cacb91
      provenance: machine
      outputHash: 209bd79efad4
    paths./api/v1/tools/image/transparency-fixer.post.description:
      sourceHash: 67b2a3c1cb7b
      provenance: machine
      outputHash: 9f3b11a76fcb
    paths./api/v1/tools/image/upscale.post.summary:
      sourceHash: ce44856d9e64
      provenance: machine
      outputHash: acb755451d74
    paths./api/v1/tools/image/upscale.post.description:
      sourceHash: e603db60e711
      provenance: machine
      outputHash: 2a75c8c4cce0
    paths./api/v1/tools/image/blur-faces.post.summary:
      sourceHash: 635ee975dace
      provenance: machine
      outputHash: babbec115673
    paths./api/v1/tools/image/blur-faces.post.description:
      sourceHash: 28790ae08ac7
      provenance: machine
      outputHash: d677c58cd0cf
    paths./api/v1/tools/image/ocr.post.summary:
      sourceHash: 0b508f19963b
      provenance: machine
      outputHash: addfba4b2625
    paths./api/v1/tools/image/ocr.post.description:
      sourceHash: 7b74b30329a5
      provenance: machine
      outputHash: 5c2fb3ccd4a0
    paths./api/v1/tools/image/info.post.summary:
      sourceHash: 1823e118aed5
      provenance: machine
      outputHash: 3cdea08912f1
    paths./api/v1/tools/image/info.post.description:
      sourceHash: 196d7190f16c
      provenance: machine
      outputHash: 0a9f57c00d81
    paths./api/v1/tools/image/color-palette.post.summary:
      sourceHash: 81a9be1a8044
      provenance: machine
      outputHash: d1316484ee2f
    paths./api/v1/tools/image/color-palette.post.description:
      sourceHash: d34d5ff2ade7
      provenance: machine
      outputHash: a742dc4404b8
    paths./api/v1/tools/image/barcode-read.post.summary:
      sourceHash: b51dcac65420
      provenance: machine
      outputHash: 2ddd105080e8
    paths./api/v1/tools/image/barcode-read.post.description:
      sourceHash: ae952218a233
      provenance: machine
      outputHash: 2a1942f344cc
    paths./api/v1/tools/image/find-duplicates.post.summary:
      sourceHash: bd6e5d3e4d26
      provenance: machine
      outputHash: 2f963e8c53a8
    paths./api/v1/tools/image/find-duplicates.post.description:
      sourceHash: 61322792b4bd
      provenance: machine
      outputHash: 07732aa9c7ce
    paths./api/v1/tools/image/qr-generate.post.summary:
      sourceHash: 799653bb4ee1
      provenance: machine
      outputHash: b7d79ea31dbc
    paths./api/v1/tools/image/qr-generate.post.description:
      sourceHash: ad4154c7b555
      provenance: machine
      outputHash: 88264ced6358
    paths./api/v1/tools/image/html-to-image.post.summary:
      sourceHash: d4dcd8d52cfd
      provenance: machine
      outputHash: 9fb5d33c26df
    paths./api/v1/tools/image/html-to-image.post.description:
      sourceHash: adb3e27eb0e6
      provenance: machine
      outputHash: 10d43bae1b48
    paths./api/v1/tools/image/strip-metadata/inspect.post.summary:
      sourceHash: 26c48e56ad62
      provenance: machine
      outputHash: 8295877cc718
    paths./api/v1/tools/image/strip-metadata/inspect.post.description:
      sourceHash: a6a7d5c72088
      provenance: machine
      outputHash: 4a9447142c11
    paths./api/v1/tools/image/edit-metadata.post.summary:
      sourceHash: a4ce10f7b615
      provenance: machine
      outputHash: e9bc05c05094
    paths./api/v1/tools/image/edit-metadata.post.description:
      sourceHash: 70d277ec5566
      provenance: machine
      outputHash: 59384e2056b5
    paths./api/v1/tools/image/edit-metadata/inspect.post.summary:
      sourceHash: a9b31ad7470f
      provenance: machine
      outputHash: e636430c3310
    paths./api/v1/tools/image/edit-metadata/inspect.post.description:
      sourceHash: 71173fd89d89
      provenance: machine
      outputHash: eb121605af02
    paths./api/v1/tools/{section}/{toolId}/batch.post.summary:
      sourceHash: c48b2b0a9212
      provenance: machine
      outputHash: 0007c78e5392
    paths./api/v1/tools/{section}/{toolId}/batch.post.description:
      sourceHash: e2ddfe55094d
      provenance: machine
      outputHash: 7f15d0dc331e
    paths./api/v1/pipeline/execute.post.summary:
      sourceHash: 6357b47fa7a9
      provenance: machine
      outputHash: 11a2f9172303
    paths./api/v1/pipeline/execute.post.description:
      sourceHash: ca3c9eb4acf1
      provenance: machine
      outputHash: f83db438b468
    paths./api/v1/pipeline/save.post.summary:
      sourceHash: 8209ec3e0c9b
      provenance: machine
      outputHash: b88e4a309143
    paths./api/v1/pipeline/save.post.description:
      sourceHash: a904ab4f3701
      provenance: machine
      outputHash: d60829e24877
    paths./api/v1/pipeline/list.get.summary:
      sourceHash: c8d290eff4a7
      provenance: machine
      outputHash: 79d8ae067919
    paths./api/v1/pipeline/list.get.description:
      sourceHash: d11170073568
      provenance: machine
      outputHash: 2f566290dc2d
    paths./api/v1/pipeline/{id}.delete.summary:
      sourceHash: 0a798831c1be
      provenance: machine
      outputHash: e6b51bd86173
    paths./api/v1/pipeline/{id}.delete.description:
      sourceHash: e3b43bf6d73d
      provenance: machine
      outputHash: ee03d2a53bd3
    paths./api/v1/pipeline/tools.get.summary:
      sourceHash: 79f28e6075f7
      provenance: machine
      outputHash: a1ec3047c241
    paths./api/v1/pipeline/tools.get.description:
      sourceHash: 6f341cc670d3
      provenance: machine
      outputHash: 25517e7b7ad5
    paths./api/v1/pipeline/batch.post.summary:
      sourceHash: 14abb819b562
      provenance: machine
      outputHash: 5f32841503f2
    paths./api/v1/pipeline/batch.post.description:
      sourceHash: 0c7b90acb5d8
      provenance: machine
      outputHash: 3fc9df898380
    paths./api/v1/upload.post.summary:
      sourceHash: 25d0eba68bdc
      provenance: machine
      outputHash: 2b4ca8d8ec5f
    paths./api/v1/upload.post.description:
      sourceHash: a9e18800407e
      provenance: machine
      outputHash: 498927c50959
    paths./api/v1/download/{jobId}/{filename}.get.summary:
      sourceHash: 6246d8c07e4b
      provenance: machine
      outputHash: da10a64a5aae
    paths./api/v1/download/{jobId}/{filename}.get.description:
      sourceHash: a09d603b069c
      provenance: machine
      outputHash: 999986db9b79
    paths./api/v1/preview.post.summary:
      sourceHash: 8266fbd3ff72
      provenance: machine
      outputHash: d43492997cc4
    paths./api/v1/preview.post.description:
      sourceHash: d5e04d49c594
      provenance: machine
      outputHash: 012dd4a68848
    paths./api/v1/files.get.summary:
      sourceHash: aefb51ff8aaa
      provenance: machine
      outputHash: e87ef7754adb
    paths./api/v1/files.get.description:
      sourceHash: 45f45ad625ed
      provenance: machine
      outputHash: c361f9ca9119
    paths./api/v1/files.delete.summary:
      sourceHash: 179bb72a6deb
      provenance: machine
      outputHash: b3c4c31b577d
    paths./api/v1/files.delete.description:
      sourceHash: 1bfaa5c376bb
      provenance: machine
      outputHash: 98c8a58c6a6d
    paths./api/v1/files/upload.post.summary:
      sourceHash: b94c597782ee
      provenance: machine
      outputHash: 1d5434ab240a
    paths./api/v1/files/upload.post.description:
      sourceHash: 8a070581c1e5
      provenance: machine
      outputHash: abb951ceee17
    paths./api/v1/files/save-result.post.summary:
      sourceHash: c35e320d9164
      provenance: machine
      outputHash: 838e89317568
    paths./api/v1/files/save-result.post.description:
      sourceHash: 3c434713031e
      provenance: machine
      outputHash: e2f08be7e010
    paths./api/v1/files/{id}.get.summary:
      sourceHash: a09c1fb7e33a
      provenance: machine
      outputHash: b6a34e63d5bd
    paths./api/v1/files/{id}.get.description:
      sourceHash: 8193066e23c5
      provenance: machine
      outputHash: 8ff3f5343c99
    paths./api/v1/files/{id}/download.get.summary:
      sourceHash: c0043bd46394
      provenance: machine
      outputHash: 7cf5cedfc3cc
    paths./api/v1/files/{id}/download.get.description:
      sourceHash: 96df2a599e22
      provenance: machine
      outputHash: dabb42ae932a
    paths./api/v1/files/{id}/thumbnail.get.summary:
      sourceHash: 07714ad0c5f8
      provenance: machine
      outputHash: d7af5c244182
    paths./api/v1/files/{id}/thumbnail.get.description:
      sourceHash: 9b04bbf75cba
      provenance: machine
      outputHash: cbd1c95ac700
    paths./api/v1/fetch-urls.post.summary:
      sourceHash: a00341e59161
      provenance: machine
      outputHash: 0745ee734c8e
    paths./api/v1/fetch-urls.post.description:
      sourceHash: ac474639261c
      provenance: machine
      outputHash: 1c611aaff520
    paths./api/v1/config/locale.get.summary:
      sourceHash: 78de2255222d
      provenance: machine
      outputHash: 657de281001d
    paths./api/v1/config/locale.get.description:
      sourceHash: b9967e400d09
      provenance: machine
      outputHash: 003be504d344
    paths./api/auth/login.post.summary:
      sourceHash: c189840cf7e2
      provenance: machine
      outputHash: 9ba14a4f9578
    paths./api/auth/login.post.description:
      sourceHash: 91ad9a828bf3
      provenance: machine
      outputHash: b89bd3f084db
    paths./api/auth/logout.post.summary:
      sourceHash: 49616145514e
      provenance: machine
      outputHash: 18db4ebaf4df
    paths./api/auth/logout.post.description:
      sourceHash: 0b9ae300808d
      provenance: machine
      outputHash: 0c01dcea5488
    paths./api/auth/session.get.summary:
      sourceHash: f4f7376ca1ea
      provenance: machine
      outputHash: 9e232412546e
    paths./api/auth/session.get.description:
      sourceHash: 2dad822c46ea
      provenance: machine
      outputHash: d0ac0c5acf25
    paths./api/auth/change-password.post.summary:
      sourceHash: 3f9c991f63a9
      provenance: machine
      outputHash: 75c8ccb342a1
    paths./api/auth/change-password.post.description:
      sourceHash: 5190db8a04e7
      provenance: machine
      outputHash: fcfc22f3c584
    paths./api/auth/users.get.summary:
      sourceHash: '9029730603e0'
      provenance: machine
      outputHash: e67d4f6c9f56
    paths./api/auth/users.get.description:
      sourceHash: 34d5a11733bd
      provenance: machine
      outputHash: b5a15da87ccd
    paths./api/auth/register.post.summary:
      sourceHash: f06da128c342
      provenance: machine
      outputHash: 62b96d8096f0
    paths./api/auth/register.post.description:
      sourceHash: 34d5a11733bd
      provenance: machine
      outputHash: b5a15da87ccd
    paths./api/auth/users/{id}.put.summary:
      sourceHash: 602b8b1607b0
      provenance: machine
      outputHash: a399c72dbe03
    paths./api/auth/users/{id}.put.description:
      sourceHash: 34d5a11733bd
      provenance: machine
      outputHash: b5a15da87ccd
    paths./api/auth/users/{id}.delete.summary:
      sourceHash: 58363fd94240
      provenance: machine
      outputHash: 560803f5aa97
    paths./api/auth/users/{id}.delete.description:
      sourceHash: 34d5a11733bd
      provenance: machine
      outputHash: b5a15da87ccd
    paths./api/auth/users/{id}/reset-password.post.summary:
      sourceHash: aae899c2d3ad
      provenance: machine
      outputHash: 401e65a13a7f
    paths./api/auth/users/{id}/reset-password.post.description:
      sourceHash: 34d5a11733bd
      provenance: machine
      outputHash: b5a15da87ccd
    paths./api/v1/api-keys.get.summary:
      sourceHash: ad0527749f03
      provenance: machine
      outputHash: f4defa731a3c
    paths./api/v1/api-keys.get.description:
      sourceHash: 470cee6220f0
      provenance: machine
      outputHash: cddd738579f6
    paths./api/v1/api-keys.post.summary:
      sourceHash: eb136082215a
      provenance: machine
      outputHash: f03d4828514a
    paths./api/v1/api-keys.post.description:
      sourceHash: 7568aec4ab9b
      provenance: machine
      outputHash: 378be85993a0
    paths./api/v1/api-keys/{id}.delete.summary:
      sourceHash: 9ebde15b7f02
      provenance: machine
      outputHash: e76205c854a4
    paths./api/v1/api-keys/{id}.delete.description:
      sourceHash: 46a57dbc68a5
      provenance: machine
      outputHash: 824b4aa3ca94
    paths./api/v1/settings.get.summary:
      sourceHash: 8530e17de153
      provenance: machine
      outputHash: 2d107a5eb4f3
    paths./api/v1/settings.get.description:
      sourceHash: 65d8ff0f5873
      provenance: machine
      outputHash: 307ae971697e
    paths./api/v1/settings.put.summary:
      sourceHash: 46ed69579c39
      provenance: machine
      outputHash: 98cfb65d08f6
    paths./api/v1/settings.put.description:
      sourceHash: 887f8facdd94
      provenance: machine
      outputHash: d5c6d2ccac83
    paths./api/v1/settings/{key}.get.summary:
      sourceHash: 186add054525
      provenance: machine
      outputHash: 48db6298cef0
    paths./api/v1/settings/{key}.get.description:
      sourceHash: 962db9ab06a3
      provenance: machine
      outputHash: 4fa6b2c77ee2
    paths./api/v1/teams.get.summary:
      sourceHash: 863546c57b78
      provenance: machine
      outputHash: 924723cd43e3
    paths./api/v1/teams.get.description:
      sourceHash: 88ce5170c6ed
      provenance: machine
      outputHash: a01897e4f21d
    paths./api/v1/teams.post.summary:
      sourceHash: 00766ad4b096
      provenance: machine
      outputHash: 1c3dcd0b133b
    paths./api/v1/teams.post.description:
      sourceHash: 88ce5170c6ed
      provenance: machine
      outputHash: a01897e4f21d
    paths./api/v1/teams/{id}.put.summary:
      sourceHash: c5dd33827a5b
      provenance: machine
      outputHash: 79f3fe518eec
    paths./api/v1/teams/{id}.put.description:
      sourceHash: 88ce5170c6ed
      provenance: machine
      outputHash: a01897e4f21d
    paths./api/v1/teams/{id}.delete.summary:
      sourceHash: b51488367ba6
      provenance: machine
      outputHash: ae4c56a9a758
    paths./api/v1/teams/{id}.delete.description:
      sourceHash: 88ce5170c6ed
      provenance: machine
      outputHash: a01897e4f21d
    paths./api/v1/config/auth.get.summary:
      sourceHash: 13189298880a
      provenance: machine
      outputHash: 3724c1b98ff9
    paths./api/v1/config/auth.get.description:
      sourceHash: 404909083a24
      provenance: machine
      outputHash: c2b81c6c48fa
    paths./api/v1/jobs/{jobId}/progress.get.summary:
      sourceHash: bcf19846aa43
      provenance: machine
      outputHash: 8b1ce0c02b31
    paths./api/v1/jobs/{jobId}/progress.get.description:
      sourceHash: 2df913c75d85
      provenance: human
      outputHash: dcb6e36358c6
    paths./api/v1/admin/health.get.summary:
      sourceHash: 92f8575c9c94
      provenance: machine
      outputHash: ff6bcd8c7307
    paths./api/v1/admin/health.get.description:
      sourceHash: 4abbba893ea4
      provenance: machine
      outputHash: 93fc9ccc48a0
    paths./api/v1/config/analytics.get.summary:
      sourceHash: b7fdcf112830
      provenance: machine
      outputHash: 8f9e60428429
    paths./api/v1/config/analytics.get.description:
      sourceHash: 2447c239373f
      provenance: machine
      outputHash: 25baa5abf459
    paths./api/v1/features.get.summary:
      sourceHash: e45ee3163f96
      provenance: machine
      outputHash: f368dd287de1
    paths./api/v1/features.get.description:
      sourceHash: aa0b433ca9a9
      provenance: machine
      outputHash: 3207d63d09ce
    paths./api/v1/admin/features/{bundleId}/install.post.summary:
      sourceHash: bebc6f7ea7c3
      provenance: machine
      outputHash: 2dc83d03e453
    paths./api/v1/admin/features/{bundleId}/install.post.description:
      sourceHash: 6051af19be7e
      provenance: machine
      outputHash: 6c431937ae7f
    paths./api/v1/admin/features/{bundleId}/uninstall.post.summary:
      sourceHash: 160eaf179d4f
      provenance: machine
      outputHash: 47ae5a019bb3
    paths./api/v1/admin/features/{bundleId}/uninstall.post.description:
      sourceHash: 35bdb459a1a7
      provenance: machine
      outputHash: f2e2cebc8865
    paths./api/v1/admin/features/disk-usage.get.summary:
      sourceHash: 54dc8991b9a7
      provenance: machine
      outputHash: d46ae289ae5a
    paths./api/v1/admin/features/disk-usage.get.description:
      sourceHash: a78fac026ea3
      provenance: machine
      outputHash: 18cce6d7713d
    paths./api/v1/audit-log.get.summary:
      sourceHash: 05536545727c
      provenance: machine
      outputHash: d665a6fa8071
    paths./api/v1/audit-log.get.description:
      sourceHash: b7950dcf7e49
      provenance: machine
      outputHash: 1649178c66e1
    paths./api/v1/roles.get.summary:
      sourceHash: 833a75e86b93
      provenance: machine
      outputHash: 0dcf555b180a
    paths./api/v1/roles.get.description:
      sourceHash: 9ccdec8630d3
      provenance: machine
      outputHash: 7538e63b71af
    paths./api/v1/roles.post.summary:
      sourceHash: 6dd06b2529d4
      provenance: machine
      outputHash: 19e49c3dad64
    paths./api/v1/roles.post.description:
      sourceHash: d740216038e7
      provenance: machine
      outputHash: 3a506079cd32
    paths./api/v1/roles/{id}.put.summary:
      sourceHash: 6c0cda1bc967
      provenance: machine
      outputHash: 0d228a56532b
    paths./api/v1/roles/{id}.put.description:
      sourceHash: e06c42a9c3ed
      provenance: machine
      outputHash: f48dda7570b3
    paths./api/v1/roles/{id}.delete.summary:
      sourceHash: d5a3ab01055a
      provenance: machine
      outputHash: 7a00dca6075f
    paths./api/v1/roles/{id}.delete.description:
      sourceHash: b4c1c8c0c9e0
      provenance: machine
      outputHash: a5bc43dcc141
    paths./api/v1/tools/video/convert-video.post.summary:
      sourceHash: b4defe1d6906
      provenance: machine
      outputHash: 5276043d41a0
    paths./api/v1/tools/video/convert-video.post.description:
      sourceHash: 00ab19fbffe4
      provenance: machine
      outputHash: 25ee49a7930f
    paths./api/v1/tools/video/compress-video.post.summary:
      sourceHash: 8af3d4b27a4b
      provenance: machine
      outputHash: 73f520b8b207
    paths./api/v1/tools/video/compress-video.post.description:
      sourceHash: 7f517e885483
      provenance: machine
      outputHash: 6c198c932692
    paths./api/v1/tools/video/trim-video.post.summary:
      sourceHash: af6ed946e090
      provenance: machine
      outputHash: cb86dce79e98
    paths./api/v1/tools/video/trim-video.post.description:
      sourceHash: c01dd24a19dd
      provenance: machine
      outputHash: 1be462ffae25
    paths./api/v1/tools/video/mute-video.post.summary:
      sourceHash: 48ed61cb4802
      provenance: machine
      outputHash: fbe7a504a2e1
    paths./api/v1/tools/video/mute-video.post.description:
      sourceHash: 76898fc22f53
      provenance: machine
      outputHash: 182fdd1501a2
    paths./api/v1/tools/video/video-to-gif.post.summary:
      sourceHash: 4abd5ab74c35
      provenance: machine
      outputHash: ac02b7a38283
    paths./api/v1/tools/video/video-to-gif.post.description:
      sourceHash: d2d195459149
      provenance: machine
      outputHash: 59b3b7a9e7f4
    paths./api/v1/tools/video/resize-video.post.summary:
      sourceHash: 99fde7cc129d
      provenance: machine
      outputHash: 9e50028d1a83
    paths./api/v1/tools/video/resize-video.post.description:
      sourceHash: ffe090ccf55b
      provenance: machine
      outputHash: afd9ebeb58fc
    paths./api/v1/tools/video/crop-video.post.summary:
      sourceHash: f042b21b17c0
      provenance: machine
      outputHash: 53ffc7e4f665
    paths./api/v1/tools/video/crop-video.post.description:
      sourceHash: a70528f0d600
      provenance: machine
      outputHash: e8044ba4857f
    paths./api/v1/tools/video/rotate-video.post.summary:
      sourceHash: a1d5d2488fd5
      provenance: machine
      outputHash: 5820f9fbd397
    paths./api/v1/tools/video/rotate-video.post.description:
      sourceHash: 0858085b737b
      provenance: machine
      outputHash: 6d98adad1ffd
    paths./api/v1/tools/video/change-fps.post.summary:
      sourceHash: bc48dbea9413
      provenance: machine
      outputHash: 638e28ee11c1
    paths./api/v1/tools/video/change-fps.post.description:
      sourceHash: 20c9ae3f7c23
      provenance: machine
      outputHash: 4c0292ecfae3
    paths./api/v1/tools/video/video-color.post.summary:
      sourceHash: 6005cc99c426
      provenance: machine
      outputHash: 0e4d81a5b033
    paths./api/v1/tools/video/video-color.post.description:
      sourceHash: f8ce7dae4f04
      provenance: machine
      outputHash: 1b2f059c2f77
    paths./api/v1/tools/video/video-speed.post.summary:
      sourceHash: 4439901f7d61
      provenance: machine
      outputHash: 69b7e5cbdda4
    paths./api/v1/tools/video/video-speed.post.description:
      sourceHash: 7a4b64e60829
      provenance: machine
      outputHash: c2948c350198
    paths./api/v1/tools/video/reverse-video.post.summary:
      sourceHash: 4f0d7b5ea9ae
      provenance: machine
      outputHash: 02d6e9727b35
    paths./api/v1/tools/video/reverse-video.post.description:
      sourceHash: 77abcb48d823
      provenance: machine
      outputHash: 65146799daca
    paths./api/v1/tools/video/video-loudnorm.post.summary:
      sourceHash: f5298327c322
      provenance: machine
      outputHash: 4c6522f4b650
    paths./api/v1/tools/video/video-loudnorm.post.description:
      sourceHash: 019f6147f722
      provenance: machine
      outputHash: 21f15dd617ba
    paths./api/v1/tools/video/aspect-pad.post.summary:
      sourceHash: aa7c02eeacf1
      provenance: machine
      outputHash: e99ace8f10e7
    paths./api/v1/tools/video/aspect-pad.post.description:
      sourceHash: 288b33cc62de
      provenance: machine
      outputHash: f7d88481415e
    paths./api/v1/tools/video/blur-pad.post.summary:
      sourceHash: 84af9c217d52
      provenance: machine
      outputHash: fa9054b1335e
    paths./api/v1/tools/video/blur-pad.post.description:
      sourceHash: d49a3fab5e02
      provenance: machine
      outputHash: 9acb716c9ad0
    paths./api/v1/tools/video/watermark-video.post.summary:
      sourceHash: bbbe197c46c5
      provenance: machine
      outputHash: 8f4d4a7b75ec
    paths./api/v1/tools/video/watermark-video.post.description:
      sourceHash: d914078985d7
      provenance: machine
      outputHash: 87b223cc8d18
    paths./api/v1/tools/video/stabilize-video.post.summary:
      sourceHash: f76615a74aac
      provenance: machine
      outputHash: c0fc7093ce17
    paths./api/v1/tools/video/stabilize-video.post.description:
      sourceHash: 42e5e8a954ae
      provenance: machine
      outputHash: 47b6252fc73a
    paths./api/v1/tools/video/gif-to-video.post.summary:
      sourceHash: 766d5d54badb
      provenance: machine
      outputHash: 0509498f67d5
    paths./api/v1/tools/video/gif-to-video.post.description:
      sourceHash: 22a13cbfe672
      provenance: machine
      outputHash: e88d6dcc0ff5
    paths./api/v1/tools/video/video-to-webp.post.summary:
      sourceHash: 0c4028ecac61
      provenance: machine
      outputHash: 04a4d5760cf0
    paths./api/v1/tools/video/video-to-webp.post.description:
      sourceHash: 48fa0306cbe8
      provenance: machine
      outputHash: a07ea5b97e39
    paths./api/v1/tools/video/video-to-frames.post.summary:
      sourceHash: 8941a5926866
      provenance: machine
      outputHash: 2e3837a9e7eb
    paths./api/v1/tools/video/video-to-frames.post.description:
      sourceHash: 907e19fcf6a2
      provenance: machine
      outputHash: 7b6d0baf7181
    paths./api/v1/tools/video/merge-videos.post.summary:
      sourceHash: afbd2e62e1dd
      provenance: machine
      outputHash: 39ecbc1f51b2
    paths./api/v1/tools/video/merge-videos.post.description:
      sourceHash: 30e77c05314f
      provenance: machine
      outputHash: c0e1d51c2a1f
    paths./api/v1/tools/video/replace-audio.post.summary:
      sourceHash: 0cd096774105
      provenance: machine
      outputHash: fbd76f5e55a3
    paths./api/v1/tools/video/replace-audio.post.description:
      sourceHash: f9aef92c2ad2
      provenance: machine
      outputHash: d47412bca033
    paths./api/v1/tools/video/burn-subtitles.post.summary:
      sourceHash: 53e23068613b
      provenance: machine
      outputHash: 35a8b851f8de
    paths./api/v1/tools/video/burn-subtitles.post.description:
      sourceHash: a501f3253970
      provenance: machine
      outputHash: 6218db251ce2
    paths./api/v1/tools/video/embed-subtitles.post.summary:
      sourceHash: 55a0c6104e9e
      provenance: machine
      outputHash: 318a8fbac3ea
    paths./api/v1/tools/video/embed-subtitles.post.description:
      sourceHash: 9ed219a50b61
      provenance: machine
      outputHash: d532bd37a19a
    paths./api/v1/tools/video/extract-subtitles.post.summary:
      sourceHash: 0ae3dd9f7012
      provenance: machine
      outputHash: f96ade7bdab5
    paths./api/v1/tools/video/extract-subtitles.post.description:
      sourceHash: 4daf70632777
      provenance: machine
      outputHash: f46fda297e10
    paths./api/v1/tools/video/images-to-video.post.summary:
      sourceHash: feb4b6d19902
      provenance: machine
      outputHash: d74ac8e60cc6
    paths./api/v1/tools/video/images-to-video.post.description:
      sourceHash: c7ec30bbfec8
      provenance: machine
      outputHash: 27f1cf435aae
    paths./api/v1/tools/video/video-metadata.post.summary:
      sourceHash: f2902a6112ff
      provenance: machine
      outputHash: 9814a0aa0612
    paths./api/v1/tools/video/video-metadata.post.description:
      sourceHash: 25b0eb0d1f64
      provenance: machine
      outputHash: 361f08d77b23
    paths./api/v1/tools/video/auto-subtitles.post.summary:
      sourceHash: 61b5c42d8cd0
      provenance: machine
      outputHash: 95b3230d48eb
    paths./api/v1/tools/video/auto-subtitles.post.description:
      sourceHash: fe628b19b3dc
      provenance: machine
      outputHash: 1baf9bff1b43
    paths./api/v1/tools/video/extract-audio.post.summary:
      sourceHash: ad7f2c51f215
      provenance: machine
      outputHash: 8c6e8ef8fb19
    paths./api/v1/tools/video/extract-audio.post.description:
      sourceHash: 97425b1da99f
      provenance: machine
      outputHash: 27ae85447042
    paths./api/v1/tools/audio/convert-audio.post.summary:
      sourceHash: 295e4a5939e1
      provenance: machine
      outputHash: 1ebe4bc39177
    paths./api/v1/tools/audio/convert-audio.post.description:
      sourceHash: db7c32326757
      provenance: machine
      outputHash: 198ac78deff2
    paths./api/v1/tools/audio/trim-audio.post.summary:
      sourceHash: 6fdc83d79136
      provenance: machine
      outputHash: 683af6da54aa
    paths./api/v1/tools/audio/trim-audio.post.description:
      sourceHash: 1150da04e398
      provenance: machine
      outputHash: 649cdc74af43
    paths./api/v1/tools/audio/volume-adjust.post.summary:
      sourceHash: 399e64735825
      provenance: machine
      outputHash: 73d8a482db3d
    paths./api/v1/tools/audio/volume-adjust.post.description:
      sourceHash: 18308a469600
      provenance: machine
      outputHash: d95093df41d8
    paths./api/v1/tools/audio/normalize-audio.post.summary:
      sourceHash: f5298327c322
      provenance: machine
      outputHash: 4c6522f4b650
    paths./api/v1/tools/audio/normalize-audio.post.description:
      sourceHash: d9993db45ccb
      provenance: machine
      outputHash: 327b320a2290
    paths./api/v1/tools/audio/fade-audio.post.summary:
      sourceHash: fc7e035a63d5
      provenance: machine
      outputHash: 2441692abe39
    paths./api/v1/tools/audio/fade-audio.post.description:
      sourceHash: a85ba1a120e5
      provenance: machine
      outputHash: b584d0013674
    paths./api/v1/tools/audio/reverse-audio.post.summary:
      sourceHash: b2eddae3a717
      provenance: machine
      outputHash: e1414a0f4e88
    paths./api/v1/tools/audio/reverse-audio.post.description:
      sourceHash: 218f9b33f5eb
      provenance: machine
      outputHash: e72639652212
    paths./api/v1/tools/audio/audio-speed.post.summary:
      sourceHash: aee2a47692bd
      provenance: machine
      outputHash: a3f735eb6bd1
    paths./api/v1/tools/audio/audio-speed.post.description:
      sourceHash: 51fe7745adbd
      provenance: machine
      outputHash: 05d482ac5e74
    paths./api/v1/tools/audio/pitch-shift.post.summary:
      sourceHash: b26d4482b076
      provenance: machine
      outputHash: b1119c4d3cc3
    paths./api/v1/tools/audio/pitch-shift.post.description:
      sourceHash: 04cf48eca03e
      provenance: machine
      outputHash: da7b58f9665e
    paths./api/v1/tools/audio/audio-channels.post.summary:
      sourceHash: 409c74651939
      provenance: machine
      outputHash: 876578c867ca
    paths./api/v1/tools/audio/audio-channels.post.description:
      sourceHash: 0974271a6730
      provenance: machine
      outputHash: 13f8cb3d8e95
    paths./api/v1/tools/audio/silence-removal.post.summary:
      sourceHash: dc27812cd572
      provenance: machine
      outputHash: f0ee743ccbc3
    paths./api/v1/tools/audio/silence-removal.post.description:
      sourceHash: 29d4e890a62d
      provenance: machine
      outputHash: d525eda5f498
    paths./api/v1/tools/audio/noise-reduction.post.summary:
      sourceHash: 0bcf30a006cb
      provenance: machine
      outputHash: 9ecea4b5da09
    paths./api/v1/tools/audio/noise-reduction.post.description:
      sourceHash: 032c91b97641
      provenance: machine
      outputHash: 680da66ede3b
    paths./api/v1/tools/audio/merge-audio.post.summary:
      sourceHash: 780965910dff
      provenance: machine
      outputHash: 756df9614cf2
    paths./api/v1/tools/audio/merge-audio.post.description:
      sourceHash: 1397edf49056
      provenance: machine
      outputHash: a099b9b74d4c
    paths./api/v1/tools/audio/split-audio.post.summary:
      sourceHash: e59c513c0ba2
      provenance: machine
      outputHash: fd483e636782
    paths./api/v1/tools/audio/split-audio.post.description:
      sourceHash: 763e0458c032
      provenance: machine
      outputHash: c2723638be21
    paths./api/v1/tools/audio/ringtone-maker.post.summary:
      sourceHash: f0e142322ff9
      provenance: machine
      outputHash: 9d780c421cc1
    paths./api/v1/tools/audio/ringtone-maker.post.description:
      sourceHash: c301dcbbedc1
      provenance: machine
      outputHash: 214b2637bea1
    paths./api/v1/tools/audio/waveform-image.post.summary:
      sourceHash: 0e373ca45197
      provenance: machine
      outputHash: 4d79bdff49a9
    paths./api/v1/tools/audio/waveform-image.post.description:
      sourceHash: 70f63b279b5d
      provenance: machine
      outputHash: efd2317c3bdf
    paths./api/v1/tools/audio/audio-metadata.post.summary:
      sourceHash: 55d1a5a0e65d
      provenance: machine
      outputHash: b32038ff9480
    paths./api/v1/tools/audio/audio-metadata.post.description:
      sourceHash: 5c799135421c
      provenance: machine
      outputHash: 78b8820888a5
    paths./api/v1/tools/audio/transcribe-audio.post.summary:
      sourceHash: a723c84c3ab0
      provenance: machine
      outputHash: c9e2a76c3712
    paths./api/v1/tools/audio/transcribe-audio.post.description:
      sourceHash: 2967ad141d14
      provenance: machine
      outputHash: 28ef72f1f14c
    paths./api/v1/tools/pdf/merge-pdf.post.summary:
      sourceHash: 43d1539b7596
      provenance: machine
      outputHash: 8b7929c125b9
    paths./api/v1/tools/pdf/merge-pdf.post.description:
      sourceHash: 728388cddb8a
      provenance: machine
      outputHash: 05fb7209b004
    paths./api/v1/tools/pdf/split-pdf.post.summary:
      sourceHash: 4f1b846cb144
      provenance: machine
      outputHash: ae6d8d7a1b90
    paths./api/v1/tools/pdf/split-pdf.post.description:
      sourceHash: fbe9d9c35006
      provenance: machine
      outputHash: cac7313ffa62
    paths./api/v1/tools/pdf/compress-pdf.post.summary:
      sourceHash: 932c3c48a4de
      provenance: machine
      outputHash: 4a6283334f09
    paths./api/v1/tools/pdf/compress-pdf.post.description:
      sourceHash: 89f23fb6b3c8
      provenance: machine
      outputHash: 945f018b73df
    paths./api/v1/tools/pdf/rotate-pdf.post.summary:
      sourceHash: 1eccabbce838
      provenance: machine
      outputHash: 909afd014f13
    paths./api/v1/tools/pdf/rotate-pdf.post.description:
      sourceHash: 7c561cc7ed48
      provenance: machine
      outputHash: 1f1f09a9a13e
    paths./api/v1/tools/pdf/extract-pages.post.summary:
      sourceHash: 0247b9f3e6cf
      provenance: machine
      outputHash: a93194d3488e
    paths./api/v1/tools/pdf/extract-pages.post.description:
      sourceHash: c07eea0c017f
      provenance: machine
      outputHash: d0588e9ca66b
    paths./api/v1/tools/pdf/remove-pages.post.summary:
      sourceHash: 670a5909f883
      provenance: machine
      outputHash: a4351c2f619e
    paths./api/v1/tools/pdf/remove-pages.post.description:
      sourceHash: 6231b16e7bea
      provenance: machine
      outputHash: 5aa7f889ba92
    paths./api/v1/tools/pdf/organize-pdf.post.summary:
      sourceHash: d5a641ebd93a
      provenance: machine
      outputHash: 0cefc8651377
    paths./api/v1/tools/pdf/organize-pdf.post.description:
      sourceHash: ac32f48665e7
      provenance: machine
      outputHash: c979a70536fa
    paths./api/v1/tools/pdf/protect-pdf.post.summary:
      sourceHash: dae6c607e963
      provenance: machine
      outputHash: e1105e7acc46
    paths./api/v1/tools/pdf/protect-pdf.post.description:
      sourceHash: 600a3be47970
      provenance: machine
      outputHash: 49f97cae5ad1
    paths./api/v1/tools/pdf/unlock-pdf.post.summary:
      sourceHash: 893947d82ded
      provenance: machine
      outputHash: 6f49bc509f12
    paths./api/v1/tools/pdf/unlock-pdf.post.description:
      sourceHash: 1d199055f386
      provenance: machine
      outputHash: 411bf3154137
    paths./api/v1/tools/pdf/repair-pdf.post.summary:
      sourceHash: 43faa052d4cf
      provenance: machine
      outputHash: cdcaa66d63f9
    paths./api/v1/tools/pdf/repair-pdf.post.description:
      sourceHash: 77f71d364df4
      provenance: machine
      outputHash: 707a6875644a
    paths./api/v1/tools/pdf/linearize-pdf.post.summary:
      sourceHash: 75fa522e3fe3
      provenance: machine
      outputHash: 2f5083bdef93
    paths./api/v1/tools/pdf/linearize-pdf.post.description:
      sourceHash: 200f45aa4a91
      provenance: machine
      outputHash: 2471432713f6
    paths./api/v1/tools/pdf/grayscale-pdf.post.summary:
      sourceHash: 58b0007c3425
      provenance: machine
      outputHash: 8d35e9558673
    paths./api/v1/tools/pdf/grayscale-pdf.post.description:
      sourceHash: 7b7e2b4857e6
      provenance: machine
      outputHash: 8e3f366a802a
    paths./api/v1/tools/pdf/pdfa-convert.post.summary:
      sourceHash: 1a5ec4af8f0e
      provenance: machine
      outputHash: 9154639f2ea5
    paths./api/v1/tools/pdf/pdfa-convert.post.description:
      sourceHash: 3c8ad47aa904
      provenance: machine
      outputHash: 2752c9d14daf
    paths./api/v1/tools/pdf/crop-pdf.post.summary:
      sourceHash: dc4bdbce825f
      provenance: machine
      outputHash: 979b9cab87a1
    paths./api/v1/tools/pdf/crop-pdf.post.description:
      sourceHash: 94eb9575aecc
      provenance: machine
      outputHash: 0d4afefe5f77
    paths./api/v1/tools/pdf/nup-pdf.post.summary:
      sourceHash: 779949cba319
      provenance: machine
      outputHash: b5f08fba0094
    paths./api/v1/tools/pdf/nup-pdf.post.description:
      sourceHash: 5e2a264dafb0
      provenance: machine
      outputHash: af3c34789cff
    paths./api/v1/tools/pdf/booklet-pdf.post.summary:
      sourceHash: a1fdff0bc065
      provenance: machine
      outputHash: 44360fd814ec
    paths./api/v1/tools/pdf/booklet-pdf.post.description:
      sourceHash: 0ce5f88843c3
      provenance: machine
      outputHash: bbe1d26b5d9f
    paths./api/v1/tools/pdf/watermark-pdf.post.summary:
      sourceHash: 903ce98c712a
      provenance: machine
      outputHash: 20a794bf9a20
    paths./api/v1/tools/pdf/watermark-pdf.post.description:
      sourceHash: c15a57a06624
      provenance: machine
      outputHash: 48cbc3e6613f
    paths./api/v1/tools/pdf/pdf-page-numbers.post.summary:
      sourceHash: 44eb045097be
      provenance: machine
      outputHash: 17128d997c7c
    paths./api/v1/tools/pdf/pdf-page-numbers.post.description:
      sourceHash: 30ce839cac96
      provenance: machine
      outputHash: 637f6b59c003
    paths./api/v1/tools/files/convert-document.post.summary:
      sourceHash: 51556213858a
      provenance: machine
      outputHash: 6d158a27a2d2
    paths./api/v1/tools/files/convert-document.post.description:
      sourceHash: 8a61bf0f3459
      provenance: machine
      outputHash: 90936ee32e3f
    paths./api/v1/tools/files/convert-presentation.post.summary:
      sourceHash: 5e2293e30835
      provenance: machine
      outputHash: 3955cb4690e4
    paths./api/v1/tools/files/convert-presentation.post.description:
      sourceHash: c24f1d70d630
      provenance: machine
      outputHash: 56ee222cfc4e
    paths./api/v1/tools/files/convert-spreadsheet.post.summary:
      sourceHash: 670b7aa81c49
      provenance: machine
      outputHash: ae3658c207be
    paths./api/v1/tools/files/convert-spreadsheet.post.description:
      sourceHash: 1c254c7db8bc
      provenance: machine
      outputHash: 0c60dc204b96
    paths./api/v1/tools/files/excel-to-pdf.post.summary:
      sourceHash: edba3432df77
      provenance: machine
      outputHash: 0661f555624a
    paths./api/v1/tools/files/excel-to-pdf.post.description:
      sourceHash: c2e4aaa88cb7
      provenance: machine
      outputHash: 49ab13392531
    paths./api/v1/tools/files/word-to-pdf.post.summary:
      sourceHash: e6cb1150e762
      provenance: machine
      outputHash: 838144c0b611
    paths./api/v1/tools/files/word-to-pdf.post.description:
      sourceHash: eca3c027bc10
      provenance: machine
      outputHash: 3cec7eb3f3c8
    paths./api/v1/tools/files/powerpoint-to-pdf.post.summary:
      sourceHash: 799dd29fe97a
      provenance: machine
      outputHash: 0b10b45aba81
    paths./api/v1/tools/files/powerpoint-to-pdf.post.description:
      sourceHash: cdc18f172040
      provenance: machine
      outputHash: 45b449e998ae
    paths./api/v1/tools/files/html-to-pdf.post.summary:
      sourceHash: e03b0e6dda95
      provenance: machine
      outputHash: 7ffa4ec19151
    paths./api/v1/tools/files/html-to-pdf.post.description:
      sourceHash: e189b21c7bed
      provenance: machine
      outputHash: ef44e40d95ce
    paths./api/v1/tools/files/markdown-to-docx.post.summary:
      sourceHash: 1162393274fa
      provenance: machine
      outputHash: a5d1443f415b
    paths./api/v1/tools/files/markdown-to-docx.post.description:
      sourceHash: 2bc0fd718613
      provenance: machine
      outputHash: 06641079c5db
    paths./api/v1/tools/files/markdown-to-html.post.summary:
      sourceHash: e5e6e736b649
      provenance: machine
      outputHash: 6b381cacdb05
    paths./api/v1/tools/files/markdown-to-html.post.description:
      sourceHash: 4f6f706c371e
      provenance: machine
      outputHash: c115f38edf31
    paths./api/v1/tools/files/markdown-to-pdf.post.summary:
      sourceHash: 82712b1fc2b5
      provenance: machine
      outputHash: 98c342df5661
    paths./api/v1/tools/files/markdown-to-pdf.post.description:
      sourceHash: f588e3d2847d
      provenance: machine
      outputHash: aa11dd9b2443
    paths./api/v1/tools/files/epub-convert.post.summary:
      sourceHash: f03cffcd951f
      provenance: machine
      outputHash: d0b0129d7f1e
    paths./api/v1/tools/files/epub-convert.post.description:
      sourceHash: 5a42072194d3
      provenance: machine
      outputHash: a7b2141f3dab
    paths./api/v1/tools/files/to-epub.post.summary:
      sourceHash: 542d3fb468be
      provenance: machine
      outputHash: 44398e179d94
    paths./api/v1/tools/files/to-epub.post.description:
      sourceHash: 1e0b8c39ef9d
      provenance: machine
      outputHash: 4df874fa3691
    paths./api/v1/tools/pdf/flatten-pdf.post.summary:
      sourceHash: 86c838d2d497
      provenance: machine
      outputHash: bd37ef3fc9b8
    paths./api/v1/tools/pdf/flatten-pdf.post.description:
      sourceHash: cb0b0bdeaaa5
      provenance: machine
      outputHash: 93e7dad5d33d
    paths./api/v1/tools/pdf/redact-pdf.post.summary:
      sourceHash: 1437d196213c
      provenance: machine
      outputHash: d0cd1480607a
    paths./api/v1/tools/pdf/redact-pdf.post.description:
      sourceHash: c2652c3da04a
      provenance: machine
      outputHash: e67e5ac94c71
    paths./api/v1/tools/pdf/pdf-to-text.post.summary:
      sourceHash: f1a719f99a31
      provenance: machine
      outputHash: e68cc028fb58
    paths./api/v1/tools/pdf/pdf-to-text.post.description:
      sourceHash: f107a4082454
      provenance: machine
      outputHash: c6837636896e
    paths./api/v1/tools/pdf/pdf-to-word.post.summary:
      sourceHash: 283553b063ad
      provenance: machine
      outputHash: 8e10c5182944
    paths./api/v1/tools/pdf/pdf-to-word.post.description:
      sourceHash: 324b6f3d35f7
      provenance: machine
      outputHash: 13cc74b8ddbf
    paths./api/v1/tools/pdf/pdf-metadata.post.summary:
      sourceHash: 7aa0fbdbeac6
      provenance: machine
      outputHash: d469bdf0ce9b
    paths./api/v1/tools/pdf/pdf-metadata.post.description:
      sourceHash: 4267623afc19
      provenance: machine
      outputHash: 175081e807ee
    paths./api/v1/tools/pdf/ocr-pdf.post.summary:
      sourceHash: 2d9dfe74b2d1
      provenance: machine
      outputHash: 2d9dfe74b2d1
    paths./api/v1/tools/pdf/ocr-pdf.post.description:
      sourceHash: fbed951c39df
      provenance: machine
      outputHash: 7be0ddb62f0f
    paths./api/v1/tools/files/chart-maker.post.summary:
      sourceHash: dbcca76ccbf5
      provenance: machine
      outputHash: 1cbef6b7cd5d
    paths./api/v1/tools/files/chart-maker.post.description:
      sourceHash: e4adb80dab58
      provenance: machine
      outputHash: aaf365538808
    paths./api/v1/tools/files/csv-excel.post.summary:
      sourceHash: c53386a6d40d
      provenance: machine
      outputHash: ae42e7eb3bbd
    paths./api/v1/tools/files/csv-excel.post.description:
      sourceHash: b567a5eb0ee5
      provenance: machine
      outputHash: e28e64bd7866
    paths./api/v1/tools/files/csv-json.post.summary:
      sourceHash: 3ed0542f5ca4
      provenance: machine
      outputHash: 23b12631c9c5
    paths./api/v1/tools/files/csv-json.post.description:
      sourceHash: 6ed58f7ca5ac
      provenance: machine
      outputHash: 7c52ac5a8016
    paths./api/v1/tools/files/json-xml.post.summary:
      sourceHash: 0e374572749d
      provenance: machine
      outputHash: 32d5223f9135
    paths./api/v1/tools/files/json-xml.post.description:
      sourceHash: bbb56d23a274
      provenance: machine
      outputHash: ee4fb521a806
    paths./api/v1/tools/files/split-csv.post.summary:
      sourceHash: 06c1cfb6cb2c
      provenance: machine
      outputHash: 3f8fbe73c833
    paths./api/v1/tools/files/split-csv.post.description:
      sourceHash: 1181658ae239
      provenance: machine
      outputHash: 30cc437f893c
    paths./api/v1/tools/files/merge-csvs.post.summary:
      sourceHash: 0042c1153e55
      provenance: machine
      outputHash: d20e22a08993
    paths./api/v1/tools/files/merge-csvs.post.description:
      sourceHash: 4956c801900a
      provenance: machine
      outputHash: d1ff3f87049d
    paths./api/v1/tools/files/yaml-json.post.summary:
      sourceHash: c73b92121dff
      provenance: machine
      outputHash: c73b92121dff
    paths./api/v1/tools/files/yaml-json.post.description:
      sourceHash: a7fa089ac00a
      provenance: machine
      outputHash: ccc290d01826
    paths./api/v1/tools/files/xml-to-csv.post.summary:
      sourceHash: 909195e97a63
      provenance: machine
      outputHash: 5db9fa129119
    paths./api/v1/tools/files/xml-to-csv.post.description:
      sourceHash: 156ea77c8a62
      provenance: machine
      outputHash: f44225a11260
    paths./api/v1/tools/files/create-zip.post.summary:
      sourceHash: 8463eb4799ce
      provenance: machine
      outputHash: 25da6290710f
    paths./api/v1/tools/files/create-zip.post.description:
      sourceHash: 26c97508b498
      provenance: machine
      outputHash: d1bc14303d0a
    paths./api/v1/tools/files/extract-zip.post.summary:
      sourceHash: aa7dad519142
      provenance: machine
      outputHash: c5214b39a86c
    paths./api/v1/tools/files/extract-zip.post.description:
      sourceHash: 5b025b124d8b
      provenance: machine
      outputHash: 82a20aa7293d
    paths./api/v1/tools/image/background-replace.post.summary:
      sourceHash: 971cacdad5ce
      provenance: machine
      outputHash: a9d08e97472e
    paths./api/v1/tools/image/background-replace.post.description:
      sourceHash: 5f7a0c0dbbc7
      provenance: machine
      outputHash: c5bf6dbe2798
    paths./api/v1/tools/image/blur-background.post.summary:
      sourceHash: 2383b0d9c928
      provenance: machine
      outputHash: 1bad70286942
    paths./api/v1/tools/image/blur-background.post.description:
      sourceHash: f3afcae74767
      provenance: machine
      outputHash: 28f506548679
    paths./api/v1/tools/image/barcode-generate.post.summary:
      sourceHash: 4717ee594681
      provenance: machine
      outputHash: 672e394a6f57
    paths./api/v1/tools/image/barcode-generate.post.description:
      sourceHash: e174f8923d5d
      provenance: machine
      outputHash: cd023684800a
    paths./api/v1/tools/image/circle-crop.post.summary:
      sourceHash: d76473270976
      provenance: machine
      outputHash: b5f8746224cb
    paths./api/v1/tools/image/circle-crop.post.description:
      sourceHash: 3e4d4819d594
      provenance: machine
      outputHash: 63930e5fecd8
    paths./api/v1/tools/image/duotone.post.summary:
      sourceHash: 27e78e7e8cca
      provenance: machine
      outputHash: eda2fe2b5da1
    paths./api/v1/tools/image/duotone.post.description:
      sourceHash: f3d5b592526a
      provenance: machine
      outputHash: 4e9944760f79
    paths./api/v1/tools/image/image-pad.post.summary:
      sourceHash: 7cbc7ca5de5b
      provenance: machine
      outputHash: 44f48ad94185
    paths./api/v1/tools/image/image-pad.post.description:
      sourceHash: f222b64a52bc
      provenance: machine
      outputHash: 8537a1ffbcc7
    paths./api/v1/tools/image/pixelate.post.summary:
      sourceHash: f4982f86e732
      provenance: machine
      outputHash: be5110ce22e9
    paths./api/v1/tools/image/pixelate.post.description:
      sourceHash: c47f345410f8
      provenance: machine
      outputHash: e1e4ca6ea900
    paths./api/v1/tools/image/vignette.post.summary:
      sourceHash: f841c2080740
      provenance: machine
      outputHash: 384f22c9e051
    paths./api/v1/tools/image/vignette.post.description:
      sourceHash: 9e1e807e89e0
      provenance: machine
      outputHash: c89ad3b1ef3f
    paths./api/v1/tools/image/gif-webp.post.summary:
      sourceHash: 337e539d70a4
      provenance: machine
      outputHash: 0587ae9bf938
    paths./api/v1/tools/image/gif-webp.post.description:
      sourceHash: f397405d0b9c
      provenance: machine
      outputHash: 11f870bcc947
    paths./api/v1/tools/image/histogram.post.summary:
      sourceHash: b32dc489fba8
      provenance: machine
      outputHash: 8a9c4d63d739
    paths./api/v1/tools/image/histogram.post.description:
      sourceHash: f98c4a1d1bf0
      provenance: machine
      outputHash: 5366d4734a90
    paths./api/v1/tools/image/lqip-placeholder.post.summary:
      sourceHash: 878e3f3518db
      provenance: machine
      outputHash: 27e1ac811783
    paths./api/v1/tools/image/lqip-placeholder.post.description:
      sourceHash: 2100f7e08142
      provenance: machine
      outputHash: 519c6105fa9b
    paths./api/v1/tools/image/sprite-sheet.post.summary:
      sourceHash: f1c0772c4599
      provenance: machine
      outputHash: 6e4dbdac8e06
    paths./api/v1/tools/image/sprite-sheet.post.description:
      sourceHash: 4ef141ec92ed
      provenance: machine
      outputHash: f651dd096e9e
    paths./api/v1/tools/image/passport-photo.post.summary:
      sourceHash: 535e5bf9e5e6
      provenance: machine
      outputHash: 5d54f3fd6a1f
    paths./api/v1/tools/image/passport-photo.post.description:
      sourceHash: 7d3a81dbb05c
      provenance: machine
      outputHash: a6c4d4d463d9
    paths./api/v1/tools/pdf/sign-pdf.post.summary:
      sourceHash: 1bf93e215df9
      provenance: machine
      outputHash: 26b4257a4d0e
    paths./api/v1/tools/pdf/sign-pdf.post.description:
      sourceHash: d66e168d79ba
      provenance: machine
      outputHash: b1ea3b90869b
    paths./api/v1/tools/image/jpg-to-png.post.summary:
      sourceHash: 2b455c24e59e
      provenance: machine
      outputHash: f7eb011443a9
    paths./api/v1/tools/image/jpg-to-png.post.description:
      sourceHash: 1d504faac90f
      provenance: machine
      outputHash: 55de47a23bb2
    paths./api/v1/tools/image/png-to-jpg.post.summary:
      sourceHash: 9e2eef671853
      provenance: machine
      outputHash: 61f17838f418
    paths./api/v1/tools/image/png-to-jpg.post.description:
      sourceHash: 12d6c8d6aa01
      provenance: machine
      outputHash: 820b5d6daa35
    paths./api/v1/tools/image/jpg-to-webp.post.summary:
      sourceHash: 1e67451d3358
      provenance: machine
      outputHash: 56f0ac0e4329
    paths./api/v1/tools/image/jpg-to-webp.post.description:
      sourceHash: 0e7e4b18daa5
      provenance: machine
      outputHash: 5b54eedbdb50
    paths./api/v1/tools/image/png-to-webp.post.summary:
      sourceHash: 6c9410143dd5
      provenance: machine
      outputHash: bfa711c3deb9
    paths./api/v1/tools/image/png-to-webp.post.description:
      sourceHash: 7505ddaf944b
      provenance: machine
      outputHash: 71f9ea4e57b8
    paths./api/v1/tools/image/webp-to-jpg.post.summary:
      sourceHash: 670a9097dec8
      provenance: machine
      outputHash: 6d665fd39be8
    paths./api/v1/tools/image/webp-to-jpg.post.description:
      sourceHash: 2b7701fe02eb
      provenance: machine
      outputHash: a8963d23ed22
    paths./api/v1/tools/image/webp-to-png.post.summary:
      sourceHash: 781a17cb5ec0
      provenance: machine
      outputHash: 822c607d1238
    paths./api/v1/tools/image/webp-to-png.post.description:
      sourceHash: 78645c123b20
      provenance: machine
      outputHash: 768cf14add08
    paths./api/v1/tools/image/jpg-to-avif.post.summary:
      sourceHash: 1c50ea7bdfce
      provenance: machine
      outputHash: c1ca24db3220
    paths./api/v1/tools/image/jpg-to-avif.post.description:
      sourceHash: fbc8f6ae407c
      provenance: machine
      outputHash: 8435b66d5b9b
    paths./api/v1/tools/image/png-to-avif.post.summary:
      sourceHash: c3ba0668ee1f
      provenance: machine
      outputHash: ea31b7f85491
    paths./api/v1/tools/image/png-to-avif.post.description:
      sourceHash: d0dcdde42555
      provenance: machine
      outputHash: 39bc97612bde
    paths./api/v1/tools/image/webp-to-avif.post.summary:
      sourceHash: 690ea37f961e
      provenance: machine
      outputHash: 3613acc4545b
    paths./api/v1/tools/image/webp-to-avif.post.description:
      sourceHash: bcae3fc230b8
      provenance: machine
      outputHash: 1e60bfb49dd4
    paths./api/v1/tools/image/heic-to-jpg.post.summary:
      sourceHash: 0013ad2f7e97
      provenance: machine
      outputHash: 49d4c54c0911
    paths./api/v1/tools/image/heic-to-jpg.post.description:
      sourceHash: deb0641a6bfd
      provenance: machine
      outputHash: b69ddb8eee24
    paths./api/v1/tools/image/heic-to-png.post.summary:
      sourceHash: a191f1708025
      provenance: machine
      outputHash: c00aade43d39
    paths./api/v1/tools/image/heic-to-png.post.description:
      sourceHash: 583345b636d6
      provenance: machine
      outputHash: 806c716dbc66
    paths./api/v1/tools/image/heic-to-avif.post.summary:
      sourceHash: a03031e5c80d
      provenance: machine
      outputHash: 45f900be50d3
    paths./api/v1/tools/image/heic-to-avif.post.description:
      sourceHash: 6812ebf99c0b
      provenance: machine
      outputHash: 4f25ea06e0cb
    paths./api/v1/tools/image/jpg-to-gif.post.summary:
      sourceHash: 46b3bcd3a38f
      provenance: machine
      outputHash: b5f60b4612e6
    paths./api/v1/tools/image/jpg-to-gif.post.description:
      sourceHash: 365097fb68a9
      provenance: machine
      outputHash: ee98f36e997c
    paths./api/v1/tools/image/png-to-gif.post.summary:
      sourceHash: e4de0e11bf3d
      provenance: machine
      outputHash: 1b0b1ff55618
    paths./api/v1/tools/image/png-to-gif.post.description:
      sourceHash: 89620988c5bd
      provenance: machine
      outputHash: '010601736143'
    paths./api/v1/tools/image/gif-to-jpg.post.summary:
      sourceHash: 016ce94121b8
      provenance: machine
      outputHash: 88f30d716256
    paths./api/v1/tools/image/gif-to-jpg.post.description:
      sourceHash: d3c99d325441
      provenance: machine
      outputHash: 9d68e618ace8
    paths./api/v1/tools/image/gif-to-png.post.summary:
      sourceHash: 8f3239971851
      provenance: machine
      outputHash: e9dcb7beca88
    paths./api/v1/tools/image/gif-to-png.post.description:
      sourceHash: 6b363c6c5b6a
      provenance: machine
      outputHash: b45f307f0848
    paths./api/v1/tools/image/webp-to-gif.post.summary:
      sourceHash: db331b95e19b
      provenance: machine
      outputHash: 20daf361f6ec
    paths./api/v1/tools/image/webp-to-gif.post.description:
      sourceHash: 9295d8ac8c09
      provenance: machine
      outputHash: 956b3cd27d64
    paths./api/v1/tools/image/jpg-to-tiff.post.summary:
      sourceHash: 86ae950de6a3
      provenance: machine
      outputHash: 28a3b01d166c
    paths./api/v1/tools/image/jpg-to-tiff.post.description:
      sourceHash: 4f0ac37f2e06
      provenance: machine
      outputHash: d2985e3ab20b
    paths./api/v1/tools/image/png-to-tiff.post.summary:
      sourceHash: 3fa124cbaa4a
      provenance: machine
      outputHash: 252daab8de2b
    paths./api/v1/tools/image/png-to-tiff.post.description:
      sourceHash: 5cf97e597ee8
      provenance: machine
      outputHash: 9239b164af41
    paths./api/v1/tools/image/tiff-to-jpg.post.summary:
      sourceHash: c803f62926b8
      provenance: machine
      outputHash: 230fac87250f
    paths./api/v1/tools/image/tiff-to-jpg.post.description:
      sourceHash: cf928b0513e5
      provenance: machine
      outputHash: d07fd51be2fd
    paths./api/v1/tools/image/tiff-to-png.post.summary:
      sourceHash: 73c313976bb4
      provenance: machine
      outputHash: fa5b1dc702c3
    paths./api/v1/tools/image/tiff-to-png.post.description:
      sourceHash: 52f065d2f324
      provenance: machine
      outputHash: 03c0890b98b7
    paths./api/v1/tools/image/psd-to-jpg.post.summary:
      sourceHash: b9d50a4fd5a9
      provenance: machine
      outputHash: 4568e017cf64
    paths./api/v1/tools/image/psd-to-jpg.post.description:
      sourceHash: a9897ebb6e8c
      provenance: machine
      outputHash: 791ede867b82
    paths./api/v1/tools/image/psd-to-png.post.summary:
      sourceHash: af36a6f53221
      provenance: machine
      outputHash: bd912e8e7f96
    paths./api/v1/tools/image/psd-to-png.post.description:
      sourceHash: b15171b20160
      provenance: machine
      outputHash: 3dd152602702
    paths./api/v1/tools/image/png-to-eps.post.summary:
      sourceHash: 33ce1b37d63f
      provenance: machine
      outputHash: b54384e96317
    paths./api/v1/tools/image/png-to-eps.post.description:
      sourceHash: 31d8bfe76c0d
      provenance: machine
      outputHash: e466fb93a945
    paths./api/v1/tools/image/jpg-to-eps.post.summary:
      sourceHash: e8bde3997aaf
      provenance: machine
      outputHash: 09efd472f7b9
    paths./api/v1/tools/image/jpg-to-eps.post.description:
      sourceHash: 62cce0cb73ba
      provenance: machine
      outputHash: 1053f06688ba
    paths./api/v1/tools/image/eps-to-png.post.summary:
      sourceHash: be7ed58f6b7e
      provenance: machine
      outputHash: be5a04b5eb07
    paths./api/v1/tools/image/eps-to-png.post.description:
      sourceHash: e9157e4e334b
      provenance: machine
      outputHash: 6deb52d21b76
    paths./api/v1/tools/image/eps-to-jpg.post.summary:
      sourceHash: c05512db4202
      provenance: machine
      outputHash: 26511e27bfc8
    paths./api/v1/tools/image/eps-to-jpg.post.description:
      sourceHash: b582104358cd
      provenance: machine
      outputHash: fa9ff107c778
    paths./api/v1/tools/image/png-to-svg.post.summary:
      sourceHash: 651353e8e22c
      provenance: machine
      outputHash: 36d48a210641
    paths./api/v1/tools/image/png-to-svg.post.description:
      sourceHash: 5cbb3b81f383
      provenance: machine
      outputHash: 2b8f0f1c2293
    paths./api/v1/tools/image/jpg-to-svg.post.summary:
      sourceHash: 46f10c1c31b4
      provenance: machine
      outputHash: f8ac86865ed8
    paths./api/v1/tools/image/jpg-to-svg.post.description:
      sourceHash: ea475d6542cb
      provenance: machine
      outputHash: 2cfc09a29653
    paths./api/v1/tools/image/tiff-to-svg.post.summary:
      sourceHash: 52f9db13bb53
      provenance: machine
      outputHash: 36e9aa6286c3
    paths./api/v1/tools/image/tiff-to-svg.post.description:
      sourceHash: 26ac4eed4312
      provenance: machine
      outputHash: 194f0eb4aee2
    paths./api/v1/tools/image/psd-to-svg.post.summary:
      sourceHash: 654d378a3882
      provenance: machine
      outputHash: 07a168e37bb8
    paths./api/v1/tools/image/psd-to-svg.post.description:
      sourceHash: 990b5ce49f61
      provenance: machine
      outputHash: dd9c87f0a331
    paths./api/v1/tools/image/eps-to-svg.post.summary:
      sourceHash: 36137967b026
      provenance: machine
      outputHash: 1c097457c91c
    paths./api/v1/tools/image/eps-to-svg.post.description:
      sourceHash: 8c2914626a16
      provenance: machine
      outputHash: 8b754087005c
    paths./api/v1/tools/image/svg-to-png.post.summary:
      sourceHash: 1f1eb20e97fe
      provenance: machine
      outputHash: 98838e4eb2a2
    paths./api/v1/tools/image/svg-to-png.post.description:
      sourceHash: 5338d3a81ff7
      provenance: machine
      outputHash: 02763e31d01b
    paths./api/v1/tools/image/svg-to-jpg.post.summary:
      sourceHash: 8e51faf94ff1
      provenance: machine
      outputHash: 7a32b45abb02
    paths./api/v1/tools/image/svg-to-jpg.post.description:
      sourceHash: 23241e664642
      provenance: machine
      outputHash: 7623d8ab1e0e
    paths./api/v1/tools/image/jpg-to-pdf.post.summary:
      sourceHash: 480104dd2622
      provenance: machine
      outputHash: 0235b0c3057a
    paths./api/v1/tools/image/jpg-to-pdf.post.description:
      sourceHash: daffc3441cf6
      provenance: machine
      outputHash: f559b70bb8f0
    paths./api/v1/tools/image/png-to-pdf.post.summary:
      sourceHash: 7cab3612203f
      provenance: machine
      outputHash: 905393567f1c
    paths./api/v1/tools/image/png-to-pdf.post.description:
      sourceHash: 135646c47b37
      provenance: machine
      outputHash: 36e0016325ef
    paths./api/v1/tools/image/heic-to-pdf.post.summary:
      sourceHash: d9a32701511d
      provenance: machine
      outputHash: 9e75d9d3627c
    paths./api/v1/tools/image/heic-to-pdf.post.description:
      sourceHash: cb1a4bc6ad48
      provenance: machine
      outputHash: b1c020f10542
    paths./api/v1/tools/image/tiff-to-pdf.post.summary:
      sourceHash: 76458bef5bb7
      provenance: machine
      outputHash: 6010c96f3df2
    paths./api/v1/tools/image/tiff-to-pdf.post.description:
      sourceHash: f26e932da894
      provenance: machine
      outputHash: e0feac3de524
    paths./api/v1/tools/image/webp-to-pdf.post.summary:
      sourceHash: fdbcd78acb8e
      provenance: machine
      outputHash: 14457d6f8011
    paths./api/v1/tools/image/webp-to-pdf.post.description:
      sourceHash: 02d3842d170f
      provenance: machine
      outputHash: fdec62de2c5b
    paths./api/v1/tools/image/gif-to-pdf.post.summary:
      sourceHash: 62a5f26018fc
      provenance: machine
      outputHash: bceec3c97a7c
    paths./api/v1/tools/image/gif-to-pdf.post.description:
      sourceHash: e1d4ce05a711
      provenance: machine
      outputHash: 55d1a5aa00f8
    paths./api/v1/tools/image/eps-to-pdf.post.summary:
      sourceHash: b183686e176a
      provenance: machine
      outputHash: b8c34e3f0dfc
    paths./api/v1/tools/image/eps-to-pdf.post.description:
      sourceHash: 797845fc1a13
      provenance: machine
      outputHash: c7a466014ba8
    paths./api/v1/tools/pdf/pdf-to-jpg.post.summary:
      sourceHash: f84d053c3344
      provenance: machine
      outputHash: 74a6e0da024a
    paths./api/v1/tools/pdf/pdf-to-jpg.post.description:
      sourceHash: 294e7071a0cd
      provenance: machine
      outputHash: 912ee6177e2f
    paths./api/v1/tools/pdf/pdf-to-png.post.summary:
      sourceHash: 18362545fcb8
      provenance: machine
      outputHash: b009b6be6f06
    paths./api/v1/tools/pdf/pdf-to-png.post.description:
      sourceHash: 879e2d2c287b
      provenance: machine
      outputHash: 9a3032be71cb
    paths./api/v1/tools/pdf/pdf-to-tiff.post.summary:
      sourceHash: cd0065e23473
      provenance: machine
      outputHash: 3d7e151cceac
    paths./api/v1/tools/pdf/pdf-to-tiff.post.description:
      sourceHash: 4e46452ab02e
      provenance: machine
      outputHash: 7ed16856fc49
    paths./api/v1/tools/video/mov-to-mp4.post.summary:
      sourceHash: b88833009ff9
      provenance: machine
      outputHash: ae19254883a2
    paths./api/v1/tools/video/mov-to-mp4.post.description:
      sourceHash: a248339c26d1
      provenance: machine
      outputHash: d94730f5d00d
    paths./api/v1/tools/video/webm-to-mp4.post.summary:
      sourceHash: 66e6dae5210e
      provenance: machine
      outputHash: 240d114a19c3
    paths./api/v1/tools/video/webm-to-mp4.post.description:
      sourceHash: a9977ca09125
      provenance: machine
      outputHash: 811abd52d410
    paths./api/v1/tools/video/mkv-to-mp4.post.summary:
      sourceHash: cffe53cd9489
      provenance: machine
      outputHash: 89289ee1baff
    paths./api/v1/tools/video/mkv-to-mp4.post.description:
      sourceHash: d4c9bfc05b71
      provenance: machine
      outputHash: 27ff45252e2e
    paths./api/v1/tools/video/avi-to-mp4.post.summary:
      sourceHash: a5ed5b5782d3
      provenance: machine
      outputHash: eb8ecb00cd55
    paths./api/v1/tools/video/avi-to-mp4.post.description:
      sourceHash: 59f0c2a1860b
      provenance: machine
      outputHash: 47138d18c575
    paths./api/v1/tools/video/mp4-to-mov.post.summary:
      sourceHash: a20af2c4b1fb
      provenance: machine
      outputHash: ea2bea417930
    paths./api/v1/tools/video/mp4-to-mov.post.description:
      sourceHash: 8d99e8459b46
      provenance: machine
      outputHash: 68e9550a4ca2
    paths./api/v1/tools/video/mp4-to-webm.post.summary:
      sourceHash: a1b67df8d74e
      provenance: machine
      outputHash: 653fccf6d2a0
    paths./api/v1/tools/video/mp4-to-webm.post.description:
      sourceHash: 2d0fec602c82
      provenance: machine
      outputHash: cdd874b538ca
    paths./api/v1/tools/video/webm-to-mov.post.summary:
      sourceHash: 60df7d3b385b
      provenance: machine
      outputHash: 176e21ecd067
    paths./api/v1/tools/video/webm-to-mov.post.description:
      sourceHash: 6982fc1cb933
      provenance: machine
      outputHash: 6f816e85f83e
    paths./api/v1/tools/video/mkv-to-mov.post.summary:
      sourceHash: dc04291270e6
      provenance: machine
      outputHash: 08d4a0538e97
    paths./api/v1/tools/video/mkv-to-mov.post.description:
      sourceHash: 71c9c1b99374
      provenance: machine
      outputHash: 0c07d684f26c
    paths./api/v1/tools/video/avi-to-mov.post.summary:
      sourceHash: 449c58ca9486
      provenance: machine
      outputHash: 97c37f5ac4b9
    paths./api/v1/tools/video/avi-to-mov.post.description:
      sourceHash: 62b1fd2e8e0c
      provenance: machine
      outputHash: 32677edfac76
    paths./api/v1/tools/video/mp4-to-avi.post.summary:
      sourceHash: 700763fbdb4a
      provenance: machine
      outputHash: 14ad566ee776
    paths./api/v1/tools/video/mp4-to-avi.post.description:
      sourceHash: da2c6f405f96
      provenance: machine
      outputHash: 0115167b35de
    paths./api/v1/tools/video/mov-to-avi.post.summary:
      sourceHash: 6cc35a24d9fa
      provenance: machine
      outputHash: cc41bea586c5
    paths./api/v1/tools/video/mov-to-avi.post.description:
      sourceHash: e1dd8989a812
      provenance: machine
      outputHash: 347f5bb56941
    paths./api/v1/tools/video/mkv-to-avi.post.summary:
      sourceHash: 993ee5fab472
      provenance: machine
      outputHash: 34793968fbd0
    paths./api/v1/tools/video/mkv-to-avi.post.description:
      sourceHash: f1b9b4ced057
      provenance: machine
      outputHash: 43a775fd58ce
    paths./api/v1/tools/video/avi-to-mkv.post.summary:
      sourceHash: 3393bba23656
      provenance: machine
      outputHash: b32890dc70f6
    paths./api/v1/tools/video/avi-to-mkv.post.description:
      sourceHash: ce821c05956b
      provenance: machine
      outputHash: b19858302e2a
    paths./api/v1/tools/video/mp4-to-gif.post.summary:
      sourceHash: 62a7b6e94a53
      provenance: machine
      outputHash: 356b8f035b8b
    paths./api/v1/tools/video/mp4-to-gif.post.description:
      sourceHash: eb95a94fc0e0
      provenance: machine
      outputHash: 3e7b45c012e2
    paths./api/v1/tools/video/mov-to-gif.post.summary:
      sourceHash: c0e8c3dcfdfd
      provenance: machine
      outputHash: 19ae17401d6c
    paths./api/v1/tools/video/mov-to-gif.post.description:
      sourceHash: f0f7c24adb4f
      provenance: machine
      outputHash: 9b089ab2f3d7
    paths./api/v1/tools/video/mkv-to-gif.post.summary:
      sourceHash: c19834912c0e
      provenance: machine
      outputHash: 85cb2b210169
    paths./api/v1/tools/video/mkv-to-gif.post.description:
      sourceHash: 591062eb518f
      provenance: machine
      outputHash: ecb5b719347a
    paths./api/v1/tools/video/avi-to-gif.post.summary:
      sourceHash: b6286a84b91d
      provenance: machine
      outputHash: ac4bb341ce9d
    paths./api/v1/tools/video/avi-to-gif.post.description:
      sourceHash: dd44bdc0686d
      provenance: machine
      outputHash: 9dd7d7c4feb6
    paths./api/v1/tools/video/gif-to-mp4.post.summary:
      sourceHash: 9c2d0df2d1e9
      provenance: machine
      outputHash: a00e73f14984
    paths./api/v1/tools/video/gif-to-mp4.post.description:
      sourceHash: 96914944fdc5
      provenance: machine
      outputHash: 5dd4dea4e3f2
    paths./api/v1/tools/video/gif-to-webm.post.summary:
      sourceHash: f2b694034e10
      provenance: machine
      outputHash: b93a8980c30f
    paths./api/v1/tools/video/gif-to-webm.post.description:
      sourceHash: 221c2fee1775
      provenance: machine
      outputHash: 9f72172f5ae9
    paths./api/v1/tools/video/gif-to-mov.post.summary:
      sourceHash: 45c5f35f45f3
      provenance: machine
      outputHash: c743e07ae94e
    paths./api/v1/tools/video/gif-to-mov.post.description:
      sourceHash: 57996ae8e8cb
      provenance: machine
      outputHash: e0da32b0779b
    paths./api/v1/tools/video/mp4-to-mp3.post.summary:
      sourceHash: 69e107b048a8
      provenance: machine
      outputHash: 0fa8c75a91bb
    paths./api/v1/tools/video/mp4-to-mp3.post.description:
      sourceHash: 40de83aa6ed2
      provenance: machine
      outputHash: aa62bd8948cc
    paths./api/v1/tools/video/mov-to-mp3.post.summary:
      sourceHash: e149b039deaf
      provenance: machine
      outputHash: 865f5ab1f40f
    paths./api/v1/tools/video/mov-to-mp3.post.description:
      sourceHash: 27f1a1f36a88
      provenance: machine
      outputHash: 520024de0f8d
    paths./api/v1/tools/video/mkv-to-mp3.post.summary:
      sourceHash: a017e66660ad
      provenance: machine
      outputHash: bc56f7cba4fa
    paths./api/v1/tools/video/mkv-to-mp3.post.description:
      sourceHash: 12cb145afcd0
      provenance: machine
      outputHash: 13e30c6050ac
    paths./api/v1/tools/video/webm-to-mp3.post.summary:
      sourceHash: 9fbcc26dd542
      provenance: machine
      outputHash: 24cc5de0575d
    paths./api/v1/tools/video/webm-to-mp3.post.description:
      sourceHash: 5213b45f3c7d
      provenance: machine
      outputHash: 86cd62c66612
    paths./api/v1/tools/video/avi-to-mp3.post.summary:
      sourceHash: 5f4d7167967b
      provenance: machine
      outputHash: 5892fe4b615b
    paths./api/v1/tools/video/avi-to-mp3.post.description:
      sourceHash: dea6a15cea91
      provenance: machine
      outputHash: e94f3a78f9c0
    paths./api/v1/tools/video/mp4-to-wav.post.summary:
      sourceHash: b6769af66349
      provenance: machine
      outputHash: e21bff6a252c
    paths./api/v1/tools/video/mp4-to-wav.post.description:
      sourceHash: 5b9126a6bb99
      provenance: machine
      outputHash: 21617e4d7b0c
    paths./api/v1/tools/video/mov-to-wav.post.summary:
      sourceHash: 4194226fdf68
      provenance: machine
      outputHash: af6212eae5ab
    paths./api/v1/tools/video/mov-to-wav.post.description:
      sourceHash: fe4925d7babd
      provenance: machine
      outputHash: e05f50f57ed8
    paths./api/v1/tools/video/mp4-to-ogg.post.summary:
      sourceHash: dcb33be4e615
      provenance: machine
      outputHash: a11673c06ea2
    paths./api/v1/tools/video/mp4-to-ogg.post.description:
      sourceHash: d8be42b38707
      provenance: machine
      outputHash: 736ac7f33a45
    paths./api/v1/tools/audio/m4a-to-mp3.post.summary:
      sourceHash: e4f86f589bd4
      provenance: machine
      outputHash: 191741e637c4
    paths./api/v1/tools/audio/m4a-to-mp3.post.description:
      sourceHash: ad144ccdc1d4
      provenance: machine
      outputHash: e167e796d05d
    paths./api/v1/tools/audio/m4a-to-wav.post.summary:
      sourceHash: 5e8aa1c62150
      provenance: machine
      outputHash: 6272cc71efc5
    paths./api/v1/tools/audio/m4a-to-wav.post.description:
      sourceHash: dab8ed1c0668
      provenance: machine
      outputHash: 44a8c35e68c3
    paths./api/v1/tools/audio/aac-to-mp3.post.summary:
      sourceHash: dd7d2c46e335
      provenance: machine
      outputHash: ead0b5c53de0
    paths./api/v1/tools/audio/aac-to-mp3.post.description:
      sourceHash: 4187ef8fb789
      provenance: machine
      outputHash: aae2d7ae8a45
    paths./api/v1/tools/audio/aac-to-wav.post.summary:
      sourceHash: 6485f4d30fd0
      provenance: machine
      outputHash: 05c9ec504965
    paths./api/v1/tools/audio/aac-to-wav.post.description:
      sourceHash: c2221db94cf4
      provenance: machine
      outputHash: 166b56d0c84a
    paths./api/v1/tools/audio/aac-to-flac.post.summary:
      sourceHash: 9dc0a9886938
      provenance: machine
      outputHash: dd03c7a73864
    paths./api/v1/tools/audio/aac-to-flac.post.description:
      sourceHash: 945f63a2be18
      provenance: machine
      outputHash: aae71f753940
    paths./api/v1/tools/audio/ogg-to-mp3.post.summary:
      sourceHash: c3cff86b4282
      provenance: machine
      outputHash: d3ddecc1389f
    paths./api/v1/tools/audio/ogg-to-mp3.post.description:
      sourceHash: 8889a760d19f
      provenance: machine
      outputHash: d006fa8d64c1
    paths./api/v1/tools/audio/ogg-to-wav.post.summary:
      sourceHash: 85fbfd8ee338
      provenance: machine
      outputHash: 7a0aad7add5d
    paths./api/v1/tools/audio/ogg-to-wav.post.description:
      sourceHash: 99156f0db542
      provenance: machine
      outputHash: 8c0f12cd8f3f
    paths./api/v1/tools/audio/wav-to-mp3.post.summary:
      sourceHash: d8344c7d753c
      provenance: machine
      outputHash: d6d5918e8d95
    paths./api/v1/tools/audio/wav-to-mp3.post.description:
      sourceHash: 7f83d006baf8
      provenance: machine
      outputHash: f16dba3c9240
    paths./api/v1/tools/audio/mp3-to-wav.post.summary:
      sourceHash: a7de59ab62b9
      provenance: machine
      outputHash: 7a7b7f4ce901
    paths./api/v1/tools/audio/mp3-to-wav.post.description:
      sourceHash: c19d5fba1478
      provenance: machine
      outputHash: 61ee40bf691e
    paths./api/v1/tools/audio/flac-to-mp3.post.summary:
      sourceHash: da353684dd2b
      provenance: machine
      outputHash: 89bd92fedd47
    paths./api/v1/tools/audio/flac-to-mp3.post.description:
      sourceHash: e83797072608
      provenance: machine
      outputHash: c8bc1633eff7
    paths./api/v1/tools/files/excel-to-csv.post.summary:
      sourceHash: 8610a88e662a
      provenance: machine
      outputHash: 459cf2470d99
    paths./api/v1/tools/files/excel-to-csv.post.description:
      sourceHash: c5b6027afaa5
      provenance: machine
      outputHash: 2585d83efeab
    paths./api/v1/readyz.get.summary:
      sourceHash: 2f70aef72373
      provenance: machine
      outputHash: 67726d9fbe4e
    paths./api/v1/readyz.get.description:
      sourceHash: 1600aad09ab9
      provenance: machine
      outputHash: 25ede57a2d02
    paths./api/v1/jobs/{jobId}/cancel.post.summary:
      sourceHash: a5032d26d61d
      provenance: machine
      outputHash: 09d594a6ca5d
    paths./api/v1/jobs/{jobId}/cancel.post.description:
      sourceHash: c828ad6eed72
      provenance: machine
      outputHash: 3602571f3ac5
    paths./api/v1/preferences.get.summary:
      sourceHash: 14eccfecf549
      provenance: machine
      outputHash: 7c41da2cfac4
    paths./api/v1/preferences.get.description:
      sourceHash: be05d444bc85
      provenance: machine
      outputHash: 7e7dc0a5b557
    paths./api/v1/preferences.put.summary:
      sourceHash: 2649ca8f6a25
      provenance: machine
      outputHash: 0262e0f05194
    paths./api/v1/preferences.put.description:
      sourceHash: ac325d43a780
      provenance: machine
      outputHash: 953abd7a087c
    paths./api/auth/mfa/enroll.post.summary:
      sourceHash: c138aec8b79b
      provenance: machine
      outputHash: 5f9c1433f4f2
    paths./api/auth/mfa/enroll.post.description:
      sourceHash: e00560362017
      provenance: machine
      outputHash: 7d26428fff28
    paths./api/auth/mfa/verify.post.summary:
      sourceHash: 12b670ebf850
      provenance: machine
      outputHash: b7c125e4a574
    paths./api/auth/mfa/verify.post.description:
      sourceHash: ae628486beb5
      provenance: machine
      outputHash: a3d405bda3ed
    paths./api/auth/mfa/complete.post.summary:
      sourceHash: 16f65adb8582
      provenance: machine
      outputHash: 65922e31f562
    paths./api/auth/mfa/complete.post.description:
      sourceHash: 3dc67142d5da
      provenance: machine
      outputHash: 087b13228c45
    paths./api/auth/mfa/disable.post.summary:
      sourceHash: 8adb8274a7c6
      provenance: machine
      outputHash: cfb846867aad
    paths./api/auth/mfa/disable.post.description:
      sourceHash: 7980f5f0d9f0
      provenance: machine
      outputHash: 041a70c9fbea
    paths./api/auth/users/{id}/mfa/reset.post.summary:
      sourceHash: c765204eb72c
      provenance: machine
      outputHash: f11fe9de1839
    paths./api/auth/users/{id}/mfa/reset.post.description:
      sourceHash: 9d650aeb7d10
      provenance: machine
      outputHash: f623c780de5a
    paths./api/auth/oidc/login.get.summary:
      sourceHash: bac046b3ef24
      provenance: machine
      outputHash: eeaab6d87e03
    paths./api/auth/oidc/login.get.description:
      sourceHash: e48289d1e534
      provenance: machine
      outputHash: acb8b3c24522
    paths./api/auth/oidc/callback.get.summary:
      sourceHash: 396eda750d3c
      provenance: machine
      outputHash: e9fbc3fc7814
    paths./api/auth/oidc/callback.get.description:
      sourceHash: e9437a06c530
      provenance: machine
      outputHash: 72a1eaa09652
    paths./api/auth/saml/metadata.get.summary:
      sourceHash: d6b4c80ec1e4
      provenance: machine
      outputHash: 966b7b864475
    paths./api/auth/saml/metadata.get.description:
      sourceHash: 0b08445f03af
      provenance: machine
      outputHash: a96d947bca2c
    paths./api/auth/saml/login.get.summary:
      sourceHash: 6de21463c7cb
      provenance: machine
      outputHash: 2d07f38781d1
    paths./api/auth/saml/login.get.description:
      sourceHash: eca8bcf506ea
      provenance: machine
      outputHash: 95d2606ab4b9
    paths./api/auth/saml/callback.post.summary:
      sourceHash: 2a95ad454bd5
      provenance: machine
      outputHash: ca9bcc9acc5a
    paths./api/auth/saml/callback.post.description:
      sourceHash: a03dcebde897
      provenance: machine
      outputHash: a022a663e01d
    paths./api/v1/files/{id}/preview.get.summary:
      sourceHash: 71d50aa39313
      provenance: machine
      outputHash: 5f12c7f0bfe3
    paths./api/v1/files/{id}/preview.get.description:
      sourceHash: fb3edf8df6cf
      provenance: machine
      outputHash: 4580ae6b81e9
    paths./api/v1/preview/generate.post.summary:
      sourceHash: cff28fa8e380
      provenance: machine
      outputHash: 44f89deb59b2
    paths./api/v1/preview/generate.post.description:
      sourceHash: 56d484d0c85b
      provenance: machine
      outputHash: f2c5cbc9f2c4
    paths./api/v1/admin/log-level.get.summary:
      sourceHash: f9fa3ff842cd
      provenance: machine
      outputHash: '136534382936'
    paths./api/v1/admin/log-level.get.description:
      sourceHash: 277cddb1b284
      provenance: machine
      outputHash: ec7834a802ff
    paths./api/v1/admin/log-level.post.summary:
      sourceHash: e9ac46f76c49
      provenance: machine
      outputHash: d052456d8945
    paths./api/v1/admin/log-level.post.description:
      sourceHash: 5f12391622b9
      provenance: machine
      outputHash: 6d6f2bcd152f
    paths./api/v1/metrics.get.summary:
      sourceHash: 05be9e156194
      provenance: machine
      outputHash: af7da3597d57
    paths./api/v1/metrics.get.description:
      sourceHash: e5723b8ba79e
      provenance: machine
      outputHash: 9f786d4a32a7
    paths./api/v1/admin/support-bundle.get.summary:
      sourceHash: 3fc31382eaf7
      provenance: machine
      outputHash: 3944acd6ff93
    paths./api/v1/admin/support-bundle.get.description:
      sourceHash: c04e47b20915
      provenance: machine
      outputHash: 035e529982b8
    paths./api/v1/admin/usage.get.summary:
      sourceHash: 2f5017840735
      provenance: machine
      outputHash: a6eb2ffa592d
    paths./api/v1/admin/usage.get.description:
      sourceHash: 5ba73241349d
      provenance: machine
      outputHash: eb32151cb10e
    paths./api/v1/admin/backup-status.get.summary:
      sourceHash: cea8a0d690fc
      provenance: machine
      outputHash: 50ea1d93af1d
    paths./api/v1/admin/backup-status.get.description:
      sourceHash: e42e4494117a
      provenance: machine
      outputHash: 2b78fe43022a
    paths./api/v1/admin/backup-status.post.summary:
      sourceHash: 1a7ed3984596
      provenance: machine
      outputHash: bcc1d297fa06
    paths./api/v1/admin/backup-status.post.description:
      sourceHash: 84b506533dbf
      provenance: machine
      outputHash: 2676311b4325
    paths./api/v1/admin/features/import.post.summary:
      sourceHash: 31b34aebf83b
      provenance: machine
      outputHash: 64a41cfbb291
    paths./api/v1/admin/features/import.post.description:
      sourceHash: 229fcb03f525
      provenance: machine
      outputHash: 860db81bcb0f
    paths./api/v1/enterprise/scim/token.post.summary:
      sourceHash: e550c4aaeecf
      provenance: machine
      outputHash: 554df0aa342c
    paths./api/v1/enterprise/scim/token.post.description:
      sourceHash: 39d3b5d5066b
      provenance: machine
      outputHash: 5115e5701562
    paths./api/v1/enterprise/scim/token.delete.summary:
      sourceHash: e57912d50189
      provenance: machine
      outputHash: f54e7a615d87
    paths./api/v1/enterprise/scim/token.delete.description:
      sourceHash: 412b38ac0141
      provenance: machine
      outputHash: 5a28097ae2e9
    paths./api/v1/scim/v2/ServiceProviderConfig.get.summary:
      sourceHash: 6d9bb054fb7b
      provenance: machine
      outputHash: 97e6d2352c03
    paths./api/v1/scim/v2/ServiceProviderConfig.get.description:
      sourceHash: a111048aed19
      provenance: machine
      outputHash: f0b9cb83b39c
    paths./api/v1/scim/v2/Schemas.get.summary:
      sourceHash: c04171c8ce40
      provenance: machine
      outputHash: f0468f26a84e
    paths./api/v1/scim/v2/Schemas.get.description:
      sourceHash: 74cf47ef6038
      provenance: machine
      outputHash: 96e18c907821
    paths./api/v1/scim/v2/ResourceTypes.get.summary:
      sourceHash: b3f61558b888
      provenance: machine
      outputHash: 2ef116629594
    paths./api/v1/scim/v2/ResourceTypes.get.description:
      sourceHash: d3935183ed14
      provenance: machine
      outputHash: ab9aabdbe24b
    paths./api/v1/scim/v2/Users.get.summary:
      sourceHash: 06aa223783d0
      provenance: machine
      outputHash: daf8d7a31f2e
    paths./api/v1/scim/v2/Users.get.description:
      sourceHash: dccfb3116fb5
      provenance: machine
      outputHash: c63db930d2a2
    paths./api/v1/scim/v2/Users.post.summary:
      sourceHash: ba4ce4467898
      provenance: machine
      outputHash: 56c34a26ddf7
    paths./api/v1/scim/v2/Users.post.description:
      sourceHash: ab9d5413dfe0
      provenance: machine
      outputHash: 5a0741e3135e
    paths./api/v1/scim/v2/Users/{id}.get.summary:
      sourceHash: 06a26f48a1d7
      provenance: machine
      outputHash: 74f789597acd
    paths./api/v1/scim/v2/Users/{id}.get.description:
      sourceHash: f643db8015e1
      provenance: machine
      outputHash: e86882bea056
    paths./api/v1/scim/v2/Users/{id}.put.summary:
      sourceHash: acfe35ddd973
      provenance: machine
      outputHash: 17899aa8bc40
    paths./api/v1/scim/v2/Users/{id}.put.description:
      sourceHash: 7051119f905f
      provenance: machine
      outputHash: 9c79fc68cd52
    paths./api/v1/scim/v2/Users/{id}.delete.summary:
      sourceHash: 11c8da919810
      provenance: machine
      outputHash: a3de3b587ec9
    paths./api/v1/scim/v2/Users/{id}.delete.description:
      sourceHash: f8a3ee46d088
      provenance: machine
      outputHash: 92392a4733ed
    paths./api/v1/scim/v2/Groups.get.summary:
      sourceHash: 7f5cfe4da60d
      provenance: machine
      outputHash: ee4dec3e6e30
    paths./api/v1/scim/v2/Groups.get.description:
      sourceHash: b3dea6667317
      provenance: machine
      outputHash: 1658a1f61cde
    paths./api/v1/scim/v2/Groups.post.summary:
      sourceHash: 2477896fba1c
      provenance: machine
      outputHash: 743e5665151a
    paths./api/v1/scim/v2/Groups.post.description:
      sourceHash: fbb117796dfb
      provenance: machine
      outputHash: bdbc703a20f3
    paths./api/v1/scim/v2/Groups/{id}.get.summary:
      sourceHash: cf7956970f8e
      provenance: machine
      outputHash: 07eaa8506fbe
    paths./api/v1/scim/v2/Groups/{id}.get.description:
      sourceHash: 7aeb5a733198
      provenance: machine
      outputHash: 63cbb81a6edf
    paths./api/v1/scim/v2/Groups/{id}.put.summary:
      sourceHash: 5878fbd31c27
      provenance: machine
      outputHash: b3baacc5323e
    paths./api/v1/scim/v2/Groups/{id}.put.description:
      sourceHash: 0b41d735866c
      provenance: machine
      outputHash: e456b5d67427
    paths./api/v1/scim/v2/Groups/{id}.delete.summary:
      sourceHash: 37212ab76d6b
      provenance: machine
      outputHash: 074ae39fdb62
    paths./api/v1/scim/v2/Groups/{id}.delete.description:
      sourceHash: faf5572bb482
      provenance: machine
      outputHash: 5918965243ee
    paths./api/v1/enterprise/audit/export.get.summary:
      sourceHash: bb1ef74df6a7
      provenance: machine
      outputHash: 9640077d3c07
    paths./api/v1/enterprise/audit/export.get.description:
      sourceHash: 78723fd7a551
      provenance: machine
      outputHash: 8758a71760ab
    paths./api/v1/enterprise/config/export.get.summary:
      sourceHash: 0657bced852f
      provenance: machine
      outputHash: efea2136bc64
    paths./api/v1/enterprise/config/export.get.description:
      sourceHash: 8e3ce9c435f5
      provenance: machine
      outputHash: cf999b06bf18
    paths./api/v1/enterprise/config/import.post.summary:
      sourceHash: 8a507f40b80f
      provenance: machine
      outputHash: 9658e7ae2aa5
    paths./api/v1/enterprise/config/import.post.description:
      sourceHash: cf4809a9459d
      provenance: machine
      outputHash: 277b03d84d4f
    paths./api/v1/enterprise/ip-allowlist.get.summary:
      sourceHash: 8756df674b27
      provenance: machine
      outputHash: 13b37a4ee4eb
    paths./api/v1/enterprise/ip-allowlist.get.description:
      sourceHash: 42fe8d5e6555
      provenance: machine
      outputHash: b8c6c08478c0
    paths./api/v1/enterprise/ip-allowlist.put.summary:
      sourceHash: e0d442c15809
      provenance: machine
      outputHash: df14810f07f8
    paths./api/v1/enterprise/ip-allowlist.put.description:
      sourceHash: 0cb040540d15
      provenance: machine
      outputHash: d6e6bc3730d6
    paths./api/v1/enterprise/legal-hold.get.summary:
      sourceHash: a3b33d97f1b3
      provenance: machine
      outputHash: 14db0c27e797
    paths./api/v1/enterprise/legal-hold.get.description:
      sourceHash: 2b51807e41b2
      provenance: machine
      outputHash: a5bede450d17
    paths./api/v1/enterprise/legal-hold.put.summary:
      sourceHash: f0f8a1f6bd23
      provenance: machine
      outputHash: bd1391e93302
    paths./api/v1/enterprise/legal-hold.put.description:
      sourceHash: 2f22192732ff
      provenance: machine
      outputHash: 0357f2c207bd
    paths./api/v1/enterprise/siem/config.get.summary:
      sourceHash: 7ac4058ce316
      provenance: machine
      outputHash: bf339f958868
    paths./api/v1/enterprise/siem/config.get.description:
      sourceHash: b4c92171b7fb
      provenance: machine
      outputHash: a59b993b5927
    paths./api/v1/enterprise/siem/config.put.summary:
      sourceHash: b236d6669c61
      provenance: machine
      outputHash: c66bf5d39cc2
    paths./api/v1/enterprise/siem/config.put.description:
      sourceHash: 94cec3b103c8
      provenance: machine
      outputHash: 24e33a1b579c
    paths./api/v1/enterprise/webhooks.get.summary:
      sourceHash: a43cfb1a8c96
      provenance: machine
      outputHash: 8d63dc36c5d0
    paths./api/v1/enterprise/webhooks.get.description:
      sourceHash: 292a38feca49
      provenance: machine
      outputHash: b405449af8f8
    paths./api/v1/enterprise/webhooks.post.summary:
      sourceHash: 4a2b33ad2c1f
      provenance: machine
      outputHash: 3659e44b3b00
    paths./api/v1/enterprise/webhooks.post.description:
      sourceHash: 2cc3c4b18327
      provenance: machine
      outputHash: 8fb58ec28596
    paths./api/v1/enterprise/webhooks/{index}.put.summary:
      sourceHash: c5be5387d7be
      provenance: machine
      outputHash: 14cb097e9c7c
    paths./api/v1/enterprise/webhooks/{index}.put.description:
      sourceHash: 28806bb5b6ce
      provenance: machine
      outputHash: 6cfd1c948ba3
    paths./api/v1/enterprise/webhooks/{index}.delete.summary:
      sourceHash: 60f85e57e4a7
      provenance: machine
      outputHash: d1086b65375d
    paths./api/v1/enterprise/webhooks/{index}.delete.description:
      sourceHash: 97b9800c59a2
      provenance: machine
      outputHash: 79e2d1614b10
    paths./api/v1/enterprise/webhooks/{index}/test.post.summary:
      sourceHash: 333e8368ac5c
      provenance: machine
      outputHash: b608defa2ecf
    paths./api/v1/enterprise/webhooks/{index}/test.post.description:
      sourceHash: 5626cb4c037d
      provenance: machine
      outputHash: 744642328d4c
    paths./api/v1/enterprise/users/{id}/export.post.summary:
      sourceHash: 75556a6714fc
      provenance: machine
      outputHash: bbea12032bd3
    paths./api/v1/enterprise/users/{id}/export.post.description:
      sourceHash: 4f90b2a09ef8
      provenance: machine
      outputHash: 2a6f496ee34a
    paths./api/v1/enterprise/users/{id}/export/{jobId}.get.summary:
      sourceHash: 7fa624472044
      provenance: machine
      outputHash: 24f619f622bf
    paths./api/v1/enterprise/users/{id}/export/{jobId}.get.description:
      sourceHash: fb51267dcbf7
      provenance: machine
      outputHash: d54706af500c
    paths./api/v1/enterprise/users/{id}/purge.delete.summary:
      sourceHash: 09c40d574cc0
      provenance: machine
      outputHash: 7d3d2d0f7f73
    paths./api/v1/enterprise/users/{id}/purge.delete.description:
      sourceHash: 6db9ed4dabf1
      provenance: machine
      outputHash: b471b9c3b40e
    paths./api/v1/enterprise/teams/{id}/purge.delete.summary:
      sourceHash: e5f557c6db8b
      provenance: machine
      outputHash: 7928afcd1a35
    paths./api/v1/enterprise/teams/{id}/purge.delete.description:
      sourceHash: 5bcd69dee315
      provenance: machine
      outputHash: 9b8da1e0afa7
    paths./api/v1/admin/version.get.summary:
      sourceHash: 1fa5f3f5fa4a
      provenance: machine
      outputHash: c851e12b9293
    paths./api/v1/admin/version.get.description:
      sourceHash: b72b0bfc24e6
      provenance: machine
      outputHash: c197e5690b19
    paths./api/v1/admin/migrations/pending.get.summary:
      sourceHash: dd3d4aea6207
      provenance: machine
      outputHash: 1fdce1a93468
    paths./api/v1/admin/migrations/pending.get.description:
      sourceHash: 434f121758d2
      provenance: machine
      outputHash: 243781f53717
    paths./api/v1/admin/upgrade-check.get.summary:
      sourceHash: abc755f5b011
      provenance: machine
      outputHash: 10823b8b39da
    paths./api/v1/admin/upgrade-check.get.description:
      sourceHash: a21396abfd74
      provenance: machine
      outputHash: 8d02028134d5
    paths./llms.txt.get.summary:
      sourceHash: ba31286a938b
      provenance: machine
      outputHash: 93b4ead35737
    paths./llms.txt.get.description:
      sourceHash: 5f69094320cb
      provenance: machine
      outputHash: cfe51bcdfd23
    paths./llms-full.txt.get.summary:
      sourceHash: 6f12d5cecc9d
      provenance: machine
      outputHash: cf6b30791109
    paths./llms-full.txt.get.description:
      sourceHash: a135dfc6aac1
      provenance: machine
      outputHash: 478c8b3f4785
    paths./api/v1/openapi.yaml.get.summary:
      sourceHash: 9ee7dc77d462
      provenance: machine
      outputHash: 9ee7dc77d462
    paths./api/v1/openapi.yaml.get.description:
      sourceHash: 10ceb656a256
      provenance: machine
      outputHash: 15245ce506a1
    paths./api/v1/tools/popular.get.summary:
      sourceHash: de2139ffd4c3
      provenance: machine
      outputHash: 7b5e98c645f5
    paths./api/v1/tools/popular.get.description:
      sourceHash: 40c9637d843b
      provenance: machine
      outputHash: eeac4dc05501
