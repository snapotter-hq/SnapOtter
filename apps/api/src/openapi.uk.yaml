openapi: 3.1.0
info:
  title: SnapOtter API
  version: 2.0.0
  description: |
    REST API для SnapOtter, самостійно розгорнутої інфраструктури обробки файлів із 200+ інструментами для image, video, audio, PDF та документів.

    ## Автентифікація

    Більшість кінцевих точок вимагають автентифікації одним із двох способів:

    1. **Сесійний cookie** - Викличте `POST /api/auth/login` з ім'ям користувача та паролем. Відповідь містить поле `token`. Передавайте його як `Authorization: Bearer <token>` у подальших запитах.

    2. **Ключ API** - Згенеруйте ключ через інтерфейс налаштувань або `POST /api/v1/api-keys`. Ключі мають префікс `si_`. Передавайте його як `Authorization: Bearer si_...`.

    Кінцеві точки, позначені піктограмою замка, вимагають автентифікації. Кінцеві точки, доступні лише адміністраторам, зазначені в їхньому описі.
  license:
    name: AGPL-3.0
    url: https://github.com/snapotter-hq/snapotter/blob/main/LICENSE
servers:
  - url: /
    description: Current instance
tags:
  - name: Tools
    description: Інструменти обробки файлів для image, video, audio, PDF і files. Кожен приймає multipart-завантаження файлу та повертає URL для завантаження.
  - name: Batch
    description: Обробка кількох файлів одним інструментом за один запит.
  - name: Pipelines
    description: Об'єднання кількох інструментів у багаторазові робочі процеси.
  - name: Files
    description: Завантаження, вивантаження та керування обробленими файлами.
  - name: Auth
    description: Вхід, вихід, керування сесіями та адміністрування користувачів.
  - name: API Keys
    description: Створення та керування API-ключами для програмного доступу.
  - name: Settings
    description: Загальносистемна конфігурація (запис лише для адміністраторів).
  - name: Teams
    description: Організація користувачів у команди.
  - name: Roles
    description: Керування власними ролями з детальними дозволами.
  - name: Audit
    description: Журнал аудиту для відстеження адміністративних дій.
  - name: Analytics
    description: Конфігурація аналітики та згода користувача.
  - name: Features
    description: Встановлення та керування пакетами AI-функцій.
  - name: Admin
    description: Адміністративна діагностика стану.
  - name: System
    description: Перевірки стану, конфігурація та прогрес завдань.
  - name: Enterprise
    description: Адміністрування, відповідність, SCIM та інтеграційні API рівня enterprise, обмежені ліцензією.
components:
  securitySchemes:
    bearerAuth:
      type: http
      scheme: bearer
      description: Session token from login or API key (prefixed with si_)
  responses:
    RateLimited:
      description: Too many requests. The client has exceeded the rate limit.
      headers:
        Retry-After:
          schema:
            type: integer
          description: Seconds until the rate limit window resets
        X-RateLimit-Limit:
          schema:
            type: integer
          description: Maximum requests per time window
        X-RateLimit-Remaining:
          schema:
            type: integer
          description: Remaining requests in current window
      content:
        application/json:
          schema:
            type: object
            properties:
              error:
                type: string
                example: Rate limit exceeded
  schemas:
    Error:
      type: object
      required:
        - error
      properties:
        error:
          type: string
          example: Invalid image format
        details:
          type: string
          description: Additional error details (e.g. Zod validation errors)
    ToolResponse:
      type: object
      properties:
        jobId:
          type: string
          description: Unique job identifier
        downloadUrl:
          type: string
          description: URL to download the processed image
          example: /api/v1/download/abc123/output.png
        originalSize:
          type: integer
          description: Original file size in bytes
        processedSize:
          type: integer
          description: Processed file size in bytes
        previewUrl:
          type: string
          description: URL to a WebP preview (present when output is not browser-previewable, e.g. TIFF, HEIF)
        savedFileId:
          type: string
          description: ID of the auto-saved file in the library (present when fileId was provided in the request)
    UnauthorizedError:
      type: object
      properties:
        statusCode:
          type: integer
          example: 401
        error:
          type: string
          example: Unauthorized
        message:
          type: string
          example: Authentication required
    ForbiddenError:
      type: object
      properties:
        statusCode:
          type: integer
          example: 403
        error:
          type: string
          example: Forbidden
        message:
          type: string
          example: Insufficient permissions
    ConflictError:
      type: object
      properties:
        statusCode:
          type: integer
          example: 409
        error:
          type: string
          example: Conflict
        message:
          type: string
          example: Resource already exists
    FeatureNotInstalledError:
      type: object
      properties:
        error:
          type: string
          example: Feature not installed
        code:
          type: string
          enum:
            - FEATURE_NOT_INSTALLED
            - FEATURE_INCOMPATIBLE
          example: FEATURE_NOT_INSTALLED
        feature:
          type: string
          description: Feature bundle identifier
        featureName:
          type: string
          description: Human-readable feature name
        estimatedSize:
          type: string
          description: Estimated download size
        requestedQuality:
          type: string
          enum:
            - fast
            - balanced
            - best
          description: OCR tier that could not be honored; Fast is reported when Korean is explicitly requested with Fast or the legacy Tesseract alias
        compatibilityReason:
          type: string
          description: Machine-readable runtime compatibility reason, when available
        guidance:
          type: string
          description: Actionable recovery guidance, when available
    HealthResponse:
      type: object
      properties:
        status:
          type: string
          enum:
            - healthy
            - unhealthy
        version:
          type: string
    AdminHealthResponse:
      type: object
      properties:
        status:
          type: string
          enum:
            - healthy
            - degraded
        version:
          type: string
        uptime:
          type: string
        storage:
          type: object
          properties:
            mode:
              type: string
            available:
              type: string
        database:
          type: string
          enum:
            - ok
            - error
        queue:
          type: object
          properties:
            active:
              type: integer
            pending:
              type: integer
        ai:
          type: object
          properties:
            gpu:
              type: boolean
security:
  - bearerAuth: []
paths:
  /api/v1/health:
    get:
      operationId: healthCheck
      tags:
        - System
      summary: Перевірка стану
      description: Повертає стан справності сервера. Використовується Docker HEALTHCHECK. Публічний ендпоінт.
      security: []
      responses:
        '200':
          description: Server health
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/HealthResponse'
        '503':
          description: Database unavailable
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/HealthResponse'
  /api/v1/tools/image/resize:
    post:
      operationId: resizeImage
      tags:
        - Tools
      summary: Зміна розміру зображення
      description: Змінити розмір зображення до конкретних розмірів або у відсотках.
      security:
        - bearerAuth: []
      requestBody:
        required: true
        content:
          multipart/form-data:
            schema:
              type: object
              required:
                - file
              properties:
                file:
                  type: string
                  format: binary
                  description: Image file to process
                settings:
                  type: string
                  description: |
                    JSON string with options:
                    - `width` (number, optional) - Target width in pixels
                    - `height` (number, optional) - Target height in pixels
                    - `fit` (string, default "contain") - One of: contain, cover, fill, inside, outside
                    - `withoutEnlargement` (boolean, default false) - Prevent upscaling
                    - `percentage` (number, optional) - Scale by percentage instead of fixed dimensions
      responses:
        '200':
          description: Processed image
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ToolResponse'
        '400':
          description: Invalid input
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Error'
        '401':
          description: Authentication required
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/UnauthorizedError'
  /api/v1/tools/image/content-aware-resize:
    post:
      operationId: contentAwareResize
      tags:
        - Tools
      summary: Розумна зміна розміру
      description: Змінити розмір зображення за допомогою seam carving, розумно видаляючи або додаючи вміст зі збереженням важливих елементів.
      security:
        - bearerAuth: []
      requestBody:
        required: true
        content:
          multipart/form-data:
            schema:
              type: object
              required:
                - file
              properties:
                file:
                  type: string
                  format: binary
                  description: Image file to process
                settings:
                  type: string
                  description: |
                    JSON string with options:
                    - `width` (number, optional) - Target width in pixels
                    - `height` (number, optional) - Target height in pixels
                    - `protectFaces` (boolean, default false) - Detect and protect faces from distortion
                    - `blurRadius` (number 0-20, default 4) - Blur radius for energy map
                    - `sobelThreshold` (number 1-20, default 2) - Edge detection threshold
                    - `square` (boolean, default false) - Crop to square aspect ratio

                    At least one of `width`, `height`, or `square` must be provided.
      responses:
        '200':
          description: Processed image
          content:
            application/json:
              schema:
                allOf:
                  - $ref: '#/components/schemas/ToolResponse'
                  - type: object
                    properties:
                      width:
                        type: integer
                      height:
                        type: integer
        '400':
          description: Invalid input
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Error'
        '401':
          description: Authentication required
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/UnauthorizedError'
  /api/v1/tools/image/crop:
    post:
      operationId: cropImage
      tags:
        - Tools
      summary: Обрізка зображення
      description: Обрізати зображення до конкретної області.
      security:
        - bearerAuth: []
      requestBody:
        required: true
        content:
          multipart/form-data:
            schema:
              type: object
              required:
                - file
              properties:
                file:
                  type: string
                  format: binary
                  description: Image file to process
                settings:
                  type: string
                  description: |
                    JSON string with options:
                    - `left` (number, required) - Left offset in pixels (min 0)
                    - `top` (number, required) - Top offset in pixels (min 0)
                    - `width` (number, required) - Width of crop region in pixels
                    - `height` (number, required) - Height of crop region in pixels
                    - `unit` (string, optional) - One of: px, percent
      responses:
        '200':
          description: Processed image
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ToolResponse'
        '400':
          description: Invalid input
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Error'
        '401':
          description: Authentication required
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/UnauthorizedError'
  /api/v1/tools/image/rotate:
    post:
      operationId: rotateImage
      tags:
        - Tools
      summary: Обертання та відображення зображення
      description: Обернути зображення на кут або віддзеркалити горизонтально/вертикально.
      security:
        - bearerAuth: []
      requestBody:
        required: true
        content:
          multipart/form-data:
            schema:
              type: object
              required:
                - file
              properties:
                file:
                  type: string
                  format: binary
                  description: Image file to process
                settings:
                  type: string
                  description: |
                    JSON string with options:
                    - `angle` (number, default 0) - Rotation angle in degrees
                    - `horizontal` (boolean, default false) - Flip horizontally
                    - `vertical` (boolean, default false) - Flip vertically
      responses:
        '200':
          description: Processed image
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ToolResponse'
        '400':
          description: Invalid input
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Error'
        '401':
          description: Authentication required
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/UnauthorizedError'
  /api/v1/tools/image/convert:
    post:
      operationId: convertFormat
      tags:
        - Tools
      summary: Конвертація зображення
      description: Конвертувати зображення в інший формат.
      security:
        - bearerAuth: []
      requestBody:
        required: true
        content:
          multipart/form-data:
            schema:
              type: object
              required:
                - file
              properties:
                file:
                  type: string
                  format: binary
                  description: Image file to process
                settings:
                  type: string
                  description: |
                    JSON string with options:
                    - `format` (string, required) - One of: jpg, png, webp, avif, tiff, gif, heic, heif, jxl, bmp, ico, jp2, qoi, psd
                    - `quality` (number 1-100, optional) - Output quality
      responses:
        '200':
          description: Processed image
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ToolResponse'
        '400':
          description: Invalid input
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Error'
        '401':
          description: Authentication required
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/UnauthorizedError'
  /api/v1/tools/image/compress:
    post:
      operationId: compressImage
      tags:
        - Tools
      summary: Стиснення зображення
      description: Зменшити розмір файлу зображення за рівнем якості або цільовим розміром.
      security:
        - bearerAuth: []
      requestBody:
        required: true
        content:
          multipart/form-data:
            schema:
              type: object
              required:
                - file
              properties:
                file:
                  type: string
                  format: binary
                  description: Image file to process
                settings:
                  type: string
                  description: |
                    JSON string with options:
                    - `mode` (string, default "quality") - One of: quality, targetSize
                    - `quality` (number 1-100, optional) - Compression quality level
                    - `targetSizeKb` (number, optional) - Target file size in kilobytes
      responses:
        '200':
          description: Processed image
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ToolResponse'
        '400':
          description: Invalid input
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Error'
        '401':
          description: Authentication required
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/UnauthorizedError'
  /api/v1/tools/image/strip-metadata:
    post:
      operationId: stripMetadata
      tags:
        - Tools
      summary: Видалення метаданих зображення
      description: Видалити метадані EXIF, GPS, ICC або XMP із зображення.
      security:
        - bearerAuth: []
      requestBody:
        required: true
        content:
          multipart/form-data:
            schema:
              type: object
              required:
                - file
              properties:
                file:
                  type: string
                  format: binary
                  description: Image file to process
                settings:
                  type: string
                  description: |
                    JSON string with options:
                    - `stripExif` (boolean, default false) - Remove EXIF data
                    - `stripGps` (boolean, default false) - Remove GPS data
                    - `stripIcc` (boolean, default false) - Remove ICC profile
                    - `stripXmp` (boolean, default false) - Remove XMP data
                    - `stripAll` (boolean, default true) - Remove all metadata
      responses:
        '200':
          description: Processed image
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ToolResponse'
        '400':
          description: Invalid input
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Error'
        '401':
          description: Authentication required
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/UnauthorizedError'
  /api/v1/tools/image/border:
    post:
      operationId: addBorder
      tags:
        - Tools
      summary: Рамка та обрамлення
      description: Додати до зображення рамку, заокруглені кути, відступи або тінь.
      security:
        - bearerAuth: []
      requestBody:
        required: true
        content:
          multipart/form-data:
            schema:
              type: object
              required:
                - file
              properties:
                file:
                  type: string
                  format: binary
                  description: Image file to process
                settings:
                  type: string
                  description: |
                    JSON string with options:
                    - `borderWidth` (number 0-2000, default 10) - Border thickness in pixels
                    - `borderColor` (hex string, default "#000000") - Border color
                    - `padding` (number 0-200, default 0) - Inner padding in pixels
                    - `paddingColor` (hex string, default "#FFFFFF") - Padding fill color
                    - `cornerRadius` (number 0-2000, default 0) - Corner rounding radius
                    - `shadow` (boolean, default false) - Enable drop shadow
                    - `shadowBlur` (number 1-200, default 15) - Drop shadow blur radius
                    - `shadowOffsetX` (number -50 to 50, default 0) - Shadow horizontal offset
                    - `shadowOffsetY` (number -50 to 50, default 5) - Shadow vertical offset
                    - `shadowColor` (hex string, default "#000000") - Drop shadow color
                    - `shadowOpacity` (number 0-100, default 40) - Shadow opacity percentage
      responses:
        '200':
          description: Processed image
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ToolResponse'
        '400':
          description: Invalid input
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Error'
        '401':
          description: Authentication required
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/UnauthorizedError'
  /api/v1/tools/image/beautify:
    post:
      operationId: beautifyScreenshot
      tags:
        - Tools
      summary: Прикрасити скріншот
      description: |
        Додати до скріншотів градієнтні фони, рамки пристроїв, тіні, водяні знаки та
        розміри для соціальних мереж. Підтримує суцільні, лінійно-градієнтні, радіально-градієнтні,
        зображувані та прозорі фони. Містить рамки пристроїв macOS, Windows, браузера, iPhone,
        MacBook та iPad. Пресети соціальних мереж для Twitter, LinkedIn,
        Instagram, Facebook та Product Hunt.
      security:
        - bearerAuth: []
      requestBody:
        required: true
        content:
          multipart/form-data:
            schema:
              type: object
              required:
                - file
              properties:
                file:
                  type: string
                  format: binary
                  description: Screenshot or image file to beautify
                backgroundImage:
                  type: string
                  format: binary
                  description: Optional image for image backgrounds (used when backgroundType is "image")
                settings:
                  type: string
                  description: |
                    JSON string with options:
                    - `backgroundType` (string, default "linear-gradient") -- One of: solid, linear-gradient, radial-gradient, image, transparent
                    - `backgroundColor` (hex string, default "#667eea") -- Background color (for solid backgrounds)
                    - `gradientStops` (array, default [{color:"#667eea",position:0},{color:"#764ba2",position:100}]) -- Array of {color, position} objects for gradient backgrounds
                    - `gradientAngle` (number 0-360, default 135) -- Gradient angle in degrees
                    - `padding` (number 0-256, default 64) -- Space between screenshot and canvas edge in pixels
                    - `borderRadius` (number 0-64, default 12) -- Corner rounding radius for the screenshot
                    - `shadowPreset` (string, default "subtle") -- One of: none, subtle, medium, dramatic, custom
                    - `shadowBlur` (number 0-100, default 20) -- Shadow blur radius (used with custom shadow preset)
                    - `shadowOffsetX` (number -50 to 50, default 0) -- Shadow horizontal offset
                    - `shadowOffsetY` (number -50 to 50, default 10) -- Shadow vertical offset
                    - `shadowColor` (hex string, default "#000000") -- Shadow color
                    - `shadowOpacity` (number 0-100, default 30) -- Shadow opacity percentage
                    - `frame` (string, default "none") -- One of: none, macos-light, macos-dark, windows-light, windows-dark, browser-light, browser-dark, iphone, iphone-dark, macbook, macbook-dark, ipad, ipad-dark
                    - `frameTitle` (string, optional) -- Title text shown in window title bars
                    - `socialPreset` (string, default "none") -- One of: none, twitter, linkedin, instagram-square, instagram-story, facebook, producthunt
                    - `watermarkText` (string, optional) -- Watermark text to overlay
                    - `watermarkPosition` (string, default "bottom-right") -- One of: top-left, top-right, bottom-left, bottom-right, center
                    - `watermarkOpacity` (number 0-100, default 50) -- Watermark opacity percentage
                    - `outputFormat` (string, default "png") -- One of: png, jpeg, webp
      responses:
        '200':
          description: Beautified image
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ToolResponse'
        '400':
          description: Invalid input
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Error'
        '401':
          description: Authentication required
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/UnauthorizedError'
  /api/v1/tools/image/adjust-colors:
    post:
      operationId: adjustColors
      tags:
        - Tools
      summary: Корекція кольорів
      description: |
        Об'єднаний інструмент корекції кольорів. Регулює яскравість, контраст, експозицію, насиченість, температуру, відтінок, тон, різкість, кольорові канали та ефекти за один прохід.
      security:
        - bearerAuth: []
      requestBody:
        required: true
        content:
          multipart/form-data:
            schema:
              type: object
              required:
                - file
              properties:
                file:
                  type: string
                  format: binary
                  description: Image file to process
                settings:
                  type: string
                  description: |
                    JSON string with options:
                    - `brightness` (number -100 to 100, default 0) - Brightness adjustment
                    - `contrast` (number -100 to 100, default 0) - Contrast adjustment
                    - `exposure` (number -100 to 100, default 0) - Exposure adjustment (gamma)
                    - `saturation` (number -100 to 100, default 0) - Color saturation
                    - `temperature` (number -100 to 100, default 0) - Color temperature (cool to warm)
                    - `tint` (number -100 to 100, default 0) - Tint shift (green to magenta)
                    - `hue` (number -180 to 180, default 0) - Hue rotation in degrees
                    - `sharpness` (number 0 to 100, default 0) - Sharpness enhancement
                    - `red` (number 0-200, default 100) - Red channel multiplier
                    - `green` (number 0-200, default 100) - Green channel multiplier
                    - `blue` (number 0-200, default 100) - Blue channel multiplier
                    - `effect` (string, default "none") - One of: none, grayscale, sepia, invert
      responses:
        '200':
          description: Processed image
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ToolResponse'
        '400':
          description: Invalid input
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Error'
        '401':
          description: Authentication required
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/UnauthorizedError'
  /api/v1/tools/image/sharpening:
    post:
      operationId: sharpenImage
      tags:
        - Tools
      summary: Підвищення різкості зображення
      description: Підвищити різкість зображення методами адаптивного, unsharp mask або high-pass із додатковим зменшенням шуму.
      security:
        - bearerAuth: []
      requestBody:
        required: true
        content:
          multipart/form-data:
            schema:
              type: object
              required:
                - file
              properties:
                file:
                  type: string
                  format: binary
                  description: Image file to process
                settings:
                  type: string
                  description: |
                    JSON string with options:
                    - `method` (string, default "adaptive") - One of: adaptive, unsharp-mask, high-pass
                    - `sigma` (number 0.5-10, default 1.0) - Gaussian sigma (adaptive)
                    - `m1` (number 0-10, default 1.0) - Flat area sharpening (adaptive)
                    - `m2` (number 0-20, default 3.0) - Jagged area sharpening (adaptive)
                    - `x1` (number 0-10, default 2.0) - Flat/jagged threshold (adaptive)
                    - `y2` (number 0-50, default 12) - Maximum brightening (adaptive)
                    - `y3` (number 0-50, default 20) - Maximum darkening (adaptive)
                    - `amount` (number 0-1000, default 100) - Sharpen amount (unsharp-mask)
                    - `radius` (number 0.1-5, default 1.0) - Blur radius (unsharp-mask)
                    - `threshold` (number 0-255, default 0) - Luminance threshold (unsharp-mask)
                    - `strength` (number 0-100, default 50) - Blend strength (high-pass)
                    - `kernelSize` (integer, default 3) - One of: 3, 5 (high-pass)
                    - `denoise` (string, default "off") - One of: off, light, medium, strong
      responses:
        '200':
          description: Processed image
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ToolResponse'
        '400':
          description: Invalid input
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Error'
        '401':
          description: Authentication required
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/UnauthorizedError'
  /api/v1/tools/image/optimize-for-web:
    post:
      operationId: optimizeForWeb
      tags:
        - Tools
      summary: Оптимізувати для вебу
      description: Оптимізувати зображення для доставки в мережі шляхом конвертації в сучасні формати, коригування якості та за потреби зміни розміру.
      security:
        - bearerAuth: []
      requestBody:
        required: true
        content:
          multipart/form-data:
            schema:
              type: object
              required:
                - file
              properties:
                file:
                  type: string
                  format: binary
                  description: Image file to process
                settings:
                  type: string
                  description: |
                    JSON string with options:
                    - `format` (string, default "webp") - One of: webp, jpeg, avif, png, jxl
                    - `quality` (number 1-100, default 80) - Output quality
                    - `maxWidth` (number, optional) - Maximum width in pixels
                    - `maxHeight` (number, optional) - Maximum height in pixels
                    - `progressive` (boolean, default true) - Enable progressive encoding
                    - `stripMetadata` (boolean, default true) - Remove metadata
      responses:
        '200':
          description: Processed image
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ToolResponse'
        '400':
          description: Invalid input
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Error'
        '401':
          description: Authentication required
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/UnauthorizedError'
  /api/v1/tools/image/optimize-for-web/preview:
    post:
      operationId: optimizeForWebPreview
      tags:
        - Tools
      summary: Оптимізувати для вебу (попередній перегляд)
      description: |
        Легкий ендпоінт попереднього перегляду для живого налаштування параметрів. Повертає
        оптимізоване зображення у двійковому вигляді напряму (не JSON). Інформація про розмір
        міститься в заголовках відповіді. Не створює робочий простір і не зберігає результати.
      security:
        - bearerAuth: []
      requestBody:
        required: true
        content:
          multipart/form-data:
            schema:
              type: object
              required:
                - file
              properties:
                file:
                  type: string
                  format: binary
                  description: Image file to preview
                settings:
                  type: string
                  description: Same settings as the main optimize-for-web endpoint
      responses:
        '200':
          description: Optimized image binary
          headers:
            X-Original-Size:
              schema:
                type: string
              description: Original file size in bytes
            X-Processed-Size:
              schema:
                type: string
              description: Processed file size in bytes
            X-Output-Filename:
              schema:
                type: string
              description: Suggested output filename
          content:
            image/*:
              schema:
                type: string
                format: binary
        '400':
          description: Invalid input
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Error'
        '422':
          description: Processing failed
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Error'
  /api/v1/tools/image/image-enhancement:
    post:
      operationId: enhanceImage
      tags:
        - Tools
      summary: Покращення зображення
      description: |
        Автоматичне покращення зображення на основі AI. Аналізує зображення та застосовує
        корекції експозиції, контрасту, балансу білого, насиченості, різкості
        та шуму залежно від обраного режиму сцени.
      security:
        - bearerAuth: []
      requestBody:
        required: true
        content:
          multipart/form-data:
            schema:
              type: object
              required:
                - file
              properties:
                file:
                  type: string
                  format: binary
                  description: Image file to process
                settings:
                  type: string
                  description: |
                    JSON string with options:
                    - `mode` (string, default "auto") - One of: auto, portrait, landscape, low-light, food, document
                    - `intensity` (number 0-100, default 50) - Overall enhancement intensity
                    - `corrections` (object, optional) - Toggle individual corrections:
                      - `exposure` (boolean, default true)
                      - `contrast` (boolean, default true)
                      - `whiteBalance` (boolean, default true)
                      - `saturation` (boolean, default true)
                      - `sharpness` (boolean, default true)
                      - `denoise` (boolean, default true)
                    - `deepEnhance` (boolean, default false) - Use AI noise removal for deeper enhancement (requires noise-removal feature)
      responses:
        '200':
          description: Processed image
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ToolResponse'
        '400':
          description: Invalid input
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Error'
        '401':
          description: Authentication required
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/UnauthorizedError'
  /api/v1/tools/image/image-enhancement/analyze:
    post:
      operationId: analyzeImageEnhancement
      tags:
        - Tools
      summary: Аналіз зображення для покращення
      description: |
        Проаналізувати зображення та повернути пропоновані корекції без їх застосування.
        Корисно для попереднього перегляду того, які покращення буде зроблено.
      security:
        - bearerAuth: []
      requestBody:
        required: true
        content:
          multipart/form-data:
            schema:
              type: object
              required:
                - file
              properties:
                file:
                  type: string
                  format: binary
                  description: Image file to analyze
      responses:
        '200':
          description: Image analysis with suggested corrections
          content:
            application/json:
              schema:
                type: object
                properties:
                  corrections:
                    type: object
                    description: Suggested correction values
                  metrics:
                    type: object
                    description: Image quality metrics
        '400':
          description: Invalid input
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Error'
        '422':
          description: Analysis failed
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Error'
  /api/v1/tools/image/noise-removal:
    post:
      operationId: removeNoise
      tags:
        - Tools
      summary: Видалення шуму
      description: |
        Видалення шуму на основі AI через Python sidecar. Підтримує кілька
        рівнів якості від швидкого перегляду до максимальної якості. Потребує встановленого
        пакета функцій noise-removal.
      security:
        - bearerAuth: []
      requestBody:
        required: true
        content:
          multipart/form-data:
            schema:
              type: object
              required:
                - file
              properties:
                file:
                  type: string
                  format: binary
                  description: Image file to process
                settings:
                  type: string
                  description: |
                    JSON string with options:
                    - `tier` (string, default "balanced") - One of: quick, balanced, quality, maximum
                    - `strength` (number, default 50) - Denoising strength
                    - `detailPreservation` (number, default 50) - Detail preservation level
                    - `colorNoise` (number, default 30) - Color noise reduction
                    - `format` (string, default "original") - One of: original, png, jpeg, webp, avif, jxl
                    - `quality` (number, default 90) - Output quality (1-100)
                clientJobId:
                  type: string
                  description: Client-provided job ID for SSE progress tracking
      responses:
        '202':
          description: Accepted for async processing. Track progress via SSE at /api/v1/jobs/{jobId}/progress.
          content:
            application/json:
              schema:
                type: object
                properties:
                  jobId:
                    type: string
                  async:
                    type: boolean
                    example: true
        '400':
          description: Invalid input
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Error'
        '401':
          description: Authentication required
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/UnauthorizedError'
        '422':
          description: Processing failed
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Error'
        '501':
          description: Feature not installed
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/FeatureNotInstalledError'
  /api/v1/tools/image/red-eye-removal:
    post:
      operationId: removeRedEye
      tags:
        - Tools
      summary: Видалення ефекту червоних очей
      description: |
        Виявлення та видалення ефекту червоних очей на основі AI. Виявляє обличчя та виправляє
        артефакти червоних очей на фотографіях. Потребує встановленого пакета функцій
        red-eye-removal.
      security:
        - bearerAuth: []
      requestBody:
        required: true
        content:
          multipart/form-data:
            schema:
              type: object
              required:
                - file
              properties:
                file:
                  type: string
                  format: binary
                  description: Image file to process
                settings:
                  type: string
                  description: |
                    JSON string with options:
                    - `sensitivity` (number 0-100, default 50) - Detection sensitivity
                    - `strength` (number 0-100, default 70) - Correction strength
                    - `format` (string, optional) - Output format
                    - `quality` (number 1-100, default 90) - Output quality
                clientJobId:
                  type: string
                  description: Client-provided job ID for SSE progress tracking
      responses:
        '202':
          description: Accepted for async processing. Track progress via SSE at /api/v1/jobs/{jobId}/progress.
          content:
            application/json:
              schema:
                type: object
                properties:
                  jobId:
                    type: string
                  async:
                    type: boolean
                    example: true
        '400':
          description: Invalid input
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Error'
        '401':
          description: Authentication required
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/UnauthorizedError'
        '422':
          description: Processing failed
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Error'
        '501':
          description: Feature not installed
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/FeatureNotInstalledError'
  /api/v1/tools/image/restore-photo:
    post:
      operationId: restorePhoto
      tags:
        - Tools
      summary: Реставрація фото
      description: |
        Конвеєр реставрації фото на основі AI. Усуває подряпини, покращує обличчя,
        зменшує шум і за потреби розфарбовує старі чи пошкоджені фотографії. Потребує
        встановленого пакета функцій photo-restoration.
      security:
        - bearerAuth: []
      requestBody:
        required: true
        content:
          multipart/form-data:
            schema:
              type: object
              required:
                - file
              properties:
                file:
                  type: string
                  format: binary
                  description: Image file to process
                settings:
                  type: string
                  description: |
                    JSON string with options:
                    - `scratchRemoval` (boolean, default true) - Enable scratch repair
                    - `faceEnhancement` (boolean, default true) - Enhance detected faces
                    - `fidelity` (number 0-1, default 0.7) - Face enhancement fidelity
                    - `denoise` (boolean, default true) - Enable denoising
                    - `denoiseStrength` (number 0-100, default 25) - Denoising strength
                    - `colorize` (boolean, default false) - Colorize grayscale photos
                    - `colorizeStrength` (number 0-100, default 85) - Colorization strength
                clientJobId:
                  type: string
                  description: Client-provided job ID for SSE progress tracking
      responses:
        '202':
          description: Accepted for async processing. Track progress via SSE at /api/v1/jobs/{jobId}/progress.
          content:
            application/json:
              schema:
                type: object
                properties:
                  jobId:
                    type: string
                  async:
                    type: boolean
                    example: true
        '400':
          description: Invalid input
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Error'
        '401':
          description: Authentication required
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/UnauthorizedError'
        '422':
          description: Processing failed
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Error'
        '501':
          description: Feature not installed
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/FeatureNotInstalledError'
  /api/v1/tools/image/passport-photo/analyze:
    post:
      operationId: analyzePassportPhoto
      tags:
        - Tools
      summary: Фото на паспорт - аналіз
      description: |
        Фаза 1 створення фото на паспорт. Виявляє орієнтири обличчя та видаляє
        фон. Повертає дані орієнтирів і попередній перегляд у форматі base64 для
        використання клієнтом на фазі створення. Потребує встановленого пакета функцій
        passport-photo.
      security:
        - bearerAuth: []
      requestBody:
        required: true
        content:
          multipart/form-data:
            schema:
              type: object
              required:
                - file
              properties:
                file:
                  type: string
                  format: binary
                  description: Photo with a clear, front-facing face
                clientJobId:
                  type: string
                  description: Client-provided job ID for SSE progress tracking
      responses:
        '200':
          description: Face analysis and background removal result
          content:
            application/json:
              schema:
                type: object
                properties:
                  jobId:
                    type: string
                    description: Job ID to pass to the generate endpoint
                  filename:
                    type: string
                  preview:
                    type: string
                    description: Base64-encoded PNG preview of the background-removed image
                  previewWidth:
                    type: integer
                  previewHeight:
                    type: integer
                  landmarks:
                    type: object
                    description: Normalized face landmark coordinates (0-1)
                    properties:
                      leftEye:
                        type: object
                        properties:
                          x:
                            type: number
                          'y':
                            type: number
                      rightEye:
                        type: object
                        properties:
                          x:
                            type: number
                          'y':
                            type: number
                      eyeCenter:
                        type: object
                        properties:
                          x:
                            type: number
                          'y':
                            type: number
                      chin:
                        type: object
                        properties:
                          x:
                            type: number
                          'y':
                            type: number
                      forehead:
                        type: object
                        properties:
                          x:
                            type: number
                          'y':
                            type: number
                      crown:
                        type: object
                        properties:
                          x:
                            type: number
                          'y':
                            type: number
                      nose:
                        type: object
                        properties:
                          x:
                            type: number
                          'y':
                            type: number
                      faceCenterX:
                        type: number
                  imageWidth:
                    type: integer
                  imageHeight:
                    type: integer
        '400':
          description: Invalid input
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Error'
        '401':
          description: Authentication required
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/UnauthorizedError'
        '422':
          description: No face detected or analysis failed
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Error'
        '501':
          description: Feature not installed
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/FeatureNotInstalledError'
  /api/v1/tools/image/passport-photo/generate:
    post:
      operationId: generatePassportPhoto
      tags:
        - Tools
      summary: Фото на паспорт - створення
      description: |
        Фаза 2 створення фото на паспорт. Використовує jobId та орієнтири з
        фази аналізу для обрізання, зміни розміру та розкладки фото за вказаною
        специфікацією країни/документа. Швидка відповідь (без повторного запуску AI).
      security:
        - bearerAuth: []
      requestBody:
        required: true
        content:
          application/json:
            schema:
              type: object
              required:
                - jobId
                - filename
                - countryCode
                - landmarks
                - imageWidth
                - imageHeight
              properties:
                jobId:
                  type: string
                  description: Job ID from the analyze phase
                filename:
                  type: string
                  description: Original filename from analyze phase
                countryCode:
                  type: string
                  description: ISO country code for passport specification
                documentType:
                  type: string
                  default: passport
                  description: Document type (passport, visa, etc.)
                bgColor:
                  type: string
                  default: '#FFFFFF'
                  description: Background color (hex)
                printLayout:
                  type: string
                  default: none
                  description: Print sheet layout (none, 4x6, A4, letter)
                maxFileSizeKb:
                  type: number
                  default: 0
                  description: Max file size in KB (0 = no limit)
                dpi:
                  type: number
                  minimum: 72
                  maximum: 1200
                  default: 300
                  description: Output DPI
                customWidthMm:
                  type: number
                  description: Custom photo width in mm (overrides country spec)
                customHeightMm:
                  type: number
                  description: Custom photo height in mm (overrides country spec)
                zoom:
                  type: number
                  minimum: 0.5
                  maximum: 3
                  default: 1
                  description: Zoom level (>1 = tighter crop)
                adjustX:
                  type: number
                  default: 0
                  description: Horizontal position adjustment
                adjustY:
                  type: number
                  default: 0
                  description: Vertical position adjustment
                landmarks:
                  type: object
                  description: Face landmarks from analyze phase
                imageWidth:
                  type: integer
                  description: Original image width from analyze phase
                imageHeight:
                  type: integer
                  description: Original image height from analyze phase
      responses:
        '200':
          description: Generated passport photo
          content:
            application/json:
              schema:
                type: object
                properties:
                  jobId:
                    type: string
                  downloadUrl:
                    type: string
                  dimensions:
                    type: object
                    properties:
                      widthMm:
                        type: number
                      heightMm:
                        type: number
                      widthPx:
                        type: integer
                      heightPx:
                        type: integer
                      dpi:
                        type: integer
                  spec:
                    type: object
                    properties:
                      country:
                        type: string
                      countryCode:
                        type: string
                      documentType:
                        type: string
                      documentLabel:
                        type: string
                  printDownloadUrl:
                    type: string
                    description: URL for print sheet download (if printLayout was specified)
        '400':
          description: Invalid settings or unknown country code
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Error'
        '401':
          description: Authentication required
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/UnauthorizedError'
        '422':
          description: Generation failed
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Error'
  /api/v1/tools/image/colorize:
    post:
      operationId: colorizeImage
      tags:
        - Tools
      summary: Розфарбувати
      description: |
        Розфарбовування фото на основі AI. Перетворює чорно-білі або відтінкові сірого
        фотографії на повнокольорові за допомогою DDColor із резервним варіантом OpenCV DNN. Потребує
        встановленого пакета функцій colorize.
      security:
        - bearerAuth: []
      requestBody:
        required: true
        content:
          multipart/form-data:
            schema:
              type: object
              required:
                - file
              properties:
                file:
                  type: string
                  format: binary
                  description: Grayscale or B&W image file
                settings:
                  type: string
                  description: |
                    JSON string with options:
                    - `intensity` (number 0-1, default 1.0) - Colorization intensity
                    - `model` (string, default "auto") - One of: auto, ddcolor, opencv
                clientJobId:
                  type: string
                  description: Client-provided job ID for SSE progress tracking
      responses:
        '202':
          description: Accepted for async processing. Track progress via SSE at /api/v1/jobs/{jobId}/progress.
          content:
            application/json:
              schema:
                type: object
                properties:
                  jobId:
                    type: string
                  async:
                    type: boolean
                    example: true
        '400':
          description: Invalid input
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Error'
        '401':
          description: Authentication required
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/UnauthorizedError'
        '422':
          description: Processing failed
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Error'
        '501':
          description: Feature not installed
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/FeatureNotInstalledError'
  /api/v1/tools/image/enhance-faces:
    post:
      operationId: enhanceFaces
      tags:
        - Tools
      summary: Покращення облич
      description: |
        Покращення облич на основі AI за допомогою GFPGAN або CodeFormer. Виявляє обличчя
        на зображенні та покращує деталі обличчя, текстуру шкіри та чіткість.
        Потребує встановленого пакета функцій enhance-faces.
      security:
        - bearerAuth: []
      requestBody:
        required: true
        content:
          multipart/form-data:
            schema:
              type: object
              required:
                - file
              properties:
                file:
                  type: string
                  format: binary
                  description: Image file containing faces
                settings:
                  type: string
                  description: |
                    JSON string with options:
                    - `model` (string, default "auto") - One of: auto, gfpgan, codeformer
                    - `strength` (number 0-1, default 0.8) - Enhancement strength
                    - `onlyCenterFace` (boolean, default false) - Only enhance the center face
                    - `sensitivity` (number 0-1, default 0.5) - Face detection sensitivity
                clientJobId:
                  type: string
                  description: Client-provided job ID for SSE progress tracking
      responses:
        '202':
          description: Accepted for async processing. Track progress via SSE at /api/v1/jobs/{jobId}/progress.
          content:
            application/json:
              schema:
                type: object
                properties:
                  jobId:
                    type: string
                  async:
                    type: boolean
                    example: true
        '400':
          description: Invalid input
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Error'
        '401':
          description: Authentication required
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/UnauthorizedError'
        '422':
          description: Processing failed
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Error'
        '501':
          description: Feature not installed
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/FeatureNotInstalledError'
  /api/v1/tools/image/image-to-base64:
    post:
      operationId: imageToBase64
      tags:
        - Tools
      summary: Зображення в Base64
      description: |
        Конвертувати одне або кілька зображень у рядки в кодуванні Base64 з data URI.
        Підтримує додаткову конвертацію формату та зміну розміру. Повертає як необроблений
        рядок Base64, так і готовий до використання data URI для кожного файлу.
      security:
        - bearerAuth: []
      requestBody:
        required: true
        content:
          multipart/form-data:
            schema:
              type: object
              required:
                - file
              properties:
                file:
                  type: array
                  items:
                    type: string
                    format: binary
                  description: One or more image files to convert
                settings:
                  type: string
                  description: |
                    JSON string with options:
                    - `outputFormat` (string, default "original") - One of: original, jpeg, png, webp, avif, jxl
                    - `quality` (integer 1-100, default 80) - Output quality for lossy formats
                    - `maxWidth` (integer, default 0) - Max width in pixels (0 = no limit)
                    - `maxHeight` (integer, default 0) - Max height in pixels (0 = no limit)
      responses:
        '200':
          description: Base64-encoded results
          content:
            application/json:
              schema:
                type: object
                properties:
                  results:
                    type: array
                    items:
                      type: object
                      properties:
                        filename:
                          type: string
                        mimeType:
                          type: string
                        width:
                          type: integer
                        height:
                          type: integer
                        originalSize:
                          type: integer
                          description: Original file size in bytes
                        encodedSize:
                          type: integer
                          description: Base64 string size in bytes
                        overheadPercent:
                          type: number
                          description: Size overhead of Base64 encoding
                        base64:
                          type: string
                          description: Raw Base64-encoded image data
                        dataUri:
                          type: string
                          description: Complete data URI (data:mime;base64,...)
                  errors:
                    type: array
                    items:
                      type: object
                      properties:
                        filename:
                          type: string
                        error:
                          type: string
        '400':
          description: Invalid input
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Error'
        '401':
          description: Authentication required
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/UnauthorizedError'
  /api/v1/tools/image/watermark-text:
    post:
      operationId: addTextWatermark
      tags:
        - Tools
      summary: Текстовий водяний знак
      description: Додати текстовий водяний знак до зображення.
      security:
        - bearerAuth: []
      requestBody:
        required: true
        content:
          multipart/form-data:
            schema:
              type: object
              required:
                - file
              properties:
                file:
                  type: string
                  format: binary
                  description: Image file to process
                settings:
                  type: string
                  description: |
                    JSON string with options:
                    - `text` (string 1-500, required) - Watermark text
                    - `fontSize` (number 8-1000, default 48) - Font size in pixels
                    - `color` (hex string, default "#000000") - Text color
                    - `opacity` (number 0-100, default 50) - Opacity percentage
                    - `position` (string, default "center") - One of: center, top-left, top-right, bottom-left, bottom-right, tiled
                    - `rotation` (number -360 to 360, default 0) - Text rotation angle
      responses:
        '200':
          description: Processed image
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ToolResponse'
        '400':
          description: Invalid input
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Error'
        '401':
          description: Authentication required
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/UnauthorizedError'
  /api/v1/tools/image/text-overlay:
    post:
      operationId: addTextOverlay
      tags:
        - Tools
      summary: Накладання тексту
      description: Додати стилізоване накладання тексту з додатковим фоновим блоком.
      security:
        - bearerAuth: []
      requestBody:
        required: true
        content:
          multipart/form-data:
            schema:
              type: object
              required:
                - file
              properties:
                file:
                  type: string
                  format: binary
                  description: Image file to process
                settings:
                  type: string
                  description: |
                    JSON string with options:
                    - `text` (string 1-500, required) - Overlay text
                    - `fontSize` (number 8-200, default 48) - Font size in pixels
                    - `color` (hex string, default "#FFFFFF") - Text color
                    - `position` (string, default "bottom") - One of: top, center, bottom
                    - `backgroundBox` (boolean, default false) - Show background box behind text
                    - `backgroundColor` (hex string, default "#000000") - Background box color
                    - `shadow` (boolean, default true) - Add text shadow
      responses:
        '200':
          description: Processed image
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ToolResponse'
        '400':
          description: Invalid input
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Error'
        '401':
          description: Authentication required
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/UnauthorizedError'
  /api/v1/tools/image/replace-color:
    post:
      operationId: replaceColor
      tags:
        - Tools
      summary: Замінити колір
      description: Замінити конкретний колір у зображенні іншим кольором або прозорістю.
      security:
        - bearerAuth: []
      requestBody:
        required: true
        content:
          multipart/form-data:
            schema:
              type: object
              required:
                - file
              properties:
                file:
                  type: string
                  format: binary
                  description: Image file to process
                settings:
                  type: string
                  description: |
                    JSON string with options:
                    - `sourceColor` (hex string, default "#FF0000") - Color to replace
                    - `targetColor` (hex string, default "#00FF00") - Replacement color
                    - `makeTransparent` (boolean, default false) - Make matched pixels transparent instead
                    - `tolerance` (number 0-255, default 30) - Color matching tolerance
      responses:
        '200':
          description: Processed image
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ToolResponse'
        '400':
          description: Invalid input
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Error'
        '401':
          description: Authentication required
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/UnauthorizedError'
  /api/v1/tools/image/color-blindness:
    post:
      operationId: simulateColorBlindness
      tags:
        - Tools
      summary: Симуляція дальтонізму
      description: Симулювати, як зображення виглядає для людей із різними типами порушення колірного зору.
      security:
        - bearerAuth: []
      requestBody:
        required: true
        content:
          multipart/form-data:
            schema:
              type: object
              required:
                - file
              properties:
                file:
                  type: string
                  format: binary
                  description: Image file to process
                settings:
                  type: string
                  description: |
                    JSON string with options:
                    - `simulationType` (enum, default "deuteranomaly") -- Type of color vision deficiency to simulate. One of: protanopia, deuteranopia, tritanopia, protanomaly, deuteranomaly, tritanomaly, achromatopsia, blueConeMonochromacy
      responses:
        '200':
          description: Processed image
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ToolResponse'
        '400':
          description: Invalid input
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Error'
        '401':
          description: Authentication required
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/UnauthorizedError'
  /api/v1/tools/image/gif-tools:
    post:
      operationId: processGif
      tags:
        - Tools
      summary: Інструменти GIF
      description: Змінити розмір, оптимізувати, скоригувати швидкість, реверсувати, витягти кадри або обернути анімовані GIF.
      security:
        - bearerAuth: []
      requestBody:
        required: true
        content:
          multipart/form-data:
            schema:
              type: object
              required:
                - file
              properties:
                file:
                  type: string
                  format: binary
                  description: GIF file to process
                settings:
                  type: string
                  description: |
                    JSON string with options:
                    - `mode` (string, default "resize") - One of: resize, optimize, speed, reverse, extract, rotate
                    - `width` (number 1-16384, optional) - Target width in pixels (resize mode)
                    - `height` (number 1-16384, optional) - Target height in pixels (resize mode)
                    - `percentage` (number 1-500, optional) - Scale by percentage (resize mode)
                    - `colors` (number 2-256, default 256) - Color palette size (optimize mode)
                    - `dither` (number 0-1, default 1.0) - Dither amount (optimize mode)
                    - `effort` (number 1-10, default 7) - Compression effort (optimize mode)
                    - `speedFactor` (number 0.1-10, default 1.0) - Speed multiplier (speed mode)
                    - `extractMode` (string, default "single") - One of: single, range, all (extract mode)
                    - `frameNumber` (number, default 0) - Frame index to extract (extract/single mode)
                    - `frameStart` (number, default 0) - Start frame index (extract/range mode)
                    - `frameEnd` (number, optional) - End frame index (extract/range mode)
                    - `extractFormat` (string, default "png") - One of: png, webp (extract mode)
                    - `angle` (number, optional) - Rotation angle, one of: 90, 180, 270 (rotate mode)
                    - `flipH` (boolean, default false) - Flip horizontally (rotate mode)
                    - `flipV` (boolean, default false) - Flip vertically (rotate mode)
                    - `loop` (number 0-100, default 0) - Loop count, 0 = infinite
      responses:
        '200':
          description: Processed image
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ToolResponse'
        '400':
          description: Invalid input
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Error'
        '401':
          description: Authentication required
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/UnauthorizedError'
  /api/v1/tools/image/gif-tools/info:
    post:
      operationId: getGifInfo
      tags:
        - Tools
      summary: Інформація про GIF
      description: Отримати метадані про анімований GIF, зокрема розміри, кількість кадрів, затримки та тривалість.
      security: []
      requestBody:
        required: true
        content:
          multipart/form-data:
            schema:
              type: object
              required:
                - file
              properties:
                file:
                  type: string
                  format: binary
                  description: GIF file to inspect
      responses:
        '200':
          description: GIF metadata
          content:
            application/json:
              schema:
                type: object
                properties:
                  width:
                    type: integer
                  height:
                    type: integer
                  pages:
                    type: integer
                    description: Number of frames
                  delay:
                    type: array
                    items:
                      type: integer
                    description: Per-frame delay in milliseconds
                  loop:
                    type: integer
                    description: Loop count (0 = infinite)
                  fileSize:
                    type: integer
                  duration:
                    type: integer
                    description: Total duration in milliseconds
        '400':
          description: Invalid input
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Error'
  /api/v1/tools/image/smart-crop:
    post:
      operationId: smartCrop
      tags:
        - Tools
      summary: Розумне обрізання
      description: 'Розумне обрізання з трьома режимами: фокус на об''єкті, фокус на обличчі або автообрізання.'
      security:
        - bearerAuth: []
      requestBody:
        required: true
        content:
          multipart/form-data:
            schema:
              type: object
              required:
                - file
              properties:
                file:
                  type: string
                  format: binary
                  description: Image file to process
                settings:
                  type: string
                  description: |
                    JSON string with options:
                    - `mode` (string, default "subject") - One of: subject, face, trim, attention (alias for subject), content (alias for trim)
                    - `strategy` (string, default "attention") - One of: attention, entropy (subject mode)
                    - `width` (integer, optional) - Target width in pixels
                    - `height` (integer, optional) - Target height in pixels
                    - `padding` (integer 0-50, default 0) - Padding percentage around focus area
                    - `facePreset` (string, default "head-shoulders") - One of: closeup, head-shoulders, upper-body, half-body (face mode)
                    - `sensitivity` (number 0-1, default 0.5) - Face detection sensitivity (face mode)
                    - `threshold` (integer 0-255, default 30) - Trim tolerance (trim mode)
                    - `padToSquare` (boolean, default false) - Pad to square after trimming (trim mode)
                    - `padColor` (string, default "#ffffff") - Hex color for padding (trim mode)
                    - `targetSize` (integer, optional) - Target size for padded output (trim mode)
                    - `quality` (integer 1-100, optional) - Output quality
      responses:
        '200':
          description: Processed image
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ToolResponse'
        '400':
          description: Invalid input
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Error'
        '401':
          description: Authentication required
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/UnauthorizedError'
  /api/v1/tools/image/vectorize:
    post:
      operationId: vectorizeImage
      tags:
        - Tools
      summary: Зображення в SVG
      description: Конвертувати растрове зображення у векторний формат SVG за допомогою potrace (Ч/Б) або VTracer (кольоровий).
      security:
        - bearerAuth: []
      requestBody:
        required: true
        content:
          multipart/form-data:
            schema:
              type: object
              required:
                - file
              properties:
                file:
                  type: string
                  format: binary
                  description: Image file to process
                settings:
                  type: string
                  description: |
                    JSON string with options:
                    - `colorMode` (string, default "bw") - One of: bw, color
                    - `threshold` (number 0-255, default 128) - B&W binarization threshold
                    - `colorPrecision` (number 1-16, default 6) - Color bits per channel
                    - `layerDifference` (number 1-128, default 6) - Color gradient step
                    - `filterSpeckle` (number 1-256, default 4) - Noise filter size
                    - `pathMode` (string, default "spline") - One of: none, polygon, spline
                    - `cornerThreshold` (number 0-180, default 60) - Corner detection angle
                    - `invert` (boolean, default false) - Invert colors before tracing
      responses:
        '200':
          description: Processed image (downloadUrl points to .svg file)
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ToolResponse'
        '400':
          description: Invalid input
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Error'
        '401':
          description: Authentication required
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/UnauthorizedError'
  /api/v1/tools/image/svg-to-raster:
    post:
      operationId: svgToRaster
      tags:
        - Tools
      summary: SVG у растр
      description: Конвертувати файл SVG у растровий формат зображення з довільним масштабом і DPI.
      security:
        - bearerAuth: []
      requestBody:
        required: true
        content:
          multipart/form-data:
            schema:
              type: object
              required:
                - file
              properties:
                file:
                  type: string
                  format: binary
                  description: SVG file to convert
                settings:
                  type: string
                  description: |
                    JSON string with options:
                    - `width` (number 1-65536, optional) - Output width in pixels
                    - `height` (number 1-65536, optional) - Output height in pixels
                    - `dpi` (number 36-2400, default 300) - Render density for SVG rasterization
                    - `quality` (number 1-100, default 90) - Output quality for lossy formats
                    - `backgroundColor` (hex string, default "#00000000") - Background color
                    - `outputFormat` (string, default "png") - One of: png, jpg, webp, avif, tiff, gif, heif, jxl
      responses:
        '200':
          description: Processed image
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ToolResponse'
        '400':
          description: Invalid input
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Error'
        '401':
          description: Authentication required
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/UnauthorizedError'
  /api/v1/tools/image/svg-to-raster/batch:
    post:
      operationId: svgToRasterBatch
      tags:
        - Tools
      summary: SVG у растр (пакетно)
      description: Конвертувати кілька файлів SVG у растрові зображення. Повертає ZIP-архів.
      security:
        - bearerAuth: []
      requestBody:
        required: true
        content:
          multipart/form-data:
            schema:
              type: object
              required:
                - file
              properties:
                file:
                  type: array
                  items:
                    type: string
                    format: binary
                  description: SVG files to convert
                settings:
                  type: string
                  description: Same settings as single-file endpoint
                clientJobId:
                  type: string
                  description: Optional client-generated job ID for progress tracking
      responses:
        '200':
          description: ZIP archive containing processed images
          content:
            application/zip:
              schema:
                type: string
                format: binary
        '400':
          description: Invalid input
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Error'
        '401':
          description: Authentication required
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/UnauthorizedError'
        '422':
          description: All files failed processing
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Error'
  /api/v1/tools/image/image-to-pdf:
    post:
      operationId: imageToPdf
      tags:
        - Tools
      summary: Зображення в PDF
      description: Конвертувати одне або кілька зображень у документ PDF.
      security:
        - bearerAuth: []
      requestBody:
        required: true
        content:
          multipart/form-data:
            schema:
              type: object
              required:
                - file
              properties:
                file:
                  type: array
                  items:
                    type: string
                    format: binary
                  description: One or more image files to include in the PDF
                settings:
                  type: string
                  description: |
                    JSON string with options:
                    - `pageSize` (string, default "A4") - One of: A4, Letter, A3, A5
                    - `orientation` (string, default "portrait") - One of: portrait, landscape
                    - `margin` (number 0-500, default 20) - Page margin in points
                    - `targetSize` (object, optional) - Target output file size
                      - `value` (number, required) - Positive number (decimals allowed)
                      - `unit` (string, required) - One of: KB, MB
                      Minimum effective target is 50KB. When set, images are compressed as JPEG with quality auto-tuned to meet the target.
                    - `collate` (boolean, default true) - When true, all images go into one PDF. When false, each image becomes a separate PDF and the result is a ZIP.
      responses:
        '200':
          description: Generated PDF (downloadUrl points to .pdf file)
          content:
            application/json:
              schema:
                allOf:
                  - $ref: '#/components/schemas/ToolResponse'
                  - type: object
                    properties:
                      pages:
                        type: integer
                        description: Number of images/pages in the PDF
                      compression:
                        type: object
                        description: Present only when targetSize was specified
                        properties:
                          targetRequested:
                            type: integer
                            description: Target size in bytes
                          targetMet:
                            type: boolean
                            description: Whether the target was achieved
                          jpegQuality:
                            type: integer
                            description: Final JPEG quality used (10-95)
        '400':
          description: Invalid input
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Error'
        '401':
          description: Authentication required
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/UnauthorizedError'
        '422':
          description: Processing failed
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Error'
  /api/v1/tools/pdf/pdf-to-image:
    post:
      operationId: pdfToImage
      tags:
        - Tools
      summary: PDF у зображення
      description: Конвертувати сторінки PDF у зображення. Повертає окремі завантаження сторінок і ZIP усіх сторінок.
      security:
        - bearerAuth: []
      requestBody:
        required: true
        content:
          multipart/form-data:
            schema:
              type: object
              required:
                - file
              properties:
                file:
                  type: string
                  format: binary
                  description: PDF file to convert
                settings:
                  type: string
                  description: |
                    JSON string with options:
                    - `format` (string, default "png") - Output format: png, jpg, webp, avif, tiff, gif, heic, heif, jxl
                    - `dpi` (number 36-2400, default 150) - Resolution in dots per inch
                    - `quality` (number 1-100, default 85) - Output quality
                    - `colorMode` (string, default "color") - One of: color, grayscale, bw
                    - `pages` (string, default "all") - Page selection, e.g. "all", "1-3", "1,3,5"
      responses:
        '200':
          description: Converted pages
          content:
            application/json:
              schema:
                type: object
                properties:
                  jobId:
                    type: string
                  pageCount:
                    type: integer
                    description: Total pages in the PDF
                  selectedPages:
                    type: array
                    items:
                      type: integer
                  format:
                    type: string
                  pages:
                    type: array
                    items:
                      type: object
                      properties:
                        page:
                          type: integer
                        downloadUrl:
                          type: string
                        size:
                          type: integer
                  zipUrl:
                    type: string
                    description: URL to download all pages as ZIP
                  zipSize:
                    type: integer
        '400':
          description: Invalid input
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Error'
        '401':
          description: Authentication required
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/UnauthorizedError'
  /api/v1/tools/pdf/pdf-to-image/info:
    post:
      operationId: getPdfInfo
      tags:
        - Tools
      summary: Кількість сторінок PDF
      description: Отримати кількість сторінок у файлі PDF.
      security: []
      requestBody:
        required: true
        content:
          multipart/form-data:
            schema:
              type: object
              required:
                - file
              properties:
                file:
                  type: string
                  format: binary
                  description: PDF file to inspect
      responses:
        '200':
          description: PDF info
          content:
            application/json:
              schema:
                type: object
                properties:
                  pageCount:
                    type: integer
        '400':
          description: Invalid input
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Error'
  /api/v1/tools/pdf/pdf-to-image/preview:
    post:
      operationId: previewPdfPages
      tags:
        - Tools
      summary: Мініатюри сторінок PDF
      description: Згенерувати попередні мініатюри для кожної сторінки PDF (макс. 200 сторінок, JPEG шириною 300px).
      security: []
      requestBody:
        required: true
        content:
          multipart/form-data:
            schema:
              type: object
              required:
                - file
              properties:
                file:
                  type: string
                  format: binary
                  description: PDF file to preview
      responses:
        '200':
          description: Page thumbnails
          content:
            application/json:
              schema:
                type: object
                properties:
                  pageCount:
                    type: integer
                  thumbnails:
                    type: array
                    items:
                      type: object
                      properties:
                        page:
                          type: integer
                        dataUrl:
                          type: string
                          description: Base64-encoded JPEG data URL
                        width:
                          type: integer
                        height:
                          type: integer
        '400':
          description: Invalid input
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Error'
  /api/v1/tools/image/split:
    post:
      operationId: splitImage
      tags:
        - Tools
      summary: Розділення зображення
      description: Розділити зображення на сітку фрагментів. Повертає файл ZIP.
      security:
        - bearerAuth: []
      requestBody:
        required: true
        content:
          multipart/form-data:
            schema:
              type: object
              required:
                - file
              properties:
                file:
                  type: string
                  format: binary
                  description: Image file to split
                settings:
                  type: string
                  description: |
                    JSON string with options:
                    - `columns` (number 1-100, default 3) - Number of columns
                    - `rows` (number 1-100, default 3) - Number of rows
                    - `tileWidth` (number, min 10, optional) - Fixed tile width in pixels
                    - `tileHeight` (number, min 10, optional) - Fixed tile height in pixels
                    - `outputFormat` (string, default "original") - One of: original, png, jpg, webp, avif, jxl
                    - `quality` (number 1-100, default 90) - Output quality
      responses:
        '200':
          description: ZIP archive with results
          content:
            application/zip:
              schema:
                type: string
                format: binary
        '400':
          description: Invalid input
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Error'
        '401':
          description: Authentication required
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/UnauthorizedError'
  /api/v1/tools/image/bulk-rename:
    post:
      operationId: bulkRename
      tags:
        - Tools
      summary: Масове перейменування
      description: Перейменувати кілька зображень за шаблоном. Підтримує токени {{index}}, {{padded}} та {{original}}. Повертає файл ZIP.
      security:
        - bearerAuth: []
      requestBody:
        required: true
        content:
          multipart/form-data:
            schema:
              type: object
              required:
                - file
              properties:
                file:
                  type: array
                  items:
                    type: string
                    format: binary
                  description: Multiple image files to rename
                settings:
                  type: string
                  description: |
                    JSON string with options:
                    - `pattern` (string 1-1000, default "image-{{index}}") - Naming pattern template
                    - `startIndex` (number, default 1) - Starting index number
      responses:
        '200':
          description: ZIP archive with results
          content:
            application/zip:
              schema:
                type: string
                format: binary
        '400':
          description: Invalid input
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Error'
        '401':
          description: Authentication required
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/UnauthorizedError'
  /api/v1/tools/image/favicon:
    post:
      operationId: generateFavicon
      tags:
        - Tools
      summary: Генератор favicon
      description: Згенерувати повний набір favicon (PNG від 16px до 512px, ICO та manifest.json) із зображення. Повертає файл ZIP.
      security:
        - bearerAuth: []
      requestBody:
        required: true
        content:
          multipart/form-data:
            schema:
              type: object
              required:
                - file
              properties:
                file:
                  type: string
                  format: binary
                  description: Image file to use as favicon source
      responses:
        '200':
          description: ZIP archive with results
          content:
            application/zip:
              schema:
                type: string
                format: binary
        '400':
          description: Invalid input
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Error'
        '401':
          description: Authentication required
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/UnauthorizedError'
  /api/v1/tools/image/watermark-image:
    post:
      operationId: addImageWatermark
      tags:
        - Tools
      summary: Водяний знак зображенням
      description: Додати накладання водяного знака зображенням.
      security:
        - bearerAuth: []
      requestBody:
        required: true
        content:
          multipart/form-data:
            schema:
              type: object
              required:
                - file
                - watermark
              properties:
                file:
                  type: string
                  format: binary
                  description: Main image file
                watermark:
                  type: string
                  format: binary
                  description: Watermark overlay image
                settings:
                  type: string
                  description: |
                    JSON string with options:
                    - `position` (string, default "bottom-right") - One of: center, top-left, top-right, bottom-left, bottom-right
                    - `opacity` (number 0-100, default 50) - Watermark opacity percentage
                    - `scale` (number 1-100, default 25) - Watermark size as percentage of base image
      responses:
        '200':
          description: Processed image
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ToolResponse'
        '400':
          description: Invalid input
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Error'
        '401':
          description: Authentication required
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/UnauthorizedError'
  /api/v1/tools/image/compose:
    post:
      operationId: composeImages
      tags:
        - Tools
      summary: Композиція зображень
      description: Накласти зображення-оверлей на базове зображення з режимами накладання.
      security:
        - bearerAuth: []
      requestBody:
        required: true
        content:
          multipart/form-data:
            schema:
              type: object
              required:
                - file
                - overlay
              properties:
                file:
                  type: string
                  format: binary
                  description: Base image file
                overlay:
                  type: string
                  format: binary
                  description: Overlay image file
                settings:
                  type: string
                  description: |
                    JSON string with options:
                    - `x` (number, default 0) - Horizontal offset of overlay
                    - `y` (number, default 0) - Vertical offset of overlay
                    - `opacity` (number 0-100, default 100) - Overlay opacity percentage
                    - `blendMode` (string, default "over") - One of: over, multiply, screen, overlay, darken, lighten, hard-light, soft-light, difference, exclusion
      responses:
        '200':
          description: Processed image
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ToolResponse'
        '400':
          description: Invalid input
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Error'
        '401':
          description: Authentication required
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/UnauthorizedError'
  /api/v1/tools/image/meme-generator:
    post:
      operationId: generateMeme
      tags:
        - Tools
      summary: Генератор мемів
      description: |
        Створення мемів за шаблонами або з власних зображень. Підтримує два режими: (1) Режим шаблону - надішліть тіло JSON з `templateId` та `textBoxes`. (2) Режим власного зображення - надішліть multipart з `file` та `settings`.
      security:
        - bearerAuth: []
      requestBody:
        required: true
        content:
          multipart/form-data:
            schema:
              type: object
              properties:
                file:
                  type: string
                  format: binary
                  description: Custom image file (optional if using templateId)
                settings:
                  type: string
                  description: |
                    JSON string with options:
                    - `templateId` (string, optional) - ID of a meme template from /api/v1/meme-templates
                    - `textLayout` (string, default "top-bottom") - One of: top-bottom, top-only, bottom-only, center, side-by-side
                    - `textBoxes` (array of {id, text}) - Text content for each position
                    - `fontFamily` (string, default "anton") - One of: anton, arial-black, comic-sans, montserrat, bebas-neue, permanent-marker, roboto
                    - `fontSize` (number 8-200, optional) - Font size in pixels
                    - `textColor` (hex string, default "#ffffff") - Text color
                    - `strokeColor` (hex string, default "#000000") - Text outline color
                    - `textAlign` (string, default "center") - One of: left, center, right
                    - `allCaps` (boolean, default true) - Convert text to uppercase
          application/json:
            schema:
              type: object
              description: Template mode - send templateId and textBoxes as JSON body
              properties:
                templateId:
                  type: string
                  description: ID of a meme template
                textBoxes:
                  type: array
                  items:
                    type: object
                    properties:
                      id:
                        type: string
                      text:
                        type: string
      responses:
        '200':
          description: Processed meme image
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ToolResponse'
        '400':
          description: Invalid input or template not found
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Error'
        '401':
          description: Authentication required
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/UnauthorizedError'
        '422':
          description: Processing failed
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Error'
  /api/v1/meme-templates:
    get:
      operationId: listMemeTemplates
      tags:
        - Tools
      summary: Список шаблонів мемів
      description: Повертає повний маніфест доступних шаблонів мемів, зокрема позиції текстових блоків.
      security:
        - bearerAuth: []
      responses:
        '200':
          description: Meme template manifest
          content:
            application/json:
              schema:
                type: object
                properties:
                  templates:
                    type: array
                    items:
                      type: object
                      properties:
                        id:
                          type: string
                        filename:
                          type: string
                        width:
                          type: integer
                        height:
                          type: integer
                        textBoxes:
                          type: array
                          items:
                            type: object
                            properties:
                              id:
                                type: string
                              x:
                                type: number
                              'y':
                                type: number
                              width:
                                type: number
                              height:
                                type: number
                              defaultText:
                                type: string
        '401':
          description: Authentication required
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/UnauthorizedError'
  /api/v1/meme-templates/full/{filename}:
    get:
      operationId: getMemeTemplateImage
      tags:
        - Tools
      summary: Отримати зображення шаблону мема
      description: Віддати повнорозмірне зображення шаблону мема.
      security:
        - bearerAuth: []
      parameters:
        - name: filename
          in: path
          required: true
          schema:
            type: string
          description: Template image filename
      responses:
        '200':
          description: Template image
          content:
            image/*:
              schema:
                type: string
                format: binary
        '400':
          description: Invalid filename
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Error'
        '404':
          description: Template not found
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Error'
  /api/v1/meme-templates/thumbs/{filename}:
    get:
      operationId: getMemeTemplateThumbnail
      tags:
        - Tools
      summary: Отримати мініатюру шаблону мема
      description: Віддати мініатюру шаблону мема.
      security:
        - bearerAuth: []
      parameters:
        - name: filename
          in: path
          required: true
          schema:
            type: string
          description: Template thumbnail filename
      responses:
        '200':
          description: Thumbnail image
          content:
            image/*:
              schema:
                type: string
                format: binary
        '400':
          description: Invalid filename
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Error'
        '404':
          description: Thumbnail not found
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Error'
  /api/v1/meme-templates/fonts/{filename}:
    get:
      operationId: getMemeTemplateFont
      tags:
        - Tools
      summary: Отримати шрифт шаблону мема
      description: Віддати файл шрифту, який використовує генератор мемів.
      security:
        - bearerAuth: []
      parameters:
        - name: filename
          in: path
          required: true
          schema:
            type: string
          description: Font filename (e.g. anton.ttf)
      responses:
        '200':
          description: Font file
          content:
            font/ttf:
              schema:
                type: string
                format: binary
        '400':
          description: Invalid filename
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Error'
        '404':
          description: Font not found
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Error'
  /api/v1/tools/image/compare:
    post:
      operationId: compareImages
      tags:
        - Tools
      summary: Порівняння зображень
      description: Порівняти два зображення та згенерувати візуальний звіт про відмінності.
      security:
        - bearerAuth: []
      requestBody:
        required: true
        content:
          multipart/form-data:
            schema:
              type: object
              required:
                - file1
                - file2
              properties:
                file1:
                  type: string
                  format: binary
                  description: First image to compare
                file2:
                  type: string
                  format: binary
                  description: Second image to compare
      responses:
        '200':
          description: Comparison result
          content:
            application/json:
              schema:
                allOf:
                  - $ref: '#/components/schemas/ToolResponse'
                  - type: object
                    properties:
                      similarity:
                        type: number
                        description: Similarity percentage (0-100)
                      dimensions:
                        type: object
                        properties:
                          width:
                            type: integer
                          height:
                            type: integer
        '400':
          description: Invalid input
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Error'
        '401':
          description: Authentication required
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/UnauthorizedError'
        '422':
          description: Processing failed
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Error'
  /api/v1/tools/image/erase-object:
    post:
      operationId: eraseObject
      tags:
        - Tools
      summary: Стирання об'єктів
      description: Стерти об'єкти із зображення за маскою. Білі області маски позначають ділянки для стирання. Використовує inpainting LaMa.
      security:
        - bearerAuth: []
      requestBody:
        required: true
        content:
          multipart/form-data:
            schema:
              type: object
              required:
                - image
                - mask
              properties:
                image:
                  type: string
                  format: binary
                  description: Source image file
                mask:
                  type: string
                  format: binary
                  description: Mask image (white areas will be erased)
                format:
                  type: string
                  enum:
                    - auto
                    - png
                    - jpg
                    - jpeg
                    - webp
                    - tiff
                    - gif
                    - avif
                    - heic
                    - heif
                    - jxl
                  default: auto
                  description: Output format
                quality:
                  type: integer
                  minimum: 1
                  maximum: 100
                  default: 95
                  description: Output quality
                clientJobId:
                  type: string
                  description: Client-provided job ID for SSE progress tracking
      responses:
        '202':
          description: Accepted for async processing. Track progress via SSE at /api/v1/jobs/{jobId}/progress.
          content:
            application/json:
              schema:
                type: object
                properties:
                  jobId:
                    type: string
                  async:
                    type: boolean
                    example: true
        '400':
          description: Invalid input
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Error'
        '401':
          description: Authentication required
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/UnauthorizedError'
        '422':
          description: Processing failed
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Error'
        '501':
          description: Feature not installed
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/FeatureNotInstalledError'
  /api/v1/tools/image/ai-canvas-expand:
    post:
      operationId: aiCanvasExpand
      tags:
        - Tools
      summary: AI-розширення полотна (експериментально)
      description: |
        Розширити полотно зображення за допомогою AI-outpainting. Продовжує зображення в будь-якому
        напрямку, генеруючи новий вміст, що безшовно поєднується з
        оригіналом. Використовує outpainting на основі LaMa. Потребує встановленого пакета функцій
        ai-canvas-expand.
      security:
        - bearerAuth: []
      requestBody:
        required: true
        content:
          multipart/form-data:
            schema:
              type: object
              required:
                - file
              properties:
                file:
                  type: string
                  format: binary
                  description: Image file to expand
                settings:
                  type: string
                  description: |
                    JSON string with options:
                    - `extendTop` (integer >= 0, default 0) - Pixels to extend upward
                    - `extendRight` (integer >= 0, default 0) - Pixels to extend rightward
                    - `extendBottom` (integer >= 0, default 0) - Pixels to extend downward
                    - `extendLeft` (integer >= 0, default 0) - Pixels to extend leftward
                    - `tier` (string, default "balanced") - One of: fast, balanced, high
                    - `format` (string, default "auto") - One of: auto, png, jpg, jpeg, webp, tiff, gif, avif, heic, heif, jxl
                    - `quality` (integer 1-100, default 95) - Output quality

                    At least one extend direction must be greater than 0.
                clientJobId:
                  type: string
                  description: Client-provided job ID for SSE progress tracking
      responses:
        '202':
          description: Accepted for async processing. Track progress via SSE at /api/v1/jobs/{jobId}/progress.
          content:
            application/json:
              schema:
                type: object
                properties:
                  jobId:
                    type: string
                  async:
                    type: boolean
                    example: true
        '400':
          description: Invalid input (or no extend direction > 0)
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Error'
        '401':
          description: Authentication required
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/UnauthorizedError'
        '422':
          description: Processing failed
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Error'
        '501':
          description: Feature not installed
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/FeatureNotInstalledError'
  /api/v1/tools/image/collage:
    post:
      operationId: createCollage
      tags:
        - Tools
      summary: Колаж та сітка
      description: Об'єднати кілька зображень у колаж-сітку.
      security:
        - bearerAuth: []
      requestBody:
        required: true
        content:
          multipart/form-data:
            schema:
              type: object
              required:
                - file
              properties:
                file:
                  type: array
                  items:
                    type: string
                    format: binary
                  description: Multiple image files to combine
                settings:
                  type: string
                  description: |
                    JSON string with options:
                    - `templateId` (string, required) - Template ID defining the grid layout
                    - `cells` (array, optional) - Per-cell configuration overrides
                    - `gap` (number 0-500, default 8) - Gap between images in pixels
                    - `cornerRadius` (number 0-500, default 0) - Corner rounding radius
                    - `backgroundColor` (string, default "#FFFFFF") - Background fill color
                    - `aspectRatio` (string, default "free") - Aspect ratio constraint
                    - `outputFormat` (string, default "png") - One of: png, jpeg, webp, avif, jxl
                    - `quality` (number 1-100, default 90) - Output quality
      responses:
        '200':
          description: Processed image
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ToolResponse'
        '400':
          description: Invalid input
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Error'
        '401':
          description: Authentication required
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/UnauthorizedError'
  /api/v1/tools/image/stitch:
    post:
      operationId: stitchImages
      tags:
        - Tools
      summary: Склейка зображень
      description: З'єднати кілька зображень горизонтально, вертикально або в макет сітки.
      security:
        - bearerAuth: []
      requestBody:
        required: true
        content:
          multipart/form-data:
            schema:
              type: object
              required:
                - file
              properties:
                file:
                  type: array
                  items:
                    type: string
                    format: binary
                  description: Two or more image files to stitch together
                settings:
                  type: string
                  description: |
                    JSON string with options:
                    - `direction` (string, default "horizontal") - One of: horizontal, vertical, grid
                    - `gridColumns` (integer 2-100, default 2) - Columns when direction is grid
                    - `resizeMode` (string, default "fit") - One of: fit, original, stretch, crop
                    - `alignment` (string, default "center") - One of: start, center, end
                    - `gap` (number 0-1000, default 0) - Gap between images in pixels
                    - `border` (number 0-500, default 0) - Border width in pixels
                    - `cornerRadius` (number 0-500, default 0) - Corner radius in pixels
                    - `backgroundColor` (hex string, default "#FFFFFF") - Hex color for background and gap fill
                    - `format` (string, default "png") - One of: png, jpeg, webp, avif, jxl
                    - `quality` (number 1-100, default 90) - Output quality
      responses:
        '200':
          description: Stitched image
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ToolResponse'
        '400':
          description: Invalid input
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Error'
        '401':
          description: Authentication required
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/UnauthorizedError'
  /api/v1/tools/image/remove-gif-background:
    post:
      operationId: removeGifBackground
      tags:
        - Tools
      summary: Видалення фону з анімованого зображення
      description: Видаляйте фон з анімованих GIF, WebP або APNG кадр за кадром за допомогою ШІ (rembg). Запускається локально.
      security:
        - bearerAuth: []
      requestBody:
        required: true
        content:
          multipart/form-data:
            schema:
              type: object
              required:
                - file
              properties:
                file:
                  type: string
                  format: binary
                  description: Animated image file (GIF, animated WebP, or APNG)
                settings:
                  type: string
                  description: |
                    JSON string with options:
                    - `model` (string, optional) - AI model name
                    - `outputFormat` (string, optional) - One of: webp, apng, gif
                    - `backgroundType` (string, optional) - One of: transparent, color, gradient, blur, image
                    - `backgroundColor` (string, optional) - Hex color for the color background
                    - `gradientColor1` (string, optional) - First gradient color
                    - `gradientColor2` (string, optional) - Second gradient color
                    - `gradientAngle` (number, optional) - Gradient angle in degrees
                    - `blurIntensity` (number 0-100, optional) - Blur strength for the blur background
                    - `shadowEnabled` (boolean, optional) - Enable drop shadow
                    - `shadowOpacity` (number 0-100, optional) - Shadow opacity
                    - `edgeRefine` (number 0-3, optional) - Edge refinement level
                    - `decontaminate` (boolean, optional) - Remove background color spill
                backgroundImage:
                  type: string
                  format: binary
                  description: Background image, required when backgroundType is "image"
                clientJobId:
                  type: string
                  description: Client-provided job ID for SSE progress tracking
      responses:
        '202':
          description: Accepted for async processing. Track progress via SSE at /api/v1/jobs/{jobId}/progress.
          content:
            application/json:
              schema:
                type: object
                properties:
                  jobId:
                    type: string
                  async:
                    type: boolean
                    example: true
        '400':
          description: Invalid input (not animated, over the frame cap, or bad settings)
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Error'
        '401':
          description: Authentication required
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/UnauthorizedError'
        '501':
          description: Feature not installed
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/FeatureNotInstalledError'
  /api/v1/tools/image/remove-background:
    post:
      operationId: removeBackground
      tags:
        - Tools
      summary: Видалити фон
      description: Видалити фон із зображення за допомогою AI (rembg). Виконується локально.
      security:
        - bearerAuth: []
      requestBody:
        required: true
        content:
          multipart/form-data:
            schema:
              type: object
              required:
                - file
              properties:
                file:
                  type: string
                  format: binary
                  description: Image file to process
                settings:
                  type: string
                  description: |
                    JSON string with options:
                    - `model` (string, optional) - AI model name
                    - `backgroundType` (string, optional) - One of: transparent, color, gradient, blur, image
                    - `backgroundColor` (string, optional) - Hex color to replace removed background with
                    - `gradientColor1` (string, optional) - First gradient color
                    - `gradientColor2` (string, optional) - Second gradient color
                    - `gradientAngle` (number, optional) - Gradient angle in degrees
                    - `blurEnabled` (boolean, optional) - Enable background blur effect
                    - `blurIntensity` (number 0-100, optional) - Blur strength
                    - `shadowEnabled` (boolean, optional) - Enable drop shadow
                    - `shadowOpacity` (number 0-100, optional) - Shadow opacity
                clientJobId:
                  type: string
                  description: Client-provided job ID for SSE progress tracking
      responses:
        '202':
          description: Accepted for async processing. Track progress via SSE at /api/v1/jobs/{jobId}/progress.
          content:
            application/json:
              schema:
                type: object
                properties:
                  jobId:
                    type: string
                  async:
                    type: boolean
                    example: true
        '400':
          description: Invalid input
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Error'
        '401':
          description: Authentication required
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/UnauthorizedError'
        '422':
          description: Processing failed
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Error'
        '501':
          description: Feature not installed
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/FeatureNotInstalledError'
  /api/v1/tools/image/remove-background/effects:
    post:
      operationId: applyBackgroundEffects
      tags:
        - Tools
      summary: Застосувати ефекти фону
      description: |
        Застосувати заміну фону або ефекти до раніше обробленого результату remove-background. Потребує jobId із попереднього виклику POST /api/v1/tools/image/remove-background.
      security:
        - bearerAuth: []
      requestBody:
        required: true
        content:
          multipart/form-data:
            schema:
              type: object
              required:
                - settings
              properties:
                backgroundImage:
                  type: string
                  format: binary
                  description: Custom background image (required when backgroundType is "image")
                settings:
                  type: string
                  description: |
                    JSON string with options:
                    - `jobId` (string, required) - Job ID from the initial remove-background call
                    - `filename` (string, required) - Original filename
                    - `backgroundType` (string) - One of: transparent, color, gradient, blur, image
                    - `backgroundColor` (string) - Hex color for solid background
                    - `gradientColor1` (string) - First gradient color
                    - `gradientColor2` (string) - Second gradient color
                    - `gradientAngle` (number) - Gradient angle in degrees
                    - `blurEnabled` (boolean) - Enable background blur effect
                    - `blurIntensity` (number 0-100) - Blur strength
                    - `shadowEnabled` (boolean) - Enable drop shadow
                    - `shadowOpacity` (number 0-100) - Shadow opacity
      responses:
        '200':
          description: Image with applied effects
          content:
            application/json:
              schema:
                type: object
                properties:
                  jobId:
                    type: string
                  downloadUrl:
                    type: string
                  processedSize:
                    type: integer
        '400':
          description: Invalid input
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Error'
        '401':
          description: Authentication required
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/UnauthorizedError'
  /api/v1/tools/image/transparency-fixer:
    post:
      operationId: fixTransparency
      tags:
        - Tools
      summary: Виправити фальшиву прозорість
      description: |
        Виправити "фальшиво прозорі" PNG, що мають облямівку, ореоли або напівпрозорі
        артефакти від попереднього видалення фону. Використовує BiRefNet HR-matting
        (2048x2048) для створення чистого альфа-каналу з налаштовуваною обробкою
        defringe. Резервно переходить на birefnet-general, потім u2net при OOM. Потребує
        встановленого пакета функцій background-removal.
      security:
        - bearerAuth: []
      requestBody:
        required: true
        content:
          multipart/form-data:
            schema:
              type: object
              required:
                - file
              properties:
                file:
                  type: string
                  format: binary
                  description: PNG image with fake or damaged transparency
                settings:
                  type: string
                  description: |
                    JSON string with options:
                    - `defringe` (number 0-100, default 30) - Edge defringe strength to remove color contamination
                    - `outputFormat` (string, default "png") - One of: png, webp
                    - `removeWatermark` (boolean, default false) - Apply median filter to reduce watermark artifacts before matting
                clientJobId:
                  type: string
                  description: Client-provided job ID for SSE progress tracking
      responses:
        '202':
          description: Accepted for async processing. Track progress via SSE at /api/v1/jobs/{jobId}/progress.
          content:
            application/json:
              schema:
                type: object
                properties:
                  jobId:
                    type: string
                  async:
                    type: boolean
                    example: true
        '400':
          description: Invalid input
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Error'
        '401':
          description: Authentication required
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/UnauthorizedError'
        '422':
          description: Processing failed
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Error'
        '501':
          description: Feature not installed
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/FeatureNotInstalledError'
  /api/v1/tools/image/upscale:
    post:
      operationId: upscaleImage
      tags:
        - Tools
      summary: Збільшення зображення
      description: Збільшити зображення за допомогою AI (Real-ESRGAN). Виконується локально.
      security:
        - bearerAuth: []
      requestBody:
        required: true
        content:
          multipart/form-data:
            schema:
              type: object
              required:
                - file
              properties:
                file:
                  type: string
                  format: binary
                  description: Image file to upscale
                settings:
                  type: string
                  description: |
                    JSON string with options:
                    - `scale` (number, default 2) - Upscale factor
                    - `model` (string, default "auto") - AI model name
                    - `faceEnhance` (boolean, default false) - Enable face enhancement
                    - `denoise` (number, default 0) - Denoise strength
                    - `format` (string, default "auto") - Output format (auto, png, jpg, jpeg, webp, tiff, gif, avif, heic, heif, jxl)
                    - `quality` (number, default 95) - Output quality (1-100)
                clientJobId:
                  type: string
                  description: Client-provided job ID for SSE progress tracking
      responses:
        '202':
          description: Accepted for async processing. Track progress via SSE at /api/v1/jobs/{jobId}/progress.
          content:
            application/json:
              schema:
                type: object
                properties:
                  jobId:
                    type: string
                  async:
                    type: boolean
                    example: true
        '400':
          description: Invalid input
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Error'
        '401':
          description: Authentication required
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/UnauthorizedError'
        '422':
          description: Processing failed
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Error'
        '501':
          description: Feature not installed
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/FeatureNotInstalledError'
  /api/v1/tools/image/blur-faces:
    post:
      operationId: blurFaces
      tags:
        - Tools
      summary: Розмиття облич та ПД
      description: Виявити та розмити обличчя на зображенні для приватності. Використовує OpenCV. Виконується локально.
      security:
        - bearerAuth: []
      requestBody:
        required: true
        content:
          multipart/form-data:
            schema:
              type: object
              required:
                - file
              properties:
                file:
                  type: string
                  format: binary
                  description: Image file to process
                settings:
                  type: string
                  description: |
                    JSON string with options:
                    - `blurRadius` (number 1-100, default 30) - Blur strength radius
                    - `sensitivity` (number 0-1, default 0.5) - Face detection sensitivity
                clientJobId:
                  type: string
                  description: Client-provided job ID for SSE progress tracking
      responses:
        '202':
          description: Accepted for async processing. Track progress via SSE at /api/v1/jobs/{jobId}/progress.
          content:
            application/json:
              schema:
                type: object
                properties:
                  jobId:
                    type: string
                  async:
                    type: boolean
                    example: true
        '400':
          description: Invalid input
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Error'
        '401':
          description: Authentication required
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/UnauthorizedError'
        '422':
          description: Processing failed
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Error'
        '501':
          description: Feature not installed
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/FeatureNotInstalledError'
  /api/v1/tools/image/ocr:
    post:
      operationId: extractText
      tags:
        - Tools
      summary: OCR / вилучення тексту
      description: |-
        Витягніть текст із зображення локально. Вбудований рівень Fast використовує
        Tesseract. Balanced і Best використовують необов’язкове підписане середовище виконання RapidOCR
        із закріпленими моделями PP-OCR ONNX. Явні запити рівня ніколи не мовчки
        перейти на інший рівень.
      security:
        - bearerAuth: []
      requestBody:
        required: true
        content:
          multipart/form-data:
            schema:
              type: object
              required:
                - file
              properties:
                file:
                  type: string
                  format: binary
                  maxLength: 536870912
                  description: Image file to extract text from
                settings:
                  type: string
                  description: |
                    JSON string with options:
                    - `quality` (string, optional) - One of: fast, balanced, best. When both quality and engine are omitted, uses the best available tier in this order: best, balanced, fast. Korean never selects fast; it uses best, then balanced, or returns an accurate-runtime install/compatibility error
                    - `language` (string, default "auto") - One of: auto, en, de, fr, es, zh, ja, ko. Korean is unsupported by fast and the legacy tesseract alias
                    - `enhance` (boolean, optional) - Improve local contrast. Fast applies it directly; accurate tiers retain the variant only when calibrated scoring improves the result. Defaults to true for Best and false for Fast/Balanced
                    - `engine` (string, deprecated) - Compatibility alias: tesseract maps to fast; the legacy paddleocr value maps to balanced but does not load PaddlePaddle
                clientJobId:
                  type: string
      responses:
        '202':
          description: Accepted after validation and enqueueing. Track progress via SSE at /api/v1/jobs/{jobId}/progress; processing failures arrive in the terminal failed event.
          content:
            application/json:
              schema:
                type: object
                required:
                  - jobId
                  - async
                properties:
                  jobId:
                    type: string
                  progressJobId:
                    type: string
                  artifactJobId:
                    type: string
                  async:
                    type: boolean
                    const: true
        '400':
          description: Invalid input
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Error'
        '401':
          description: Authentication required
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/UnauthorizedError'
        '413':
          description: Encoded OCR input exceeds the hard 512 MiB safety limit
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Error'
        '501':
          description: Requested Balanced/Best runtime is not installed or is incompatible
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/FeatureNotInstalledError'
        '503':
          description: OCR could not be queued
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Error'
  /api/v1/tools/image/info:
    post:
      operationId: getImageInfo
      tags:
        - Tools
      summary: Інформація про зображення
      description: Отримати детальні метадані про зображення, зокрема розміри, формат, колірний простір і дані EXIF.
      security:
        - bearerAuth: []
      requestBody:
        required: true
        content:
          multipart/form-data:
            schema:
              type: object
              required:
                - file
              properties:
                file:
                  type: string
                  format: binary
                  description: Image file to inspect
      responses:
        '200':
          description: Image metadata
          content:
            application/json:
              schema:
                type: object
                properties:
                  width:
                    type: integer
                  height:
                    type: integer
                  filename:
                    type: string
                  format:
                    type: string
                  colorSpace:
                    type: string
                  channels:
                    type: integer
                  bitDepth:
                    type: string
                  density:
                    type:
                      - integer
                      - 'null'
                  hasAlpha:
                    type: boolean
                  fileSize:
                    type: integer
                  hasExif:
                    type: boolean
                  isProgressive:
                    type: boolean
                  orientation:
                    type:
                      - integer
                      - 'null'
                  hasProfile:
                    type: boolean
                  hasIcc:
                    type: boolean
                  hasXmp:
                    type: boolean
                  pages:
                    type: integer
                  histogram:
                    type: array
                    items:
                      type: object
                      properties:
                        channel:
                          type: string
                        min:
                          type: number
                        max:
                          type: number
                        mean:
                          type: number
                        stdev:
                          type: number
        '400':
          description: Invalid input
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Error'
        '401':
          description: Authentication required
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/UnauthorizedError'
        '422':
          description: Processing failed
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Error'
  /api/v1/tools/image/color-palette:
    post:
      operationId: extractColorPalette
      tags:
        - Tools
      summary: Палітра кольорів
      description: Вилучити домінантні кольори із зображення.
      security:
        - bearerAuth: []
      requestBody:
        required: true
        content:
          multipart/form-data:
            schema:
              type: object
              required:
                - file
              properties:
                file:
                  type: string
                  format: binary
                  description: Image file to analyze
      responses:
        '200':
          description: Dominant colors
          content:
            application/json:
              schema:
                type: object
                properties:
                  filename:
                    type: string
                  colors:
                    type: array
                    items:
                      type: string
                    description: Hex color strings
                  count:
                    type: integer
        '400':
          description: Invalid input
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Error'
        '401':
          description: Authentication required
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/UnauthorizedError'
  /api/v1/tools/image/barcode-read:
    post:
      operationId: readBarcode
      tags:
        - Tools
      summary: Зчитувач штрихкодів
      description: Зчитати QR-коди та штрихкоди із зображення.
      security:
        - bearerAuth: []
      requestBody:
        required: true
        content:
          multipart/form-data:
            schema:
              type: object
              required:
                - file
              properties:
                file:
                  type: string
                  format: binary
                  description: Image file containing barcode or QR code
                settings:
                  type: string
                  description: |
                    JSON string with options:
                    - `tryHarder` (boolean, default true) - Enable more aggressive barcode detection
      responses:
        '200':
          description: Decoded barcode data
          content:
            application/json:
              schema:
                type: object
                properties:
                  filename:
                    type: string
                  barcodes:
                    type: array
                    items:
                      type: object
                      properties:
                        type:
                          type: string
                        text:
                          type: string
                        position:
                          type: object
                          properties:
                            topLeft:
                              type: object
                              properties:
                                x:
                                  type: number
                                'y':
                                  type: number
                            topRight:
                              type: object
                              properties:
                                x:
                                  type: number
                                'y':
                                  type: number
                            bottomLeft:
                              type: object
                              properties:
                                x:
                                  type: number
                                'y':
                                  type: number
                            bottomRight:
                              type: object
                              properties:
                                x:
                                  type: number
                                'y':
                                  type: number
                  annotatedUrl:
                    type:
                      - string
                      - 'null'
                  previewUrl:
                    type:
                      - string
                      - 'null'
        '400':
          description: Invalid input
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Error'
        '401':
          description: Authentication required
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/UnauthorizedError'
        '422':
          description: Processing failed
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Error'
  /api/v1/tools/image/find-duplicates:
    post:
      operationId: findDuplicates
      tags:
        - Tools
      summary: Знайти дублікати
      description: Знайти дублікати зображень у наборі за допомогою перцептивного хешування.
      security:
        - bearerAuth: []
      requestBody:
        required: true
        content:
          multipart/form-data:
            schema:
              type: object
              required:
                - file
              properties:
                file:
                  type: array
                  items:
                    type: string
                    format: binary
                  description: Multiple image files to check for duplicates
                settings:
                  type: string
                  description: |
                    JSON string with options:
                    - `threshold` (number 0-20, default 8) - Hamming distance threshold for duplicate detection
      responses:
        '200':
          description: Duplicate groups
          content:
            application/json:
              schema:
                type: object
                properties:
                  totalImages:
                    type: integer
                  duplicateGroups:
                    type: array
                    items:
                      type: object
                      properties:
                        groupId:
                          type: integer
                        files:
                          type: array
                          items:
                            type: object
                            properties:
                              filename:
                                type: string
                              similarity:
                                type: number
                              width:
                                type: integer
                              height:
                                type: integer
                              fileSize:
                                type: integer
                              format:
                                type: string
                              isBest:
                                type: boolean
                              thumbnail:
                                type:
                                  - string
                                  - 'null'
                  uniqueImages:
                    type: integer
                  spaceSaveable:
                    type: integer
                  skippedFiles:
                    type: array
                    description: Files that could not be processed (present only when non-empty)
                    items:
                      type: object
                      properties:
                        filename:
                          type: string
                        reason:
                          type: string
        '400':
          description: Invalid input
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Error'
        '401':
          description: Authentication required
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/UnauthorizedError'
        '422':
          description: Processing failed
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Error'
  /api/v1/tools/image/qr-generate:
    post:
      operationId: generateQrCode
      tags:
        - Tools
      summary: Генератор QR-кодів
      description: Згенерувати зображення QR-коду з тексту. Цей інструмент приймає тіло JSON, а не multipart.
      security:
        - bearerAuth: []
      requestBody:
        required: true
        content:
          application/json:
            schema:
              type: object
              required:
                - text
              properties:
                text:
                  type: string
                  maxLength: 2000
                  description: Text to encode in the QR code
                size:
                  type: integer
                  minimum: 100
                  maximum: 10000
                  default: 400
                  description: Image size in pixels
                errorCorrection:
                  type: string
                  enum:
                    - L
                    - M
                    - Q
                    - H
                  default: M
                foreground:
                  type: string
                  default: '#000000'
                  description: Foreground color (hex)
                background:
                  type: string
                  default: '#FFFFFF'
                  description: Background color (hex)
      responses:
        '200':
          description: Generated QR code
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ToolResponse'
        '400':
          description: Invalid input
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Error'
        '401':
          description: Authentication required
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/UnauthorizedError'
  /api/v1/tools/image/html-to-image:
    post:
      operationId: captureWebpage
      tags:
        - Tools
      summary: Захопити вебсторінку як зображення
      description: Захопити вебсторінку як високоякісний скріншот. Приймає тіло JSON з URL та параметрами захоплення.
      security:
        - bearerAuth: []
      requestBody:
        required: true
        content:
          application/json:
            schema:
              type: object
              required:
                - url
              properties:
                url:
                  type: string
                  format: uri
                  description: URL to capture (http/https only)
                format:
                  type: string
                  enum:
                    - jpg
                    - png
                    - webp
                  default: png
                quality:
                  type: integer
                  minimum: 1
                  maximum: 100
                  default: 90
                fullPage:
                  type: boolean
                  default: false
                devicePreset:
                  type: string
                  enum:
                    - desktop
                    - tablet
                    - mobile
                    - custom
                  default: desktop
                viewportWidth:
                  type: integer
                  minimum: 320
                  maximum: 3840
                  default: 1280
                viewportHeight:
                  type: integer
                  minimum: 320
                  maximum: 2160
                  default: 720
      responses:
        '200':
          description: Screenshot captured successfully
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ToolResponse'
        '400':
          description: Invalid URL or parameters
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Error'
        '401':
          description: Authentication required
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/UnauthorizedError'
        '503':
          description: Browser service unavailable
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Error'
        '504':
          description: Page load timeout
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Error'
  /api/v1/tools/image/strip-metadata/inspect:
    post:
      operationId: inspectMetadata
      tags:
        - Tools
      summary: Переглянути метадані
      description: Переглянути всі метадані (EXIF, GPS, ICC, XMP) у зображенні, не видаляючи їх.
      security:
        - bearerAuth: []
      requestBody:
        required: true
        content:
          multipart/form-data:
            schema:
              type: object
              required:
                - file
              properties:
                file:
                  type: string
                  format: binary
                  description: Image file to inspect
      responses:
        '200':
          description: Image metadata
          content:
            application/json:
              schema:
                type: object
                properties:
                  filename:
                    type: string
                  fileSize:
                    type: integer
                  exif:
                    type: object
                    description: EXIF metadata
                  exifError:
                    type: string
                    description: Present when EXIF parsing fails
                  gps:
                    type: object
                    description: GPS metadata
                  icc:
                    type: object
                    description: ICC profile metadata
                  xmp:
                    type: object
                    description: XMP metadata
        '400':
          description: Invalid input
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Error'
        '401':
          description: Authentication required
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/UnauthorizedError'
        '422':
          description: Failed to read image metadata
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Error'
  /api/v1/tools/image/edit-metadata:
    post:
      operationId: editMetadata
      tags:
        - Tools
      summary: Редагування метаданих зображення
      description: Редагувати поля метаданих EXIF, IPTC, GPS та інші в зображенні за допомогою ExifTool.
      security:
        - bearerAuth: []
      requestBody:
        required: true
        content:
          multipart/form-data:
            schema:
              type: object
              required:
                - file
              properties:
                file:
                  type: string
                  format: binary
                  description: Image file to process
                settings:
                  type: string
                  description: |
                    JSON string with options:
                    - `title` (string) - Image title
                    - `author` (string) - Author name
                    - `artist` (string) - Artist or author name
                    - `copyright` (string) - Copyright notice
                    - `imageDescription` (string) - Image description
                    - `software` (string) - Software used
                    - `dateTime` (string) - Date/time string
                    - `dateTimeOriginal` (string) - Original capture date/time
                    - `clearGps` (boolean, default false) - Remove all GPS data
                    - `fieldsToRemove` (string[]) - Specific metadata fields to remove
                    - `gpsLatitude` (number -90 to 90) - GPS latitude
                    - `gpsLongitude` (number -180 to 180) - GPS longitude
                    - `gpsAltitude` (number) - GPS altitude in meters
                    - `keywords` (string[]) - Keywords or tags
                    - `keywordsMode` (string, default "add") - One of: add, set
                    - `dateShift` (string) - Shift dates by offset, e.g. "+2:00" or "-1:30"
                    - `setAllDates` (string) - Set all date fields to this value
                    - `iptcTitle` (string) - IPTC title
                    - `iptcHeadline` (string) - IPTC headline
                    - `iptcCity` (string) - IPTC city
                    - `iptcState` (string) - IPTC state or province
                    - `iptcCountry` (string) - IPTC country
      responses:
        '200':
          description: Image with updated metadata
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ToolResponse'
        '400':
          description: Invalid input
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Error'
        '401':
          description: Authentication required
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/UnauthorizedError'
  /api/v1/tools/image/edit-metadata/inspect:
    post:
      operationId: inspectMetadataExifTool
      tags:
        - Tools
      summary: Переглянути метадані (ExifTool)
      description: Прочитати всі метадані із зображення за допомогою ExifTool. Повертає всі вбудовані поля EXIF, IPTC, XMP та GPS.
      security:
        - bearerAuth: []
      requestBody:
        required: true
        content:
          multipart/form-data:
            schema:
              type: object
              required:
                - file
              properties:
                file:
                  type: string
                  format: binary
                  description: Image file to inspect
      responses:
        '200':
          description: Full metadata object (keys vary by image)
          content:
            application/json:
              schema:
                type: object
                additionalProperties: true
        '400':
          description: Invalid input
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Error'
        '401':
          description: Authentication required
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/UnauthorizedError'
        '422':
          description: Failed to read image metadata
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Error'
  /api/v1/tools/{section}/{toolId}/batch:
    post:
      operationId: batchProcess
      tags:
        - Batch
      summary: Пакетна обробка зображень
      description: |
        Надіслати кілька зображень через один інструмент. Повертає файл ZIP. Відповідь містить заголовок X-Job-Id для відстеження прогресу.
      security:
        - bearerAuth: []
      parameters:
        - name: section
          in: path
          required: true
          schema:
            type: string
          description: Tool section slug (image, video, audio, pdf, files)
        - name: toolId
          in: path
          required: true
          schema:
            type: string
          description: Tool identifier (e.g. resize, crop)
      requestBody:
        required: true
        content:
          multipart/form-data:
            schema:
              type: object
              required:
                - file
              properties:
                file:
                  type: array
                  items:
                    type: string
                    format: binary
                  description: Image files to process (one or more)
                settings:
                  type: string
                  description: JSON string of tool settings applied to all images
      responses:
        '200':
          description: ZIP archive of processed images
          headers:
            X-Job-Id:
              schema:
                type: string
              description: Job identifier for progress tracking
          content:
            application/zip:
              schema:
                type: string
                format: binary
        '400':
          description: Invalid input
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Error'
        '401':
          description: Authentication required
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/UnauthorizedError'
  /api/v1/pipeline/execute:
    post:
      operationId: executePipeline
      tags:
        - Pipelines
      summary: Виконати конвеєр
      description: Пропустити зображення через ланцюжок інструментів. Визначення конвеєра надсилається як рядок JSON у multipart-полі `pipeline`.
      security:
        - bearerAuth: []
      requestBody:
        required: true
        content:
          multipart/form-data:
            schema:
              type: object
              required:
                - file
                - pipeline
              properties:
                file:
                  type: string
                  format: binary
                  description: Image file to process
                pipeline:
                  type: string
                  description: 'JSON string: { "steps": [{ "toolId": "resize", "settings": {...} }, ...] }'
      responses:
        '200':
          description: Pipeline result
          content:
            application/json:
              schema:
                type: object
                properties:
                  jobId:
                    type: string
                  downloadUrl:
                    type: string
                  originalSize:
                    type: integer
                  processedSize:
                    type: integer
                  stepsCompleted:
                    type: integer
                  steps:
                    type: array
                    items:
                      type: object
        '400':
          description: Invalid input
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Error'
        '401':
          description: Authentication required
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/UnauthorizedError'
        '422':
          description: Processing failed
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Error'
        '501':
          description: Feature not installed
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/FeatureNotInstalledError'
  /api/v1/pipeline/save:
    post:
      operationId: savePipeline
      tags:
        - Pipelines
      summary: Зберегти конвеєр
      description: Зберегти іменоване визначення конвеєра з його кроками інструментів для подальшого виконання. Використовуйте для збереження багаторазових робочих процесів.
      security:
        - bearerAuth: []
      requestBody:
        required: true
        content:
          application/json:
            schema:
              type: object
              required:
                - name
                - steps
              properties:
                name:
                  type: string
                description:
                  type: string
                  description: Optional human-readable description of the pipeline
                steps:
                  type: array
                  items:
                    type: object
                    properties:
                      toolId:
                        type: string
                      settings:
                        type: object
      responses:
        '201':
          description: Pipeline saved
          content:
            application/json:
              schema:
                type: object
                properties:
                  id:
                    type: string
                  name:
                    type: string
                  description:
                    type: string
                    description: Human-readable description of the pipeline
                  steps:
                    type: array
                    items:
                      type: object
                  createdAt:
                    type: string
                    format: date-time
        '400':
          description: Invalid input
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Error'
        '401':
          description: Authentication required
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/UnauthorizedError'
  /api/v1/pipeline/list:
    get:
      operationId: listPipelines
      tags:
        - Pipelines
      summary: Список збережених конвеєрів
      description: Отримати всі збережені визначення конвеєрів для поточного користувача. Повертає назву, кроки та дату створення для кожного.
      security:
        - bearerAuth: []
      responses:
        '200':
          description: List of pipelines
          content:
            application/json:
              schema:
                type: object
                properties:
                  pipelines:
                    type: array
                    items:
                      type: object
                      properties:
                        id:
                          type: string
                        name:
                          type: string
                        description:
                          type: string
                          description: Human-readable description of the pipeline
                        steps:
                          type: array
                          items:
                            type: object
                        createdAt:
                          type: string
                          format: date-time
        '400':
          description: Invalid input
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Error'
        '401':
          description: Authentication required
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/UnauthorizedError'
  /api/v1/pipeline/{id}:
    delete:
      operationId: deletePipeline
      tags:
        - Pipelines
      summary: Видалити конвеєр
      description: Остаточно видалити збережений конвеєр за його ID. Видалити може лише власник конвеєра або адміністратор.
      security:
        - bearerAuth: []
      parameters:
        - name: id
          in: path
          required: true
          description: Pipeline UUID
          schema:
            type: string
      responses:
        '200':
          description: Pipeline deleted
          content:
            application/json:
              schema:
                type: object
                properties:
                  ok:
                    type: boolean
        '400':
          description: Invalid input
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Error'
        '401':
          description: Authentication required
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/UnauthorizedError'
        '403':
          description: Not authorized to delete this pipeline
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ForbiddenError'
        '404':
          description: Pipeline not found
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Error'
  /api/v1/pipeline/tools:
    get:
      operationId: listPipelineTools
      tags:
        - Pipelines
      summary: Список інструментів, сумісних із конвеєром
      description: Повертає всі ID інструментів, які можна використовувати як кроки конвеєра.
      security: []
      responses:
        '200':
          description: Tool IDs
          content:
            application/json:
              schema:
                type: object
                properties:
                  toolIds:
                    type: array
                    items:
                      type: string
  /api/v1/pipeline/batch:
    post:
      operationId: batchPipeline
      tags:
        - Pipelines
      summary: Запустити конвеєр на кількох файлах
      description: Виконати конвеєр для кількох зображень. Повертає файл ZIP з усіма обробленими результатами.
      security:
        - bearerAuth: []
      requestBody:
        required: true
        content:
          multipart/form-data:
            schema:
              type: object
              required:
                - file
                - pipeline
              properties:
                file:
                  type: array
                  items:
                    type: string
                    format: binary
                  description: Image files to process
                pipeline:
                  type: string
                  description: |
                    JSON string with pipeline definition:
                    - `steps` (array, required, 1-20 items) - Pipeline steps
                    - `steps[].toolId` (string, required) - Tool ID for this step
                    - `steps[].settings` (object, optional) - Tool-specific settings
                clientJobId:
                  type: string
                  description: Optional client-provided job ID for progress tracking via SSE
      responses:
        '200':
          description: ZIP file with processed images
          headers:
            X-Job-Id:
              schema:
                type: string
              description: Job ID for progress tracking
            X-File-Results:
              schema:
                type: string
              description: Percent-encoded JSON mapping of input index to output filename (decode with decodeURIComponent before JSON.parse)
          content:
            application/zip:
              schema:
                type: string
                format: binary
        '400':
          description: Invalid input
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Error'
        '401':
          description: Authentication required
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/UnauthorizedError'
  /api/v1/upload:
    post:
      operationId: uploadImage
      tags:
        - Files
      summary: Завантажити зображення
      description: Завантажити необроблений файл зображення для подальшої обробки ендпоінтами інструментів. Повертає jobId та метадані файлу.
      security:
        - bearerAuth: []
      requestBody:
        required: true
        content:
          multipart/form-data:
            schema:
              type: object
              required:
                - file
              properties:
                file:
                  type: string
                  format: binary
      responses:
        '200':
          description: Upload result
          content:
            application/json:
              schema:
                type: object
                properties:
                  jobId:
                    type: string
                  files:
                    type: array
                    items:
                      type: object
                      properties:
                        name:
                          type: string
                        size:
                          type: integer
                        format:
                          type: string
        '400':
          description: Invalid input
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Error'
        '401':
          description: Authentication required
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/UnauthorizedError'
  /api/v1/download/{jobId}/{filename}:
    get:
      operationId: downloadProcessedImage
      tags:
        - Files
      summary: Завантажити оброблене зображення
      description: Завантажити оброблене зображення за його job ID та іменем файлу. Це URL, що повертається у downloadUrl у відповідях інструментів.
      security: []
      parameters:
        - name: jobId
          in: path
          required: true
          description: Job identifier from a tool processing response
          schema:
            type: string
        - name: filename
          in: path
          required: true
          description: Output filename from the processing response
          schema:
            type: string
      responses:
        '200':
          description: Image binary
          content:
            image/*:
              schema:
                type: string
                format: binary
        '404':
          description: File not found
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Error'
  /api/v1/preview:
    post:
      operationId: generatePreview
      tags:
        - Files
      summary: Згенерувати попередній перегляд зображення
      description: Конвертувати будь-яке зображення (зокрема HEIC/HEIF) у попередній перегляд WebP, зменшений до розмірів у межах 1200x1200 пікселів.
      security: []
      requestBody:
        required: true
        content:
          multipart/form-data:
            schema:
              type: object
              required:
                - file
              properties:
                file:
                  type: string
                  format: binary
                  description: Image file to preview
      responses:
        '200':
          description: WebP preview image
          content:
            image/webp:
              schema:
                type: string
                format: binary
        '400':
          description: Invalid input
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Error'
  /api/v1/files:
    get:
      operationId: listFiles
      tags:
        - Files
      summary: Список збережених файлів
      description: Повертає останню версію кожного файлу (по одній на ланцюжок версій). Підтримує пагінацію та пошук.
      security:
        - bearerAuth: []
      parameters:
        - name: limit
          in: query
          description: Maximum number of files to return (default 50)
          schema:
            type: integer
            default: 50
        - name: offset
          in: query
          description: Number of files to skip for pagination (default 0)
          schema:
            type: integer
            default: 0
        - name: search
          in: query
          description: Filter files by original filename (substring match)
          schema:
            type: string
      responses:
        '200':
          description: List of saved files
          content:
            application/json:
              schema:
                type: object
                properties:
                  files:
                    type: array
                    items:
                      type: object
                      properties:
                        id:
                          type: string
                        filename:
                          type: string
                        size:
                          type: integer
                        contentType:
                          type: string
                        createdAt:
                          type: string
                          format: date-time
                  total:
                    type: integer
                    description: Total number of matching files (for pagination)
                  limit:
                    type: integer
                    description: Applied limit value
                  offset:
                    type: integer
                    description: Applied offset value
        '400':
          description: Invalid input
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Error'
        '401':
          description: Authentication required
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/UnauthorizedError'
    delete:
      operationId: bulkDeleteFiles
      tags:
        - Files
      summary: Масове видалення збережених файлів
      description: Видалити файли та їхні повні ланцюжки версій. Неадміністратори можуть видаляти лише власні файли.
      security:
        - bearerAuth: []
      requestBody:
        required: true
        content:
          application/json:
            schema:
              type: object
              required:
                - ids
              properties:
                ids:
                  type: array
                  items:
                    type: string
                  description: File IDs to delete (deletes entire version chain for each)
      responses:
        '200':
          description: Deletion result
          content:
            application/json:
              schema:
                type: object
                properties:
                  deleted:
                    type: integer
                    description: Total number of records deleted across all version chains
        '400':
          description: Invalid input
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Error'
        '401':
          description: Authentication required
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/UnauthorizedError'
  /api/v1/files/upload:
    post:
      operationId: saveFileToLibrary
      tags:
        - Files
      summary: Зберегти файл у бібліотеку
      description: Завантажити один або кілька файлів зображень безпосередньо до бібліотеки файлів для постійного зберігання та відстеження версій.
      security:
        - bearerAuth: []
      requestBody:
        required: true
        content:
          multipart/form-data:
            schema:
              type: object
              required:
                - file
              properties:
                file:
                  type: string
                  format: binary
      responses:
        '201':
          description: Files uploaded
          content:
            application/json:
              schema:
                type: object
                properties:
                  files:
                    type: array
                    items:
                      type: object
                      description: Full file object with all metadata
        '400':
          description: Invalid input
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Error'
        '401':
          description: Authentication required
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/UnauthorizedError'
  /api/v1/files/save-result:
    post:
      operationId: saveProcessingResult
      tags:
        - Files
      summary: Зберегти результат обробки в бібліотеку
      description: Зберегти результат обробки інструментом як нову версію наявного файлу. Створює ланцюжок версій.
      security:
        - bearerAuth: []
      requestBody:
        required: true
        content:
          multipart/form-data:
            schema:
              type: object
              required:
                - file
                - parentId
              properties:
                file:
                  type: string
                  format: binary
                  description: The processed image file
                parentId:
                  type: string
                  description: ID of the parent file in the version chain
                toolId:
                  type: string
                  description: Tool that produced this result
      responses:
        '201':
          description: File saved
          content:
            application/json:
              schema:
                type: object
                properties:
                  file:
                    type: object
                    description: Full file object with all metadata
        '400':
          description: Invalid input
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Error'
        '401':
          description: Authentication required
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/UnauthorizedError'
        '404':
          description: Parent file not found
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Error'
  /api/v1/files/{id}:
    get:
      operationId: getFile
      tags:
        - Files
      summary: Отримати метадані файлу та історію версій
      description: Отримати метадані та повну історію версій збереженого файлу. Використовуйте для перегляду деталей файлу перед завантаженням.
      security:
        - bearerAuth: []
      parameters:
        - name: id
          in: path
          required: true
          description: Saved file UUID
          schema:
            type: string
      responses:
        '200':
          description: File metadata with version history
          content:
            application/json:
              schema:
                type: object
                properties:
                  file:
                    type: object
                    properties:
                      id:
                        type: string
                      originalName:
                        type: string
                      mimeType:
                        type: string
                      size:
                        type: integer
                      width:
                        type: integer
                      height:
                        type: integer
                      version:
                        type: integer
                      parentId:
                        type:
                          - string
                          - 'null'
                      toolChain:
                        type: array
                        items:
                          type: string
                      createdAt:
                        type: string
                        format: date-time
                  versions:
                    type: array
                    items:
                      type: object
                    description: Full version chain of this file
        '401':
          description: Authentication required
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/UnauthorizedError'
        '404':
          description: File not found
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Error'
  /api/v1/files/{id}/download:
    get:
      operationId: downloadSavedFile
      tags:
        - Files
      summary: Завантажити збережений файл
      description: Завантажити двійковий вміст збереженого файлу з бібліотеки за його ID.
      security:
        - bearerAuth: []
      parameters:
        - name: id
          in: path
          required: true
          description: Saved file UUID
          schema:
            type: string
      responses:
        '200':
          description: Image binary
          content:
            image/*:
              schema:
                type: string
                format: binary
        '401':
          description: Authentication required
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/UnauthorizedError'
        '404':
          description: File not found
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Error'
  /api/v1/files/{id}/thumbnail:
    get:
      operationId: getFileThumbnail
      tags:
        - Files
      summary: Отримати мініатюру файлу
      description: Отримати мініатюру JPEG 300px збереженого файлу. Корисно для галерейних виглядів і файлових браузерів.
      security:
        - bearerAuth: []
      parameters:
        - name: id
          in: path
          required: true
          description: Saved file UUID
          schema:
            type: string
      responses:
        '200':
          description: 300px JPEG thumbnail
          content:
            image/jpeg:
              schema:
                type: string
                format: binary
        '404':
          description: File not found
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Error'
        '422':
          description: Thumbnail generation failed
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Error'
  /api/v1/fetch-urls:
    post:
      operationId: fetchImageUrls
      tags:
        - Files
      summary: Отримати зображення за URL
      description: |
        Отримати одне або кілька зображень із зовнішніх URL. Кожен URL перевіряється на
        атаки SSRF, обмежується за розміром і верифікується як дійсне зображення. Повертає URL для завантаження
        кожного успішно отриманого зображення. Обмеження до 200 запитів на годину.
      security:
        - bearerAuth: []
      requestBody:
        required: true
        content:
          application/json:
            schema:
              type: object
              required:
                - urls
              properties:
                urls:
                  type: array
                  items:
                    type: string
                    format: uri
                  minItems: 1
                  maxItems: 50
                  description: URLs of images to fetch (1-50)
      responses:
        '200':
          description: Fetch results for each URL
          content:
            application/json:
              schema:
                type: object
                properties:
                  results:
                    type: array
                    items:
                      type: object
                      properties:
                        success:
                          type: boolean
                        url:
                          type: string
                        filename:
                          type: string
                        contentType:
                          type: string
                        size:
                          type: integer
                        width:
                          type: integer
                        height:
                          type: integer
                        downloadUrl:
                          type: string
                        previewUrl:
                          type:
                            - string
                            - 'null'
                        error:
                          type: string
                          description: Present only when success is false
        '400':
          description: Invalid input
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Error'
        '401':
          description: Authentication required
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/UnauthorizedError'
  /api/v1/config/locale:
    get:
      operationId: getLocaleConfig
      tags:
        - System
      summary: Отримати локаль за замовчуванням
      description: Повертає налаштування локалі за замовчуванням. Публічний ендпоінт.
      security: []
      responses:
        '200':
          description: Default locale
          content:
            application/json:
              schema:
                type: object
                properties:
                  defaultLocale:
                    type: string
                    example: en
  /api/auth/login:
    post:
      operationId: login
      tags:
        - Auth
      summary: Увійти
      description: Автентифікуватися за іменем користувача та паролем. Повертає bearer-токен для наступних запитів API.
      security: []
      requestBody:
        required: true
        content:
          application/json:
            schema:
              type: object
              required:
                - username
                - password
              properties:
                username:
                  type: string
                password:
                  type: string
                  format: password
      responses:
        '200':
          description: Login successful
          content:
            application/json:
              schema:
                type: object
                properties:
                  token:
                    type: string
                  user:
                    type: object
                    properties:
                      id:
                        type: string
                      username:
                        type: string
                      role:
                        type: string
                      mustChangePassword:
                        type: boolean
                      permissions:
                        type: array
                        items:
                          type: string
                      teamName:
                        type: string
                  expiresAt:
                    type: string
                    format: date-time
        '400':
          description: Invalid input
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Error'
        '401':
          description: Invalid credentials
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/UnauthorizedError'
  /api/auth/logout:
    post:
      operationId: logout
      tags:
        - Auth
      summary: Вийти
      description: Анулювати поточний токен сесії. Після виходу токен не можна використовувати для подальших запитів.
      security:
        - bearerAuth: []
      responses:
        '200':
          description: Logged out
          content:
            application/json:
              schema:
                type: object
                properties:
                  ok:
                    type: boolean
        '401':
          description: Authentication required
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/UnauthorizedError'
  /api/auth/session:
    get:
      operationId: getSession
      tags:
        - Auth
      summary: Отримати поточну сесію
      description: Отримати деталі поточної сесії, зокрема профіль користувача, роль і дозволи. Повертає null у полі user, коли не автентифіковано.
      security: []
      responses:
        '200':
          description: Session info
          content:
            application/json:
              schema:
                type: object
                properties:
                  user:
                    type: object
                    properties:
                      id:
                        type: string
                      username:
                        type: string
                      role:
                        type: string
                      mustChangePassword:
                        type: boolean
                      permissions:
                        type: array
                        items:
                          type: string
                  expiresAt:
                    type:
                      - string
                      - 'null'
                    format: date-time
  /api/auth/change-password:
    post:
      operationId: changePassword
      tags:
        - Auth
      summary: Змінити пароль
      description: Змінити пароль автентифікованого користувача. Потребує поточного пароля для перевірки.
      security:
        - bearerAuth: []
      requestBody:
        required: true
        content:
          application/json:
            schema:
              type: object
              required:
                - currentPassword
                - newPassword
              properties:
                currentPassword:
                  type: string
                  format: password
                newPassword:
                  type: string
                  format: password
      responses:
        '200':
          description: Password changed
          content:
            application/json:
              schema:
                type: object
                properties:
                  ok:
                    type: boolean
        '400':
          description: Invalid input
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Error'
        '401':
          description: Authentication required
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/UnauthorizedError'
  /api/auth/users:
    get:
      operationId: listUsers
      tags:
        - Auth
      summary: Список користувачів
      description: Потребує дозволу users:manage.
      security:
        - bearerAuth: []
      responses:
        '200':
          description: List of users
          content:
            application/json:
              schema:
                type: object
                properties:
                  users:
                    type: array
                    items:
                      type: object
                      properties:
                        id:
                          type: string
                        username:
                          type: string
                        role:
                          type: string
                        team:
                          type: string
                        createdAt:
                          type: string
                          format: date-time
                  maxUsers:
                    type: integer
        '401':
          description: Authentication required
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/UnauthorizedError'
        '403':
          description: Insufficient permissions
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ForbiddenError'
  /api/auth/register:
    post:
      operationId: createUser
      tags:
        - Auth
      summary: Створити користувача
      description: |
        Requires users:manage permission. The assigned role must be within the actor's effective role authority, including hierarchy, permissions, tool access, and any API-key scope.
      security:
        - bearerAuth: []
      requestBody:
        required: true
        content:
          application/json:
            schema:
              type: object
              required:
                - username
                - password
              properties:
                username:
                  type: string
                password:
                  type: string
                  format: password
                role:
                  type: string
                  description: Built-in role (admin, editor, user) or custom role name
                  default: user
                team:
                  type: string
                  default: Default
      responses:
        '201':
          description: User created
          content:
            application/json:
              schema:
                type: object
                properties:
                  id:
                    type: string
                  username:
                    type: string
                  role:
                    type: string
                  team:
                    type: string
        '400':
          description: Invalid input
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Error'
        '401':
          description: Authentication required
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/UnauthorizedError'
        '403':
          description: Insufficient permissions
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ForbiddenError'
        '409':
          description: Username already exists
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ConflictError'
  /api/auth/users/{id}:
    put:
      operationId: updateUser
      tags:
        - Auth
      summary: Оновити користувача
      description: |
        Requires users:manage permission and authority over the target's current role. Any newly assigned role must also be within the actor's effective hierarchy, permissions, tool access, and API-key scope.
      security:
        - bearerAuth: []
      parameters:
        - name: id
          in: path
          required: true
          description: User UUID
          schema:
            type: string
      requestBody:
        required: true
        content:
          application/json:
            schema:
              type: object
              properties:
                role:
                  type: string
                  description: Built-in role (admin, editor, user) or custom role name
                team:
                  type: string
      responses:
        '200':
          description: User updated
          content:
            application/json:
              schema:
                type: object
                properties:
                  ok:
                    type: boolean
        '400':
          description: Invalid input
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Error'
        '401':
          description: Authentication required
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/UnauthorizedError'
        '403':
          description: Insufficient permissions
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ForbiddenError'
    delete:
      operationId: deleteUser
      tags:
        - Auth
      summary: Видалити користувача
      description: |
        Requires users:manage permission and authority over the target's current role, including hierarchy, permissions, tool access, and any API-key scope. Self-deletion is not allowed.
      security:
        - bearerAuth: []
      parameters:
        - name: id
          in: path
          required: true
          description: User UUID
          schema:
            type: string
      responses:
        '200':
          description: User deleted
          content:
            application/json:
              schema:
                type: object
                properties:
                  ok:
                    type: boolean
        '401':
          description: Authentication required
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/UnauthorizedError'
        '403':
          description: Insufficient permissions
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ForbiddenError'
  /api/auth/users/{id}/reset-password:
    post:
      operationId: resetUserPassword
      tags:
        - Auth
      summary: Скинути пароль користувача
      description: |
        Reset a local user's password. Requires users:manage permission and authority over the target's current role, including hierarchy, permissions, tool access, and any API-key scope.
      security:
        - bearerAuth: []
      parameters:
        - name: id
          in: path
          required: true
          description: User UUID
          schema:
            type: string
      requestBody:
        required: true
        content:
          application/json:
            schema:
              type: object
              required:
                - newPassword
              properties:
                newPassword:
                  type: string
                  format: password
      responses:
        '200':
          description: Password reset
          content:
            application/json:
              schema:
                type: object
                properties:
                  ok:
                    type: boolean
        '400':
          description: Invalid input
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Error'
        '401':
          description: Authentication required
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/UnauthorizedError'
        '403':
          description: Insufficient permissions
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ForbiddenError'
  /api/v1/api-keys:
    post:
      operationId: createApiKey
      tags:
        - API Keys
      summary: Створити API-ключ
      description: |
        Згенерувати новий API-ключ. Повний ключ (із префіксом si_) повертається лише один раз. Ключі зберігаються з хешуванням scrypt.
      security:
        - bearerAuth: []
      requestBody:
        required: true
        content:
          application/json:
            schema:
              type: object
              properties:
                name:
                  type: string
                  maxLength: 100
                  default: Default API Key
                permissions:
                  type: array
                  items:
                    type: string
                  description: Scoped permissions for this key
                expiresAt:
                  type: string
                  format: date-time
                  description: Optional expiration date
      responses:
        '201':
          description: API key created
          content:
            application/json:
              schema:
                type: object
                properties:
                  id:
                    type: string
                  key:
                    type: string
                    description: Full key (only shown once)
                  name:
                    type: string
                  permissions:
                    type:
                      - array
                      - 'null'
                    items:
                      type: string
                  expiresAt:
                    type:
                      - string
                      - 'null'
                    format: date-time
                  createdAt:
                    type: string
                    format: date-time
        '400':
          description: Invalid input
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Error'
        '401':
          description: Authentication required
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/UnauthorizedError'
    get:
      operationId: listApiKeys
      tags:
        - API Keys
      summary: Список API-ключів
      description: Повертає метадані ключа, але не повний ключ.
      security:
        - bearerAuth: []
      responses:
        '200':
          description: List of API keys
          content:
            application/json:
              schema:
                type: object
                properties:
                  apiKeys:
                    type: array
                    items:
                      type: object
                      properties:
                        id:
                          type: string
                        name:
                          type: string
                        permissions:
                          type:
                            - array
                            - 'null'
                          items:
                            type: string
                        createdAt:
                          type: string
                          format: date-time
                        lastUsedAt:
                          type:
                            - string
                            - 'null'
                          format: date-time
                        expiresAt:
                          type:
                            - string
                            - 'null'
                          format: date-time
        '401':
          description: Authentication required
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/UnauthorizedError'
  /api/v1/api-keys/{id}:
    delete:
      operationId: deleteApiKey
      tags:
        - API Keys
      summary: Видалити API-ключ
      description: Остаточно відкликати та видалити API-ключ. Після видалення ключ не можна використовувати для автентифікації.
      security:
        - bearerAuth: []
      parameters:
        - name: id
          in: path
          required: true
          description: API key UUID
          schema:
            type: string
      responses:
        '200':
          description: API key deleted
          content:
            application/json:
              schema:
                type: object
                properties:
                  ok:
                    type: boolean
        '401':
          description: Authentication required
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/UnauthorizedError'
  /api/v1/settings:
    get:
      operationId: getSettings
      tags:
        - Settings
      summary: Отримати всі налаштування
      description: Отримати всі системні налаштування як пари ключ-значення. Потребує автентифікації.
      security:
        - bearerAuth: []
      responses:
        '200':
          description: All settings wrapped in a settings object
          content:
            application/json:
              schema:
                type: object
                properties:
                  settings:
                    type: object
                    additionalProperties:
                      type: string
        '401':
          description: Authentication required
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/UnauthorizedError'
    put:
      operationId: updateSettings
      tags:
        - Settings
      summary: Оновити налаштування
      description: Приймає плоский об'єкт JSON із парами ключ-значення для встановлення. Потребує ролі адміністратора.
      security:
        - bearerAuth: []
      requestBody:
        required: true
        content:
          application/json:
            schema:
              type: object
              additionalProperties:
                type: string
      responses:
        '200':
          description: Settings updated
          content:
            application/json:
              schema:
                type: object
                properties:
                  ok:
                    type: boolean
                  updatedCount:
                    type: integer
        '400':
          description: Invalid input
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Error'
        '401':
          description: Authentication required
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/UnauthorizedError'
        '403':
          description: Admin access required
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ForbiddenError'
  /api/v1/settings/{key}:
    get:
      operationId: getSetting
      tags:
        - Settings
      summary: Отримати одне налаштування
      description: Отримати значення одного налаштування за іменем ключа. Повертає значення та мітку часу останнього оновлення.
      security:
        - bearerAuth: []
      parameters:
        - name: key
          in: path
          required: true
          description: Setting key name
          schema:
            type: string
      responses:
        '200':
          description: Setting value
          content:
            application/json:
              schema:
                type: object
                properties:
                  key:
                    type: string
                  value:
                    type: string
                  updatedAt:
                    type: string
                    format: date-time
        '401':
          description: Authentication required
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/UnauthorizedError'
        '404':
          description: Setting not found
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Error'
  /api/v1/teams:
    get:
      operationId: listTeams
      tags:
        - Teams
      summary: Список команд
      description: Потребує дозволу teams:manage.
      security:
        - bearerAuth: []
      responses:
        '200':
          description: List of teams
          content:
            application/json:
              schema:
                type: object
                properties:
                  teams:
                    type: array
                    items:
                      type: object
                      properties:
                        id:
                          type: string
                        name:
                          type: string
                        memberCount:
                          type: integer
                        createdAt:
                          type: string
                          format: date-time
        '401':
          description: Authentication required
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/UnauthorizedError'
        '403':
          description: Insufficient permissions
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ForbiddenError'
    post:
      operationId: createTeam
      tags:
        - Teams
      summary: Створити команду
      description: Потребує дозволу teams:manage.
      security:
        - bearerAuth: []
      requestBody:
        required: true
        content:
          application/json:
            schema:
              type: object
              required:
                - name
              properties:
                name:
                  type: string
      responses:
        '201':
          description: Team created
          content:
            application/json:
              schema:
                type: object
                properties:
                  id:
                    type: string
                  name:
                    type: string
        '400':
          description: Invalid input
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Error'
        '401':
          description: Authentication required
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/UnauthorizedError'
        '403':
          description: Insufficient permissions
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ForbiddenError'
        '409':
          description: Team name already exists
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ConflictError'
  /api/v1/teams/{id}:
    put:
      operationId: updateTeam
      tags:
        - Teams
      summary: Перейменувати команду
      description: Потребує дозволу teams:manage.
      security:
        - bearerAuth: []
      parameters:
        - name: id
          in: path
          required: true
          description: Team UUID
          schema:
            type: string
      requestBody:
        required: true
        content:
          application/json:
            schema:
              type: object
              required:
                - name
              properties:
                name:
                  type: string
      responses:
        '200':
          description: Team updated
          content:
            application/json:
              schema:
                type: object
                properties:
                  ok:
                    type: boolean
        '401':
          description: Authentication required
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/UnauthorizedError'
        '403':
          description: Insufficient permissions
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ForbiddenError'
        '404':
          description: Team not found
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Error'
        '409':
          description: Team name already exists
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ConflictError'
    delete:
      operationId: deleteTeam
      tags:
        - Teams
      summary: Видалити команду
      description: Потребує дозволу teams:manage.
      security:
        - bearerAuth: []
      parameters:
        - name: id
          in: path
          required: true
          description: Team UUID
          schema:
            type: string
      responses:
        '200':
          description: Team deleted
          content:
            application/json:
              schema:
                type: object
                properties:
                  ok:
                    type: boolean
        '401':
          description: Authentication required
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/UnauthorizedError'
        '403':
          description: Insufficient permissions
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ForbiddenError'
        '404':
          description: Team not found
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Error'
  /api/v1/config/auth:
    get:
      operationId: getAuthConfig
      tags:
        - System
      summary: Конфігурація автентифікації
      description: Перевірити, чи ввімкнено автентифікацію на цьому екземплярі. Публічний ендпоінт, який фронтенд використовує для визначення потоку входу.
      security: []
      responses:
        '200':
          description: Auth config
          content:
            application/json:
              schema:
                type: object
                properties:
                  authEnabled:
                    type: boolean
  /api/v1/jobs/{jobId}/progress:
    get:
      operationId: getJobProgress
      tags:
        - System
      summary: Прогрес завдання (SSE)
      description: Потік Server-Sent Events для відстеження тривалих завдань. Він надсилає кадри окремих файлів, пакетів і heartbeat та закривається одразу після кінцевої події завдання.
      security: []
      parameters:
        - name: jobId
          in: path
          required: true
          description: Job UUID from a tool processing or installation response
          schema:
            type: string
      responses:
        '200':
          description: |
            SSE stream of job progress frames. Single-file frames use `phase`, `percent`, optional `stage`, and terminal `result` or `error` fields. Batch frames use `status`, file counters, and `errors`. Heartbeat frames keep idle connections alive.
          content:
            text/event-stream:
              schema:
                oneOf:
                  - title: Single-file progress frame
                    type: object
                    required:
                      - jobId
                      - type
                      - phase
                      - percent
                    properties:
                      jobId:
                        type: string
                      type:
                        type: string
                        const: single
                      phase:
                        type: string
                        enum:
                          - processing
                          - complete
                          - failed
                      stage:
                        type: string
                      percent:
                        type: number
                        minimum: 0
                        maximum: 100
                      error:
                        type: string
                        description: Present on terminal failed frames
                      result:
                        type: object
                        additionalProperties: true
                        description: Present on terminal complete frames; contains the tool result
                  - title: Batch progress frame
                    type: object
                    required:
                      - jobId
                      - type
                      - status
                      - totalFiles
                      - completedFiles
                      - failedFiles
                      - errors
                    properties:
                      jobId:
                        type: string
                      type:
                        type: string
                        const: batch
                      status:
                        type: string
                        enum:
                          - processing
                          - completed
                          - failed
                      totalFiles:
                        type: integer
                        minimum: 0
                      completedFiles:
                        type: integer
                        minimum: 0
                      failedFiles:
                        type: integer
                        minimum: 0
                      currentFile:
                        type: string
                      errors:
                        type: array
                        items:
                          type: object
                          required:
                            - filename
                            - error
                          properties:
                            filename:
                              type: string
                            error:
                              type: string
                  - title: Heartbeat frame
                    type: object
                    required:
                      - type
                    properties:
                      type:
                        type: string
                        const: heartbeat
  /api/v1/admin/health:
    get:
      operationId: adminHealthCheck
      tags:
        - Admin
      summary: Адміністративна перевірка стану
      description: |
        Повна діагностика системи, зокрема стан бази даних, режим сховища, стан
        черги та доступність GPU. Потребує дозволу system:health.
      security:
        - bearerAuth: []
      responses:
        '200':
          description: Detailed system health
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/AdminHealthResponse'
        '401':
          description: Authentication required
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/UnauthorizedError'
        '403':
          description: Insufficient permissions
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ForbiddenError'
  /api/v1/config/analytics:
    get:
      operationId: getAnalyticsConfig
      tags:
        - Analytics
      summary: Отримати конфігурацію аналітики
      description: |
        Повертає фактичну конфігурацію провайдера аналітики. Аналітика
        увімкнена лише тоді, коли її дозволяють і компіляційне запікання, і
        налаштування екземпляра analyticsEnabled. Коли вимкнено, ключі, DSN та
        instanceId порожні. Публічний ендпоінт.
      security: []
      responses:
        '200':
          description: Analytics configuration
          content:
            application/json:
              schema:
                type: object
                properties:
                  enabled:
                    type: boolean
                    description: |
                      Effective analytics state. True only when both the
                      compile-time bake and the instance analyticsEnabled
                      setting are on. When false, the keys, DSN, and instanceId
                      are blank.
                  posthogApiKey:
                    type: string
                  posthogHost:
                    type: string
                  sentryDsn:
                    type: string
                  sentryDsnWeb:
                    type: string
                  posthogSampleRate:
                    type: number
                  instanceId:
                    type: string
  /api/v1/features:
    get:
      operationId: listFeatures
      tags:
        - Features
      summary: Список пакетів функцій
      description: |
        Повертає всі доступні пакети AI-функцій та їхній статус встановлення.
        У середовищах без Docker усі пакети відображаються як встановлені.
      security:
        - bearerAuth: []
      responses:
        '200':
          description: Feature bundle list
          content:
            application/json:
              schema:
                type: object
                properties:
                  bundles:
                    type: array
                    items:
                      type: object
                      properties:
                        id:
                          type: string
                        name:
                          type: string
                        description:
                          type: string
                          description: Human-readable description of the feature bundle
                        status:
                          type: string
                          enum:
                            - installed
                            - not_installed
                            - installing
                            - queued
                            - error
                        installedVersion:
                          type:
                            - string
                            - 'null'
                        estimatedSize:
                          type: string
                        downloadBytes:
                          type:
                            - integer
                            - 'null'
                          description: Compressed archive size for this host's architecture, when the manifest records it. Null in native (non-Docker) mode or when unmeasured.
                        installedBytes:
                          type:
                            - integer
                            - 'null'
                          description: Extracted on-disk size for this host's architecture, when the manifest records it.
                        compatibility:
                          type: string
                          enum:
                            - compatible
                            - incompatible
                            - invalid
                          description: OCR runtime compatibility; omitted for legacy bundles
                        compatibilityReason:
                          type:
                            - string
                            - 'null'
                          description: OCR runtime compatibility reason; omitted for legacy bundles
                        selectedTarget:
                          type:
                            - string
                            - 'null'
                          description: Selected OCR runtime target, such as linux-amd64-cpu-py312
                        missingDownloadBytes:
                          type:
                            - integer
                            - 'null'
                          description: Remaining OCR runtime download size for this host
                        requiredMemoryBytes:
                          type:
                            - integer
                            - 'null'
                          description: Minimum effective memory required by the selected OCR runtime; null for legacy bundles
                        effectiveMemoryBytes:
                          type:
                            - integer
                            - 'null'
                          description: Effective memory available to OCR (the configured container cgroup limit, otherwise host memory); null for legacy bundles
                        healthyGeneration:
                          type:
                            - string
                            - 'null'
                          description: Active immutable OCR generation identifier
                        availableQualities:
                          type: array
                          description: OCR quality tiers currently available on this host
                          items:
                            type: string
                            enum:
                              - fast
                              - balanced
                              - best
                        enablesTools:
                          type: array
                          items:
                            type: string
                        progress:
                          type:
                            - object
                            - 'null'
                          properties:
                            percent:
                              type: number
                            stage:
                              type: string
                        error:
                          type:
                            - string
                            - 'null'
        '401':
          description: Authentication required
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/UnauthorizedError'
  /api/v1/admin/features/{bundleId}/install:
    post:
      operationId: installFeature
      tags:
        - Features
      summary: Встановити пакет функцій
      description: |-
        Розпочніть асинхронну інсталяцію набору функцій ШІ. Завантаження моделей і
        налаштовує Python sidecar. Повертає jobId для відстеження прогресу
        через кінцеву точку SSE. Потрібен дозвіл features:manage.

        Для bundleId=ocr встановлення підтримується в офіційних контейнерах Linux amd64 і arm64.
        Він завантажує ціль, вибрану для цього хоста, перевіряє випуск
        підпис Ed25519 індексу та дайджести artifact/model, димові тести a
        поетапне незмінне покоління та атомарно активує його. OCR
        середовище виконання використовує CPU, навіть якщо контейнер має доступ NVIDIA GPU. The
        точний час виконання вимагає принаймні 4 GiB ефективної пам’яті (
        обмеження конфігурованого контейнера cgroup, інакше пам’ять хоста). Системи під цим підписом
        мінімальна сумісність відхиляються перед завантаженням з
        compatibilityReason=insufficient-memory. Fast Tesseract OCR не
        відповідно до цієї вимоги.

        Встановлення серіалізуються на стороні сервера: запит виконується під час іншого
        запущена інсталяція ставиться в чергу (queued=true), а не відхилена, і
        запускається автоматично, коли поточне встановлення завершується. Повторення
        запит на той самий пакет у польоті дедуплюється та повертає те саме
        jobId.
      security:
        - bearerAuth: []
      parameters:
        - name: bundleId
          in: path
          required: true
          description: Feature bundle identifier
          schema:
            type: string
      responses:
        '202':
          description: Installation started or queued
          content:
            application/json:
              schema:
                type: object
                properties:
                  jobId:
                    type: string
                    description: Job ID for tracking installation progress via SSE
                  queued:
                    type: boolean
                    description: True when the install was queued behind another running install instead of starting immediately.
        '401':
          description: Authentication required
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/UnauthorizedError'
        '403':
          description: Insufficient permissions
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ForbiddenError'
        '404':
          description: Unknown bundle ID
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Error'
        '409':
          description: Bundle already installed or OCR runtime unsupported on this host
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ConflictError'
        '503':
          description: OCR runtime trust key is not configured in this image
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Error'
  /api/v1/admin/features/{bundleId}/uninstall:
    post:
      operationId: uninstallFeature
      tags:
        - Features
      summary: Видалити пакет функцій
      description: |
        Видалити пакет AI-функцій і видалити його файли моделей (якщо вони не спільні
        з іншим встановленим пакетом). Потребує дозволу features:manage.
      security:
        - bearerAuth: []
      parameters:
        - name: bundleId
          in: path
          required: true
          description: Feature bundle identifier
          schema:
            type: string
      responses:
        '200':
          description: Bundle uninstalled
          content:
            application/json:
              schema:
                type: object
                properties:
                  ok:
                    type: boolean
        '401':
          description: Authentication required
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/UnauthorizedError'
        '403':
          description: Insufficient permissions
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ForbiddenError'
        '404':
          description: Unknown bundle ID
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Error'
        '409':
          description: Bundle is not installed
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ConflictError'
  /api/v1/admin/features/disk-usage:
    get:
      operationId: getFeatureDiskUsage
      tags:
        - Features
      summary: Використання диска AI-моделями
      description: |
        Повертає загальний обсяг дискового простору, зайнятий файлами AI-моделей.
        Потребує дозволу features:manage.
      security:
        - bearerAuth: []
      responses:
        '200':
          description: Disk usage
          content:
            application/json:
              schema:
                type: object
                properties:
                  totalBytes:
                    type: integer
                    description: Total bytes used by AI model files
        '401':
          description: Authentication required
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/UnauthorizedError'
        '403':
          description: Insufficient permissions
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ForbiddenError'
  /api/v1/audit-log:
    get:
      operationId: queryAuditLog
      tags:
        - Audit
      summary: Запит журналу аудиту
      description: |
        Постраничний журнал аудиту адміністративних дій. Підтримує фільтрацію за
        типом дії та діапазоном дат. Потребує дозволу audit:read.
      security:
        - bearerAuth: []
      parameters:
        - name: page
          in: query
          description: Page number (default 1)
          schema:
            type: integer
            minimum: 1
            default: 1
        - name: limit
          in: query
          description: Results per page (1-100, default 50)
          schema:
            type: integer
            minimum: 1
            maximum: 100
            default: 50
        - name: action
          in: query
          description: Filter by action type
          schema:
            type: string
        - name: from
          in: query
          description: Filter entries after this ISO 8601 date
          schema:
            type: string
            format: date-time
        - name: to
          in: query
          description: Filter entries before this ISO 8601 date
          schema:
            type: string
            format: date-time
      responses:
        '200':
          description: Paginated audit log entries
          content:
            application/json:
              schema:
                type: object
                properties:
                  entries:
                    type: array
                    items:
                      type: object
                      properties:
                        id:
                          type: string
                        actorId:
                          type:
                            - string
                            - 'null'
                        actorUsername:
                          type: string
                        action:
                          type: string
                        targetType:
                          type:
                            - string
                            - 'null'
                        targetId:
                          type:
                            - string
                            - 'null'
                        details:
                          type:
                            - object
                            - 'null'
                        ipAddress:
                          type:
                            - string
                            - 'null'
                        requestId:
                          type:
                            - string
                            - 'null'
                        createdAt:
                          type: string
                          format: date-time
                  total:
                    type: integer
                    description: Total matching entries
                  page:
                    type: integer
                  limit:
                    type: integer
        '401':
          description: Authentication required
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/UnauthorizedError'
        '403':
          description: Insufficient permissions
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ForbiddenError'
  /api/v1/roles:
    get:
      operationId: listRoles
      tags:
        - Roles
      summary: Список ролей
      description: |
        Список усіх ролей (вбудованих і власних) з їхніми дозволами та кількістю
        користувачів. Потребує дозволу audit:read.
      security:
        - bearerAuth: []
      responses:
        '200':
          description: Role list
          content:
            application/json:
              schema:
                type: object
                properties:
                  roles:
                    type: array
                    items:
                      type: object
                      properties:
                        id:
                          type: string
                        name:
                          type: string
                        description:
                          type: string
                          description: Human-readable description of the role
                        permissions:
                          type: array
                          items:
                            type: string
                        isBuiltin:
                          type: boolean
                        userCount:
                          type: integer
                        createdAt:
                          type: string
                          format: date-time
                        updatedAt:
                          type: string
                          format: date-time
        '401':
          description: Authentication required
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/UnauthorizedError'
        '403':
          description: Insufficient permissions
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ForbiddenError'
    post:
      operationId: createRole
      tags:
        - Roles
      summary: Створити власну роль
      description: |
        Create a new custom role with specified permissions. Role names must be
        2-30 characters, lowercase alphanumeric with hyphens and underscores.
        Requires security:manage permission. The new role's permissions and tool
        access must be contained by the actor's effective authority.
      security:
        - bearerAuth: []
      requestBody:
        required: true
        content:
          application/json:
            schema:
              type: object
              required:
                - name
                - permissions
              properties:
                name:
                  type: string
                  minLength: 2
                  maxLength: 30
                  pattern: ^[a-z0-9_-]+$
                  description: Lowercase role name
                description:
                  type: string
                  description: Human-readable description
                permissions:
                  type: array
                  items:
                    type: string
                  description: |
                    Permission strings. Valid values: tools:use, files:own, files:all,
                    apikeys:own, apikeys:all, pipelines:own, pipelines:all,
                    settings:read, settings:write, users:manage, teams:manage,
                    features:manage, system:health, audit:read, compliance:manage,
                    webhooks:manage, security:manage
      responses:
        '201':
          description: Role created
          content:
            application/json:
              schema:
                type: object
                properties:
                  id:
                    type: string
                  name:
                    type: string
                  description:
                    type: string
                    description: Human-readable description of the role
                  permissions:
                    type: array
                    items:
                      type: string
                  isBuiltin:
                    type: boolean
        '400':
          description: Validation error
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Error'
        '401':
          description: Authentication required
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/UnauthorizedError'
        '403':
          description: Insufficient permissions
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ForbiddenError'
        '409':
          description: Role name already exists
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ConflictError'
  /api/v1/roles/{id}:
    put:
      operationId: updateRole
      tags:
        - Roles
      summary: Оновити власну роль
      description: |
        Update name, description, or permissions of a custom role. Built-in
        roles cannot be modified. Requires security:manage permission and
        authority over the current role; the resulting permissions and tool
        access must remain contained by the actor's effective authority.
      security:
        - bearerAuth: []
      parameters:
        - name: id
          in: path
          required: true
          description: Role UUID
          schema:
            type: string
      requestBody:
        required: true
        content:
          application/json:
            schema:
              type: object
              properties:
                name:
                  type: string
                  minLength: 2
                  maxLength: 30
                description:
                  type: string
                  description: Updated description for the role
                permissions:
                  type: array
                  items:
                    type: string
      responses:
        '200':
          description: Role updated
          content:
            application/json:
              schema:
                type: object
                properties:
                  ok:
                    type: boolean
        '400':
          description: Validation error or cannot modify built-in role
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Error'
        '401':
          description: Authentication required
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/UnauthorizedError'
        '403':
          description: Insufficient permissions
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ForbiddenError'
        '404':
          description: Role not found
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Error'
        '409':
          description: Role name already exists
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ConflictError'
    delete:
      operationId: deleteRole
      tags:
        - Roles
      summary: Видалити власну роль
      description: |
        Delete a custom role. Users assigned to this role are reassigned to the
        default "user" role. Built-in roles cannot be deleted. Requires
        security:manage permission, authority over the current role, and
        authority to assign the fallback role.
      security:
        - bearerAuth: []
      parameters:
        - name: id
          in: path
          required: true
          description: Role UUID
          schema:
            type: string
      responses:
        '200':
          description: Role deleted
          content:
            application/json:
              schema:
                type: object
                properties:
                  ok:
                    type: boolean
        '400':
          description: Cannot delete built-in role
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Error'
        '401':
          description: Authentication required
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/UnauthorizedError'
        '403':
          description: Insufficient permissions
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ForbiddenError'
        '404':
          description: Role not found
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Error'
  /api/v1/tools/video/convert-video:
    post:
      operationId: convertVideo
      tags:
        - Tools
      summary: Конвертувати відео
      description: Конвертувати відео між MP4, MOV, WebM, AVI та MKV.
      security:
        - bearerAuth: []
      requestBody:
        required: true
        content:
          multipart/form-data:
            schema:
              type: object
              required:
                - file
              properties:
                file:
                  type: string
                  format: binary
                  description: Video file to process
                settings:
                  type: string
                  description: |
                    JSON string with options:
                    - `format` (string, default "mp4") - One of: mp4, mov, webm, avi, mkv
                    - `quality` (string, default "balanced") - One of: high, balanced, small
                clientJobId:
                  type: string
                  description: Client-provided job ID for SSE progress tracking
      responses:
        '202':
          description: Accepted for async processing. Track progress via SSE at /api/v1/jobs/{jobId}/progress.
          content:
            application/json:
              schema:
                type: object
                properties:
                  jobId:
                    type: string
                  async:
                    type: boolean
                    example: true
        '400':
          description: Invalid input
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Error'
        '401':
          description: Authentication required
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/UnauthorizedError'
        '422':
          description: Processing failed
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Error'
  /api/v1/tools/video/compress-video:
    post:
      operationId: compressVideo
      tags:
        - Tools
      summary: Стиснути відео
      description: Зменшити розмір файлу відео з контролем якості.
      security:
        - bearerAuth: []
      requestBody:
        required: true
        content:
          multipart/form-data:
            schema:
              type: object
              required:
                - file
              properties:
                file:
                  type: string
                  format: binary
                  description: Video file to process
                settings:
                  type: string
                  description: |
                    JSON string with options:
                    - `quality` (string, default "balanced") - One of: light, balanced, strong
                    - `resolution` (string, default "original") - One of: original, 1080p, 720p, 480p
                clientJobId:
                  type: string
                  description: Client-provided job ID for SSE progress tracking
      responses:
        '202':
          description: Accepted for async processing. Track progress via SSE at /api/v1/jobs/{jobId}/progress.
          content:
            application/json:
              schema:
                type: object
                properties:
                  jobId:
                    type: string
                  async:
                    type: boolean
                    example: true
        '400':
          description: Invalid input
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Error'
        '401':
          description: Authentication required
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/UnauthorizedError'
        '422':
          description: Processing failed
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Error'
  /api/v1/tools/video/trim-video:
    post:
      operationId: trimVideo
      tags:
        - Tools
      summary: Обрізати відео
      description: Вирізати кліп із відео.
      security:
        - bearerAuth: []
      requestBody:
        required: true
        content:
          multipart/form-data:
            schema:
              type: object
              required:
                - file
              properties:
                file:
                  type: string
                  format: binary
                  description: Video file to process
                settings:
                  type: string
                  description: |
                    JSON string with options:
                    - `startS` (number >= 0, default 0) - Start time in seconds
                    - `endS` (number, required) - End time in seconds (must be after startS)
                    - `precise` (boolean, default false) - Re-encode for frame-accurate cuts instead of keyframe seek
      responses:
        '200':
          description: Processed video
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ToolResponse'
        '400':
          description: Invalid input
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Error'
        '401':
          description: Authentication required
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/UnauthorizedError'
  /api/v1/tools/video/mute-video:
    post:
      operationId: muteVideo
      tags:
        - Tools
      summary: Прибрати звук з відео
      description: Видалити аудіодоріжку з відео.
      security:
        - bearerAuth: []
      requestBody:
        required: true
        content:
          multipart/form-data:
            schema:
              type: object
              required:
                - file
              properties:
                file:
                  type: string
                  format: binary
                  description: Video file to process
      responses:
        '200':
          description: Processed video
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ToolResponse'
        '400':
          description: Invalid input
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Error'
        '401':
          description: Authentication required
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/UnauthorizedError'
  /api/v1/tools/video/video-to-gif:
    post:
      operationId: videoToGif
      tags:
        - Tools
      summary: Відео в GIF
      description: Перетворити відеокліп на анімований GIF.
      security:
        - bearerAuth: []
      requestBody:
        required: true
        content:
          multipart/form-data:
            schema:
              type: object
              required:
                - file
              properties:
                file:
                  type: string
                  format: binary
                  description: Video file to process
                settings:
                  type: string
                  description: |
                    JSON string with options:
                    - `fps` (integer 1-30, default 12) - Output frame rate
                    - `width` (integer 64-1280, default 480) - Output width in pixels
                    - `startS` (number >= 0, default 0) - Start time in seconds
                    - `durationS` (number 0-60, default 5) - Duration in seconds
                clientJobId:
                  type: string
                  description: Client-provided job ID for SSE progress tracking
      responses:
        '202':
          description: Accepted for async processing. Track progress via SSE at /api/v1/jobs/{jobId}/progress.
          content:
            application/json:
              schema:
                type: object
                properties:
                  jobId:
                    type: string
                  async:
                    type: boolean
                    example: true
        '400':
          description: Invalid input
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Error'
        '401':
          description: Authentication required
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/UnauthorizedError'
        '422':
          description: Processing failed
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Error'
  /api/v1/tools/video/resize-video:
    post:
      operationId: resizeVideo
      tags:
        - Tools
      summary: Змінити розмір відео
      description: Масштабувати відео до нової роздільної здатності або пресетного розміру.
      security:
        - bearerAuth: []
      requestBody:
        required: true
        content:
          multipart/form-data:
            schema:
              type: object
              required:
                - file
              properties:
                file:
                  type: string
                  format: binary
                  description: Video file to process
                settings:
                  type: string
                  description: |
                    JSON string with options:
                    - `width` (integer 16-7680, optional) - Target width in pixels
                    - `height` (integer 16-4320, optional) - Target height in pixels
                    - `preset` (string, default "custom") - One of: custom, 2160p, 1440p, 1080p, 720p, 480p, 360p. When "custom", at least width or height is required
      responses:
        '200':
          description: Processed video
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ToolResponse'
        '400':
          description: Invalid input
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Error'
        '401':
          description: Authentication required
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/UnauthorizedError'
  /api/v1/tools/video/crop-video:
    post:
      operationId: cropVideo
      tags:
        - Tools
      summary: Обрізати відео
      description: Обрізати область із відео.
      security:
        - bearerAuth: []
      requestBody:
        required: true
        content:
          multipart/form-data:
            schema:
              type: object
              required:
                - file
              properties:
                file:
                  type: string
                  format: binary
                  description: Video file to process
                settings:
                  type: string
                  description: |
                    JSON string with options:
                    - `width` (integer >= 16, required) - Crop region width in pixels
                    - `height` (integer >= 16, required) - Crop region height in pixels
                    - `x` (integer >= 0, default 0) - Horizontal offset from top-left
                    - `y` (integer >= 0, default 0) - Vertical offset from top-left
      responses:
        '200':
          description: Processed video
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ToolResponse'
        '400':
          description: Invalid input
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Error'
        '401':
          description: Authentication required
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/UnauthorizedError'
  /api/v1/tools/video/rotate-video:
    post:
      operationId: rotateVideo
      tags:
        - Tools
      summary: Обернути відео
      description: Обернути або віддзеркалити відео.
      security:
        - bearerAuth: []
      requestBody:
        required: true
        content:
          multipart/form-data:
            schema:
              type: object
              required:
                - file
              properties:
                file:
                  type: string
                  format: binary
                  description: Video file to process
                settings:
                  type: string
                  description: |
                    JSON string with options:
                    - `transform` (string, required) - One of: cw90, ccw90, 180, hflip, vflip
      responses:
        '200':
          description: Processed video
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ToolResponse'
        '400':
          description: Invalid input
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Error'
        '401':
          description: Authentication required
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/UnauthorizedError'
  /api/v1/tools/video/change-fps:
    post:
      operationId: changeFps
      tags:
        - Tools
      summary: Змінити FPS
      description: Змінити частоту кадрів відео.
      security:
        - bearerAuth: []
      requestBody:
        required: true
        content:
          multipart/form-data:
            schema:
              type: object
              required:
                - file
              properties:
                file:
                  type: string
                  format: binary
                  description: Video file to process
                settings:
                  type: string
                  description: |
                    JSON string with options:
                    - `fps` (number 1-120, default 30) - Target frame rate
      responses:
        '200':
          description: Processed video
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ToolResponse'
        '400':
          description: Invalid input
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Error'
        '401':
          description: Authentication required
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/UnauthorizedError'
  /api/v1/tools/video/video-color:
    post:
      operationId: videoColor
      tags:
        - Tools
      summary: Колір відео
      description: Скоригувати яскравість, контраст, насиченість і гаму.
      security:
        - bearerAuth: []
      requestBody:
        required: true
        content:
          multipart/form-data:
            schema:
              type: object
              required:
                - file
              properties:
                file:
                  type: string
                  format: binary
                  description: Video file to process
                settings:
                  type: string
                  description: |
                    JSON string with options:
                    - `brightness` (number -1 to 1, default 0) - Brightness adjustment
                    - `contrast` (number 0-4, default 1) - Contrast multiplier
                    - `saturation` (number 0-3, default 1) - Saturation multiplier
                    - `gamma` (number 0.1-10, default 1) - Gamma correction
      responses:
        '200':
          description: Processed video
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ToolResponse'
        '400':
          description: Invalid input
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Error'
        '401':
          description: Authentication required
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/UnauthorizedError'
  /api/v1/tools/video/video-speed:
    post:
      operationId: videoSpeed
      tags:
        - Tools
      summary: Швидкість відео
      description: Прискорити або сповільнити відео.
      security:
        - bearerAuth: []
      requestBody:
        required: true
        content:
          multipart/form-data:
            schema:
              type: object
              required:
                - file
              properties:
                file:
                  type: string
                  format: binary
                  description: Video file to process
                settings:
                  type: string
                  description: |
                    JSON string with options:
                    - `factor` (number 0.25-4, default 2) - Speed multiplier (2 = double speed, 0.5 = half speed)
                    - `keepPitch` (boolean, default true) - Preserve audio pitch when changing speed
      responses:
        '200':
          description: Processed video
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ToolResponse'
        '400':
          description: Invalid input
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Error'
        '401':
          description: Authentication required
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/UnauthorizedError'
  /api/v1/tools/video/reverse-video:
    post:
      operationId: reverseVideo
      tags:
        - Tools
      summary: Реверсувати відео
      description: Відтворити відеокліп у зворотному напрямку. Обмежено кліпами до 5 хвилин.
      security:
        - bearerAuth: []
      requestBody:
        required: true
        content:
          multipart/form-data:
            schema:
              type: object
              required:
                - file
              properties:
                file:
                  type: string
                  format: binary
                  description: Video file to process
      responses:
        '200':
          description: Processed video
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ToolResponse'
        '400':
          description: Invalid input
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Error'
        '401':
          description: Authentication required
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/UnauthorizedError'
  /api/v1/tools/video/video-loudnorm:
    post:
      operationId: videoLoudnorm
      tags:
        - Tools
      summary: Нормалізація звуку відео
      description: Нормалізувати гучність звуку відео до трансляційного стандарту (EBU R128).
      security:
        - bearerAuth: []
      requestBody:
        required: true
        content:
          multipart/form-data:
            schema:
              type: object
              required:
                - file
              properties:
                file:
                  type: string
                  format: binary
                  description: Video file to process
      responses:
        '200':
          description: Processed video
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ToolResponse'
        '400':
          description: Invalid input
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Error'
        '401':
          description: Authentication required
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/UnauthorizedError'
  /api/v1/tools/video/aspect-pad:
    post:
      operationId: aspectPad
      tags:
        - Tools
      summary: Доповнення до співвідношення
      description: Додати суцільнокольорові смуги для відповідності цільовому співвідношенню сторін.
      security:
        - bearerAuth: []
      requestBody:
        required: true
        content:
          multipart/form-data:
            schema:
              type: object
              required:
                - file
              properties:
                file:
                  type: string
                  format: binary
                  description: Video file to process
                settings:
                  type: string
                  description: |
                    JSON string with options:
                    - `target` (string, default "9:16") - One of: 16:9, 9:16, 1:1, 4:3, 3:4
                    - `color` (string, default "#000000") - Hex color for padding bars (e.g. "#000000")
      responses:
        '200':
          description: Processed video
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ToolResponse'
        '400':
          description: Invalid input
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Error'
        '401':
          description: Authentication required
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/UnauthorizedError'
  /api/v1/tools/video/blur-pad:
    post:
      operationId: blurPad
      tags:
        - Tools
      summary: Розмите доповнення
      description: Заповнити смуги розмитою копією відео.
      security:
        - bearerAuth: []
      requestBody:
        required: true
        content:
          multipart/form-data:
            schema:
              type: object
              required:
                - file
              properties:
                file:
                  type: string
                  format: binary
                  description: Video file to process
                settings:
                  type: string
                  description: |
                    JSON string with options:
                    - `target` (string, default "16:9") - One of: 16:9, 9:16, 1:1, 4:3, 3:4
                    - `blur` (number 2-50, default 20) - Gaussian blur sigma for the background
      responses:
        '200':
          description: Processed video
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ToolResponse'
        '400':
          description: Invalid input
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Error'
        '401':
          description: Authentication required
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/UnauthorizedError'
  /api/v1/tools/video/watermark-video:
    post:
      operationId: watermarkVideo
      tags:
        - Tools
      summary: Водяний знак на відео
      description: Вбудувати текстовий водяний знак у кадри відео.
      security:
        - bearerAuth: []
      requestBody:
        required: true
        content:
          multipart/form-data:
            schema:
              type: object
              required:
                - file
              properties:
                file:
                  type: string
                  format: binary
                  description: Video file to process
                settings:
                  type: string
                  description: |
                    JSON string with options:
                    - `text` (string, required) - Watermark text (1-200 characters)
                    - `position` (string, default "br") - One of: tl, tc, tr, l, c, r, bl, bc, br
                    - `fontSize` (integer 8-120, default 36) - Font size in pixels
                    - `opacity` (number 0.05-1, default 0.5) - Watermark opacity
                    - `color` (string, default "#ffffff") - Hex color for the text (e.g. "#ffffff")
      responses:
        '200':
          description: Processed video
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ToolResponse'
        '400':
          description: Invalid input
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Error'
        '401':
          description: Authentication required
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/UnauthorizedError'
  /api/v1/tools/video/stabilize-video:
    post:
      operationId: stabilizeVideo
      tags:
        - Tools
      summary: Стабілізувати відео
      description: Зменшити тремтіння камери двопрохідною стабілізацією.
      security:
        - bearerAuth: []
      requestBody:
        required: true
        content:
          multipart/form-data:
            schema:
              type: object
              required:
                - file
              properties:
                file:
                  type: string
                  format: binary
                  description: Video file to process
                settings:
                  type: string
                  description: |
                    JSON string with options:
                    - `smoothing` (integer 5-60, default 15) - Smoothing window size in frames
                clientJobId:
                  type: string
                  description: Client-provided job ID for SSE progress tracking
      responses:
        '202':
          description: Accepted for async processing. Track progress via SSE at /api/v1/jobs/{jobId}/progress.
          content:
            application/json:
              schema:
                type: object
                properties:
                  jobId:
                    type: string
                  async:
                    type: boolean
                    example: true
        '400':
          description: Invalid input
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Error'
        '401':
          description: Authentication required
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/UnauthorizedError'
        '422':
          description: Processing failed
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Error'
  /api/v1/tools/video/gif-to-video:
    post:
      operationId: gifToVideo
      tags:
        - Tools
      summary: GIF у відео
      description: Конвертувати анімований GIF у відео MP4, WebM або MOV.
      security:
        - bearerAuth: []
      requestBody:
        required: true
        content:
          multipart/form-data:
            schema:
              type: object
              required:
                - file
              properties:
                file:
                  type: string
                  format: binary
                  description: GIF file to convert
                settings:
                  type: string
                  description: |
                    JSON string with options:
                    - `format` (string, default "mp4") - One of: mp4, webm, mov
      responses:
        '200':
          description: Converted video
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ToolResponse'
        '400':
          description: Invalid input
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Error'
        '401':
          description: Authentication required
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/UnauthorizedError'
  /api/v1/tools/video/video-to-webp:
    post:
      operationId: videoToWebp
      tags:
        - Tools
      summary: Відео в WebP
      description: Конвертувати відеокліп в анімоване зображення WebP.
      security:
        - bearerAuth: []
      requestBody:
        required: true
        content:
          multipart/form-data:
            schema:
              type: object
              required:
                - file
              properties:
                file:
                  type: string
                  format: binary
                  description: Video file to process
                settings:
                  type: string
                  description: |
                    JSON string with options:
                    - `fps` (integer 1-30, default 12) - Output frame rate
                    - `width` (integer 16-1920, default 480) - Output width in pixels
                    - `quality` (integer 1-100, default 75) - WebP compression quality
                    - `loop` (boolean, default true) - Loop the animation
      responses:
        '200':
          description: Converted animated WebP
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ToolResponse'
        '400':
          description: Invalid input
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Error'
        '401':
          description: Authentication required
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/UnauthorizedError'
  /api/v1/tools/video/video-to-frames:
    post:
      operationId: videoToFrames
      tags:
        - Tools
      summary: Відео в кадри
      description: Витягти кадри з відео у вигляді ZIP-архіву зображень.
      security:
        - bearerAuth: []
      requestBody:
        required: true
        content:
          multipart/form-data:
            schema:
              type: object
              required:
                - file
              properties:
                file:
                  type: string
                  format: binary
                  description: Video file to process
                settings:
                  type: string
                  description: |
                    JSON string with options:
                    - `mode` (string, default "all") - One of: all, nth, timestamps
                    - `n` (integer 2-1000, default 10) - Extract every Nth frame (used when mode is "nth")
                    - `timestamps` (string, default "") - Comma-separated timestamps in seconds (required when mode is "timestamps")
                    - `format` (string, default "png") - One of: png, jpg
      responses:
        '200':
          description: ZIP archive containing extracted frames
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ToolResponse'
        '400':
          description: Invalid input
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Error'
        '401':
          description: Authentication required
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/UnauthorizedError'
  /api/v1/tools/video/merge-videos:
    post:
      operationId: mergeVideos
      tags:
        - Tools
      summary: Об'єднати відео
      description: |
        З'єднати кілька відеокліпів в один файл MP4. Усі вхідні файли
        нормалізуються до роздільної здатності першого відео та 30 fps.
      security:
        - bearerAuth: []
      requestBody:
        required: true
        content:
          multipart/form-data:
            schema:
              type: object
              required:
                - file
              properties:
                file:
                  type: array
                  items:
                    type: string
                    format: binary
                  description: Two or more video files to merge (up to 10)
                clientJobId:
                  type: string
                  description: Client-provided job ID for SSE progress tracking
      responses:
        '202':
          description: Accepted for async processing. Track progress via SSE at /api/v1/jobs/{jobId}/progress.
          content:
            application/json:
              schema:
                type: object
                properties:
                  jobId:
                    type: string
                  async:
                    type: boolean
                    example: true
        '400':
          description: Invalid input
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Error'
        '401':
          description: Authentication required
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/UnauthorizedError'
        '422':
          description: Processing failed
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Error'
  /api/v1/tools/video/replace-audio:
    post:
      operationId: replaceAudio
      tags:
        - Tools
      summary: Замінити звук
      description: Замінити аудіодоріжку відео іншим файлом.
      security:
        - bearerAuth: []
      requestBody:
        required: true
        content:
          multipart/form-data:
            schema:
              type: object
              required:
                - file
              properties:
                file:
                  type: array
                  items:
                    type: string
                    format: binary
                  description: A video file followed by an audio file (exactly 2 files)
      responses:
        '200':
          description: Processed video
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ToolResponse'
        '400':
          description: Invalid input
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Error'
        '401':
          description: Authentication required
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/UnauthorizedError'
  /api/v1/tools/video/burn-subtitles:
    post:
      operationId: burnSubtitles
      tags:
        - Tools
      summary: Вшити субтитри
      description: Назавжди вбудувати субтитри в кадри відео.
      security:
        - bearerAuth: []
      requestBody:
        required: true
        content:
          multipart/form-data:
            schema:
              type: object
              required:
                - file
              properties:
                file:
                  type: array
                  items:
                    type: string
                    format: binary
                  description: A video file followed by a subtitle file (.srt, .vtt, .ass)
                settings:
                  type: string
                  description: |
                    JSON string with options:
                    - `fontSize` (integer 8-72, default 24) - Subtitle font size in pixels
                clientJobId:
                  type: string
                  description: Client-provided job ID for SSE progress tracking
      responses:
        '202':
          description: Accepted for async processing. Track progress via SSE at /api/v1/jobs/{jobId}/progress.
          content:
            application/json:
              schema:
                type: object
                properties:
                  jobId:
                    type: string
                  async:
                    type: boolean
                    example: true
        '400':
          description: Invalid input
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Error'
        '401':
          description: Authentication required
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/UnauthorizedError'
        '422':
          description: Processing failed
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Error'
  /api/v1/tools/video/embed-subtitles:
    post:
      operationId: embedSubtitles
      tags:
        - Tools
      summary: Вбудувати субтитри
      description: Мультиплексувати доріжку субтитрів у контейнер відео.
      security:
        - bearerAuth: []
      requestBody:
        required: true
        content:
          multipart/form-data:
            schema:
              type: object
              required:
                - file
              properties:
                file:
                  type: array
                  items:
                    type: string
                    format: binary
                  description: A video file followed by a subtitle file (.srt, .vtt, .ass)
                settings:
                  type: string
                  description: |
                    JSON string with options:
                    - `language` (string, default "eng") - ISO 639-2/B language code (3 lowercase letters, e.g. "eng", "fra", "deu")
      responses:
        '200':
          description: Processed video
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ToolResponse'
        '400':
          description: Invalid input
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Error'
        '401':
          description: Authentication required
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/UnauthorizedError'
  /api/v1/tools/video/extract-subtitles:
    post:
      operationId: extractSubtitles
      tags:
        - Tools
      summary: Витягти субтитри
      description: Витягти доріжку субтитрів із відео у вигляді файлу SRT.
      security:
        - bearerAuth: []
      requestBody:
        required: true
        content:
          multipart/form-data:
            schema:
              type: object
              required:
                - file
              properties:
                file:
                  type: string
                  format: binary
                  description: Video file with an embedded subtitle track
      responses:
        '200':
          description: Extracted subtitle file
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ToolResponse'
        '400':
          description: Invalid input
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Error'
        '401':
          description: Authentication required
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/UnauthorizedError'
  /api/v1/tools/video/images-to-video:
    post:
      operationId: imagesToVideo
      tags:
        - Tools
      summary: Зображення у відео
      description: Перетворити набір зображень на слайдшоу-відео.
      security:
        - bearerAuth: []
      requestBody:
        required: true
        content:
          multipart/form-data:
            schema:
              type: object
              required:
                - file
              properties:
                file:
                  type: array
                  items:
                    type: string
                    format: binary
                  description: Two or more image files (up to 60)
                settings:
                  type: string
                  description: |
                    JSON string with options:
                    - `secondsPerImage` (number 0.5-10, default 2) - Display duration per image in seconds
                    - `resolution` (string, default "720p") - One of: 1080p, 720p, square
                    - `fps` (integer 10-60, default 30) - Output frame rate
      responses:
        '200':
          description: Generated slideshow video
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ToolResponse'
        '400':
          description: Invalid input
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Error'
        '401':
          description: Authentication required
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/UnauthorizedError'
  /api/v1/tools/video/video-metadata:
    post:
      operationId: videoMetadata
      tags:
        - Tools
      summary: Очистити метадані відео
      description: Видалити метадані з відео та повідомити, що було знайдено.
      security:
        - bearerAuth: []
      requestBody:
        required: true
        content:
          multipart/form-data:
            schema:
              type: object
              required:
                - file
              properties:
                file:
                  type: string
                  format: binary
                  description: Video file to process
      responses:
        '200':
          description: Processed video with metadata report
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ToolResponse'
        '400':
          description: Invalid input
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Error'
        '401':
          description: Authentication required
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/UnauthorizedError'
  /api/v1/tools/video/auto-subtitles:
    post:
      operationId: autoSubtitles
      tags:
        - Tools
      summary: Автоматичні субтитри
      description: |
        Генерація субтитрів на основі AI з аудіодоріжок відео за допомогою
        faster-whisper. Підтримує автовизначення та 10 явних мов.
        Потребує встановленого пакета функцій transcription.
      security:
        - bearerAuth: []
      requestBody:
        required: true
        content:
          multipart/form-data:
            schema:
              type: object
              required:
                - file
              properties:
                file:
                  type: string
                  format: binary
                  description: Video file to transcribe
                settings:
                  type: string
                  description: |
                    JSON string with options:
                    - `language` (string, default "auto") - One of: auto, en, de, fr, es, zh, ja, ko, id, th, vi
                    - `format` (string, default "srt") - One of: srt, vtt
                clientJobId:
                  type: string
                  description: Client-provided job ID for SSE progress tracking
      responses:
        '202':
          description: Accepted for async processing. Track progress via SSE at /api/v1/jobs/{jobId}/progress.
          content:
            application/json:
              schema:
                type: object
                properties:
                  jobId:
                    type: string
                  async:
                    type: boolean
                    example: true
        '400':
          description: Invalid input
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Error'
        '401':
          description: Authentication required
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/UnauthorizedError'
        '422':
          description: Processing failed
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Error'
        '501':
          description: Feature not installed
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/FeatureNotInstalledError'
  /api/v1/tools/video/extract-audio:
    post:
      operationId: extractAudio
      tags:
        - Tools
      summary: Витягти звук
      description: Витягти аудіодоріжку з відео.
      security:
        - bearerAuth: []
      requestBody:
        required: true
        content:
          multipart/form-data:
            schema:
              type: object
              required:
                - file
              properties:
                file:
                  type: string
                  format: binary
                  description: Video file to extract audio from
                settings:
                  type: string
                  description: |
                    JSON string with options:
                    - `format` (string, default "mp3") - One of: mp3, wav, m4a, ogg
      responses:
        '200':
          description: Extracted audio file
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ToolResponse'
        '400':
          description: Invalid input
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Error'
        '401':
          description: Authentication required
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/UnauthorizedError'
  /api/v1/tools/audio/convert-audio:
    post:
      operationId: convertAudio
      tags:
        - Tools
      summary: Конвертувати звук
      description: Конвертувати аудіо між MP3, WAV, OGG та іншими форматами.
      security:
        - bearerAuth: []
      requestBody:
        required: true
        content:
          multipart/form-data:
            schema:
              type: object
              required:
                - file
              properties:
                file:
                  type: string
                  format: binary
                  description: Audio file to process
                settings:
                  type: string
                  description: |
                    JSON string with options:
                    - `format` (string, default "mp3") - One of: mp3, wav, ogg, flac, m4a
                    - `bitrateKbps` (integer 32-320, default 192) - Output bitrate in kbps
      responses:
        '200':
          description: Processed audio
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ToolResponse'
        '400':
          description: Invalid input
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Error'
        '401':
          description: Authentication required
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/UnauthorizedError'
  /api/v1/tools/audio/trim-audio:
    post:
      operationId: trimAudio
      tags:
        - Tools
      summary: Обрізати звук
      description: Вирізати фрагмент з аудіофайлу.
      security:
        - bearerAuth: []
      requestBody:
        required: true
        content:
          multipart/form-data:
            schema:
              type: object
              required:
                - file
              properties:
                file:
                  type: string
                  format: binary
                  description: Audio file to process
                settings:
                  type: string
                  description: |
                    JSON string with options:
                    - `startS` (number, min 0, default 0) - Start time in seconds
                    - `endS` (number, required) - End time in seconds (must be after start)
      responses:
        '200':
          description: Processed audio
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ToolResponse'
        '400':
          description: Invalid input
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Error'
        '401':
          description: Authentication required
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/UnauthorizedError'
  /api/v1/tools/audio/volume-adjust:
    post:
      operationId: volumeAdjust
      tags:
        - Tools
      summary: Регулювання гучності
      description: Збільшити або зменшити гучність аудіо на фіксоване підсилення.
      security:
        - bearerAuth: []
      requestBody:
        required: true
        content:
          multipart/form-data:
            schema:
              type: object
              required:
                - file
              properties:
                file:
                  type: string
                  format: binary
                  description: Audio file to process
                settings:
                  type: string
                  description: |
                    JSON string with options:
                    - `gainDb` (number -30 to 30, default 3) - Volume adjustment in decibels
      responses:
        '200':
          description: Processed audio
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ToolResponse'
        '400':
          description: Invalid input
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Error'
        '401':
          description: Authentication required
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/UnauthorizedError'
  /api/v1/tools/audio/normalize-audio:
    post:
      operationId: normalizeAudio
      tags:
        - Tools
      summary: Нормалізувати звук
      description: Вирівняти гучність до трансляційних стандартних рівнів (EBU R128, -16 LUFS).
      security:
        - bearerAuth: []
      requestBody:
        required: true
        content:
          multipart/form-data:
            schema:
              type: object
              required:
                - file
              properties:
                file:
                  type: string
                  format: binary
                  description: Audio file to process
      responses:
        '200':
          description: Processed audio
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ToolResponse'
        '400':
          description: Invalid input
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Error'
        '401':
          description: Authentication required
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/UnauthorizedError'
  /api/v1/tools/audio/fade-audio:
    post:
      operationId: fadeAudio
      tags:
        - Tools
      summary: Затухання звуку
      description: Додати ефекти наростання та затухання до аудіо.
      security:
        - bearerAuth: []
      requestBody:
        required: true
        content:
          multipart/form-data:
            schema:
              type: object
              required:
                - file
              properties:
                file:
                  type: string
                  format: binary
                  description: Audio file to process
                settings:
                  type: string
                  description: |
                    JSON string with options:
                    - `fadeInS` (number 0-30, default 1) - Fade-in duration in seconds
                    - `fadeOutS` (number 0-30, default 1) - Fade-out duration in seconds
      responses:
        '200':
          description: Processed audio
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ToolResponse'
        '400':
          description: Invalid input
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Error'
        '401':
          description: Authentication required
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/UnauthorizedError'
  /api/v1/tools/audio/reverse-audio:
    post:
      operationId: reverseAudio
      tags:
        - Tools
      summary: Реверсувати звук
      description: Відтворити аудіо у зворотному напрямку.
      security:
        - bearerAuth: []
      requestBody:
        required: true
        content:
          multipart/form-data:
            schema:
              type: object
              required:
                - file
              properties:
                file:
                  type: string
                  format: binary
                  description: Audio file to process
      responses:
        '200':
          description: Processed audio
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ToolResponse'
        '400':
          description: Invalid input
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Error'
        '401':
          description: Authentication required
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/UnauthorizedError'
  /api/v1/tools/audio/audio-speed:
    post:
      operationId: audioSpeed
      tags:
        - Tools
      summary: Швидкість звуку
      description: Прискорити або сповільнити відтворення аудіо.
      security:
        - bearerAuth: []
      requestBody:
        required: true
        content:
          multipart/form-data:
            schema:
              type: object
              required:
                - file
              properties:
                file:
                  type: string
                  format: binary
                  description: Audio file to process
                settings:
                  type: string
                  description: |
                    JSON string with options:
                    - `factor` (number 0.25-4, default 1.5) - Speed multiplier (0.25 = quarter speed, 4 = quadruple speed)
      responses:
        '200':
          description: Processed audio
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ToolResponse'
        '400':
          description: Invalid input
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Error'
        '401':
          description: Authentication required
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/UnauthorizedError'
  /api/v1/tools/audio/pitch-shift:
    post:
      operationId: pitchShift
      tags:
        - Tools
      summary: Зсув висоти тону
      description: Підвищити або знизити висоту тону на півтони без зміни швидкості.
      security:
        - bearerAuth: []
      requestBody:
        required: true
        content:
          multipart/form-data:
            schema:
              type: object
              required:
                - file
              properties:
                file:
                  type: string
                  format: binary
                  description: Audio file to process
                settings:
                  type: string
                  description: |
                    JSON string with options:
                    - `semitones` (integer -12 to 12, default 3) - Semitones to shift (must be nonzero)
      responses:
        '200':
          description: Processed audio
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ToolResponse'
        '400':
          description: Invalid input
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Error'
        '401':
          description: Authentication required
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/UnauthorizedError'
  /api/v1/tools/audio/audio-channels:
    post:
      operationId: audioChannels
      tags:
        - Tools
      summary: Аудіоканали
      description: Конвертувати між моно, стерео або поміняти місцями лівий і правий канали.
      security:
        - bearerAuth: []
      requestBody:
        required: true
        content:
          multipart/form-data:
            schema:
              type: object
              required:
                - file
              properties:
                file:
                  type: string
                  format: binary
                  description: Audio file to process
                settings:
                  type: string
                  description: |
                    JSON string with options:
                    - `mode` (string, required) - One of: stereo-to-mono, mono-to-stereo, swap
      responses:
        '200':
          description: Processed audio
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ToolResponse'
        '400':
          description: Invalid input
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Error'
        '401':
          description: Authentication required
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/UnauthorizedError'
  /api/v1/tools/audio/silence-removal:
    post:
      operationId: removeSilence
      tags:
        - Tools
      summary: Видалення тиші
      description: Видалити мовчазні ділянки з аудіо.
      security:
        - bearerAuth: []
      requestBody:
        required: true
        content:
          multipart/form-data:
            schema:
              type: object
              required:
                - file
              properties:
                file:
                  type: string
                  format: binary
                  description: Audio file to process
                settings:
                  type: string
                  description: |
                    JSON string with options:
                    - `thresholdDb` (number -80 to -20, default -50) - Silence threshold in dB
                    - `minSilenceS` (number 0.1-5, default 0.5) - Minimum silence duration in seconds to remove
      responses:
        '200':
          description: Processed audio
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ToolResponse'
        '400':
          description: Invalid input
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Error'
        '401':
          description: Authentication required
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/UnauthorizedError'
  /api/v1/tools/audio/noise-reduction:
    post:
      operationId: reduceNoise
      tags:
        - Tools
      summary: Зменшення шуму
      description: Зменшити фоновий шум за допомогою FFT-шумозаглушення.
      security:
        - bearerAuth: []
      requestBody:
        required: true
        content:
          multipart/form-data:
            schema:
              type: object
              required:
                - file
              properties:
                file:
                  type: string
                  format: binary
                  description: Audio file to process
                settings:
                  type: string
                  description: |
                    JSON string with options:
                    - `strength` (string, default "medium") - One of: light, medium, strong
      responses:
        '200':
          description: Processed audio
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ToolResponse'
        '400':
          description: Invalid input
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Error'
        '401':
          description: Authentication required
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/UnauthorizedError'
  /api/v1/tools/audio/merge-audio:
    post:
      operationId: mergeAudio
      tags:
        - Tools
      summary: Об'єднати звук
      description: Об'єднати кілька аудіофайлів в одну послідовну доріжку.
      security:
        - bearerAuth: []
      requestBody:
        required: true
        content:
          multipart/form-data:
            schema:
              type: object
              required:
                - file
              properties:
                file:
                  type: array
                  items:
                    type: string
                    format: binary
                  description: Two or more audio files to merge (max 10)
                settings:
                  type: string
                  description: |
                    JSON string with options:
                    - `format` (string, default "mp3") - One of: mp3, wav, flac, m4a
      responses:
        '200':
          description: Processed audio
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ToolResponse'
        '400':
          description: Invalid input
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Error'
        '401':
          description: Authentication required
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/UnauthorizedError'
  /api/v1/tools/audio/split-audio:
    post:
      operationId: splitAudio
      tags:
        - Tools
      summary: Розділити звук
      description: |
        Розділити аудіо за часом, рівними частинами або визначенням тиші. Повертає
        ToolResponse з downloadUrl, що вказує на ZIP-архів сегментів.
      security:
        - bearerAuth: []
      requestBody:
        required: true
        content:
          multipart/form-data:
            schema:
              type: object
              required:
                - file
              properties:
                file:
                  type: string
                  format: binary
                  description: Audio file to split
                settings:
                  type: string
                  description: |
                    JSON string with options:
                    - `mode` (string, default "time") - One of: time, parts, silence
                    - `segmentS` (number 1-3600, default 60) - Segment length in seconds (mode=time)
                    - `parts` (integer 2-20, default 2) - Number of equal parts (mode=parts)
                    - `thresholdDb` (number -80 to -20, default -40) - Silence threshold in dB (mode=silence)
                    - `minSilenceS` (number 0.1-10, default 0.3) - Minimum silence gap in seconds (mode=silence)
      responses:
        '200':
          description: Processed audio (downloadUrl points to a ZIP archive)
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ToolResponse'
        '400':
          description: Invalid input
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Error'
        '401':
          description: Authentication required
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/UnauthorizedError'
  /api/v1/tools/audio/ringtone-maker:
    post:
      operationId: ringtoneMaker
      tags:
        - Tools
      summary: Створювач рингтонів
      description: Створити кліп рингтону (.m4r) з будь-якого аудіофайлу.
      security:
        - bearerAuth: []
      requestBody:
        required: true
        content:
          multipart/form-data:
            schema:
              type: object
              required:
                - file
              properties:
                file:
                  type: string
                  format: binary
                  description: Audio file to process
                settings:
                  type: string
                  description: |
                    JSON string with options:
                    - `startS` (number, min 0, default 0) - Start time in seconds
                    - `durationS` (number 1-30, default 30) - Clip duration in seconds
      responses:
        '200':
          description: Processed audio
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ToolResponse'
        '400':
          description: Invalid input
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Error'
        '401':
          description: Authentication required
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/UnauthorizedError'
  /api/v1/tools/audio/waveform-image:
    post:
      operationId: waveformImage
      tags:
        - Tools
      summary: Зображення хвилі
      description: Згенерувати візуалізацію форми хвилі як зображення PNG з аудіофайлу.
      security:
        - bearerAuth: []
      requestBody:
        required: true
        content:
          multipart/form-data:
            schema:
              type: object
              required:
                - file
              properties:
                file:
                  type: string
                  format: binary
                  description: Audio file to process
                settings:
                  type: string
                  description: |
                    JSON string with options:
                    - `width` (integer 256-3840, default 1024) - Image width in pixels
                    - `height` (integer 64-1080, default 256) - Image height in pixels
                    - `color` (string, default "#4f46e5") - Waveform hex color (e.g. "#4f46e5")
      responses:
        '200':
          description: Processed image
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ToolResponse'
        '400':
          description: Invalid input
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Error'
        '401':
          description: Authentication required
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/UnauthorizedError'
  /api/v1/tools/audio/audio-metadata:
    post:
      operationId: audioMetadata
      tags:
        - Tools
      summary: Метадані аудіо
      description: |
        Переглянути, редагувати або видалити аудіотеги (ID3). Відповідь містить
        об'єкт `metadata` з container, durationS, bitrateKbps та tags
        разом зі стандартними полями ToolResponse.
      security:
        - bearerAuth: []
      requestBody:
        required: true
        content:
          multipart/form-data:
            schema:
              type: object
              required:
                - file
              properties:
                file:
                  type: string
                  format: binary
                  description: Audio file to process
                settings:
                  type: string
                  description: |
                    JSON string with options:
                    - `strip` (boolean, default false) - Remove all existing metadata
                    - `title` (string, optional, max 500) - Set the title tag
                    - `artist` (string, optional, max 500) - Set the artist tag
                    - `album` (string, optional, max 500) - Set the album tag
      responses:
        '200':
          description: Processed audio with metadata
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ToolResponse'
        '400':
          description: Invalid input
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Error'
        '401':
          description: Authentication required
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/UnauthorizedError'
  /api/v1/tools/audio/transcribe-audio:
    post:
      operationId: transcribeAudio
      tags:
        - Tools
      summary: Транскрибувати звук
      description: |
        Транскрипція мовлення в текст на основі AI за допомогою faster-whisper. Виводить
        простий текст або формати субтитрів SRT чи VTT. Потребує встановленого пакета
        функцій transcription.
      security:
        - bearerAuth: []
      requestBody:
        required: true
        content:
          multipart/form-data:
            schema:
              type: object
              required:
                - file
              properties:
                file:
                  type: string
                  format: binary
                  description: Audio file to transcribe
                settings:
                  type: string
                  description: |
                    JSON string with options:
                    - `language` (string, default "auto") - One of: auto, en, de, fr, es, zh, ja, ko, id, th, vi
                    - `outputFormat` (string, default "txt") - One of: txt, srt, vtt
                clientJobId:
                  type: string
                  description: Client-provided job ID for SSE progress tracking
      responses:
        '202':
          description: Accepted for async processing. Track progress via SSE at /api/v1/jobs/{jobId}/progress.
          content:
            application/json:
              schema:
                type: object
                properties:
                  jobId:
                    type: string
                  async:
                    type: boolean
                    example: true
        '400':
          description: Invalid input
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Error'
        '401':
          description: Authentication required
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/UnauthorizedError'
        '422':
          description: Processing failed
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Error'
        '501':
          description: Feature not installed
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/FeatureNotInstalledError'
  /api/v1/tools/pdf/merge-pdf:
    post:
      operationId: mergePdf
      tags:
        - Tools
      summary: Об'єднати PDF
      description: Об'єднати кілька PDF в один. Потребує щонайменше двох файлів.
      security:
        - bearerAuth: []
      requestBody:
        required: true
        content:
          multipart/form-data:
            schema:
              type: object
              required:
                - file
              properties:
                file:
                  type: array
                  items:
                    type: string
                    format: binary
                  description: Two or more PDF files to merge (max 20)
      responses:
        '200':
          description: Merged PDF
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ToolResponse'
        '400':
          description: Invalid input
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Error'
        '401':
          description: Authentication required
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/UnauthorizedError'
  /api/v1/tools/pdf/split-pdf:
    post:
      operationId: splitPdf
      tags:
        - Tools
      summary: Розділити PDF
      description: |
        Витягти сторінки або розділити на частини. У режимі "range" повертає один PDF
        з обраними сторінками. У режимі "every" розділяє на частини по N сторінок
        і повертає ZIP-архів із частинами.
      security:
        - bearerAuth: []
      requestBody:
        required: true
        content:
          multipart/form-data:
            schema:
              type: object
              required:
                - file
              properties:
                file:
                  type: string
                  format: binary
                  description: PDF file to split
                settings:
                  type: string
                  description: |
                    JSON string with options:
                    - `mode` (string, default "range") - One of: range, every
                    - `range` (string, optional) - Page range in qpdf syntax, e.g. "1-5,8,10-z" (required when mode is "range")
                    - `everyN` (integer 1-500, optional) - Split into chunks of N pages (required when mode is "every")
      responses:
        '200':
          description: Split PDF or ZIP archive of parts
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ToolResponse'
        '400':
          description: Invalid input
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Error'
        '401':
          description: Authentication required
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/UnauthorizedError'
  /api/v1/tools/pdf/compress-pdf:
    post:
      operationId: compressPdf
      tags:
        - Tools
      summary: Стиснути PDF
      description: |
        Зменшити розмір файлу PDF шляхом даунсемплінгу вбудованих зображень. У режимі "quality"
        повзунок 1-100 контролює DPI зображень. У режимі "targetSize" бінарний пошук
        знаходить найвищий DPI, що вкладається в цільовий розмір. Якщо стиснення збільшило б
        файл, повертаються незмінені оригінальні байти.
      security:
        - bearerAuth: []
      requestBody:
        required: true
        content:
          multipart/form-data:
            schema:
              type: object
              required:
                - file
              properties:
                file:
                  type: string
                  format: binary
                  description: PDF file to compress
                settings:
                  type: string
                  description: |
                    JSON string with options:
                    - `mode` (string, default "quality") - One of: quality, targetSize
                    - `quality` (integer 1-100, optional) - Compression quality (higher = less compression). Used in quality mode
                    - `targetSizeKb` (number, optional) - Target file size in kilobytes. Used in targetSize mode
      responses:
        '200':
          description: Compressed PDF
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ToolResponse'
        '400':
          description: Invalid input
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Error'
        '401':
          description: Authentication required
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/UnauthorizedError'
  /api/v1/tools/pdf/rotate-pdf:
    post:
      operationId: rotatePdf
      tags:
        - Tools
      summary: Обернути PDF
      description: Обернути сторінки PDF на 90, 180 або 270 градусів.
      security:
        - bearerAuth: []
      requestBody:
        required: true
        content:
          multipart/form-data:
            schema:
              type: object
              required:
                - file
              properties:
                file:
                  type: string
                  format: binary
                  description: PDF file to process
                settings:
                  type: string
                  description: |
                    JSON string with options:
                    - `angle` (integer, default 90) - One of: 90, 180, 270
                    - `range` (string, default "1-z") - Page range in qpdf syntax, e.g. "1-5,8" ("1-z" = all pages)
      responses:
        '200':
          description: Rotated PDF
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ToolResponse'
        '400':
          description: Invalid input
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Error'
        '401':
          description: Authentication required
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/UnauthorizedError'
  /api/v1/tools/pdf/extract-pages:
    post:
      operationId: extractPages
      tags:
        - Tools
      summary: Витягти сторінки
      description: Витягти обрані сторінки в новий PDF.
      security:
        - bearerAuth: []
      requestBody:
        required: true
        content:
          multipart/form-data:
            schema:
              type: object
              required:
                - file
              properties:
                file:
                  type: string
                  format: binary
                  description: PDF file to extract from
                settings:
                  type: string
                  description: |
                    JSON string with options:
                    - `range` (string, required) - Page range in qpdf syntax, e.g. "1-5,8,10-z"
      responses:
        '200':
          description: Extracted pages as a new PDF
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ToolResponse'
        '400':
          description: Invalid input
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Error'
        '401':
          description: Authentication required
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/UnauthorizedError'
  /api/v1/tools/pdf/remove-pages:
    post:
      operationId: removePages
      tags:
        - Tools
      summary: Видалити сторінки
      description: Видалити конкретні сторінки з PDF. Не можна видалити всі сторінки.
      security:
        - bearerAuth: []
      requestBody:
        required: true
        content:
          multipart/form-data:
            schema:
              type: object
              required:
                - file
              properties:
                file:
                  type: string
                  format: binary
                  description: PDF file to process
                settings:
                  type: string
                  description: |
                    JSON string with options:
                    - `pages` (string, required) - Page range to remove in qpdf syntax, e.g. "3,5-7"
      responses:
        '200':
          description: PDF with pages removed
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ToolResponse'
        '400':
          description: Invalid input
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Error'
        '401':
          description: Authentication required
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/UnauthorizedError'
  /api/v1/tools/pdf/organize-pdf:
    post:
      operationId: organizePdf
      tags:
        - Tools
      summary: Упорядкувати PDF
      description: Переупорядкувати сторінки з явним порядком сторінок.
      security:
        - bearerAuth: []
      requestBody:
        required: true
        content:
          multipart/form-data:
            schema:
              type: object
              required:
                - file
              properties:
                file:
                  type: string
                  format: binary
                  description: PDF file to reorganize
                settings:
                  type: string
                  description: |
                    JSON string with options:
                    - `order` (string, required) - Desired page order in qpdf syntax, e.g. "3,1,2,5-z"
      responses:
        '200':
          description: Reorganized PDF
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ToolResponse'
        '400':
          description: Invalid input
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Error'
        '401':
          description: Authentication required
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/UnauthorizedError'
  /api/v1/tools/pdf/protect-pdf:
    post:
      operationId: protectPdf
      tags:
        - Tools
      summary: Захистити PDF
      description: Додати захист паролем із шифруванням AES-256. Паролі приховуються в журналах аудиту.
      security:
        - bearerAuth: []
      requestBody:
        required: true
        content:
          multipart/form-data:
            schema:
              type: object
              required:
                - file
              properties:
                file:
                  type: string
                  format: binary
                  description: PDF file to protect
                settings:
                  type: string
                  description: |
                    JSON string with options:
                    - `userPassword` (string 1-256 chars, required) - Password required to open the PDF
                    - `ownerPassword` (string 1-256 chars, optional) - Owner password for permissions; defaults to userPassword if omitted
      responses:
        '200':
          description: Encrypted PDF
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ToolResponse'
        '400':
          description: Invalid input
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Error'
        '401':
          description: Authentication required
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/UnauthorizedError'
  /api/v1/tools/pdf/unlock-pdf:
    post:
      operationId: unlockPdf
      tags:
        - Tools
      summary: Розблокувати PDF
      description: Видалити захист паролем із PDF. Потрібно надати правильний пароль. Паролі приховуються в журналах аудиту.
      security:
        - bearerAuth: []
      requestBody:
        required: true
        content:
          multipart/form-data:
            schema:
              type: object
              required:
                - file
              properties:
                file:
                  type: string
                  format: binary
                  description: Password-protected PDF file
                settings:
                  type: string
                  description: |
                    JSON string with options:
                    - `password` (string 1-256 chars, required) - Password to decrypt the PDF
      responses:
        '200':
          description: Decrypted PDF
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ToolResponse'
        '400':
          description: Invalid input
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Error'
        '401':
          description: Authentication required
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/UnauthorizedError'
  /api/v1/tools/pdf/repair-pdf:
    post:
      operationId: repairPdf
      tags:
        - Tools
      summary: Відновити PDF
      description: Спробувати відновити пошкоджений або зіпсований PDF. Структурна валідація вхідних даних пропускається, щоб дозволити некоректні файли.
      security:
        - bearerAuth: []
      requestBody:
        required: true
        content:
          multipart/form-data:
            schema:
              type: object
              required:
                - file
              properties:
                file:
                  type: string
                  format: binary
                  description: Damaged PDF file to repair
      responses:
        '200':
          description: Repaired PDF
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ToolResponse'
        '400':
          description: Invalid input
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Error'
        '401':
          description: Authentication required
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/UnauthorizedError'
  /api/v1/tools/pdf/linearize-pdf:
    post:
      operationId: linearizePdf
      tags:
        - Tools
      summary: Веб-оптимізація PDF
      description: Лінеаризувати PDF для швидкого веб-перегляду (прогресивне завантаження).
      security:
        - bearerAuth: []
      requestBody:
        required: true
        content:
          multipart/form-data:
            schema:
              type: object
              required:
                - file
              properties:
                file:
                  type: string
                  format: binary
                  description: PDF file to linearize
      responses:
        '200':
          description: Linearized PDF
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ToolResponse'
        '400':
          description: Invalid input
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Error'
        '401':
          description: Authentication required
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/UnauthorizedError'
  /api/v1/tools/pdf/grayscale-pdf:
    post:
      operationId: grayscalePdf
      tags:
        - Tools
      summary: PDF у відтінках сірого
      description: Перетворити всі кольори в PDF на відтінки сірого.
      security:
        - bearerAuth: []
      requestBody:
        required: true
        content:
          multipart/form-data:
            schema:
              type: object
              required:
                - file
              properties:
                file:
                  type: string
                  format: binary
                  description: PDF file to convert
      responses:
        '200':
          description: Grayscale PDF
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ToolResponse'
        '400':
          description: Invalid input
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Error'
        '401':
          description: Authentication required
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/UnauthorizedError'
  /api/v1/tools/pdf/pdfa-convert:
    post:
      operationId: pdfaConvert
      tags:
        - Tools
      summary: Конвертер PDF/A
      description: Конвертувати PDF в архівний формат PDF/A-2 для довготривалого зберігання.
      security:
        - bearerAuth: []
      requestBody:
        required: true
        content:
          multipart/form-data:
            schema:
              type: object
              required:
                - file
              properties:
                file:
                  type: string
                  format: binary
                  description: PDF file to convert
      responses:
        '200':
          description: PDF/A-2 compliant file
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ToolResponse'
        '400':
          description: Invalid input
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Error'
        '401':
          description: Authentication required
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/UnauthorizedError'
  /api/v1/tools/pdf/crop-pdf:
    post:
      operationId: cropPdf
      tags:
        - Tools
      summary: Обрізати PDF
      description: Обрізати всі сторінки з рівномірним полем.
      security:
        - bearerAuth: []
      requestBody:
        required: true
        content:
          multipart/form-data:
            schema:
              type: object
              required:
                - file
              properties:
                file:
                  type: string
                  format: binary
                  description: PDF file to crop
                settings:
                  type: string
                  description: |
                    JSON string with options:
                    - `margin` (number 0-2000, default 20) - Uniform crop margin in points
      responses:
        '200':
          description: Cropped PDF
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ToolResponse'
        '400':
          description: Invalid input
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Error'
        '401':
          description: Authentication required
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/UnauthorizedError'
  /api/v1/tools/pdf/nup-pdf:
    post:
      operationId: nupPdf
      tags:
        - Tools
      summary: Сторінок на аркуш (N-up)
      description: Розмістити кілька сторінок на одному аркуші (наприклад, 2-up, 4-up).
      security:
        - bearerAuth: []
      requestBody:
        required: true
        content:
          multipart/form-data:
            schema:
              type: object
              required:
                - file
              properties:
                file:
                  type: string
                  format: binary
                  description: PDF file to process
                settings:
                  type: string
                  description: |
                    JSON string with options:
                    - `perSheet` (integer, default 2) - One of: 2, 3, 4, 8, 9, 12, 16
      responses:
        '200':
          description: N-up PDF
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ToolResponse'
        '400':
          description: Invalid input
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Error'
        '401':
          description: Authentication required
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/UnauthorizedError'
  /api/v1/tools/pdf/booklet-pdf:
    post:
      operationId: bookletPdf
      tags:
        - Tools
      summary: Буклет PDF
      description: Розмістити сторінки для складання в буклет (спуск для двостороннього друку).
      security:
        - bearerAuth: []
      requestBody:
        required: true
        content:
          multipart/form-data:
            schema:
              type: object
              required:
                - file
              properties:
                file:
                  type: string
                  format: binary
                  description: PDF file to process
                settings:
                  type: string
                  description: |
                    JSON string with options:
                    - `perSheet` (integer, default 2) - One of: 2, 4, 6, 8
      responses:
        '200':
          description: Booklet-imposed PDF
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ToolResponse'
        '400':
          description: Invalid input
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Error'
        '401':
          description: Authentication required
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/UnauthorizedError'
  /api/v1/tools/pdf/watermark-pdf:
    post:
      operationId: watermarkPdf
      tags:
        - Tools
      summary: Водяний знак на PDF
      description: Додати текстовий водяний знак на кожну сторінку PDF.
      security:
        - bearerAuth: []
      requestBody:
        required: true
        content:
          multipart/form-data:
            schema:
              type: object
              required:
                - file
              properties:
                file:
                  type: string
                  format: binary
                  description: PDF file to watermark
                settings:
                  type: string
                  description: |
                    JSON string with options:
                    - `text` (string 1-200 chars, required) - Watermark text
                    - `position` (string, default "c") - One of: tl, tc, tr, l, c, r, bl, bc, br
                    - `fontSize` (integer 6-72, default 48) - Font size in points
                    - `opacity` (number 0.05-1, default 0.3) - Watermark opacity
                    - `rotation` (number -180 to 180, default 45) - Rotation angle in degrees
      responses:
        '200':
          description: Watermarked PDF
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ToolResponse'
        '400':
          description: Invalid input
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Error'
        '401':
          description: Authentication required
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/UnauthorizedError'
  /api/v1/tools/pdf/pdf-page-numbers:
    post:
      operationId: pdfPageNumbers
      tags:
        - Tools
      summary: Номери сторінок PDF
      description: Додати номери сторінок ("Page N of M") на кожну сторінку PDF.
      security:
        - bearerAuth: []
      requestBody:
        required: true
        content:
          multipart/form-data:
            schema:
              type: object
              required:
                - file
              properties:
                file:
                  type: string
                  format: binary
                  description: PDF file to process
                settings:
                  type: string
                  description: |
                    JSON string with options:
                    - `position` (string, default "bc") - One of: bl, bc, br, tl, tc, tr
                    - `fontSize` (integer 6-24, default 10) - Font size in points
      responses:
        '200':
          description: PDF with page numbers
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ToolResponse'
        '400':
          description: Invalid input
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Error'
        '401':
          description: Authentication required
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/UnauthorizedError'
  /api/v1/tools/files/convert-document:
    post:
      operationId: convertDocument
      tags:
        - Tools
      summary: Конвертувати документ
      description: Конвертувати між форматами Word, OpenDocument, RTF та простим текстом.
      security:
        - bearerAuth: []
      requestBody:
        required: true
        content:
          multipart/form-data:
            schema:
              type: object
              required:
                - file
              properties:
                file:
                  type: string
                  format: binary
                  description: Document file to convert (.docx, .doc, .odt, .rtf, .txt)
                settings:
                  type: string
                  description: |
                    JSON string with options:
                    - `format` (string, required) - One of: docx, odt, rtf, txt
                clientJobId:
                  type: string
                  description: Client-provided job ID for SSE progress tracking
      responses:
        '202':
          description: Accepted for async processing. Track progress via SSE at /api/v1/jobs/{jobId}/progress.
          content:
            application/json:
              schema:
                type: object
                properties:
                  jobId:
                    type: string
                  async:
                    type: boolean
                    example: true
        '400':
          description: Invalid input
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Error'
        '401':
          description: Authentication required
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/UnauthorizedError'
  /api/v1/tools/files/convert-presentation:
    post:
      operationId: convertPresentation
      tags:
        - Tools
      summary: Конвертувати презентацію
      description: Конвертувати між форматами презентацій PowerPoint та OpenDocument.
      security:
        - bearerAuth: []
      requestBody:
        required: true
        content:
          multipart/form-data:
            schema:
              type: object
              required:
                - file
              properties:
                file:
                  type: string
                  format: binary
                  description: Presentation file to convert (.pptx, .ppt, .odp)
                settings:
                  type: string
                  description: |
                    JSON string with options:
                    - `format` (string, required) - One of: pptx, odp
                clientJobId:
                  type: string
                  description: Client-provided job ID for SSE progress tracking
      responses:
        '202':
          description: Accepted for async processing. Track progress via SSE at /api/v1/jobs/{jobId}/progress.
          content:
            application/json:
              schema:
                type: object
                properties:
                  jobId:
                    type: string
                  async:
                    type: boolean
                    example: true
        '400':
          description: Invalid input
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Error'
        '401':
          description: Authentication required
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/UnauthorizedError'
  /api/v1/tools/files/convert-spreadsheet:
    post:
      operationId: convertSpreadsheet
      tags:
        - Tools
      summary: Конвертувати таблицю
      description: Конвертувати між форматами Excel, OpenDocument та CSV. Багатоаркушні книги експортують перший аркуш у CSV.
      security:
        - bearerAuth: []
      requestBody:
        required: true
        content:
          multipart/form-data:
            schema:
              type: object
              required:
                - file
              properties:
                file:
                  type: string
                  format: binary
                  description: Spreadsheet file to convert (.xlsx, .xls, .ods, .csv)
                settings:
                  type: string
                  description: |
                    JSON string with options:
                    - `format` (string, required) - One of: xlsx, ods, csv
                clientJobId:
                  type: string
                  description: Client-provided job ID for SSE progress tracking
      responses:
        '202':
          description: Accepted for async processing. Track progress via SSE at /api/v1/jobs/{jobId}/progress.
          content:
            application/json:
              schema:
                type: object
                properties:
                  jobId:
                    type: string
                  async:
                    type: boolean
                    example: true
        '400':
          description: Invalid input
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Error'
        '401':
          description: Authentication required
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/UnauthorizedError'
  /api/v1/tools/files/excel-to-pdf:
    post:
      operationId: excelToPdf
      tags:
        - Tools
      summary: Excel у PDF
      description: Конвертувати таблиці в PDF. Широкі аркуші можуть розбиватися на кілька сторінок.
      security:
        - bearerAuth: []
      requestBody:
        required: true
        content:
          multipart/form-data:
            schema:
              type: object
              required:
                - file
              properties:
                file:
                  type: string
                  format: binary
                  description: Spreadsheet file to convert (.xlsx, .xls, .ods, .csv)
                clientJobId:
                  type: string
                  description: Client-provided job ID for SSE progress tracking
      responses:
        '202':
          description: Accepted for async processing. Track progress via SSE at /api/v1/jobs/{jobId}/progress.
          content:
            application/json:
              schema:
                type: object
                properties:
                  jobId:
                    type: string
                  async:
                    type: boolean
                    example: true
        '400':
          description: Invalid input
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Error'
        '401':
          description: Authentication required
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/UnauthorizedError'
  /api/v1/tools/files/word-to-pdf:
    post:
      operationId: wordToPdf
      tags:
        - Tools
      summary: Word у PDF
      description: Конвертувати документи Word у PDF.
      security:
        - bearerAuth: []
      requestBody:
        required: true
        content:
          multipart/form-data:
            schema:
              type: object
              required:
                - file
              properties:
                file:
                  type: string
                  format: binary
                  description: Word document to convert (.docx, .doc, .odt, .rtf, .txt)
                clientJobId:
                  type: string
                  description: Client-provided job ID for SSE progress tracking
      responses:
        '202':
          description: Accepted for async processing. Track progress via SSE at /api/v1/jobs/{jobId}/progress.
          content:
            application/json:
              schema:
                type: object
                properties:
                  jobId:
                    type: string
                  async:
                    type: boolean
                    example: true
        '400':
          description: Invalid input
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Error'
        '401':
          description: Authentication required
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/UnauthorizedError'
  /api/v1/tools/files/powerpoint-to-pdf:
    post:
      operationId: powerpointToPdf
      tags:
        - Tools
      summary: PowerPoint у PDF
      description: Конвертувати презентації в PDF.
      security:
        - bearerAuth: []
      requestBody:
        required: true
        content:
          multipart/form-data:
            schema:
              type: object
              required:
                - file
              properties:
                file:
                  type: string
                  format: binary
                  description: Presentation file to convert (.pptx, .ppt, .odp)
                clientJobId:
                  type: string
                  description: Client-provided job ID for SSE progress tracking
      responses:
        '202':
          description: Accepted for async processing. Track progress via SSE at /api/v1/jobs/{jobId}/progress.
          content:
            application/json:
              schema:
                type: object
                properties:
                  jobId:
                    type: string
                  async:
                    type: boolean
                    example: true
        '400':
          description: Invalid input
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Error'
        '401':
          description: Authentication required
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/UnauthorizedError'
  /api/v1/tools/files/html-to-pdf:
    post:
      operationId: htmlToPdf
      tags:
        - Tools
      summary: HTML у PDF
      description: Конвертувати файл HTML у PDF. Віддалені ресурси вимкнено для приватності.
      security:
        - bearerAuth: []
      requestBody:
        required: true
        content:
          multipart/form-data:
            schema:
              type: object
              required:
                - file
              properties:
                file:
                  type: string
                  format: binary
                  description: HTML file to convert (.html, .htm)
                clientJobId:
                  type: string
                  description: Client-provided job ID for SSE progress tracking
      responses:
        '202':
          description: Accepted for async processing. Track progress via SSE at /api/v1/jobs/{jobId}/progress.
          content:
            application/json:
              schema:
                type: object
                properties:
                  jobId:
                    type: string
                  async:
                    type: boolean
                    example: true
        '400':
          description: Invalid input
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Error'
        '401':
          description: Authentication required
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/UnauthorizedError'
  /api/v1/tools/files/markdown-to-docx:
    post:
      operationId: markdownToDocx
      tags:
        - Tools
      summary: Markdown у Word
      description: Конвертувати файл Markdown у документ Word (DOCX).
      security:
        - bearerAuth: []
      requestBody:
        required: true
        content:
          multipart/form-data:
            schema:
              type: object
              required:
                - file
              properties:
                file:
                  type: string
                  format: binary
                  description: Markdown file to convert (.md, .markdown)
      responses:
        '200':
          description: Processed document
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ToolResponse'
        '400':
          description: Invalid input
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Error'
        '401':
          description: Authentication required
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/UnauthorizedError'
  /api/v1/tools/files/markdown-to-html:
    post:
      operationId: markdownToHtml
      tags:
        - Tools
      summary: Markdown у HTML
      description: Конвертувати файл Markdown у самодостатню сторінку HTML. Віддалені зображення у джерелі залишаються без змін у вихідних даних.
      security:
        - bearerAuth: []
      requestBody:
        required: true
        content:
          multipart/form-data:
            schema:
              type: object
              required:
                - file
              properties:
                file:
                  type: string
                  format: binary
                  description: Markdown file to convert (.md, .markdown)
      responses:
        '200':
          description: Processed document
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ToolResponse'
        '400':
          description: Invalid input
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Error'
        '401':
          description: Authentication required
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/UnauthorizedError'
  /api/v1/tools/files/markdown-to-pdf:
    post:
      operationId: markdownToPdf
      tags:
        - Tools
      summary: Markdown у PDF
      description: Конвертувати файл Markdown у стилізований PDF. Віддалені ресурси вимкнено для приватності.
      security:
        - bearerAuth: []
      requestBody:
        required: true
        content:
          multipart/form-data:
            schema:
              type: object
              required:
                - file
              properties:
                file:
                  type: string
                  format: binary
                  description: Markdown file to convert (.md, .markdown)
                clientJobId:
                  type: string
                  description: Client-provided job ID for SSE progress tracking
      responses:
        '202':
          description: Accepted for async processing. Track progress via SSE at /api/v1/jobs/{jobId}/progress.
          content:
            application/json:
              schema:
                type: object
                properties:
                  jobId:
                    type: string
                  async:
                    type: boolean
                    example: true
        '400':
          description: Invalid input
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Error'
        '401':
          description: Authentication required
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/UnauthorizedError'
  /api/v1/tools/files/epub-convert:
    post:
      operationId: epubConvert
      tags:
        - Tools
      summary: Конвертація з EPUB
      description: Конвертувати EPUB у PDF, DOCX, HTML або Markdown. Віддалені ресурси всередині книги не завантажуються.
      security:
        - bearerAuth: []
      requestBody:
        required: true
        content:
          multipart/form-data:
            schema:
              type: object
              required:
                - file
              properties:
                file:
                  type: string
                  format: binary
                  description: EPUB file to convert (.epub)
                settings:
                  type: string
                  description: |
                    JSON string with options:
                    - `format` (string, required) - One of: pdf, docx, html, md
                clientJobId:
                  type: string
                  description: Client-provided job ID for SSE progress tracking
      responses:
        '202':
          description: Accepted for async processing. Track progress via SSE at /api/v1/jobs/{jobId}/progress.
          content:
            application/json:
              schema:
                type: object
                properties:
                  jobId:
                    type: string
                  async:
                    type: boolean
                    example: true
        '400':
          description: Invalid input
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Error'
        '401':
          description: Authentication required
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/UnauthorizedError'
  /api/v1/tools/files/to-epub:
    post:
      operationId: toEpub
      tags:
        - Tools
      summary: Конвертувати в EPUB
      description: Конвертувати файли Word, Markdown, HTML або простий текст у EPUB.
      security:
        - bearerAuth: []
      requestBody:
        required: true
        content:
          multipart/form-data:
            schema:
              type: object
              required:
                - file
              properties:
                file:
                  type: string
                  format: binary
                  description: Document file to convert (.docx, .md, .html, .txt)
                clientJobId:
                  type: string
                  description: Client-provided job ID for SSE progress tracking
      responses:
        '202':
          description: Accepted for async processing. Track progress via SSE at /api/v1/jobs/{jobId}/progress.
          content:
            application/json:
              schema:
                type: object
                properties:
                  jobId:
                    type: string
                  async:
                    type: boolean
                    example: true
        '400':
          description: Invalid input
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Error'
        '401':
          description: Authentication required
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/UnauthorizedError'
  /api/v1/tools/pdf/flatten-pdf:
    post:
      operationId: flattenPdf
      tags:
        - Tools
      summary: Звести PDF
      description: Вбудувати форми й анотації у вміст сторінки.
      security:
        - bearerAuth: []
      requestBody:
        required: true
        content:
          multipart/form-data:
            schema:
              type: object
              required:
                - file
              properties:
                file:
                  type: string
                  format: binary
                  description: PDF file to process (.pdf)
      responses:
        '200':
          description: Processed PDF
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ToolResponse'
        '400':
          description: Invalid input
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Error'
        '401':
          description: Authentication required
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/UnauthorizedError'
  /api/v1/tools/pdf/redact-pdf:
    post:
      operationId: redactPdf
      tags:
        - Tools
      summary: Редагувати PDF
      description: Назавжди видалити входження тексту (перевірене справжнє редагування).
      security:
        - bearerAuth: []
      requestBody:
        required: true
        content:
          multipart/form-data:
            schema:
              type: object
              required:
                - file
              properties:
                file:
                  type: string
                  format: binary
                  description: PDF file to process (.pdf)
                settings:
                  type: string
                  description: |
                    JSON string with options:
                    - `terms` (string[], required) - Text strings to redact (1-50 terms, each up to 200 characters)
                    - `caseSensitive` (boolean, default false) - Whether matching is case-sensitive
      responses:
        '200':
          description: Processed PDF
          content:
            application/json:
              schema:
                allOf:
                  - $ref: '#/components/schemas/ToolResponse'
                  - type: object
                    properties:
                      found:
                        type: integer
                        description: Number of redacted occurrences found
        '400':
          description: Invalid input
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Error'
        '401':
          description: Authentication required
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/UnauthorizedError'
  /api/v1/tools/pdf/pdf-to-text:
    post:
      operationId: pdfToText
      tags:
        - Tools
      summary: PDF у текст
      description: Вилучити простий текст із PDF.
      security:
        - bearerAuth: []
      requestBody:
        required: true
        content:
          multipart/form-data:
            schema:
              type: object
              required:
                - file
              properties:
                file:
                  type: string
                  format: binary
                  description: PDF file to process (.pdf)
      responses:
        '200':
          description: Extracted text file
          content:
            application/json:
              schema:
                allOf:
                  - $ref: '#/components/schemas/ToolResponse'
                  - type: object
                    properties:
                      chars:
                        type: integer
                        description: Number of characters extracted
        '400':
          description: Invalid input
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Error'
        '401':
          description: Authentication required
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/UnauthorizedError'
  /api/v1/tools/pdf/pdf-to-word:
    post:
      operationId: pdfToWord
      tags:
        - Tools
      summary: PDF у Word
      description: Найкраще для текстових PDF; сканованим сторінкам потрібен OCR.
      security:
        - bearerAuth: []
      requestBody:
        required: true
        content:
          multipart/form-data:
            schema:
              type: object
              required:
                - file
              properties:
                file:
                  type: string
                  format: binary
                  description: PDF file to convert (.pdf)
                clientJobId:
                  type: string
                  description: Client-provided job ID for SSE progress tracking
      responses:
        '202':
          description: Accepted for async processing. Track progress via SSE at /api/v1/jobs/{jobId}/progress.
          content:
            application/json:
              schema:
                type: object
                properties:
                  jobId:
                    type: string
                  async:
                    type: boolean
                    example: true
        '400':
          description: Invalid input
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Error'
        '401':
          description: Authentication required
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/UnauthorizedError'
  /api/v1/tools/pdf/pdf-metadata:
    post:
      operationId: pdfMetadata
      tags:
        - Tools
      summary: Метадані PDF
      description: Прочитати й записати метадані документа PDF.
      security:
        - bearerAuth: []
      requestBody:
        required: true
        content:
          multipart/form-data:
            schema:
              type: object
              required:
                - file
              properties:
                file:
                  type: string
                  format: binary
                  description: PDF file to process (.pdf)
                settings:
                  type: string
                  description: |
                    JSON string with options:
                    - `title` (string, optional, max 500) - Document title
                    - `author` (string, optional, max 500) - Document author
                    - `subject` (string, optional, max 500) - Document subject
                    - `keywords` (string, optional, max 500) - Document keywords
      responses:
        '200':
          description: Processed PDF with updated metadata
          content:
            application/json:
              schema:
                allOf:
                  - $ref: '#/components/schemas/ToolResponse'
                  - type: object
                    properties:
                      metadata:
                        type: object
                        description: The resulting PDF metadata fields
        '400':
          description: Invalid input
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Error'
        '401':
          description: Authentication required
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/UnauthorizedError'
  /api/v1/tools/pdf/ocr-pdf:
    post:
      operationId: ocrPdf
      tags:
        - Tools
      summary: OCR для PDF
      description: |-
        Витягуйте текст зі сканованих документів PDF сторінка за сторінкою. Fast використовує
        вбудоване середовище виконання Tesseract. Balanced і Best використовують необов’язковий підпис
        Середа виконання RapidOCR із закріпленими моделями PP-OCR ONNX. Явні запити рівня
        ніколи мовчки не знижуйтеся. Запит може обирати не більше 50 сторінок.
      security:
        - bearerAuth: []
      requestBody:
        required: true
        content:
          multipart/form-data:
            schema:
              type: object
              required:
                - file
              properties:
                file:
                  type: string
                  format: binary
                  maxLength: 536870912
                  description: PDF file to process (.pdf)
                settings:
                  type: string
                  description: |
                    JSON string with options:
                    - `quality` (string, optional) - One of: fast, balanced, best. When both quality and engine are omitted, uses the best available tier in this order: best, balanced, fast. Korean never selects fast; it uses best, then balanced, or returns an accurate-runtime install/compatibility error
                    - `language` (string, default "auto") - One of: auto, en, de, fr, es, zh, ja, ko. Korean is unsupported by fast and the legacy tesseract alias
                    - `pages` (string, default "all") - Page selection, e.g. "all", "1-3", "1,3,5"
                    - `enhance` (boolean, optional) - Improve local contrast. Fast applies it directly; accurate tiers retain the variant only when calibrated scoring improves the result. Defaults to true for Best and false for Fast/Balanced
                    - `engine` (string, deprecated) - Compatibility alias: tesseract maps to fast; the legacy paddleocr value maps to balanced but does not load PaddlePaddle
                clientJobId:
                  type: string
                  description: Client-provided job ID for SSE progress tracking
      responses:
        '202':
          description: Accepted for async processing. Track progress via SSE at /api/v1/jobs/{jobId}/progress.
          content:
            application/json:
              schema:
                type: object
                required:
                  - jobId
                  - async
                properties:
                  jobId:
                    type: string
                  progressJobId:
                    type: string
                  artifactJobId:
                    type: string
                  async:
                    type: boolean
                    const: true
        '400':
          description: Invalid input
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Error'
        '401':
          description: Authentication required
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/UnauthorizedError'
        '413':
          description: Encoded PDF exceeds the hard 512 MiB safety limit
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Error'
        '422':
          description: Processing failed
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Error'
        '501':
          description: Requested Balanced/Best runtime is not installed or is incompatible
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/FeatureNotInstalledError'
        '503':
          description: PDF OCR could not be queued
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Error'
  /api/v1/tools/files/chart-maker:
    post:
      operationId: chartMaker
      tags:
        - Tools
      summary: Створювач діаграм
      description: Створити стовпчикові, лінійні або кругові діаграми з даних CSV чи JSON. Повертає зображення PNG.
      security:
        - bearerAuth: []
      requestBody:
        required: true
        content:
          multipart/form-data:
            schema:
              type: object
              required:
                - file
              properties:
                file:
                  type: string
                  format: binary
                  description: CSV or JSON data file
                settings:
                  type: string
                  description: |
                    JSON string with options:
                    - `kind` (string, default "bar") - One of: bar, line, pie
                    - `title` (string, optional) - Chart title (max 120 characters)
                    - `width` (integer 320-2048, default 960) - Chart width in pixels
                    - `height` (integer 240-1536, default 540) - Chart height in pixels
      responses:
        '200':
          description: Generated chart image
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ToolResponse'
        '400':
          description: Invalid input
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Error'
        '401':
          description: Authentication required
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/UnauthorizedError'
  /api/v1/tools/files/csv-excel:
    post:
      operationId: csvExcel
      tags:
        - Tools
      summary: CSV у Excel
      description: Конвертувати між CSV та Excel (XLSX) в обох напрямках. Завантажте файл CSV або TSV, щоб отримати XLSX, або завантажте файл XLSX, щоб отримати CSV.
      security:
        - bearerAuth: []
      requestBody:
        required: true
        content:
          multipart/form-data:
            schema:
              type: object
              required:
                - file
              properties:
                file:
                  type: string
                  format: binary
                  description: CSV, TSV, or XLSX file to convert
                settings:
                  type: string
                  description: |
                    JSON string with options:
                    - `sheet` (integer, min 1, default 1) - Worksheet number to export when converting from XLSX
      responses:
        '200':
          description: Converted file
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ToolResponse'
        '400':
          description: Invalid input
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Error'
        '401':
          description: Authentication required
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/UnauthorizedError'
  /api/v1/tools/files/csv-json:
    post:
      operationId: csvJson
      tags:
        - Tools
      summary: CSV у JSON
      description: Конвертувати між CSV та JSON в обох напрямках. Завантажте файл CSV або TSV, щоб отримати JSON, або завантажте масив об'єктів JSON, щоб отримати CSV.
      security:
        - bearerAuth: []
      requestBody:
        required: true
        content:
          multipart/form-data:
            schema:
              type: object
              required:
                - file
              properties:
                file:
                  type: string
                  format: binary
                  description: CSV, TSV, or JSON file to convert
                settings:
                  type: string
                  description: |
                    JSON string with options:
                    - `pretty` (boolean, default true) - Pretty-print JSON output with indentation
      responses:
        '200':
          description: Converted file
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ToolResponse'
        '400':
          description: Invalid input
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Error'
        '401':
          description: Authentication required
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/UnauthorizedError'
  /api/v1/tools/files/json-xml:
    post:
      operationId: jsonXml
      tags:
        - Tools
      summary: JSON у XML
      description: Конвертувати між JSON та XML в обох напрямках. Завантажте файл JSON, щоб отримати XML, або завантажте файл XML, щоб отримати JSON.
      security:
        - bearerAuth: []
      requestBody:
        required: true
        content:
          multipart/form-data:
            schema:
              type: object
              required:
                - file
              properties:
                file:
                  type: string
                  format: binary
                  description: JSON or XML file to convert
                settings:
                  type: string
                  description: |
                    JSON string with options:
                    - `pretty` (boolean, default true) - Pretty-print output with indentation
      responses:
        '200':
          description: Converted file
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ToolResponse'
        '400':
          description: Invalid input
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Error'
        '401':
          description: Authentication required
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/UnauthorizedError'
  /api/v1/tools/files/split-csv:
    post:
      operationId: splitCsv
      tags:
        - Tools
      summary: Розділити CSV
      description: Розділити файл CSV на менші файли за кількістю рядків. Повертає ZIP-архів із частинами.
      security:
        - bearerAuth: []
      requestBody:
        required: true
        content:
          multipart/form-data:
            schema:
              type: object
              required:
                - file
              properties:
                file:
                  type: string
                  format: binary
                  description: CSV or TSV file to split
                settings:
                  type: string
                  description: |
                    JSON string with options:
                    - `rowsPerFile` (integer 1-1000000, default 1000) - Number of data rows per output file
                    - `keepHeader` (boolean, default true) - Repeat the header row in each output file
      responses:
        '200':
          description: ZIP archive containing split CSV parts
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ToolResponse'
        '400':
          description: Invalid input
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Error'
        '401':
          description: Authentication required
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/UnauthorizedError'
  /api/v1/tools/files/merge-csvs:
    post:
      operationId: mergeCsvs
      tags:
        - Tools
      summary: Об'єднати CSV
      description: Об'єднати кілька файлів CSV або TSV зі збіжними стовпцями в один об'єднаний файл. Усі файли мають мати однакові заголовки стовпців.
      security:
        - bearerAuth: []
      requestBody:
        required: true
        content:
          multipart/form-data:
            schema:
              type: object
              required:
                - file
              properties:
                file:
                  type: array
                  items:
                    type: string
                    format: binary
                  description: Two or more CSV or TSV files to merge (2-20 files)
      responses:
        '200':
          description: Merged CSV file
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ToolResponse'
        '400':
          description: Invalid input
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Error'
        '401':
          description: Authentication required
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/UnauthorizedError'
  /api/v1/tools/files/yaml-json:
    post:
      operationId: yamlJson
      tags:
        - Tools
      summary: Конвертація YAML / JSON
      description: Конвертувати між YAML та JSON в обох напрямках. Завантажте файл YAML, щоб отримати JSON, або завантажте файл JSON, щоб отримати YAML.
      security:
        - bearerAuth: []
      requestBody:
        required: true
        content:
          multipart/form-data:
            schema:
              type: object
              required:
                - file
              properties:
                file:
                  type: string
                  format: binary
                  description: YAML (.yaml, .yml) or JSON file to convert
      responses:
        '200':
          description: Converted file
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ToolResponse'
        '400':
          description: Invalid input
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Error'
        '401':
          description: Authentication required
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/UnauthorizedError'
  /api/v1/tools/files/xml-to-csv:
    post:
      operationId: xmlToCsv
      tags:
        - Tools
      summary: XML у CSV
      description: Витягти повторювані елементи з файлу XML у таблицю CSV. Автоматично знаходить перший масив об'єктів у дереві XML.
      security:
        - bearerAuth: []
      requestBody:
        required: true
        content:
          multipart/form-data:
            schema:
              type: object
              required:
                - file
              properties:
                file:
                  type: string
                  format: binary
                  description: XML file to convert
      responses:
        '200':
          description: Extracted CSV file
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ToolResponse'
        '400':
          description: Invalid input
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Error'
        '401':
          description: Authentication required
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/UnauthorizedError'
  /api/v1/tools/files/create-zip:
    post:
      operationId: createZip
      tags:
        - Tools
      summary: Створити ZIP
      description: Зібрати кілька файлів в один ZIP-архів. Дубльовані імена файлів автоматично дедуплікуються.
      security:
        - bearerAuth: []
      requestBody:
        required: true
        content:
          multipart/form-data:
            schema:
              type: object
              required:
                - file
              properties:
                file:
                  type: array
                  items:
                    type: string
                    format: binary
                  description: Two or more files to bundle (2-50 files, any type)
      responses:
        '200':
          description: ZIP archive
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ToolResponse'
        '400':
          description: Invalid input
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Error'
        '401':
          description: Authentication required
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/UnauthorizedError'
  /api/v1/tools/files/extract-zip:
    post:
      operationId: extractZip
      tags:
        - Tools
      summary: Розпакувати ZIP
      description: Безпечно розпакувати файли з ZIP-архіву із захистом від zip-бомб. Однофайлові архіви повертають файл напряму; багатофайлові архіви повертають плоский ZIP.
      security:
        - bearerAuth: []
      requestBody:
        required: true
        content:
          multipart/form-data:
            schema:
              type: object
              required:
                - file
              properties:
                file:
                  type: string
                  format: binary
                  description: ZIP archive to extract
      responses:
        '200':
          description: Extracted file or repackaged ZIP archive
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ToolResponse'
        '400':
          description: Invalid input
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Error'
        '401':
          description: Authentication required
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/UnauthorizedError'
  /api/v1/tools/image/background-replace:
    post:
      operationId: backgroundReplace
      tags:
        - Tools
      summary: Замінити фон
      description: |
        Заміна фону на основі AI. Видаляє фон об'єкта
        та композитує його на суцільний колір або градієнт. Потребує встановленого
        пакета функцій background-removal.
      security:
        - bearerAuth: []
      requestBody:
        required: true
        content:
          multipart/form-data:
            schema:
              type: object
              required:
                - file
              properties:
                file:
                  type: string
                  format: binary
                  description: Image file to process
                settings:
                  type: string
                  description: |
                    JSON string with options:
                    - `backgroundType` (string, default "color") - One of: color, gradient
                    - `color` (string, default "#ffffff") - Background hex color (when backgroundType is color)
                    - `gradientColor1` (string, optional) - First gradient hex color
                    - `gradientColor2` (string, optional) - Second gradient hex color
                    - `gradientAngle` (integer 0-360, default 180) - Gradient angle in degrees
                    - `feather` (integer 0-20, default 0) - Edge feathering radius
                    - `format` (string, default "png") - One of: png, webp
                clientJobId:
                  type: string
                  description: Client-provided job ID for SSE progress tracking
      responses:
        '202':
          description: Accepted for async processing. Track progress via SSE at /api/v1/jobs/{jobId}/progress.
          content:
            application/json:
              schema:
                type: object
                properties:
                  jobId:
                    type: string
                  async:
                    type: boolean
                    example: true
        '400':
          description: Invalid input
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Error'
        '401':
          description: Authentication required
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/UnauthorizedError'
        '422':
          description: Processing failed
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Error'
        '501':
          description: Feature not installed
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/FeatureNotInstalledError'
  /api/v1/tools/image/blur-background:
    post:
      operationId: blurBackground
      tags:
        - Tools
      summary: Розмити фон
      description: |
        Розмиття фону на основі AI. Видаляє фон, розмиває
        оригінал і композитує чіткий об'єкт зверху. Потребує встановленого
        пакета функцій background-removal.
      security:
        - bearerAuth: []
      requestBody:
        required: true
        content:
          multipart/form-data:
            schema:
              type: object
              required:
                - file
              properties:
                file:
                  type: string
                  format: binary
                  description: Image file to process
                settings:
                  type: string
                  description: |
                    JSON string with options:
                    - `intensity` (integer 1-100, default 50) - Blur intensity
                    - `feather` (integer 0-20, default 0) - Edge feathering radius
                    - `format` (string, default "png") - One of: png, webp
                clientJobId:
                  type: string
                  description: Client-provided job ID for SSE progress tracking
      responses:
        '202':
          description: Accepted for async processing. Track progress via SSE at /api/v1/jobs/{jobId}/progress.
          content:
            application/json:
              schema:
                type: object
                properties:
                  jobId:
                    type: string
                  async:
                    type: boolean
                    example: true
        '400':
          description: Invalid input
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Error'
        '401':
          description: Authentication required
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/UnauthorizedError'
        '422':
          description: Processing failed
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Error'
        '501':
          description: Feature not installed
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/FeatureNotInstalledError'
  /api/v1/tools/image/barcode-generate:
    post:
      operationId: generateBarcode
      tags:
        - Tools
      summary: Генератор штрихкодів
      description: Згенерувати зображення штрихкоду з тексту. Цей інструмент приймає тіло JSON, а не multipart.
      security:
        - bearerAuth: []
      requestBody:
        required: true
        content:
          application/json:
            schema:
              type: object
              required:
                - text
              properties:
                text:
                  type: string
                  minLength: 1
                  maxLength: 256
                  description: Text to encode in the barcode
                type:
                  type: string
                  enum:
                    - code128
                    - ean13
                    - upca
                    - code39
                    - itf14
                    - datamatrix
                  default: code128
                  description: Barcode format
                scale:
                  type: integer
                  minimum: 1
                  maximum: 8
                  default: 3
                  description: Image scale factor
                includeText:
                  type: boolean
                  default: true
                  description: Whether to render the text below the barcode
      responses:
        '200':
          description: Generated barcode
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ToolResponse'
        '400':
          description: Invalid input
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Error'
        '401':
          description: Authentication required
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/UnauthorizedError'
  /api/v1/tools/image/circle-crop:
    post:
      operationId: circleCrop
      tags:
        - Tools
      summary: Кругле обрізання
      description: Обрізати зображення до центрованого кола з прозорими кутами, додатковою рамкою та регульованим масштабом/зсувом.
      security:
        - bearerAuth: []
      requestBody:
        required: true
        content:
          multipart/form-data:
            schema:
              type: object
              required:
                - file
              properties:
                file:
                  type: string
                  format: binary
                  description: Image file to process
                settings:
                  type: string
                  description: |
                    JSON string with options:
                    - `zoom` (number 1-5, default 1) - Zoom factor (>=1 crops tighter)
                    - `offsetX` (number 0-1, default 0.5) - Horizontal center position
                    - `offsetY` (number 0-1, default 0.5) - Vertical center position
                    - `borderWidth` (integer 0-200, default 0) - Border width in pixels
                    - `borderColor` (string, default "#ffffff") - Border hex color
                    - `background` (string, default "transparent") - "transparent" or a hex color for corners
                    - `outputSize` (integer 16-4096, optional) - Final square dimension in pixels
      responses:
        '200':
          description: Processed image
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ToolResponse'
        '400':
          description: Invalid input
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Error'
        '401':
          description: Authentication required
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/UnauthorizedError'
  /api/v1/tools/image/rounded-crop:
    post:
      operationId: roundedCrop
      tags:
        - Tools
      summary: Rounded crop
      description: Crop an image to a rounded square or squircle with transparent corners, optional border, and adjustable zoom/offset.
      security:
        - bearerAuth: []
      requestBody:
        required: true
        content:
          multipart/form-data:
            schema:
              type: object
              required:
                - file
              properties:
                file:
                  type: string
                  format: binary
                  description: Image file to process
                settings:
                  type: string
                  description: |
                    JSON string with options:
                    - `shape` (string, default "rounded-square") - "rounded-square" or "squircle"
                    - `cornerRadius` (number 0-50, default 25) - Corner radius as a percent of the shorter side (rounded-square only)
                    - `zoom` (number 1-5, default 1) - Zoom factor (>=1 crops tighter)
                    - `offsetX` (number 0-1, default 0.5) - Horizontal center position
                    - `offsetY` (number 0-1, default 0.5) - Vertical center position
                    - `borderWidth` (integer 0-200, default 0) - Border width in pixels
                    - `borderColor` (string, default "#ffffff") - Border hex color
                    - `background` (string, default "transparent") - "transparent" or a hex color for corners
                    - `outputSize` (integer 16-4096, optional) - Final square dimension in pixels
      responses:
        '200':
          description: Processed image
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ToolResponse'
        '400':
          description: Invalid input
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Error'
        '401':
          description: Authentication required
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/UnauthorizedError'
  /api/v1/tools/image/duotone:
    post:
      operationId: duotone
      tags:
        - Tools
      summary: Дуотон
      description: Застосувати двоколірний ефект дуотон з довільними кольорами тіней і світлих ділянок.
      security:
        - bearerAuth: []
      requestBody:
        required: true
        content:
          multipart/form-data:
            schema:
              type: object
              required:
                - file
              properties:
                file:
                  type: string
                  format: binary
                  description: Image file to process
                settings:
                  type: string
                  description: |
                    JSON string with options:
                    - `shadow` (string, default "#1e3a8a") - Shadow hex color
                    - `highlight` (string, default "#fbbf24") - Highlight hex color
                    - `intensity` (integer 0-100, default 100) - Effect intensity (0 = original, 100 = full duotone)
      responses:
        '200':
          description: Processed image
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ToolResponse'
        '400':
          description: Invalid input
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Error'
        '401':
          description: Authentication required
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/UnauthorizedError'
  /api/v1/tools/image/image-pad:
    post:
      operationId: imagePad
      tags:
        - Tools
      summary: Доповнення зображення
      description: Доповнити зображення до цільового співвідношення сторін суцільним кольором, прозорим або розмитим фоном.
      security:
        - bearerAuth: []
      requestBody:
        required: true
        content:
          multipart/form-data:
            schema:
              type: object
              required:
                - file
              properties:
                file:
                  type: string
                  format: binary
                  description: Image file to process
                settings:
                  type: string
                  description: |
                    JSON string with options:
                    - `target` (string, default "1:1") - One of: 16:9, 9:16, 1:1, 4:3, 3:4, custom
                    - `ratioW` (integer 1-100, default 1) - Custom ratio width (when target is custom)
                    - `ratioH` (integer 1-100, default 1) - Custom ratio height (when target is custom)
                    - `background` (string, default "color") - One of: color, transparent, blur
                    - `color` (string, default "#ffffff") - Background hex color (when background is color)
                    - `padding` (integer 0-50, default 0) - Extra padding as percentage of canvas
      responses:
        '200':
          description: Processed image
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ToolResponse'
        '400':
          description: Invalid input
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Error'
        '401':
          description: Authentication required
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/UnauthorizedError'
  /api/v1/tools/image/pixelate:
    post:
      operationId: pixelate
      tags:
        - Tools
      summary: Пікселізація
      description: Застосувати ефект пікселізації до всього зображення або конкретної прямокутної області.
      security:
        - bearerAuth: []
      requestBody:
        required: true
        content:
          multipart/form-data:
            schema:
              type: object
              required:
                - file
              properties:
                file:
                  type: string
                  format: binary
                  description: Image file to process
                settings:
                  type: string
                  description: |
                    JSON string with options:
                    - `blockSize` (integer 2-128, default 12) - Pixel block size
                    - `region` (object, optional) - Restrict pixelation to a rectangle:
                      - `left` (integer >= 0) - Left offset in pixels
                      - `top` (integer >= 0) - Top offset in pixels
                      - `width` (integer >= 1) - Region width in pixels
                      - `height` (integer >= 1) - Region height in pixels
      responses:
        '200':
          description: Processed image
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ToolResponse'
        '400':
          description: Invalid input
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Error'
        '401':
          description: Authentication required
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/UnauthorizedError'
  /api/v1/tools/image/vignette:
    post:
      operationId: vignette
      tags:
        - Tools
      summary: Віньєтка
      description: Додати ефект віньєтки з регульованими інтенсивністю, кольором, радіусом, м'якістю, округлістю та положенням центру.
      security:
        - bearerAuth: []
      requestBody:
        required: true
        content:
          multipart/form-data:
            schema:
              type: object
              required:
                - file
              properties:
                file:
                  type: string
                  format: binary
                  description: Image file to process
                settings:
                  type: string
                  description: |
                    JSON string with options:
                    - `strength` (number 0.1-1, default 0.5) - Vignette opacity
                    - `color` (string, default "#000000") - Vignette hex color
                    - `radius` (integer 0-100, default 70) - Outer radius as percentage of half-diagonal
                    - `softness` (integer 0-100, default 50) - Feather softness (higher = more gradual)
                    - `roundness` (integer 0-100, default 100) - 100 = circle, 0 = ellipse matching image aspect ratio
                    - `centerX` (integer 0-100, default 50) - Horizontal center position as percentage
                    - `centerY` (integer 0-100, default 50) - Vertical center position as percentage
      responses:
        '200':
          description: Processed image
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ToolResponse'
        '400':
          description: Invalid input
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Error'
        '401':
          description: Authentication required
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/UnauthorizedError'
  /api/v1/tools/image/gif-webp:
    post:
      operationId: gifWebp
      tags:
        - Tools
      summary: Конвертер GIF/WebP
      description: Конвертувати анімований GIF у WebP і навпаки зі збереженням усіх кадрів. Приймає лише вхідні дані .gif та .webp.
      security:
        - bearerAuth: []
      requestBody:
        required: true
        content:
          multipart/form-data:
            schema:
              type: object
              required:
                - file
              properties:
                file:
                  type: string
                  format: binary
                  description: GIF or WebP file to convert
                settings:
                  type: string
                  description: |
                    JSON string with options:
                    - `quality` (integer 1-100, default 80) - Output quality for WebP encoding
                    - `lossless` (boolean, default false) - Use lossless WebP compression
                    - `resizePercent` (integer 10-100, default 100) - Scale the output by percentage
      responses:
        '200':
          description: Converted animation
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ToolResponse'
        '400':
          description: Invalid input
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Error'
        '401':
          description: Authentication required
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/UnauthorizedError'
  /api/v1/tools/image/histogram:
    post:
      operationId: histogram
      tags:
        - Tools
      summary: Гістограма
      description: |
        Згенерувати діаграму RGB-гістограми із зображення. Повертає зображення гістограми
        PNG плюс поканальну статистику та необроблені дані гістограми з 256 інтервалів у
        відповіді JSON.
      security:
        - bearerAuth: []
      requestBody:
        required: true
        content:
          multipart/form-data:
            schema:
              type: object
              required:
                - file
              properties:
                file:
                  type: string
                  format: binary
                  description: Image file to analyze
                settings:
                  type: string
                  description: |
                    JSON string with options:
                    - `scale` (string, default "linear") - One of: linear, log
      responses:
        '200':
          description: Histogram image and channel statistics
          content:
            application/json:
              schema:
                allOf:
                  - $ref: '#/components/schemas/ToolResponse'
                  - type: object
                    properties:
                      bins:
                        type: object
                        description: Raw 256-bin histogram arrays per channel
                        properties:
                          r:
                            type: array
                            items:
                              type: integer
                          g:
                            type: array
                            items:
                              type: integer
                          b:
                            type: array
                            items:
                              type: integer
                          lum:
                            type: array
                            items:
                              type: integer
                      stats:
                        type: object
                        description: Per-channel statistics
                        properties:
                          r:
                            type: object
                            properties:
                              mean:
                                type: integer
                              median:
                                type: integer
                              stdev:
                                type: number
                          g:
                            type: object
                            properties:
                              mean:
                                type: integer
                              median:
                                type: integer
                              stdev:
                                type: number
                          b:
                            type: object
                            properties:
                              mean:
                                type: integer
                              median:
                                type: integer
                              stdev:
                                type: number
                          lum:
                            type: object
                            properties:
                              mean:
                                type: integer
                              median:
                                type: integer
                              stdev:
                                type: number
                      mean:
                        type: object
                        description: Backward-compatible mean values
                        properties:
                          r:
                            type: integer
                          g:
                            type: integer
                          b:
                            type: integer
                      max:
                        type: object
                        description: Backward-compatible max bin values
                        properties:
                          r:
                            type: integer
                          g:
                            type: integer
                          b:
                            type: integer
        '400':
          description: Invalid input
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Error'
        '401':
          description: Authentication required
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/UnauthorizedError'
  /api/v1/tools/image/lqip-placeholder:
    post:
      operationId: lqipPlaceholder
      tags:
        - Tools
      summary: Заповнювач LQIP
      description: |
        Згенерувати крихітний низькоякісний заповнювач зображення. Повертає невеликий файл
        зображення плюс data URI у base64, готовий до використання тег HTML img та CSS-фрагмент
        фону у відповіді JSON.
      security:
        - bearerAuth: []
      requestBody:
        required: true
        content:
          multipart/form-data:
            schema:
              type: object
              required:
                - file
              properties:
                file:
                  type: string
                  format: binary
                  description: Image file to process
                settings:
                  type: string
                  description: |
                    JSON string with options:
                    - `width` (integer 4-64, default 16) - Target width in pixels
                    - `blur` (number 0-20, default 2) - Blur radius (for blur strategy)
                    - `strategy` (string, default "blur") - One of: blur, pixelate, solid
                    - `format` (string, default "webp") - One of: webp, png, jpeg
                    - `quality` (integer 1-100, default 50) - Output quality
      responses:
        '200':
          description: LQIP placeholder image and data URI
          content:
            application/json:
              schema:
                allOf:
                  - $ref: '#/components/schemas/ToolResponse'
                  - type: object
                    properties:
                      dataUri:
                        type: string
                        description: Complete data URI (data:mime;base64,...)
                      width:
                        type: integer
                        description: Placeholder width in pixels
                      height:
                        type: integer
                        description: Placeholder height in pixels
                      bytes:
                        type: integer
                        description: Placeholder file size in bytes
                      strategy:
                        type: string
                        description: Strategy used (blur, pixelate, or solid)
                      html:
                        type: string
                        description: Ready-to-use HTML img tag
                      css:
                        type: string
                        description: Ready-to-use CSS background-image snippet
        '400':
          description: Invalid input
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Error'
        '401':
          description: Authentication required
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/UnauthorizedError'
  /api/v1/tools/image/sprite-sheet:
    post:
      operationId: spriteSheet
      tags:
        - Tools
      summary: Спрайт-лист
      description: |
        Об'єднати кілька зображень в одну сітку спрайт-листа. Приймає
        від 2 до 64 зображень. Кожне зображення змінюється до розмірів першого
        зображення. Повертає зображення спрайт-листа плюс метадані координат
        кадрів.
      security:
        - bearerAuth: []
      requestBody:
        required: true
        content:
          multipart/form-data:
            schema:
              type: object
              required:
                - file
              properties:
                file:
                  type: array
                  items:
                    type: string
                    format: binary
                  description: Two or more image files to combine (max 64)
                settings:
                  type: string
                  description: |
                    JSON string with options:
                    - `columns` (integer 1-16, default 4) - Number of columns in the grid
                    - `padding` (integer 0-64, default 0) - Padding between cells in pixels
                    - `background` (string, default "#ffffff") - Background hex color
                    - `format` (string, default "png") - One of: png, webp, jpeg
                    - `quality` (integer 1-100, default 90) - Output quality
      responses:
        '200':
          description: Sprite sheet image with frame metadata
          content:
            application/json:
              schema:
                allOf:
                  - $ref: '#/components/schemas/ToolResponse'
                  - type: object
                    properties:
                      frames:
                        type: array
                        description: Per-frame coordinates in the sprite sheet
                        items:
                          type: object
                          properties:
                            index:
                              type: integer
                            left:
                              type: integer
                            top:
                              type: integer
                            width:
                              type: integer
                            height:
                              type: integer
                      cols:
                        type: integer
                        description: Number of columns
                      rows:
                        type: integer
                        description: Number of rows
                      cellWidth:
                        type: integer
                        description: Cell width in pixels
                      cellHeight:
                        type: integer
                        description: Cell height in pixels
                      canvasWidth:
                        type: integer
                        description: Total canvas width in pixels
                      canvasHeight:
                        type: integer
                        description: Total canvas height in pixels
        '400':
          description: Invalid input
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Error'
        '401':
          description: Authentication required
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/UnauthorizedError'
  /api/v1/tools/image/passport-photo:
    post:
      operationId: passportPhotoTool
      tags:
        - Tools
      summary: Фото на паспорт
      description: |
        Довідковий ендпоінт для двофазного процесу створення фото на паспорт. Використовуйте
        `/api/v1/tools/image/passport-photo/analyze` для аналізу зображення,
        а потім `/api/v1/tools/image/passport-photo/generate` для створення фінального
        фото на паспорт. Цей базовий маршрут повертає довідкову помилку 400, коли
        потрібні пакети функцій встановлено, або 501, коли вони відсутні.
      security:
        - bearerAuth: []
      responses:
        '400':
          description: Use the analyze or generate endpoint
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Error'
        '401':
          description: Authentication required
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/UnauthorizedError'
        '501':
          description: Feature not installed
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/FeatureNotInstalledError'
  /api/v1/tools/pdf/sign-pdf:
    post:
      operationId: signPdfTool
      tags:
        - Tools
      summary: Підписати PDF
      description: Проштампувати одне або кілька зображень підпису PNG на PDF за нормалізованими розміщеннями на сторінці.
      security:
        - bearerAuth: []
      requestBody:
        required: true
        content:
          multipart/form-data:
            schema:
              type: object
              required:
                - file
                - placements
                - sig0
              properties:
                file:
                  type: string
                  format: binary
                  description: PDF file to sign
                sig0:
                  type: string
                  format: binary
                  description: First signature image. Additional signatures use sig1, sig2, and so on.
                placements:
                  type: string
                  description: JSON array of placement objects with sig, page, x, y, w, and h. Coordinates are page fractions with top-left origin.
                clientJobId:
                  type: string
                  description: Client-provided job ID for SSE progress tracking
                fileId:
                  type: string
                  description: Optional file library ID to save the signed result as a new version
      responses:
        '200':
          description: Signed PDF
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ToolResponse'
        '202':
          description: Accepted for async processing. Track progress via SSE at /api/v1/jobs/{jobId}/progress.
          content:
            application/json:
              schema:
                type: object
                properties:
                  jobId:
                    type: string
                  async:
                    type: boolean
                    example: true
        '400':
          description: Invalid input or settings
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Error'
        '401':
          description: Authentication required
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/UnauthorizedError'
        '422':
          description: Processing failed
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Error'
  /api/v1/tools/image/jpg-to-png:
    post:
      operationId: jpgToPngTool
      tags:
        - Tools
      summary: JPG у PNG
      description: |
        Конвертувати JPG у PNG
        Це спеціальний ендпоінт-пресет конвертації на основі `convert`.
      security:
        - bearerAuth: []
      requestBody:
        required: true
        content:
          multipart/form-data:
            schema:
              type: object
              required:
                - file
              properties:
                file:
                  type: string
                  format: binary
                  description: JPG to PNG input file (.jpg, .jpeg)
                settings:
                  type: string
                  description: |
                    Dedicated JPG to PNG converter using the convert pipeline.
                    Optional settings: quality (number 1-100). The output format is locked by the endpoint.
                clientJobId:
                  type: string
                  description: Client-provided job ID for SSE progress tracking
      responses:
        '200':
          description: Processed file
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ToolResponse'
        '400':
          description: Invalid input or settings
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Error'
        '401':
          description: Authentication required
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/UnauthorizedError'
        '422':
          description: Processing failed
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Error'
  /api/v1/tools/image/png-to-jpg:
    post:
      operationId: pngToJpgTool
      tags:
        - Tools
      summary: PNG у JPG
      description: |
        Конвертувати PNG у JPG
        Це спеціальний ендпоінт-пресет конвертації на основі `convert`.
      security:
        - bearerAuth: []
      requestBody:
        required: true
        content:
          multipart/form-data:
            schema:
              type: object
              required:
                - file
              properties:
                file:
                  type: string
                  format: binary
                  description: PNG to JPG input file (.png)
                settings:
                  type: string
                  description: |
                    Dedicated PNG to JPG converter using the convert pipeline.
                    Optional settings: quality (number 1-100). The output format is locked by the endpoint.
                clientJobId:
                  type: string
                  description: Client-provided job ID for SSE progress tracking
      responses:
        '200':
          description: Processed file
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ToolResponse'
        '400':
          description: Invalid input or settings
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Error'
        '401':
          description: Authentication required
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/UnauthorizedError'
        '422':
          description: Processing failed
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Error'
  /api/v1/tools/image/jpg-to-webp:
    post:
      operationId: jpgToWebpTool
      tags:
        - Tools
      summary: JPG у WebP
      description: |
        Конвертувати JPG у WebP
        Це спеціальний ендпоінт-пресет конвертації на основі `convert`.
      security:
        - bearerAuth: []
      requestBody:
        required: true
        content:
          multipart/form-data:
            schema:
              type: object
              required:
                - file
              properties:
                file:
                  type: string
                  format: binary
                  description: JPG to WebP input file (.jpg, .jpeg)
                settings:
                  type: string
                  description: |
                    Dedicated JPG to WebP converter using the convert pipeline.
                    Optional settings: quality (number 1-100). The output format is locked by the endpoint.
                clientJobId:
                  type: string
                  description: Client-provided job ID for SSE progress tracking
      responses:
        '200':
          description: Processed file
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ToolResponse'
        '400':
          description: Invalid input or settings
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Error'
        '401':
          description: Authentication required
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/UnauthorizedError'
        '422':
          description: Processing failed
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Error'
  /api/v1/tools/image/png-to-webp:
    post:
      operationId: pngToWebpTool
      tags:
        - Tools
      summary: PNG у WebP
      description: |
        Конвертувати PNG у WebP
        Це спеціальний ендпоінт-пресет конвертації на основі `convert`.
      security:
        - bearerAuth: []
      requestBody:
        required: true
        content:
          multipart/form-data:
            schema:
              type: object
              required:
                - file
              properties:
                file:
                  type: string
                  format: binary
                  description: PNG to WebP input file (.png)
                settings:
                  type: string
                  description: |
                    Dedicated PNG to WebP converter using the convert pipeline.
                    Optional settings: quality (number 1-100). The output format is locked by the endpoint.
                clientJobId:
                  type: string
                  description: Client-provided job ID for SSE progress tracking
      responses:
        '200':
          description: Processed file
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ToolResponse'
        '400':
          description: Invalid input or settings
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Error'
        '401':
          description: Authentication required
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/UnauthorizedError'
        '422':
          description: Processing failed
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Error'
  /api/v1/tools/image/webp-to-jpg:
    post:
      operationId: webpToJpgTool
      tags:
        - Tools
      summary: WebP у JPG
      description: |
        Конвертувати WebP у JPG
        Це спеціальний ендпоінт-пресет конвертації на основі `convert`.
      security:
        - bearerAuth: []
      requestBody:
        required: true
        content:
          multipart/form-data:
            schema:
              type: object
              required:
                - file
              properties:
                file:
                  type: string
                  format: binary
                  description: WebP to JPG input file (.webp)
                settings:
                  type: string
                  description: |
                    Dedicated WebP to JPG converter using the convert pipeline.
                    Optional settings: quality (number 1-100). The output format is locked by the endpoint.
                clientJobId:
                  type: string
                  description: Client-provided job ID for SSE progress tracking
      responses:
        '200':
          description: Processed file
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ToolResponse'
        '400':
          description: Invalid input or settings
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Error'
        '401':
          description: Authentication required
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/UnauthorizedError'
        '422':
          description: Processing failed
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Error'
  /api/v1/tools/image/webp-to-png:
    post:
      operationId: webpToPngTool
      tags:
        - Tools
      summary: WebP у PNG
      description: |
        Конвертувати WebP у PNG
        Це спеціальний ендпоінт-пресет конвертації на основі `convert`.
      security:
        - bearerAuth: []
      requestBody:
        required: true
        content:
          multipart/form-data:
            schema:
              type: object
              required:
                - file
              properties:
                file:
                  type: string
                  format: binary
                  description: WebP to PNG input file (.webp)
                settings:
                  type: string
                  description: |
                    Dedicated WebP to PNG converter using the convert pipeline.
                    Optional settings: quality (number 1-100). The output format is locked by the endpoint.
                clientJobId:
                  type: string
                  description: Client-provided job ID for SSE progress tracking
      responses:
        '200':
          description: Processed file
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ToolResponse'
        '400':
          description: Invalid input or settings
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Error'
        '401':
          description: Authentication required
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/UnauthorizedError'
        '422':
          description: Processing failed
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Error'
  /api/v1/tools/image/jpg-to-avif:
    post:
      operationId: jpgToAvifTool
      tags:
        - Tools
      summary: JPG у AVIF
      description: |
        Конвертувати JPG у AVIF
        Це спеціальний ендпоінт-пресет конвертації на основі `convert`.
      security:
        - bearerAuth: []
      requestBody:
        required: true
        content:
          multipart/form-data:
            schema:
              type: object
              required:
                - file
              properties:
                file:
                  type: string
                  format: binary
                  description: JPG to AVIF input file (.jpg, .jpeg)
                settings:
                  type: string
                  description: |
                    Dedicated JPG to AVIF converter using the convert pipeline.
                    Optional settings: quality (number 1-100). The output format is locked by the endpoint.
                clientJobId:
                  type: string
                  description: Client-provided job ID for SSE progress tracking
      responses:
        '200':
          description: Processed file
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ToolResponse'
        '400':
          description: Invalid input or settings
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Error'
        '401':
          description: Authentication required
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/UnauthorizedError'
        '422':
          description: Processing failed
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Error'
  /api/v1/tools/image/png-to-avif:
    post:
      operationId: pngToAvifTool
      tags:
        - Tools
      summary: PNG у AVIF
      description: |
        Конвертувати PNG у AVIF
        Це спеціальний ендпоінт-пресет конвертації на основі `convert`.
      security:
        - bearerAuth: []
      requestBody:
        required: true
        content:
          multipart/form-data:
            schema:
              type: object
              required:
                - file
              properties:
                file:
                  type: string
                  format: binary
                  description: PNG to AVIF input file (.png)
                settings:
                  type: string
                  description: |
                    Dedicated PNG to AVIF converter using the convert pipeline.
                    Optional settings: quality (number 1-100). The output format is locked by the endpoint.
                clientJobId:
                  type: string
                  description: Client-provided job ID for SSE progress tracking
      responses:
        '200':
          description: Processed file
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ToolResponse'
        '400':
          description: Invalid input or settings
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Error'
        '401':
          description: Authentication required
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/UnauthorizedError'
        '422':
          description: Processing failed
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Error'
  /api/v1/tools/image/webp-to-avif:
    post:
      operationId: webpToAvifTool
      tags:
        - Tools
      summary: WebP у AVIF
      description: |
        Конвертувати WebP у AVIF
        Це спеціальний ендпоінт-пресет конвертації на основі `convert`.
      security:
        - bearerAuth: []
      requestBody:
        required: true
        content:
          multipart/form-data:
            schema:
              type: object
              required:
                - file
              properties:
                file:
                  type: string
                  format: binary
                  description: WebP to AVIF input file (.webp)
                settings:
                  type: string
                  description: |
                    Dedicated WebP to AVIF converter using the convert pipeline.
                    Optional settings: quality (number 1-100). The output format is locked by the endpoint.
                clientJobId:
                  type: string
                  description: Client-provided job ID for SSE progress tracking
      responses:
        '200':
          description: Processed file
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ToolResponse'
        '400':
          description: Invalid input or settings
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Error'
        '401':
          description: Authentication required
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/UnauthorizedError'
        '422':
          description: Processing failed
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Error'
  /api/v1/tools/image/heic-to-jpg:
    post:
      operationId: heicToJpgTool
      tags:
        - Tools
      summary: HEIC у JPG
      description: |
        Конвертувати HEIC у JPG
        Це спеціальний ендпоінт-пресет конвертації на основі `convert`.
      security:
        - bearerAuth: []
      requestBody:
        required: true
        content:
          multipart/form-data:
            schema:
              type: object
              required:
                - file
              properties:
                file:
                  type: string
                  format: binary
                  description: HEIC to JPG input file (.heic, .heif)
                settings:
                  type: string
                  description: |
                    Dedicated HEIC to JPG converter using the convert pipeline.
                    Optional settings: quality (number 1-100). The output format is locked by the endpoint.
                clientJobId:
                  type: string
                  description: Client-provided job ID for SSE progress tracking
      responses:
        '200':
          description: Processed file
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ToolResponse'
        '400':
          description: Invalid input or settings
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Error'
        '401':
          description: Authentication required
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/UnauthorizedError'
        '422':
          description: Processing failed
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Error'
  /api/v1/tools/image/heic-to-png:
    post:
      operationId: heicToPngTool
      tags:
        - Tools
      summary: HEIC у PNG
      description: |
        Конвертувати HEIC у PNG
        Це спеціальний ендпоінт-пресет конвертації на основі `convert`.
      security:
        - bearerAuth: []
      requestBody:
        required: true
        content:
          multipart/form-data:
            schema:
              type: object
              required:
                - file
              properties:
                file:
                  type: string
                  format: binary
                  description: HEIC to PNG input file (.heic, .heif)
                settings:
                  type: string
                  description: |
                    Dedicated HEIC to PNG converter using the convert pipeline.
                    Optional settings: quality (number 1-100). The output format is locked by the endpoint.
                clientJobId:
                  type: string
                  description: Client-provided job ID for SSE progress tracking
      responses:
        '200':
          description: Processed file
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ToolResponse'
        '400':
          description: Invalid input or settings
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Error'
        '401':
          description: Authentication required
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/UnauthorizedError'
        '422':
          description: Processing failed
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Error'
  /api/v1/tools/image/heic-to-avif:
    post:
      operationId: heicToAvifTool
      tags:
        - Tools
      summary: HEIC у AVIF
      description: |
        Конвертувати HEIC у AVIF
        Це спеціальний ендпоінт-пресет конвертації на основі `convert`.
      security:
        - bearerAuth: []
      requestBody:
        required: true
        content:
          multipart/form-data:
            schema:
              type: object
              required:
                - file
              properties:
                file:
                  type: string
                  format: binary
                  description: HEIC to AVIF input file (.heic, .heif)
                settings:
                  type: string
                  description: |
                    Dedicated HEIC to AVIF converter using the convert pipeline.
                    Optional settings: quality (number 1-100). The output format is locked by the endpoint.
                clientJobId:
                  type: string
                  description: Client-provided job ID for SSE progress tracking
      responses:
        '200':
          description: Processed file
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ToolResponse'
        '400':
          description: Invalid input or settings
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Error'
        '401':
          description: Authentication required
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/UnauthorizedError'
        '422':
          description: Processing failed
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Error'
  /api/v1/tools/image/jpg-to-gif:
    post:
      operationId: jpgToGifTool
      tags:
        - Tools
      summary: JPG у GIF
      description: |
        Конвертувати JPG у GIF
        Це спеціальний ендпоінт-пресет конвертації на основі `convert`.
      security:
        - bearerAuth: []
      requestBody:
        required: true
        content:
          multipart/form-data:
            schema:
              type: object
              required:
                - file
              properties:
                file:
                  type: string
                  format: binary
                  description: JPG to GIF input file (.jpg, .jpeg)
                settings:
                  type: string
                  description: |
                    Dedicated JPG to GIF converter using the convert pipeline.
                    Optional settings: quality (number 1-100). The output format is locked by the endpoint.
                clientJobId:
                  type: string
                  description: Client-provided job ID for SSE progress tracking
      responses:
        '200':
          description: Processed file
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ToolResponse'
        '400':
          description: Invalid input or settings
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Error'
        '401':
          description: Authentication required
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/UnauthorizedError'
        '422':
          description: Processing failed
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Error'
  /api/v1/tools/image/png-to-gif:
    post:
      operationId: pngToGifTool
      tags:
        - Tools
      summary: PNG у GIF
      description: |
        Конвертувати PNG у GIF
        Це спеціальний ендпоінт-пресет конвертації на основі `convert`.
      security:
        - bearerAuth: []
      requestBody:
        required: true
        content:
          multipart/form-data:
            schema:
              type: object
              required:
                - file
              properties:
                file:
                  type: string
                  format: binary
                  description: PNG to GIF input file (.png)
                settings:
                  type: string
                  description: |
                    Dedicated PNG to GIF converter using the convert pipeline.
                    Optional settings: quality (number 1-100). The output format is locked by the endpoint.
                clientJobId:
                  type: string
                  description: Client-provided job ID for SSE progress tracking
      responses:
        '200':
          description: Processed file
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ToolResponse'
        '400':
          description: Invalid input or settings
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Error'
        '401':
          description: Authentication required
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/UnauthorizedError'
        '422':
          description: Processing failed
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Error'
  /api/v1/tools/image/gif-to-jpg:
    post:
      operationId: gifToJpgTool
      tags:
        - Tools
      summary: GIF у JPG
      description: |
        Конвертувати GIF у JPG
        Це спеціальний ендпоінт-пресет конвертації на основі `convert`.
      security:
        - bearerAuth: []
      requestBody:
        required: true
        content:
          multipart/form-data:
            schema:
              type: object
              required:
                - file
              properties:
                file:
                  type: string
                  format: binary
                  description: GIF to JPG input file (.gif)
                settings:
                  type: string
                  description: |
                    Dedicated GIF to JPG converter using the convert pipeline.
                    Optional settings: quality (number 1-100). The output format is locked by the endpoint.
                clientJobId:
                  type: string
                  description: Client-provided job ID for SSE progress tracking
      responses:
        '200':
          description: Processed file
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ToolResponse'
        '400':
          description: Invalid input or settings
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Error'
        '401':
          description: Authentication required
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/UnauthorizedError'
        '422':
          description: Processing failed
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Error'
  /api/v1/tools/image/gif-to-png:
    post:
      operationId: gifToPngTool
      tags:
        - Tools
      summary: GIF у PNG
      description: |
        Конвертувати GIF у PNG
        Це спеціальний ендпоінт-пресет конвертації на основі `convert`.
      security:
        - bearerAuth: []
      requestBody:
        required: true
        content:
          multipart/form-data:
            schema:
              type: object
              required:
                - file
              properties:
                file:
                  type: string
                  format: binary
                  description: GIF to PNG input file (.gif)
                settings:
                  type: string
                  description: |
                    Dedicated GIF to PNG converter using the convert pipeline.
                    Optional settings: quality (number 1-100). The output format is locked by the endpoint.
                clientJobId:
                  type: string
                  description: Client-provided job ID for SSE progress tracking
      responses:
        '200':
          description: Processed file
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ToolResponse'
        '400':
          description: Invalid input or settings
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Error'
        '401':
          description: Authentication required
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/UnauthorizedError'
        '422':
          description: Processing failed
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Error'
  /api/v1/tools/image/webp-to-gif:
    post:
      operationId: webpToGifTool
      tags:
        - Tools
      summary: WebP у GIF
      description: |
        Конвертувати WebP у GIF
        Це спеціальний ендпоінт-пресет конвертації на основі `convert`.
      security:
        - bearerAuth: []
      requestBody:
        required: true
        content:
          multipart/form-data:
            schema:
              type: object
              required:
                - file
              properties:
                file:
                  type: string
                  format: binary
                  description: WebP to GIF input file (.webp)
                settings:
                  type: string
                  description: |
                    Dedicated WebP to GIF converter using the convert pipeline.
                    Optional settings: quality (number 1-100). The output format is locked by the endpoint.
                clientJobId:
                  type: string
                  description: Client-provided job ID for SSE progress tracking
      responses:
        '200':
          description: Processed file
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ToolResponse'
        '400':
          description: Invalid input or settings
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Error'
        '401':
          description: Authentication required
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/UnauthorizedError'
        '422':
          description: Processing failed
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Error'
  /api/v1/tools/image/jpg-to-tiff:
    post:
      operationId: jpgToTiffTool
      tags:
        - Tools
      summary: JPG у TIFF
      description: |
        Конвертувати JPG у TIFF
        Це спеціальний ендпоінт-пресет конвертації на основі `convert`.
      security:
        - bearerAuth: []
      requestBody:
        required: true
        content:
          multipart/form-data:
            schema:
              type: object
              required:
                - file
              properties:
                file:
                  type: string
                  format: binary
                  description: JPG to TIFF input file (.jpg, .jpeg)
                settings:
                  type: string
                  description: |
                    Dedicated JPG to TIFF converter using the convert pipeline.
                    Optional settings: quality (number 1-100). The output format is locked by the endpoint.
                clientJobId:
                  type: string
                  description: Client-provided job ID for SSE progress tracking
      responses:
        '200':
          description: Processed file
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ToolResponse'
        '400':
          description: Invalid input or settings
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Error'
        '401':
          description: Authentication required
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/UnauthorizedError'
        '422':
          description: Processing failed
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Error'
  /api/v1/tools/image/png-to-tiff:
    post:
      operationId: pngToTiffTool
      tags:
        - Tools
      summary: PNG у TIFF
      description: |
        Конвертувати PNG у TIFF
        Це спеціальний ендпоінт-пресет конвертації на основі `convert`.
      security:
        - bearerAuth: []
      requestBody:
        required: true
        content:
          multipart/form-data:
            schema:
              type: object
              required:
                - file
              properties:
                file:
                  type: string
                  format: binary
                  description: PNG to TIFF input file (.png)
                settings:
                  type: string
                  description: |
                    Dedicated PNG to TIFF converter using the convert pipeline.
                    Optional settings: quality (number 1-100). The output format is locked by the endpoint.
                clientJobId:
                  type: string
                  description: Client-provided job ID for SSE progress tracking
      responses:
        '200':
          description: Processed file
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ToolResponse'
        '400':
          description: Invalid input or settings
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Error'
        '401':
          description: Authentication required
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/UnauthorizedError'
        '422':
          description: Processing failed
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Error'
  /api/v1/tools/image/tiff-to-jpg:
    post:
      operationId: tiffToJpgTool
      tags:
        - Tools
      summary: TIFF у JPG
      description: |
        Конвертувати TIFF у JPG
        Це спеціальний ендпоінт-пресет конвертації на основі `convert`.
      security:
        - bearerAuth: []
      requestBody:
        required: true
        content:
          multipart/form-data:
            schema:
              type: object
              required:
                - file
              properties:
                file:
                  type: string
                  format: binary
                  description: TIFF to JPG input file (.tiff, .tif)
                settings:
                  type: string
                  description: |
                    Dedicated TIFF to JPG converter using the convert pipeline.
                    Optional settings: quality (number 1-100). The output format is locked by the endpoint.
                clientJobId:
                  type: string
                  description: Client-provided job ID for SSE progress tracking
      responses:
        '200':
          description: Processed file
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ToolResponse'
        '400':
          description: Invalid input or settings
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Error'
        '401':
          description: Authentication required
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/UnauthorizedError'
        '422':
          description: Processing failed
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Error'
  /api/v1/tools/image/tiff-to-png:
    post:
      operationId: tiffToPngTool
      tags:
        - Tools
      summary: TIFF у PNG
      description: |
        Конвертувати TIFF у PNG
        Це спеціальний ендпоінт-пресет конвертації на основі `convert`.
      security:
        - bearerAuth: []
      requestBody:
        required: true
        content:
          multipart/form-data:
            schema:
              type: object
              required:
                - file
              properties:
                file:
                  type: string
                  format: binary
                  description: TIFF to PNG input file (.tiff, .tif)
                settings:
                  type: string
                  description: |
                    Dedicated TIFF to PNG converter using the convert pipeline.
                    Optional settings: quality (number 1-100). The output format is locked by the endpoint.
                clientJobId:
                  type: string
                  description: Client-provided job ID for SSE progress tracking
      responses:
        '200':
          description: Processed file
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ToolResponse'
        '400':
          description: Invalid input or settings
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Error'
        '401':
          description: Authentication required
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/UnauthorizedError'
        '422':
          description: Processing failed
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Error'
  /api/v1/tools/image/psd-to-jpg:
    post:
      operationId: psdToJpgTool
      tags:
        - Tools
      summary: PSD у JPG
      description: |
        Конвертувати PSD у JPG
        Це спеціальний ендпоінт-пресет конвертації на основі `convert`.
      security:
        - bearerAuth: []
      requestBody:
        required: true
        content:
          multipart/form-data:
            schema:
              type: object
              required:
                - file
              properties:
                file:
                  type: string
                  format: binary
                  description: PSD to JPG input file (.psd)
                settings:
                  type: string
                  description: |
                    Dedicated PSD to JPG converter using the convert pipeline.
                    Optional settings: quality (number 1-100). The output format is locked by the endpoint.
                clientJobId:
                  type: string
                  description: Client-provided job ID for SSE progress tracking
      responses:
        '200':
          description: Processed file
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ToolResponse'
        '400':
          description: Invalid input or settings
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Error'
        '401':
          description: Authentication required
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/UnauthorizedError'
        '422':
          description: Processing failed
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Error'
  /api/v1/tools/image/psd-to-png:
    post:
      operationId: psdToPngTool
      tags:
        - Tools
      summary: PSD у PNG
      description: |
        Конвертувати PSD у PNG
        Це спеціальний ендпоінт-пресет конвертації на основі `convert`.
      security:
        - bearerAuth: []
      requestBody:
        required: true
        content:
          multipart/form-data:
            schema:
              type: object
              required:
                - file
              properties:
                file:
                  type: string
                  format: binary
                  description: PSD to PNG input file (.psd)
                settings:
                  type: string
                  description: |
                    Dedicated PSD to PNG converter using the convert pipeline.
                    Optional settings: quality (number 1-100). The output format is locked by the endpoint.
                clientJobId:
                  type: string
                  description: Client-provided job ID for SSE progress tracking
      responses:
        '200':
          description: Processed file
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ToolResponse'
        '400':
          description: Invalid input or settings
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Error'
        '401':
          description: Authentication required
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/UnauthorizedError'
        '422':
          description: Processing failed
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Error'
  /api/v1/tools/image/png-to-eps:
    post:
      operationId: pngToEpsTool
      tags:
        - Tools
      summary: PNG у EPS
      description: |
        Конвертувати PNG у EPS
        Це спеціальний ендпоінт-пресет конвертації на основі `convert`.
      security:
        - bearerAuth: []
      requestBody:
        required: true
        content:
          multipart/form-data:
            schema:
              type: object
              required:
                - file
              properties:
                file:
                  type: string
                  format: binary
                  description: PNG to EPS input file (.png)
                settings:
                  type: string
                  description: |
                    Dedicated PNG to EPS converter using the convert pipeline.
                    Optional settings: quality (number 1-100). The output format is locked by the endpoint.
                clientJobId:
                  type: string
                  description: Client-provided job ID for SSE progress tracking
      responses:
        '200':
          description: Processed file
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ToolResponse'
        '400':
          description: Invalid input or settings
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Error'
        '401':
          description: Authentication required
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/UnauthorizedError'
        '422':
          description: Processing failed
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Error'
  /api/v1/tools/image/jpg-to-eps:
    post:
      operationId: jpgToEpsTool
      tags:
        - Tools
      summary: JPG у EPS
      description: |
        Конвертувати JPG у EPS
        Це спеціальний ендпоінт-пресет конвертації на основі `convert`.
      security:
        - bearerAuth: []
      requestBody:
        required: true
        content:
          multipart/form-data:
            schema:
              type: object
              required:
                - file
              properties:
                file:
                  type: string
                  format: binary
                  description: JPG to EPS input file (.jpg, .jpeg)
                settings:
                  type: string
                  description: |
                    Dedicated JPG to EPS converter using the convert pipeline.
                    Optional settings: quality (number 1-100). The output format is locked by the endpoint.
                clientJobId:
                  type: string
                  description: Client-provided job ID for SSE progress tracking
      responses:
        '200':
          description: Processed file
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ToolResponse'
        '400':
          description: Invalid input or settings
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Error'
        '401':
          description: Authentication required
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/UnauthorizedError'
        '422':
          description: Processing failed
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Error'
  /api/v1/tools/image/eps-to-png:
    post:
      operationId: epsToPngTool
      tags:
        - Tools
      summary: EPS у PNG
      description: |
        Конвертувати EPS у PNG
        Це спеціальний ендпоінт-пресет конвертації на основі `convert`.
      security:
        - bearerAuth: []
      requestBody:
        required: true
        content:
          multipart/form-data:
            schema:
              type: object
              required:
                - file
              properties:
                file:
                  type: string
                  format: binary
                  description: EPS to PNG input file (.eps)
                settings:
                  type: string
                  description: |
                    Dedicated EPS to PNG converter using the convert pipeline.
                    Optional settings: quality (number 1-100). The output format is locked by the endpoint.
                clientJobId:
                  type: string
                  description: Client-provided job ID for SSE progress tracking
      responses:
        '200':
          description: Processed file
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ToolResponse'
        '400':
          description: Invalid input or settings
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Error'
        '401':
          description: Authentication required
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/UnauthorizedError'
        '422':
          description: Processing failed
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Error'
  /api/v1/tools/image/eps-to-jpg:
    post:
      operationId: epsToJpgTool
      tags:
        - Tools
      summary: EPS у JPG
      description: |
        Конвертувати EPS у JPG
        Це спеціальний ендпоінт-пресет конвертації на основі `convert`.
      security:
        - bearerAuth: []
      requestBody:
        required: true
        content:
          multipart/form-data:
            schema:
              type: object
              required:
                - file
              properties:
                file:
                  type: string
                  format: binary
                  description: EPS to JPG input file (.eps)
                settings:
                  type: string
                  description: |
                    Dedicated EPS to JPG converter using the convert pipeline.
                    Optional settings: quality (number 1-100). The output format is locked by the endpoint.
                clientJobId:
                  type: string
                  description: Client-provided job ID for SSE progress tracking
      responses:
        '200':
          description: Processed file
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ToolResponse'
        '400':
          description: Invalid input or settings
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Error'
        '401':
          description: Authentication required
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/UnauthorizedError'
        '422':
          description: Processing failed
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Error'
  /api/v1/tools/image/png-to-svg:
    post:
      operationId: pngToSvgTool
      tags:
        - Tools
      summary: PNG у SVG
      description: |
        Конвертувати PNG у SVG
        Це спеціальний ендпоінт-пресет конвертації на основі `vectorize`.
      security:
        - bearerAuth: []
      requestBody:
        required: true
        content:
          multipart/form-data:
            schema:
              type: object
              required:
                - file
              properties:
                file:
                  type: string
                  format: binary
                  description: PNG to SVG input file (.png)
                settings:
                  type: string
                  description: |
                    Dedicated PNG to SVG converter using the vectorize pipeline.
                    The output format is locked by the endpoint.
                clientJobId:
                  type: string
                  description: Client-provided job ID for SSE progress tracking
      responses:
        '200':
          description: Processed file
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ToolResponse'
        '400':
          description: Invalid input or settings
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Error'
        '401':
          description: Authentication required
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/UnauthorizedError'
        '422':
          description: Processing failed
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Error'
  /api/v1/tools/image/jpg-to-svg:
    post:
      operationId: jpgToSvgTool
      tags:
        - Tools
      summary: JPG у SVG
      description: |
        Конвертувати JPG у SVG
        Це спеціальний ендпоінт-пресет конвертації на основі `vectorize`.
      security:
        - bearerAuth: []
      requestBody:
        required: true
        content:
          multipart/form-data:
            schema:
              type: object
              required:
                - file
              properties:
                file:
                  type: string
                  format: binary
                  description: JPG to SVG input file (.jpg, .jpeg)
                settings:
                  type: string
                  description: |
                    Dedicated JPG to SVG converter using the vectorize pipeline.
                    The output format is locked by the endpoint.
                clientJobId:
                  type: string
                  description: Client-provided job ID for SSE progress tracking
      responses:
        '200':
          description: Processed file
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ToolResponse'
        '400':
          description: Invalid input or settings
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Error'
        '401':
          description: Authentication required
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/UnauthorizedError'
        '422':
          description: Processing failed
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Error'
  /api/v1/tools/image/tiff-to-svg:
    post:
      operationId: tiffToSvgTool
      tags:
        - Tools
      summary: TIFF у SVG
      description: |
        Конвертувати TIFF у SVG
        Це спеціальний ендпоінт-пресет конвертації на основі `vectorize`.
      security:
        - bearerAuth: []
      requestBody:
        required: true
        content:
          multipart/form-data:
            schema:
              type: object
              required:
                - file
              properties:
                file:
                  type: string
                  format: binary
                  description: TIFF to SVG input file (.tiff, .tif)
                settings:
                  type: string
                  description: |
                    Dedicated TIFF to SVG converter using the vectorize pipeline.
                    The output format is locked by the endpoint.
                clientJobId:
                  type: string
                  description: Client-provided job ID for SSE progress tracking
      responses:
        '200':
          description: Processed file
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ToolResponse'
        '400':
          description: Invalid input or settings
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Error'
        '401':
          description: Authentication required
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/UnauthorizedError'
        '422':
          description: Processing failed
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Error'
  /api/v1/tools/image/psd-to-svg:
    post:
      operationId: psdToSvgTool
      tags:
        - Tools
      summary: PSD у SVG
      description: |
        Конвертувати PSD у SVG
        Це спеціальний ендпоінт-пресет конвертації на основі `vectorize`.
      security:
        - bearerAuth: []
      requestBody:
        required: true
        content:
          multipart/form-data:
            schema:
              type: object
              required:
                - file
              properties:
                file:
                  type: string
                  format: binary
                  description: PSD to SVG input file (.psd)
                settings:
                  type: string
                  description: |
                    Dedicated PSD to SVG converter using the vectorize pipeline.
                    The output format is locked by the endpoint.
                clientJobId:
                  type: string
                  description: Client-provided job ID for SSE progress tracking
      responses:
        '200':
          description: Processed file
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ToolResponse'
        '400':
          description: Invalid input or settings
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Error'
        '401':
          description: Authentication required
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/UnauthorizedError'
        '422':
          description: Processing failed
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Error'
  /api/v1/tools/image/eps-to-svg:
    post:
      operationId: epsToSvgTool
      tags:
        - Tools
      summary: EPS у SVG
      description: |
        Конвертувати EPS у SVG
        Це спеціальний ендпоінт-пресет конвертації на основі `vectorize`.
      security:
        - bearerAuth: []
      requestBody:
        required: true
        content:
          multipart/form-data:
            schema:
              type: object
              required:
                - file
              properties:
                file:
                  type: string
                  format: binary
                  description: EPS to SVG input file (.eps)
                settings:
                  type: string
                  description: |
                    Dedicated EPS to SVG converter using the vectorize pipeline.
                    The output format is locked by the endpoint.
                clientJobId:
                  type: string
                  description: Client-provided job ID for SSE progress tracking
      responses:
        '200':
          description: Processed file
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ToolResponse'
        '400':
          description: Invalid input or settings
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Error'
        '401':
          description: Authentication required
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/UnauthorizedError'
        '422':
          description: Processing failed
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Error'
  /api/v1/tools/image/svg-to-png:
    post:
      operationId: svgToPngTool
      tags:
        - Tools
      summary: SVG у PNG
      description: |
        Конвертувати SVG у PNG
        Це спеціальний ендпоінт-пресет конвертації на основі `svg-to-raster`.
      security:
        - bearerAuth: []
      requestBody:
        required: true
        content:
          multipart/form-data:
            schema:
              type: object
              required:
                - file
              properties:
                file:
                  type: string
                  format: binary
                  description: SVG to PNG input file (.svg, .svgz)
                settings:
                  type: string
                  description: |
                    Dedicated SVG to PNG converter using the svg-to-raster pipeline.
                    Optional settings: quality (number 1-100). The output format is locked by the endpoint.
                clientJobId:
                  type: string
                  description: Client-provided job ID for SSE progress tracking
      responses:
        '200':
          description: Processed file
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ToolResponse'
        '400':
          description: Invalid input or settings
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Error'
        '401':
          description: Authentication required
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/UnauthorizedError'
        '422':
          description: Processing failed
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Error'
  /api/v1/tools/image/svg-to-jpg:
    post:
      operationId: svgToJpgTool
      tags:
        - Tools
      summary: SVG у JPG
      description: |
        Конвертувати SVG у JPG
        Це спеціальний ендпоінт-пресет конвертації на основі `svg-to-raster`.
      security:
        - bearerAuth: []
      requestBody:
        required: true
        content:
          multipart/form-data:
            schema:
              type: object
              required:
                - file
              properties:
                file:
                  type: string
                  format: binary
                  description: SVG to JPG input file (.svg, .svgz)
                settings:
                  type: string
                  description: |
                    Dedicated SVG to JPG converter using the svg-to-raster pipeline.
                    Optional settings: quality (number 1-100). The output format is locked by the endpoint.
                clientJobId:
                  type: string
                  description: Client-provided job ID for SSE progress tracking
      responses:
        '200':
          description: Processed file
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ToolResponse'
        '400':
          description: Invalid input or settings
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Error'
        '401':
          description: Authentication required
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/UnauthorizedError'
        '422':
          description: Processing failed
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Error'
  /api/v1/tools/image/jpg-to-pdf:
    post:
      operationId: jpgToPdfTool
      tags:
        - Tools
      summary: JPG у PDF
      description: |
        Конвертувати JPG у PDF
        Це спеціальний ендпоінт-пресет конвертації на основі `image-to-pdf`.
      security:
        - bearerAuth: []
      requestBody:
        required: true
        content:
          multipart/form-data:
            schema:
              type: object
              required:
                - file
              properties:
                file:
                  type: array
                  items:
                    type: string
                    format: binary
                  description: JPG to PDF input files (.jpg, .jpeg)
                settings:
                  type: string
                  description: |
                    Dedicated JPG to PDF converter using the image-to-pdf pipeline.
                    Optional settings: pageSize (A4, Letter, A3, A5), orientation (portrait or landscape), margin, targetSize, and collate.
                clientJobId:
                  type: string
                  description: Client-provided job ID for SSE progress tracking
      responses:
        '200':
          description: Processed file
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ToolResponse'
        '400':
          description: Invalid input or settings
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Error'
        '401':
          description: Authentication required
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/UnauthorizedError'
        '422':
          description: Processing failed
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Error'
  /api/v1/tools/image/png-to-pdf:
    post:
      operationId: pngToPdfTool
      tags:
        - Tools
      summary: PNG у PDF
      description: |
        Конвертувати PNG у PDF
        Це спеціальний ендпоінт-пресет конвертації на основі `image-to-pdf`.
      security:
        - bearerAuth: []
      requestBody:
        required: true
        content:
          multipart/form-data:
            schema:
              type: object
              required:
                - file
              properties:
                file:
                  type: array
                  items:
                    type: string
                    format: binary
                  description: PNG to PDF input files (.png)
                settings:
                  type: string
                  description: |
                    Dedicated PNG to PDF converter using the image-to-pdf pipeline.
                    Optional settings: pageSize (A4, Letter, A3, A5), orientation (portrait or landscape), margin, targetSize, and collate.
                clientJobId:
                  type: string
                  description: Client-provided job ID for SSE progress tracking
      responses:
        '200':
          description: Processed file
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ToolResponse'
        '400':
          description: Invalid input or settings
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Error'
        '401':
          description: Authentication required
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/UnauthorizedError'
        '422':
          description: Processing failed
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Error'
  /api/v1/tools/image/heic-to-pdf:
    post:
      operationId: heicToPdfTool
      tags:
        - Tools
      summary: HEIC у PDF
      description: |
        Конвертувати HEIC у PDF
        Це спеціальний ендпоінт-пресет конвертації на основі `image-to-pdf`.
      security:
        - bearerAuth: []
      requestBody:
        required: true
        content:
          multipart/form-data:
            schema:
              type: object
              required:
                - file
              properties:
                file:
                  type: array
                  items:
                    type: string
                    format: binary
                  description: HEIC to PDF input files (.heic, .heif)
                settings:
                  type: string
                  description: |
                    Dedicated HEIC to PDF converter using the image-to-pdf pipeline.
                    Optional settings: pageSize (A4, Letter, A3, A5), orientation (portrait or landscape), margin, targetSize, and collate.
                clientJobId:
                  type: string
                  description: Client-provided job ID for SSE progress tracking
      responses:
        '200':
          description: Processed file
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ToolResponse'
        '400':
          description: Invalid input or settings
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Error'
        '401':
          description: Authentication required
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/UnauthorizedError'
        '422':
          description: Processing failed
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Error'
  /api/v1/tools/image/tiff-to-pdf:
    post:
      operationId: tiffToPdfTool
      tags:
        - Tools
      summary: TIFF у PDF
      description: |
        Конвертувати TIFF у PDF
        Це спеціальний ендпоінт-пресет конвертації на основі `image-to-pdf`.
      security:
        - bearerAuth: []
      requestBody:
        required: true
        content:
          multipart/form-data:
            schema:
              type: object
              required:
                - file
              properties:
                file:
                  type: array
                  items:
                    type: string
                    format: binary
                  description: TIFF to PDF input files (.tiff, .tif)
                settings:
                  type: string
                  description: |
                    Dedicated TIFF to PDF converter using the image-to-pdf pipeline.
                    Optional settings: pageSize (A4, Letter, A3, A5), orientation (portrait or landscape), margin, targetSize, and collate.
                clientJobId:
                  type: string
                  description: Client-provided job ID for SSE progress tracking
      responses:
        '200':
          description: Processed file
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ToolResponse'
        '400':
          description: Invalid input or settings
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Error'
        '401':
          description: Authentication required
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/UnauthorizedError'
        '422':
          description: Processing failed
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Error'
  /api/v1/tools/image/webp-to-pdf:
    post:
      operationId: webpToPdfTool
      tags:
        - Tools
      summary: WebP у PDF
      description: |
        Конвертувати WebP у PDF
        Це спеціальний ендпоінт-пресет конвертації на основі `image-to-pdf`.
      security:
        - bearerAuth: []
      requestBody:
        required: true
        content:
          multipart/form-data:
            schema:
              type: object
              required:
                - file
              properties:
                file:
                  type: array
                  items:
                    type: string
                    format: binary
                  description: WebP to PDF input files (.webp)
                settings:
                  type: string
                  description: |
                    Dedicated WebP to PDF converter using the image-to-pdf pipeline.
                    Optional settings: pageSize (A4, Letter, A3, A5), orientation (portrait or landscape), margin, targetSize, and collate.
                clientJobId:
                  type: string
                  description: Client-provided job ID for SSE progress tracking
      responses:
        '200':
          description: Processed file
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ToolResponse'
        '400':
          description: Invalid input or settings
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Error'
        '401':
          description: Authentication required
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/UnauthorizedError'
        '422':
          description: Processing failed
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Error'
  /api/v1/tools/image/gif-to-pdf:
    post:
      operationId: gifToPdfTool
      tags:
        - Tools
      summary: GIF у PDF
      description: |
        Конвертувати GIF у PDF
        Це спеціальний ендпоінт-пресет конвертації на основі `image-to-pdf`.
      security:
        - bearerAuth: []
      requestBody:
        required: true
        content:
          multipart/form-data:
            schema:
              type: object
              required:
                - file
              properties:
                file:
                  type: array
                  items:
                    type: string
                    format: binary
                  description: GIF to PDF input files (.gif)
                settings:
                  type: string
                  description: |
                    Dedicated GIF to PDF converter using the image-to-pdf pipeline.
                    Optional settings: pageSize (A4, Letter, A3, A5), orientation (portrait or landscape), margin, targetSize, and collate.
                clientJobId:
                  type: string
                  description: Client-provided job ID for SSE progress tracking
      responses:
        '200':
          description: Processed file
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ToolResponse'
        '400':
          description: Invalid input or settings
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Error'
        '401':
          description: Authentication required
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/UnauthorizedError'
        '422':
          description: Processing failed
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Error'
  /api/v1/tools/image/eps-to-pdf:
    post:
      operationId: epsToPdfTool
      tags:
        - Tools
      summary: EPS у PDF
      description: |
        Конвертувати EPS у PDF
        Це спеціальний ендпоінт-пресет конвертації на основі `image-to-pdf`.
      security:
        - bearerAuth: []
      requestBody:
        required: true
        content:
          multipart/form-data:
            schema:
              type: object
              required:
                - file
              properties:
                file:
                  type: array
                  items:
                    type: string
                    format: binary
                  description: EPS to PDF input files (.eps)
                settings:
                  type: string
                  description: |
                    Dedicated EPS to PDF converter using the image-to-pdf pipeline.
                    Optional settings: pageSize (A4, Letter, A3, A5), orientation (portrait or landscape), margin, targetSize, and collate.
                clientJobId:
                  type: string
                  description: Client-provided job ID for SSE progress tracking
      responses:
        '200':
          description: Processed file
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ToolResponse'
        '400':
          description: Invalid input or settings
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Error'
        '401':
          description: Authentication required
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/UnauthorizedError'
        '422':
          description: Processing failed
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Error'
  /api/v1/tools/pdf/pdf-to-jpg:
    post:
      operationId: pdfToJpgTool
      tags:
        - Tools
      summary: PDF у JPG
      description: |
        Конвертувати PDF у JPG
        Це спеціальний ендпоінт-пресет конвертації на основі `pdf-to-image`.
      security:
        - bearerAuth: []
      requestBody:
        required: true
        content:
          multipart/form-data:
            schema:
              type: object
              required:
                - file
              properties:
                file:
                  type: string
                  format: binary
                  description: PDF to JPG input file (.pdf)
                settings:
                  type: string
                  description: |
                    Dedicated PDF to JPG converter using the pdf-to-image pipeline.
                    The output format is locked by the endpoint.
                clientJobId:
                  type: string
                  description: Client-provided job ID for SSE progress tracking
      responses:
        '200':
          description: Processed file
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ToolResponse'
        '400':
          description: Invalid input or settings
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Error'
        '401':
          description: Authentication required
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/UnauthorizedError'
        '422':
          description: Processing failed
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Error'
  /api/v1/tools/pdf/pdf-to-png:
    post:
      operationId: pdfToPngTool
      tags:
        - Tools
      summary: PDF у PNG
      description: |
        Конвертувати PDF у PNG
        Це спеціальний ендпоінт-пресет конвертації на основі `pdf-to-image`.
      security:
        - bearerAuth: []
      requestBody:
        required: true
        content:
          multipart/form-data:
            schema:
              type: object
              required:
                - file
              properties:
                file:
                  type: string
                  format: binary
                  description: PDF to PNG input file (.pdf)
                settings:
                  type: string
                  description: |
                    Dedicated PDF to PNG converter using the pdf-to-image pipeline.
                    The output format is locked by the endpoint.
                clientJobId:
                  type: string
                  description: Client-provided job ID for SSE progress tracking
      responses:
        '200':
          description: Processed file
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ToolResponse'
        '400':
          description: Invalid input or settings
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Error'
        '401':
          description: Authentication required
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/UnauthorizedError'
        '422':
          description: Processing failed
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Error'
  /api/v1/tools/pdf/pdf-to-tiff:
    post:
      operationId: pdfToTiffTool
      tags:
        - Tools
      summary: PDF у TIFF
      description: |
        Конвертувати PDF у TIFF
        Це спеціальний ендпоінт-пресет конвертації на основі `pdf-to-image`.
      security:
        - bearerAuth: []
      requestBody:
        required: true
        content:
          multipart/form-data:
            schema:
              type: object
              required:
                - file
              properties:
                file:
                  type: string
                  format: binary
                  description: PDF to TIFF input file (.pdf)
                settings:
                  type: string
                  description: |
                    Dedicated PDF to TIFF converter using the pdf-to-image pipeline.
                    The output format is locked by the endpoint.
                clientJobId:
                  type: string
                  description: Client-provided job ID for SSE progress tracking
      responses:
        '200':
          description: Processed file
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ToolResponse'
        '400':
          description: Invalid input or settings
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Error'
        '401':
          description: Authentication required
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/UnauthorizedError'
        '422':
          description: Processing failed
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Error'
  /api/v1/tools/video/mov-to-mp4:
    post:
      operationId: movToMp4Tool
      tags:
        - Tools
      summary: MOV у MP4
      description: |
        Конвертувати MOV у MP4
        Це спеціальний ендпоінт-пресет конвертації на основі `convert-video`.
      security:
        - bearerAuth: []
      requestBody:
        required: true
        content:
          multipart/form-data:
            schema:
              type: object
              required:
                - file
              properties:
                file:
                  type: string
                  format: binary
                  description: MOV to MP4 input file (.mov)
                settings:
                  type: string
                  description: |
                    Dedicated MOV to MP4 converter using the convert-video pipeline.
                    Optional settings: quality (high, balanced, small). The output format is locked by the endpoint.
                clientJobId:
                  type: string
                  description: Client-provided job ID for SSE progress tracking
      responses:
        '202':
          description: Accepted for async processing. Track progress via SSE at /api/v1/jobs/{jobId}/progress.
          content:
            application/json:
              schema:
                type: object
                properties:
                  jobId:
                    type: string
                  async:
                    type: boolean
                    example: true
        '400':
          description: Invalid input or settings
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Error'
        '401':
          description: Authentication required
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/UnauthorizedError'
        '422':
          description: Processing failed
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Error'
  /api/v1/tools/video/webm-to-mp4:
    post:
      operationId: webmToMp4Tool
      tags:
        - Tools
      summary: WEBM у MP4
      description: |
        Конвертувати WEBM у MP4
        Це спеціальний ендпоінт-пресет конвертації на основі `convert-video`.
      security:
        - bearerAuth: []
      requestBody:
        required: true
        content:
          multipart/form-data:
            schema:
              type: object
              required:
                - file
              properties:
                file:
                  type: string
                  format: binary
                  description: WEBM to MP4 input file (.webm)
                settings:
                  type: string
                  description: |
                    Dedicated WEBM to MP4 converter using the convert-video pipeline.
                    Optional settings: quality (high, balanced, small). The output format is locked by the endpoint.
                clientJobId:
                  type: string
                  description: Client-provided job ID for SSE progress tracking
      responses:
        '202':
          description: Accepted for async processing. Track progress via SSE at /api/v1/jobs/{jobId}/progress.
          content:
            application/json:
              schema:
                type: object
                properties:
                  jobId:
                    type: string
                  async:
                    type: boolean
                    example: true
        '400':
          description: Invalid input or settings
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Error'
        '401':
          description: Authentication required
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/UnauthorizedError'
        '422':
          description: Processing failed
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Error'
  /api/v1/tools/video/mkv-to-mp4:
    post:
      operationId: mkvToMp4Tool
      tags:
        - Tools
      summary: MKV у MP4
      description: |
        Конвертувати MKV у MP4
        Це спеціальний ендпоінт-пресет конвертації на основі `convert-video`.
      security:
        - bearerAuth: []
      requestBody:
        required: true
        content:
          multipart/form-data:
            schema:
              type: object
              required:
                - file
              properties:
                file:
                  type: string
                  format: binary
                  description: MKV to MP4 input file (.mkv)
                settings:
                  type: string
                  description: |
                    Dedicated MKV to MP4 converter using the convert-video pipeline.
                    Optional settings: quality (high, balanced, small). The output format is locked by the endpoint.
                clientJobId:
                  type: string
                  description: Client-provided job ID for SSE progress tracking
      responses:
        '202':
          description: Accepted for async processing. Track progress via SSE at /api/v1/jobs/{jobId}/progress.
          content:
            application/json:
              schema:
                type: object
                properties:
                  jobId:
                    type: string
                  async:
                    type: boolean
                    example: true
        '400':
          description: Invalid input or settings
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Error'
        '401':
          description: Authentication required
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/UnauthorizedError'
        '422':
          description: Processing failed
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Error'
  /api/v1/tools/video/avi-to-mp4:
    post:
      operationId: aviToMp4Tool
      tags:
        - Tools
      summary: AVI у MP4
      description: |
        Конвертувати AVI у MP4
        Це спеціальний ендпоінт-пресет конвертації на основі `convert-video`.
      security:
        - bearerAuth: []
      requestBody:
        required: true
        content:
          multipart/form-data:
            schema:
              type: object
              required:
                - file
              properties:
                file:
                  type: string
                  format: binary
                  description: AVI to MP4 input file (.avi)
                settings:
                  type: string
                  description: |
                    Dedicated AVI to MP4 converter using the convert-video pipeline.
                    Optional settings: quality (high, balanced, small). The output format is locked by the endpoint.
                clientJobId:
                  type: string
                  description: Client-provided job ID for SSE progress tracking
      responses:
        '202':
          description: Accepted for async processing. Track progress via SSE at /api/v1/jobs/{jobId}/progress.
          content:
            application/json:
              schema:
                type: object
                properties:
                  jobId:
                    type: string
                  async:
                    type: boolean
                    example: true
        '400':
          description: Invalid input or settings
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Error'
        '401':
          description: Authentication required
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/UnauthorizedError'
        '422':
          description: Processing failed
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Error'
  /api/v1/tools/video/mp4-to-mov:
    post:
      operationId: mp4ToMovTool
      tags:
        - Tools
      summary: MP4 у MOV
      description: |
        Конвертувати MP4 у MOV
        Це спеціальний ендпоінт-пресет конвертації на основі `convert-video`.
      security:
        - bearerAuth: []
      requestBody:
        required: true
        content:
          multipart/form-data:
            schema:
              type: object
              required:
                - file
              properties:
                file:
                  type: string
                  format: binary
                  description: MP4 to MOV input file (.mp4)
                settings:
                  type: string
                  description: |
                    Dedicated MP4 to MOV converter using the convert-video pipeline.
                    Optional settings: quality (high, balanced, small). The output format is locked by the endpoint.
                clientJobId:
                  type: string
                  description: Client-provided job ID for SSE progress tracking
      responses:
        '202':
          description: Accepted for async processing. Track progress via SSE at /api/v1/jobs/{jobId}/progress.
          content:
            application/json:
              schema:
                type: object
                properties:
                  jobId:
                    type: string
                  async:
                    type: boolean
                    example: true
        '400':
          description: Invalid input or settings
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Error'
        '401':
          description: Authentication required
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/UnauthorizedError'
        '422':
          description: Processing failed
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Error'
  /api/v1/tools/video/mp4-to-webm:
    post:
      operationId: mp4ToWebmTool
      tags:
        - Tools
      summary: MP4 у WEBM
      description: |
        Конвертувати MP4 у WEBM
        Це спеціальний ендпоінт-пресет конвертації на основі `convert-video`.
      security:
        - bearerAuth: []
      requestBody:
        required: true
        content:
          multipart/form-data:
            schema:
              type: object
              required:
                - file
              properties:
                file:
                  type: string
                  format: binary
                  description: MP4 to WEBM input file (.mp4)
                settings:
                  type: string
                  description: |
                    Dedicated MP4 to WEBM converter using the convert-video pipeline.
                    Optional settings: quality (high, balanced, small). The output format is locked by the endpoint.
                clientJobId:
                  type: string
                  description: Client-provided job ID for SSE progress tracking
      responses:
        '202':
          description: Accepted for async processing. Track progress via SSE at /api/v1/jobs/{jobId}/progress.
          content:
            application/json:
              schema:
                type: object
                properties:
                  jobId:
                    type: string
                  async:
                    type: boolean
                    example: true
        '400':
          description: Invalid input or settings
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Error'
        '401':
          description: Authentication required
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/UnauthorizedError'
        '422':
          description: Processing failed
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Error'
  /api/v1/tools/video/webm-to-mov:
    post:
      operationId: webmToMovTool
      tags:
        - Tools
      summary: WEBM у MOV
      description: |
        Конвертувати WEBM у MOV
        Це спеціальний ендпоінт-пресет конвертації на основі `convert-video`.
      security:
        - bearerAuth: []
      requestBody:
        required: true
        content:
          multipart/form-data:
            schema:
              type: object
              required:
                - file
              properties:
                file:
                  type: string
                  format: binary
                  description: WEBM to MOV input file (.webm)
                settings:
                  type: string
                  description: |
                    Dedicated WEBM to MOV converter using the convert-video pipeline.
                    Optional settings: quality (high, balanced, small). The output format is locked by the endpoint.
                clientJobId:
                  type: string
                  description: Client-provided job ID for SSE progress tracking
      responses:
        '202':
          description: Accepted for async processing. Track progress via SSE at /api/v1/jobs/{jobId}/progress.
          content:
            application/json:
              schema:
                type: object
                properties:
                  jobId:
                    type: string
                  async:
                    type: boolean
                    example: true
        '400':
          description: Invalid input or settings
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Error'
        '401':
          description: Authentication required
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/UnauthorizedError'
        '422':
          description: Processing failed
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Error'
  /api/v1/tools/video/mkv-to-mov:
    post:
      operationId: mkvToMovTool
      tags:
        - Tools
      summary: MKV у MOV
      description: |
        Конвертувати MKV у MOV
        Це спеціальний ендпоінт-пресет конвертації на основі `convert-video`.
      security:
        - bearerAuth: []
      requestBody:
        required: true
        content:
          multipart/form-data:
            schema:
              type: object
              required:
                - file
              properties:
                file:
                  type: string
                  format: binary
                  description: MKV to MOV input file (.mkv)
                settings:
                  type: string
                  description: |
                    Dedicated MKV to MOV converter using the convert-video pipeline.
                    Optional settings: quality (high, balanced, small). The output format is locked by the endpoint.
                clientJobId:
                  type: string
                  description: Client-provided job ID for SSE progress tracking
      responses:
        '202':
          description: Accepted for async processing. Track progress via SSE at /api/v1/jobs/{jobId}/progress.
          content:
            application/json:
              schema:
                type: object
                properties:
                  jobId:
                    type: string
                  async:
                    type: boolean
                    example: true
        '400':
          description: Invalid input or settings
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Error'
        '401':
          description: Authentication required
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/UnauthorizedError'
        '422':
          description: Processing failed
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Error'
  /api/v1/tools/video/avi-to-mov:
    post:
      operationId: aviToMovTool
      tags:
        - Tools
      summary: AVI у MOV
      description: |
        Конвертувати AVI у MOV
        Це спеціальний ендпоінт-пресет конвертації на основі `convert-video`.
      security:
        - bearerAuth: []
      requestBody:
        required: true
        content:
          multipart/form-data:
            schema:
              type: object
              required:
                - file
              properties:
                file:
                  type: string
                  format: binary
                  description: AVI to MOV input file (.avi)
                settings:
                  type: string
                  description: |
                    Dedicated AVI to MOV converter using the convert-video pipeline.
                    Optional settings: quality (high, balanced, small). The output format is locked by the endpoint.
                clientJobId:
                  type: string
                  description: Client-provided job ID for SSE progress tracking
      responses:
        '202':
          description: Accepted for async processing. Track progress via SSE at /api/v1/jobs/{jobId}/progress.
          content:
            application/json:
              schema:
                type: object
                properties:
                  jobId:
                    type: string
                  async:
                    type: boolean
                    example: true
        '400':
          description: Invalid input or settings
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Error'
        '401':
          description: Authentication required
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/UnauthorizedError'
        '422':
          description: Processing failed
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Error'
  /api/v1/tools/video/mp4-to-avi:
    post:
      operationId: mp4ToAviTool
      tags:
        - Tools
      summary: MP4 у AVI
      description: |
        Конвертувати MP4 у AVI
        Це спеціальний ендпоінт-пресет конвертації на основі `convert-video`.
      security:
        - bearerAuth: []
      requestBody:
        required: true
        content:
          multipart/form-data:
            schema:
              type: object
              required:
                - file
              properties:
                file:
                  type: string
                  format: binary
                  description: MP4 to AVI input file (.mp4)
                settings:
                  type: string
                  description: |
                    Dedicated MP4 to AVI converter using the convert-video pipeline.
                    Optional settings: quality (high, balanced, small). The output format is locked by the endpoint.
                clientJobId:
                  type: string
                  description: Client-provided job ID for SSE progress tracking
      responses:
        '202':
          description: Accepted for async processing. Track progress via SSE at /api/v1/jobs/{jobId}/progress.
          content:
            application/json:
              schema:
                type: object
                properties:
                  jobId:
                    type: string
                  async:
                    type: boolean
                    example: true
        '400':
          description: Invalid input or settings
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Error'
        '401':
          description: Authentication required
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/UnauthorizedError'
        '422':
          description: Processing failed
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Error'
  /api/v1/tools/video/mov-to-avi:
    post:
      operationId: movToAviTool
      tags:
        - Tools
      summary: MOV у AVI
      description: |
        Конвертувати MOV у AVI
        Це спеціальний ендпоінт-пресет конвертації на основі `convert-video`.
      security:
        - bearerAuth: []
      requestBody:
        required: true
        content:
          multipart/form-data:
            schema:
              type: object
              required:
                - file
              properties:
                file:
                  type: string
                  format: binary
                  description: MOV to AVI input file (.mov)
                settings:
                  type: string
                  description: |
                    Dedicated MOV to AVI converter using the convert-video pipeline.
                    Optional settings: quality (high, balanced, small). The output format is locked by the endpoint.
                clientJobId:
                  type: string
                  description: Client-provided job ID for SSE progress tracking
      responses:
        '202':
          description: Accepted for async processing. Track progress via SSE at /api/v1/jobs/{jobId}/progress.
          content:
            application/json:
              schema:
                type: object
                properties:
                  jobId:
                    type: string
                  async:
                    type: boolean
                    example: true
        '400':
          description: Invalid input or settings
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Error'
        '401':
          description: Authentication required
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/UnauthorizedError'
        '422':
          description: Processing failed
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Error'
  /api/v1/tools/video/mkv-to-avi:
    post:
      operationId: mkvToAviTool
      tags:
        - Tools
      summary: MKV у AVI
      description: |
        Конвертувати MKV у AVI
        Це спеціальний ендпоінт-пресет конвертації на основі `convert-video`.
      security:
        - bearerAuth: []
      requestBody:
        required: true
        content:
          multipart/form-data:
            schema:
              type: object
              required:
                - file
              properties:
                file:
                  type: string
                  format: binary
                  description: MKV to AVI input file (.mkv)
                settings:
                  type: string
                  description: |
                    Dedicated MKV to AVI converter using the convert-video pipeline.
                    Optional settings: quality (high, balanced, small). The output format is locked by the endpoint.
                clientJobId:
                  type: string
                  description: Client-provided job ID for SSE progress tracking
      responses:
        '202':
          description: Accepted for async processing. Track progress via SSE at /api/v1/jobs/{jobId}/progress.
          content:
            application/json:
              schema:
                type: object
                properties:
                  jobId:
                    type: string
                  async:
                    type: boolean
                    example: true
        '400':
          description: Invalid input or settings
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Error'
        '401':
          description: Authentication required
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/UnauthorizedError'
        '422':
          description: Processing failed
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Error'
  /api/v1/tools/video/avi-to-mkv:
    post:
      operationId: aviToMkvTool
      tags:
        - Tools
      summary: AVI у MKV
      description: |
        Конвертувати AVI у MKV
        Це спеціальний ендпоінт-пресет конвертації на основі `convert-video`.
      security:
        - bearerAuth: []
      requestBody:
        required: true
        content:
          multipart/form-data:
            schema:
              type: object
              required:
                - file
              properties:
                file:
                  type: string
                  format: binary
                  description: AVI to MKV input file (.avi)
                settings:
                  type: string
                  description: |
                    Dedicated AVI to MKV converter using the convert-video pipeline.
                    Optional settings: quality (high, balanced, small). The output format is locked by the endpoint.
                clientJobId:
                  type: string
                  description: Client-provided job ID for SSE progress tracking
      responses:
        '202':
          description: Accepted for async processing. Track progress via SSE at /api/v1/jobs/{jobId}/progress.
          content:
            application/json:
              schema:
                type: object
                properties:
                  jobId:
                    type: string
                  async:
                    type: boolean
                    example: true
        '400':
          description: Invalid input or settings
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Error'
        '401':
          description: Authentication required
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/UnauthorizedError'
        '422':
          description: Processing failed
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Error'
  /api/v1/tools/video/mp4-to-gif:
    post:
      operationId: mp4ToGifTool
      tags:
        - Tools
      summary: MP4 у GIF
      description: |
        Конвертувати MP4 у GIF
        Це спеціальний ендпоінт-пресет конвертації на основі `video-to-gif`.
      security:
        - bearerAuth: []
      requestBody:
        required: true
        content:
          multipart/form-data:
            schema:
              type: object
              required:
                - file
              properties:
                file:
                  type: string
                  format: binary
                  description: MP4 to GIF input file (.mp4)
                settings:
                  type: string
                  description: |
                    Dedicated MP4 to GIF converter using the video-to-gif pipeline.
                    The output format is locked by the endpoint.
                clientJobId:
                  type: string
                  description: Client-provided job ID for SSE progress tracking
      responses:
        '202':
          description: Accepted for async processing. Track progress via SSE at /api/v1/jobs/{jobId}/progress.
          content:
            application/json:
              schema:
                type: object
                properties:
                  jobId:
                    type: string
                  async:
                    type: boolean
                    example: true
        '400':
          description: Invalid input or settings
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Error'
        '401':
          description: Authentication required
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/UnauthorizedError'
        '422':
          description: Processing failed
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Error'
  /api/v1/tools/video/mov-to-gif:
    post:
      operationId: movToGifTool
      tags:
        - Tools
      summary: MOV у GIF
      description: |
        Конвертувати MOV у GIF
        Це спеціальний ендпоінт-пресет конвертації на основі `video-to-gif`.
      security:
        - bearerAuth: []
      requestBody:
        required: true
        content:
          multipart/form-data:
            schema:
              type: object
              required:
                - file
              properties:
                file:
                  type: string
                  format: binary
                  description: MOV to GIF input file (.mov)
                settings:
                  type: string
                  description: |
                    Dedicated MOV to GIF converter using the video-to-gif pipeline.
                    The output format is locked by the endpoint.
                clientJobId:
                  type: string
                  description: Client-provided job ID for SSE progress tracking
      responses:
        '202':
          description: Accepted for async processing. Track progress via SSE at /api/v1/jobs/{jobId}/progress.
          content:
            application/json:
              schema:
                type: object
                properties:
                  jobId:
                    type: string
                  async:
                    type: boolean
                    example: true
        '400':
          description: Invalid input or settings
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Error'
        '401':
          description: Authentication required
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/UnauthorizedError'
        '422':
          description: Processing failed
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Error'
  /api/v1/tools/video/mkv-to-gif:
    post:
      operationId: mkvToGifTool
      tags:
        - Tools
      summary: MKV у GIF
      description: |
        Конвертувати MKV у GIF
        Це спеціальний ендпоінт-пресет конвертації на основі `video-to-gif`.
      security:
        - bearerAuth: []
      requestBody:
        required: true
        content:
          multipart/form-data:
            schema:
              type: object
              required:
                - file
              properties:
                file:
                  type: string
                  format: binary
                  description: MKV to GIF input file (.mkv)
                settings:
                  type: string
                  description: |
                    Dedicated MKV to GIF converter using the video-to-gif pipeline.
                    The output format is locked by the endpoint.
                clientJobId:
                  type: string
                  description: Client-provided job ID for SSE progress tracking
      responses:
        '202':
          description: Accepted for async processing. Track progress via SSE at /api/v1/jobs/{jobId}/progress.
          content:
            application/json:
              schema:
                type: object
                properties:
                  jobId:
                    type: string
                  async:
                    type: boolean
                    example: true
        '400':
          description: Invalid input or settings
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Error'
        '401':
          description: Authentication required
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/UnauthorizedError'
        '422':
          description: Processing failed
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Error'
  /api/v1/tools/video/avi-to-gif:
    post:
      operationId: aviToGifTool
      tags:
        - Tools
      summary: AVI у GIF
      description: |
        Конвертувати AVI у GIF
        Це спеціальний ендпоінт-пресет конвертації на основі `video-to-gif`.
      security:
        - bearerAuth: []
      requestBody:
        required: true
        content:
          multipart/form-data:
            schema:
              type: object
              required:
                - file
              properties:
                file:
                  type: string
                  format: binary
                  description: AVI to GIF input file (.avi)
                settings:
                  type: string
                  description: |
                    Dedicated AVI to GIF converter using the video-to-gif pipeline.
                    The output format is locked by the endpoint.
                clientJobId:
                  type: string
                  description: Client-provided job ID for SSE progress tracking
      responses:
        '202':
          description: Accepted for async processing. Track progress via SSE at /api/v1/jobs/{jobId}/progress.
          content:
            application/json:
              schema:
                type: object
                properties:
                  jobId:
                    type: string
                  async:
                    type: boolean
                    example: true
        '400':
          description: Invalid input or settings
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Error'
        '401':
          description: Authentication required
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/UnauthorizedError'
        '422':
          description: Processing failed
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Error'
  /api/v1/tools/video/gif-to-mp4:
    post:
      operationId: gifToMp4Tool
      tags:
        - Tools
      summary: GIF у MP4
      description: |
        Конвертувати GIF у MP4
        Це спеціальний ендпоінт-пресет конвертації на основі `gif-to-video`.
      security:
        - bearerAuth: []
      requestBody:
        required: true
        content:
          multipart/form-data:
            schema:
              type: object
              required:
                - file
              properties:
                file:
                  type: string
                  format: binary
                  description: GIF to MP4 input file (.gif)
                settings:
                  type: string
                  description: |
                    Dedicated GIF to MP4 converter using the gif-to-video pipeline.
                    The output format is locked by the endpoint.
                clientJobId:
                  type: string
                  description: Client-provided job ID for SSE progress tracking
      responses:
        '200':
          description: Processed file
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ToolResponse'
        '400':
          description: Invalid input or settings
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Error'
        '401':
          description: Authentication required
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/UnauthorizedError'
        '422':
          description: Processing failed
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Error'
  /api/v1/tools/video/gif-to-webm:
    post:
      operationId: gifToWebmTool
      tags:
        - Tools
      summary: GIF у WEBM
      description: |
        Конвертувати GIF у WEBM
        Це спеціальний ендпоінт-пресет конвертації на основі `gif-to-video`.
      security:
        - bearerAuth: []
      requestBody:
        required: true
        content:
          multipart/form-data:
            schema:
              type: object
              required:
                - file
              properties:
                file:
                  type: string
                  format: binary
                  description: GIF to WEBM input file (.gif)
                settings:
                  type: string
                  description: |
                    Dedicated GIF to WEBM converter using the gif-to-video pipeline.
                    The output format is locked by the endpoint.
                clientJobId:
                  type: string
                  description: Client-provided job ID for SSE progress tracking
      responses:
        '200':
          description: Processed file
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ToolResponse'
        '400':
          description: Invalid input or settings
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Error'
        '401':
          description: Authentication required
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/UnauthorizedError'
        '422':
          description: Processing failed
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Error'
  /api/v1/tools/video/gif-to-mov:
    post:
      operationId: gifToMovTool
      tags:
        - Tools
      summary: GIF у MOV
      description: |
        Конвертувати GIF у MOV
        Це спеціальний ендпоінт-пресет конвертації на основі `gif-to-video`.
      security:
        - bearerAuth: []
      requestBody:
        required: true
        content:
          multipart/form-data:
            schema:
              type: object
              required:
                - file
              properties:
                file:
                  type: string
                  format: binary
                  description: GIF to MOV input file (.gif)
                settings:
                  type: string
                  description: |
                    Dedicated GIF to MOV converter using the gif-to-video pipeline.
                    The output format is locked by the endpoint.
                clientJobId:
                  type: string
                  description: Client-provided job ID for SSE progress tracking
      responses:
        '200':
          description: Processed file
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ToolResponse'
        '400':
          description: Invalid input or settings
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Error'
        '401':
          description: Authentication required
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/UnauthorizedError'
        '422':
          description: Processing failed
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Error'
  /api/v1/tools/video/mp4-to-mp3:
    post:
      operationId: mp4ToMp3Tool
      tags:
        - Tools
      summary: MP4 у MP3
      description: |
        Конвертувати MP4 у MP3
        Це спеціальний ендпоінт-пресет конвертації на основі `extract-audio`.
      security:
        - bearerAuth: []
      requestBody:
        required: true
        content:
          multipart/form-data:
            schema:
              type: object
              required:
                - file
              properties:
                file:
                  type: string
                  format: binary
                  description: MP4 to MP3 input file (.mp4)
                settings:
                  type: string
                  description: |
                    Dedicated MP4 to MP3 converter using the extract-audio pipeline.
                    The output format is locked by the endpoint.
                clientJobId:
                  type: string
                  description: Client-provided job ID for SSE progress tracking
      responses:
        '200':
          description: Processed file
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ToolResponse'
        '400':
          description: Invalid input or settings
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Error'
        '401':
          description: Authentication required
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/UnauthorizedError'
        '422':
          description: Processing failed
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Error'
  /api/v1/tools/video/mov-to-mp3:
    post:
      operationId: movToMp3Tool
      tags:
        - Tools
      summary: MOV у MP3
      description: |
        Конвертувати MOV у MP3
        Це спеціальний ендпоінт-пресет конвертації на основі `extract-audio`.
      security:
        - bearerAuth: []
      requestBody:
        required: true
        content:
          multipart/form-data:
            schema:
              type: object
              required:
                - file
              properties:
                file:
                  type: string
                  format: binary
                  description: MOV to MP3 input file (.mov)
                settings:
                  type: string
                  description: |
                    Dedicated MOV to MP3 converter using the extract-audio pipeline.
                    The output format is locked by the endpoint.
                clientJobId:
                  type: string
                  description: Client-provided job ID for SSE progress tracking
      responses:
        '200':
          description: Processed file
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ToolResponse'
        '400':
          description: Invalid input or settings
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Error'
        '401':
          description: Authentication required
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/UnauthorizedError'
        '422':
          description: Processing failed
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Error'
  /api/v1/tools/video/mkv-to-mp3:
    post:
      operationId: mkvToMp3Tool
      tags:
        - Tools
      summary: MKV у MP3
      description: |
        Конвертувати MKV у MP3
        Це спеціальний ендпоінт-пресет конвертації на основі `extract-audio`.
      security:
        - bearerAuth: []
      requestBody:
        required: true
        content:
          multipart/form-data:
            schema:
              type: object
              required:
                - file
              properties:
                file:
                  type: string
                  format: binary
                  description: MKV to MP3 input file (.mkv)
                settings:
                  type: string
                  description: |
                    Dedicated MKV to MP3 converter using the extract-audio pipeline.
                    The output format is locked by the endpoint.
                clientJobId:
                  type: string
                  description: Client-provided job ID for SSE progress tracking
      responses:
        '200':
          description: Processed file
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ToolResponse'
        '400':
          description: Invalid input or settings
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Error'
        '401':
          description: Authentication required
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/UnauthorizedError'
        '422':
          description: Processing failed
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Error'
  /api/v1/tools/video/webm-to-mp3:
    post:
      operationId: webmToMp3Tool
      tags:
        - Tools
      summary: WEBM у MP3
      description: |
        Конвертувати WEBM у MP3
        Це спеціальний ендпоінт-пресет конвертації на основі `extract-audio`.
      security:
        - bearerAuth: []
      requestBody:
        required: true
        content:
          multipart/form-data:
            schema:
              type: object
              required:
                - file
              properties:
                file:
                  type: string
                  format: binary
                  description: WEBM to MP3 input file (.webm)
                settings:
                  type: string
                  description: |
                    Dedicated WEBM to MP3 converter using the extract-audio pipeline.
                    The output format is locked by the endpoint.
                clientJobId:
                  type: string
                  description: Client-provided job ID for SSE progress tracking
      responses:
        '200':
          description: Processed file
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ToolResponse'
        '400':
          description: Invalid input or settings
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Error'
        '401':
          description: Authentication required
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/UnauthorizedError'
        '422':
          description: Processing failed
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Error'
  /api/v1/tools/video/avi-to-mp3:
    post:
      operationId: aviToMp3Tool
      tags:
        - Tools
      summary: AVI у MP3
      description: |
        Конвертувати AVI у MP3
        Це спеціальний ендпоінт-пресет конвертації на основі `extract-audio`.
      security:
        - bearerAuth: []
      requestBody:
        required: true
        content:
          multipart/form-data:
            schema:
              type: object
              required:
                - file
              properties:
                file:
                  type: string
                  format: binary
                  description: AVI to MP3 input file (.avi)
                settings:
                  type: string
                  description: |
                    Dedicated AVI to MP3 converter using the extract-audio pipeline.
                    The output format is locked by the endpoint.
                clientJobId:
                  type: string
                  description: Client-provided job ID for SSE progress tracking
      responses:
        '200':
          description: Processed file
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ToolResponse'
        '400':
          description: Invalid input or settings
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Error'
        '401':
          description: Authentication required
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/UnauthorizedError'
        '422':
          description: Processing failed
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Error'
  /api/v1/tools/video/mp4-to-wav:
    post:
      operationId: mp4ToWavTool
      tags:
        - Tools
      summary: MP4 у WAV
      description: |
        Конвертувати MP4 у WAV
        Це спеціальний ендпоінт-пресет конвертації на основі `extract-audio`.
      security:
        - bearerAuth: []
      requestBody:
        required: true
        content:
          multipart/form-data:
            schema:
              type: object
              required:
                - file
              properties:
                file:
                  type: string
                  format: binary
                  description: MP4 to WAV input file (.mp4)
                settings:
                  type: string
                  description: |
                    Dedicated MP4 to WAV converter using the extract-audio pipeline.
                    The output format is locked by the endpoint.
                clientJobId:
                  type: string
                  description: Client-provided job ID for SSE progress tracking
      responses:
        '200':
          description: Processed file
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ToolResponse'
        '400':
          description: Invalid input or settings
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Error'
        '401':
          description: Authentication required
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/UnauthorizedError'
        '422':
          description: Processing failed
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Error'
  /api/v1/tools/video/mov-to-wav:
    post:
      operationId: movToWavTool
      tags:
        - Tools
      summary: MOV у WAV
      description: |
        Конвертувати MOV у WAV
        Це спеціальний ендпоінт-пресет конвертації на основі `extract-audio`.
      security:
        - bearerAuth: []
      requestBody:
        required: true
        content:
          multipart/form-data:
            schema:
              type: object
              required:
                - file
              properties:
                file:
                  type: string
                  format: binary
                  description: MOV to WAV input file (.mov)
                settings:
                  type: string
                  description: |
                    Dedicated MOV to WAV converter using the extract-audio pipeline.
                    The output format is locked by the endpoint.
                clientJobId:
                  type: string
                  description: Client-provided job ID for SSE progress tracking
      responses:
        '200':
          description: Processed file
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ToolResponse'
        '400':
          description: Invalid input or settings
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Error'
        '401':
          description: Authentication required
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/UnauthorizedError'
        '422':
          description: Processing failed
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Error'
  /api/v1/tools/video/mp4-to-ogg:
    post:
      operationId: mp4ToOggTool
      tags:
        - Tools
      summary: MP4 у OGG
      description: |
        Конвертувати MP4 у OGG
        Це спеціальний ендпоінт-пресет конвертації на основі `extract-audio`.
      security:
        - bearerAuth: []
      requestBody:
        required: true
        content:
          multipart/form-data:
            schema:
              type: object
              required:
                - file
              properties:
                file:
                  type: string
                  format: binary
                  description: MP4 to OGG input file (.mp4)
                settings:
                  type: string
                  description: |
                    Dedicated MP4 to OGG converter using the extract-audio pipeline.
                    The output format is locked by the endpoint.
                clientJobId:
                  type: string
                  description: Client-provided job ID for SSE progress tracking
      responses:
        '200':
          description: Processed file
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ToolResponse'
        '400':
          description: Invalid input or settings
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Error'
        '401':
          description: Authentication required
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/UnauthorizedError'
        '422':
          description: Processing failed
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Error'
  /api/v1/tools/audio/m4a-to-mp3:
    post:
      operationId: m4aToMp3Tool
      tags:
        - Tools
      summary: M4A у MP3
      description: |
        Конвертувати M4A у MP3
        Це спеціальний ендпоінт-пресет конвертації на основі `convert-audio`.
      security:
        - bearerAuth: []
      requestBody:
        required: true
        content:
          multipart/form-data:
            schema:
              type: object
              required:
                - file
              properties:
                file:
                  type: string
                  format: binary
                  description: M4A to MP3 input file (.m4a)
                settings:
                  type: string
                  description: |
                    Dedicated M4A to MP3 converter using the convert-audio pipeline.
                    The output format is locked by the endpoint.
                clientJobId:
                  type: string
                  description: Client-provided job ID for SSE progress tracking
      responses:
        '200':
          description: Processed file
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ToolResponse'
        '400':
          description: Invalid input or settings
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Error'
        '401':
          description: Authentication required
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/UnauthorizedError'
        '422':
          description: Processing failed
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Error'
  /api/v1/tools/audio/m4a-to-wav:
    post:
      operationId: m4aToWavTool
      tags:
        - Tools
      summary: M4A у WAV
      description: |
        Конвертувати M4A у WAV
        Це спеціальний ендпоінт-пресет конвертації на основі `convert-audio`.
      security:
        - bearerAuth: []
      requestBody:
        required: true
        content:
          multipart/form-data:
            schema:
              type: object
              required:
                - file
              properties:
                file:
                  type: string
                  format: binary
                  description: M4A to WAV input file (.m4a)
                settings:
                  type: string
                  description: |
                    Dedicated M4A to WAV converter using the convert-audio pipeline.
                    The output format is locked by the endpoint.
                clientJobId:
                  type: string
                  description: Client-provided job ID for SSE progress tracking
      responses:
        '200':
          description: Processed file
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ToolResponse'
        '400':
          description: Invalid input or settings
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Error'
        '401':
          description: Authentication required
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/UnauthorizedError'
        '422':
          description: Processing failed
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Error'
  /api/v1/tools/audio/aac-to-mp3:
    post:
      operationId: aacToMp3Tool
      tags:
        - Tools
      summary: AAC у MP3
      description: |
        Конвертувати AAC у MP3
        Це спеціальний ендпоінт-пресет конвертації на основі `convert-audio`.
      security:
        - bearerAuth: []
      requestBody:
        required: true
        content:
          multipart/form-data:
            schema:
              type: object
              required:
                - file
              properties:
                file:
                  type: string
                  format: binary
                  description: AAC to MP3 input file (.aac)
                settings:
                  type: string
                  description: |
                    Dedicated AAC to MP3 converter using the convert-audio pipeline.
                    The output format is locked by the endpoint.
                clientJobId:
                  type: string
                  description: Client-provided job ID for SSE progress tracking
      responses:
        '200':
          description: Processed file
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ToolResponse'
        '400':
          description: Invalid input or settings
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Error'
        '401':
          description: Authentication required
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/UnauthorizedError'
        '422':
          description: Processing failed
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Error'
  /api/v1/tools/audio/aac-to-wav:
    post:
      operationId: aacToWavTool
      tags:
        - Tools
      summary: AAC у WAV
      description: |
        Конвертувати AAC у WAV
        Це спеціальний ендпоінт-пресет конвертації на основі `convert-audio`.
      security:
        - bearerAuth: []
      requestBody:
        required: true
        content:
          multipart/form-data:
            schema:
              type: object
              required:
                - file
              properties:
                file:
                  type: string
                  format: binary
                  description: AAC to WAV input file (.aac)
                settings:
                  type: string
                  description: |
                    Dedicated AAC to WAV converter using the convert-audio pipeline.
                    The output format is locked by the endpoint.
                clientJobId:
                  type: string
                  description: Client-provided job ID for SSE progress tracking
      responses:
        '200':
          description: Processed file
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ToolResponse'
        '400':
          description: Invalid input or settings
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Error'
        '401':
          description: Authentication required
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/UnauthorizedError'
        '422':
          description: Processing failed
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Error'
  /api/v1/tools/audio/aac-to-flac:
    post:
      operationId: aacToFlacTool
      tags:
        - Tools
      summary: AAC у FLAC
      description: |
        Конвертувати AAC у FLAC
        Це спеціальний ендпоінт-пресет конвертації на основі `convert-audio`.
      security:
        - bearerAuth: []
      requestBody:
        required: true
        content:
          multipart/form-data:
            schema:
              type: object
              required:
                - file
              properties:
                file:
                  type: string
                  format: binary
                  description: AAC to FLAC input file (.aac)
                settings:
                  type: string
                  description: |
                    Dedicated AAC to FLAC converter using the convert-audio pipeline.
                    The output format is locked by the endpoint.
                clientJobId:
                  type: string
                  description: Client-provided job ID for SSE progress tracking
      responses:
        '200':
          description: Processed file
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ToolResponse'
        '400':
          description: Invalid input or settings
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Error'
        '401':
          description: Authentication required
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/UnauthorizedError'
        '422':
          description: Processing failed
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Error'
  /api/v1/tools/audio/ogg-to-mp3:
    post:
      operationId: oggToMp3Tool
      tags:
        - Tools
      summary: OGG у MP3
      description: |
        Конвертувати OGG у MP3
        Це спеціальний ендпоінт-пресет конвертації на основі `convert-audio`.
      security:
        - bearerAuth: []
      requestBody:
        required: true
        content:
          multipart/form-data:
            schema:
              type: object
              required:
                - file
              properties:
                file:
                  type: string
                  format: binary
                  description: OGG to MP3 input file (.ogg)
                settings:
                  type: string
                  description: |
                    Dedicated OGG to MP3 converter using the convert-audio pipeline.
                    The output format is locked by the endpoint.
                clientJobId:
                  type: string
                  description: Client-provided job ID for SSE progress tracking
      responses:
        '200':
          description: Processed file
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ToolResponse'
        '400':
          description: Invalid input or settings
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Error'
        '401':
          description: Authentication required
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/UnauthorizedError'
        '422':
          description: Processing failed
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Error'
  /api/v1/tools/audio/ogg-to-wav:
    post:
      operationId: oggToWavTool
      tags:
        - Tools
      summary: OGG у WAV
      description: |
        Конвертувати OGG у WAV
        Це спеціальний ендпоінт-пресет конвертації на основі `convert-audio`.
      security:
        - bearerAuth: []
      requestBody:
        required: true
        content:
          multipart/form-data:
            schema:
              type: object
              required:
                - file
              properties:
                file:
                  type: string
                  format: binary
                  description: OGG to WAV input file (.ogg)
                settings:
                  type: string
                  description: |
                    Dedicated OGG to WAV converter using the convert-audio pipeline.
                    The output format is locked by the endpoint.
                clientJobId:
                  type: string
                  description: Client-provided job ID for SSE progress tracking
      responses:
        '200':
          description: Processed file
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ToolResponse'
        '400':
          description: Invalid input or settings
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Error'
        '401':
          description: Authentication required
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/UnauthorizedError'
        '422':
          description: Processing failed
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Error'
  /api/v1/tools/audio/wav-to-mp3:
    post:
      operationId: wavToMp3Tool
      tags:
        - Tools
      summary: WAV у MP3
      description: |
        Конвертувати WAV у MP3
        Це спеціальний ендпоінт-пресет конвертації на основі `convert-audio`.
      security:
        - bearerAuth: []
      requestBody:
        required: true
        content:
          multipart/form-data:
            schema:
              type: object
              required:
                - file
              properties:
                file:
                  type: string
                  format: binary
                  description: WAV to MP3 input file (.wav)
                settings:
                  type: string
                  description: |
                    Dedicated WAV to MP3 converter using the convert-audio pipeline.
                    The output format is locked by the endpoint.
                clientJobId:
                  type: string
                  description: Client-provided job ID for SSE progress tracking
      responses:
        '200':
          description: Processed file
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ToolResponse'
        '400':
          description: Invalid input or settings
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Error'
        '401':
          description: Authentication required
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/UnauthorizedError'
        '422':
          description: Processing failed
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Error'
  /api/v1/tools/audio/mp3-to-wav:
    post:
      operationId: mp3ToWavTool
      tags:
        - Tools
      summary: MP3 у WAV
      description: |
        Конвертувати MP3 у WAV
        Це спеціальний ендпоінт-пресет конвертації на основі `convert-audio`.
      security:
        - bearerAuth: []
      requestBody:
        required: true
        content:
          multipart/form-data:
            schema:
              type: object
              required:
                - file
              properties:
                file:
                  type: string
                  format: binary
                  description: MP3 to WAV input file (.mp3)
                settings:
                  type: string
                  description: |
                    Dedicated MP3 to WAV converter using the convert-audio pipeline.
                    The output format is locked by the endpoint.
                clientJobId:
                  type: string
                  description: Client-provided job ID for SSE progress tracking
      responses:
        '200':
          description: Processed file
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ToolResponse'
        '400':
          description: Invalid input or settings
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Error'
        '401':
          description: Authentication required
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/UnauthorizedError'
        '422':
          description: Processing failed
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Error'
  /api/v1/tools/audio/flac-to-mp3:
    post:
      operationId: flacToMp3Tool
      tags:
        - Tools
      summary: FLAC у MP3
      description: |
        Конвертувати FLAC у MP3
        Це спеціальний ендпоінт-пресет конвертації на основі `convert-audio`.
      security:
        - bearerAuth: []
      requestBody:
        required: true
        content:
          multipart/form-data:
            schema:
              type: object
              required:
                - file
              properties:
                file:
                  type: string
                  format: binary
                  description: FLAC to MP3 input file (.flac)
                settings:
                  type: string
                  description: |
                    Dedicated FLAC to MP3 converter using the convert-audio pipeline.
                    The output format is locked by the endpoint.
                clientJobId:
                  type: string
                  description: Client-provided job ID for SSE progress tracking
      responses:
        '200':
          description: Processed file
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ToolResponse'
        '400':
          description: Invalid input or settings
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Error'
        '401':
          description: Authentication required
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/UnauthorizedError'
        '422':
          description: Processing failed
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Error'
  /api/v1/tools/files/excel-to-csv:
    post:
      operationId: excelToCsvTool
      tags:
        - Tools
      summary: Excel у CSV
      description: |
        Конвертувати Excel у CSV
        Це спеціальний ендпоінт-пресет конвертації на основі `convert-spreadsheet`.
      security:
        - bearerAuth: []
      requestBody:
        required: true
        content:
          multipart/form-data:
            schema:
              type: object
              required:
                - file
              properties:
                file:
                  type: string
                  format: binary
                  description: Excel to CSV input file (.xlsx, .xls)
                settings:
                  type: string
                  description: |
                    Dedicated Excel to CSV converter using the convert-spreadsheet pipeline.
                    The output format is locked by the endpoint.
                clientJobId:
                  type: string
                  description: Client-provided job ID for SSE progress tracking
      responses:
        '202':
          description: Accepted for async processing. Track progress via SSE at /api/v1/jobs/{jobId}/progress.
          content:
            application/json:
              schema:
                type: object
                properties:
                  jobId:
                    type: string
                  async:
                    type: boolean
                    example: true
        '400':
          description: Invalid input or settings
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Error'
        '401':
          description: Authentication required
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/UnauthorizedError'
        '422':
          description: Processing failed
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Error'
  /api/v1/readyz:
    get:
      operationId: readinessCheck
      tags:
        - System
      summary: Перевірка готовності
      description: Перевіряє PostgreSQL, Redis, локальний дисковий простір і доступність S3, коли налаштовано. Публічний ендпоінт для балансувальників навантаження та оркестраторів.
      security: []
      responses:
        '200':
          description: Instance is ready to receive traffic
        '503':
          description: Instance is not ready to receive traffic
  /api/v1/jobs/{jobId}/cancel:
    post:
      operationId: cancelJob
      tags:
        - System
      summary: Скасувати завдання
      description: Запросити скасування завдання в черзі або того, що виконується.
      security:
        - bearerAuth: []
      parameters:
        - name: jobId
          in: path
          required: true
          schema:
            type: string
      responses:
        '200':
          description: Cancellation request result
        '401':
          description: Authentication required
  /api/v1/preferences:
    get:
      operationId: getPreferences
      tags:
        - Settings
      summary: Отримати налаштування користувача
      description: Повернути карту персональних налаштувань поточного користувача.
      security:
        - bearerAuth: []
      responses:
        '200':
          description: Preference map
        '401':
          description: Authentication required
    put:
      operationId: updatePreferences
      tags:
        - Settings
      summary: Оновити налаштування користувача
      description: Вставити або оновити одне чи кілька налаштувань для поточного користувача.
      security:
        - bearerAuth: []
      requestBody:
        required: true
        content:
          application/json:
            schema:
              type: object
      responses:
        '200':
          description: Preferences updated
        '400':
          description: Invalid preferences body
        '401':
          description: Authentication required
  /api/auth/mfa/enroll:
    post:
      operationId: enrollMfa
      tags:
        - Auth
      summary: Почати реєстрацію MFA
      description: Почати реєстрацію TOTP MFA для поточного користувача. Потребує enterprise-функції MFA.
      security:
        - bearerAuth: []
      responses:
        '200':
          description: Enrollment URI and recovery codes
        '401':
          description: Authentication required
        '403':
          description: MFA feature not licensed
        '409':
          description: MFA enrollment already enabled or pending
  /api/auth/mfa/verify:
    post:
      operationId: verifyMfaEnrollment
      tags:
        - Auth
      summary: Підтвердити реєстрацію MFA
      description: Підтвердити очікувану реєстрацію MFA кодом TOTP.
      security:
        - bearerAuth: []
      requestBody:
        required: true
        content:
          application/json:
            schema:
              type: object
              required:
                - code
              properties:
                code:
                  type: string
      responses:
        '200':
          description: MFA enabled
        '400':
          description: No pending enrollment or invalid body
        '401':
          description: Invalid TOTP code
  /api/auth/mfa/complete:
    post:
      operationId: completeMfaLogin
      tags:
        - Auth
      summary: Завершити вхід із MFA
      description: Завершити очікуваний виклик входу кодом TOTP або кодом відновлення.
      security: []
      requestBody:
        required: true
        content:
          application/json:
            schema:
              type: object
              required:
                - mfaToken
                - code
              properties:
                mfaToken:
                  type: string
                code:
                  type: string
      responses:
        '200':
          description: Session token and user profile
        '400':
          description: Invalid request body
        '401':
          description: Invalid or expired MFA challenge
  /api/auth/mfa/disable:
    post:
      operationId: disableMfa
      tags:
        - Auth
      summary: Вимкнути MFA
      description: Вимкнути MFA для поточного користувача після перевірки коду TOTP.
      security:
        - bearerAuth: []
      requestBody:
        required: true
        content:
          application/json:
            schema:
              type: object
              required:
                - code
              properties:
                code:
                  type: string
      responses:
        '200':
          description: MFA disabled
        '400':
          description: MFA is not enabled or body is invalid
        '401':
          description: Authentication required or invalid code
  /api/auth/users/{id}/mfa/reset:
    post:
      operationId: resetUserMfa
      tags:
        - Auth
      summary: Скинути MFA користувача
      description: |
        Reset a user's MFA enrollment. Requires users:manage permission and authority over the target's current role, including hierarchy, permissions, tool access, and any API-key scope.
      security:
        - bearerAuth: []
      parameters:
        - name: id
          in: path
          required: true
          schema:
            type: string
      responses:
        '200':
          description: MFA reset
        '401':
          description: Authentication required
        '403':
          description: Insufficient permissions
        '404':
          description: User not found
  /api/auth/oidc/login:
    get:
      operationId: oidcLogin
      tags:
        - Auth
      summary: Почати вхід через OIDC
      description: Перенаправити до налаштованого провайдера OpenID Connect.
      security: []
      responses:
        '302':
          description: Redirect to identity provider
        '500':
          description: OIDC provider discovery failed
  /api/auth/oidc/callback:
    get:
      operationId: oidcCallback
      tags:
        - Auth
      summary: Зворотний виклик OIDC
      description: Завершити потік коду авторизації OIDC та створити сесію.
      security: []
      responses:
        '302':
          description: Redirect to the application
  /api/auth/saml/metadata:
    get:
      operationId: samlMetadata
      tags:
        - Auth
      summary: Метадані SAML
      description: Повернути XML метаданих постачальника послуг SAML.
      security: []
      responses:
        '200':
          description: SAML metadata XML
  /api/auth/saml/login:
    get:
      operationId: samlLogin
      tags:
        - Auth
      summary: Почати вхід через SAML
      description: Перенаправити до налаштованого постачальника ідентифікації SAML.
      security: []
      responses:
        '302':
          description: Redirect to identity provider
  /api/auth/saml/callback:
    post:
      operationId: samlCallback
      tags:
        - Auth
      summary: Зворотний виклик SAML
      description: Ендпоінт служби споживача твердження для входу через SAML.
      security: []
      responses:
        '302':
          description: Redirect to the application
  /api/v1/files/{id}/preview:
    get:
      operationId: getFilePreview
      tags:
        - Files
      summary: Попередній перегляд файлу
      description: Передати потоком або згенерувати сумісний із браузером попередній перегляд для збереженого PDF, офісного документа, відео чи аудіофайлу.
      security:
        - bearerAuth: []
      parameters:
        - name: id
          in: path
          required: true
          schema:
            type: string
      responses:
        '200':
          description: Preview binary
        '400':
          description: Preview not supported
        '401':
          description: Authentication required
        '404':
          description: File not found
        '422':
          description: Could not generate preview
  /api/v1/preview/generate:
    post:
      operationId: generatePreview
      tags:
        - Files
      summary: Згенерувати попередній перегляд завантаження
      description: Згенерувати на вимогу попередній перегляд MP4 або MP3 для завантаженого медіафайлу.
      security: []
      requestBody:
        required: true
        content:
          multipart/form-data:
            schema:
              type: object
              required:
                - file
              properties:
                file:
                  type: string
                  format: binary
      responses:
        '200':
          description: Preview binary
        '400':
          description: Unsupported file type or missing file
        '422':
          description: Could not generate preview
  /api/v1/admin/log-level:
    get:
      operationId: getLogLevel
      tags:
        - Admin
      summary: Отримати рівень логування
      description: Прочитати поточний рівень логування середовища виконання. Потребує дозволу settings:write.
      security:
        - bearerAuth: []
      responses:
        '200':
          description: Current log level
        '403':
          description: Insufficient permissions
    post:
      operationId: setLogLevel
      tags:
        - Admin
      summary: Встановити рівень логування
      description: Змінити рівень логування середовища виконання. Потребує дозволу settings:write.
      security:
        - bearerAuth: []
      requestBody:
        required: true
        content:
          application/json:
            schema:
              type: object
              required:
                - level
              properties:
                level:
                  type: string
                  enum:
                    - fatal
                    - error
                    - warn
                    - info
                    - debug
                    - trace
                    - silent
      responses:
        '200':
          description: New log level
        '400':
          description: Invalid log level
        '403':
          description: Insufficient permissions
  /api/v1/metrics:
    get:
      operationId: getMetrics
      tags:
        - Admin
      summary: Метрики Prometheus
      description: Повернути метрики Prometheus. Потребує дозволу system:health.
      security:
        - bearerAuth: []
      responses:
        '200':
          description: Prometheus text format
        '403':
          description: Insufficient permissions
  /api/v1/admin/support-bundle:
    get:
      operationId: getSupportBundle
      tags:
        - Admin
      summary: Пакет підтримки
      description: Завантажити приховуваний діагностичний пакет підтримки ZIP. Потребує дозволу system:health.
      security:
        - bearerAuth: []
      responses:
        '200':
          description: Support bundle ZIP
        '403':
          description: Insufficient permissions
  /api/v1/admin/usage:
    get:
      operationId: getAdminUsage
      tags:
        - Admin
      summary: Дані панелі використання
      description: Повернути локальні дані панелі використання. Потребує дозволу audit:read.
      security:
        - bearerAuth: []
      parameters:
        - name: days
          in: query
          required: false
          schema:
            type: string
      responses:
        '200':
          description: Usage dashboard data
        '400':
          description: Invalid query parameters
        '403':
          description: Insufficient permissions
  /api/v1/admin/backup-status:
    get:
      operationId: getBackupStatus
      tags:
        - Admin
      summary: Стан резервного копіювання
      description: Повернути метадані та актуальність останнього резервного копіювання. Потребує дозволу system:health.
      security:
        - bearerAuth: []
      responses:
        '200':
          description: Backup status
        '403':
          description: Insufficient permissions
    post:
      operationId: setBackupStatus
      tags:
        - Admin
      summary: Записати стан резервного копіювання
      description: Записати завершене резервне копіювання. Потребує дозволу system:health.
      security:
        - bearerAuth: []
      requestBody:
        required: true
        content:
          application/json:
            schema:
              type: object
              required:
                - type
              properties:
                type:
                  type: string
                sizeBytes:
                  type: integer
                notes:
                  type: string
      responses:
        '200':
          description: Backup status recorded
        '400':
          description: Invalid backup status
        '403':
          description: Insufficient permissions
  /api/v1/admin/features/import:
    post:
      operationId: importFeatureBundle
      tags:
        - Features
      summary: Імпортувати пакет функцій
      description: |-
        Імпортуйте застарілий архів набору AI або імпортуйте офлайн-точний OCR
        випуск з використанням підписаного індексу та відповідного архіву платформи. OCR
        імпорт виконує той самий підпис, хеш, сумісність, вилучення,
        і smoke-test перевіряє як онлайн-інсталяцію, в тому числі точну
        мінімум 4 GiB ефективної пам’яті під час виконання. Fast Tesseract OCR не
        відповідно до цієї вимоги. Потрібен дозвіл features:manage.
      security:
        - bearerAuth: []
      requestBody:
        required: true
        content:
          multipart/form-data:
            schema:
              type: object
              oneOf:
                - required:
                    - file
                - required:
                    - index
                    - archive
              properties:
                file:
                  type: string
                  format: binary
                  description: One legacy AI bundle archive; mutually exclusive with index/archive
                index:
                  type: string
                  format: binary
                  description: Signed ocr-runtime-index.json from the SnapOtter release
                archive:
                  type: string
                  format: binary
                  description: OCR runtime archive for the current Linux amd64 or arm64 target; must follow index
      responses:
        '200':
          description: Bundle verified, imported, and activated
        '400':
          description: Invalid archive
        '403':
          description: Insufficient permissions
        '409':
          description: Import lock is held
        '507':
          description: Insufficient storage for the authenticated archive and expanded runtime
  /api/v1/enterprise/scim/token:
    post:
      operationId: createScimToken
      tags:
        - Enterprise
      summary: Створити токен SCIM
      description: |
        Generate a global SCIM bearer token. Requires the enterprise SCIM feature and full effective built-in administrator authority; custom roles and API keys missing any administrator permission do not qualify. Legacy unversioned tokens are invalid and must be reissued as v2 tokens.
      security:
        - bearerAuth: []
      responses:
        '201':
          description: SCIM token created
        '403':
          description: SCIM feature not licensed or insufficient permissions
    delete:
      operationId: revokeScimToken
      tags:
        - Enterprise
      summary: Відкликати токен SCIM
      description: |
        Revoke the global SCIM bearer token. Requires the enterprise SCIM feature and full effective built-in administrator authority; custom roles and API keys missing any administrator permission do not qualify.
      security:
        - bearerAuth: []
      responses:
        '204':
          description: SCIM token revoked
        '403':
          description: SCIM feature not licensed or insufficient permissions
  /api/v1/scim/v2/ServiceProviderConfig:
    get:
      operationId: getScimServiceProviderConfig
      tags:
        - Enterprise
      summary: Конфігурація постачальника послуг SCIM
      description: Публічний ендпоінт виявлення SCIM.
      security: []
      responses:
        '200':
          description: SCIM service provider capabilities
  /api/v1/scim/v2/Schemas:
    get:
      operationId: getScimSchemas
      tags:
        - Enterprise
      summary: Схеми SCIM
      description: Публічний ендпоінт виявлення схем SCIM.
      security: []
      responses:
        '200':
          description: SCIM schemas
  /api/v1/scim/v2/ResourceTypes:
    get:
      operationId: getScimResourceTypes
      tags:
        - Enterprise
      summary: Типи ресурсів SCIM
      description: Публічний ендпоінт виявлення типів ресурсів SCIM.
      security: []
      responses:
        '200':
          description: SCIM resource types
  /api/v1/scim/v2/Users:
    get:
      operationId: listScimUsers
      tags:
        - Enterprise
      summary: Список користувачів SCIM
      description: Список користувачів SCIM. Потребує bearer-токена SCIM.
      responses:
        '200':
          description: SCIM user list
    post:
      operationId: createScimUser
      tags:
        - Enterprise
      summary: Створити користувача SCIM
      description: Створити користувача SCIM. Потребує bearer-токена SCIM.
      responses:
        '201':
          description: SCIM user created
  /api/v1/scim/v2/Users/{id}:
    get:
      operationId: getScimUser
      tags:
        - Enterprise
      summary: Отримати користувача SCIM
      description: Отримати користувача SCIM за ID. Потребує bearer-токена SCIM.
      parameters:
        - name: id
          in: path
          required: true
          schema:
            type: string
      responses:
        '200':
          description: SCIM user
    put:
      operationId: updateScimUser
      tags:
        - Enterprise
      summary: Оновити користувача SCIM
      description: Замінити користувача SCIM за ID. Потребує bearer-токена SCIM.
      parameters:
        - name: id
          in: path
          required: true
          schema:
            type: string
      responses:
        '200':
          description: SCIM user updated
    delete:
      operationId: deleteScimUser
      tags:
        - Enterprise
      summary: Видалити користувача SCIM
      description: М'яко деактивувати користувача SCIM за ID. Потребує bearer-токена SCIM.
      parameters:
        - name: id
          in: path
          required: true
          schema:
            type: string
      responses:
        '200':
          description: SCIM user deactivated
  /api/v1/scim/v2/Groups:
    get:
      operationId: listScimGroups
      tags:
        - Enterprise
      summary: Список груп SCIM
      description: Список груп SCIM. Потребує bearer-токена SCIM.
      responses:
        '200':
          description: SCIM group list
    post:
      operationId: createScimGroup
      tags:
        - Enterprise
      summary: Створити групу SCIM
      description: Створити групу SCIM. Потребує bearer-токена SCIM.
      responses:
        '201':
          description: SCIM group created
  /api/v1/scim/v2/Groups/{id}:
    get:
      operationId: getScimGroup
      tags:
        - Enterprise
      summary: Отримати групу SCIM
      description: Отримати групу SCIM за ID. Потребує bearer-токена SCIM.
      parameters:
        - name: id
          in: path
          required: true
          schema:
            type: string
      responses:
        '200':
          description: SCIM group
    put:
      operationId: updateScimGroup
      tags:
        - Enterprise
      summary: Оновити групу SCIM
      description: Замінити групу SCIM за ID. Потребує bearer-токена SCIM.
      parameters:
        - name: id
          in: path
          required: true
          schema:
            type: string
      responses:
        '200':
          description: SCIM group updated
    delete:
      operationId: deleteScimGroup
      tags:
        - Enterprise
      summary: Видалити групу SCIM
      description: Видалити групу SCIM за ID. Потребує bearer-токена SCIM.
      parameters:
        - name: id
          in: path
          required: true
          schema:
            type: string
      responses:
        '204':
          description: SCIM group deleted
  /api/v1/enterprise/audit/export:
    get:
      operationId: exportAuditLog
      tags:
        - Enterprise
      summary: Експортувати журнал аудиту
      description: Експортувати записи аудиту як JSON або CSV. Потребує дозволу audit:read та enterprise-функції експорту аудиту.
      security:
        - bearerAuth: []
      responses:
        '200':
          description: Audit export
        '400':
          description: Invalid query parameters
        '403':
          description: Feature not licensed or insufficient permissions
  /api/v1/enterprise/config/export:
    get:
      operationId: exportEnterpriseConfig
      tags:
        - Enterprise
      summary: Експортувати конфігурацію
      description: Експортувати приховувану конфігурацію екземпляра, власні ролі та команди. Потребує дозволу system:health.
      security:
        - bearerAuth: []
      responses:
        '200':
          description: Configuration export
        '403':
          description: Feature not licensed or insufficient permissions
  /api/v1/enterprise/config/import:
    post:
      operationId: importEnterpriseConfig
      tags:
        - Enterprise
      summary: Імпортувати конфігурацію
      description: |
        Import instance configuration, custom roles, and teams. Requires full effective built-in administrator authority; custom roles and API keys missing any administrator permission do not qualify.
      security:
        - bearerAuth: []
      responses:
        '200':
          description: Import result
        '400':
          description: Invalid import payload
        '403':
          description: Feature not licensed or insufficient permissions
  /api/v1/enterprise/ip-allowlist:
    get:
      operationId: getIpAllowlist
      tags:
        - Enterprise
      summary: Отримати список дозволених IP
      description: Прочитати enterprise-список дозволених IP. Потребує дозволу security:manage.
      security:
        - bearerAuth: []
      responses:
        '200':
          description: IP allowlist
        '403':
          description: Feature not licensed or insufficient permissions
    put:
      operationId: updateIpAllowlist
      tags:
        - Enterprise
      summary: Оновити список дозволених IP
      description: Оновити записи списку дозволених CIDR із запобіганням самоблокуванню. Потребує дозволу security:manage.
      security:
        - bearerAuth: []
      responses:
        '200':
          description: IP allowlist updated
        '400':
          description: Invalid CIDR entries
        '403':
          description: Feature not licensed or insufficient permissions
  /api/v1/enterprise/legal-hold:
    get:
      operationId: listLegalHolds
      tags:
        - Enterprise
      summary: Список юридичних утримань
      description: Список користувачів і команд під юридичним утриманням. Потребує дозволу compliance:manage.
      security:
        - bearerAuth: []
      responses:
        '200':
          description: Legal holds
        '403':
          description: Feature not licensed or insufficient permissions
    put:
      operationId: updateLegalHold
      tags:
        - Enterprise
      summary: Оновити юридичне утримання
      description: Застосувати або зняти юридичне утримання для користувача чи команди. Потребує дозволу compliance:manage.
      security:
        - bearerAuth: []
      responses:
        '200':
          description: Legal hold updated
        '400':
          description: Invalid request body
        '403':
          description: Feature not licensed or insufficient permissions
  /api/v1/enterprise/siem/config:
    get:
      operationId: getSiemConfig
      tags:
        - Enterprise
      summary: Отримати конфігурацію SIEM
      description: Прочитати конфігурацію пересилання SIEM. Потребує дозволу webhooks:manage.
      security:
        - bearerAuth: []
      responses:
        '200':
          description: SIEM config
        '403':
          description: Feature not licensed or insufficient permissions
    put:
      operationId: updateSiemConfig
      tags:
        - Enterprise
      summary: Оновити конфігурацію SIEM
      description: Оновити конфігурацію пересилання SIEM. Потребує дозволу webhooks:manage.
      security:
        - bearerAuth: []
      responses:
        '200':
          description: SIEM config updated
        '400':
          description: Invalid SIEM config
        '403':
          description: Feature not licensed or insufficient permissions
  /api/v1/enterprise/webhooks:
    get:
      operationId: listWebhooks
      tags:
        - Enterprise
      summary: Список вебхуків
      description: Список призначень вебхуків. Потребує дозволу webhooks:manage.
      security:
        - bearerAuth: []
      responses:
        '200':
          description: Webhook destinations
        '403':
          description: Feature not licensed or insufficient permissions
    post:
      operationId: createWebhook
      tags:
        - Enterprise
      summary: Створити вебхук
      description: Створити призначення вебхука. Потребує дозволу webhooks:manage.
      security:
        - bearerAuth: []
      responses:
        '201':
          description: Webhook created
        '400':
          description: Invalid webhook destination
        '403':
          description: Feature not licensed or insufficient permissions
  /api/v1/enterprise/webhooks/{index}:
    put:
      operationId: updateWebhook
      tags:
        - Enterprise
      summary: Оновити вебхук
      description: Оновити призначення вебхука за індексом. Потребує дозволу webhooks:manage.
      security:
        - bearerAuth: []
      parameters:
        - name: index
          in: path
          required: true
          schema:
            type: string
      responses:
        '200':
          description: Webhook updated
        '400':
          description: Invalid index or destination
        '404':
          description: Webhook not found
    delete:
      operationId: deleteWebhook
      tags:
        - Enterprise
      summary: Видалити вебхук
      description: Видалити призначення вебхука за індексом. Потребує дозволу webhooks:manage.
      security:
        - bearerAuth: []
      parameters:
        - name: index
          in: path
          required: true
          schema:
            type: string
      responses:
        '200':
          description: Webhook deleted
        '400':
          description: Invalid index
        '404':
          description: Webhook not found
  /api/v1/enterprise/webhooks/{index}/test:
    post:
      operationId: testWebhook
      tags:
        - Enterprise
      summary: Протестувати вебхук
      description: Надіслати тестове навантаження до призначення вебхука. Потребує дозволу webhooks:manage.
      security:
        - bearerAuth: []
      parameters:
        - name: index
          in: path
          required: true
          schema:
            type: string
      responses:
        '200':
          description: Test result
        '400':
          description: Invalid index
        '404':
          description: Webhook not found
  /api/v1/enterprise/users/{id}/export:
    post:
      operationId: startGdprUserExport
      tags:
        - Enterprise
      summary: Почати експорт GDPR
      description: Почати завдання експорту даних користувача за GDPR. Потребує дозволу compliance:manage.
      security:
        - bearerAuth: []
      parameters:
        - name: id
          in: path
          required: true
          schema:
            type: string
      responses:
        '202':
          description: Export job started
        '403':
          description: Feature not licensed or insufficient permissions
        '404':
          description: User not found
  /api/v1/enterprise/users/{id}/export/{jobId}:
    get:
      operationId: getGdprUserExport
      tags:
        - Enterprise
      summary: Отримати стан експорту GDPR
      description: Прочитати стан і URL для завантаження завдання експорту користувача за GDPR. Потребує дозволу compliance:manage.
      security:
        - bearerAuth: []
      parameters:
        - name: id
          in: path
          required: true
          schema:
            type: string
        - name: jobId
          in: path
          required: true
          schema:
            type: string
      responses:
        '200':
          description: Export status
        '404':
          description: Export job not found
  /api/v1/enterprise/users/{id}/purge:
    delete:
      operationId: purgeEnterpriseUser
      tags:
        - Enterprise
      summary: Очистити дані користувача
      description: |
        Permanently purge a user's data after explicit confirmation. Requires compliance:manage permission and authority over the target's current role, including hierarchy, permissions, tool access, and any API-key scope.
      security:
        - bearerAuth: []
      parameters:
        - name: id
          in: path
          required: true
          schema:
            type: string
      responses:
        '200':
          description: User purged
        '400':
          description: Confirmation required
        '403':
          description: Feature not licensed, insufficient permission, or target role beyond actor authority
        '409':
          description: User or team is under legal hold
  /api/v1/enterprise/teams/{id}/purge:
    delete:
      operationId: purgeEnterpriseTeam
      tags:
        - Enterprise
      summary: Очистити дані команди
      description: |
        Permanently purge a team's data after explicit confirmation. Requires compliance:manage permission and authority over every member's current role, including hierarchy, permissions, tool access, and any API-key scope. A team containing the actor cannot be purged.
      security:
        - bearerAuth: []
      parameters:
        - name: id
          in: path
          required: true
          schema:
            type: string
      responses:
        '200':
          description: Team purged
        '400':
          description: Confirmation required
        '403':
          description: Feature not licensed, insufficient permission, or a member role beyond actor authority
        '409':
          description: Team is under legal hold
  /api/v1/admin/version:
    get:
      operationId: getAdminVersion
      tags:
        - Admin
      summary: Метадані версії
      description: Прочитати версію застосунку, дату збірки, версію Node та версію схеми. Потребує дозволу system:health та enterprise-функції керування оновленнями.
      security:
        - bearerAuth: []
      responses:
        '200':
          description: Version metadata
        '403':
          description: Feature not licensed or insufficient permissions
  /api/v1/admin/migrations/pending:
    get:
      operationId: getPendingMigrations
      tags:
        - Admin
      summary: Стан міграцій
      description: Порівняти запаковані міграції із застосованими міграціями. Потребує дозволу system:health та enterprise-функції керування оновленнями.
      security:
        - bearerAuth: []
      responses:
        '200':
          description: Migration status
        '403':
          description: Feature not licensed or insufficient permissions
  /api/v1/admin/upgrade-check:
    get:
      operationId: getUpgradeCheck
      tags:
        - Admin
      summary: Готовність до оновлення
      description: Запустити перевірки готовності до оновлення. Потребує дозволу system:health та enterprise-функції керування оновленнями.
      security:
        - bearerAuth: []
      responses:
        '200':
          description: Upgrade readiness result
        '403':
          description: Feature not licensed or insufficient permissions
  /llms.txt:
    get:
      operationId: getLlmsTxt
      tags:
        - System
      summary: Зведення для LLM
      description: Зручне для LLM зведення API у простому тексті, згенероване зі специфікації OpenAPI.
      security: []
      responses:
        '200':
          description: Plain-text LLM summary
          content:
            text/plain:
              schema:
                type: string
  /llms-full.txt:
    get:
      operationId: getLlmsFullTxt
      tags:
        - System
      summary: Повна документація API для LLM
      description: Повна документація API у простому тексті, згенерована зі специфікації OpenAPI.
      security: []
      responses:
        '200':
          description: Full plain-text API docs
          content:
            text/plain:
              schema:
                type: string
  /api/v1/openapi.yaml:
    get:
      operationId: getOpenApiYaml
      tags:
        - System
      summary: OpenAPI YAML
      description: Машиночитна специфікація OpenAPI 3.1 для цього екземпляра SnapOtter.
      security: []
      responses:
        '200':
          description: OpenAPI YAML document
          content:
            text/yaml:
              schema:
                type: string
  /api/v1/tools/popular:
    get:
      operationId: getPopularTools
      tags:
        - Tools
      summary: Популярні інструменти
      description: Повернути до 12 ID популярних інструментів, повертаючись до підготовленого списку за замовчуванням, коли даних про використання недостатньо.
      security: []
      responses:
        '200':
          description: Popular tool IDs
          content:
            application/json:
              schema:
                type: object
                properties:
                  tools:
                    type: array
                    items:
                      type: string
x-i18n:
  locale: uk
  generator: scripts/i18n/adapters/api-spec.mjs
  entries:
    info.description:
      sourceHash: 0a7d56a602a9
      provenance: machine
      outputHash: 151fe56cb74f
    tags.Tools.description:
      sourceHash: 9c71553d06df
      provenance: machine
      outputHash: a2dbddc8b963
    tags.Batch.description:
      sourceHash: f9c99eda7653
      provenance: machine
      outputHash: 9ac12388319a
    tags.Pipelines.description:
      sourceHash: 18d60931b26d
      provenance: machine
      outputHash: 88bb2ce273e6
    tags.Files.description:
      sourceHash: c3b0e1590a61
      provenance: machine
      outputHash: ce65849fa1f9
    tags.Auth.description:
      sourceHash: 23f6a0fcf611
      provenance: machine
      outputHash: decd2b143686
    tags.API Keys.description:
      sourceHash: fb725a6a642a
      provenance: machine
      outputHash: e203c88319cd
    tags.Settings.description:
      sourceHash: 25788b024fe2
      provenance: machine
      outputHash: e5195ed96676
    tags.Teams.description:
      sourceHash: 750d92c8bb9f
      provenance: machine
      outputHash: 355feb4cf8eb
    tags.Roles.description:
      sourceHash: d031f589a884
      provenance: machine
      outputHash: 9b327f826113
    tags.Audit.description:
      sourceHash: ed43b35a8636
      provenance: machine
      outputHash: c348bed070d8
    tags.Analytics.description:
      sourceHash: 36832a1b1df0
      provenance: machine
      outputHash: 017f35e029c3
    tags.Features.description:
      sourceHash: a6cdd16968b5
      provenance: machine
      outputHash: 7917df380054
    tags.Admin.description:
      sourceHash: c19c4b4f68c4
      provenance: machine
      outputHash: '365762891864'
    tags.System.description:
      sourceHash: 91561bc11296
      provenance: machine
      outputHash: cc879fe3895a
    tags.Enterprise.description:
      sourceHash: 91b6b46b0dd1
      provenance: machine
      outputHash: a25ce5bdade9
    paths./api/v1/health.get.summary:
      sourceHash: 14290e1d0681
      provenance: machine
      outputHash: f02754042384
    paths./api/v1/health.get.description:
      sourceHash: 784e94eaa9df
      provenance: machine
      outputHash: 50bcfab63c7b
    paths./api/v1/tools/image/resize.post.summary:
      sourceHash: 6cc5dc48975b
      provenance: machine
      outputHash: 1ff4ad002acc
    paths./api/v1/tools/image/resize.post.description:
      sourceHash: 3dd014df5258
      provenance: machine
      outputHash: c4caa0e0f9fe
    paths./api/v1/tools/image/content-aware-resize.post.summary:
      sourceHash: 7f86a6451d78
      provenance: machine
      outputHash: a842431a8a74
    paths./api/v1/tools/image/content-aware-resize.post.description:
      sourceHash: fe6de26388f5
      provenance: machine
      outputHash: 4eae334a5c69
    paths./api/v1/tools/image/crop.post.summary:
      sourceHash: fb3dabcf0d02
      provenance: machine
      outputHash: 83b1e566bf44
    paths./api/v1/tools/image/crop.post.description:
      sourceHash: db6c5c89d359
      provenance: machine
      outputHash: 23c80b6855ce
    paths./api/v1/tools/image/rotate.post.summary:
      sourceHash: 816edfdfe65e
      provenance: machine
      outputHash: 8dcb3d83e735
    paths./api/v1/tools/image/rotate.post.description:
      sourceHash: 5360b7d1a4c3
      provenance: machine
      outputHash: 24f872c66f18
    paths./api/v1/tools/image/convert.post.summary:
      sourceHash: 6ccca094d767
      provenance: machine
      outputHash: bd8834a0a909
    paths./api/v1/tools/image/convert.post.description:
      sourceHash: 2298d8af460d
      provenance: machine
      outputHash: 9a03ca37f117
    paths./api/v1/tools/image/compress.post.summary:
      sourceHash: a18573a37f49
      provenance: machine
      outputHash: 2f44e6ee0962
    paths./api/v1/tools/image/compress.post.description:
      sourceHash: 01f63e9e928a
      provenance: machine
      outputHash: 4f60cba62239
    paths./api/v1/tools/image/strip-metadata.post.summary:
      sourceHash: 1b49ff96cee6
      provenance: machine
      outputHash: 7b12b0562334
    paths./api/v1/tools/image/strip-metadata.post.description:
      sourceHash: b5718bfd5335
      provenance: machine
      outputHash: 3b27285b3ea0
    paths./api/v1/tools/image/border.post.summary:
      sourceHash: 1d23e2771077
      provenance: machine
      outputHash: 19aed1ba67ba
    paths./api/v1/tools/image/border.post.description:
      sourceHash: 6d48679c08cd
      provenance: machine
      outputHash: 53f3bfb11e3f
    paths./api/v1/tools/image/beautify.post.summary:
      sourceHash: 096b365107c5
      provenance: machine
      outputHash: 0535ff0947ab
    paths./api/v1/tools/image/beautify.post.description:
      sourceHash: 7db7fb58263f
      provenance: machine
      outputHash: 8b5d4af98340
    paths./api/v1/tools/image/adjust-colors.post.summary:
      sourceHash: a45b7d1e2535
      provenance: machine
      outputHash: 426fa465098e
    paths./api/v1/tools/image/adjust-colors.post.description:
      sourceHash: 1fb36db32da7
      provenance: machine
      outputHash: 745fb8530d98
    paths./api/v1/tools/image/sharpening.post.summary:
      sourceHash: d07b9f428bbf
      provenance: machine
      outputHash: 44505752ee02
    paths./api/v1/tools/image/sharpening.post.description:
      sourceHash: 8080cda0610a
      provenance: machine
      outputHash: 80cfeab472ff
    paths./api/v1/tools/image/optimize-for-web.post.summary:
      sourceHash: 54245d072fef
      provenance: machine
      outputHash: bc90c5e252a4
    paths./api/v1/tools/image/optimize-for-web.post.description:
      sourceHash: e846eaabbabc
      provenance: machine
      outputHash: 984c092c784e
    paths./api/v1/tools/image/optimize-for-web/preview.post.summary:
      sourceHash: f50e6149b56c
      provenance: machine
      outputHash: 0750adb4b387
    paths./api/v1/tools/image/optimize-for-web/preview.post.description:
      sourceHash: 10a427197f6e
      provenance: machine
      outputHash: 23413a953f08
    paths./api/v1/tools/image/image-enhancement.post.summary:
      sourceHash: 246a536c8207
      provenance: machine
      outputHash: 5d0c1cc1ecf1
    paths./api/v1/tools/image/image-enhancement.post.description:
      sourceHash: 17ec5e5e2b04
      provenance: machine
      outputHash: 1442ece9736c
    paths./api/v1/tools/image/image-enhancement/analyze.post.summary:
      sourceHash: 2697cf8b079f
      provenance: machine
      outputHash: f2e3e3ee15a3
    paths./api/v1/tools/image/image-enhancement/analyze.post.description:
      sourceHash: 7712bfd40c3a
      provenance: machine
      outputHash: e633335cca8b
    paths./api/v1/tools/image/noise-removal.post.summary:
      sourceHash: 9301fe47b563
      provenance: machine
      outputHash: 57578c01e594
    paths./api/v1/tools/image/noise-removal.post.description:
      sourceHash: e16aa743abcb
      provenance: machine
      outputHash: 019c6a204d58
    paths./api/v1/tools/image/red-eye-removal.post.summary:
      sourceHash: d9315c5a87b4
      provenance: machine
      outputHash: 71767ea8ad04
    paths./api/v1/tools/image/red-eye-removal.post.description:
      sourceHash: 479c687988eb
      provenance: machine
      outputHash: 757e50d63da5
    paths./api/v1/tools/image/restore-photo.post.summary:
      sourceHash: 3ef425c0ba9b
      provenance: machine
      outputHash: 2088abc11354
    paths./api/v1/tools/image/restore-photo.post.description:
      sourceHash: 6e3b2de0372c
      provenance: machine
      outputHash: 43a603bef25f
    paths./api/v1/tools/image/passport-photo/analyze.post.summary:
      sourceHash: 0a3c3687ff5f
      provenance: machine
      outputHash: f1ffd2136891
    paths./api/v1/tools/image/passport-photo/analyze.post.description:
      sourceHash: 235c51cb497e
      provenance: machine
      outputHash: d91670417e1e
    paths./api/v1/tools/image/passport-photo/generate.post.summary:
      sourceHash: 029eecf0438e
      provenance: machine
      outputHash: 44c464ed5417
    paths./api/v1/tools/image/passport-photo/generate.post.description:
      sourceHash: d0edd7c634d9
      provenance: machine
      outputHash: eb712dca841c
    paths./api/v1/tools/image/colorize.post.summary:
      sourceHash: 34df56e96234
      provenance: machine
      outputHash: 7f7cfbc0dcaa
    paths./api/v1/tools/image/colorize.post.description:
      sourceHash: 04d7dc54396a
      provenance: machine
      outputHash: 67cb68c7bfc0
    paths./api/v1/tools/image/enhance-faces.post.summary:
      sourceHash: 74f27131fec9
      provenance: machine
      outputHash: 7bd2654b735a
    paths./api/v1/tools/image/enhance-faces.post.description:
      sourceHash: beec2f7e8561
      provenance: machine
      outputHash: 8a6dd4f9018f
    paths./api/v1/tools/image/image-to-base64.post.summary:
      sourceHash: 440737c09f0d
      provenance: machine
      outputHash: aab4b70f88ca
    paths./api/v1/tools/image/image-to-base64.post.description:
      sourceHash: 02a80d960185
      provenance: machine
      outputHash: a5f5d6403202
    paths./api/v1/tools/image/watermark-text.post.summary:
      sourceHash: dfef3d1052de
      provenance: machine
      outputHash: dc590b8a52d6
    paths./api/v1/tools/image/watermark-text.post.description:
      sourceHash: 158e444c0a77
      provenance: machine
      outputHash: 3f3a9d5f282e
    paths./api/v1/tools/image/text-overlay.post.summary:
      sourceHash: 7311c0ffc340
      provenance: machine
      outputHash: 5d7644f4e6eb
    paths./api/v1/tools/image/text-overlay.post.description:
      sourceHash: e5dd74752a49
      provenance: machine
      outputHash: 68a564f7ade7
    paths./api/v1/tools/image/replace-color.post.summary:
      sourceHash: 80e60e5fe0ed
      provenance: machine
      outputHash: 7963013a25c9
    paths./api/v1/tools/image/replace-color.post.description:
      sourceHash: cbafc4d168c3
      provenance: machine
      outputHash: 2cb72060924f
    paths./api/v1/tools/image/color-blindness.post.summary:
      sourceHash: 1ab73f30893d
      provenance: machine
      outputHash: 0386f38fc2c8
    paths./api/v1/tools/image/color-blindness.post.description:
      sourceHash: 1e362d094237
      provenance: machine
      outputHash: aff785e9fb05
    paths./api/v1/tools/image/gif-tools.post.summary:
      sourceHash: 985d94d31548
      provenance: machine
      outputHash: '088723749675'
    paths./api/v1/tools/image/gif-tools.post.description:
      sourceHash: fe7113c6fefd
      provenance: machine
      outputHash: 39ec4af5c847
    paths./api/v1/tools/image/gif-tools/info.post.summary:
      sourceHash: e864c10b29f7
      provenance: machine
      outputHash: 13f179d8ce1a
    paths./api/v1/tools/image/gif-tools/info.post.description:
      sourceHash: aaeba6b11dbf
      provenance: machine
      outputHash: 753fdcbfe246
    paths./api/v1/tools/image/smart-crop.post.summary:
      sourceHash: 890848540d35
      provenance: machine
      outputHash: ada517891325
    paths./api/v1/tools/image/smart-crop.post.description:
      sourceHash: 0f00f334ba29
      provenance: machine
      outputHash: 644fd701d19e
    paths./api/v1/tools/image/vectorize.post.summary:
      sourceHash: ffa9db773ada
      provenance: machine
      outputHash: 56e69f1db3a1
    paths./api/v1/tools/image/vectorize.post.description:
      sourceHash: 3d5de38dc3f3
      provenance: machine
      outputHash: f6b36f71178e
    paths./api/v1/tools/image/svg-to-raster.post.summary:
      sourceHash: bccef23ad06b
      provenance: machine
      outputHash: 1a01ab37a930
    paths./api/v1/tools/image/svg-to-raster.post.description:
      sourceHash: 27a0b0dfe170
      provenance: machine
      outputHash: d68d9b556313
    paths./api/v1/tools/image/svg-to-raster/batch.post.summary:
      sourceHash: 340743c4e68b
      provenance: machine
      outputHash: c57dfe5c5595
    paths./api/v1/tools/image/svg-to-raster/batch.post.description:
      sourceHash: a0069d315b7e
      provenance: machine
      outputHash: d56c4e674984
    paths./api/v1/tools/image/image-to-pdf.post.summary:
      sourceHash: 966eba6e1412
      provenance: machine
      outputHash: 98e7fe0d4b1a
    paths./api/v1/tools/image/image-to-pdf.post.description:
      sourceHash: f5a0819f56f7
      provenance: machine
      outputHash: dd07baab804c
    paths./api/v1/tools/pdf/pdf-to-image.post.summary:
      sourceHash: 4c3c2ae28b1f
      provenance: machine
      outputHash: 7b3f8e1cfb16
    paths./api/v1/tools/pdf/pdf-to-image.post.description:
      sourceHash: 4f0f5c643801
      provenance: machine
      outputHash: 8e4a763565e0
    paths./api/v1/tools/pdf/pdf-to-image/info.post.summary:
      sourceHash: d9398268b597
      provenance: machine
      outputHash: 7fbb5c331ac5
    paths./api/v1/tools/pdf/pdf-to-image/info.post.description:
      sourceHash: 9ace4aaa3978
      provenance: machine
      outputHash: 98efd0115e28
    paths./api/v1/tools/pdf/pdf-to-image/preview.post.summary:
      sourceHash: 3bd369a401ba
      provenance: machine
      outputHash: 0201f88c23a5
    paths./api/v1/tools/pdf/pdf-to-image/preview.post.description:
      sourceHash: 90db92475b9a
      provenance: machine
      outputHash: 23adee040462
    paths./api/v1/tools/image/split.post.summary:
      sourceHash: 6a45e35cf509
      provenance: machine
      outputHash: b70c47aeb8fc
    paths./api/v1/tools/image/split.post.description:
      sourceHash: d7d4988765b3
      provenance: machine
      outputHash: 6ab66d0bc4da
    paths./api/v1/tools/image/bulk-rename.post.summary:
      sourceHash: 5489fe2b69b0
      provenance: machine
      outputHash: d93a136ac2ec
    paths./api/v1/tools/image/bulk-rename.post.description:
      sourceHash: 093bf3688b13
      provenance: machine
      outputHash: f5f3d15b7a6e
    paths./api/v1/tools/image/favicon.post.summary:
      sourceHash: 7a587af97797
      provenance: machine
      outputHash: bda11d3db5dd
    paths./api/v1/tools/image/favicon.post.description:
      sourceHash: 5351a1c80471
      provenance: machine
      outputHash: 3acba2a80145
    paths./api/v1/tools/image/watermark-image.post.summary:
      sourceHash: 577e5e431f77
      provenance: machine
      outputHash: 978068f8dd3b
    paths./api/v1/tools/image/watermark-image.post.description:
      sourceHash: 4e103b8a7d17
      provenance: machine
      outputHash: 50dca6486ce9
    paths./api/v1/tools/image/compose.post.summary:
      sourceHash: 06718c2a3b98
      provenance: machine
      outputHash: 0218d355a252
    paths./api/v1/tools/image/compose.post.description:
      sourceHash: d48d40e77edf
      provenance: machine
      outputHash: d3cee72b0325
    paths./api/v1/tools/image/meme-generator.post.summary:
      sourceHash: a4e89614a5c1
      provenance: machine
      outputHash: 6d4be9bf2369
    paths./api/v1/tools/image/meme-generator.post.description:
      sourceHash: 7aa04984be62
      provenance: machine
      outputHash: 37bd65875684
    paths./api/v1/meme-templates.get.summary:
      sourceHash: fd3392277306
      provenance: machine
      outputHash: 3467e65f08fc
    paths./api/v1/meme-templates.get.description:
      sourceHash: a147e348597c
      provenance: machine
      outputHash: d400cfc28231
    paths./api/v1/meme-templates/full/{filename}.get.summary:
      sourceHash: fd501704c913
      provenance: machine
      outputHash: ba365186e407
    paths./api/v1/meme-templates/full/{filename}.get.description:
      sourceHash: 3bb962f4402c
      provenance: machine
      outputHash: 530c4097925a
    paths./api/v1/meme-templates/thumbs/{filename}.get.summary:
      sourceHash: d2102190f5a2
      provenance: machine
      outputHash: 11a75a2be35c
    paths./api/v1/meme-templates/thumbs/{filename}.get.description:
      sourceHash: 77c69805732d
      provenance: machine
      outputHash: 2fa233d4cd68
    paths./api/v1/meme-templates/fonts/{filename}.get.summary:
      sourceHash: 3627280f2609
      provenance: machine
      outputHash: 742429b4c54e
    paths./api/v1/meme-templates/fonts/{filename}.get.description:
      sourceHash: 717c6764f472
      provenance: machine
      outputHash: e023f7bde900
    paths./api/v1/tools/image/compare.post.summary:
      sourceHash: 46e3612ffee1
      provenance: machine
      outputHash: 9fa03dc88005
    paths./api/v1/tools/image/compare.post.description:
      sourceHash: ac21fee13ecc
      provenance: machine
      outputHash: 37de365e1480
    paths./api/v1/tools/image/erase-object.post.summary:
      sourceHash: 21f13ba2930d
      provenance: machine
      outputHash: 2a931a879dd2
    paths./api/v1/tools/image/erase-object.post.description:
      sourceHash: 9d6614ed28c6
      provenance: machine
      outputHash: 43492fbe7321
    paths./api/v1/tools/image/ai-canvas-expand.post.summary:
      sourceHash: 990aeca723ff
      provenance: machine
      outputHash: dc5f400f6299
    paths./api/v1/tools/image/ai-canvas-expand.post.description:
      sourceHash: 084de536af45
      provenance: machine
      outputHash: 822bf0e75e44
    paths./api/v1/tools/image/collage.post.summary:
      sourceHash: 6e5e905e8c16
      provenance: machine
      outputHash: 60bc639753e2
    paths./api/v1/tools/image/collage.post.description:
      sourceHash: fb0ba3d2cf44
      provenance: machine
      outputHash: ff81dd687980
    paths./api/v1/tools/image/stitch.post.summary:
      sourceHash: d0e2b34fe22b
      provenance: machine
      outputHash: 2919f2a343c5
    paths./api/v1/tools/image/stitch.post.description:
      sourceHash: 914837a5d232
      provenance: machine
      outputHash: 81b7555b4b9e
    paths./api/v1/tools/image/remove-gif-background.post.summary:
      sourceHash: a316267cf5d1
      provenance: machine
      outputHash: 11d87bcd743b
    paths./api/v1/tools/image/remove-gif-background.post.description:
      sourceHash: ef1d100f2345
      provenance: machine
      outputHash: 3e9d86d6afab
    paths./api/v1/tools/image/remove-background.post.summary:
      sourceHash: a8d1ab54b32c
      provenance: machine
      outputHash: 247af10f7315
    paths./api/v1/tools/image/remove-background.post.description:
      sourceHash: 3bb193c0a2f9
      provenance: machine
      outputHash: c0e35724f16a
    paths./api/v1/tools/image/remove-background/effects.post.summary:
      sourceHash: 2194d6947f3f
      provenance: machine
      outputHash: bf8733aee5bc
    paths./api/v1/tools/image/remove-background/effects.post.description:
      sourceHash: 1e2ab8e3b1ae
      provenance: machine
      outputHash: 7d8d51ce13f7
    paths./api/v1/tools/image/transparency-fixer.post.summary:
      sourceHash: 0cf307cacb91
      provenance: machine
      outputHash: 4dbc65699ad5
    paths./api/v1/tools/image/transparency-fixer.post.description:
      sourceHash: 67b2a3c1cb7b
      provenance: machine
      outputHash: 33cbed325a7e
    paths./api/v1/tools/image/upscale.post.summary:
      sourceHash: ce44856d9e64
      provenance: machine
      outputHash: 321cf7eb7c75
    paths./api/v1/tools/image/upscale.post.description:
      sourceHash: e603db60e711
      provenance: machine
      outputHash: 87343d0b6fec
    paths./api/v1/tools/image/blur-faces.post.summary:
      sourceHash: d9a7e1d9a145
      provenance: machine
      outputHash: 6a3a6af5e32a
    paths./api/v1/tools/image/blur-faces.post.description:
      sourceHash: 28790ae08ac7
      provenance: machine
      outputHash: c22e9958758a
    paths./api/v1/tools/image/ocr.post.summary:
      sourceHash: 0b508f19963b
      provenance: machine
      outputHash: c19d4398f38d
    paths./api/v1/tools/image/ocr.post.description:
      sourceHash: 7b74b30329a5
      provenance: machine
      outputHash: 379122498b4d
    paths./api/v1/tools/image/info.post.summary:
      sourceHash: 1823e118aed5
      provenance: machine
      outputHash: c43399c8cf8b
    paths./api/v1/tools/image/info.post.description:
      sourceHash: 196d7190f16c
      provenance: machine
      outputHash: 3641b863c04f
    paths./api/v1/tools/image/color-palette.post.summary:
      sourceHash: 81a9be1a8044
      provenance: machine
      outputHash: bce1f6ea5e33
    paths./api/v1/tools/image/color-palette.post.description:
      sourceHash: d34d5ff2ade7
      provenance: machine
      outputHash: 8acdaa61c6f4
    paths./api/v1/tools/image/barcode-read.post.summary:
      sourceHash: b51dcac65420
      provenance: machine
      outputHash: 6e1186b48e25
    paths./api/v1/tools/image/barcode-read.post.description:
      sourceHash: ae952218a233
      provenance: machine
      outputHash: b395d1a84c45
    paths./api/v1/tools/image/find-duplicates.post.summary:
      sourceHash: bd6e5d3e4d26
      provenance: machine
      outputHash: 30c66446137e
    paths./api/v1/tools/image/find-duplicates.post.description:
      sourceHash: 61322792b4bd
      provenance: machine
      outputHash: bc656f4ee206
    paths./api/v1/tools/image/qr-generate.post.summary:
      sourceHash: 799653bb4ee1
      provenance: machine
      outputHash: 98c78f5a412c
    paths./api/v1/tools/image/qr-generate.post.description:
      sourceHash: ad4154c7b555
      provenance: machine
      outputHash: 03a71ae48425
    paths./api/v1/tools/image/html-to-image.post.summary:
      sourceHash: d4dcd8d52cfd
      provenance: machine
      outputHash: 7e18d4b5c1be
    paths./api/v1/tools/image/html-to-image.post.description:
      sourceHash: adb3e27eb0e6
      provenance: machine
      outputHash: 31784988d181
    paths./api/v1/tools/image/strip-metadata/inspect.post.summary:
      sourceHash: 26c48e56ad62
      provenance: machine
      outputHash: cdb0a054bf82
    paths./api/v1/tools/image/strip-metadata/inspect.post.description:
      sourceHash: a6a7d5c72088
      provenance: machine
      outputHash: ee01fadf80cf
    paths./api/v1/tools/image/edit-metadata.post.summary:
      sourceHash: b2e22f0aa9d8
      provenance: machine
      outputHash: c0fc65291bb3
    paths./api/v1/tools/image/edit-metadata.post.description:
      sourceHash: 70d277ec5566
      provenance: machine
      outputHash: 3cee6b063bcf
    paths./api/v1/tools/image/edit-metadata/inspect.post.summary:
      sourceHash: a9b31ad7470f
      provenance: machine
      outputHash: 1e353dfa665d
    paths./api/v1/tools/image/edit-metadata/inspect.post.description:
      sourceHash: 71173fd89d89
      provenance: machine
      outputHash: 8964673f6daa
    paths./api/v1/tools/{section}/{toolId}/batch.post.summary:
      sourceHash: c48b2b0a9212
      provenance: machine
      outputHash: 36bd533fd33d
    paths./api/v1/tools/{section}/{toolId}/batch.post.description:
      sourceHash: e2ddfe55094d
      provenance: machine
      outputHash: 4387003b17e1
    paths./api/v1/pipeline/execute.post.summary:
      sourceHash: 6357b47fa7a9
      provenance: machine
      outputHash: d10113e6ae21
    paths./api/v1/pipeline/execute.post.description:
      sourceHash: ca3c9eb4acf1
      provenance: machine
      outputHash: 2c099f58c443
    paths./api/v1/pipeline/save.post.summary:
      sourceHash: 8209ec3e0c9b
      provenance: machine
      outputHash: 2d5005eb851a
    paths./api/v1/pipeline/save.post.description:
      sourceHash: a904ab4f3701
      provenance: machine
      outputHash: f3fbd48fc246
    paths./api/v1/pipeline/list.get.summary:
      sourceHash: c8d290eff4a7
      provenance: machine
      outputHash: be5568267580
    paths./api/v1/pipeline/list.get.description:
      sourceHash: d11170073568
      provenance: machine
      outputHash: 863adb2113a6
    paths./api/v1/pipeline/{id}.delete.summary:
      sourceHash: 0a798831c1be
      provenance: machine
      outputHash: 4da79cb08ea5
    paths./api/v1/pipeline/{id}.delete.description:
      sourceHash: e3b43bf6d73d
      provenance: machine
      outputHash: 625e83d82229
    paths./api/v1/pipeline/tools.get.summary:
      sourceHash: 79f28e6075f7
      provenance: machine
      outputHash: 4e600471fddc
    paths./api/v1/pipeline/tools.get.description:
      sourceHash: 6f341cc670d3
      provenance: machine
      outputHash: 1270d7a98945
    paths./api/v1/pipeline/batch.post.summary:
      sourceHash: 14abb819b562
      provenance: machine
      outputHash: 2841aff57659
    paths./api/v1/pipeline/batch.post.description:
      sourceHash: 0c7b90acb5d8
      provenance: machine
      outputHash: 80a29b2a1ba2
    paths./api/v1/upload.post.summary:
      sourceHash: 25d0eba68bdc
      provenance: machine
      outputHash: bd195b3b44a5
    paths./api/v1/upload.post.description:
      sourceHash: a9e18800407e
      provenance: machine
      outputHash: edd2e6bb30e8
    paths./api/v1/download/{jobId}/{filename}.get.summary:
      sourceHash: 6246d8c07e4b
      provenance: machine
      outputHash: 470013166dac
    paths./api/v1/download/{jobId}/{filename}.get.description:
      sourceHash: a09d603b069c
      provenance: machine
      outputHash: 3eae49609ba4
    paths./api/v1/preview.post.summary:
      sourceHash: 8266fbd3ff72
      provenance: machine
      outputHash: 17d8e831387e
    paths./api/v1/preview.post.description:
      sourceHash: d5e04d49c594
      provenance: machine
      outputHash: 7cc6e59e3aea
    paths./api/v1/files.get.summary:
      sourceHash: aefb51ff8aaa
      provenance: machine
      outputHash: 6d4d91c21432
    paths./api/v1/files.get.description:
      sourceHash: 45f45ad625ed
      provenance: machine
      outputHash: 5c79be48752c
    paths./api/v1/files.delete.summary:
      sourceHash: 179bb72a6deb
      provenance: machine
      outputHash: 1f43c9342b8a
    paths./api/v1/files.delete.description:
      sourceHash: 1bfaa5c376bb
      provenance: machine
      outputHash: 6ece9ceaf533
    paths./api/v1/files/upload.post.summary:
      sourceHash: b94c597782ee
      provenance: machine
      outputHash: 41699438cd90
    paths./api/v1/files/upload.post.description:
      sourceHash: 8a070581c1e5
      provenance: machine
      outputHash: 64419a67e4ab
    paths./api/v1/files/save-result.post.summary:
      sourceHash: c35e320d9164
      provenance: machine
      outputHash: 904a88b01e7e
    paths./api/v1/files/save-result.post.description:
      sourceHash: 3c434713031e
      provenance: machine
      outputHash: c06def80816f
    paths./api/v1/files/{id}.get.summary:
      sourceHash: a09c1fb7e33a
      provenance: machine
      outputHash: 6cefff8887be
    paths./api/v1/files/{id}.get.description:
      sourceHash: 8193066e23c5
      provenance: machine
      outputHash: 722adf2de1b3
    paths./api/v1/files/{id}/download.get.summary:
      sourceHash: c0043bd46394
      provenance: machine
      outputHash: e90e4d543aaf
    paths./api/v1/files/{id}/download.get.description:
      sourceHash: 96df2a599e22
      provenance: machine
      outputHash: 661bab82b274
    paths./api/v1/files/{id}/thumbnail.get.summary:
      sourceHash: 07714ad0c5f8
      provenance: machine
      outputHash: d2a6fa1851ab
    paths./api/v1/files/{id}/thumbnail.get.description:
      sourceHash: 9b04bbf75cba
      provenance: machine
      outputHash: 41212877738e
    paths./api/v1/fetch-urls.post.summary:
      sourceHash: a00341e59161
      provenance: machine
      outputHash: 3f1efc2048eb
    paths./api/v1/fetch-urls.post.description:
      sourceHash: ac474639261c
      provenance: machine
      outputHash: c44a69a3d73b
    paths./api/v1/config/locale.get.summary:
      sourceHash: 78de2255222d
      provenance: machine
      outputHash: 8fd1b36ed54c
    paths./api/v1/config/locale.get.description:
      sourceHash: b9967e400d09
      provenance: machine
      outputHash: b4b4bcf60d35
    paths./api/auth/login.post.summary:
      sourceHash: c189840cf7e2
      provenance: machine
      outputHash: f584775e1c28
    paths./api/auth/login.post.description:
      sourceHash: 91ad9a828bf3
      provenance: machine
      outputHash: a55c4ea482d8
    paths./api/auth/logout.post.summary:
      sourceHash: 49616145514e
      provenance: machine
      outputHash: a374e0798f94
    paths./api/auth/logout.post.description:
      sourceHash: 0b9ae300808d
      provenance: machine
      outputHash: 011d2f1b7f08
    paths./api/auth/session.get.summary:
      sourceHash: f4f7376ca1ea
      provenance: machine
      outputHash: 6217baf05aef
    paths./api/auth/session.get.description:
      sourceHash: 2dad822c46ea
      provenance: machine
      outputHash: 5ac4faeb17a2
    paths./api/auth/change-password.post.summary:
      sourceHash: 3f9c991f63a9
      provenance: machine
      outputHash: 77bb9620cde6
    paths./api/auth/change-password.post.description:
      sourceHash: 5190db8a04e7
      provenance: machine
      outputHash: d3dd333528ac
    paths./api/auth/users.get.summary:
      sourceHash: '9029730603e0'
      provenance: machine
      outputHash: ab61ddd8121b
    paths./api/auth/users.get.description:
      sourceHash: 34d5a11733bd
      provenance: machine
      outputHash: ce657feb23af
    paths./api/auth/register.post.summary:
      sourceHash: f06da128c342
      provenance: machine
      outputHash: 15c947f247b4
    paths./api/auth/users/{id}.put.summary:
      sourceHash: 602b8b1607b0
      provenance: machine
      outputHash: 052a37f3f7b8
    paths./api/auth/users/{id}.delete.summary:
      sourceHash: 58363fd94240
      provenance: machine
      outputHash: 591461167c11
    paths./api/auth/users/{id}/reset-password.post.summary:
      sourceHash: aae899c2d3ad
      provenance: machine
      outputHash: 0f5550c0a24d
    paths./api/v1/api-keys.get.summary:
      sourceHash: ad0527749f03
      provenance: machine
      outputHash: c5fff0ec3819
    paths./api/v1/api-keys.get.description:
      sourceHash: 470cee6220f0
      provenance: machine
      outputHash: 9d845a44a2f0
    paths./api/v1/api-keys.post.summary:
      sourceHash: eb136082215a
      provenance: machine
      outputHash: b4140639b367
    paths./api/v1/api-keys.post.description:
      sourceHash: 7568aec4ab9b
      provenance: machine
      outputHash: 634115ca0e4c
    paths./api/v1/api-keys/{id}.delete.summary:
      sourceHash: 9ebde15b7f02
      provenance: machine
      outputHash: a98a3f22fd27
    paths./api/v1/api-keys/{id}.delete.description:
      sourceHash: 46a57dbc68a5
      provenance: machine
      outputHash: f6df51cb7721
    paths./api/v1/settings.get.summary:
      sourceHash: 8530e17de153
      provenance: machine
      outputHash: d5a2e5936382
    paths./api/v1/settings.get.description:
      sourceHash: 65d8ff0f5873
      provenance: machine
      outputHash: 5b6a5774bbc9
    paths./api/v1/settings.put.summary:
      sourceHash: 46ed69579c39
      provenance: machine
      outputHash: 022d75be8a33
    paths./api/v1/settings.put.description:
      sourceHash: 887f8facdd94
      provenance: machine
      outputHash: 6f8bdc5f2726
    paths./api/v1/settings/{key}.get.summary:
      sourceHash: 186add054525
      provenance: machine
      outputHash: 8d466ef13a3b
    paths./api/v1/settings/{key}.get.description:
      sourceHash: 962db9ab06a3
      provenance: machine
      outputHash: af91370069b1
    paths./api/v1/teams.get.summary:
      sourceHash: 863546c57b78
      provenance: machine
      outputHash: 30cd603c4ade
    paths./api/v1/teams.get.description:
      sourceHash: 88ce5170c6ed
      provenance: machine
      outputHash: 6c5826eac7cd
    paths./api/v1/teams.post.summary:
      sourceHash: 00766ad4b096
      provenance: machine
      outputHash: 4456d8ae015b
    paths./api/v1/teams.post.description:
      sourceHash: 88ce5170c6ed
      provenance: machine
      outputHash: 6c5826eac7cd
    paths./api/v1/teams/{id}.put.summary:
      sourceHash: c5dd33827a5b
      provenance: machine
      outputHash: 6a513bab4212
    paths./api/v1/teams/{id}.put.description:
      sourceHash: 88ce5170c6ed
      provenance: machine
      outputHash: 6c5826eac7cd
    paths./api/v1/teams/{id}.delete.summary:
      sourceHash: b51488367ba6
      provenance: machine
      outputHash: ef7c0b7fb503
    paths./api/v1/teams/{id}.delete.description:
      sourceHash: 88ce5170c6ed
      provenance: machine
      outputHash: 6c5826eac7cd
    paths./api/v1/config/auth.get.summary:
      sourceHash: 13189298880a
      provenance: machine
      outputHash: 5d4bc7b11c34
    paths./api/v1/config/auth.get.description:
      sourceHash: 404909083a24
      provenance: machine
      outputHash: 6ad612dcd941
    paths./api/v1/jobs/{jobId}/progress.get.summary:
      sourceHash: bcf19846aa43
      provenance: machine
      outputHash: ac39c064ebdc
    paths./api/v1/jobs/{jobId}/progress.get.description:
      sourceHash: 2df913c75d85
      provenance: human
      outputHash: db1a20faf44a
    paths./api/v1/admin/health.get.summary:
      sourceHash: 92f8575c9c94
      provenance: machine
      outputHash: e403d3e34580
    paths./api/v1/admin/health.get.description:
      sourceHash: 4abbba893ea4
      provenance: machine
      outputHash: 7109fa5413e3
    paths./api/v1/config/analytics.get.summary:
      sourceHash: b7fdcf112830
      provenance: machine
      outputHash: 08efe3f10331
    paths./api/v1/config/analytics.get.description:
      sourceHash: 2447c239373f
      provenance: machine
      outputHash: 8d31045da194
    paths./api/v1/features.get.summary:
      sourceHash: e45ee3163f96
      provenance: machine
      outputHash: d8315d57a816
    paths./api/v1/features.get.description:
      sourceHash: aa0b433ca9a9
      provenance: machine
      outputHash: f558cf2c21ad
    paths./api/v1/admin/features/{bundleId}/install.post.summary:
      sourceHash: bebc6f7ea7c3
      provenance: machine
      outputHash: ecd1857059a1
    paths./api/v1/admin/features/{bundleId}/install.post.description:
      sourceHash: 6051af19be7e
      provenance: machine
      outputHash: 38506c20f814
    paths./api/v1/admin/features/{bundleId}/uninstall.post.summary:
      sourceHash: 160eaf179d4f
      provenance: machine
      outputHash: 8d4f27f850a2
    paths./api/v1/admin/features/{bundleId}/uninstall.post.description:
      sourceHash: 35bdb459a1a7
      provenance: machine
      outputHash: 1843b29c8275
    paths./api/v1/admin/features/disk-usage.get.summary:
      sourceHash: 54dc8991b9a7
      provenance: machine
      outputHash: 7a669c3b842b
    paths./api/v1/admin/features/disk-usage.get.description:
      sourceHash: a78fac026ea3
      provenance: machine
      outputHash: dadac7df0951
    paths./api/v1/audit-log.get.summary:
      sourceHash: 05536545727c
      provenance: machine
      outputHash: 2539a7b3e125
    paths./api/v1/audit-log.get.description:
      sourceHash: b7950dcf7e49
      provenance: machine
      outputHash: 553b195c43f4
    paths./api/v1/roles.get.summary:
      sourceHash: 833a75e86b93
      provenance: machine
      outputHash: 5466ec29a7f3
    paths./api/v1/roles.get.description:
      sourceHash: 9ccdec8630d3
      provenance: machine
      outputHash: 768201159a41
    paths./api/v1/roles.post.summary:
      sourceHash: 6dd06b2529d4
      provenance: machine
      outputHash: 45fb1a9e83a6
    paths./api/v1/roles/{id}.put.summary:
      sourceHash: 6c0cda1bc967
      provenance: machine
      outputHash: f59d5d29c51c
    paths./api/v1/roles/{id}.delete.summary:
      sourceHash: d5a3ab01055a
      provenance: machine
      outputHash: 17d9514bafaa
    paths./api/v1/tools/video/convert-video.post.summary:
      sourceHash: b4defe1d6906
      provenance: machine
      outputHash: 8e7a5d9fcee1
    paths./api/v1/tools/video/convert-video.post.description:
      sourceHash: 00ab19fbffe4
      provenance: machine
      outputHash: 226ba486826d
    paths./api/v1/tools/video/compress-video.post.summary:
      sourceHash: 8af3d4b27a4b
      provenance: machine
      outputHash: 5205ffbc03bf
    paths./api/v1/tools/video/compress-video.post.description:
      sourceHash: 7f517e885483
      provenance: machine
      outputHash: bd8ee64aee7d
    paths./api/v1/tools/video/trim-video.post.summary:
      sourceHash: af6ed946e090
      provenance: machine
      outputHash: 8fa3607b7f0f
    paths./api/v1/tools/video/trim-video.post.description:
      sourceHash: c01dd24a19dd
      provenance: machine
      outputHash: 00d6c4fd7b28
    paths./api/v1/tools/video/mute-video.post.summary:
      sourceHash: 48ed61cb4802
      provenance: machine
      outputHash: ff14e2268ed3
    paths./api/v1/tools/video/mute-video.post.description:
      sourceHash: 76898fc22f53
      provenance: machine
      outputHash: 9fd7eb35b2bc
    paths./api/v1/tools/video/video-to-gif.post.summary:
      sourceHash: 4abd5ab74c35
      provenance: machine
      outputHash: f06d2932ab30
    paths./api/v1/tools/video/video-to-gif.post.description:
      sourceHash: d2d195459149
      provenance: machine
      outputHash: 5ba73bbe996f
    paths./api/v1/tools/video/resize-video.post.summary:
      sourceHash: 99fde7cc129d
      provenance: machine
      outputHash: 10a379148cfc
    paths./api/v1/tools/video/resize-video.post.description:
      sourceHash: ffe090ccf55b
      provenance: machine
      outputHash: 37a793c8d5ea
    paths./api/v1/tools/video/crop-video.post.summary:
      sourceHash: f042b21b17c0
      provenance: machine
      outputHash: 8fa3607b7f0f
    paths./api/v1/tools/video/crop-video.post.description:
      sourceHash: a70528f0d600
      provenance: machine
      outputHash: f0caa065cfe8
    paths./api/v1/tools/video/rotate-video.post.summary:
      sourceHash: a1d5d2488fd5
      provenance: machine
      outputHash: ae2115f6d250
    paths./api/v1/tools/video/rotate-video.post.description:
      sourceHash: 0858085b737b
      provenance: machine
      outputHash: e2f574cdc326
    paths./api/v1/tools/video/change-fps.post.summary:
      sourceHash: bc48dbea9413
      provenance: machine
      outputHash: fbc4d19b54b8
    paths./api/v1/tools/video/change-fps.post.description:
      sourceHash: 20c9ae3f7c23
      provenance: machine
      outputHash: c2dd088a5c0b
    paths./api/v1/tools/video/video-color.post.summary:
      sourceHash: 6005cc99c426
      provenance: machine
      outputHash: d2909d44bf7f
    paths./api/v1/tools/video/video-color.post.description:
      sourceHash: f8ce7dae4f04
      provenance: machine
      outputHash: fd39201bba5f
    paths./api/v1/tools/video/video-speed.post.summary:
      sourceHash: 4439901f7d61
      provenance: machine
      outputHash: f3a39984ec06
    paths./api/v1/tools/video/video-speed.post.description:
      sourceHash: 7a4b64e60829
      provenance: machine
      outputHash: cc260acf673e
    paths./api/v1/tools/video/reverse-video.post.summary:
      sourceHash: 4f0d7b5ea9ae
      provenance: machine
      outputHash: 9406c24df38b
    paths./api/v1/tools/video/reverse-video.post.description:
      sourceHash: 77abcb48d823
      provenance: machine
      outputHash: ce044167ff5d
    paths./api/v1/tools/video/video-loudnorm.post.summary:
      sourceHash: e502f3536632
      provenance: machine
      outputHash: d6e6cdd5bb54
    paths./api/v1/tools/video/video-loudnorm.post.description:
      sourceHash: 019f6147f722
      provenance: machine
      outputHash: c1a1fa46c0e2
    paths./api/v1/tools/video/aspect-pad.post.summary:
      sourceHash: aa7c02eeacf1
      provenance: machine
      outputHash: c36b7c121b0e
    paths./api/v1/tools/video/aspect-pad.post.description:
      sourceHash: 288b33cc62de
      provenance: machine
      outputHash: 1f446427cf1f
    paths./api/v1/tools/video/blur-pad.post.summary:
      sourceHash: 84af9c217d52
      provenance: machine
      outputHash: 0799a24932b8
    paths./api/v1/tools/video/blur-pad.post.description:
      sourceHash: d49a3fab5e02
      provenance: machine
      outputHash: b1ef30241f08
    paths./api/v1/tools/video/watermark-video.post.summary:
      sourceHash: bbbe197c46c5
      provenance: machine
      outputHash: 6c97c4e50512
    paths./api/v1/tools/video/watermark-video.post.description:
      sourceHash: d914078985d7
      provenance: machine
      outputHash: 8e14ec7921b3
    paths./api/v1/tools/video/stabilize-video.post.summary:
      sourceHash: f76615a74aac
      provenance: machine
      outputHash: 240a68c5b0ef
    paths./api/v1/tools/video/stabilize-video.post.description:
      sourceHash: 42e5e8a954ae
      provenance: machine
      outputHash: 8681d27c3487
    paths./api/v1/tools/video/gif-to-video.post.summary:
      sourceHash: 766d5d54badb
      provenance: machine
      outputHash: ad5a7276731e
    paths./api/v1/tools/video/gif-to-video.post.description:
      sourceHash: 22a13cbfe672
      provenance: machine
      outputHash: 5e83fced2cb1
    paths./api/v1/tools/video/video-to-webp.post.summary:
      sourceHash: 0c4028ecac61
      provenance: machine
      outputHash: 9f9f1ac9ac17
    paths./api/v1/tools/video/video-to-webp.post.description:
      sourceHash: 48fa0306cbe8
      provenance: machine
      outputHash: 07daff790ea1
    paths./api/v1/tools/video/video-to-frames.post.summary:
      sourceHash: 8941a5926866
      provenance: machine
      outputHash: 1b805842d405
    paths./api/v1/tools/video/video-to-frames.post.description:
      sourceHash: 907e19fcf6a2
      provenance: machine
      outputHash: 05d5976d59b1
    paths./api/v1/tools/video/merge-videos.post.summary:
      sourceHash: afbd2e62e1dd
      provenance: machine
      outputHash: 974e755aed07
    paths./api/v1/tools/video/merge-videos.post.description:
      sourceHash: 30e77c05314f
      provenance: machine
      outputHash: 28196f6b2c91
    paths./api/v1/tools/video/replace-audio.post.summary:
      sourceHash: 0cd096774105
      provenance: machine
      outputHash: 997e8b895ddb
    paths./api/v1/tools/video/replace-audio.post.description:
      sourceHash: f9aef92c2ad2
      provenance: machine
      outputHash: d5407ba873d9
    paths./api/v1/tools/video/burn-subtitles.post.summary:
      sourceHash: 53e23068613b
      provenance: machine
      outputHash: 71afbdf91a01
    paths./api/v1/tools/video/burn-subtitles.post.description:
      sourceHash: a501f3253970
      provenance: machine
      outputHash: 68e3a99275ca
    paths./api/v1/tools/video/embed-subtitles.post.summary:
      sourceHash: 55a0c6104e9e
      provenance: machine
      outputHash: 276d1bcef741
    paths./api/v1/tools/video/embed-subtitles.post.description:
      sourceHash: 9ed219a50b61
      provenance: machine
      outputHash: a43922d6135e
    paths./api/v1/tools/video/extract-subtitles.post.summary:
      sourceHash: 0ae3dd9f7012
      provenance: machine
      outputHash: e59af3a251bc
    paths./api/v1/tools/video/extract-subtitles.post.description:
      sourceHash: 4daf70632777
      provenance: machine
      outputHash: d8e86d4d33cc
    paths./api/v1/tools/video/images-to-video.post.summary:
      sourceHash: feb4b6d19902
      provenance: machine
      outputHash: b9b87fc83b00
    paths./api/v1/tools/video/images-to-video.post.description:
      sourceHash: c7ec30bbfec8
      provenance: machine
      outputHash: f6f7660ce90e
    paths./api/v1/tools/video/video-metadata.post.summary:
      sourceHash: f2902a6112ff
      provenance: machine
      outputHash: 4d6936265d69
    paths./api/v1/tools/video/video-metadata.post.description:
      sourceHash: 25b0eb0d1f64
      provenance: machine
      outputHash: 30c9b87870ae
    paths./api/v1/tools/video/auto-subtitles.post.summary:
      sourceHash: 61b5c42d8cd0
      provenance: machine
      outputHash: 8f21fdbca272
    paths./api/v1/tools/video/auto-subtitles.post.description:
      sourceHash: fe628b19b3dc
      provenance: machine
      outputHash: fe68a60b682c
    paths./api/v1/tools/video/extract-audio.post.summary:
      sourceHash: ad7f2c51f215
      provenance: machine
      outputHash: 03ce5972727a
    paths./api/v1/tools/video/extract-audio.post.description:
      sourceHash: 97425b1da99f
      provenance: machine
      outputHash: 279211e541e6
    paths./api/v1/tools/audio/convert-audio.post.summary:
      sourceHash: 295e4a5939e1
      provenance: machine
      outputHash: 627186fbbb6f
    paths./api/v1/tools/audio/convert-audio.post.description:
      sourceHash: db7c32326757
      provenance: machine
      outputHash: 51a679df491b
    paths./api/v1/tools/audio/trim-audio.post.summary:
      sourceHash: 6fdc83d79136
      provenance: machine
      outputHash: 5cb0b46db791
    paths./api/v1/tools/audio/trim-audio.post.description:
      sourceHash: 1150da04e398
      provenance: machine
      outputHash: 8bda1d17d691
    paths./api/v1/tools/audio/volume-adjust.post.summary:
      sourceHash: 04257764f127
      provenance: machine
      outputHash: fd3206cea181
    paths./api/v1/tools/audio/volume-adjust.post.description:
      sourceHash: 18308a469600
      provenance: machine
      outputHash: 5ed57692f6b6
    paths./api/v1/tools/audio/normalize-audio.post.summary:
      sourceHash: f5298327c322
      provenance: machine
      outputHash: e370104287b0
    paths./api/v1/tools/audio/normalize-audio.post.description:
      sourceHash: d9993db45ccb
      provenance: machine
      outputHash: 6b8c6ba0f669
    paths./api/v1/tools/audio/fade-audio.post.summary:
      sourceHash: fc7e035a63d5
      provenance: machine
      outputHash: 0a4cdcf8bb4f
    paths./api/v1/tools/audio/fade-audio.post.description:
      sourceHash: a85ba1a120e5
      provenance: machine
      outputHash: a47ec47d30cf
    paths./api/v1/tools/audio/reverse-audio.post.summary:
      sourceHash: b2eddae3a717
      provenance: machine
      outputHash: 959657dc0ec4
    paths./api/v1/tools/audio/reverse-audio.post.description:
      sourceHash: 218f9b33f5eb
      provenance: machine
      outputHash: c7fde55f5a8b
    paths./api/v1/tools/audio/audio-speed.post.summary:
      sourceHash: aee2a47692bd
      provenance: machine
      outputHash: 98b6fd837d11
    paths./api/v1/tools/audio/audio-speed.post.description:
      sourceHash: 51fe7745adbd
      provenance: machine
      outputHash: dcd059a719d9
    paths./api/v1/tools/audio/pitch-shift.post.summary:
      sourceHash: b26d4482b076
      provenance: machine
      outputHash: c047be70e276
    paths./api/v1/tools/audio/pitch-shift.post.description:
      sourceHash: 04cf48eca03e
      provenance: machine
      outputHash: ea3a6db73cfb
    paths./api/v1/tools/audio/audio-channels.post.summary:
      sourceHash: 409c74651939
      provenance: machine
      outputHash: 003e92ae385c
    paths./api/v1/tools/audio/audio-channels.post.description:
      sourceHash: 0974271a6730
      provenance: machine
      outputHash: 2c2b97ff1db7
    paths./api/v1/tools/audio/silence-removal.post.summary:
      sourceHash: dc27812cd572
      provenance: machine
      outputHash: a95c4cee32a6
    paths./api/v1/tools/audio/silence-removal.post.description:
      sourceHash: 29d4e890a62d
      provenance: machine
      outputHash: e9ac6a188e5f
    paths./api/v1/tools/audio/noise-reduction.post.summary:
      sourceHash: 0bcf30a006cb
      provenance: machine
      outputHash: 151304cc3186
    paths./api/v1/tools/audio/noise-reduction.post.description:
      sourceHash: 032c91b97641
      provenance: machine
      outputHash: c250c5f0aa9b
    paths./api/v1/tools/audio/merge-audio.post.summary:
      sourceHash: 780965910dff
      provenance: machine
      outputHash: 4d8044d863da
    paths./api/v1/tools/audio/merge-audio.post.description:
      sourceHash: 1397edf49056
      provenance: machine
      outputHash: 3f580397e7ef
    paths./api/v1/tools/audio/split-audio.post.summary:
      sourceHash: e59c513c0ba2
      provenance: machine
      outputHash: 1a6db4324dd8
    paths./api/v1/tools/audio/split-audio.post.description:
      sourceHash: 763e0458c032
      provenance: machine
      outputHash: 26a20d3f6bf2
    paths./api/v1/tools/audio/ringtone-maker.post.summary:
      sourceHash: f0e142322ff9
      provenance: machine
      outputHash: 6d56483ad05f
    paths./api/v1/tools/audio/ringtone-maker.post.description:
      sourceHash: c301dcbbedc1
      provenance: machine
      outputHash: 1a90e012dd42
    paths./api/v1/tools/audio/waveform-image.post.summary:
      sourceHash: 0e373ca45197
      provenance: machine
      outputHash: a3fad08e4784
    paths./api/v1/tools/audio/waveform-image.post.description:
      sourceHash: 70f63b279b5d
      provenance: machine
      outputHash: 1d60ddae0a9f
    paths./api/v1/tools/audio/audio-metadata.post.summary:
      sourceHash: 55d1a5a0e65d
      provenance: machine
      outputHash: 570ca8039081
    paths./api/v1/tools/audio/audio-metadata.post.description:
      sourceHash: 5c799135421c
      provenance: machine
      outputHash: f1ce212c5c2d
    paths./api/v1/tools/audio/transcribe-audio.post.summary:
      sourceHash: a723c84c3ab0
      provenance: machine
      outputHash: 006e25fcf94e
    paths./api/v1/tools/audio/transcribe-audio.post.description:
      sourceHash: 2967ad141d14
      provenance: machine
      outputHash: eb36b2f192d9
    paths./api/v1/tools/pdf/merge-pdf.post.summary:
      sourceHash: 43d1539b7596
      provenance: machine
      outputHash: 1b1db201005a
    paths./api/v1/tools/pdf/merge-pdf.post.description:
      sourceHash: 728388cddb8a
      provenance: machine
      outputHash: cb11b2d9673e
    paths./api/v1/tools/pdf/split-pdf.post.summary:
      sourceHash: 4f1b846cb144
      provenance: machine
      outputHash: 3e20cd8ef650
    paths./api/v1/tools/pdf/split-pdf.post.description:
      sourceHash: fbe9d9c35006
      provenance: machine
      outputHash: 588e86566139
    paths./api/v1/tools/pdf/compress-pdf.post.summary:
      sourceHash: 932c3c48a4de
      provenance: machine
      outputHash: 853f0ca30624
    paths./api/v1/tools/pdf/compress-pdf.post.description:
      sourceHash: 89f23fb6b3c8
      provenance: machine
      outputHash: 36543ce1dfd6
    paths./api/v1/tools/pdf/rotate-pdf.post.summary:
      sourceHash: 1eccabbce838
      provenance: machine
      outputHash: 178fab44c49b
    paths./api/v1/tools/pdf/rotate-pdf.post.description:
      sourceHash: 7c561cc7ed48
      provenance: machine
      outputHash: 4bf64b07077e
    paths./api/v1/tools/pdf/extract-pages.post.summary:
      sourceHash: 0247b9f3e6cf
      provenance: machine
      outputHash: 6c346a553f14
    paths./api/v1/tools/pdf/extract-pages.post.description:
      sourceHash: c07eea0c017f
      provenance: machine
      outputHash: 465e12f208c0
    paths./api/v1/tools/pdf/remove-pages.post.summary:
      sourceHash: 670a5909f883
      provenance: machine
      outputHash: 3aa1741e29fb
    paths./api/v1/tools/pdf/remove-pages.post.description:
      sourceHash: 6231b16e7bea
      provenance: machine
      outputHash: 720ff4eb74a8
    paths./api/v1/tools/pdf/organize-pdf.post.summary:
      sourceHash: d5a641ebd93a
      provenance: machine
      outputHash: 9864e8bd6070
    paths./api/v1/tools/pdf/organize-pdf.post.description:
      sourceHash: ac32f48665e7
      provenance: machine
      outputHash: f0f92a41705f
    paths./api/v1/tools/pdf/protect-pdf.post.summary:
      sourceHash: dae6c607e963
      provenance: machine
      outputHash: 8b48de2130f5
    paths./api/v1/tools/pdf/protect-pdf.post.description:
      sourceHash: 600a3be47970
      provenance: machine
      outputHash: a3fd8d04e782
    paths./api/v1/tools/pdf/unlock-pdf.post.summary:
      sourceHash: 893947d82ded
      provenance: machine
      outputHash: 9ccebed87d5e
    paths./api/v1/tools/pdf/unlock-pdf.post.description:
      sourceHash: 1d199055f386
      provenance: machine
      outputHash: cd0eb4011cf6
    paths./api/v1/tools/pdf/repair-pdf.post.summary:
      sourceHash: 43faa052d4cf
      provenance: machine
      outputHash: 14f44d519562
    paths./api/v1/tools/pdf/repair-pdf.post.description:
      sourceHash: 77f71d364df4
      provenance: machine
      outputHash: f39364fddafb
    paths./api/v1/tools/pdf/linearize-pdf.post.summary:
      sourceHash: 75fa522e3fe3
      provenance: machine
      outputHash: 61b071454c56
    paths./api/v1/tools/pdf/linearize-pdf.post.description:
      sourceHash: 200f45aa4a91
      provenance: machine
      outputHash: 908b1010177e
    paths./api/v1/tools/pdf/grayscale-pdf.post.summary:
      sourceHash: 58b0007c3425
      provenance: machine
      outputHash: 52f75a2aec3c
    paths./api/v1/tools/pdf/grayscale-pdf.post.description:
      sourceHash: 7b7e2b4857e6
      provenance: machine
      outputHash: 82de9f7ecc9a
    paths./api/v1/tools/pdf/pdfa-convert.post.summary:
      sourceHash: 9971b16fe624
      provenance: machine
      outputHash: 63d7a0166864
    paths./api/v1/tools/pdf/pdfa-convert.post.description:
      sourceHash: 3c8ad47aa904
      provenance: machine
      outputHash: 6f400d545efd
    paths./api/v1/tools/pdf/crop-pdf.post.summary:
      sourceHash: dc4bdbce825f
      provenance: machine
      outputHash: 7e0559e4e2e1
    paths./api/v1/tools/pdf/crop-pdf.post.description:
      sourceHash: 94eb9575aecc
      provenance: machine
      outputHash: f3481992fd76
    paths./api/v1/tools/pdf/nup-pdf.post.summary:
      sourceHash: dac1b895452a
      provenance: machine
      outputHash: eb0dcf61c7fa
    paths./api/v1/tools/pdf/nup-pdf.post.description:
      sourceHash: 5e2a264dafb0
      provenance: machine
      outputHash: bc9342170e2a
    paths./api/v1/tools/pdf/booklet-pdf.post.summary:
      sourceHash: a1fdff0bc065
      provenance: machine
      outputHash: fab3ba632ba3
    paths./api/v1/tools/pdf/booklet-pdf.post.description:
      sourceHash: 0ce5f88843c3
      provenance: machine
      outputHash: bbffd39b5136
    paths./api/v1/tools/pdf/watermark-pdf.post.summary:
      sourceHash: 903ce98c712a
      provenance: machine
      outputHash: 0a515f288794
    paths./api/v1/tools/pdf/watermark-pdf.post.description:
      sourceHash: c15a57a06624
      provenance: machine
      outputHash: c45b49a11de8
    paths./api/v1/tools/pdf/pdf-page-numbers.post.summary:
      sourceHash: 44eb045097be
      provenance: machine
      outputHash: 61534ba450ae
    paths./api/v1/tools/pdf/pdf-page-numbers.post.description:
      sourceHash: 30ce839cac96
      provenance: machine
      outputHash: cacd07b2d967
    paths./api/v1/tools/files/convert-document.post.summary:
      sourceHash: 51556213858a
      provenance: machine
      outputHash: aac6ceaa376f
    paths./api/v1/tools/files/convert-document.post.description:
      sourceHash: 8a61bf0f3459
      provenance: machine
      outputHash: 063a9836bdbc
    paths./api/v1/tools/files/convert-presentation.post.summary:
      sourceHash: 5e2293e30835
      provenance: machine
      outputHash: b681ac9b965a
    paths./api/v1/tools/files/convert-presentation.post.description:
      sourceHash: c24f1d70d630
      provenance: machine
      outputHash: 37316363a2e5
    paths./api/v1/tools/files/convert-spreadsheet.post.summary:
      sourceHash: 670b7aa81c49
      provenance: machine
      outputHash: 58a378edd595
    paths./api/v1/tools/files/convert-spreadsheet.post.description:
      sourceHash: 1c254c7db8bc
      provenance: machine
      outputHash: 23ea3c14f3c2
    paths./api/v1/tools/files/excel-to-pdf.post.summary:
      sourceHash: edba3432df77
      provenance: machine
      outputHash: d155ecd3f2bd
    paths./api/v1/tools/files/excel-to-pdf.post.description:
      sourceHash: c2e4aaa88cb7
      provenance: machine
      outputHash: 806eb6ed04d7
    paths./api/v1/tools/files/word-to-pdf.post.summary:
      sourceHash: e6cb1150e762
      provenance: machine
      outputHash: ab55bb1cab2e
    paths./api/v1/tools/files/word-to-pdf.post.description:
      sourceHash: eca3c027bc10
      provenance: machine
      outputHash: 45620ea3fa57
    paths./api/v1/tools/files/powerpoint-to-pdf.post.summary:
      sourceHash: 799dd29fe97a
      provenance: machine
      outputHash: f6f2996e7a09
    paths./api/v1/tools/files/powerpoint-to-pdf.post.description:
      sourceHash: cdc18f172040
      provenance: machine
      outputHash: 49fc10245754
    paths./api/v1/tools/files/html-to-pdf.post.summary:
      sourceHash: e03b0e6dda95
      provenance: machine
      outputHash: 85d667e4eef1
    paths./api/v1/tools/files/html-to-pdf.post.description:
      sourceHash: e189b21c7bed
      provenance: machine
      outputHash: 7a440b425faa
    paths./api/v1/tools/files/markdown-to-docx.post.summary:
      sourceHash: 1162393274fa
      provenance: machine
      outputHash: 0d6855cc0221
    paths./api/v1/tools/files/markdown-to-docx.post.description:
      sourceHash: 2bc0fd718613
      provenance: machine
      outputHash: fa3720f36175
    paths./api/v1/tools/files/markdown-to-html.post.summary:
      sourceHash: e5e6e736b649
      provenance: machine
      outputHash: 15d344aa357f
    paths./api/v1/tools/files/markdown-to-html.post.description:
      sourceHash: 4f6f706c371e
      provenance: machine
      outputHash: 15b56d5147f8
    paths./api/v1/tools/files/markdown-to-pdf.post.summary:
      sourceHash: 82712b1fc2b5
      provenance: machine
      outputHash: ecb8b172e817
    paths./api/v1/tools/files/markdown-to-pdf.post.description:
      sourceHash: f588e3d2847d
      provenance: machine
      outputHash: dfb84b931277
    paths./api/v1/tools/files/epub-convert.post.summary:
      sourceHash: dcee1b93b5d5
      provenance: machine
      outputHash: 853278c565f3
    paths./api/v1/tools/files/epub-convert.post.description:
      sourceHash: 5a42072194d3
      provenance: machine
      outputHash: 756449214c69
    paths./api/v1/tools/files/to-epub.post.summary:
      sourceHash: 542d3fb468be
      provenance: machine
      outputHash: f5348a203e21
    paths./api/v1/tools/files/to-epub.post.description:
      sourceHash: 1e0b8c39ef9d
      provenance: machine
      outputHash: 408cae918633
    paths./api/v1/tools/pdf/flatten-pdf.post.summary:
      sourceHash: 86c838d2d497
      provenance: machine
      outputHash: 6b4a83e3c5db
    paths./api/v1/tools/pdf/flatten-pdf.post.description:
      sourceHash: cb0b0bdeaaa5
      provenance: machine
      outputHash: d325e9ba969a
    paths./api/v1/tools/pdf/redact-pdf.post.summary:
      sourceHash: 1437d196213c
      provenance: machine
      outputHash: 27d5c6092e3f
    paths./api/v1/tools/pdf/redact-pdf.post.description:
      sourceHash: c2652c3da04a
      provenance: machine
      outputHash: a4816092f4c3
    paths./api/v1/tools/pdf/pdf-to-text.post.summary:
      sourceHash: f1a719f99a31
      provenance: machine
      outputHash: 6ff41b5609a0
    paths./api/v1/tools/pdf/pdf-to-text.post.description:
      sourceHash: f107a4082454
      provenance: machine
      outputHash: b853ec21c094
    paths./api/v1/tools/pdf/pdf-to-word.post.summary:
      sourceHash: 283553b063ad
      provenance: machine
      outputHash: d278e07ca654
    paths./api/v1/tools/pdf/pdf-to-word.post.description:
      sourceHash: 324b6f3d35f7
      provenance: machine
      outputHash: f4c712a0545d
    paths./api/v1/tools/pdf/pdf-metadata.post.summary:
      sourceHash: 7aa0fbdbeac6
      provenance: machine
      outputHash: 6c56a4125275
    paths./api/v1/tools/pdf/pdf-metadata.post.description:
      sourceHash: 4267623afc19
      provenance: machine
      outputHash: e9cea94aff5e
    paths./api/v1/tools/pdf/ocr-pdf.post.summary:
      sourceHash: 2d9dfe74b2d1
      provenance: machine
      outputHash: 21744922783a
    paths./api/v1/tools/pdf/ocr-pdf.post.description:
      sourceHash: fbed951c39df
      provenance: machine
      outputHash: 571ecafb8978
    paths./api/v1/tools/files/chart-maker.post.summary:
      sourceHash: dbcca76ccbf5
      provenance: machine
      outputHash: d890b18243ed
    paths./api/v1/tools/files/chart-maker.post.description:
      sourceHash: e4adb80dab58
      provenance: machine
      outputHash: f310f218533f
    paths./api/v1/tools/files/csv-excel.post.summary:
      sourceHash: c53386a6d40d
      provenance: machine
      outputHash: 0f0e51bb9963
    paths./api/v1/tools/files/csv-excel.post.description:
      sourceHash: b567a5eb0ee5
      provenance: machine
      outputHash: b1a47e774c72
    paths./api/v1/tools/files/csv-json.post.summary:
      sourceHash: 3ed0542f5ca4
      provenance: machine
      outputHash: 0f1bef88cc54
    paths./api/v1/tools/files/csv-json.post.description:
      sourceHash: 6ed58f7ca5ac
      provenance: machine
      outputHash: 646008d536e4
    paths./api/v1/tools/files/json-xml.post.summary:
      sourceHash: 0e374572749d
      provenance: machine
      outputHash: c3d87d923785
    paths./api/v1/tools/files/json-xml.post.description:
      sourceHash: bbb56d23a274
      provenance: machine
      outputHash: 7b1dc47148a5
    paths./api/v1/tools/files/split-csv.post.summary:
      sourceHash: 06c1cfb6cb2c
      provenance: machine
      outputHash: f02f891809dc
    paths./api/v1/tools/files/split-csv.post.description:
      sourceHash: 1181658ae239
      provenance: machine
      outputHash: db77bf8c1032
    paths./api/v1/tools/files/merge-csvs.post.summary:
      sourceHash: 0042c1153e55
      provenance: machine
      outputHash: a0c905746ac3
    paths./api/v1/tools/files/merge-csvs.post.description:
      sourceHash: 4956c801900a
      provenance: machine
      outputHash: 2586d72e8418
    paths./api/v1/tools/files/yaml-json.post.summary:
      sourceHash: cd3e06c3d51d
      provenance: machine
      outputHash: fbd70b47f6d6
    paths./api/v1/tools/files/yaml-json.post.description:
      sourceHash: a7fa089ac00a
      provenance: machine
      outputHash: c96378da78e0
    paths./api/v1/tools/files/xml-to-csv.post.summary:
      sourceHash: 909195e97a63
      provenance: machine
      outputHash: 50282800e4ff
    paths./api/v1/tools/files/xml-to-csv.post.description:
      sourceHash: 156ea77c8a62
      provenance: machine
      outputHash: 596e71e777c7
    paths./api/v1/tools/files/create-zip.post.summary:
      sourceHash: 8463eb4799ce
      provenance: machine
      outputHash: 1de15fb553f1
    paths./api/v1/tools/files/create-zip.post.description:
      sourceHash: 26c97508b498
      provenance: machine
      outputHash: 7ad8254855d9
    paths./api/v1/tools/files/extract-zip.post.summary:
      sourceHash: aa7dad519142
      provenance: machine
      outputHash: cecd68da0e85
    paths./api/v1/tools/files/extract-zip.post.description:
      sourceHash: 5b025b124d8b
      provenance: machine
      outputHash: f59600417f9f
    paths./api/v1/tools/image/background-replace.post.summary:
      sourceHash: 971cacdad5ce
      provenance: machine
      outputHash: b79fe4781eed
    paths./api/v1/tools/image/background-replace.post.description:
      sourceHash: 5f7a0c0dbbc7
      provenance: machine
      outputHash: 09244bad5c1b
    paths./api/v1/tools/image/blur-background.post.summary:
      sourceHash: 2383b0d9c928
      provenance: machine
      outputHash: 13f38a53cd2e
    paths./api/v1/tools/image/blur-background.post.description:
      sourceHash: f3afcae74767
      provenance: machine
      outputHash: a356f90f8809
    paths./api/v1/tools/image/barcode-generate.post.summary:
      sourceHash: 4717ee594681
      provenance: machine
      outputHash: 92593a0d0d5f
    paths./api/v1/tools/image/barcode-generate.post.description:
      sourceHash: e174f8923d5d
      provenance: machine
      outputHash: 4c12870232c5
    paths./api/v1/tools/image/circle-crop.post.summary:
      sourceHash: d76473270976
      provenance: machine
      outputHash: 3c93509e94e3
    paths./api/v1/tools/image/circle-crop.post.description:
      sourceHash: 3e4d4819d594
      provenance: machine
      outputHash: 6f30429115db
    paths./api/v1/tools/image/duotone.post.summary:
      sourceHash: 27e78e7e8cca
      provenance: machine
      outputHash: a89a2da2dfad
    paths./api/v1/tools/image/duotone.post.description:
      sourceHash: f3d5b592526a
      provenance: machine
      outputHash: 95d11b67e6c9
    paths./api/v1/tools/image/image-pad.post.summary:
      sourceHash: 7cbc7ca5de5b
      provenance: machine
      outputHash: d9b6d4067827
    paths./api/v1/tools/image/image-pad.post.description:
      sourceHash: f222b64a52bc
      provenance: machine
      outputHash: 05ca4d358981
    paths./api/v1/tools/image/pixelate.post.summary:
      sourceHash: f4982f86e732
      provenance: machine
      outputHash: 81ce2bb34dfa
    paths./api/v1/tools/image/pixelate.post.description:
      sourceHash: c47f345410f8
      provenance: machine
      outputHash: 0036a272cc97
    paths./api/v1/tools/image/vignette.post.summary:
      sourceHash: f841c2080740
      provenance: machine
      outputHash: 124afd99725d
    paths./api/v1/tools/image/vignette.post.description:
      sourceHash: 9e1e807e89e0
      provenance: machine
      outputHash: 4337018d3ba5
    paths./api/v1/tools/image/gif-webp.post.summary:
      sourceHash: 337e539d70a4
      provenance: machine
      outputHash: 9ae085591009
    paths./api/v1/tools/image/gif-webp.post.description:
      sourceHash: f397405d0b9c
      provenance: machine
      outputHash: 982dc54bd960
    paths./api/v1/tools/image/histogram.post.summary:
      sourceHash: b32dc489fba8
      provenance: machine
      outputHash: 068d8db23234
    paths./api/v1/tools/image/histogram.post.description:
      sourceHash: f98c4a1d1bf0
      provenance: machine
      outputHash: f61752e7cd6c
    paths./api/v1/tools/image/lqip-placeholder.post.summary:
      sourceHash: 878e3f3518db
      provenance: machine
      outputHash: 3605ac7e27a1
    paths./api/v1/tools/image/lqip-placeholder.post.description:
      sourceHash: 2100f7e08142
      provenance: machine
      outputHash: c3ef8049bc07
    paths./api/v1/tools/image/sprite-sheet.post.summary:
      sourceHash: f1c0772c4599
      provenance: machine
      outputHash: 2ee3441a480a
    paths./api/v1/tools/image/sprite-sheet.post.description:
      sourceHash: 4ef141ec92ed
      provenance: machine
      outputHash: d41840290858
    paths./api/v1/tools/image/passport-photo.post.summary:
      sourceHash: 535e5bf9e5e6
      provenance: machine
      outputHash: a039338519e4
    paths./api/v1/tools/image/passport-photo.post.description:
      sourceHash: 7d3a81dbb05c
      provenance: machine
      outputHash: ce6e7a0699a5
    paths./api/v1/tools/pdf/sign-pdf.post.summary:
      sourceHash: 1bf93e215df9
      provenance: machine
      outputHash: 116526dbbcd7
    paths./api/v1/tools/pdf/sign-pdf.post.description:
      sourceHash: d66e168d79ba
      provenance: machine
      outputHash: 81f9b321583c
    paths./api/v1/tools/image/jpg-to-png.post.summary:
      sourceHash: 2b455c24e59e
      provenance: machine
      outputHash: 210457defc8d
    paths./api/v1/tools/image/jpg-to-png.post.description:
      sourceHash: 1d504faac90f
      provenance: machine
      outputHash: 2a71594e7744
    paths./api/v1/tools/image/png-to-jpg.post.summary:
      sourceHash: 9e2eef671853
      provenance: machine
      outputHash: 7f1d8e35c35e
    paths./api/v1/tools/image/png-to-jpg.post.description:
      sourceHash: 12d6c8d6aa01
      provenance: machine
      outputHash: 923c5fb07ee4
    paths./api/v1/tools/image/jpg-to-webp.post.summary:
      sourceHash: 1e67451d3358
      provenance: machine
      outputHash: 4fe3627e00de
    paths./api/v1/tools/image/jpg-to-webp.post.description:
      sourceHash: 0e7e4b18daa5
      provenance: machine
      outputHash: e885790127b7
    paths./api/v1/tools/image/png-to-webp.post.summary:
      sourceHash: 6c9410143dd5
      provenance: machine
      outputHash: dc56fa1344a6
    paths./api/v1/tools/image/png-to-webp.post.description:
      sourceHash: 7505ddaf944b
      provenance: machine
      outputHash: d3adec296c6e
    paths./api/v1/tools/image/webp-to-jpg.post.summary:
      sourceHash: 670a9097dec8
      provenance: machine
      outputHash: 96032f9f5034
    paths./api/v1/tools/image/webp-to-jpg.post.description:
      sourceHash: 2b7701fe02eb
      provenance: machine
      outputHash: eb712c77358e
    paths./api/v1/tools/image/webp-to-png.post.summary:
      sourceHash: 781a17cb5ec0
      provenance: machine
      outputHash: 583543c2703a
    paths./api/v1/tools/image/webp-to-png.post.description:
      sourceHash: 78645c123b20
      provenance: machine
      outputHash: 8eb2e804285d
    paths./api/v1/tools/image/jpg-to-avif.post.summary:
      sourceHash: 1c50ea7bdfce
      provenance: machine
      outputHash: f1a15ce6bfda
    paths./api/v1/tools/image/jpg-to-avif.post.description:
      sourceHash: fbc8f6ae407c
      provenance: machine
      outputHash: 10d8d0b991ff
    paths./api/v1/tools/image/png-to-avif.post.summary:
      sourceHash: c3ba0668ee1f
      provenance: machine
      outputHash: 54f6df8645b9
    paths./api/v1/tools/image/png-to-avif.post.description:
      sourceHash: d0dcdde42555
      provenance: machine
      outputHash: dc990766a583
    paths./api/v1/tools/image/webp-to-avif.post.summary:
      sourceHash: 690ea37f961e
      provenance: machine
      outputHash: a85c5b1d92ab
    paths./api/v1/tools/image/webp-to-avif.post.description:
      sourceHash: bcae3fc230b8
      provenance: machine
      outputHash: 8493784e96d7
    paths./api/v1/tools/image/heic-to-jpg.post.summary:
      sourceHash: 0013ad2f7e97
      provenance: machine
      outputHash: 2b06f5bd867c
    paths./api/v1/tools/image/heic-to-jpg.post.description:
      sourceHash: deb0641a6bfd
      provenance: machine
      outputHash: 020b9f1fa7f7
    paths./api/v1/tools/image/heic-to-png.post.summary:
      sourceHash: a191f1708025
      provenance: machine
      outputHash: 29026550e9eb
    paths./api/v1/tools/image/heic-to-png.post.description:
      sourceHash: 583345b636d6
      provenance: machine
      outputHash: c8849f84b8ed
    paths./api/v1/tools/image/heic-to-avif.post.summary:
      sourceHash: a03031e5c80d
      provenance: machine
      outputHash: ef2f12ef6cf5
    paths./api/v1/tools/image/heic-to-avif.post.description:
      sourceHash: 6812ebf99c0b
      provenance: machine
      outputHash: 78a4e0a8fa18
    paths./api/v1/tools/image/jpg-to-gif.post.summary:
      sourceHash: 46b3bcd3a38f
      provenance: machine
      outputHash: 616b583293dc
    paths./api/v1/tools/image/jpg-to-gif.post.description:
      sourceHash: 365097fb68a9
      provenance: machine
      outputHash: 2024ae3d83dc
    paths./api/v1/tools/image/png-to-gif.post.summary:
      sourceHash: e4de0e11bf3d
      provenance: machine
      outputHash: 5a930f750597
    paths./api/v1/tools/image/png-to-gif.post.description:
      sourceHash: 89620988c5bd
      provenance: machine
      outputHash: 89fd317c404c
    paths./api/v1/tools/image/gif-to-jpg.post.summary:
      sourceHash: 016ce94121b8
      provenance: machine
      outputHash: 890808c4d2f9
    paths./api/v1/tools/image/gif-to-jpg.post.description:
      sourceHash: d3c99d325441
      provenance: machine
      outputHash: 9bb0a68ad238
    paths./api/v1/tools/image/gif-to-png.post.summary:
      sourceHash: 8f3239971851
      provenance: machine
      outputHash: 22068fb7d0f6
    paths./api/v1/tools/image/gif-to-png.post.description:
      sourceHash: 6b363c6c5b6a
      provenance: machine
      outputHash: 026d70fef7b5
    paths./api/v1/tools/image/webp-to-gif.post.summary:
      sourceHash: db331b95e19b
      provenance: machine
      outputHash: e158f8accbb4
    paths./api/v1/tools/image/webp-to-gif.post.description:
      sourceHash: 9295d8ac8c09
      provenance: machine
      outputHash: 2a5581c32d78
    paths./api/v1/tools/image/jpg-to-tiff.post.summary:
      sourceHash: 86ae950de6a3
      provenance: machine
      outputHash: 49e7b3943b77
    paths./api/v1/tools/image/jpg-to-tiff.post.description:
      sourceHash: 4f0ac37f2e06
      provenance: machine
      outputHash: e129f4b72809
    paths./api/v1/tools/image/png-to-tiff.post.summary:
      sourceHash: 3fa124cbaa4a
      provenance: machine
      outputHash: 32050f69e0d9
    paths./api/v1/tools/image/png-to-tiff.post.description:
      sourceHash: 5cf97e597ee8
      provenance: machine
      outputHash: bd7a2ae6703a
    paths./api/v1/tools/image/tiff-to-jpg.post.summary:
      sourceHash: c803f62926b8
      provenance: machine
      outputHash: ea399a8c7977
    paths./api/v1/tools/image/tiff-to-jpg.post.description:
      sourceHash: cf928b0513e5
      provenance: machine
      outputHash: b4dfca341841
    paths./api/v1/tools/image/tiff-to-png.post.summary:
      sourceHash: 73c313976bb4
      provenance: machine
      outputHash: 1d57cbb4f57d
    paths./api/v1/tools/image/tiff-to-png.post.description:
      sourceHash: 52f065d2f324
      provenance: machine
      outputHash: 0703c9bfda6f
    paths./api/v1/tools/image/psd-to-jpg.post.summary:
      sourceHash: b9d50a4fd5a9
      provenance: machine
      outputHash: 9044a05488af
    paths./api/v1/tools/image/psd-to-jpg.post.description:
      sourceHash: a9897ebb6e8c
      provenance: machine
      outputHash: cfb274af3572
    paths./api/v1/tools/image/psd-to-png.post.summary:
      sourceHash: af36a6f53221
      provenance: machine
      outputHash: 19c57f98e004
    paths./api/v1/tools/image/psd-to-png.post.description:
      sourceHash: b15171b20160
      provenance: machine
      outputHash: 1110d88c2983
    paths./api/v1/tools/image/png-to-eps.post.summary:
      sourceHash: 33ce1b37d63f
      provenance: machine
      outputHash: 0dd4eb794189
    paths./api/v1/tools/image/png-to-eps.post.description:
      sourceHash: 31d8bfe76c0d
      provenance: machine
      outputHash: 9e67d0f694a4
    paths./api/v1/tools/image/jpg-to-eps.post.summary:
      sourceHash: e8bde3997aaf
      provenance: machine
      outputHash: c180c91518ef
    paths./api/v1/tools/image/jpg-to-eps.post.description:
      sourceHash: 62cce0cb73ba
      provenance: machine
      outputHash: 1912b40b179b
    paths./api/v1/tools/image/eps-to-png.post.summary:
      sourceHash: be7ed58f6b7e
      provenance: machine
      outputHash: 16b4a5e7c1d2
    paths./api/v1/tools/image/eps-to-png.post.description:
      sourceHash: e9157e4e334b
      provenance: machine
      outputHash: d7d372e3bb37
    paths./api/v1/tools/image/eps-to-jpg.post.summary:
      sourceHash: c05512db4202
      provenance: machine
      outputHash: 45a664741381
    paths./api/v1/tools/image/eps-to-jpg.post.description:
      sourceHash: b582104358cd
      provenance: machine
      outputHash: '867438458652'
    paths./api/v1/tools/image/png-to-svg.post.summary:
      sourceHash: 651353e8e22c
      provenance: machine
      outputHash: c99c2bf95521
    paths./api/v1/tools/image/png-to-svg.post.description:
      sourceHash: 5cbb3b81f383
      provenance: machine
      outputHash: cd14b21599a7
    paths./api/v1/tools/image/jpg-to-svg.post.summary:
      sourceHash: 46f10c1c31b4
      provenance: machine
      outputHash: c0a3d1fb1f00
    paths./api/v1/tools/image/jpg-to-svg.post.description:
      sourceHash: ea475d6542cb
      provenance: machine
      outputHash: 65a678933c29
    paths./api/v1/tools/image/tiff-to-svg.post.summary:
      sourceHash: 52f9db13bb53
      provenance: machine
      outputHash: e80bceed5aa1
    paths./api/v1/tools/image/tiff-to-svg.post.description:
      sourceHash: 26ac4eed4312
      provenance: machine
      outputHash: c3bc33dedcf7
    paths./api/v1/tools/image/psd-to-svg.post.summary:
      sourceHash: 654d378a3882
      provenance: machine
      outputHash: 70c570bc8bf3
    paths./api/v1/tools/image/psd-to-svg.post.description:
      sourceHash: 990b5ce49f61
      provenance: machine
      outputHash: 6a083ded575b
    paths./api/v1/tools/image/eps-to-svg.post.summary:
      sourceHash: 36137967b026
      provenance: machine
      outputHash: f2b5976adb2d
    paths./api/v1/tools/image/eps-to-svg.post.description:
      sourceHash: 8c2914626a16
      provenance: machine
      outputHash: 2c79e231f8be
    paths./api/v1/tools/image/svg-to-png.post.summary:
      sourceHash: 1f1eb20e97fe
      provenance: machine
      outputHash: 24777608135a
    paths./api/v1/tools/image/svg-to-png.post.description:
      sourceHash: 5338d3a81ff7
      provenance: machine
      outputHash: f609968b8151
    paths./api/v1/tools/image/svg-to-jpg.post.summary:
      sourceHash: 8e51faf94ff1
      provenance: machine
      outputHash: 3eae09f81fcd
    paths./api/v1/tools/image/svg-to-jpg.post.description:
      sourceHash: 23241e664642
      provenance: machine
      outputHash: 15c76a3ca8d0
    paths./api/v1/tools/image/jpg-to-pdf.post.summary:
      sourceHash: 480104dd2622
      provenance: machine
      outputHash: e9dc4d686c44
    paths./api/v1/tools/image/jpg-to-pdf.post.description:
      sourceHash: daffc3441cf6
      provenance: machine
      outputHash: 38a63432d158
    paths./api/v1/tools/image/png-to-pdf.post.summary:
      sourceHash: 7cab3612203f
      provenance: machine
      outputHash: c7beacb3c5be
    paths./api/v1/tools/image/png-to-pdf.post.description:
      sourceHash: 135646c47b37
      provenance: machine
      outputHash: '447991564597'
    paths./api/v1/tools/image/heic-to-pdf.post.summary:
      sourceHash: d9a32701511d
      provenance: machine
      outputHash: 5676c34d1b94
    paths./api/v1/tools/image/heic-to-pdf.post.description:
      sourceHash: cb1a4bc6ad48
      provenance: machine
      outputHash: 59574f53d845
    paths./api/v1/tools/image/tiff-to-pdf.post.summary:
      sourceHash: 76458bef5bb7
      provenance: machine
      outputHash: fba9ee4f6e50
    paths./api/v1/tools/image/tiff-to-pdf.post.description:
      sourceHash: f26e932da894
      provenance: machine
      outputHash: 3f9031a58fc0
    paths./api/v1/tools/image/webp-to-pdf.post.summary:
      sourceHash: fdbcd78acb8e
      provenance: machine
      outputHash: 2545bc95421c
    paths./api/v1/tools/image/webp-to-pdf.post.description:
      sourceHash: 02d3842d170f
      provenance: machine
      outputHash: a6375db86444
    paths./api/v1/tools/image/gif-to-pdf.post.summary:
      sourceHash: 62a5f26018fc
      provenance: machine
      outputHash: e9cf10e923fb
    paths./api/v1/tools/image/gif-to-pdf.post.description:
      sourceHash: e1d4ce05a711
      provenance: machine
      outputHash: 251de1e145c1
    paths./api/v1/tools/image/eps-to-pdf.post.summary:
      sourceHash: b183686e176a
      provenance: machine
      outputHash: 11a1ec6f5af2
    paths./api/v1/tools/image/eps-to-pdf.post.description:
      sourceHash: 797845fc1a13
      provenance: machine
      outputHash: 3d6cc470e858
    paths./api/v1/tools/pdf/pdf-to-jpg.post.summary:
      sourceHash: f84d053c3344
      provenance: machine
      outputHash: 9856755e7599
    paths./api/v1/tools/pdf/pdf-to-jpg.post.description:
      sourceHash: 294e7071a0cd
      provenance: machine
      outputHash: 2034069a161c
    paths./api/v1/tools/pdf/pdf-to-png.post.summary:
      sourceHash: 18362545fcb8
      provenance: machine
      outputHash: e450d0b9ccae
    paths./api/v1/tools/pdf/pdf-to-png.post.description:
      sourceHash: 879e2d2c287b
      provenance: machine
      outputHash: 7505b2e1a5cd
    paths./api/v1/tools/pdf/pdf-to-tiff.post.summary:
      sourceHash: cd0065e23473
      provenance: machine
      outputHash: 738a26b0bbf7
    paths./api/v1/tools/pdf/pdf-to-tiff.post.description:
      sourceHash: 4e46452ab02e
      provenance: machine
      outputHash: 40d889ab215b
    paths./api/v1/tools/video/mov-to-mp4.post.summary:
      sourceHash: b88833009ff9
      provenance: machine
      outputHash: 3425108098a0
    paths./api/v1/tools/video/mov-to-mp4.post.description:
      sourceHash: a248339c26d1
      provenance: machine
      outputHash: 6a518813bd77
    paths./api/v1/tools/video/webm-to-mp4.post.summary:
      sourceHash: 66e6dae5210e
      provenance: machine
      outputHash: 4c85ae136e86
    paths./api/v1/tools/video/webm-to-mp4.post.description:
      sourceHash: a9977ca09125
      provenance: machine
      outputHash: af11cf21b6ad
    paths./api/v1/tools/video/mkv-to-mp4.post.summary:
      sourceHash: cffe53cd9489
      provenance: machine
      outputHash: 55dc1145a894
    paths./api/v1/tools/video/mkv-to-mp4.post.description:
      sourceHash: d4c9bfc05b71
      provenance: machine
      outputHash: 4ed260f978ef
    paths./api/v1/tools/video/avi-to-mp4.post.summary:
      sourceHash: a5ed5b5782d3
      provenance: machine
      outputHash: 4c8d261677cb
    paths./api/v1/tools/video/avi-to-mp4.post.description:
      sourceHash: 59f0c2a1860b
      provenance: machine
      outputHash: 2c7024cc0969
    paths./api/v1/tools/video/mp4-to-mov.post.summary:
      sourceHash: a20af2c4b1fb
      provenance: machine
      outputHash: 9f725236462d
    paths./api/v1/tools/video/mp4-to-mov.post.description:
      sourceHash: 8d99e8459b46
      provenance: machine
      outputHash: 8a3059b53694
    paths./api/v1/tools/video/mp4-to-webm.post.summary:
      sourceHash: a1b67df8d74e
      provenance: machine
      outputHash: d01eb9c15b25
    paths./api/v1/tools/video/mp4-to-webm.post.description:
      sourceHash: 2d0fec602c82
      provenance: machine
      outputHash: 19e16215c1a9
    paths./api/v1/tools/video/webm-to-mov.post.summary:
      sourceHash: 60df7d3b385b
      provenance: machine
      outputHash: f260d6b12737
    paths./api/v1/tools/video/webm-to-mov.post.description:
      sourceHash: 6982fc1cb933
      provenance: machine
      outputHash: 278d7e0a4696
    paths./api/v1/tools/video/mkv-to-mov.post.summary:
      sourceHash: dc04291270e6
      provenance: machine
      outputHash: 9e8e5d7e920a
    paths./api/v1/tools/video/mkv-to-mov.post.description:
      sourceHash: 71c9c1b99374
      provenance: machine
      outputHash: 921c73d1ef58
    paths./api/v1/tools/video/avi-to-mov.post.summary:
      sourceHash: 449c58ca9486
      provenance: machine
      outputHash: 5f35a3e90dc0
    paths./api/v1/tools/video/avi-to-mov.post.description:
      sourceHash: 62b1fd2e8e0c
      provenance: machine
      outputHash: bf39badf4425
    paths./api/v1/tools/video/mp4-to-avi.post.summary:
      sourceHash: 700763fbdb4a
      provenance: machine
      outputHash: 25d1a1779241
    paths./api/v1/tools/video/mp4-to-avi.post.description:
      sourceHash: da2c6f405f96
      provenance: machine
      outputHash: 48732ce2884e
    paths./api/v1/tools/video/mov-to-avi.post.summary:
      sourceHash: 6cc35a24d9fa
      provenance: machine
      outputHash: 28630b7250c9
    paths./api/v1/tools/video/mov-to-avi.post.description:
      sourceHash: e1dd8989a812
      provenance: machine
      outputHash: 03823118d401
    paths./api/v1/tools/video/mkv-to-avi.post.summary:
      sourceHash: 993ee5fab472
      provenance: machine
      outputHash: 65fa46622e16
    paths./api/v1/tools/video/mkv-to-avi.post.description:
      sourceHash: f1b9b4ced057
      provenance: machine
      outputHash: dc4a7a65d4b8
    paths./api/v1/tools/video/avi-to-mkv.post.summary:
      sourceHash: 3393bba23656
      provenance: machine
      outputHash: 4b6fd3963367
    paths./api/v1/tools/video/avi-to-mkv.post.description:
      sourceHash: ce821c05956b
      provenance: machine
      outputHash: 0e3172c76b7e
    paths./api/v1/tools/video/mp4-to-gif.post.summary:
      sourceHash: 62a7b6e94a53
      provenance: machine
      outputHash: 5578934fdaf3
    paths./api/v1/tools/video/mp4-to-gif.post.description:
      sourceHash: eb95a94fc0e0
      provenance: machine
      outputHash: 8ce28f8e73d9
    paths./api/v1/tools/video/mov-to-gif.post.summary:
      sourceHash: c0e8c3dcfdfd
      provenance: machine
      outputHash: 7f921260b207
    paths./api/v1/tools/video/mov-to-gif.post.description:
      sourceHash: f0f7c24adb4f
      provenance: machine
      outputHash: ce7067dcfe6a
    paths./api/v1/tools/video/mkv-to-gif.post.summary:
      sourceHash: c19834912c0e
      provenance: machine
      outputHash: 00a156ecafdb
    paths./api/v1/tools/video/mkv-to-gif.post.description:
      sourceHash: 591062eb518f
      provenance: machine
      outputHash: 2e44a1a698de
    paths./api/v1/tools/video/avi-to-gif.post.summary:
      sourceHash: b6286a84b91d
      provenance: machine
      outputHash: 4daa06bb0415
    paths./api/v1/tools/video/avi-to-gif.post.description:
      sourceHash: dd44bdc0686d
      provenance: machine
      outputHash: f20e208b0604
    paths./api/v1/tools/video/gif-to-mp4.post.summary:
      sourceHash: 9c2d0df2d1e9
      provenance: machine
      outputHash: c65d85703b7b
    paths./api/v1/tools/video/gif-to-mp4.post.description:
      sourceHash: 96914944fdc5
      provenance: machine
      outputHash: 896b5983b6bc
    paths./api/v1/tools/video/gif-to-webm.post.summary:
      sourceHash: f2b694034e10
      provenance: machine
      outputHash: 38fe27150d3d
    paths./api/v1/tools/video/gif-to-webm.post.description:
      sourceHash: 221c2fee1775
      provenance: machine
      outputHash: 2ff38c5ec7f8
    paths./api/v1/tools/video/gif-to-mov.post.summary:
      sourceHash: 45c5f35f45f3
      provenance: machine
      outputHash: 5aa6cf239ce2
    paths./api/v1/tools/video/gif-to-mov.post.description:
      sourceHash: 57996ae8e8cb
      provenance: machine
      outputHash: 36fac11023db
    paths./api/v1/tools/video/mp4-to-mp3.post.summary:
      sourceHash: 69e107b048a8
      provenance: machine
      outputHash: 477296650a9b
    paths./api/v1/tools/video/mp4-to-mp3.post.description:
      sourceHash: 40de83aa6ed2
      provenance: machine
      outputHash: 336f4c647e2a
    paths./api/v1/tools/video/mov-to-mp3.post.summary:
      sourceHash: e149b039deaf
      provenance: machine
      outputHash: 1d3ee6e9ad97
    paths./api/v1/tools/video/mov-to-mp3.post.description:
      sourceHash: 27f1a1f36a88
      provenance: machine
      outputHash: f61d1ec2ec67
    paths./api/v1/tools/video/mkv-to-mp3.post.summary:
      sourceHash: a017e66660ad
      provenance: machine
      outputHash: 19a43e1e975c
    paths./api/v1/tools/video/mkv-to-mp3.post.description:
      sourceHash: 12cb145afcd0
      provenance: machine
      outputHash: ff1a5d9b1eac
    paths./api/v1/tools/video/webm-to-mp3.post.summary:
      sourceHash: 9fbcc26dd542
      provenance: machine
      outputHash: 66725761a3a5
    paths./api/v1/tools/video/webm-to-mp3.post.description:
      sourceHash: 5213b45f3c7d
      provenance: machine
      outputHash: 4dbaf2b87183
    paths./api/v1/tools/video/avi-to-mp3.post.summary:
      sourceHash: 5f4d7167967b
      provenance: machine
      outputHash: 33c9934c6374
    paths./api/v1/tools/video/avi-to-mp3.post.description:
      sourceHash: dea6a15cea91
      provenance: machine
      outputHash: d2edd66ba2e2
    paths./api/v1/tools/video/mp4-to-wav.post.summary:
      sourceHash: b6769af66349
      provenance: machine
      outputHash: 8a3760c9c92d
    paths./api/v1/tools/video/mp4-to-wav.post.description:
      sourceHash: 5b9126a6bb99
      provenance: machine
      outputHash: 706210b41bc9
    paths./api/v1/tools/video/mov-to-wav.post.summary:
      sourceHash: 4194226fdf68
      provenance: machine
      outputHash: 860fd76762f1
    paths./api/v1/tools/video/mov-to-wav.post.description:
      sourceHash: fe4925d7babd
      provenance: machine
      outputHash: caf0ff33d2b7
    paths./api/v1/tools/video/mp4-to-ogg.post.summary:
      sourceHash: dcb33be4e615
      provenance: machine
      outputHash: 6ac68b730647
    paths./api/v1/tools/video/mp4-to-ogg.post.description:
      sourceHash: d8be42b38707
      provenance: machine
      outputHash: c154d89f1927
    paths./api/v1/tools/audio/m4a-to-mp3.post.summary:
      sourceHash: e4f86f589bd4
      provenance: machine
      outputHash: 29e51760bcfc
    paths./api/v1/tools/audio/m4a-to-mp3.post.description:
      sourceHash: ad144ccdc1d4
      provenance: machine
      outputHash: 8810df7ce2b4
    paths./api/v1/tools/audio/m4a-to-wav.post.summary:
      sourceHash: 5e8aa1c62150
      provenance: machine
      outputHash: e7ef66288225
    paths./api/v1/tools/audio/m4a-to-wav.post.description:
      sourceHash: dab8ed1c0668
      provenance: machine
      outputHash: 97a793250c05
    paths./api/v1/tools/audio/aac-to-mp3.post.summary:
      sourceHash: dd7d2c46e335
      provenance: machine
      outputHash: 4d4ec1fd6c8d
    paths./api/v1/tools/audio/aac-to-mp3.post.description:
      sourceHash: 4187ef8fb789
      provenance: machine
      outputHash: d1fac1bd4bb3
    paths./api/v1/tools/audio/aac-to-wav.post.summary:
      sourceHash: 6485f4d30fd0
      provenance: machine
      outputHash: 7d7e56151365
    paths./api/v1/tools/audio/aac-to-wav.post.description:
      sourceHash: c2221db94cf4
      provenance: machine
      outputHash: 51b7de7e7248
    paths./api/v1/tools/audio/aac-to-flac.post.summary:
      sourceHash: 9dc0a9886938
      provenance: machine
      outputHash: 857207a85f8e
    paths./api/v1/tools/audio/aac-to-flac.post.description:
      sourceHash: 945f63a2be18
      provenance: machine
      outputHash: 919a874c972b
    paths./api/v1/tools/audio/ogg-to-mp3.post.summary:
      sourceHash: c3cff86b4282
      provenance: machine
      outputHash: c854a44a70d4
    paths./api/v1/tools/audio/ogg-to-mp3.post.description:
      sourceHash: 8889a760d19f
      provenance: machine
      outputHash: 289c3ffcd19b
    paths./api/v1/tools/audio/ogg-to-wav.post.summary:
      sourceHash: 85fbfd8ee338
      provenance: machine
      outputHash: dc4755cfa006
    paths./api/v1/tools/audio/ogg-to-wav.post.description:
      sourceHash: 99156f0db542
      provenance: machine
      outputHash: 1840f3e49841
    paths./api/v1/tools/audio/wav-to-mp3.post.summary:
      sourceHash: d8344c7d753c
      provenance: machine
      outputHash: c8c7c02738ef
    paths./api/v1/tools/audio/wav-to-mp3.post.description:
      sourceHash: 7f83d006baf8
      provenance: machine
      outputHash: 2738b69b6b50
    paths./api/v1/tools/audio/mp3-to-wav.post.summary:
      sourceHash: a7de59ab62b9
      provenance: machine
      outputHash: 6237ec35854f
    paths./api/v1/tools/audio/mp3-to-wav.post.description:
      sourceHash: c19d5fba1478
      provenance: machine
      outputHash: 55db846a7104
    paths./api/v1/tools/audio/flac-to-mp3.post.summary:
      sourceHash: da353684dd2b
      provenance: machine
      outputHash: 3f15abb9f08d
    paths./api/v1/tools/audio/flac-to-mp3.post.description:
      sourceHash: e83797072608
      provenance: machine
      outputHash: be024945c45a
    paths./api/v1/tools/files/excel-to-csv.post.summary:
      sourceHash: 8610a88e662a
      provenance: machine
      outputHash: e04f2c15fba9
    paths./api/v1/tools/files/excel-to-csv.post.description:
      sourceHash: c5b6027afaa5
      provenance: machine
      outputHash: 379508e6f129
    paths./api/v1/readyz.get.summary:
      sourceHash: 2f70aef72373
      provenance: machine
      outputHash: ca542c4bdcdf
    paths./api/v1/readyz.get.description:
      sourceHash: 1600aad09ab9
      provenance: machine
      outputHash: 76b9c8c69432
    paths./api/v1/jobs/{jobId}/cancel.post.summary:
      sourceHash: a5032d26d61d
      provenance: machine
      outputHash: 4861894d69b9
    paths./api/v1/jobs/{jobId}/cancel.post.description:
      sourceHash: c828ad6eed72
      provenance: machine
      outputHash: e7a8d7849f22
    paths./api/v1/preferences.get.summary:
      sourceHash: 14eccfecf549
      provenance: machine
      outputHash: 8ad875aa45ab
    paths./api/v1/preferences.get.description:
      sourceHash: be05d444bc85
      provenance: machine
      outputHash: d761088919f0
    paths./api/v1/preferences.put.summary:
      sourceHash: 2649ca8f6a25
      provenance: machine
      outputHash: 340ab15fb395
    paths./api/v1/preferences.put.description:
      sourceHash: ac325d43a780
      provenance: machine
      outputHash: 78079a160ec3
    paths./api/auth/mfa/enroll.post.summary:
      sourceHash: c138aec8b79b
      provenance: machine
      outputHash: 4a80ca00c84c
    paths./api/auth/mfa/enroll.post.description:
      sourceHash: e00560362017
      provenance: machine
      outputHash: a6d570f78d61
    paths./api/auth/mfa/verify.post.summary:
      sourceHash: 12b670ebf850
      provenance: machine
      outputHash: 3517c7baeb5c
    paths./api/auth/mfa/verify.post.description:
      sourceHash: ae628486beb5
      provenance: machine
      outputHash: 9324906572fe
    paths./api/auth/mfa/complete.post.summary:
      sourceHash: 16f65adb8582
      provenance: machine
      outputHash: f7ae4d591c33
    paths./api/auth/mfa/complete.post.description:
      sourceHash: 3dc67142d5da
      provenance: machine
      outputHash: 8547957e7a49
    paths./api/auth/mfa/disable.post.summary:
      sourceHash: 8adb8274a7c6
      provenance: machine
      outputHash: 1d6ca17bbca7
    paths./api/auth/mfa/disable.post.description:
      sourceHash: 7980f5f0d9f0
      provenance: machine
      outputHash: 49ad848b0c97
    paths./api/auth/users/{id}/mfa/reset.post.summary:
      sourceHash: c765204eb72c
      provenance: machine
      outputHash: 9361a8782a36
    paths./api/auth/oidc/login.get.summary:
      sourceHash: bac046b3ef24
      provenance: machine
      outputHash: 968b6ce0c791
    paths./api/auth/oidc/login.get.description:
      sourceHash: e48289d1e534
      provenance: machine
      outputHash: 859dc121a484
    paths./api/auth/oidc/callback.get.summary:
      sourceHash: 396eda750d3c
      provenance: machine
      outputHash: 4196ee1feaf6
    paths./api/auth/oidc/callback.get.description:
      sourceHash: e9437a06c530
      provenance: machine
      outputHash: b49eb3d70dca
    paths./api/auth/saml/metadata.get.summary:
      sourceHash: d6b4c80ec1e4
      provenance: machine
      outputHash: 4348accce10a
    paths./api/auth/saml/metadata.get.description:
      sourceHash: 0b08445f03af
      provenance: machine
      outputHash: 75e0996bd5aa
    paths./api/auth/saml/login.get.summary:
      sourceHash: 6de21463c7cb
      provenance: machine
      outputHash: c7a0aad6d93b
    paths./api/auth/saml/login.get.description:
      sourceHash: eca8bcf506ea
      provenance: machine
      outputHash: 3e8caaa66997
    paths./api/auth/saml/callback.post.summary:
      sourceHash: 2a95ad454bd5
      provenance: machine
      outputHash: bd7f756674fa
    paths./api/auth/saml/callback.post.description:
      sourceHash: a03dcebde897
      provenance: machine
      outputHash: 5faf8392d37e
    paths./api/v1/files/{id}/preview.get.summary:
      sourceHash: 71d50aa39313
      provenance: machine
      outputHash: 2a43e9744b77
    paths./api/v1/files/{id}/preview.get.description:
      sourceHash: fb3edf8df6cf
      provenance: machine
      outputHash: 1cf884b3cda0
    paths./api/v1/preview/generate.post.summary:
      sourceHash: cff28fa8e380
      provenance: machine
      outputHash: c7bedeef388e
    paths./api/v1/preview/generate.post.description:
      sourceHash: 56d484d0c85b
      provenance: machine
      outputHash: 997d1a34e7bc
    paths./api/v1/admin/log-level.get.summary:
      sourceHash: f9fa3ff842cd
      provenance: machine
      outputHash: 162909b19691
    paths./api/v1/admin/log-level.get.description:
      sourceHash: 277cddb1b284
      provenance: machine
      outputHash: fedaf2c07c7d
    paths./api/v1/admin/log-level.post.summary:
      sourceHash: e9ac46f76c49
      provenance: machine
      outputHash: e72bdd6d705f
    paths./api/v1/admin/log-level.post.description:
      sourceHash: 5f12391622b9
      provenance: machine
      outputHash: 8bd428168259
    paths./api/v1/metrics.get.summary:
      sourceHash: 05be9e156194
      provenance: machine
      outputHash: 824947cd2246
    paths./api/v1/metrics.get.description:
      sourceHash: e5723b8ba79e
      provenance: machine
      outputHash: c306110b1159
    paths./api/v1/admin/support-bundle.get.summary:
      sourceHash: 3fc31382eaf7
      provenance: machine
      outputHash: a749d2637246
    paths./api/v1/admin/support-bundle.get.description:
      sourceHash: c04e47b20915
      provenance: machine
      outputHash: ba4bc6783966
    paths./api/v1/admin/usage.get.summary:
      sourceHash: 2f5017840735
      provenance: machine
      outputHash: a257f7eb2e63
    paths./api/v1/admin/usage.get.description:
      sourceHash: 5ba73241349d
      provenance: machine
      outputHash: 82f049c112d8
    paths./api/v1/admin/backup-status.get.summary:
      sourceHash: cea8a0d690fc
      provenance: machine
      outputHash: 5f60ba5dc01b
    paths./api/v1/admin/backup-status.get.description:
      sourceHash: e42e4494117a
      provenance: machine
      outputHash: 6ded183e1e1e
    paths./api/v1/admin/backup-status.post.summary:
      sourceHash: 1a7ed3984596
      provenance: machine
      outputHash: f5742c36b05d
    paths./api/v1/admin/backup-status.post.description:
      sourceHash: 84b506533dbf
      provenance: machine
      outputHash: 09a326759259
    paths./api/v1/admin/features/import.post.summary:
      sourceHash: 31b34aebf83b
      provenance: machine
      outputHash: 03bc5562cf0a
    paths./api/v1/admin/features/import.post.description:
      sourceHash: 229fcb03f525
      provenance: machine
      outputHash: fa44d5250933
    paths./api/v1/enterprise/scim/token.post.summary:
      sourceHash: e550c4aaeecf
      provenance: machine
      outputHash: 9ca8595144b6
    paths./api/v1/enterprise/scim/token.delete.summary:
      sourceHash: e57912d50189
      provenance: machine
      outputHash: 17e1baf1c0db
    paths./api/v1/scim/v2/ServiceProviderConfig.get.summary:
      sourceHash: 6d9bb054fb7b
      provenance: machine
      outputHash: bb5b9c598907
    paths./api/v1/scim/v2/ServiceProviderConfig.get.description:
      sourceHash: a111048aed19
      provenance: machine
      outputHash: 9bad8cbdc31e
    paths./api/v1/scim/v2/Schemas.get.summary:
      sourceHash: c04171c8ce40
      provenance: machine
      outputHash: 2caf98c5911e
    paths./api/v1/scim/v2/Schemas.get.description:
      sourceHash: 74cf47ef6038
      provenance: machine
      outputHash: 944f6b5f1f8b
    paths./api/v1/scim/v2/ResourceTypes.get.summary:
      sourceHash: b3f61558b888
      provenance: machine
      outputHash: bd817fc6c51b
    paths./api/v1/scim/v2/ResourceTypes.get.description:
      sourceHash: d3935183ed14
      provenance: machine
      outputHash: 94630f4c98cf
    paths./api/v1/scim/v2/Users.get.summary:
      sourceHash: 06aa223783d0
      provenance: machine
      outputHash: e357bb4742d6
    paths./api/v1/scim/v2/Users.get.description:
      sourceHash: dccfb3116fb5
      provenance: machine
      outputHash: cd0e7559e85b
    paths./api/v1/scim/v2/Users.post.summary:
      sourceHash: ba4ce4467898
      provenance: machine
      outputHash: 22cf3cf1022a
    paths./api/v1/scim/v2/Users.post.description:
      sourceHash: ab9d5413dfe0
      provenance: machine
      outputHash: f7e0462b24fd
    paths./api/v1/scim/v2/Users/{id}.get.summary:
      sourceHash: 06a26f48a1d7
      provenance: machine
      outputHash: 5c350b15bd12
    paths./api/v1/scim/v2/Users/{id}.get.description:
      sourceHash: f643db8015e1
      provenance: machine
      outputHash: 4cc1e54c2fe4
    paths./api/v1/scim/v2/Users/{id}.put.summary:
      sourceHash: acfe35ddd973
      provenance: machine
      outputHash: d75bb7409bb0
    paths./api/v1/scim/v2/Users/{id}.put.description:
      sourceHash: 7051119f905f
      provenance: machine
      outputHash: 605622efd316
    paths./api/v1/scim/v2/Users/{id}.delete.summary:
      sourceHash: 11c8da919810
      provenance: machine
      outputHash: d7e398bd560a
    paths./api/v1/scim/v2/Users/{id}.delete.description:
      sourceHash: f8a3ee46d088
      provenance: machine
      outputHash: 1ceca85cfffe
    paths./api/v1/scim/v2/Groups.get.summary:
      sourceHash: 7f5cfe4da60d
      provenance: machine
      outputHash: ffdf17e3060e
    paths./api/v1/scim/v2/Groups.get.description:
      sourceHash: b3dea6667317
      provenance: machine
      outputHash: 93d1f52912c6
    paths./api/v1/scim/v2/Groups.post.summary:
      sourceHash: 2477896fba1c
      provenance: machine
      outputHash: 55ed57ed3b18
    paths./api/v1/scim/v2/Groups.post.description:
      sourceHash: fbb117796dfb
      provenance: machine
      outputHash: de2a7a9fd161
    paths./api/v1/scim/v2/Groups/{id}.get.summary:
      sourceHash: cf7956970f8e
      provenance: machine
      outputHash: a36ed19cf3b3
    paths./api/v1/scim/v2/Groups/{id}.get.description:
      sourceHash: 7aeb5a733198
      provenance: machine
      outputHash: fa199288a5df
    paths./api/v1/scim/v2/Groups/{id}.put.summary:
      sourceHash: 5878fbd31c27
      provenance: machine
      outputHash: '073269391242'
    paths./api/v1/scim/v2/Groups/{id}.put.description:
      sourceHash: 0b41d735866c
      provenance: machine
      outputHash: 6a9d1bffcf6c
    paths./api/v1/scim/v2/Groups/{id}.delete.summary:
      sourceHash: 37212ab76d6b
      provenance: machine
      outputHash: 60e2fd03dec2
    paths./api/v1/scim/v2/Groups/{id}.delete.description:
      sourceHash: faf5572bb482
      provenance: machine
      outputHash: e4029fdc8e86
    paths./api/v1/enterprise/audit/export.get.summary:
      sourceHash: bb1ef74df6a7
      provenance: machine
      outputHash: 9d6f72503e7b
    paths./api/v1/enterprise/audit/export.get.description:
      sourceHash: 78723fd7a551
      provenance: machine
      outputHash: b4a8d06a39c4
    paths./api/v1/enterprise/config/export.get.summary:
      sourceHash: 0657bced852f
      provenance: machine
      outputHash: aefff1771914
    paths./api/v1/enterprise/config/export.get.description:
      sourceHash: 8e3ce9c435f5
      provenance: machine
      outputHash: cdae7926ad8d
    paths./api/v1/enterprise/config/import.post.summary:
      sourceHash: 8a507f40b80f
      provenance: machine
      outputHash: 9913cd1c8f54
    paths./api/v1/enterprise/ip-allowlist.get.summary:
      sourceHash: 8756df674b27
      provenance: machine
      outputHash: 8cecf0e3f2f6
    paths./api/v1/enterprise/ip-allowlist.get.description:
      sourceHash: 42fe8d5e6555
      provenance: machine
      outputHash: 19b4d8feb457
    paths./api/v1/enterprise/ip-allowlist.put.summary:
      sourceHash: e0d442c15809
      provenance: machine
      outputHash: b24934dc9c32
    paths./api/v1/enterprise/ip-allowlist.put.description:
      sourceHash: 0cb040540d15
      provenance: machine
      outputHash: 854bd7c9ec60
    paths./api/v1/enterprise/legal-hold.get.summary:
      sourceHash: a3b33d97f1b3
      provenance: machine
      outputHash: adfac1f47ab5
    paths./api/v1/enterprise/legal-hold.get.description:
      sourceHash: 2b51807e41b2
      provenance: machine
      outputHash: 8ae9994d7500
    paths./api/v1/enterprise/legal-hold.put.summary:
      sourceHash: f0f8a1f6bd23
      provenance: machine
      outputHash: ec5b12cfe440
    paths./api/v1/enterprise/legal-hold.put.description:
      sourceHash: 2f22192732ff
      provenance: machine
      outputHash: 7dc9ed15e4c8
    paths./api/v1/enterprise/siem/config.get.summary:
      sourceHash: 7ac4058ce316
      provenance: machine
      outputHash: 0238c3577d9c
    paths./api/v1/enterprise/siem/config.get.description:
      sourceHash: b4c92171b7fb
      provenance: machine
      outputHash: 5eeeb9bf772c
    paths./api/v1/enterprise/siem/config.put.summary:
      sourceHash: b236d6669c61
      provenance: machine
      outputHash: 1f913e44c834
    paths./api/v1/enterprise/siem/config.put.description:
      sourceHash: 94cec3b103c8
      provenance: machine
      outputHash: f960a5fb9879
    paths./api/v1/enterprise/webhooks.get.summary:
      sourceHash: a43cfb1a8c96
      provenance: machine
      outputHash: bdcec664ffd0
    paths./api/v1/enterprise/webhooks.get.description:
      sourceHash: 292a38feca49
      provenance: machine
      outputHash: 2089850c6ac5
    paths./api/v1/enterprise/webhooks.post.summary:
      sourceHash: 4a2b33ad2c1f
      provenance: machine
      outputHash: 98379da360be
    paths./api/v1/enterprise/webhooks.post.description:
      sourceHash: 2cc3c4b18327
      provenance: machine
      outputHash: 102c7686f33c
    paths./api/v1/enterprise/webhooks/{index}.put.summary:
      sourceHash: c5be5387d7be
      provenance: machine
      outputHash: 197b079da90f
    paths./api/v1/enterprise/webhooks/{index}.put.description:
      sourceHash: 28806bb5b6ce
      provenance: machine
      outputHash: cf2234b57834
    paths./api/v1/enterprise/webhooks/{index}.delete.summary:
      sourceHash: 60f85e57e4a7
      provenance: machine
      outputHash: 62795fb43346
    paths./api/v1/enterprise/webhooks/{index}.delete.description:
      sourceHash: 97b9800c59a2
      provenance: machine
      outputHash: edc285188cb7
    paths./api/v1/enterprise/webhooks/{index}/test.post.summary:
      sourceHash: 333e8368ac5c
      provenance: machine
      outputHash: 8d7e11ddb752
    paths./api/v1/enterprise/webhooks/{index}/test.post.description:
      sourceHash: 5626cb4c037d
      provenance: machine
      outputHash: 7fdbb65d3473
    paths./api/v1/enterprise/users/{id}/export.post.summary:
      sourceHash: 75556a6714fc
      provenance: machine
      outputHash: fbe17b773fe4
    paths./api/v1/enterprise/users/{id}/export.post.description:
      sourceHash: 4f90b2a09ef8
      provenance: machine
      outputHash: e1f3dc3b36a3
    paths./api/v1/enterprise/users/{id}/export/{jobId}.get.summary:
      sourceHash: 7fa624472044
      provenance: machine
      outputHash: 63f225a3ea06
    paths./api/v1/enterprise/users/{id}/export/{jobId}.get.description:
      sourceHash: fb51267dcbf7
      provenance: machine
      outputHash: 83304a741f0e
    paths./api/v1/enterprise/users/{id}/purge.delete.summary:
      sourceHash: 09c40d574cc0
      provenance: machine
      outputHash: 8323ebcab76b
    paths./api/v1/enterprise/teams/{id}/purge.delete.summary:
      sourceHash: e5f557c6db8b
      provenance: machine
      outputHash: d402d13a6530
    paths./api/v1/admin/version.get.summary:
      sourceHash: 1fa5f3f5fa4a
      provenance: machine
      outputHash: 5c8af0a72039
    paths./api/v1/admin/version.get.description:
      sourceHash: b72b0bfc24e6
      provenance: machine
      outputHash: a37f9d1f6080
    paths./api/v1/admin/migrations/pending.get.summary:
      sourceHash: dd3d4aea6207
      provenance: machine
      outputHash: dcedbb4f0092
    paths./api/v1/admin/migrations/pending.get.description:
      sourceHash: 434f121758d2
      provenance: machine
      outputHash: 5bbfbf993052
    paths./api/v1/admin/upgrade-check.get.summary:
      sourceHash: abc755f5b011
      provenance: machine
      outputHash: 205e38991c75
    paths./api/v1/admin/upgrade-check.get.description:
      sourceHash: a21396abfd74
      provenance: machine
      outputHash: c285598c5b61
    paths./llms.txt.get.summary:
      sourceHash: ba31286a938b
      provenance: machine
      outputHash: af6768172166
    paths./llms.txt.get.description:
      sourceHash: 5f69094320cb
      provenance: machine
      outputHash: a95f92506bff
    paths./llms-full.txt.get.summary:
      sourceHash: 6f12d5cecc9d
      provenance: machine
      outputHash: d83ac4812ed1
    paths./llms-full.txt.get.description:
      sourceHash: a135dfc6aac1
      provenance: machine
      outputHash: 07a9766d0b9b
    paths./api/v1/openapi.yaml.get.summary:
      sourceHash: 9ee7dc77d462
      provenance: machine
      outputHash: 9ee7dc77d462
    paths./api/v1/openapi.yaml.get.description:
      sourceHash: 10ceb656a256
      provenance: machine
      outputHash: 56480e0aa118
    paths./api/v1/tools/popular.get.summary:
      sourceHash: de2139ffd4c3
      provenance: machine
      outputHash: 3a9363d63bcc
    paths./api/v1/tools/popular.get.description:
      sourceHash: 40c9637d843b
      provenance: machine
      outputHash: 13690c54ccae
