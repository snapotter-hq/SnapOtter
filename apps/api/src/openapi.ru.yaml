openapi: 3.1.0
info:
  title: SnapOtter API
  version: 2.0.0
  description: |
    REST API для SnapOtter, самостоятельно размещаемой инфраструктуры обработки файлов с 200+ инструментами для image, video, audio, PDF и документов.

    ## Аутентификация

    Большинство конечных точек требуют аутентификации одним из двух способов:

    1. **Сессионная cookie** - Вызовите `POST /api/auth/login` с именем пользователя и паролем. Ответ содержит поле `token`. Передавайте его как `Authorization: Bearer <token>` в последующих запросах.

    2. **API-ключ** - Создайте ключ через интерфейс настроек или `POST /api/v1/api-keys`. Ключи имеют префикс `si_`. Передавайте его как `Authorization: Bearer si_...`.

    Конечные точки, отмеченные значком замка, требуют аутентификации. Конечные точки, доступные только администраторам, отмечены в их описании.
  license:
    name: AGPL-3.0
    url: https://github.com/snapotter-hq/snapotter/blob/main/LICENSE
servers:
  - url: /
    description: Current instance
tags:
  - name: Tools
    description: Инструменты обработки файлов для изображений, видео, аудио, PDF и файлов. Каждый принимает multipart-загрузку файла и возвращает URL для скачивания.
  - name: Batch
    description: Обработка нескольких файлов через один инструмент за один запрос.
  - name: Pipelines
    description: Объединение нескольких инструментов в переиспользуемые рабочие процессы.
  - name: Files
    description: Загрузка, скачивание и управление обработанными файлами.
  - name: Auth
    description: Вход, выход, управление сессиями и администрирование пользователей.
  - name: API Keys
    description: Создание и управление API key для программного доступа.
  - name: Settings
    description: Общесистемная конфигурация (запись только для администраторов).
  - name: Teams
    description: Объединение пользователей в команды.
  - name: Roles
    description: Управление пользовательскими ролями с детальными разрешениями.
  - name: Audit
    description: Журнал аудита для отслеживания административных действий.
  - name: Analytics
    description: Конфигурация аналитики и согласие пользователя.
  - name: Features
    description: Установка и управление наборами AI-функций.
  - name: Admin
    description: Административная диагностика состояния.
  - name: System
    description: Проверки состояния, конфигурация и прогресс задач.
  - name: Enterprise
    description: Enterprise-администрирование с лицензионным доступом, соответствие требованиям, SCIM и API интеграций.
components:
  securitySchemes:
    bearerAuth:
      type: http
      scheme: bearer
      description: Session token from login or API key (prefixed with si_)
  responses:
    RateLimited:
      description: Too many requests. The client has exceeded the rate limit.
      headers:
        Retry-After:
          schema:
            type: integer
          description: Seconds until the rate limit window resets
        X-RateLimit-Limit:
          schema:
            type: integer
          description: Maximum requests per time window
        X-RateLimit-Remaining:
          schema:
            type: integer
          description: Remaining requests in current window
      content:
        application/json:
          schema:
            type: object
            properties:
              error:
                type: string
                example: Rate limit exceeded
  schemas:
    Error:
      type: object
      required:
        - error
      properties:
        error:
          type: string
          example: Invalid image format
        details:
          type: string
          description: Additional error details (e.g. Zod validation errors)
    ToolResponse:
      type: object
      properties:
        jobId:
          type: string
          description: Unique job identifier
        downloadUrl:
          type: string
          description: URL to download the processed image
          example: /api/v1/download/abc123/output.png
        originalSize:
          type: integer
          description: Original file size in bytes
        processedSize:
          type: integer
          description: Processed file size in bytes
        previewUrl:
          type: string
          description: URL to a WebP preview (present when output is not browser-previewable, e.g. TIFF, HEIF)
        savedFileId:
          type: string
          description: ID of the auto-saved file in the library (present when fileId was provided in the request)
    UnauthorizedError:
      type: object
      properties:
        statusCode:
          type: integer
          example: 401
        error:
          type: string
          example: Unauthorized
        message:
          type: string
          example: Authentication required
    ForbiddenError:
      type: object
      properties:
        statusCode:
          type: integer
          example: 403
        error:
          type: string
          example: Forbidden
        message:
          type: string
          example: Insufficient permissions
    ConflictError:
      type: object
      properties:
        statusCode:
          type: integer
          example: 409
        error:
          type: string
          example: Conflict
        message:
          type: string
          example: Resource already exists
    FeatureNotInstalledError:
      type: object
      properties:
        error:
          type: string
          example: Feature not installed
        code:
          type: string
          enum:
            - FEATURE_NOT_INSTALLED
            - FEATURE_INCOMPATIBLE
          example: FEATURE_NOT_INSTALLED
        feature:
          type: string
          description: Feature bundle identifier
        featureName:
          type: string
          description: Human-readable feature name
        estimatedSize:
          type: string
          description: Estimated download size
        requestedQuality:
          type: string
          enum:
            - fast
            - balanced
            - best
          description: OCR tier that could not be honored; Fast is reported when Korean is explicitly requested with Fast or the legacy Tesseract alias
        compatibilityReason:
          type: string
          description: Machine-readable runtime compatibility reason, when available
        guidance:
          type: string
          description: Actionable recovery guidance, when available
    HealthResponse:
      type: object
      properties:
        status:
          type: string
          enum:
            - healthy
            - unhealthy
        version:
          type: string
    AdminHealthResponse:
      type: object
      properties:
        status:
          type: string
          enum:
            - healthy
            - degraded
        version:
          type: string
        uptime:
          type: string
        storage:
          type: object
          properties:
            mode:
              type: string
            available:
              type: string
        database:
          type: string
          enum:
            - ok
            - error
        queue:
          type: object
          properties:
            active:
              type: integer
            pending:
              type: integer
        ai:
          type: object
          properties:
            gpu:
              type: boolean
security:
  - bearerAuth: []
paths:
  /api/v1/health:
    get:
      operationId: healthCheck
      tags:
        - System
      summary: Проверка состояния
      description: Возвращает состояние сервера. Используется Docker HEALTHCHECK. Публичный эндпоинт.
      security: []
      responses:
        '200':
          description: Server health
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/HealthResponse'
        '503':
          description: Database unavailable
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/HealthResponse'
  /api/v1/tools/image/resize:
    post:
      operationId: resizeImage
      tags:
        - Tools
      summary: Изменение размера
      description: Изменение размера изображения до заданных размеров или в процентах.
      security:
        - bearerAuth: []
      requestBody:
        required: true
        content:
          multipart/form-data:
            schema:
              type: object
              required:
                - file
              properties:
                file:
                  type: string
                  format: binary
                  description: Image file to process
                settings:
                  type: string
                  description: |
                    JSON string with options:
                    - `width` (number, optional) - Target width in pixels
                    - `height` (number, optional) - Target height in pixels
                    - `fit` (string, default "contain") - One of: contain, cover, fill, inside, outside
                    - `withoutEnlargement` (boolean, default false) - Prevent upscaling
                    - `percentage` (number, optional) - Scale by percentage instead of fixed dimensions
      responses:
        '200':
          description: Processed image
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ToolResponse'
        '400':
          description: Invalid input
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Error'
        '401':
          description: Authentication required
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/UnauthorizedError'
  /api/v1/tools/image/content-aware-resize:
    post:
      operationId: contentAwareResize
      tags:
        - Tools
      summary: Контентно-зависимое изменение размера
      description: Изменение размера изображения методом seam carving для интеллектуального удаления или добавления содержимого с сохранением важных элементов.
      security:
        - bearerAuth: []
      requestBody:
        required: true
        content:
          multipart/form-data:
            schema:
              type: object
              required:
                - file
              properties:
                file:
                  type: string
                  format: binary
                  description: Image file to process
                settings:
                  type: string
                  description: |
                    JSON string with options:
                    - `width` (number, optional) - Target width in pixels
                    - `height` (number, optional) - Target height in pixels
                    - `protectFaces` (boolean, default false) - Detect and protect faces from distortion
                    - `blurRadius` (number 0-20, default 4) - Blur radius for energy map
                    - `sobelThreshold` (number 1-20, default 2) - Edge detection threshold
                    - `square` (boolean, default false) - Crop to square aspect ratio

                    At least one of `width`, `height`, or `square` must be provided.
      responses:
        '200':
          description: Processed image
          content:
            application/json:
              schema:
                allOf:
                  - $ref: '#/components/schemas/ToolResponse'
                  - type: object
                    properties:
                      width:
                        type: integer
                      height:
                        type: integer
        '400':
          description: Invalid input
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Error'
        '401':
          description: Authentication required
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/UnauthorizedError'
  /api/v1/tools/image/crop:
    post:
      operationId: cropImage
      tags:
        - Tools
      summary: Обрезка
      description: Обрезка изображения по заданной области.
      security:
        - bearerAuth: []
      requestBody:
        required: true
        content:
          multipart/form-data:
            schema:
              type: object
              required:
                - file
              properties:
                file:
                  type: string
                  format: binary
                  description: Image file to process
                settings:
                  type: string
                  description: |
                    JSON string with options:
                    - `left` (number, required) - Left offset in pixels (min 0)
                    - `top` (number, required) - Top offset in pixels (min 0)
                    - `width` (number, required) - Width of crop region in pixels
                    - `height` (number, required) - Height of crop region in pixels
                    - `unit` (string, optional) - One of: px, percent
      responses:
        '200':
          description: Processed image
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ToolResponse'
        '400':
          description: Invalid input
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Error'
        '401':
          description: Authentication required
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/UnauthorizedError'
  /api/v1/tools/image/rotate:
    post:
      operationId: rotateImage
      tags:
        - Tools
      summary: Поворот и отражение
      description: Поворот изображения на угол или отражение по горизонтали/вертикали.
      security:
        - bearerAuth: []
      requestBody:
        required: true
        content:
          multipart/form-data:
            schema:
              type: object
              required:
                - file
              properties:
                file:
                  type: string
                  format: binary
                  description: Image file to process
                settings:
                  type: string
                  description: |
                    JSON string with options:
                    - `angle` (number, default 0) - Rotation angle in degrees
                    - `horizontal` (boolean, default false) - Flip horizontally
                    - `vertical` (boolean, default false) - Flip vertically
      responses:
        '200':
          description: Processed image
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ToolResponse'
        '400':
          description: Invalid input
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Error'
        '401':
          description: Authentication required
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/UnauthorizedError'
  /api/v1/tools/image/convert:
    post:
      operationId: convertFormat
      tags:
        - Tools
      summary: Конвертация формата
      description: Конвертация изображения в другой формат.
      security:
        - bearerAuth: []
      requestBody:
        required: true
        content:
          multipart/form-data:
            schema:
              type: object
              required:
                - file
              properties:
                file:
                  type: string
                  format: binary
                  description: Image file to process
                settings:
                  type: string
                  description: |
                    JSON string with options:
                    - `format` (string, required) - One of: jpg, png, webp, avif, tiff, gif, heic, heif, jxl, bmp, ico, jp2, qoi, psd
                    - `quality` (number 1-100, optional) - Output quality
      responses:
        '200':
          description: Processed image
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ToolResponse'
        '400':
          description: Invalid input
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Error'
        '401':
          description: Authentication required
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/UnauthorizedError'
  /api/v1/tools/image/compress:
    post:
      operationId: compressImage
      tags:
        - Tools
      summary: Сжатие
      description: Уменьшение размера файла изображения по уровню качества или целевому размеру.
      security:
        - bearerAuth: []
      requestBody:
        required: true
        content:
          multipart/form-data:
            schema:
              type: object
              required:
                - file
              properties:
                file:
                  type: string
                  format: binary
                  description: Image file to process
                settings:
                  type: string
                  description: |
                    JSON string with options:
                    - `mode` (string, default "quality") - One of: quality, targetSize
                    - `quality` (number 1-100, optional) - Compression quality level
                    - `targetSizeKb` (number, optional) - Target file size in kilobytes
      responses:
        '200':
          description: Processed image
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ToolResponse'
        '400':
          description: Invalid input
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Error'
        '401':
          description: Authentication required
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/UnauthorizedError'
  /api/v1/tools/image/strip-metadata:
    post:
      operationId: stripMetadata
      tags:
        - Tools
      summary: Удаление метаданных
      description: Удаление метаданных EXIF, GPS, ICC или XMP из изображения.
      security:
        - bearerAuth: []
      requestBody:
        required: true
        content:
          multipart/form-data:
            schema:
              type: object
              required:
                - file
              properties:
                file:
                  type: string
                  format: binary
                  description: Image file to process
                settings:
                  type: string
                  description: |
                    JSON string with options:
                    - `stripExif` (boolean, default false) - Remove EXIF data
                    - `stripGps` (boolean, default false) - Remove GPS data
                    - `stripIcc` (boolean, default false) - Remove ICC profile
                    - `stripXmp` (boolean, default false) - Remove XMP data
                    - `stripAll` (boolean, default true) - Remove all metadata
      responses:
        '200':
          description: Processed image
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ToolResponse'
        '400':
          description: Invalid input
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Error'
        '401':
          description: Authentication required
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/UnauthorizedError'
  /api/v1/tools/image/border:
    post:
      operationId: addBorder
      tags:
        - Tools
      summary: Рамка и обрамление
      description: Добавление рамки, скруглённых углов, отступов или тени к изображению.
      security:
        - bearerAuth: []
      requestBody:
        required: true
        content:
          multipart/form-data:
            schema:
              type: object
              required:
                - file
              properties:
                file:
                  type: string
                  format: binary
                  description: Image file to process
                settings:
                  type: string
                  description: |
                    JSON string with options:
                    - `borderWidth` (number 0-2000, default 10) - Border thickness in pixels
                    - `borderColor` (hex string, default "#000000") - Border color
                    - `padding` (number 0-200, default 0) - Inner padding in pixels
                    - `paddingColor` (hex string, default "#FFFFFF") - Padding fill color
                    - `cornerRadius` (number 0-2000, default 0) - Corner rounding radius
                    - `shadow` (boolean, default false) - Enable drop shadow
                    - `shadowBlur` (number 1-200, default 15) - Drop shadow blur radius
                    - `shadowOffsetX` (number -50 to 50, default 0) - Shadow horizontal offset
                    - `shadowOffsetY` (number -50 to 50, default 5) - Shadow vertical offset
                    - `shadowColor` (hex string, default "#000000") - Drop shadow color
                    - `shadowOpacity` (number 0-100, default 40) - Shadow opacity percentage
      responses:
        '200':
          description: Processed image
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ToolResponse'
        '400':
          description: Invalid input
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Error'
        '401':
          description: Authentication required
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/UnauthorizedError'
  /api/v1/tools/image/beautify:
    post:
      operationId: beautifyScreenshot
      tags:
        - Tools
      summary: Улучшение скриншота
      description: |
        Добавление градиентных фонов, рамок устройств, теней, водяных знаков и
        размеров для соцсетей к скриншотам. Поддерживает сплошной, линейный градиент,
        радиальный градиент, изображение и прозрачный фоны. Включает рамки устройств
        macOS, Windows, браузера, iPhone, MacBook и iPad. Пресеты соцсетей для Twitter,
        LinkedIn, Instagram, Facebook и Product Hunt.
      security:
        - bearerAuth: []
      requestBody:
        required: true
        content:
          multipart/form-data:
            schema:
              type: object
              required:
                - file
              properties:
                file:
                  type: string
                  format: binary
                  description: Screenshot or image file to beautify
                backgroundImage:
                  type: string
                  format: binary
                  description: Optional image for image backgrounds (used when backgroundType is "image")
                settings:
                  type: string
                  description: |
                    JSON string with options:
                    - `backgroundType` (string, default "linear-gradient") -- One of: solid, linear-gradient, radial-gradient, image, transparent
                    - `backgroundColor` (hex string, default "#667eea") -- Background color (for solid backgrounds)
                    - `gradientStops` (array, default [{color:"#667eea",position:0},{color:"#764ba2",position:100}]) -- Array of {color, position} objects for gradient backgrounds
                    - `gradientAngle` (number 0-360, default 135) -- Gradient angle in degrees
                    - `padding` (number 0-256, default 64) -- Space between screenshot and canvas edge in pixels
                    - `borderRadius` (number 0-64, default 12) -- Corner rounding radius for the screenshot
                    - `shadowPreset` (string, default "subtle") -- One of: none, subtle, medium, dramatic, custom
                    - `shadowBlur` (number 0-100, default 20) -- Shadow blur radius (used with custom shadow preset)
                    - `shadowOffsetX` (number -50 to 50, default 0) -- Shadow horizontal offset
                    - `shadowOffsetY` (number -50 to 50, default 10) -- Shadow vertical offset
                    - `shadowColor` (hex string, default "#000000") -- Shadow color
                    - `shadowOpacity` (number 0-100, default 30) -- Shadow opacity percentage
                    - `frame` (string, default "none") -- One of: none, macos-light, macos-dark, windows-light, windows-dark, browser-light, browser-dark, iphone, iphone-dark, macbook, macbook-dark, ipad, ipad-dark
                    - `frameTitle` (string, optional) -- Title text shown in window title bars
                    - `socialPreset` (string, default "none") -- One of: none, twitter, linkedin, instagram-square, instagram-story, facebook, producthunt
                    - `watermarkText` (string, optional) -- Watermark text to overlay
                    - `watermarkPosition` (string, default "bottom-right") -- One of: top-left, top-right, bottom-left, bottom-right, center
                    - `watermarkOpacity` (number 0-100, default 50) -- Watermark opacity percentage
                    - `outputFormat` (string, default "png") -- One of: png, jpeg, webp
      responses:
        '200':
          description: Beautified image
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ToolResponse'
        '400':
          description: Invalid input
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Error'
        '401':
          description: Authentication required
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/UnauthorizedError'
  /api/v1/tools/image/adjust-colors:
    post:
      operationId: adjustColors
      tags:
        - Tools
      summary: Цветокоррекция
      description: |
        Единый инструмент цветокоррекции. Настраивает яркость, контраст, экспозицию, насыщенность, температуру, оттенок, тон, резкость, цветовые каналы и эффекты за один проход.
      security:
        - bearerAuth: []
      requestBody:
        required: true
        content:
          multipart/form-data:
            schema:
              type: object
              required:
                - file
              properties:
                file:
                  type: string
                  format: binary
                  description: Image file to process
                settings:
                  type: string
                  description: |
                    JSON string with options:
                    - `brightness` (number -100 to 100, default 0) - Brightness adjustment
                    - `contrast` (number -100 to 100, default 0) - Contrast adjustment
                    - `exposure` (number -100 to 100, default 0) - Exposure adjustment (gamma)
                    - `saturation` (number -100 to 100, default 0) - Color saturation
                    - `temperature` (number -100 to 100, default 0) - Color temperature (cool to warm)
                    - `tint` (number -100 to 100, default 0) - Tint shift (green to magenta)
                    - `hue` (number -180 to 180, default 0) - Hue rotation in degrees
                    - `sharpness` (number 0 to 100, default 0) - Sharpness enhancement
                    - `red` (number 0-200, default 100) - Red channel multiplier
                    - `green` (number 0-200, default 100) - Green channel multiplier
                    - `blue` (number 0-200, default 100) - Blue channel multiplier
                    - `effect` (string, default "none") - One of: none, grayscale, sepia, invert
      responses:
        '200':
          description: Processed image
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ToolResponse'
        '400':
          description: Invalid input
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Error'
        '401':
          description: Authentication required
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/UnauthorizedError'
  /api/v1/tools/image/sharpening:
    post:
      operationId: sharpenImage
      tags:
        - Tools
      summary: Повышение резкости
      description: Повышение резкости изображения методами адаптивной резкости, unsharp mask или high-pass с опциональным шумоподавлением.
      security:
        - bearerAuth: []
      requestBody:
        required: true
        content:
          multipart/form-data:
            schema:
              type: object
              required:
                - file
              properties:
                file:
                  type: string
                  format: binary
                  description: Image file to process
                settings:
                  type: string
                  description: |
                    JSON string with options:
                    - `method` (string, default "adaptive") - One of: adaptive, unsharp-mask, high-pass
                    - `sigma` (number 0.5-10, default 1.0) - Gaussian sigma (adaptive)
                    - `m1` (number 0-10, default 1.0) - Flat area sharpening (adaptive)
                    - `m2` (number 0-20, default 3.0) - Jagged area sharpening (adaptive)
                    - `x1` (number 0-10, default 2.0) - Flat/jagged threshold (adaptive)
                    - `y2` (number 0-50, default 12) - Maximum brightening (adaptive)
                    - `y3` (number 0-50, default 20) - Maximum darkening (adaptive)
                    - `amount` (number 0-1000, default 100) - Sharpen amount (unsharp-mask)
                    - `radius` (number 0.1-5, default 1.0) - Blur radius (unsharp-mask)
                    - `threshold` (number 0-255, default 0) - Luminance threshold (unsharp-mask)
                    - `strength` (number 0-100, default 50) - Blend strength (high-pass)
                    - `kernelSize` (integer, default 3) - One of: 3, 5 (high-pass)
                    - `denoise` (string, default "off") - One of: off, light, medium, strong
      responses:
        '200':
          description: Processed image
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ToolResponse'
        '400':
          description: Invalid input
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Error'
        '401':
          description: Authentication required
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/UnauthorizedError'
  /api/v1/tools/image/optimize-for-web:
    post:
      operationId: optimizeForWeb
      tags:
        - Tools
      summary: Оптимизация для веба
      description: Оптимизация изображения для доставки в вебе путём конвертации в современные форматы, настройки качества и опционального изменения размера.
      security:
        - bearerAuth: []
      requestBody:
        required: true
        content:
          multipart/form-data:
            schema:
              type: object
              required:
                - file
              properties:
                file:
                  type: string
                  format: binary
                  description: Image file to process
                settings:
                  type: string
                  description: |
                    JSON string with options:
                    - `format` (string, default "webp") - One of: webp, jpeg, avif, png, jxl
                    - `quality` (number 1-100, default 80) - Output quality
                    - `maxWidth` (number, optional) - Maximum width in pixels
                    - `maxHeight` (number, optional) - Maximum height in pixels
                    - `progressive` (boolean, default true) - Enable progressive encoding
                    - `stripMetadata` (boolean, default true) - Remove metadata
      responses:
        '200':
          description: Processed image
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ToolResponse'
        '400':
          description: Invalid input
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Error'
        '401':
          description: Authentication required
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/UnauthorizedError'
  /api/v1/tools/image/optimize-for-web/preview:
    post:
      operationId: optimizeForWebPreview
      tags:
        - Tools
      summary: Оптимизация для веба (предпросмотр)
      description: |
        Облегчённый эндпоинт предпросмотра для настройки параметров в реальном времени.
        Возвращает оптимизированные бинарные данные изображения напрямую (не JSON).
        Информация о размере в заголовках ответа. Не создаёт рабочую область и не
        сохраняет результаты.
      security:
        - bearerAuth: []
      requestBody:
        required: true
        content:
          multipart/form-data:
            schema:
              type: object
              required:
                - file
              properties:
                file:
                  type: string
                  format: binary
                  description: Image file to preview
                settings:
                  type: string
                  description: Same settings as the main optimize-for-web endpoint
      responses:
        '200':
          description: Optimized image binary
          headers:
            X-Original-Size:
              schema:
                type: string
              description: Original file size in bytes
            X-Processed-Size:
              schema:
                type: string
              description: Processed file size in bytes
            X-Output-Filename:
              schema:
                type: string
              description: Suggested output filename
          content:
            image/*:
              schema:
                type: string
                format: binary
        '400':
          description: Invalid input
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Error'
        '422':
          description: Processing failed
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Error'
  /api/v1/tools/image/image-enhancement:
    post:
      operationId: enhanceImage
      tags:
        - Tools
      summary: Улучшение изображения
      description: |
        Автоматическое улучшение изображения на базе AI. Анализирует изображение и
        применяет коррекции экспозиции, контраста, баланса белого, насыщенности,
        резкости и шума на основе выбранного режима сцены.
      security:
        - bearerAuth: []
      requestBody:
        required: true
        content:
          multipart/form-data:
            schema:
              type: object
              required:
                - file
              properties:
                file:
                  type: string
                  format: binary
                  description: Image file to process
                settings:
                  type: string
                  description: |
                    JSON string with options:
                    - `mode` (string, default "auto") - One of: auto, portrait, landscape, low-light, food, document
                    - `intensity` (number 0-100, default 50) - Overall enhancement intensity
                    - `corrections` (object, optional) - Toggle individual corrections:
                      - `exposure` (boolean, default true)
                      - `contrast` (boolean, default true)
                      - `whiteBalance` (boolean, default true)
                      - `saturation` (boolean, default true)
                      - `sharpness` (boolean, default true)
                      - `denoise` (boolean, default true)
                    - `deepEnhance` (boolean, default false) - Use AI noise removal for deeper enhancement (requires noise-removal feature)
      responses:
        '200':
          description: Processed image
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ToolResponse'
        '400':
          description: Invalid input
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Error'
        '401':
          description: Authentication required
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/UnauthorizedError'
  /api/v1/tools/image/image-enhancement/analyze:
    post:
      operationId: analyzeImageEnhancement
      tags:
        - Tools
      summary: Анализ изображения для улучшения
      description: |
        Анализ изображения с возвратом предлагаемых коррекций без их применения.
        Полезно для предпросмотра того, какие улучшения будут выполнены.
      security:
        - bearerAuth: []
      requestBody:
        required: true
        content:
          multipart/form-data:
            schema:
              type: object
              required:
                - file
              properties:
                file:
                  type: string
                  format: binary
                  description: Image file to analyze
      responses:
        '200':
          description: Image analysis with suggested corrections
          content:
            application/json:
              schema:
                type: object
                properties:
                  corrections:
                    type: object
                    description: Suggested correction values
                  metrics:
                    type: object
                    description: Image quality metrics
        '400':
          description: Invalid input
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Error'
        '422':
          description: Analysis failed
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Error'
  /api/v1/tools/image/noise-removal:
    post:
      operationId: removeNoise
      tags:
        - Tools
      summary: Удаление шума
      description: |
        Удаление шума на базе AI через Python sidecar. Поддерживает несколько
        уровней качества от быстрого предпросмотра до максимального качества.
        Требует установки набора функций noise-removal.
      security:
        - bearerAuth: []
      requestBody:
        required: true
        content:
          multipart/form-data:
            schema:
              type: object
              required:
                - file
              properties:
                file:
                  type: string
                  format: binary
                  description: Image file to process
                settings:
                  type: string
                  description: |
                    JSON string with options:
                    - `tier` (string, default "balanced") - One of: quick, balanced, quality, maximum
                    - `strength` (number, default 50) - Denoising strength
                    - `detailPreservation` (number, default 50) - Detail preservation level
                    - `colorNoise` (number, default 30) - Color noise reduction
                    - `format` (string, default "original") - One of: original, png, jpeg, webp, avif, jxl
                    - `quality` (number, default 90) - Output quality (1-100)
                clientJobId:
                  type: string
                  description: Client-provided job ID for SSE progress tracking
      responses:
        '202':
          description: Accepted for async processing. Track progress via SSE at /api/v1/jobs/{jobId}/progress.
          content:
            application/json:
              schema:
                type: object
                properties:
                  jobId:
                    type: string
                  async:
                    type: boolean
                    example: true
        '400':
          description: Invalid input
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Error'
        '401':
          description: Authentication required
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/UnauthorizedError'
        '422':
          description: Processing failed
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Error'
        '501':
          description: Feature not installed
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/FeatureNotInstalledError'
  /api/v1/tools/image/red-eye-removal:
    post:
      operationId: removeRedEye
      tags:
        - Tools
      summary: Удаление красных глаз
      description: |
        Обнаружение и удаление красных глаз на базе AI. Обнаруживает лица и
        исправляет артефакты красных глаз на фотографиях. Требует установки набора
        функций red-eye-removal.
      security:
        - bearerAuth: []
      requestBody:
        required: true
        content:
          multipart/form-data:
            schema:
              type: object
              required:
                - file
              properties:
                file:
                  type: string
                  format: binary
                  description: Image file to process
                settings:
                  type: string
                  description: |
                    JSON string with options:
                    - `sensitivity` (number 0-100, default 50) - Detection sensitivity
                    - `strength` (number 0-100, default 70) - Correction strength
                    - `format` (string, optional) - Output format
                    - `quality` (number 1-100, default 90) - Output quality
                clientJobId:
                  type: string
                  description: Client-provided job ID for SSE progress tracking
      responses:
        '202':
          description: Accepted for async processing. Track progress via SSE at /api/v1/jobs/{jobId}/progress.
          content:
            application/json:
              schema:
                type: object
                properties:
                  jobId:
                    type: string
                  async:
                    type: boolean
                    example: true
        '400':
          description: Invalid input
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Error'
        '401':
          description: Authentication required
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/UnauthorizedError'
        '422':
          description: Processing failed
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Error'
        '501':
          description: Feature not installed
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/FeatureNotInstalledError'
  /api/v1/tools/image/restore-photo:
    post:
      operationId: restorePhoto
      tags:
        - Tools
      summary: Реставрация фото
      description: |
        Конвейер реставрации фото на базе AI. Устраняет царапины, улучшает лица,
        подавляет шум и опционально раскрашивает старые или повреждённые фотографии.
        Требует установки набора функций photo-restoration.
      security:
        - bearerAuth: []
      requestBody:
        required: true
        content:
          multipart/form-data:
            schema:
              type: object
              required:
                - file
              properties:
                file:
                  type: string
                  format: binary
                  description: Image file to process
                settings:
                  type: string
                  description: |
                    JSON string with options:
                    - `scratchRemoval` (boolean, default true) - Enable scratch repair
                    - `faceEnhancement` (boolean, default true) - Enhance detected faces
                    - `fidelity` (number 0-1, default 0.7) - Face enhancement fidelity
                    - `denoise` (boolean, default true) - Enable denoising
                    - `denoiseStrength` (number 0-100, default 25) - Denoising strength
                    - `colorize` (boolean, default false) - Colorize grayscale photos
                    - `colorizeStrength` (number 0-100, default 85) - Colorization strength
                clientJobId:
                  type: string
                  description: Client-provided job ID for SSE progress tracking
      responses:
        '202':
          description: Accepted for async processing. Track progress via SSE at /api/v1/jobs/{jobId}/progress.
          content:
            application/json:
              schema:
                type: object
                properties:
                  jobId:
                    type: string
                  async:
                    type: boolean
                    example: true
        '400':
          description: Invalid input
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Error'
        '401':
          description: Authentication required
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/UnauthorizedError'
        '422':
          description: Processing failed
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Error'
        '501':
          description: Feature not installed
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/FeatureNotInstalledError'
  /api/v1/tools/image/passport-photo/analyze:
    post:
      operationId: analyzePassportPhoto
      tags:
        - Tools
      summary: Фото на паспорт - анализ
      description: |
        Этап 1 создания фото на паспорт. Обнаруживает опорные точки лица и удаляет
        фон. Возвращает данные опорных точек и предпросмотр в base64 для использования
        клиентом на этапе создания. Требует установки набора функций passport-photo.
      security:
        - bearerAuth: []
      requestBody:
        required: true
        content:
          multipart/form-data:
            schema:
              type: object
              required:
                - file
              properties:
                file:
                  type: string
                  format: binary
                  description: Photo with a clear, front-facing face
                clientJobId:
                  type: string
                  description: Client-provided job ID for SSE progress tracking
      responses:
        '200':
          description: Face analysis and background removal result
          content:
            application/json:
              schema:
                type: object
                properties:
                  jobId:
                    type: string
                    description: Job ID to pass to the generate endpoint
                  filename:
                    type: string
                  preview:
                    type: string
                    description: Base64-encoded PNG preview of the background-removed image
                  previewWidth:
                    type: integer
                  previewHeight:
                    type: integer
                  landmarks:
                    type: object
                    description: Normalized face landmark coordinates (0-1)
                    properties:
                      leftEye:
                        type: object
                        properties:
                          x:
                            type: number
                          'y':
                            type: number
                      rightEye:
                        type: object
                        properties:
                          x:
                            type: number
                          'y':
                            type: number
                      eyeCenter:
                        type: object
                        properties:
                          x:
                            type: number
                          'y':
                            type: number
                      chin:
                        type: object
                        properties:
                          x:
                            type: number
                          'y':
                            type: number
                      forehead:
                        type: object
                        properties:
                          x:
                            type: number
                          'y':
                            type: number
                      crown:
                        type: object
                        properties:
                          x:
                            type: number
                          'y':
                            type: number
                      nose:
                        type: object
                        properties:
                          x:
                            type: number
                          'y':
                            type: number
                      faceCenterX:
                        type: number
                  imageWidth:
                    type: integer
                  imageHeight:
                    type: integer
        '400':
          description: Invalid input
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Error'
        '401':
          description: Authentication required
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/UnauthorizedError'
        '422':
          description: No face detected or analysis failed
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Error'
        '501':
          description: Feature not installed
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/FeatureNotInstalledError'
  /api/v1/tools/image/passport-photo/generate:
    post:
      operationId: generatePassportPhoto
      tags:
        - Tools
      summary: Фото на паспорт - создание
      description: |
        Этап 2 создания фото на паспорт. Использует jobId и опорные точки из этапа
        анализа для обрезки, изменения размера и размещения фото по заданной
        спецификации страны/документа. Быстрый ответ (без повторного запуска AI).
      security:
        - bearerAuth: []
      requestBody:
        required: true
        content:
          application/json:
            schema:
              type: object
              required:
                - jobId
                - filename
                - countryCode
                - landmarks
                - imageWidth
                - imageHeight
              properties:
                jobId:
                  type: string
                  description: Job ID from the analyze phase
                filename:
                  type: string
                  description: Original filename from analyze phase
                countryCode:
                  type: string
                  description: ISO country code for passport specification
                documentType:
                  type: string
                  default: passport
                  description: Document type (passport, visa, etc.)
                bgColor:
                  type: string
                  default: '#FFFFFF'
                  description: Background color (hex)
                printLayout:
                  type: string
                  default: none
                  description: Print sheet layout (none, 4x6, A4, letter)
                maxFileSizeKb:
                  type: number
                  default: 0
                  description: Max file size in KB (0 = no limit)
                dpi:
                  type: number
                  minimum: 72
                  maximum: 1200
                  default: 300
                  description: Output DPI
                customWidthMm:
                  type: number
                  description: Custom photo width in mm (overrides country spec)
                customHeightMm:
                  type: number
                  description: Custom photo height in mm (overrides country spec)
                zoom:
                  type: number
                  minimum: 0.5
                  maximum: 3
                  default: 1
                  description: Zoom level (>1 = tighter crop)
                adjustX:
                  type: number
                  default: 0
                  description: Horizontal position adjustment
                adjustY:
                  type: number
                  default: 0
                  description: Vertical position adjustment
                landmarks:
                  type: object
                  description: Face landmarks from analyze phase
                imageWidth:
                  type: integer
                  description: Original image width from analyze phase
                imageHeight:
                  type: integer
                  description: Original image height from analyze phase
      responses:
        '200':
          description: Generated passport photo
          content:
            application/json:
              schema:
                type: object
                properties:
                  jobId:
                    type: string
                  downloadUrl:
                    type: string
                  dimensions:
                    type: object
                    properties:
                      widthMm:
                        type: number
                      heightMm:
                        type: number
                      widthPx:
                        type: integer
                      heightPx:
                        type: integer
                      dpi:
                        type: integer
                  spec:
                    type: object
                    properties:
                      country:
                        type: string
                      countryCode:
                        type: string
                      documentType:
                        type: string
                      documentLabel:
                        type: string
                  printDownloadUrl:
                    type: string
                    description: URL for print sheet download (if printLayout was specified)
        '400':
          description: Invalid settings or unknown country code
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Error'
        '401':
          description: Authentication required
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/UnauthorizedError'
        '422':
          description: Generation failed
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Error'
  /api/v1/tools/image/colorize:
    post:
      operationId: colorizeImage
      tags:
        - Tools
      summary: Раскрашивание
      description: |
        Раскрашивание фото на базе AI. Преобразует чёрно-белые или полутоновые
        фотографии в полноцветные с помощью DDColor с резервным вариантом OpenCV DNN.
        Требует установки набора функций colorize.
      security:
        - bearerAuth: []
      requestBody:
        required: true
        content:
          multipart/form-data:
            schema:
              type: object
              required:
                - file
              properties:
                file:
                  type: string
                  format: binary
                  description: Grayscale or B&W image file
                settings:
                  type: string
                  description: |
                    JSON string with options:
                    - `intensity` (number 0-1, default 1.0) - Colorization intensity
                    - `model` (string, default "auto") - One of: auto, ddcolor, opencv
                clientJobId:
                  type: string
                  description: Client-provided job ID for SSE progress tracking
      responses:
        '202':
          description: Accepted for async processing. Track progress via SSE at /api/v1/jobs/{jobId}/progress.
          content:
            application/json:
              schema:
                type: object
                properties:
                  jobId:
                    type: string
                  async:
                    type: boolean
                    example: true
        '400':
          description: Invalid input
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Error'
        '401':
          description: Authentication required
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/UnauthorizedError'
        '422':
          description: Processing failed
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Error'
        '501':
          description: Feature not installed
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/FeatureNotInstalledError'
  /api/v1/tools/image/enhance-faces:
    post:
      operationId: enhanceFaces
      tags:
        - Tools
      summary: Улучшение лиц
      description: |
        Улучшение лиц на базе AI с помощью GFPGAN или CodeFormer. Обнаруживает лица
        на изображении и улучшает детали лица, текстуру кожи и чёткость.
        Требует установки набора функций enhance-faces.
      security:
        - bearerAuth: []
      requestBody:
        required: true
        content:
          multipart/form-data:
            schema:
              type: object
              required:
                - file
              properties:
                file:
                  type: string
                  format: binary
                  description: Image file containing faces
                settings:
                  type: string
                  description: |
                    JSON string with options:
                    - `model` (string, default "auto") - One of: auto, gfpgan, codeformer
                    - `strength` (number 0-1, default 0.8) - Enhancement strength
                    - `onlyCenterFace` (boolean, default false) - Only enhance the center face
                    - `sensitivity` (number 0-1, default 0.5) - Face detection sensitivity
                clientJobId:
                  type: string
                  description: Client-provided job ID for SSE progress tracking
      responses:
        '202':
          description: Accepted for async processing. Track progress via SSE at /api/v1/jobs/{jobId}/progress.
          content:
            application/json:
              schema:
                type: object
                properties:
                  jobId:
                    type: string
                  async:
                    type: boolean
                    example: true
        '400':
          description: Invalid input
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Error'
        '401':
          description: Authentication required
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/UnauthorizedError'
        '422':
          description: Processing failed
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Error'
        '501':
          description: Feature not installed
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/FeatureNotInstalledError'
  /api/v1/tools/image/image-to-base64:
    post:
      operationId: imageToBase64
      tags:
        - Tools
      summary: Изображение в Base64
      description: |
        Конвертация одного или нескольких изображений в строки Base64 с data URI.
        Поддерживает опциональную конвертацию формата и изменение размера. Возвращает
        как сырую строку Base64, так и готовый к использованию data URI для каждого файла.
      security:
        - bearerAuth: []
      requestBody:
        required: true
        content:
          multipart/form-data:
            schema:
              type: object
              required:
                - file
              properties:
                file:
                  type: array
                  items:
                    type: string
                    format: binary
                  description: One or more image files to convert
                settings:
                  type: string
                  description: |
                    JSON string with options:
                    - `outputFormat` (string, default "original") - One of: original, jpeg, png, webp, avif, jxl
                    - `quality` (integer 1-100, default 80) - Output quality for lossy formats
                    - `maxWidth` (integer, default 0) - Max width in pixels (0 = no limit)
                    - `maxHeight` (integer, default 0) - Max height in pixels (0 = no limit)
      responses:
        '200':
          description: Base64-encoded results
          content:
            application/json:
              schema:
                type: object
                properties:
                  results:
                    type: array
                    items:
                      type: object
                      properties:
                        filename:
                          type: string
                        mimeType:
                          type: string
                        width:
                          type: integer
                        height:
                          type: integer
                        originalSize:
                          type: integer
                          description: Original file size in bytes
                        encodedSize:
                          type: integer
                          description: Base64 string size in bytes
                        overheadPercent:
                          type: number
                          description: Size overhead of Base64 encoding
                        base64:
                          type: string
                          description: Raw Base64-encoded image data
                        dataUri:
                          type: string
                          description: Complete data URI (data:mime;base64,...)
                  errors:
                    type: array
                    items:
                      type: object
                      properties:
                        filename:
                          type: string
                        error:
                          type: string
        '400':
          description: Invalid input
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Error'
        '401':
          description: Authentication required
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/UnauthorizedError'
  /api/v1/tools/image/watermark-text:
    post:
      operationId: addTextWatermark
      tags:
        - Tools
      summary: Текстовый водяной знак
      description: Добавление текстового водяного знака на изображение.
      security:
        - bearerAuth: []
      requestBody:
        required: true
        content:
          multipart/form-data:
            schema:
              type: object
              required:
                - file
              properties:
                file:
                  type: string
                  format: binary
                  description: Image file to process
                settings:
                  type: string
                  description: |
                    JSON string with options:
                    - `text` (string 1-500, required) - Watermark text
                    - `fontSize` (number 8-1000, default 48) - Font size in pixels
                    - `color` (hex string, default "#000000") - Text color
                    - `opacity` (number 0-100, default 50) - Opacity percentage
                    - `position` (string, default "center") - One of: center, top-left, top-right, bottom-left, bottom-right, tiled
                    - `rotation` (number -360 to 360, default 0) - Text rotation angle
      responses:
        '200':
          description: Processed image
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ToolResponse'
        '400':
          description: Invalid input
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Error'
        '401':
          description: Authentication required
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/UnauthorizedError'
  /api/v1/tools/image/text-overlay:
    post:
      operationId: addTextOverlay
      tags:
        - Tools
      summary: Текстовое наложение
      description: Добавление стилизованного текстового наложения с опциональным фоновым блоком.
      security:
        - bearerAuth: []
      requestBody:
        required: true
        content:
          multipart/form-data:
            schema:
              type: object
              required:
                - file
              properties:
                file:
                  type: string
                  format: binary
                  description: Image file to process
                settings:
                  type: string
                  description: |
                    JSON string with options:
                    - `text` (string 1-500, required) - Overlay text
                    - `fontSize` (number 8-200, default 48) - Font size in pixels
                    - `color` (hex string, default "#FFFFFF") - Text color
                    - `position` (string, default "bottom") - One of: top, center, bottom
                    - `backgroundBox` (boolean, default false) - Show background box behind text
                    - `backgroundColor` (hex string, default "#000000") - Background box color
                    - `shadow` (boolean, default true) - Add text shadow
      responses:
        '200':
          description: Processed image
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ToolResponse'
        '400':
          description: Invalid input
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Error'
        '401':
          description: Authentication required
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/UnauthorizedError'
  /api/v1/tools/image/replace-color:
    post:
      operationId: replaceColor
      tags:
        - Tools
      summary: Замена цвета
      description: Замена определённого цвета на изображении на другой цвет или прозрачность.
      security:
        - bearerAuth: []
      requestBody:
        required: true
        content:
          multipart/form-data:
            schema:
              type: object
              required:
                - file
              properties:
                file:
                  type: string
                  format: binary
                  description: Image file to process
                settings:
                  type: string
                  description: |
                    JSON string with options:
                    - `sourceColor` (hex string, default "#FF0000") - Color to replace
                    - `targetColor` (hex string, default "#00FF00") - Replacement color
                    - `makeTransparent` (boolean, default false) - Make matched pixels transparent instead
                    - `tolerance` (number 0-255, default 30) - Color matching tolerance
      responses:
        '200':
          description: Processed image
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ToolResponse'
        '400':
          description: Invalid input
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Error'
        '401':
          description: Authentication required
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/UnauthorizedError'
  /api/v1/tools/image/color-blindness:
    post:
      operationId: simulateColorBlindness
      tags:
        - Tools
      summary: Симуляция дальтонизма
      description: Симуляция того, как изображение выглядит для людей с различными типами нарушения цветового восприятия.
      security:
        - bearerAuth: []
      requestBody:
        required: true
        content:
          multipart/form-data:
            schema:
              type: object
              required:
                - file
              properties:
                file:
                  type: string
                  format: binary
                  description: Image file to process
                settings:
                  type: string
                  description: |
                    JSON string with options:
                    - `simulationType` (enum, default "deuteranomaly") -- Type of color vision deficiency to simulate. One of: protanopia, deuteranopia, tritanopia, protanomaly, deuteranomaly, tritanomaly, achromatopsia, blueConeMonochromacy
      responses:
        '200':
          description: Processed image
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ToolResponse'
        '400':
          description: Invalid input
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Error'
        '401':
          description: Authentication required
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/UnauthorizedError'
  /api/v1/tools/image/gif-tools:
    post:
      operationId: processGif
      tags:
        - Tools
      summary: Инструменты GIF
      description: Изменение размера, оптимизация, изменение скорости, реверс, извлечение кадров или поворот анимированных GIF.
      security:
        - bearerAuth: []
      requestBody:
        required: true
        content:
          multipart/form-data:
            schema:
              type: object
              required:
                - file
              properties:
                file:
                  type: string
                  format: binary
                  description: GIF file to process
                settings:
                  type: string
                  description: |
                    JSON string with options:
                    - `mode` (string, default "resize") - One of: resize, optimize, speed, reverse, extract, rotate
                    - `width` (number 1-16384, optional) - Target width in pixels (resize mode)
                    - `height` (number 1-16384, optional) - Target height in pixels (resize mode)
                    - `percentage` (number 1-500, optional) - Scale by percentage (resize mode)
                    - `colors` (number 2-256, default 256) - Color palette size (optimize mode)
                    - `dither` (number 0-1, default 1.0) - Dither amount (optimize mode)
                    - `effort` (number 1-10, default 7) - Compression effort (optimize mode)
                    - `speedFactor` (number 0.1-10, default 1.0) - Speed multiplier (speed mode)
                    - `extractMode` (string, default "single") - One of: single, range, all (extract mode)
                    - `frameNumber` (number, default 0) - Frame index to extract (extract/single mode)
                    - `frameStart` (number, default 0) - Start frame index (extract/range mode)
                    - `frameEnd` (number, optional) - End frame index (extract/range mode)
                    - `extractFormat` (string, default "png") - One of: png, webp (extract mode)
                    - `angle` (number, optional) - Rotation angle, one of: 90, 180, 270 (rotate mode)
                    - `flipH` (boolean, default false) - Flip horizontally (rotate mode)
                    - `flipV` (boolean, default false) - Flip vertically (rotate mode)
                    - `loop` (number 0-100, default 0) - Loop count, 0 = infinite
      responses:
        '200':
          description: Processed image
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ToolResponse'
        '400':
          description: Invalid input
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Error'
        '401':
          description: Authentication required
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/UnauthorizedError'
  /api/v1/tools/image/gif-tools/info:
    post:
      operationId: getGifInfo
      tags:
        - Tools
      summary: Информация о GIF
      description: Получение метаданных об анимированном GIF, включая размеры, количество кадров, задержки и длительность.
      security: []
      requestBody:
        required: true
        content:
          multipart/form-data:
            schema:
              type: object
              required:
                - file
              properties:
                file:
                  type: string
                  format: binary
                  description: GIF file to inspect
      responses:
        '200':
          description: GIF metadata
          content:
            application/json:
              schema:
                type: object
                properties:
                  width:
                    type: integer
                  height:
                    type: integer
                  pages:
                    type: integer
                    description: Number of frames
                  delay:
                    type: array
                    items:
                      type: integer
                    description: Per-frame delay in milliseconds
                  loop:
                    type: integer
                    description: Loop count (0 = infinite)
                  fileSize:
                    type: integer
                  duration:
                    type: integer
                    description: Total duration in milliseconds
        '400':
          description: Invalid input
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Error'
  /api/v1/tools/image/smart-crop:
    post:
      operationId: smartCrop
      tags:
        - Tools
      summary: Умная обрезка
      description: Умная обрезка с тремя режимами - фокус на объекте, фокус на лице или автообрезка.
      security:
        - bearerAuth: []
      requestBody:
        required: true
        content:
          multipart/form-data:
            schema:
              type: object
              required:
                - file
              properties:
                file:
                  type: string
                  format: binary
                  description: Image file to process
                settings:
                  type: string
                  description: |
                    JSON string with options:
                    - `mode` (string, default "subject") - One of: subject, face, trim, attention (alias for subject), content (alias for trim)
                    - `strategy` (string, default "attention") - One of: attention, entropy (subject mode)
                    - `width` (integer, optional) - Target width in pixels
                    - `height` (integer, optional) - Target height in pixels
                    - `padding` (integer 0-50, default 0) - Padding percentage around focus area
                    - `facePreset` (string, default "head-shoulders") - One of: closeup, head-shoulders, upper-body, half-body (face mode)
                    - `sensitivity` (number 0-1, default 0.5) - Face detection sensitivity (face mode)
                    - `threshold` (integer 0-255, default 30) - Trim tolerance (trim mode)
                    - `padToSquare` (boolean, default false) - Pad to square after trimming (trim mode)
                    - `padColor` (string, default "#ffffff") - Hex color for padding (trim mode)
                    - `targetSize` (integer, optional) - Target size for padded output (trim mode)
                    - `quality` (integer 1-100, optional) - Output quality
      responses:
        '200':
          description: Processed image
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ToolResponse'
        '400':
          description: Invalid input
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Error'
        '401':
          description: Authentication required
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/UnauthorizedError'
  /api/v1/tools/image/vectorize:
    post:
      operationId: vectorizeImage
      tags:
        - Tools
      summary: Изображение в SVG
      description: Конвертация растрового изображения в векторный формат SVG с помощью potrace (Ч/Б) или VTracer (цвет).
      security:
        - bearerAuth: []
      requestBody:
        required: true
        content:
          multipart/form-data:
            schema:
              type: object
              required:
                - file
              properties:
                file:
                  type: string
                  format: binary
                  description: Image file to process
                settings:
                  type: string
                  description: |
                    JSON string with options:
                    - `colorMode` (string, default "bw") - One of: bw, color
                    - `threshold` (number 0-255, default 128) - B&W binarization threshold
                    - `colorPrecision` (number 1-16, default 6) - Color bits per channel
                    - `layerDifference` (number 1-128, default 6) - Color gradient step
                    - `filterSpeckle` (number 1-256, default 4) - Noise filter size
                    - `pathMode` (string, default "spline") - One of: none, polygon, spline
                    - `cornerThreshold` (number 0-180, default 60) - Corner detection angle
                    - `invert` (boolean, default false) - Invert colors before tracing
      responses:
        '200':
          description: Processed image (downloadUrl points to .svg file)
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ToolResponse'
        '400':
          description: Invalid input
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Error'
        '401':
          description: Authentication required
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/UnauthorizedError'
  /api/v1/tools/image/svg-to-raster:
    post:
      operationId: svgToRaster
      tags:
        - Tools
      summary: SVG в растр
      description: Конвертация файла SVG в растровый формат изображения с заданным масштабом и DPI.
      security:
        - bearerAuth: []
      requestBody:
        required: true
        content:
          multipart/form-data:
            schema:
              type: object
              required:
                - file
              properties:
                file:
                  type: string
                  format: binary
                  description: SVG file to convert
                settings:
                  type: string
                  description: |
                    JSON string with options:
                    - `width` (number 1-65536, optional) - Output width in pixels
                    - `height` (number 1-65536, optional) - Output height in pixels
                    - `dpi` (number 36-2400, default 300) - Render density for SVG rasterization
                    - `quality` (number 1-100, default 90) - Output quality for lossy formats
                    - `backgroundColor` (hex string, default "#00000000") - Background color
                    - `outputFormat` (string, default "png") - One of: png, jpg, webp, avif, tiff, gif, heif, jxl
      responses:
        '200':
          description: Processed image
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ToolResponse'
        '400':
          description: Invalid input
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Error'
        '401':
          description: Authentication required
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/UnauthorizedError'
  /api/v1/tools/image/svg-to-raster/batch:
    post:
      operationId: svgToRasterBatch
      tags:
        - Tools
      summary: SVG в растр (пакетно)
      description: Конвертация нескольких файлов SVG в растровые изображения. Возвращает ZIP-архив.
      security:
        - bearerAuth: []
      requestBody:
        required: true
        content:
          multipart/form-data:
            schema:
              type: object
              required:
                - file
              properties:
                file:
                  type: array
                  items:
                    type: string
                    format: binary
                  description: SVG files to convert
                settings:
                  type: string
                  description: Same settings as single-file endpoint
                clientJobId:
                  type: string
                  description: Optional client-generated job ID for progress tracking
      responses:
        '200':
          description: ZIP archive containing processed images
          content:
            application/zip:
              schema:
                type: string
                format: binary
        '400':
          description: Invalid input
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Error'
        '401':
          description: Authentication required
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/UnauthorizedError'
        '422':
          description: All files failed processing
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Error'
  /api/v1/tools/image/image-to-pdf:
    post:
      operationId: imageToPdf
      tags:
        - Tools
      summary: Изображение в PDF
      description: Конвертация одного или нескольких изображений в PDF-документ.
      security:
        - bearerAuth: []
      requestBody:
        required: true
        content:
          multipart/form-data:
            schema:
              type: object
              required:
                - file
              properties:
                file:
                  type: array
                  items:
                    type: string
                    format: binary
                  description: One or more image files to include in the PDF
                settings:
                  type: string
                  description: |
                    JSON string with options:
                    - `pageSize` (string, default "A4") - One of: A4, Letter, A3, A5
                    - `orientation` (string, default "portrait") - One of: portrait, landscape
                    - `margin` (number 0-500, default 20) - Page margin in points
                    - `targetSize` (object, optional) - Target output file size
                      - `value` (number, required) - Positive number (decimals allowed)
                      - `unit` (string, required) - One of: KB, MB
                      Minimum effective target is 50KB. When set, images are compressed as JPEG with quality auto-tuned to meet the target.
                    - `collate` (boolean, default true) - When true, all images go into one PDF. When false, each image becomes a separate PDF and the result is a ZIP.
      responses:
        '200':
          description: Generated PDF (downloadUrl points to .pdf file)
          content:
            application/json:
              schema:
                allOf:
                  - $ref: '#/components/schemas/ToolResponse'
                  - type: object
                    properties:
                      pages:
                        type: integer
                        description: Number of images/pages in the PDF
                      compression:
                        type: object
                        description: Present only when targetSize was specified
                        properties:
                          targetRequested:
                            type: integer
                            description: Target size in bytes
                          targetMet:
                            type: boolean
                            description: Whether the target was achieved
                          jpegQuality:
                            type: integer
                            description: Final JPEG quality used (10-95)
        '400':
          description: Invalid input
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Error'
        '401':
          description: Authentication required
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/UnauthorizedError'
        '422':
          description: Processing failed
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Error'
  /api/v1/tools/pdf/pdf-to-image:
    post:
      operationId: pdfToImage
      tags:
        - Tools
      summary: PDF в изображение
      description: Конвертация страниц PDF в изображения. Возвращает скачивание отдельных страниц и ZIP со всеми страницами.
      security:
        - bearerAuth: []
      requestBody:
        required: true
        content:
          multipart/form-data:
            schema:
              type: object
              required:
                - file
              properties:
                file:
                  type: string
                  format: binary
                  description: PDF file to convert
                settings:
                  type: string
                  description: |
                    JSON string with options:
                    - `format` (string, default "png") - Output format: png, jpg, webp, avif, tiff, gif, heic, heif, jxl
                    - `dpi` (number 36-2400, default 150) - Resolution in dots per inch
                    - `quality` (number 1-100, default 85) - Output quality
                    - `colorMode` (string, default "color") - One of: color, grayscale, bw
                    - `pages` (string, default "all") - Page selection, e.g. "all", "1-3", "1,3,5"
      responses:
        '200':
          description: Converted pages
          content:
            application/json:
              schema:
                type: object
                properties:
                  jobId:
                    type: string
                  pageCount:
                    type: integer
                    description: Total pages in the PDF
                  selectedPages:
                    type: array
                    items:
                      type: integer
                  format:
                    type: string
                  pages:
                    type: array
                    items:
                      type: object
                      properties:
                        page:
                          type: integer
                        downloadUrl:
                          type: string
                        size:
                          type: integer
                  zipUrl:
                    type: string
                    description: URL to download all pages as ZIP
                  zipSize:
                    type: integer
        '400':
          description: Invalid input
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Error'
        '401':
          description: Authentication required
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/UnauthorizedError'
  /api/v1/tools/pdf/pdf-to-image/info:
    post:
      operationId: getPdfInfo
      tags:
        - Tools
      summary: Количество страниц PDF
      description: Получение количества страниц в файле PDF.
      security: []
      requestBody:
        required: true
        content:
          multipart/form-data:
            schema:
              type: object
              required:
                - file
              properties:
                file:
                  type: string
                  format: binary
                  description: PDF file to inspect
      responses:
        '200':
          description: PDF info
          content:
            application/json:
              schema:
                type: object
                properties:
                  pageCount:
                    type: integer
        '400':
          description: Invalid input
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Error'
  /api/v1/tools/pdf/pdf-to-image/preview:
    post:
      operationId: previewPdfPages
      tags:
        - Tools
      summary: Миниатюры страниц PDF
      description: Создание миниатюр предпросмотра для каждой страницы PDF (макс. 200 страниц, JPEG шириной 300px).
      security: []
      requestBody:
        required: true
        content:
          multipart/form-data:
            schema:
              type: object
              required:
                - file
              properties:
                file:
                  type: string
                  format: binary
                  description: PDF file to preview
      responses:
        '200':
          description: Page thumbnails
          content:
            application/json:
              schema:
                type: object
                properties:
                  pageCount:
                    type: integer
                  thumbnails:
                    type: array
                    items:
                      type: object
                      properties:
                        page:
                          type: integer
                        dataUrl:
                          type: string
                          description: Base64-encoded JPEG data URL
                        width:
                          type: integer
                        height:
                          type: integer
        '400':
          description: Invalid input
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Error'
  /api/v1/tools/image/split:
    post:
      operationId: splitImage
      tags:
        - Tools
      summary: Разделение изображения
      description: Разделение изображения на сетку из фрагментов. Возвращает ZIP-файл.
      security:
        - bearerAuth: []
      requestBody:
        required: true
        content:
          multipart/form-data:
            schema:
              type: object
              required:
                - file
              properties:
                file:
                  type: string
                  format: binary
                  description: Image file to split
                settings:
                  type: string
                  description: |
                    JSON string with options:
                    - `columns` (number 1-100, default 3) - Number of columns
                    - `rows` (number 1-100, default 3) - Number of rows
                    - `tileWidth` (number, min 10, optional) - Fixed tile width in pixels
                    - `tileHeight` (number, min 10, optional) - Fixed tile height in pixels
                    - `outputFormat` (string, default "original") - One of: original, png, jpg, webp, avif, jxl
                    - `quality` (number 1-100, default 90) - Output quality
      responses:
        '200':
          description: ZIP archive with results
          content:
            application/zip:
              schema:
                type: string
                format: binary
        '400':
          description: Invalid input
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Error'
        '401':
          description: Authentication required
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/UnauthorizedError'
  /api/v1/tools/image/bulk-rename:
    post:
      operationId: bulkRename
      tags:
        - Tools
      summary: Массовое переименование
      description: Переименование нескольких изображений по шаблону. Поддерживает токены {{index}}, {{padded}} и {{original}}. Возвращает ZIP-файл.
      security:
        - bearerAuth: []
      requestBody:
        required: true
        content:
          multipart/form-data:
            schema:
              type: object
              required:
                - file
              properties:
                file:
                  type: array
                  items:
                    type: string
                    format: binary
                  description: Multiple image files to rename
                settings:
                  type: string
                  description: |
                    JSON string with options:
                    - `pattern` (string 1-1000, default "image-{{index}}") - Naming pattern template
                    - `startIndex` (number, default 1) - Starting index number
      responses:
        '200':
          description: ZIP archive with results
          content:
            application/zip:
              schema:
                type: string
                format: binary
        '400':
          description: Invalid input
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Error'
        '401':
          description: Authentication required
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/UnauthorizedError'
  /api/v1/tools/image/favicon:
    post:
      operationId: generateFavicon
      tags:
        - Tools
      summary: Генератор favicon
      description: Создание полного набора favicon (PNG от 16px до 512px, ICO и manifest.json) из изображения. Возвращает ZIP-файл.
      security:
        - bearerAuth: []
      requestBody:
        required: true
        content:
          multipart/form-data:
            schema:
              type: object
              required:
                - file
              properties:
                file:
                  type: string
                  format: binary
                  description: Image file to use as favicon source
      responses:
        '200':
          description: ZIP archive with results
          content:
            application/zip:
              schema:
                type: string
                format: binary
        '400':
          description: Invalid input
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Error'
        '401':
          description: Authentication required
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/UnauthorizedError'
  /api/v1/tools/image/watermark-image:
    post:
      operationId: addImageWatermark
      tags:
        - Tools
      summary: Изображение-водяной знак
      description: Добавление наложения изображения-водяного знака.
      security:
        - bearerAuth: []
      requestBody:
        required: true
        content:
          multipart/form-data:
            schema:
              type: object
              required:
                - file
                - watermark
              properties:
                file:
                  type: string
                  format: binary
                  description: Main image file
                watermark:
                  type: string
                  format: binary
                  description: Watermark overlay image
                settings:
                  type: string
                  description: |
                    JSON string with options:
                    - `position` (string, default "bottom-right") - One of: center, top-left, top-right, bottom-left, bottom-right
                    - `opacity` (number 0-100, default 50) - Watermark opacity percentage
                    - `scale` (number 1-100, default 25) - Watermark size as percentage of base image
      responses:
        '200':
          description: Processed image
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ToolResponse'
        '400':
          description: Invalid input
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Error'
        '401':
          description: Authentication required
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/UnauthorizedError'
  /api/v1/tools/image/compose:
    post:
      operationId: composeImages
      tags:
        - Tools
      summary: Композиция изображений
      description: Наложение изображения поверх базового изображения с режимами смешивания.
      security:
        - bearerAuth: []
      requestBody:
        required: true
        content:
          multipart/form-data:
            schema:
              type: object
              required:
                - file
                - overlay
              properties:
                file:
                  type: string
                  format: binary
                  description: Base image file
                overlay:
                  type: string
                  format: binary
                  description: Overlay image file
                settings:
                  type: string
                  description: |
                    JSON string with options:
                    - `x` (number, default 0) - Horizontal offset of overlay
                    - `y` (number, default 0) - Vertical offset of overlay
                    - `opacity` (number 0-100, default 100) - Overlay opacity percentage
                    - `blendMode` (string, default "over") - One of: over, multiply, screen, overlay, darken, lighten, hard-light, soft-light, difference, exclusion
      responses:
        '200':
          description: Processed image
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ToolResponse'
        '400':
          description: Invalid input
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Error'
        '401':
          description: Authentication required
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/UnauthorizedError'
  /api/v1/tools/image/meme-generator:
    post:
      operationId: generateMeme
      tags:
        - Tools
      summary: Генератор мемов
      description: |
        Создание мемов с помощью шаблонов или собственных изображений. Поддерживает два режима: (1) Режим шаблона - отправьте JSON body с `templateId` и `textBoxes`. (2) Режим собственного изображения - отправьте multipart с `file` и `settings`.
      security:
        - bearerAuth: []
      requestBody:
        required: true
        content:
          multipart/form-data:
            schema:
              type: object
              properties:
                file:
                  type: string
                  format: binary
                  description: Custom image file (optional if using templateId)
                settings:
                  type: string
                  description: |
                    JSON string with options:
                    - `templateId` (string, optional) - ID of a meme template from /api/v1/meme-templates
                    - `textLayout` (string, default "top-bottom") - One of: top-bottom, top-only, bottom-only, center, side-by-side
                    - `textBoxes` (array of {id, text}) - Text content for each position
                    - `fontFamily` (string, default "anton") - One of: anton, arial-black, comic-sans, montserrat, bebas-neue, permanent-marker, roboto
                    - `fontSize` (number 8-200, optional) - Font size in pixels
                    - `textColor` (hex string, default "#ffffff") - Text color
                    - `strokeColor` (hex string, default "#000000") - Text outline color
                    - `textAlign` (string, default "center") - One of: left, center, right
                    - `allCaps` (boolean, default true) - Convert text to uppercase
          application/json:
            schema:
              type: object
              description: Template mode - send templateId and textBoxes as JSON body
              properties:
                templateId:
                  type: string
                  description: ID of a meme template
                textBoxes:
                  type: array
                  items:
                    type: object
                    properties:
                      id:
                        type: string
                      text:
                        type: string
      responses:
        '200':
          description: Processed meme image
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ToolResponse'
        '400':
          description: Invalid input or template not found
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Error'
        '401':
          description: Authentication required
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/UnauthorizedError'
        '422':
          description: Processing failed
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Error'
  /api/v1/meme-templates:
    get:
      operationId: listMemeTemplates
      tags:
        - Tools
      summary: Список шаблонов мемов
      description: Возвращает полный манифест доступных шаблонов мемов, включая позиции текстовых блоков.
      security:
        - bearerAuth: []
      responses:
        '200':
          description: Meme template manifest
          content:
            application/json:
              schema:
                type: object
                properties:
                  templates:
                    type: array
                    items:
                      type: object
                      properties:
                        id:
                          type: string
                        filename:
                          type: string
                        width:
                          type: integer
                        height:
                          type: integer
                        textBoxes:
                          type: array
                          items:
                            type: object
                            properties:
                              id:
                                type: string
                              x:
                                type: number
                              'y':
                                type: number
                              width:
                                type: number
                              height:
                                type: number
                              defaultText:
                                type: string
        '401':
          description: Authentication required
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/UnauthorizedError'
  /api/v1/meme-templates/full/{filename}:
    get:
      operationId: getMemeTemplateImage
      tags:
        - Tools
      summary: Получить изображение шаблона мема
      description: Отдаёт полноразмерное изображение шаблона мема.
      security:
        - bearerAuth: []
      parameters:
        - name: filename
          in: path
          required: true
          schema:
            type: string
          description: Template image filename
      responses:
        '200':
          description: Template image
          content:
            image/*:
              schema:
                type: string
                format: binary
        '400':
          description: Invalid filename
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Error'
        '404':
          description: Template not found
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Error'
  /api/v1/meme-templates/thumbs/{filename}:
    get:
      operationId: getMemeTemplateThumbnail
      tags:
        - Tools
      summary: Получить миниатюру шаблона мема
      description: Отдаёт миниатюру шаблона мема.
      security:
        - bearerAuth: []
      parameters:
        - name: filename
          in: path
          required: true
          schema:
            type: string
          description: Template thumbnail filename
      responses:
        '200':
          description: Thumbnail image
          content:
            image/*:
              schema:
                type: string
                format: binary
        '400':
          description: Invalid filename
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Error'
        '404':
          description: Thumbnail not found
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Error'
  /api/v1/meme-templates/fonts/{filename}:
    get:
      operationId: getMemeTemplateFont
      tags:
        - Tools
      summary: Получить шрифт шаблона мема
      description: Отдаёт файл шрифта, используемый генератором мемов.
      security:
        - bearerAuth: []
      parameters:
        - name: filename
          in: path
          required: true
          schema:
            type: string
          description: Font filename (e.g. anton.ttf)
      responses:
        '200':
          description: Font file
          content:
            font/ttf:
              schema:
                type: string
                format: binary
        '400':
          description: Invalid filename
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Error'
        '404':
          description: Font not found
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Error'
  /api/v1/tools/image/compare:
    post:
      operationId: compareImages
      tags:
        - Tools
      summary: Сравнение изображений
      description: Сравнение двух изображений и создание визуального отчёта о различиях.
      security:
        - bearerAuth: []
      requestBody:
        required: true
        content:
          multipart/form-data:
            schema:
              type: object
              required:
                - file1
                - file2
              properties:
                file1:
                  type: string
                  format: binary
                  description: First image to compare
                file2:
                  type: string
                  format: binary
                  description: Second image to compare
      responses:
        '200':
          description: Comparison result
          content:
            application/json:
              schema:
                allOf:
                  - $ref: '#/components/schemas/ToolResponse'
                  - type: object
                    properties:
                      similarity:
                        type: number
                        description: Similarity percentage (0-100)
                      dimensions:
                        type: object
                        properties:
                          width:
                            type: integer
                          height:
                            type: integer
        '400':
          description: Invalid input
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Error'
        '401':
          description: Authentication required
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/UnauthorizedError'
        '422':
          description: Processing failed
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Error'
  /api/v1/tools/image/erase-object:
    post:
      operationId: eraseObject
      tags:
        - Tools
      summary: Удаление объектов
      description: Удаление объектов с изображения с помощью маски. Белые области маски обозначают участки для удаления. Использует инпейнтинг LaMa.
      security:
        - bearerAuth: []
      requestBody:
        required: true
        content:
          multipart/form-data:
            schema:
              type: object
              required:
                - image
                - mask
              properties:
                image:
                  type: string
                  format: binary
                  description: Source image file
                mask:
                  type: string
                  format: binary
                  description: Mask image (white areas will be erased)
                format:
                  type: string
                  enum:
                    - auto
                    - png
                    - jpg
                    - jpeg
                    - webp
                    - tiff
                    - gif
                    - avif
                    - heic
                    - heif
                    - jxl
                  default: auto
                  description: Output format
                quality:
                  type: integer
                  minimum: 1
                  maximum: 100
                  default: 95
                  description: Output quality
                clientJobId:
                  type: string
                  description: Client-provided job ID for SSE progress tracking
      responses:
        '202':
          description: Accepted for async processing. Track progress via SSE at /api/v1/jobs/{jobId}/progress.
          content:
            application/json:
              schema:
                type: object
                properties:
                  jobId:
                    type: string
                  async:
                    type: boolean
                    example: true
        '400':
          description: Invalid input
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Error'
        '401':
          description: Authentication required
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/UnauthorizedError'
        '422':
          description: Processing failed
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Error'
        '501':
          description: Feature not installed
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/FeatureNotInstalledError'
  /api/v1/tools/image/ai-canvas-expand:
    post:
      operationId: aiCanvasExpand
      tags:
        - Tools
      summary: AI-расширение холста (экспериментальное)
      description: |
        Расширение холста изображения с помощью AI outpainting. Достраивает
        изображение в любом направлении, генерируя новое содержимое, которое
        бесшовно сочетается с оригиналом. Использует outpainting на базе LaMa.
        Требует установки набора функций ai-canvas-expand.
      security:
        - bearerAuth: []
      requestBody:
        required: true
        content:
          multipart/form-data:
            schema:
              type: object
              required:
                - file
              properties:
                file:
                  type: string
                  format: binary
                  description: Image file to expand
                settings:
                  type: string
                  description: |
                    JSON string with options:
                    - `extendTop` (integer >= 0, default 0) - Pixels to extend upward
                    - `extendRight` (integer >= 0, default 0) - Pixels to extend rightward
                    - `extendBottom` (integer >= 0, default 0) - Pixels to extend downward
                    - `extendLeft` (integer >= 0, default 0) - Pixels to extend leftward
                    - `tier` (string, default "balanced") - One of: fast, balanced, high
                    - `format` (string, default "auto") - One of: auto, png, jpg, jpeg, webp, tiff, gif, avif, heic, heif, jxl
                    - `quality` (integer 1-100, default 95) - Output quality

                    At least one extend direction must be greater than 0.
                clientJobId:
                  type: string
                  description: Client-provided job ID for SSE progress tracking
      responses:
        '202':
          description: Accepted for async processing. Track progress via SSE at /api/v1/jobs/{jobId}/progress.
          content:
            application/json:
              schema:
                type: object
                properties:
                  jobId:
                    type: string
                  async:
                    type: boolean
                    example: true
        '400':
          description: Invalid input (or no extend direction > 0)
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Error'
        '401':
          description: Authentication required
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/UnauthorizedError'
        '422':
          description: Processing failed
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Error'
        '501':
          description: Feature not installed
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/FeatureNotInstalledError'
  /api/v1/tools/image/collage:
    post:
      operationId: createCollage
      tags:
        - Tools
      summary: Коллаж / сетка
      description: Объединение нескольких изображений в коллаж-сетку.
      security:
        - bearerAuth: []
      requestBody:
        required: true
        content:
          multipart/form-data:
            schema:
              type: object
              required:
                - file
              properties:
                file:
                  type: array
                  items:
                    type: string
                    format: binary
                  description: Multiple image files to combine
                settings:
                  type: string
                  description: |
                    JSON string with options:
                    - `templateId` (string, required) - Template ID defining the grid layout
                    - `cells` (array, optional) - Per-cell configuration overrides
                    - `gap` (number 0-500, default 8) - Gap between images in pixels
                    - `cornerRadius` (number 0-500, default 0) - Corner rounding radius
                    - `backgroundColor` (string, default "#FFFFFF") - Background fill color
                    - `aspectRatio` (string, default "free") - Aspect ratio constraint
                    - `outputFormat` (string, default "png") - One of: png, jpeg, webp, avif, jxl
                    - `quality` (number 1-100, default 90) - Output quality
      responses:
        '200':
          description: Processed image
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ToolResponse'
        '400':
          description: Invalid input
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Error'
        '401':
          description: Authentication required
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/UnauthorizedError'
  /api/v1/tools/image/stitch:
    post:
      operationId: stitchImages
      tags:
        - Tools
      summary: Сшивка изображений
      description: Соединение нескольких изображений по горизонтали, вертикали или в виде сетки.
      security:
        - bearerAuth: []
      requestBody:
        required: true
        content:
          multipart/form-data:
            schema:
              type: object
              required:
                - file
              properties:
                file:
                  type: array
                  items:
                    type: string
                    format: binary
                  description: Two or more image files to stitch together
                settings:
                  type: string
                  description: |
                    JSON string with options:
                    - `direction` (string, default "horizontal") - One of: horizontal, vertical, grid
                    - `gridColumns` (integer 2-100, default 2) - Columns when direction is grid
                    - `resizeMode` (string, default "fit") - One of: fit, original, stretch, crop
                    - `alignment` (string, default "center") - One of: start, center, end
                    - `gap` (number 0-1000, default 0) - Gap between images in pixels
                    - `border` (number 0-500, default 0) - Border width in pixels
                    - `cornerRadius` (number 0-500, default 0) - Corner radius in pixels
                    - `backgroundColor` (hex string, default "#FFFFFF") - Hex color for background and gap fill
                    - `format` (string, default "png") - One of: png, jpeg, webp, avif, jxl
                    - `quality` (number 1-100, default 90) - Output quality
      responses:
        '200':
          description: Stitched image
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ToolResponse'
        '400':
          description: Invalid input
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Error'
        '401':
          description: Authentication required
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/UnauthorizedError'
  /api/v1/tools/image/remove-gif-background:
    post:
      operationId: removeGifBackground
      tags:
        - Tools
      summary: Удаление фона из анимированного изображения
      description: Удалите фон из анимированного GIF, WebP или APNG покадрово с помощью AI (rembg). Запускается локально.
      security:
        - bearerAuth: []
      requestBody:
        required: true
        content:
          multipart/form-data:
            schema:
              type: object
              required:
                - file
              properties:
                file:
                  type: string
                  format: binary
                  description: Animated image file (GIF, animated WebP, or APNG)
                settings:
                  type: string
                  description: |
                    JSON string with options:
                    - `model` (string, optional) - AI model name
                    - `outputFormat` (string, optional) - One of: webp, apng, gif
                    - `backgroundType` (string, optional) - One of: transparent, color, gradient, blur, image
                    - `backgroundColor` (string, optional) - Hex color for the color background
                    - `gradientColor1` (string, optional) - First gradient color
                    - `gradientColor2` (string, optional) - Second gradient color
                    - `gradientAngle` (number, optional) - Gradient angle in degrees
                    - `blurIntensity` (number 0-100, optional) - Blur strength for the blur background
                    - `shadowEnabled` (boolean, optional) - Enable drop shadow
                    - `shadowOpacity` (number 0-100, optional) - Shadow opacity
                    - `edgeRefine` (number 0-3, optional) - Edge refinement level
                    - `decontaminate` (boolean, optional) - Remove background color spill
                backgroundImage:
                  type: string
                  format: binary
                  description: Background image, required when backgroundType is "image"
                clientJobId:
                  type: string
                  description: Client-provided job ID for SSE progress tracking
      responses:
        '202':
          description: Accepted for async processing. Track progress via SSE at /api/v1/jobs/{jobId}/progress.
          content:
            application/json:
              schema:
                type: object
                properties:
                  jobId:
                    type: string
                  async:
                    type: boolean
                    example: true
        '400':
          description: Invalid input (not animated, over the frame cap, or bad settings)
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Error'
        '401':
          description: Authentication required
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/UnauthorizedError'
        '501':
          description: Feature not installed
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/FeatureNotInstalledError'
  /api/v1/tools/image/remove-background:
    post:
      operationId: removeBackground
      tags:
        - Tools
      summary: Удаление фона
      description: Удаление фона с изображения с помощью AI (rembg). Работает локально.
      security:
        - bearerAuth: []
      requestBody:
        required: true
        content:
          multipart/form-data:
            schema:
              type: object
              required:
                - file
              properties:
                file:
                  type: string
                  format: binary
                  description: Image file to process
                settings:
                  type: string
                  description: |
                    JSON string with options:
                    - `model` (string, optional) - AI model name
                    - `backgroundType` (string, optional) - One of: transparent, color, gradient, blur, image
                    - `backgroundColor` (string, optional) - Hex color to replace removed background with
                    - `gradientColor1` (string, optional) - First gradient color
                    - `gradientColor2` (string, optional) - Second gradient color
                    - `gradientAngle` (number, optional) - Gradient angle in degrees
                    - `blurEnabled` (boolean, optional) - Enable background blur effect
                    - `blurIntensity` (number 0-100, optional) - Blur strength
                    - `shadowEnabled` (boolean, optional) - Enable drop shadow
                    - `shadowOpacity` (number 0-100, optional) - Shadow opacity
                clientJobId:
                  type: string
                  description: Client-provided job ID for SSE progress tracking
      responses:
        '202':
          description: Accepted for async processing. Track progress via SSE at /api/v1/jobs/{jobId}/progress.
          content:
            application/json:
              schema:
                type: object
                properties:
                  jobId:
                    type: string
                  async:
                    type: boolean
                    example: true
        '400':
          description: Invalid input
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Error'
        '401':
          description: Authentication required
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/UnauthorizedError'
        '422':
          description: Processing failed
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Error'
        '501':
          description: Feature not installed
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/FeatureNotInstalledError'
  /api/v1/tools/image/remove-background/effects:
    post:
      operationId: applyBackgroundEffects
      tags:
        - Tools
      summary: Применение эффектов фона
      description: |
        Применение замены фона или эффектов к ранее обработанному результату удаления фона. Требует jobId из предыдущего вызова POST /api/v1/tools/image/remove-background.
      security:
        - bearerAuth: []
      requestBody:
        required: true
        content:
          multipart/form-data:
            schema:
              type: object
              required:
                - settings
              properties:
                backgroundImage:
                  type: string
                  format: binary
                  description: Custom background image (required when backgroundType is "image")
                settings:
                  type: string
                  description: |
                    JSON string with options:
                    - `jobId` (string, required) - Job ID from the initial remove-background call
                    - `filename` (string, required) - Original filename
                    - `backgroundType` (string) - One of: transparent, color, gradient, blur, image
                    - `backgroundColor` (string) - Hex color for solid background
                    - `gradientColor1` (string) - First gradient color
                    - `gradientColor2` (string) - Second gradient color
                    - `gradientAngle` (number) - Gradient angle in degrees
                    - `blurEnabled` (boolean) - Enable background blur effect
                    - `blurIntensity` (number 0-100) - Blur strength
                    - `shadowEnabled` (boolean) - Enable drop shadow
                    - `shadowOpacity` (number 0-100) - Shadow opacity
      responses:
        '200':
          description: Image with applied effects
          content:
            application/json:
              schema:
                type: object
                properties:
                  jobId:
                    type: string
                  downloadUrl:
                    type: string
                  processedSize:
                    type: integer
        '400':
          description: Invalid input
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Error'
        '401':
          description: Authentication required
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/UnauthorizedError'
  /api/v1/tools/image/transparency-fixer:
    post:
      operationId: fixTransparency
      tags:
        - Tools
      summary: Исправление ложной прозрачности
      description: |
        Исправление PNG с "ложной прозрачностью", у которых есть окантовка, ореолы
        или полупрозрачные артефакты от предыдущего удаления фона. Использует
        BiRefNet HR-matting (2048x2048) для создания чистого альфа-канала с
        настраиваемой обработкой окантовки. При нехватке памяти переходит на
        birefnet-general, затем на u2net. Требует установки набора функций
        background-removal.
      security:
        - bearerAuth: []
      requestBody:
        required: true
        content:
          multipart/form-data:
            schema:
              type: object
              required:
                - file
              properties:
                file:
                  type: string
                  format: binary
                  description: PNG image with fake or damaged transparency
                settings:
                  type: string
                  description: |
                    JSON string with options:
                    - `defringe` (number 0-100, default 30) - Edge defringe strength to remove color contamination
                    - `outputFormat` (string, default "png") - One of: png, webp
                    - `removeWatermark` (boolean, default false) - Apply median filter to reduce watermark artifacts before matting
                clientJobId:
                  type: string
                  description: Client-provided job ID for SSE progress tracking
      responses:
        '202':
          description: Accepted for async processing. Track progress via SSE at /api/v1/jobs/{jobId}/progress.
          content:
            application/json:
              schema:
                type: object
                properties:
                  jobId:
                    type: string
                  async:
                    type: boolean
                    example: true
        '400':
          description: Invalid input
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Error'
        '401':
          description: Authentication required
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/UnauthorizedError'
        '422':
          description: Processing failed
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Error'
        '501':
          description: Feature not installed
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/FeatureNotInstalledError'
  /api/v1/tools/image/upscale:
    post:
      operationId: upscaleImage
      tags:
        - Tools
      summary: Увеличение изображения
      description: Увеличение изображения с помощью AI (Real-ESRGAN). Работает локально.
      security:
        - bearerAuth: []
      requestBody:
        required: true
        content:
          multipart/form-data:
            schema:
              type: object
              required:
                - file
              properties:
                file:
                  type: string
                  format: binary
                  description: Image file to upscale
                settings:
                  type: string
                  description: |
                    JSON string with options:
                    - `scale` (number, default 2) - Upscale factor
                    - `model` (string, default "auto") - AI model name
                    - `faceEnhance` (boolean, default false) - Enable face enhancement
                    - `denoise` (number, default 0) - Denoise strength
                    - `format` (string, default "auto") - Output format (auto, png, jpg, jpeg, webp, tiff, gif, avif, heic, heif, jxl)
                    - `quality` (number, default 95) - Output quality (1-100)
                clientJobId:
                  type: string
                  description: Client-provided job ID for SSE progress tracking
      responses:
        '202':
          description: Accepted for async processing. Track progress via SSE at /api/v1/jobs/{jobId}/progress.
          content:
            application/json:
              schema:
                type: object
                properties:
                  jobId:
                    type: string
                  async:
                    type: boolean
                    example: true
        '400':
          description: Invalid input
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Error'
        '401':
          description: Authentication required
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/UnauthorizedError'
        '422':
          description: Processing failed
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Error'
        '501':
          description: Feature not installed
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/FeatureNotInstalledError'
  /api/v1/tools/image/blur-faces:
    post:
      operationId: blurFaces
      tags:
        - Tools
      summary: Размытие лиц
      description: Обнаружение и размытие лиц на изображении для приватности. Использует OpenCV. Работает локально.
      security:
        - bearerAuth: []
      requestBody:
        required: true
        content:
          multipart/form-data:
            schema:
              type: object
              required:
                - file
              properties:
                file:
                  type: string
                  format: binary
                  description: Image file to process
                settings:
                  type: string
                  description: |
                    JSON string with options:
                    - `blurRadius` (number 1-100, default 30) - Blur strength radius
                    - `sensitivity` (number 0-1, default 0.5) - Face detection sensitivity
                clientJobId:
                  type: string
                  description: Client-provided job ID for SSE progress tracking
      responses:
        '202':
          description: Accepted for async processing. Track progress via SSE at /api/v1/jobs/{jobId}/progress.
          content:
            application/json:
              schema:
                type: object
                properties:
                  jobId:
                    type: string
                  async:
                    type: boolean
                    example: true
        '400':
          description: Invalid input
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Error'
        '401':
          description: Authentication required
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/UnauthorizedError'
        '422':
          description: Processing failed
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Error'
        '501':
          description: Feature not installed
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/FeatureNotInstalledError'
  /api/v1/tools/image/ocr:
    post:
      operationId: extractText
      tags:
        - Tools
      summary: OCR / извлечение текста
      description: |-
        Извлеките текст из изображения локально. Встроенный уровень Fast использует
        Tesseract. Balanced и Best используют дополнительную подписанную среду выполнения RapidOCR.
        с закрепленными моделями PP-OCR ONNX. Явные запросы уровня никогда не выполняются молча
        перейти на другой уровень.
      security:
        - bearerAuth: []
      requestBody:
        required: true
        content:
          multipart/form-data:
            schema:
              type: object
              required:
                - file
              properties:
                file:
                  type: string
                  format: binary
                  maxLength: 536870912
                  description: Image file to extract text from
                settings:
                  type: string
                  description: |
                    JSON string with options:
                    - `quality` (string, optional) - One of: fast, balanced, best. When both quality and engine are omitted, uses the best available tier in this order: best, balanced, fast. Korean never selects fast; it uses best, then balanced, or returns an accurate-runtime install/compatibility error
                    - `language` (string, default "auto") - One of: auto, en, de, fr, es, zh, ja, ko. Korean is unsupported by fast and the legacy tesseract alias
                    - `enhance` (boolean, optional) - Improve local contrast. Fast applies it directly; accurate tiers retain the variant only when calibrated scoring improves the result. Defaults to true for Best and false for Fast/Balanced
                    - `engine` (string, deprecated) - Compatibility alias: tesseract maps to fast; the legacy paddleocr value maps to balanced but does not load PaddlePaddle
                clientJobId:
                  type: string
      responses:
        '202':
          description: Accepted after validation and enqueueing. Track progress via SSE at /api/v1/jobs/{jobId}/progress; processing failures arrive in the terminal failed event.
          content:
            application/json:
              schema:
                type: object
                required:
                  - jobId
                  - async
                properties:
                  jobId:
                    type: string
                  progressJobId:
                    type: string
                  artifactJobId:
                    type: string
                  async:
                    type: boolean
                    const: true
        '400':
          description: Invalid input
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Error'
        '401':
          description: Authentication required
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/UnauthorizedError'
        '413':
          description: Encoded OCR input exceeds the hard 512 MiB safety limit
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Error'
        '501':
          description: Requested Balanced/Best runtime is not installed or is incompatible
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/FeatureNotInstalledError'
        '503':
          description: OCR could not be queued
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Error'
  /api/v1/tools/image/info:
    post:
      operationId: getImageInfo
      tags:
        - Tools
      summary: Информация об изображении
      description: Получение подробных метаданных об изображении, включая размеры, формат, цветовое пространство и данные EXIF.
      security:
        - bearerAuth: []
      requestBody:
        required: true
        content:
          multipart/form-data:
            schema:
              type: object
              required:
                - file
              properties:
                file:
                  type: string
                  format: binary
                  description: Image file to inspect
      responses:
        '200':
          description: Image metadata
          content:
            application/json:
              schema:
                type: object
                properties:
                  width:
                    type: integer
                  height:
                    type: integer
                  filename:
                    type: string
                  format:
                    type: string
                  colorSpace:
                    type: string
                  channels:
                    type: integer
                  bitDepth:
                    type: string
                  density:
                    type:
                      - integer
                      - 'null'
                  hasAlpha:
                    type: boolean
                  fileSize:
                    type: integer
                  hasExif:
                    type: boolean
                  isProgressive:
                    type: boolean
                  orientation:
                    type:
                      - integer
                      - 'null'
                  hasProfile:
                    type: boolean
                  hasIcc:
                    type: boolean
                  hasXmp:
                    type: boolean
                  pages:
                    type: integer
                  histogram:
                    type: array
                    items:
                      type: object
                      properties:
                        channel:
                          type: string
                        min:
                          type: number
                        max:
                          type: number
                        mean:
                          type: number
                        stdev:
                          type: number
        '400':
          description: Invalid input
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Error'
        '401':
          description: Authentication required
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/UnauthorizedError'
        '422':
          description: Processing failed
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Error'
  /api/v1/tools/image/color-palette:
    post:
      operationId: extractColorPalette
      tags:
        - Tools
      summary: Цветовая палитра
      description: Извлечение доминирующих цветов из изображения.
      security:
        - bearerAuth: []
      requestBody:
        required: true
        content:
          multipart/form-data:
            schema:
              type: object
              required:
                - file
              properties:
                file:
                  type: string
                  format: binary
                  description: Image file to analyze
      responses:
        '200':
          description: Dominant colors
          content:
            application/json:
              schema:
                type: object
                properties:
                  filename:
                    type: string
                  colors:
                    type: array
                    items:
                      type: string
                    description: Hex color strings
                  count:
                    type: integer
        '400':
          description: Invalid input
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Error'
        '401':
          description: Authentication required
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/UnauthorizedError'
  /api/v1/tools/image/barcode-read:
    post:
      operationId: readBarcode
      tags:
        - Tools
      summary: Чтение штрихкодов
      description: Чтение QR-кодов и штрихкодов с изображения.
      security:
        - bearerAuth: []
      requestBody:
        required: true
        content:
          multipart/form-data:
            schema:
              type: object
              required:
                - file
              properties:
                file:
                  type: string
                  format: binary
                  description: Image file containing barcode or QR code
                settings:
                  type: string
                  description: |
                    JSON string with options:
                    - `tryHarder` (boolean, default true) - Enable more aggressive barcode detection
      responses:
        '200':
          description: Decoded barcode data
          content:
            application/json:
              schema:
                type: object
                properties:
                  filename:
                    type: string
                  barcodes:
                    type: array
                    items:
                      type: object
                      properties:
                        type:
                          type: string
                        text:
                          type: string
                        position:
                          type: object
                          properties:
                            topLeft:
                              type: object
                              properties:
                                x:
                                  type: number
                                'y':
                                  type: number
                            topRight:
                              type: object
                              properties:
                                x:
                                  type: number
                                'y':
                                  type: number
                            bottomLeft:
                              type: object
                              properties:
                                x:
                                  type: number
                                'y':
                                  type: number
                            bottomRight:
                              type: object
                              properties:
                                x:
                                  type: number
                                'y':
                                  type: number
                  annotatedUrl:
                    type:
                      - string
                      - 'null'
                  previewUrl:
                    type:
                      - string
                      - 'null'
        '400':
          description: Invalid input
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Error'
        '401':
          description: Authentication required
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/UnauthorizedError'
        '422':
          description: Processing failed
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Error'
  /api/v1/tools/image/find-duplicates:
    post:
      operationId: findDuplicates
      tags:
        - Tools
      summary: Поиск дубликатов
      description: Поиск дубликатов изображений в наборе с помощью перцептивного хеширования.
      security:
        - bearerAuth: []
      requestBody:
        required: true
        content:
          multipart/form-data:
            schema:
              type: object
              required:
                - file
              properties:
                file:
                  type: array
                  items:
                    type: string
                    format: binary
                  description: Multiple image files to check for duplicates
                settings:
                  type: string
                  description: |
                    JSON string with options:
                    - `threshold` (number 0-20, default 8) - Hamming distance threshold for duplicate detection
      responses:
        '200':
          description: Duplicate groups
          content:
            application/json:
              schema:
                type: object
                properties:
                  totalImages:
                    type: integer
                  duplicateGroups:
                    type: array
                    items:
                      type: object
                      properties:
                        groupId:
                          type: integer
                        files:
                          type: array
                          items:
                            type: object
                            properties:
                              filename:
                                type: string
                              similarity:
                                type: number
                              width:
                                type: integer
                              height:
                                type: integer
                              fileSize:
                                type: integer
                              format:
                                type: string
                              isBest:
                                type: boolean
                              thumbnail:
                                type:
                                  - string
                                  - 'null'
                  uniqueImages:
                    type: integer
                  spaceSaveable:
                    type: integer
                  skippedFiles:
                    type: array
                    description: Files that could not be processed (present only when non-empty)
                    items:
                      type: object
                      properties:
                        filename:
                          type: string
                        reason:
                          type: string
        '400':
          description: Invalid input
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Error'
        '401':
          description: Authentication required
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/UnauthorizedError'
        '422':
          description: Processing failed
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Error'
  /api/v1/tools/image/qr-generate:
    post:
      operationId: generateQrCode
      tags:
        - Tools
      summary: Генератор QR-кодов
      description: Создание изображения QR-кода из текста. Этот инструмент принимает JSON body, а не multipart.
      security:
        - bearerAuth: []
      requestBody:
        required: true
        content:
          application/json:
            schema:
              type: object
              required:
                - text
              properties:
                text:
                  type: string
                  maxLength: 2000
                  description: Text to encode in the QR code
                size:
                  type: integer
                  minimum: 100
                  maximum: 10000
                  default: 400
                  description: Image size in pixels
                errorCorrection:
                  type: string
                  enum:
                    - L
                    - M
                    - Q
                    - H
                  default: M
                foreground:
                  type: string
                  default: '#000000'
                  description: Foreground color (hex)
                background:
                  type: string
                  default: '#FFFFFF'
                  description: Background color (hex)
      responses:
        '200':
          description: Generated QR code
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ToolResponse'
        '400':
          description: Invalid input
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Error'
        '401':
          description: Authentication required
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/UnauthorizedError'
  /api/v1/tools/image/html-to-image:
    post:
      operationId: captureWebpage
      tags:
        - Tools
      summary: Снимок веб-страницы как изображение
      description: Снимок веб-страницы как высококачественного изображения-скриншота. Принимает JSON body с URL и опциями захвата.
      security:
        - bearerAuth: []
      requestBody:
        required: true
        content:
          application/json:
            schema:
              type: object
              required:
                - url
              properties:
                url:
                  type: string
                  format: uri
                  description: URL to capture (http/https only)
                format:
                  type: string
                  enum:
                    - jpg
                    - png
                    - webp
                  default: png
                quality:
                  type: integer
                  minimum: 1
                  maximum: 100
                  default: 90
                fullPage:
                  type: boolean
                  default: false
                devicePreset:
                  type: string
                  enum:
                    - desktop
                    - tablet
                    - mobile
                    - custom
                  default: desktop
                viewportWidth:
                  type: integer
                  minimum: 320
                  maximum: 3840
                  default: 1280
                viewportHeight:
                  type: integer
                  minimum: 320
                  maximum: 2160
                  default: 720
      responses:
        '200':
          description: Screenshot captured successfully
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ToolResponse'
        '400':
          description: Invalid URL or parameters
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Error'
        '401':
          description: Authentication required
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/UnauthorizedError'
        '503':
          description: Browser service unavailable
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Error'
        '504':
          description: Page load timeout
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Error'
  /api/v1/tools/image/strip-metadata/inspect:
    post:
      operationId: inspectMetadata
      tags:
        - Tools
      summary: Просмотр метаданных
      description: Просмотр всех метаданных (EXIF, GPS, ICC, XMP) в изображении без их удаления.
      security:
        - bearerAuth: []
      requestBody:
        required: true
        content:
          multipart/form-data:
            schema:
              type: object
              required:
                - file
              properties:
                file:
                  type: string
                  format: binary
                  description: Image file to inspect
      responses:
        '200':
          description: Image metadata
          content:
            application/json:
              schema:
                type: object
                properties:
                  filename:
                    type: string
                  fileSize:
                    type: integer
                  exif:
                    type: object
                    description: EXIF metadata
                  exifError:
                    type: string
                    description: Present when EXIF parsing fails
                  gps:
                    type: object
                    description: GPS metadata
                  icc:
                    type: object
                    description: ICC profile metadata
                  xmp:
                    type: object
                    description: XMP metadata
        '400':
          description: Invalid input
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Error'
        '401':
          description: Authentication required
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/UnauthorizedError'
        '422':
          description: Failed to read image metadata
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Error'
  /api/v1/tools/image/edit-metadata:
    post:
      operationId: editMetadata
      tags:
        - Tools
      summary: Редактирование метаданных
      description: Редактирование полей метаданных EXIF, IPTC, GPS и других в изображении с помощью ExifTool.
      security:
        - bearerAuth: []
      requestBody:
        required: true
        content:
          multipart/form-data:
            schema:
              type: object
              required:
                - file
              properties:
                file:
                  type: string
                  format: binary
                  description: Image file to process
                settings:
                  type: string
                  description: |
                    JSON string with options:
                    - `title` (string) - Image title
                    - `author` (string) - Author name
                    - `artist` (string) - Artist or author name
                    - `copyright` (string) - Copyright notice
                    - `imageDescription` (string) - Image description
                    - `software` (string) - Software used
                    - `dateTime` (string) - Date/time string
                    - `dateTimeOriginal` (string) - Original capture date/time
                    - `clearGps` (boolean, default false) - Remove all GPS data
                    - `fieldsToRemove` (string[]) - Specific metadata fields to remove
                    - `gpsLatitude` (number -90 to 90) - GPS latitude
                    - `gpsLongitude` (number -180 to 180) - GPS longitude
                    - `gpsAltitude` (number) - GPS altitude in meters
                    - `keywords` (string[]) - Keywords or tags
                    - `keywordsMode` (string, default "add") - One of: add, set
                    - `dateShift` (string) - Shift dates by offset, e.g. "+2:00" or "-1:30"
                    - `setAllDates` (string) - Set all date fields to this value
                    - `iptcTitle` (string) - IPTC title
                    - `iptcHeadline` (string) - IPTC headline
                    - `iptcCity` (string) - IPTC city
                    - `iptcState` (string) - IPTC state or province
                    - `iptcCountry` (string) - IPTC country
      responses:
        '200':
          description: Image with updated metadata
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ToolResponse'
        '400':
          description: Invalid input
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Error'
        '401':
          description: Authentication required
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/UnauthorizedError'
  /api/v1/tools/image/edit-metadata/inspect:
    post:
      operationId: inspectMetadataExifTool
      tags:
        - Tools
      summary: Просмотр метаданных (ExifTool)
      description: Чтение всех метаданных из изображения с помощью ExifTool. Возвращает все встроенные поля EXIF, IPTC, XMP и GPS.
      security:
        - bearerAuth: []
      requestBody:
        required: true
        content:
          multipart/form-data:
            schema:
              type: object
              required:
                - file
              properties:
                file:
                  type: string
                  format: binary
                  description: Image file to inspect
      responses:
        '200':
          description: Full metadata object (keys vary by image)
          content:
            application/json:
              schema:
                type: object
                additionalProperties: true
        '400':
          description: Invalid input
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Error'
        '401':
          description: Authentication required
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/UnauthorizedError'
        '422':
          description: Failed to read image metadata
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Error'
  /api/v1/tools/{section}/{toolId}/batch:
    post:
      operationId: batchProcess
      tags:
        - Batch
      summary: Пакетная обработка изображений
      description: |
        Отправка нескольких изображений через один инструмент. Возвращает ZIP-файл. Ответ содержит заголовок X-Job-Id для отслеживания прогресса.
      security:
        - bearerAuth: []
      parameters:
        - name: section
          in: path
          required: true
          schema:
            type: string
          description: Tool section slug (image, video, audio, pdf, files)
        - name: toolId
          in: path
          required: true
          schema:
            type: string
          description: Tool identifier (e.g. resize, crop)
      requestBody:
        required: true
        content:
          multipart/form-data:
            schema:
              type: object
              required:
                - file
              properties:
                file:
                  type: array
                  items:
                    type: string
                    format: binary
                  description: Image files to process (one or more)
                settings:
                  type: string
                  description: JSON string of tool settings applied to all images
      responses:
        '200':
          description: ZIP archive of processed images
          headers:
            X-Job-Id:
              schema:
                type: string
              description: Job identifier for progress tracking
          content:
            application/zip:
              schema:
                type: string
                format: binary
        '400':
          description: Invalid input
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Error'
        '401':
          description: Authentication required
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/UnauthorizedError'
  /api/v1/pipeline/execute:
    post:
      operationId: executePipeline
      tags:
        - Pipelines
      summary: Выполнение конвейера
      description: Прогон изображения через цепочку инструментов. Определение конвейера отправляется как JSON-строка в multipart-поле `pipeline`.
      security:
        - bearerAuth: []
      requestBody:
        required: true
        content:
          multipart/form-data:
            schema:
              type: object
              required:
                - file
                - pipeline
              properties:
                file:
                  type: string
                  format: binary
                  description: Image file to process
                pipeline:
                  type: string
                  description: 'JSON string: { "steps": [{ "toolId": "resize", "settings": {...} }, ...] }'
      responses:
        '200':
          description: Pipeline result
          content:
            application/json:
              schema:
                type: object
                properties:
                  jobId:
                    type: string
                  downloadUrl:
                    type: string
                  originalSize:
                    type: integer
                  processedSize:
                    type: integer
                  stepsCompleted:
                    type: integer
                  steps:
                    type: array
                    items:
                      type: object
        '400':
          description: Invalid input
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Error'
        '401':
          description: Authentication required
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/UnauthorizedError'
        '422':
          description: Processing failed
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Error'
        '501':
          description: Feature not installed
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/FeatureNotInstalledError'
  /api/v1/pipeline/save:
    post:
      operationId: savePipeline
      tags:
        - Pipelines
      summary: Сохранение конвейера
      description: Сохранение именованного определения конвейера с его шагами-инструментами для последующего выполнения. Используйте для сохранения переиспользуемых рабочих процессов.
      security:
        - bearerAuth: []
      requestBody:
        required: true
        content:
          application/json:
            schema:
              type: object
              required:
                - name
                - steps
              properties:
                name:
                  type: string
                description:
                  type: string
                  description: Optional human-readable description of the pipeline
                steps:
                  type: array
                  items:
                    type: object
                    properties:
                      toolId:
                        type: string
                      settings:
                        type: object
      responses:
        '201':
          description: Pipeline saved
          content:
            application/json:
              schema:
                type: object
                properties:
                  id:
                    type: string
                  name:
                    type: string
                  description:
                    type: string
                    description: Human-readable description of the pipeline
                  steps:
                    type: array
                    items:
                      type: object
                  createdAt:
                    type: string
                    format: date-time
        '400':
          description: Invalid input
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Error'
        '401':
          description: Authentication required
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/UnauthorizedError'
  /api/v1/pipeline/list:
    get:
      operationId: listPipelines
      tags:
        - Pipelines
      summary: Список сохранённых конвейеров
      description: Получение всех сохранённых определений конвейеров текущего пользователя. Возвращает имя, шаги и дату создания для каждого.
      security:
        - bearerAuth: []
      responses:
        '200':
          description: List of pipelines
          content:
            application/json:
              schema:
                type: object
                properties:
                  pipelines:
                    type: array
                    items:
                      type: object
                      properties:
                        id:
                          type: string
                        name:
                          type: string
                        description:
                          type: string
                          description: Human-readable description of the pipeline
                        steps:
                          type: array
                          items:
                            type: object
                        createdAt:
                          type: string
                          format: date-time
        '400':
          description: Invalid input
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Error'
        '401':
          description: Authentication required
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/UnauthorizedError'
  /api/v1/pipeline/{id}:
    delete:
      operationId: deletePipeline
      tags:
        - Pipelines
      summary: Удаление конвейера
      description: Безвозвратное удаление сохранённого конвейера по его ID. Удалить может только владелец конвейера или администратор.
      security:
        - bearerAuth: []
      parameters:
        - name: id
          in: path
          required: true
          description: Pipeline UUID
          schema:
            type: string
      responses:
        '200':
          description: Pipeline deleted
          content:
            application/json:
              schema:
                type: object
                properties:
                  ok:
                    type: boolean
        '400':
          description: Invalid input
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Error'
        '401':
          description: Authentication required
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/UnauthorizedError'
        '403':
          description: Not authorized to delete this pipeline
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ForbiddenError'
        '404':
          description: Pipeline not found
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Error'
  /api/v1/pipeline/tools:
    get:
      operationId: listPipelineTools
      tags:
        - Pipelines
      summary: Список инструментов, совместимых с конвейером
      description: Возвращает все ID инструментов, которые могут использоваться как шаги конвейера.
      security: []
      responses:
        '200':
          description: Tool IDs
          content:
            application/json:
              schema:
                type: object
                properties:
                  toolIds:
                    type: array
                    items:
                      type: string
  /api/v1/pipeline/batch:
    post:
      operationId: batchPipeline
      tags:
        - Pipelines
      summary: Прогон конвейера по нескольким файлам
      description: Выполнение конвейера по нескольким изображениям. Возвращает ZIP-файл со всеми обработанными результатами.
      security:
        - bearerAuth: []
      requestBody:
        required: true
        content:
          multipart/form-data:
            schema:
              type: object
              required:
                - file
                - pipeline
              properties:
                file:
                  type: array
                  items:
                    type: string
                    format: binary
                  description: Image files to process
                pipeline:
                  type: string
                  description: |
                    JSON string with pipeline definition:
                    - `steps` (array, required, 1-20 items) - Pipeline steps
                    - `steps[].toolId` (string, required) - Tool ID for this step
                    - `steps[].settings` (object, optional) - Tool-specific settings
                clientJobId:
                  type: string
                  description: Optional client-provided job ID for progress tracking via SSE
      responses:
        '200':
          description: ZIP file with processed images
          headers:
            X-Job-Id:
              schema:
                type: string
              description: Job ID for progress tracking
            X-File-Results:
              schema:
                type: string
              description: Percent-encoded JSON mapping of input index to output filename (decode with decodeURIComponent before JSON.parse)
          content:
            application/zip:
              schema:
                type: string
                format: binary
        '400':
          description: Invalid input
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Error'
        '401':
          description: Authentication required
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/UnauthorizedError'
  /api/v1/upload:
    post:
      operationId: uploadImage
      tags:
        - Files
      summary: Загрузка изображения
      description: Загрузка сырого файла изображения для последующей обработки эндпоинтами инструментов. Возвращает jobId и метаданные файла.
      security:
        - bearerAuth: []
      requestBody:
        required: true
        content:
          multipart/form-data:
            schema:
              type: object
              required:
                - file
              properties:
                file:
                  type: string
                  format: binary
      responses:
        '200':
          description: Upload result
          content:
            application/json:
              schema:
                type: object
                properties:
                  jobId:
                    type: string
                  files:
                    type: array
                    items:
                      type: object
                      properties:
                        name:
                          type: string
                        size:
                          type: integer
                        format:
                          type: string
        '400':
          description: Invalid input
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Error'
        '401':
          description: Authentication required
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/UnauthorizedError'
  /api/v1/download/{jobId}/{filename}:
    get:
      operationId: downloadProcessedImage
      tags:
        - Files
      summary: Скачивание обработанного изображения
      description: Скачивание обработанного изображения по его job ID и имени файла. Это URL, возвращаемый в downloadUrl в ответах инструментов.
      security: []
      parameters:
        - name: jobId
          in: path
          required: true
          description: Job identifier from a tool processing response
          schema:
            type: string
        - name: filename
          in: path
          required: true
          description: Output filename from the processing response
          schema:
            type: string
      responses:
        '200':
          description: Image binary
          content:
            image/*:
              schema:
                type: string
                format: binary
        '404':
          description: File not found
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Error'
  /api/v1/preview:
    post:
      operationId: generatePreview
      tags:
        - Files
      summary: Создание предпросмотра изображения
      description: Конвертация любого изображения (включая HEIC/HEIF) в предпросмотр WebP, уменьшенный до размещения в пределах 1200x1200 пикселей.
      security: []
      requestBody:
        required: true
        content:
          multipart/form-data:
            schema:
              type: object
              required:
                - file
              properties:
                file:
                  type: string
                  format: binary
                  description: Image file to preview
      responses:
        '200':
          description: WebP preview image
          content:
            image/webp:
              schema:
                type: string
                format: binary
        '400':
          description: Invalid input
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Error'
  /api/v1/files:
    get:
      operationId: listFiles
      tags:
        - Files
      summary: Список сохранённых файлов
      description: Возвращает последнюю версию каждого файла (одну на цепочку версий). Поддерживает пагинацию и поиск.
      security:
        - bearerAuth: []
      parameters:
        - name: limit
          in: query
          description: Maximum number of files to return (default 50)
          schema:
            type: integer
            default: 50
        - name: offset
          in: query
          description: Number of files to skip for pagination (default 0)
          schema:
            type: integer
            default: 0
        - name: search
          in: query
          description: Filter files by original filename (substring match)
          schema:
            type: string
      responses:
        '200':
          description: List of saved files
          content:
            application/json:
              schema:
                type: object
                properties:
                  files:
                    type: array
                    items:
                      type: object
                      properties:
                        id:
                          type: string
                        filename:
                          type: string
                        size:
                          type: integer
                        contentType:
                          type: string
                        createdAt:
                          type: string
                          format: date-time
                  total:
                    type: integer
                    description: Total number of matching files (for pagination)
                  limit:
                    type: integer
                    description: Applied limit value
                  offset:
                    type: integer
                    description: Applied offset value
        '400':
          description: Invalid input
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Error'
        '401':
          description: Authentication required
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/UnauthorizedError'
    delete:
      operationId: bulkDeleteFiles
      tags:
        - Files
      summary: Массовое удаление сохранённых файлов
      description: Удаление файлов и всех их цепочек версий. Пользователи без прав администратора могут удалять только свои файлы.
      security:
        - bearerAuth: []
      requestBody:
        required: true
        content:
          application/json:
            schema:
              type: object
              required:
                - ids
              properties:
                ids:
                  type: array
                  items:
                    type: string
                  description: File IDs to delete (deletes entire version chain for each)
      responses:
        '200':
          description: Deletion result
          content:
            application/json:
              schema:
                type: object
                properties:
                  deleted:
                    type: integer
                    description: Total number of records deleted across all version chains
        '400':
          description: Invalid input
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Error'
        '401':
          description: Authentication required
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/UnauthorizedError'
  /api/v1/files/upload:
    post:
      operationId: saveFileToLibrary
      tags:
        - Files
      summary: Сохранение файла в библиотеку
      description: Загрузка одного или нескольких файлов изображений напрямую в библиотеку для постоянного хранения и отслеживания версий.
      security:
        - bearerAuth: []
      requestBody:
        required: true
        content:
          multipart/form-data:
            schema:
              type: object
              required:
                - file
              properties:
                file:
                  type: string
                  format: binary
      responses:
        '201':
          description: Files uploaded
          content:
            application/json:
              schema:
                type: object
                properties:
                  files:
                    type: array
                    items:
                      type: object
                      description: Full file object with all metadata
        '400':
          description: Invalid input
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Error'
        '401':
          description: Authentication required
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/UnauthorizedError'
  /api/v1/files/save-result:
    post:
      operationId: saveProcessingResult
      tags:
        - Files
      summary: Сохранение результата обработки в библиотеку
      description: Сохранение результата обработки инструментом как новой версии существующего файла. Создаёт цепочку версий.
      security:
        - bearerAuth: []
      requestBody:
        required: true
        content:
          multipart/form-data:
            schema:
              type: object
              required:
                - file
                - parentId
              properties:
                file:
                  type: string
                  format: binary
                  description: The processed image file
                parentId:
                  type: string
                  description: ID of the parent file in the version chain
                toolId:
                  type: string
                  description: Tool that produced this result
      responses:
        '201':
          description: File saved
          content:
            application/json:
              schema:
                type: object
                properties:
                  file:
                    type: object
                    description: Full file object with all metadata
        '400':
          description: Invalid input
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Error'
        '401':
          description: Authentication required
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/UnauthorizedError'
        '404':
          description: Parent file not found
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Error'
  /api/v1/files/{id}:
    get:
      operationId: getFile
      tags:
        - Files
      summary: Получение метаданных и истории версий файла
      description: Получение метаданных и полной истории версий сохранённого файла. Используйте для просмотра деталей файла перед скачиванием.
      security:
        - bearerAuth: []
      parameters:
        - name: id
          in: path
          required: true
          description: Saved file UUID
          schema:
            type: string
      responses:
        '200':
          description: File metadata with version history
          content:
            application/json:
              schema:
                type: object
                properties:
                  file:
                    type: object
                    properties:
                      id:
                        type: string
                      originalName:
                        type: string
                      mimeType:
                        type: string
                      size:
                        type: integer
                      width:
                        type: integer
                      height:
                        type: integer
                      version:
                        type: integer
                      parentId:
                        type:
                          - string
                          - 'null'
                      toolChain:
                        type: array
                        items:
                          type: string
                      createdAt:
                        type: string
                        format: date-time
                  versions:
                    type: array
                    items:
                      type: object
                    description: Full version chain of this file
        '401':
          description: Authentication required
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/UnauthorizedError'
        '404':
          description: File not found
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Error'
  /api/v1/files/{id}/download:
    get:
      operationId: downloadSavedFile
      tags:
        - Files
      summary: Скачивание сохранённого файла
      description: Скачивание бинарного содержимого сохранённого файла из библиотеки по его ID.
      security:
        - bearerAuth: []
      parameters:
        - name: id
          in: path
          required: true
          description: Saved file UUID
          schema:
            type: string
      responses:
        '200':
          description: Image binary
          content:
            image/*:
              schema:
                type: string
                format: binary
        '401':
          description: Authentication required
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/UnauthorizedError'
        '404':
          description: File not found
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Error'
  /api/v1/files/{id}/thumbnail:
    get:
      operationId: getFileThumbnail
      tags:
        - Files
      summary: Получение миниатюры файла
      description: Получение миниатюры JPEG 300px сохранённого файла. Полезно для галерей и файловых браузеров.
      security:
        - bearerAuth: []
      parameters:
        - name: id
          in: path
          required: true
          description: Saved file UUID
          schema:
            type: string
      responses:
        '200':
          description: 300px JPEG thumbnail
          content:
            image/jpeg:
              schema:
                type: string
                format: binary
        '404':
          description: File not found
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Error'
        '422':
          description: Thumbnail generation failed
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Error'
  /api/v1/fetch-urls:
    post:
      operationId: fetchImageUrls
      tags:
        - Files
      summary: Загрузка изображений по URL
      description: |
        Загрузка одного или нескольких изображений с внешних URL. Каждый URL проверяется
        на SSRF-атаки, ограничивается по размеру и верифицируется как корректное
        изображение. Возвращает URL для скачивания каждого успешно загруженного
        изображения. Ограничение частоты: 200 запросов в час.
      security:
        - bearerAuth: []
      requestBody:
        required: true
        content:
          application/json:
            schema:
              type: object
              required:
                - urls
              properties:
                urls:
                  type: array
                  items:
                    type: string
                    format: uri
                  minItems: 1
                  maxItems: 50
                  description: URLs of images to fetch (1-50)
      responses:
        '200':
          description: Fetch results for each URL
          content:
            application/json:
              schema:
                type: object
                properties:
                  results:
                    type: array
                    items:
                      type: object
                      properties:
                        success:
                          type: boolean
                        url:
                          type: string
                        filename:
                          type: string
                        contentType:
                          type: string
                        size:
                          type: integer
                        width:
                          type: integer
                        height:
                          type: integer
                        downloadUrl:
                          type: string
                        previewUrl:
                          type:
                            - string
                            - 'null'
                        error:
                          type: string
                          description: Present only when success is false
        '400':
          description: Invalid input
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Error'
        '401':
          description: Authentication required
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/UnauthorizedError'
  /api/v1/config/locale:
    get:
      operationId: getLocaleConfig
      tags:
        - System
      summary: Получить локаль по умолчанию
      description: Возвращает настройку локали по умолчанию. Публичный эндпоинт.
      security: []
      responses:
        '200':
          description: Default locale
          content:
            application/json:
              schema:
                type: object
                properties:
                  defaultLocale:
                    type: string
                    example: en
  /api/auth/login:
    post:
      operationId: login
      tags:
        - Auth
      summary: Вход
      description: Аутентификация по имени пользователя и паролю. Возвращает bearer-токен для последующих запросов к API.
      security: []
      requestBody:
        required: true
        content:
          application/json:
            schema:
              type: object
              required:
                - username
                - password
              properties:
                username:
                  type: string
                password:
                  type: string
                  format: password
      responses:
        '200':
          description: Login successful
          content:
            application/json:
              schema:
                type: object
                properties:
                  token:
                    type: string
                  user:
                    type: object
                    properties:
                      id:
                        type: string
                      username:
                        type: string
                      role:
                        type: string
                      mustChangePassword:
                        type: boolean
                      permissions:
                        type: array
                        items:
                          type: string
                      teamName:
                        type: string
                  expiresAt:
                    type: string
                    format: date-time
        '400':
          description: Invalid input
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Error'
        '401':
          description: Invalid credentials
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/UnauthorizedError'
  /api/auth/logout:
    post:
      operationId: logout
      tags:
        - Auth
      summary: Выход
      description: Аннулирование текущего токена сессии. После выхода токен нельзя использовать для дальнейших запросов.
      security:
        - bearerAuth: []
      responses:
        '200':
          description: Logged out
          content:
            application/json:
              schema:
                type: object
                properties:
                  ok:
                    type: boolean
        '401':
          description: Authentication required
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/UnauthorizedError'
  /api/auth/session:
    get:
      operationId: getSession
      tags:
        - Auth
      summary: Получить текущую сессию
      description: Получение деталей текущей сессии, включая профиль пользователя, роль и разрешения. Возвращает null для пользователя при отсутствии аутентификации.
      security: []
      responses:
        '200':
          description: Session info
          content:
            application/json:
              schema:
                type: object
                properties:
                  user:
                    type: object
                    properties:
                      id:
                        type: string
                      username:
                        type: string
                      role:
                        type: string
                      mustChangePassword:
                        type: boolean
                      permissions:
                        type: array
                        items:
                          type: string
                  expiresAt:
                    type:
                      - string
                      - 'null'
                    format: date-time
  /api/auth/change-password:
    post:
      operationId: changePassword
      tags:
        - Auth
      summary: Смена пароля
      description: Смена пароля аутентифицированного пользователя. Требует текущий пароль для проверки.
      security:
        - bearerAuth: []
      requestBody:
        required: true
        content:
          application/json:
            schema:
              type: object
              required:
                - currentPassword
                - newPassword
              properties:
                currentPassword:
                  type: string
                  format: password
                newPassword:
                  type: string
                  format: password
      responses:
        '200':
          description: Password changed
          content:
            application/json:
              schema:
                type: object
                properties:
                  ok:
                    type: boolean
        '400':
          description: Invalid input
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Error'
        '401':
          description: Authentication required
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/UnauthorizedError'
  /api/auth/users:
    get:
      operationId: listUsers
      tags:
        - Auth
      summary: Список пользователей
      description: Требует разрешение users:manage.
      security:
        - bearerAuth: []
      responses:
        '200':
          description: List of users
          content:
            application/json:
              schema:
                type: object
                properties:
                  users:
                    type: array
                    items:
                      type: object
                      properties:
                        id:
                          type: string
                        username:
                          type: string
                        role:
                          type: string
                        team:
                          type: string
                        createdAt:
                          type: string
                          format: date-time
                  maxUsers:
                    type: integer
        '401':
          description: Authentication required
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/UnauthorizedError'
        '403':
          description: Insufficient permissions
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ForbiddenError'
  /api/auth/register:
    post:
      operationId: createUser
      tags:
        - Auth
      summary: Создание пользователя
      description: Требует разрешение users:manage.
      security:
        - bearerAuth: []
      requestBody:
        required: true
        content:
          application/json:
            schema:
              type: object
              required:
                - username
                - password
              properties:
                username:
                  type: string
                password:
                  type: string
                  format: password
                role:
                  type: string
                  description: Built-in role (admin, editor, user) or custom role name
                  default: user
                team:
                  type: string
                  default: Default
      responses:
        '201':
          description: User created
          content:
            application/json:
              schema:
                type: object
                properties:
                  id:
                    type: string
                  username:
                    type: string
                  role:
                    type: string
                  team:
                    type: string
        '400':
          description: Invalid input
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Error'
        '401':
          description: Authentication required
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/UnauthorizedError'
        '403':
          description: Insufficient permissions
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ForbiddenError'
        '409':
          description: Username already exists
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ConflictError'
  /api/auth/users/{id}:
    put:
      operationId: updateUser
      tags:
        - Auth
      summary: Обновление пользователя
      description: Требует разрешение users:manage.
      security:
        - bearerAuth: []
      parameters:
        - name: id
          in: path
          required: true
          description: User UUID
          schema:
            type: string
      requestBody:
        required: true
        content:
          application/json:
            schema:
              type: object
              properties:
                role:
                  type: string
                  description: Built-in role (admin, editor, user) or custom role name
                team:
                  type: string
      responses:
        '200':
          description: User updated
          content:
            application/json:
              schema:
                type: object
                properties:
                  ok:
                    type: boolean
        '400':
          description: Invalid input
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Error'
        '401':
          description: Authentication required
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/UnauthorizedError'
        '403':
          description: Insufficient permissions
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ForbiddenError'
    delete:
      operationId: deleteUser
      tags:
        - Auth
      summary: Удаление пользователя
      description: Требует разрешение users:manage.
      security:
        - bearerAuth: []
      parameters:
        - name: id
          in: path
          required: true
          description: User UUID
          schema:
            type: string
      responses:
        '200':
          description: User deleted
          content:
            application/json:
              schema:
                type: object
                properties:
                  ok:
                    type: boolean
        '401':
          description: Authentication required
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/UnauthorizedError'
        '403':
          description: Insufficient permissions
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ForbiddenError'
  /api/auth/users/{id}/reset-password:
    post:
      operationId: resetUserPassword
      tags:
        - Auth
      summary: Сброс пароля пользователя
      description: Требует разрешение users:manage.
      security:
        - bearerAuth: []
      parameters:
        - name: id
          in: path
          required: true
          description: User UUID
          schema:
            type: string
      requestBody:
        required: true
        content:
          application/json:
            schema:
              type: object
              required:
                - newPassword
              properties:
                newPassword:
                  type: string
                  format: password
      responses:
        '200':
          description: Password reset
          content:
            application/json:
              schema:
                type: object
                properties:
                  ok:
                    type: boolean
        '400':
          description: Invalid input
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Error'
        '401':
          description: Authentication required
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/UnauthorizedError'
        '403':
          description: Insufficient permissions
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ForbiddenError'
  /api/v1/api-keys:
    post:
      operationId: createApiKey
      tags:
        - API Keys
      summary: Создание API key
      description: |
        Создание нового API key. Полный ключ (с префиксом si_) возвращается только один раз. Ключи хранятся с использованием хеширования scrypt.
      security:
        - bearerAuth: []
      requestBody:
        required: true
        content:
          application/json:
            schema:
              type: object
              properties:
                name:
                  type: string
                  maxLength: 100
                  default: Default API Key
                permissions:
                  type: array
                  items:
                    type: string
                  description: Scoped permissions for this key
                expiresAt:
                  type: string
                  format: date-time
                  description: Optional expiration date
      responses:
        '201':
          description: API key created
          content:
            application/json:
              schema:
                type: object
                properties:
                  id:
                    type: string
                  key:
                    type: string
                    description: Full key (only shown once)
                  name:
                    type: string
                  permissions:
                    type:
                      - array
                      - 'null'
                    items:
                      type: string
                  expiresAt:
                    type:
                      - string
                      - 'null'
                    format: date-time
                  createdAt:
                    type: string
                    format: date-time
        '400':
          description: Invalid input
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Error'
        '401':
          description: Authentication required
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/UnauthorizedError'
    get:
      operationId: listApiKeys
      tags:
        - API Keys
      summary: Список API key
      description: Возвращает метаданные ключа, но не полный ключ.
      security:
        - bearerAuth: []
      responses:
        '200':
          description: List of API keys
          content:
            application/json:
              schema:
                type: object
                properties:
                  apiKeys:
                    type: array
                    items:
                      type: object
                      properties:
                        id:
                          type: string
                        name:
                          type: string
                        permissions:
                          type:
                            - array
                            - 'null'
                          items:
                            type: string
                        createdAt:
                          type: string
                          format: date-time
                        lastUsedAt:
                          type:
                            - string
                            - 'null'
                          format: date-time
                        expiresAt:
                          type:
                            - string
                            - 'null'
                          format: date-time
        '401':
          description: Authentication required
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/UnauthorizedError'
  /api/v1/api-keys/{id}:
    delete:
      operationId: deleteApiKey
      tags:
        - API Keys
      summary: Удаление API key
      description: Безвозвратный отзыв и удаление API key. После удаления ключ нельзя использовать для аутентификации.
      security:
        - bearerAuth: []
      parameters:
        - name: id
          in: path
          required: true
          description: API key UUID
          schema:
            type: string
      responses:
        '200':
          description: API key deleted
          content:
            application/json:
              schema:
                type: object
                properties:
                  ok:
                    type: boolean
        '401':
          description: Authentication required
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/UnauthorizedError'
  /api/v1/settings:
    get:
      operationId: getSettings
      tags:
        - Settings
      summary: Получить все настройки
      description: Получение всех системных настроек в виде пар ключ-значение. Требует аутентификации.
      security:
        - bearerAuth: []
      responses:
        '200':
          description: All settings wrapped in a settings object
          content:
            application/json:
              schema:
                type: object
                properties:
                  settings:
                    type: object
                    additionalProperties:
                      type: string
        '401':
          description: Authentication required
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/UnauthorizedError'
    put:
      operationId: updateSettings
      tags:
        - Settings
      summary: Обновление настроек
      description: Принимает плоский JSON-объект пар ключ-значение для установки. Требует роль администратора.
      security:
        - bearerAuth: []
      requestBody:
        required: true
        content:
          application/json:
            schema:
              type: object
              additionalProperties:
                type: string
      responses:
        '200':
          description: Settings updated
          content:
            application/json:
              schema:
                type: object
                properties:
                  ok:
                    type: boolean
                  updatedCount:
                    type: integer
        '400':
          description: Invalid input
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Error'
        '401':
          description: Authentication required
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/UnauthorizedError'
        '403':
          description: Admin access required
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ForbiddenError'
  /api/v1/settings/{key}:
    get:
      operationId: getSetting
      tags:
        - Settings
      summary: Получить одну настройку
      description: Получение значения одной настройки по имени ключа. Возвращает значение и временную метку последнего обновления.
      security:
        - bearerAuth: []
      parameters:
        - name: key
          in: path
          required: true
          description: Setting key name
          schema:
            type: string
      responses:
        '200':
          description: Setting value
          content:
            application/json:
              schema:
                type: object
                properties:
                  key:
                    type: string
                  value:
                    type: string
                  updatedAt:
                    type: string
                    format: date-time
        '401':
          description: Authentication required
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/UnauthorizedError'
        '404':
          description: Setting not found
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Error'
  /api/v1/teams:
    get:
      operationId: listTeams
      tags:
        - Teams
      summary: Список команд
      description: Требует разрешение teams:manage.
      security:
        - bearerAuth: []
      responses:
        '200':
          description: List of teams
          content:
            application/json:
              schema:
                type: object
                properties:
                  teams:
                    type: array
                    items:
                      type: object
                      properties:
                        id:
                          type: string
                        name:
                          type: string
                        memberCount:
                          type: integer
                        createdAt:
                          type: string
                          format: date-time
        '401':
          description: Authentication required
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/UnauthorizedError'
        '403':
          description: Insufficient permissions
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ForbiddenError'
    post:
      operationId: createTeam
      tags:
        - Teams
      summary: Создание команды
      description: Требует разрешение teams:manage.
      security:
        - bearerAuth: []
      requestBody:
        required: true
        content:
          application/json:
            schema:
              type: object
              required:
                - name
              properties:
                name:
                  type: string
      responses:
        '201':
          description: Team created
          content:
            application/json:
              schema:
                type: object
                properties:
                  id:
                    type: string
                  name:
                    type: string
        '400':
          description: Invalid input
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Error'
        '401':
          description: Authentication required
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/UnauthorizedError'
        '403':
          description: Insufficient permissions
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ForbiddenError'
        '409':
          description: Team name already exists
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ConflictError'
  /api/v1/teams/{id}:
    put:
      operationId: updateTeam
      tags:
        - Teams
      summary: Переименование команды
      description: Требует разрешение teams:manage.
      security:
        - bearerAuth: []
      parameters:
        - name: id
          in: path
          required: true
          description: Team UUID
          schema:
            type: string
      requestBody:
        required: true
        content:
          application/json:
            schema:
              type: object
              required:
                - name
              properties:
                name:
                  type: string
      responses:
        '200':
          description: Team updated
          content:
            application/json:
              schema:
                type: object
                properties:
                  ok:
                    type: boolean
        '401':
          description: Authentication required
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/UnauthorizedError'
        '403':
          description: Insufficient permissions
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ForbiddenError'
        '404':
          description: Team not found
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Error'
        '409':
          description: Team name already exists
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ConflictError'
    delete:
      operationId: deleteTeam
      tags:
        - Teams
      summary: Удаление команды
      description: Требует разрешение teams:manage.
      security:
        - bearerAuth: []
      parameters:
        - name: id
          in: path
          required: true
          description: Team UUID
          schema:
            type: string
      responses:
        '200':
          description: Team deleted
          content:
            application/json:
              schema:
                type: object
                properties:
                  ok:
                    type: boolean
        '401':
          description: Authentication required
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/UnauthorizedError'
        '403':
          description: Insufficient permissions
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ForbiddenError'
        '404':
          description: Team not found
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Error'
  /api/v1/config/auth:
    get:
      operationId: getAuthConfig
      tags:
        - System
      summary: Конфигурация аутентификации
      description: Проверка, включена ли аутентификация на этом экземпляре. Публичный эндпоинт, используемый фронтендом для определения потока входа.
      security: []
      responses:
        '200':
          description: Auth config
          content:
            application/json:
              schema:
                type: object
                properties:
                  authEnabled:
                    type: boolean
  /api/v1/jobs/{jobId}/progress:
    get:
      operationId: getJobProgress
      tags:
        - System
      summary: Прогресс задачи (SSE)
      description: Поток Server-Sent Events для отслеживания длительных заданий. Он отправляет кадры одиночных файлов, пакетов и heartbeat и закрывается сразу после конечного события задания.
      security: []
      parameters:
        - name: jobId
          in: path
          required: true
          description: Job UUID from a tool processing or installation response
          schema:
            type: string
      responses:
        '200':
          description: |
            SSE stream of job progress frames. Single-file frames use `phase`, `percent`, optional `stage`, and terminal `result` or `error` fields. Batch frames use `status`, file counters, and `errors`. Heartbeat frames keep idle connections alive.
          content:
            text/event-stream:
              schema:
                oneOf:
                  - title: Single-file progress frame
                    type: object
                    required:
                      - jobId
                      - type
                      - phase
                      - percent
                    properties:
                      jobId:
                        type: string
                      type:
                        type: string
                        const: single
                      phase:
                        type: string
                        enum:
                          - processing
                          - complete
                          - failed
                      stage:
                        type: string
                      percent:
                        type: number
                        minimum: 0
                        maximum: 100
                      error:
                        type: string
                        description: Present on terminal failed frames
                      result:
                        type: object
                        additionalProperties: true
                        description: Present on terminal complete frames; contains the tool result
                  - title: Batch progress frame
                    type: object
                    required:
                      - jobId
                      - type
                      - status
                      - totalFiles
                      - completedFiles
                      - failedFiles
                      - errors
                    properties:
                      jobId:
                        type: string
                      type:
                        type: string
                        const: batch
                      status:
                        type: string
                        enum:
                          - processing
                          - completed
                          - failed
                      totalFiles:
                        type: integer
                        minimum: 0
                      completedFiles:
                        type: integer
                        minimum: 0
                      failedFiles:
                        type: integer
                        minimum: 0
                      currentFile:
                        type: string
                      errors:
                        type: array
                        items:
                          type: object
                          required:
                            - filename
                            - error
                          properties:
                            filename:
                              type: string
                            error:
                              type: string
                  - title: Heartbeat frame
                    type: object
                    required:
                      - type
                    properties:
                      type:
                        type: string
                        const: heartbeat
  /api/v1/admin/health:
    get:
      operationId: adminHealthCheck
      tags:
        - Admin
      summary: Административная проверка состояния
      description: |
        Полная диагностика системы, включая состояние базы данных, режим хранилища,
        состояние очереди и доступность GPU. Требует разрешение system:health.
      security:
        - bearerAuth: []
      responses:
        '200':
          description: Detailed system health
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/AdminHealthResponse'
        '401':
          description: Authentication required
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/UnauthorizedError'
        '403':
          description: Insufficient permissions
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ForbiddenError'
  /api/v1/config/analytics:
    get:
      operationId: getAnalyticsConfig
      tags:
        - Analytics
      summary: Получить конфигурацию аналитики
      description: |
        Возвращает эффективную конфигурацию провайдера аналитики. Аналитика
        включена только когда это разрешают и bake на этапе компиляции, и настройка
        экземпляра analyticsEnabled. При отключении ключи, DSN и instanceId пусты.
        Публичный эндпоинт.
      security: []
      responses:
        '200':
          description: Analytics configuration
          content:
            application/json:
              schema:
                type: object
                properties:
                  enabled:
                    type: boolean
                    description: |
                      Effective analytics state. True only when both the
                      compile-time bake and the instance analyticsEnabled
                      setting are on. When false, the keys, DSN, and instanceId
                      are blank.
                  posthogApiKey:
                    type: string
                  posthogHost:
                    type: string
                  sentryDsn:
                    type: string
                  sentryDsnWeb:
                    type: string
                  posthogSampleRate:
                    type: number
                  instanceId:
                    type: string
  /api/v1/features:
    get:
      operationId: listFeatures
      tags:
        - Features
      summary: Список наборов функций
      description: |
        Возвращает все доступные наборы AI-функций и их статус установки.
        В окружениях без Docker все наборы отображаются как установленные.
      security:
        - bearerAuth: []
      responses:
        '200':
          description: Feature bundle list
          content:
            application/json:
              schema:
                type: object
                properties:
                  bundles:
                    type: array
                    items:
                      type: object
                      properties:
                        id:
                          type: string
                        name:
                          type: string
                        description:
                          type: string
                          description: Human-readable description of the feature bundle
                        status:
                          type: string
                          enum:
                            - installed
                            - not_installed
                            - installing
                            - queued
                            - error
                        installedVersion:
                          type:
                            - string
                            - 'null'
                        estimatedSize:
                          type: string
                        downloadBytes:
                          type:
                            - integer
                            - 'null'
                          description: Compressed archive size for this host's architecture, when the manifest records it. Null in native (non-Docker) mode or when unmeasured.
                        installedBytes:
                          type:
                            - integer
                            - 'null'
                          description: Extracted on-disk size for this host's architecture, when the manifest records it.
                        compatibility:
                          type: string
                          enum:
                            - compatible
                            - incompatible
                            - invalid
                          description: OCR runtime compatibility; omitted for legacy bundles
                        compatibilityReason:
                          type:
                            - string
                            - 'null'
                          description: OCR runtime compatibility reason; omitted for legacy bundles
                        selectedTarget:
                          type:
                            - string
                            - 'null'
                          description: Selected OCR runtime target, such as linux-amd64-cpu-py312
                        missingDownloadBytes:
                          type:
                            - integer
                            - 'null'
                          description: Remaining OCR runtime download size for this host
                        requiredMemoryBytes:
                          type:
                            - integer
                            - 'null'
                          description: Minimum effective memory required by the selected OCR runtime; null for legacy bundles
                        effectiveMemoryBytes:
                          type:
                            - integer
                            - 'null'
                          description: Effective memory available to OCR (the configured container cgroup limit, otherwise host memory); null for legacy bundles
                        healthyGeneration:
                          type:
                            - string
                            - 'null'
                          description: Active immutable OCR generation identifier
                        availableQualities:
                          type: array
                          description: OCR quality tiers currently available on this host
                          items:
                            type: string
                            enum:
                              - fast
                              - balanced
                              - best
                        enablesTools:
                          type: array
                          items:
                            type: string
                        progress:
                          type:
                            - object
                            - 'null'
                          properties:
                            percent:
                              type: number
                            stage:
                              type: string
                        error:
                          type:
                            - string
                            - 'null'
        '401':
          description: Authentication required
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/UnauthorizedError'
  /api/v1/admin/features/{bundleId}/install:
    post:
      operationId: installFeature
      tags:
        - Features
      summary: Установка набора функций
      description: |-
        Запустите асинхронную установку пакета функций AI. Скачивает модели и
        настраивает Python sidecar. Возвращает jobId для отслеживания прогресса.
        через конечную точку SSE. Требуется разрешение features:manage.

        Для bundleId=ocr поддерживается установка в официальные контейнеры Linux amd64 и arm64.
        Он загружает цель, выбранную для этого хоста, проверяет выпуск
        подпись индекса Ed25519 и дайджесты artifact/model, дымовые тесты
        организовал неизменяемую генерацию и атомарно активировал ее. OCR
        среда выполнения использует CPU, даже если контейнер имеет доступ NVIDIA GPU.
        для точного времени выполнения требуется не менее 4 GiB эффективной памяти (
        настроенный лимит контейнера cgroup, в противном случае — память хоста). Системы ниже этого подписаны
        минимум совместимости отклоняются перед загрузкой с
        compatibilityReason=insufficient-memory. Fast Tesseract OCR не является
        при условии соблюдения этого требования.

        Установки сериализуются на стороне сервера: запрос делается, пока другой
        выполняемая установка ставится в очередь (queued=true), а не отклоняется, и
        запускается автоматически после завершения текущей установки. Повторение
        запрос на тот же текущий пакет дедуплицируется и возвращает тот же самый
        jobId.
      security:
        - bearerAuth: []
      parameters:
        - name: bundleId
          in: path
          required: true
          description: Feature bundle identifier
          schema:
            type: string
      responses:
        '202':
          description: Installation started or queued
          content:
            application/json:
              schema:
                type: object
                properties:
                  jobId:
                    type: string
                    description: Job ID for tracking installation progress via SSE
                  queued:
                    type: boolean
                    description: True when the install was queued behind another running install instead of starting immediately.
        '401':
          description: Authentication required
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/UnauthorizedError'
        '403':
          description: Insufficient permissions
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ForbiddenError'
        '404':
          description: Unknown bundle ID
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Error'
        '409':
          description: Bundle already installed or OCR runtime unsupported on this host
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ConflictError'
        '503':
          description: OCR runtime trust key is not configured in this image
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Error'
  /api/v1/admin/features/{bundleId}/uninstall:
    post:
      operationId: uninstallFeature
      tags:
        - Features
      summary: Удаление набора функций
      description: |
        Удаление набора AI-функций и удаление его файлов моделей (если они не
        используются другим установленным набором). Требует разрешение features:manage.
      security:
        - bearerAuth: []
      parameters:
        - name: bundleId
          in: path
          required: true
          description: Feature bundle identifier
          schema:
            type: string
      responses:
        '200':
          description: Bundle uninstalled
          content:
            application/json:
              schema:
                type: object
                properties:
                  ok:
                    type: boolean
        '401':
          description: Authentication required
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/UnauthorizedError'
        '403':
          description: Insufficient permissions
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ForbiddenError'
        '404':
          description: Unknown bundle ID
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Error'
        '409':
          description: Bundle is not installed
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ConflictError'
  /api/v1/admin/features/disk-usage:
    get:
      operationId: getFeatureDiskUsage
      tags:
        - Features
      summary: Использование диска AI-моделями
      description: |
        Возвращает общий объём дискового пространства, занятого файлами AI-моделей.
        Требует разрешение features:manage.
      security:
        - bearerAuth: []
      responses:
        '200':
          description: Disk usage
          content:
            application/json:
              schema:
                type: object
                properties:
                  totalBytes:
                    type: integer
                    description: Total bytes used by AI model files
        '401':
          description: Authentication required
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/UnauthorizedError'
        '403':
          description: Insufficient permissions
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ForbiddenError'
  /api/v1/audit-log:
    get:
      operationId: queryAuditLog
      tags:
        - Audit
      summary: Запрос журнала аудита
      description: |
        Постраничный журнал аудита административных действий. Поддерживает фильтрацию
        по типу действия и диапазону дат. Требует разрешение audit:read.
      security:
        - bearerAuth: []
      parameters:
        - name: page
          in: query
          description: Page number (default 1)
          schema:
            type: integer
            minimum: 1
            default: 1
        - name: limit
          in: query
          description: Results per page (1-100, default 50)
          schema:
            type: integer
            minimum: 1
            maximum: 100
            default: 50
        - name: action
          in: query
          description: Filter by action type
          schema:
            type: string
        - name: from
          in: query
          description: Filter entries after this ISO 8601 date
          schema:
            type: string
            format: date-time
        - name: to
          in: query
          description: Filter entries before this ISO 8601 date
          schema:
            type: string
            format: date-time
      responses:
        '200':
          description: Paginated audit log entries
          content:
            application/json:
              schema:
                type: object
                properties:
                  entries:
                    type: array
                    items:
                      type: object
                      properties:
                        id:
                          type: string
                        actorId:
                          type:
                            - string
                            - 'null'
                        actorUsername:
                          type: string
                        action:
                          type: string
                        targetType:
                          type:
                            - string
                            - 'null'
                        targetId:
                          type:
                            - string
                            - 'null'
                        details:
                          type:
                            - object
                            - 'null'
                        ipAddress:
                          type:
                            - string
                            - 'null'
                        requestId:
                          type:
                            - string
                            - 'null'
                        createdAt:
                          type: string
                          format: date-time
                  total:
                    type: integer
                    description: Total matching entries
                  page:
                    type: integer
                  limit:
                    type: integer
        '401':
          description: Authentication required
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/UnauthorizedError'
        '403':
          description: Insufficient permissions
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ForbiddenError'
  /api/v1/roles:
    get:
      operationId: listRoles
      tags:
        - Roles
      summary: Список ролей
      description: |
        Список всех ролей (встроенных и пользовательских) с их разрешениями и
        количеством пользователей. Требует разрешение audit:read.
      security:
        - bearerAuth: []
      responses:
        '200':
          description: Role list
          content:
            application/json:
              schema:
                type: object
                properties:
                  roles:
                    type: array
                    items:
                      type: object
                      properties:
                        id:
                          type: string
                        name:
                          type: string
                        description:
                          type: string
                          description: Human-readable description of the role
                        permissions:
                          type: array
                          items:
                            type: string
                        isBuiltin:
                          type: boolean
                        userCount:
                          type: integer
                        createdAt:
                          type: string
                          format: date-time
                        updatedAt:
                          type: string
                          format: date-time
        '401':
          description: Authentication required
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/UnauthorizedError'
        '403':
          description: Insufficient permissions
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ForbiddenError'
    post:
      operationId: createRole
      tags:
        - Roles
      summary: Создание пользовательской роли
      description: |
        Создание новой пользовательской роли с указанными разрешениями. Имена ролей
        должны быть длиной 2-30 символов, строчные буквенно-цифровые с дефисами и
        подчёркиваниями. Требует разрешение security:manage.
      security:
        - bearerAuth: []
      requestBody:
        required: true
        content:
          application/json:
            schema:
              type: object
              required:
                - name
                - permissions
              properties:
                name:
                  type: string
                  minLength: 2
                  maxLength: 30
                  pattern: ^[a-z0-9_-]+$
                  description: Lowercase role name
                description:
                  type: string
                  description: Human-readable description
                permissions:
                  type: array
                  items:
                    type: string
                  description: |
                    Permission strings. Valid values: tools:use, files:own, files:all,
                    apikeys:own, apikeys:all, pipelines:own, pipelines:all,
                    settings:read, settings:write, users:manage, teams:manage,
                    features:manage, system:health, audit:read, security:manage
      responses:
        '201':
          description: Role created
          content:
            application/json:
              schema:
                type: object
                properties:
                  id:
                    type: string
                  name:
                    type: string
                  description:
                    type: string
                    description: Human-readable description of the role
                  permissions:
                    type: array
                    items:
                      type: string
                  isBuiltin:
                    type: boolean
        '400':
          description: Validation error
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Error'
        '401':
          description: Authentication required
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/UnauthorizedError'
        '403':
          description: Insufficient permissions
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ForbiddenError'
        '409':
          description: Role name already exists
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ConflictError'
  /api/v1/roles/{id}:
    put:
      operationId: updateRole
      tags:
        - Roles
      summary: Обновление пользовательской роли
      description: |
        Обновление имени, описания или разрешений пользовательской роли. Встроенные
        роли изменить нельзя. Требует разрешение security:manage.
      security:
        - bearerAuth: []
      parameters:
        - name: id
          in: path
          required: true
          description: Role UUID
          schema:
            type: string
      requestBody:
        required: true
        content:
          application/json:
            schema:
              type: object
              properties:
                name:
                  type: string
                  minLength: 2
                  maxLength: 30
                description:
                  type: string
                  description: Updated description for the role
                permissions:
                  type: array
                  items:
                    type: string
      responses:
        '200':
          description: Role updated
          content:
            application/json:
              schema:
                type: object
                properties:
                  ok:
                    type: boolean
        '400':
          description: Validation error or cannot modify built-in role
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Error'
        '401':
          description: Authentication required
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/UnauthorizedError'
        '403':
          description: Insufficient permissions
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ForbiddenError'
        '404':
          description: Role not found
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Error'
        '409':
          description: Role name already exists
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ConflictError'
    delete:
      operationId: deleteRole
      tags:
        - Roles
      summary: Удаление пользовательской роли
      description: |
        Удаление пользовательской роли. Пользователи, назначенные на эту роль,
        переназначаются на роль "user" по умолчанию. Встроенные роли удалить нельзя.
        Требует разрешение security:manage.
      security:
        - bearerAuth: []
      parameters:
        - name: id
          in: path
          required: true
          description: Role UUID
          schema:
            type: string
      responses:
        '200':
          description: Role deleted
          content:
            application/json:
              schema:
                type: object
                properties:
                  ok:
                    type: boolean
        '400':
          description: Cannot delete built-in role
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Error'
        '401':
          description: Authentication required
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/UnauthorizedError'
        '403':
          description: Insufficient permissions
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ForbiddenError'
        '404':
          description: Role not found
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Error'
  /api/v1/tools/video/convert-video:
    post:
      operationId: convertVideo
      tags:
        - Tools
      summary: Конвертация видео
      description: Конвертация видео между MP4, MOV, WebM, AVI и MKV.
      security:
        - bearerAuth: []
      requestBody:
        required: true
        content:
          multipart/form-data:
            schema:
              type: object
              required:
                - file
              properties:
                file:
                  type: string
                  format: binary
                  description: Video file to process
                settings:
                  type: string
                  description: |
                    JSON string with options:
                    - `format` (string, default "mp4") - One of: mp4, mov, webm, avi, mkv
                    - `quality` (string, default "balanced") - One of: high, balanced, small
                clientJobId:
                  type: string
                  description: Client-provided job ID for SSE progress tracking
      responses:
        '202':
          description: Accepted for async processing. Track progress via SSE at /api/v1/jobs/{jobId}/progress.
          content:
            application/json:
              schema:
                type: object
                properties:
                  jobId:
                    type: string
                  async:
                    type: boolean
                    example: true
        '400':
          description: Invalid input
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Error'
        '401':
          description: Authentication required
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/UnauthorizedError'
        '422':
          description: Processing failed
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Error'
  /api/v1/tools/video/compress-video:
    post:
      operationId: compressVideo
      tags:
        - Tools
      summary: Сжатие видео
      description: Уменьшение размера видеофайла с контролем качества.
      security:
        - bearerAuth: []
      requestBody:
        required: true
        content:
          multipart/form-data:
            schema:
              type: object
              required:
                - file
              properties:
                file:
                  type: string
                  format: binary
                  description: Video file to process
                settings:
                  type: string
                  description: |
                    JSON string with options:
                    - `quality` (string, default "balanced") - One of: light, balanced, strong
                    - `resolution` (string, default "original") - One of: original, 1080p, 720p, 480p
                clientJobId:
                  type: string
                  description: Client-provided job ID for SSE progress tracking
      responses:
        '202':
          description: Accepted for async processing. Track progress via SSE at /api/v1/jobs/{jobId}/progress.
          content:
            application/json:
              schema:
                type: object
                properties:
                  jobId:
                    type: string
                  async:
                    type: boolean
                    example: true
        '400':
          description: Invalid input
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Error'
        '401':
          description: Authentication required
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/UnauthorizedError'
        '422':
          description: Processing failed
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Error'
  /api/v1/tools/video/trim-video:
    post:
      operationId: trimVideo
      tags:
        - Tools
      summary: Обрезка видео
      description: Вырезание фрагмента из видео.
      security:
        - bearerAuth: []
      requestBody:
        required: true
        content:
          multipart/form-data:
            schema:
              type: object
              required:
                - file
              properties:
                file:
                  type: string
                  format: binary
                  description: Video file to process
                settings:
                  type: string
                  description: |
                    JSON string with options:
                    - `startS` (number >= 0, default 0) - Start time in seconds
                    - `endS` (number, required) - End time in seconds (must be after startS)
                    - `precise` (boolean, default false) - Re-encode for frame-accurate cuts instead of keyframe seek
      responses:
        '200':
          description: Processed video
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ToolResponse'
        '400':
          description: Invalid input
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Error'
        '401':
          description: Authentication required
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/UnauthorizedError'
  /api/v1/tools/video/mute-video:
    post:
      operationId: muteVideo
      tags:
        - Tools
      summary: Отключение звука видео
      description: Удаление звуковой дорожки из видео.
      security:
        - bearerAuth: []
      requestBody:
        required: true
        content:
          multipart/form-data:
            schema:
              type: object
              required:
                - file
              properties:
                file:
                  type: string
                  format: binary
                  description: Video file to process
      responses:
        '200':
          description: Processed video
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ToolResponse'
        '400':
          description: Invalid input
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Error'
        '401':
          description: Authentication required
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/UnauthorizedError'
  /api/v1/tools/video/video-to-gif:
    post:
      operationId: videoToGif
      tags:
        - Tools
      summary: Видео в GIF
      description: Преобразование видеоклипа в анимированный GIF.
      security:
        - bearerAuth: []
      requestBody:
        required: true
        content:
          multipart/form-data:
            schema:
              type: object
              required:
                - file
              properties:
                file:
                  type: string
                  format: binary
                  description: Video file to process
                settings:
                  type: string
                  description: |
                    JSON string with options:
                    - `fps` (integer 1-30, default 12) - Output frame rate
                    - `width` (integer 64-1280, default 480) - Output width in pixels
                    - `startS` (number >= 0, default 0) - Start time in seconds
                    - `durationS` (number 0-60, default 5) - Duration in seconds
                clientJobId:
                  type: string
                  description: Client-provided job ID for SSE progress tracking
      responses:
        '202':
          description: Accepted for async processing. Track progress via SSE at /api/v1/jobs/{jobId}/progress.
          content:
            application/json:
              schema:
                type: object
                properties:
                  jobId:
                    type: string
                  async:
                    type: boolean
                    example: true
        '400':
          description: Invalid input
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Error'
        '401':
          description: Authentication required
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/UnauthorizedError'
        '422':
          description: Processing failed
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Error'
  /api/v1/tools/video/resize-video:
    post:
      operationId: resizeVideo
      tags:
        - Tools
      summary: Изменение размера видео
      description: Масштабирование видео до нового разрешения или предустановленного размера.
      security:
        - bearerAuth: []
      requestBody:
        required: true
        content:
          multipart/form-data:
            schema:
              type: object
              required:
                - file
              properties:
                file:
                  type: string
                  format: binary
                  description: Video file to process
                settings:
                  type: string
                  description: |
                    JSON string with options:
                    - `width` (integer 16-7680, optional) - Target width in pixels
                    - `height` (integer 16-4320, optional) - Target height in pixels
                    - `preset` (string, default "custom") - One of: custom, 2160p, 1440p, 1080p, 720p, 480p, 360p. When "custom", at least width or height is required
      responses:
        '200':
          description: Processed video
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ToolResponse'
        '400':
          description: Invalid input
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Error'
        '401':
          description: Authentication required
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/UnauthorizedError'
  /api/v1/tools/video/crop-video:
    post:
      operationId: cropVideo
      tags:
        - Tools
      summary: Обрезка видео
      description: Обрезка области из видео.
      security:
        - bearerAuth: []
      requestBody:
        required: true
        content:
          multipart/form-data:
            schema:
              type: object
              required:
                - file
              properties:
                file:
                  type: string
                  format: binary
                  description: Video file to process
                settings:
                  type: string
                  description: |
                    JSON string with options:
                    - `width` (integer >= 16, required) - Crop region width in pixels
                    - `height` (integer >= 16, required) - Crop region height in pixels
                    - `x` (integer >= 0, default 0) - Horizontal offset from top-left
                    - `y` (integer >= 0, default 0) - Vertical offset from top-left
      responses:
        '200':
          description: Processed video
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ToolResponse'
        '400':
          description: Invalid input
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Error'
        '401':
          description: Authentication required
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/UnauthorizedError'
  /api/v1/tools/video/rotate-video:
    post:
      operationId: rotateVideo
      tags:
        - Tools
      summary: Поворот видео
      description: Поворот или отражение видео.
      security:
        - bearerAuth: []
      requestBody:
        required: true
        content:
          multipart/form-data:
            schema:
              type: object
              required:
                - file
              properties:
                file:
                  type: string
                  format: binary
                  description: Video file to process
                settings:
                  type: string
                  description: |
                    JSON string with options:
                    - `transform` (string, required) - One of: cw90, ccw90, 180, hflip, vflip
      responses:
        '200':
          description: Processed video
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ToolResponse'
        '400':
          description: Invalid input
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Error'
        '401':
          description: Authentication required
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/UnauthorizedError'
  /api/v1/tools/video/change-fps:
    post:
      operationId: changeFps
      tags:
        - Tools
      summary: Изменение FPS
      description: Изменение частоты кадров видео.
      security:
        - bearerAuth: []
      requestBody:
        required: true
        content:
          multipart/form-data:
            schema:
              type: object
              required:
                - file
              properties:
                file:
                  type: string
                  format: binary
                  description: Video file to process
                settings:
                  type: string
                  description: |
                    JSON string with options:
                    - `fps` (number 1-120, default 30) - Target frame rate
      responses:
        '200':
          description: Processed video
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ToolResponse'
        '400':
          description: Invalid input
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Error'
        '401':
          description: Authentication required
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/UnauthorizedError'
  /api/v1/tools/video/video-color:
    post:
      operationId: videoColor
      tags:
        - Tools
      summary: Цвет видео
      description: Настройка яркости, контраста, насыщенности и гаммы.
      security:
        - bearerAuth: []
      requestBody:
        required: true
        content:
          multipart/form-data:
            schema:
              type: object
              required:
                - file
              properties:
                file:
                  type: string
                  format: binary
                  description: Video file to process
                settings:
                  type: string
                  description: |
                    JSON string with options:
                    - `brightness` (number -1 to 1, default 0) - Brightness adjustment
                    - `contrast` (number 0-4, default 1) - Contrast multiplier
                    - `saturation` (number 0-3, default 1) - Saturation multiplier
                    - `gamma` (number 0.1-10, default 1) - Gamma correction
      responses:
        '200':
          description: Processed video
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ToolResponse'
        '400':
          description: Invalid input
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Error'
        '401':
          description: Authentication required
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/UnauthorizedError'
  /api/v1/tools/video/video-speed:
    post:
      operationId: videoSpeed
      tags:
        - Tools
      summary: Скорость видео
      description: Ускорение или замедление видео.
      security:
        - bearerAuth: []
      requestBody:
        required: true
        content:
          multipart/form-data:
            schema:
              type: object
              required:
                - file
              properties:
                file:
                  type: string
                  format: binary
                  description: Video file to process
                settings:
                  type: string
                  description: |
                    JSON string with options:
                    - `factor` (number 0.25-4, default 2) - Speed multiplier (2 = double speed, 0.5 = half speed)
                    - `keepPitch` (boolean, default true) - Preserve audio pitch when changing speed
      responses:
        '200':
          description: Processed video
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ToolResponse'
        '400':
          description: Invalid input
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Error'
        '401':
          description: Authentication required
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/UnauthorizedError'
  /api/v1/tools/video/reverse-video:
    post:
      operationId: reverseVideo
      tags:
        - Tools
      summary: Реверс видео
      description: Воспроизведение видеоклипа задом наперёд. Ограничено клипами до 5 минут.
      security:
        - bearerAuth: []
      requestBody:
        required: true
        content:
          multipart/form-data:
            schema:
              type: object
              required:
                - file
              properties:
                file:
                  type: string
                  format: binary
                  description: Video file to process
      responses:
        '200':
          description: Processed video
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ToolResponse'
        '400':
          description: Invalid input
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Error'
        '401':
          description: Authentication required
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/UnauthorizedError'
  /api/v1/tools/video/video-loudnorm:
    post:
      operationId: videoLoudnorm
      tags:
        - Tools
      summary: Нормализация звука
      description: Нормализация громкости звука видео до вещательного стандарта (EBU R128).
      security:
        - bearerAuth: []
      requestBody:
        required: true
        content:
          multipart/form-data:
            schema:
              type: object
              required:
                - file
              properties:
                file:
                  type: string
                  format: binary
                  description: Video file to process
      responses:
        '200':
          description: Processed video
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ToolResponse'
        '400':
          description: Invalid input
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Error'
        '401':
          description: Authentication required
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/UnauthorizedError'
  /api/v1/tools/video/aspect-pad:
    post:
      operationId: aspectPad
      tags:
        - Tools
      summary: Заполнение по пропорции
      description: Добавление полос сплошного цвета для соответствия целевому соотношению сторон.
      security:
        - bearerAuth: []
      requestBody:
        required: true
        content:
          multipart/form-data:
            schema:
              type: object
              required:
                - file
              properties:
                file:
                  type: string
                  format: binary
                  description: Video file to process
                settings:
                  type: string
                  description: |
                    JSON string with options:
                    - `target` (string, default "9:16") - One of: 16:9, 9:16, 1:1, 4:3, 3:4
                    - `color` (string, default "#000000") - Hex color for padding bars (e.g. "#000000")
      responses:
        '200':
          description: Processed video
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ToolResponse'
        '400':
          description: Invalid input
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Error'
        '401':
          description: Authentication required
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/UnauthorizedError'
  /api/v1/tools/video/blur-pad:
    post:
      operationId: blurPad
      tags:
        - Tools
      summary: Заполнение размытием
      description: Заполнение полос размытой копией видео.
      security:
        - bearerAuth: []
      requestBody:
        required: true
        content:
          multipart/form-data:
            schema:
              type: object
              required:
                - file
              properties:
                file:
                  type: string
                  format: binary
                  description: Video file to process
                settings:
                  type: string
                  description: |
                    JSON string with options:
                    - `target` (string, default "16:9") - One of: 16:9, 9:16, 1:1, 4:3, 3:4
                    - `blur` (number 2-50, default 20) - Gaussian blur sigma for the background
      responses:
        '200':
          description: Processed video
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ToolResponse'
        '400':
          description: Invalid input
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Error'
        '401':
          description: Authentication required
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/UnauthorizedError'
  /api/v1/tools/video/watermark-video:
    post:
      operationId: watermarkVideo
      tags:
        - Tools
      summary: Водяной знак на видео
      description: Наложение текстового водяного знака на кадры видео.
      security:
        - bearerAuth: []
      requestBody:
        required: true
        content:
          multipart/form-data:
            schema:
              type: object
              required:
                - file
              properties:
                file:
                  type: string
                  format: binary
                  description: Video file to process
                settings:
                  type: string
                  description: |
                    JSON string with options:
                    - `text` (string, required) - Watermark text (1-200 characters)
                    - `position` (string, default "br") - One of: tl, tc, tr, l, c, r, bl, bc, br
                    - `fontSize` (integer 8-120, default 36) - Font size in pixels
                    - `opacity` (number 0.05-1, default 0.5) - Watermark opacity
                    - `color` (string, default "#ffffff") - Hex color for the text (e.g. "#ffffff")
      responses:
        '200':
          description: Processed video
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ToolResponse'
        '400':
          description: Invalid input
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Error'
        '401':
          description: Authentication required
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/UnauthorizedError'
  /api/v1/tools/video/stabilize-video:
    post:
      operationId: stabilizeVideo
      tags:
        - Tools
      summary: Стабилизация видео
      description: Уменьшение дрожания камеры двухпроходной стабилизацией.
      security:
        - bearerAuth: []
      requestBody:
        required: true
        content:
          multipart/form-data:
            schema:
              type: object
              required:
                - file
              properties:
                file:
                  type: string
                  format: binary
                  description: Video file to process
                settings:
                  type: string
                  description: |
                    JSON string with options:
                    - `smoothing` (integer 5-60, default 15) - Smoothing window size in frames
                clientJobId:
                  type: string
                  description: Client-provided job ID for SSE progress tracking
      responses:
        '202':
          description: Accepted for async processing. Track progress via SSE at /api/v1/jobs/{jobId}/progress.
          content:
            application/json:
              schema:
                type: object
                properties:
                  jobId:
                    type: string
                  async:
                    type: boolean
                    example: true
        '400':
          description: Invalid input
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Error'
        '401':
          description: Authentication required
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/UnauthorizedError'
        '422':
          description: Processing failed
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Error'
  /api/v1/tools/video/gif-to-video:
    post:
      operationId: gifToVideo
      tags:
        - Tools
      summary: GIF в видео
      description: Конвертация анимированного GIF в видео MP4, WebM или MOV.
      security:
        - bearerAuth: []
      requestBody:
        required: true
        content:
          multipart/form-data:
            schema:
              type: object
              required:
                - file
              properties:
                file:
                  type: string
                  format: binary
                  description: GIF file to convert
                settings:
                  type: string
                  description: |
                    JSON string with options:
                    - `format` (string, default "mp4") - One of: mp4, webm, mov
      responses:
        '200':
          description: Converted video
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ToolResponse'
        '400':
          description: Invalid input
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Error'
        '401':
          description: Authentication required
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/UnauthorizedError'
  /api/v1/tools/video/video-to-webp:
    post:
      operationId: videoToWebp
      tags:
        - Tools
      summary: Видео в WebP
      description: Конвертация видеоклипа в анимированное изображение WebP.
      security:
        - bearerAuth: []
      requestBody:
        required: true
        content:
          multipart/form-data:
            schema:
              type: object
              required:
                - file
              properties:
                file:
                  type: string
                  format: binary
                  description: Video file to process
                settings:
                  type: string
                  description: |
                    JSON string with options:
                    - `fps` (integer 1-30, default 12) - Output frame rate
                    - `width` (integer 16-1920, default 480) - Output width in pixels
                    - `quality` (integer 1-100, default 75) - WebP compression quality
                    - `loop` (boolean, default true) - Loop the animation
      responses:
        '200':
          description: Converted animated WebP
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ToolResponse'
        '400':
          description: Invalid input
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Error'
        '401':
          description: Authentication required
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/UnauthorizedError'
  /api/v1/tools/video/video-to-frames:
    post:
      operationId: videoToFrames
      tags:
        - Tools
      summary: Видео в кадры
      description: Извлечение кадров из видео в виде ZIP с изображениями.
      security:
        - bearerAuth: []
      requestBody:
        required: true
        content:
          multipart/form-data:
            schema:
              type: object
              required:
                - file
              properties:
                file:
                  type: string
                  format: binary
                  description: Video file to process
                settings:
                  type: string
                  description: |
                    JSON string with options:
                    - `mode` (string, default "all") - One of: all, nth, timestamps
                    - `n` (integer 2-1000, default 10) - Extract every Nth frame (used when mode is "nth")
                    - `timestamps` (string, default "") - Comma-separated timestamps in seconds (required when mode is "timestamps")
                    - `format` (string, default "png") - One of: png, jpg
      responses:
        '200':
          description: ZIP archive containing extracted frames
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ToolResponse'
        '400':
          description: Invalid input
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Error'
        '401':
          description: Authentication required
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/UnauthorizedError'
  /api/v1/tools/video/merge-videos:
    post:
      operationId: mergeVideos
      tags:
        - Tools
      summary: Объединение видео
      description: |
        Объединение нескольких видеоклипов в один файл MP4. Все входные файлы
        нормализуются до разрешения первого видео и 30 fps.
      security:
        - bearerAuth: []
      requestBody:
        required: true
        content:
          multipart/form-data:
            schema:
              type: object
              required:
                - file
              properties:
                file:
                  type: array
                  items:
                    type: string
                    format: binary
                  description: Two or more video files to merge (up to 10)
                clientJobId:
                  type: string
                  description: Client-provided job ID for SSE progress tracking
      responses:
        '202':
          description: Accepted for async processing. Track progress via SSE at /api/v1/jobs/{jobId}/progress.
          content:
            application/json:
              schema:
                type: object
                properties:
                  jobId:
                    type: string
                  async:
                    type: boolean
                    example: true
        '400':
          description: Invalid input
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Error'
        '401':
          description: Authentication required
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/UnauthorizedError'
        '422':
          description: Processing failed
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Error'
  /api/v1/tools/video/replace-audio:
    post:
      operationId: replaceAudio
      tags:
        - Tools
      summary: Замена звука
      description: Замена звуковой дорожки видео другим файлом.
      security:
        - bearerAuth: []
      requestBody:
        required: true
        content:
          multipart/form-data:
            schema:
              type: object
              required:
                - file
              properties:
                file:
                  type: array
                  items:
                    type: string
                    format: binary
                  description: A video file followed by an audio file (exactly 2 files)
      responses:
        '200':
          description: Processed video
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ToolResponse'
        '400':
          description: Invalid input
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Error'
        '401':
          description: Authentication required
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/UnauthorizedError'
  /api/v1/tools/video/burn-subtitles:
    post:
      operationId: burnSubtitles
      tags:
        - Tools
      summary: Впечатывание субтитров
      description: Постоянное наложение субтитров на кадры видео.
      security:
        - bearerAuth: []
      requestBody:
        required: true
        content:
          multipart/form-data:
            schema:
              type: object
              required:
                - file
              properties:
                file:
                  type: array
                  items:
                    type: string
                    format: binary
                  description: A video file followed by a subtitle file (.srt, .vtt, .ass)
                settings:
                  type: string
                  description: |
                    JSON string with options:
                    - `fontSize` (integer 8-72, default 24) - Subtitle font size in pixels
                clientJobId:
                  type: string
                  description: Client-provided job ID for SSE progress tracking
      responses:
        '202':
          description: Accepted for async processing. Track progress via SSE at /api/v1/jobs/{jobId}/progress.
          content:
            application/json:
              schema:
                type: object
                properties:
                  jobId:
                    type: string
                  async:
                    type: boolean
                    example: true
        '400':
          description: Invalid input
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Error'
        '401':
          description: Authentication required
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/UnauthorizedError'
        '422':
          description: Processing failed
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Error'
  /api/v1/tools/video/embed-subtitles:
    post:
      operationId: embedSubtitles
      tags:
        - Tools
      summary: Встраивание субтитров
      description: Мультиплексирование дорожки субтитров в контейнер видео.
      security:
        - bearerAuth: []
      requestBody:
        required: true
        content:
          multipart/form-data:
            schema:
              type: object
              required:
                - file
              properties:
                file:
                  type: array
                  items:
                    type: string
                    format: binary
                  description: A video file followed by a subtitle file (.srt, .vtt, .ass)
                settings:
                  type: string
                  description: |
                    JSON string with options:
                    - `language` (string, default "eng") - ISO 639-2/B language code (3 lowercase letters, e.g. "eng", "fra", "deu")
      responses:
        '200':
          description: Processed video
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ToolResponse'
        '400':
          description: Invalid input
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Error'
        '401':
          description: Authentication required
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/UnauthorizedError'
  /api/v1/tools/video/extract-subtitles:
    post:
      operationId: extractSubtitles
      tags:
        - Tools
      summary: Извлечение субтитров
      description: Извлечение дорожки субтитров из видео в виде файла SRT.
      security:
        - bearerAuth: []
      requestBody:
        required: true
        content:
          multipart/form-data:
            schema:
              type: object
              required:
                - file
              properties:
                file:
                  type: string
                  format: binary
                  description: Video file with an embedded subtitle track
      responses:
        '200':
          description: Extracted subtitle file
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ToolResponse'
        '400':
          description: Invalid input
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Error'
        '401':
          description: Authentication required
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/UnauthorizedError'
  /api/v1/tools/video/images-to-video:
    post:
      operationId: imagesToVideo
      tags:
        - Tools
      summary: Изображения в видео
      description: Преобразование набора изображений в видео-слайдшоу.
      security:
        - bearerAuth: []
      requestBody:
        required: true
        content:
          multipart/form-data:
            schema:
              type: object
              required:
                - file
              properties:
                file:
                  type: array
                  items:
                    type: string
                    format: binary
                  description: Two or more image files (up to 60)
                settings:
                  type: string
                  description: |
                    JSON string with options:
                    - `secondsPerImage` (number 0.5-10, default 2) - Display duration per image in seconds
                    - `resolution` (string, default "720p") - One of: 1080p, 720p, square
                    - `fps` (integer 10-60, default 30) - Output frame rate
      responses:
        '200':
          description: Generated slideshow video
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ToolResponse'
        '400':
          description: Invalid input
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Error'
        '401':
          description: Authentication required
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/UnauthorizedError'
  /api/v1/tools/video/video-metadata:
    post:
      operationId: videoMetadata
      tags:
        - Tools
      summary: Очистка метаданных видео
      description: Удаление метаданных из видео с отчётом о найденном.
      security:
        - bearerAuth: []
      requestBody:
        required: true
        content:
          multipart/form-data:
            schema:
              type: object
              required:
                - file
              properties:
                file:
                  type: string
                  format: binary
                  description: Video file to process
      responses:
        '200':
          description: Processed video with metadata report
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ToolResponse'
        '400':
          description: Invalid input
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Error'
        '401':
          description: Authentication required
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/UnauthorizedError'
  /api/v1/tools/video/auto-subtitles:
    post:
      operationId: autoSubtitles
      tags:
        - Tools
      summary: Автосубтитры
      description: |
        Генерация субтитров на базе AI из звуковых дорожек видео с помощью
        faster-whisper. Поддерживает автоопределение и 10 явных языков.
        Требует установки набора функций transcription.
      security:
        - bearerAuth: []
      requestBody:
        required: true
        content:
          multipart/form-data:
            schema:
              type: object
              required:
                - file
              properties:
                file:
                  type: string
                  format: binary
                  description: Video file to transcribe
                settings:
                  type: string
                  description: |
                    JSON string with options:
                    - `language` (string, default "auto") - One of: auto, en, de, fr, es, zh, ja, ko, id, th, vi
                    - `format` (string, default "srt") - One of: srt, vtt
                clientJobId:
                  type: string
                  description: Client-provided job ID for SSE progress tracking
      responses:
        '202':
          description: Accepted for async processing. Track progress via SSE at /api/v1/jobs/{jobId}/progress.
          content:
            application/json:
              schema:
                type: object
                properties:
                  jobId:
                    type: string
                  async:
                    type: boolean
                    example: true
        '400':
          description: Invalid input
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Error'
        '401':
          description: Authentication required
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/UnauthorizedError'
        '422':
          description: Processing failed
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Error'
        '501':
          description: Feature not installed
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/FeatureNotInstalledError'
  /api/v1/tools/video/extract-audio:
    post:
      operationId: extractAudio
      tags:
        - Tools
      summary: Извлечение звука
      description: Извлечение звуковой дорожки из видео.
      security:
        - bearerAuth: []
      requestBody:
        required: true
        content:
          multipart/form-data:
            schema:
              type: object
              required:
                - file
              properties:
                file:
                  type: string
                  format: binary
                  description: Video file to extract audio from
                settings:
                  type: string
                  description: |
                    JSON string with options:
                    - `format` (string, default "mp3") - One of: mp3, wav, m4a, ogg
      responses:
        '200':
          description: Extracted audio file
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ToolResponse'
        '400':
          description: Invalid input
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Error'
        '401':
          description: Authentication required
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/UnauthorizedError'
  /api/v1/tools/audio/convert-audio:
    post:
      operationId: convertAudio
      tags:
        - Tools
      summary: Конвертация аудио
      description: Конвертация аудио между MP3, WAV, OGG и другими форматами.
      security:
        - bearerAuth: []
      requestBody:
        required: true
        content:
          multipart/form-data:
            schema:
              type: object
              required:
                - file
              properties:
                file:
                  type: string
                  format: binary
                  description: Audio file to process
                settings:
                  type: string
                  description: |
                    JSON string with options:
                    - `format` (string, default "mp3") - One of: mp3, wav, ogg, flac, m4a
                    - `bitrateKbps` (integer 32-320, default 192) - Output bitrate in kbps
      responses:
        '200':
          description: Processed audio
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ToolResponse'
        '400':
          description: Invalid input
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Error'
        '401':
          description: Authentication required
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/UnauthorizedError'
  /api/v1/tools/audio/trim-audio:
    post:
      operationId: trimAudio
      tags:
        - Tools
      summary: Обрезка аудио
      description: Вырезание участка из аудиофайла.
      security:
        - bearerAuth: []
      requestBody:
        required: true
        content:
          multipart/form-data:
            schema:
              type: object
              required:
                - file
              properties:
                file:
                  type: string
                  format: binary
                  description: Audio file to process
                settings:
                  type: string
                  description: |
                    JSON string with options:
                    - `startS` (number, min 0, default 0) - Start time in seconds
                    - `endS` (number, required) - End time in seconds (must be after start)
      responses:
        '200':
          description: Processed audio
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ToolResponse'
        '400':
          description: Invalid input
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Error'
        '401':
          description: Authentication required
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/UnauthorizedError'
  /api/v1/tools/audio/volume-adjust:
    post:
      operationId: volumeAdjust
      tags:
        - Tools
      summary: Настройка громкости
      description: Увеличение или уменьшение громкости аудио на фиксированное усиление.
      security:
        - bearerAuth: []
      requestBody:
        required: true
        content:
          multipart/form-data:
            schema:
              type: object
              required:
                - file
              properties:
                file:
                  type: string
                  format: binary
                  description: Audio file to process
                settings:
                  type: string
                  description: |
                    JSON string with options:
                    - `gainDb` (number -30 to 30, default 3) - Volume adjustment in decibels
      responses:
        '200':
          description: Processed audio
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ToolResponse'
        '400':
          description: Invalid input
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Error'
        '401':
          description: Authentication required
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/UnauthorizedError'
  /api/v1/tools/audio/normalize-audio:
    post:
      operationId: normalizeAudio
      tags:
        - Tools
      summary: Нормализация звука
      description: Выравнивание громкости до вещательных стандартных уровней (EBU R128, -16 LUFS).
      security:
        - bearerAuth: []
      requestBody:
        required: true
        content:
          multipart/form-data:
            schema:
              type: object
              required:
                - file
              properties:
                file:
                  type: string
                  format: binary
                  description: Audio file to process
      responses:
        '200':
          description: Processed audio
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ToolResponse'
        '400':
          description: Invalid input
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Error'
        '401':
          description: Authentication required
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/UnauthorizedError'
  /api/v1/tools/audio/fade-audio:
    post:
      operationId: fadeAudio
      tags:
        - Tools
      summary: Затухание аудио
      description: Добавление эффектов нарастания и затухания к аудио.
      security:
        - bearerAuth: []
      requestBody:
        required: true
        content:
          multipart/form-data:
            schema:
              type: object
              required:
                - file
              properties:
                file:
                  type: string
                  format: binary
                  description: Audio file to process
                settings:
                  type: string
                  description: |
                    JSON string with options:
                    - `fadeInS` (number 0-30, default 1) - Fade-in duration in seconds
                    - `fadeOutS` (number 0-30, default 1) - Fade-out duration in seconds
      responses:
        '200':
          description: Processed audio
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ToolResponse'
        '400':
          description: Invalid input
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Error'
        '401':
          description: Authentication required
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/UnauthorizedError'
  /api/v1/tools/audio/reverse-audio:
    post:
      operationId: reverseAudio
      tags:
        - Tools
      summary: Реверс аудио
      description: Воспроизведение аудио задом наперёд.
      security:
        - bearerAuth: []
      requestBody:
        required: true
        content:
          multipart/form-data:
            schema:
              type: object
              required:
                - file
              properties:
                file:
                  type: string
                  format: binary
                  description: Audio file to process
      responses:
        '200':
          description: Processed audio
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ToolResponse'
        '400':
          description: Invalid input
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Error'
        '401':
          description: Authentication required
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/UnauthorizedError'
  /api/v1/tools/audio/audio-speed:
    post:
      operationId: audioSpeed
      tags:
        - Tools
      summary: Скорость аудио
      description: Ускорение или замедление воспроизведения аудио.
      security:
        - bearerAuth: []
      requestBody:
        required: true
        content:
          multipart/form-data:
            schema:
              type: object
              required:
                - file
              properties:
                file:
                  type: string
                  format: binary
                  description: Audio file to process
                settings:
                  type: string
                  description: |
                    JSON string with options:
                    - `factor` (number 0.25-4, default 1.5) - Speed multiplier (0.25 = quarter speed, 4 = quadruple speed)
      responses:
        '200':
          description: Processed audio
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ToolResponse'
        '400':
          description: Invalid input
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Error'
        '401':
          description: Authentication required
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/UnauthorizedError'
  /api/v1/tools/audio/pitch-shift:
    post:
      operationId: pitchShift
      tags:
        - Tools
      summary: Сдвиг высоты тона
      description: Повышение или понижение высоты тона на полутоны без изменения скорости.
      security:
        - bearerAuth: []
      requestBody:
        required: true
        content:
          multipart/form-data:
            schema:
              type: object
              required:
                - file
              properties:
                file:
                  type: string
                  format: binary
                  description: Audio file to process
                settings:
                  type: string
                  description: |
                    JSON string with options:
                    - `semitones` (integer -12 to 12, default 3) - Semitones to shift (must be nonzero)
      responses:
        '200':
          description: Processed audio
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ToolResponse'
        '400':
          description: Invalid input
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Error'
        '401':
          description: Authentication required
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/UnauthorizedError'
  /api/v1/tools/audio/audio-channels:
    post:
      operationId: audioChannels
      tags:
        - Tools
      summary: Аудиоканалы
      description: Преобразование между моно, стерео или обмен левого и правого каналов.
      security:
        - bearerAuth: []
      requestBody:
        required: true
        content:
          multipart/form-data:
            schema:
              type: object
              required:
                - file
              properties:
                file:
                  type: string
                  format: binary
                  description: Audio file to process
                settings:
                  type: string
                  description: |
                    JSON string with options:
                    - `mode` (string, required) - One of: stereo-to-mono, mono-to-stereo, swap
      responses:
        '200':
          description: Processed audio
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ToolResponse'
        '400':
          description: Invalid input
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Error'
        '401':
          description: Authentication required
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/UnauthorizedError'
  /api/v1/tools/audio/silence-removal:
    post:
      operationId: removeSilence
      tags:
        - Tools
      summary: Удаление тишины
      description: Удаление участков тишины из аудио.
      security:
        - bearerAuth: []
      requestBody:
        required: true
        content:
          multipart/form-data:
            schema:
              type: object
              required:
                - file
              properties:
                file:
                  type: string
                  format: binary
                  description: Audio file to process
                settings:
                  type: string
                  description: |
                    JSON string with options:
                    - `thresholdDb` (number -80 to -20, default -50) - Silence threshold in dB
                    - `minSilenceS` (number 0.1-5, default 0.5) - Minimum silence duration in seconds to remove
      responses:
        '200':
          description: Processed audio
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ToolResponse'
        '400':
          description: Invalid input
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Error'
        '401':
          description: Authentication required
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/UnauthorizedError'
  /api/v1/tools/audio/noise-reduction:
    post:
      operationId: reduceNoise
      tags:
        - Tools
      summary: Шумоподавление
      description: Уменьшение фонового шума с помощью шумоподавления на основе FFT.
      security:
        - bearerAuth: []
      requestBody:
        required: true
        content:
          multipart/form-data:
            schema:
              type: object
              required:
                - file
              properties:
                file:
                  type: string
                  format: binary
                  description: Audio file to process
                settings:
                  type: string
                  description: |
                    JSON string with options:
                    - `strength` (string, default "medium") - One of: light, medium, strong
      responses:
        '200':
          description: Processed audio
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ToolResponse'
        '400':
          description: Invalid input
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Error'
        '401':
          description: Authentication required
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/UnauthorizedError'
  /api/v1/tools/audio/merge-audio:
    post:
      operationId: mergeAudio
      tags:
        - Tools
      summary: Объединение аудио
      description: Объединение нескольких аудиофайлов в одну последовательную дорожку.
      security:
        - bearerAuth: []
      requestBody:
        required: true
        content:
          multipart/form-data:
            schema:
              type: object
              required:
                - file
              properties:
                file:
                  type: array
                  items:
                    type: string
                    format: binary
                  description: Two or more audio files to merge (max 10)
                settings:
                  type: string
                  description: |
                    JSON string with options:
                    - `format` (string, default "mp3") - One of: mp3, wav, flac, m4a
      responses:
        '200':
          description: Processed audio
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ToolResponse'
        '400':
          description: Invalid input
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Error'
        '401':
          description: Authentication required
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/UnauthorizedError'
  /api/v1/tools/audio/split-audio:
    post:
      operationId: splitAudio
      tags:
        - Tools
      summary: Разделение аудио
      description: |
        Разделение аудио по времени, на равные части или по обнаружению тишины. Возвращает
        ToolResponse с downloadUrl, указывающим на ZIP-архив сегментов.
      security:
        - bearerAuth: []
      requestBody:
        required: true
        content:
          multipart/form-data:
            schema:
              type: object
              required:
                - file
              properties:
                file:
                  type: string
                  format: binary
                  description: Audio file to split
                settings:
                  type: string
                  description: |
                    JSON string with options:
                    - `mode` (string, default "time") - One of: time, parts, silence
                    - `segmentS` (number 1-3600, default 60) - Segment length in seconds (mode=time)
                    - `parts` (integer 2-20, default 2) - Number of equal parts (mode=parts)
                    - `thresholdDb` (number -80 to -20, default -40) - Silence threshold in dB (mode=silence)
                    - `minSilenceS` (number 0.1-10, default 0.3) - Minimum silence gap in seconds (mode=silence)
      responses:
        '200':
          description: Processed audio (downloadUrl points to a ZIP archive)
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ToolResponse'
        '400':
          description: Invalid input
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Error'
        '401':
          description: Authentication required
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/UnauthorizedError'
  /api/v1/tools/audio/ringtone-maker:
    post:
      operationId: ringtoneMaker
      tags:
        - Tools
      summary: Создание рингтона
      description: Создание клипа рингтона (.m4r) из любого аудиофайла.
      security:
        - bearerAuth: []
      requestBody:
        required: true
        content:
          multipart/form-data:
            schema:
              type: object
              required:
                - file
              properties:
                file:
                  type: string
                  format: binary
                  description: Audio file to process
                settings:
                  type: string
                  description: |
                    JSON string with options:
                    - `startS` (number, min 0, default 0) - Start time in seconds
                    - `durationS` (number 1-30, default 30) - Clip duration in seconds
      responses:
        '200':
          description: Processed audio
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ToolResponse'
        '400':
          description: Invalid input
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Error'
        '401':
          description: Authentication required
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/UnauthorizedError'
  /api/v1/tools/audio/waveform-image:
    post:
      operationId: waveformImage
      tags:
        - Tools
      summary: Изображение осциллограммы
      description: Создание визуализации осциллограммы в виде изображения PNG из аудиофайла.
      security:
        - bearerAuth: []
      requestBody:
        required: true
        content:
          multipart/form-data:
            schema:
              type: object
              required:
                - file
              properties:
                file:
                  type: string
                  format: binary
                  description: Audio file to process
                settings:
                  type: string
                  description: |
                    JSON string with options:
                    - `width` (integer 256-3840, default 1024) - Image width in pixels
                    - `height` (integer 64-1080, default 256) - Image height in pixels
                    - `color` (string, default "#4f46e5") - Waveform hex color (e.g. "#4f46e5")
      responses:
        '200':
          description: Processed image
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ToolResponse'
        '400':
          description: Invalid input
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Error'
        '401':
          description: Authentication required
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/UnauthorizedError'
  /api/v1/tools/audio/audio-metadata:
    post:
      operationId: audioMetadata
      tags:
        - Tools
      summary: Метаданные аудио
      description: |
        Просмотр, редактирование или удаление аудиотегов (ID3). Ответ содержит
        объект `metadata` с container, durationS, bitrateKbps и tags
        наряду со стандартными полями ToolResponse.
      security:
        - bearerAuth: []
      requestBody:
        required: true
        content:
          multipart/form-data:
            schema:
              type: object
              required:
                - file
              properties:
                file:
                  type: string
                  format: binary
                  description: Audio file to process
                settings:
                  type: string
                  description: |
                    JSON string with options:
                    - `strip` (boolean, default false) - Remove all existing metadata
                    - `title` (string, optional, max 500) - Set the title tag
                    - `artist` (string, optional, max 500) - Set the artist tag
                    - `album` (string, optional, max 500) - Set the album tag
      responses:
        '200':
          description: Processed audio with metadata
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ToolResponse'
        '400':
          description: Invalid input
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Error'
        '401':
          description: Authentication required
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/UnauthorizedError'
  /api/v1/tools/audio/transcribe-audio:
    post:
      operationId: transcribeAudio
      tags:
        - Tools
      summary: Транскрипция аудио
      description: |
        Транскрипция речи в текст на базе AI с помощью faster-whisper. Выводит
        обычный текст, форматы субтитров SRT или VTT. Требует установки набора
        функций transcription.
      security:
        - bearerAuth: []
      requestBody:
        required: true
        content:
          multipart/form-data:
            schema:
              type: object
              required:
                - file
              properties:
                file:
                  type: string
                  format: binary
                  description: Audio file to transcribe
                settings:
                  type: string
                  description: |
                    JSON string with options:
                    - `language` (string, default "auto") - One of: auto, en, de, fr, es, zh, ja, ko, id, th, vi
                    - `outputFormat` (string, default "txt") - One of: txt, srt, vtt
                clientJobId:
                  type: string
                  description: Client-provided job ID for SSE progress tracking
      responses:
        '202':
          description: Accepted for async processing. Track progress via SSE at /api/v1/jobs/{jobId}/progress.
          content:
            application/json:
              schema:
                type: object
                properties:
                  jobId:
                    type: string
                  async:
                    type: boolean
                    example: true
        '400':
          description: Invalid input
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Error'
        '401':
          description: Authentication required
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/UnauthorizedError'
        '422':
          description: Processing failed
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Error'
        '501':
          description: Feature not installed
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/FeatureNotInstalledError'
  /api/v1/tools/pdf/merge-pdf:
    post:
      operationId: mergePdf
      tags:
        - Tools
      summary: Объединение PDF
      description: Объединение нескольких PDF в один. Требуется минимум два файла.
      security:
        - bearerAuth: []
      requestBody:
        required: true
        content:
          multipart/form-data:
            schema:
              type: object
              required:
                - file
              properties:
                file:
                  type: array
                  items:
                    type: string
                    format: binary
                  description: Two or more PDF files to merge (max 20)
      responses:
        '200':
          description: Merged PDF
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ToolResponse'
        '400':
          description: Invalid input
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Error'
        '401':
          description: Authentication required
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/UnauthorizedError'
  /api/v1/tools/pdf/split-pdf:
    post:
      operationId: splitPdf
      tags:
        - Tools
      summary: Разделение PDF
      description: |
        Извлечение страниц или разделение на части. В режиме "range" возвращает один PDF
        с выбранными страницами. В режиме "every" разбивает на блоки по N страниц
        и возвращает ZIP-архив с частями.
      security:
        - bearerAuth: []
      requestBody:
        required: true
        content:
          multipart/form-data:
            schema:
              type: object
              required:
                - file
              properties:
                file:
                  type: string
                  format: binary
                  description: PDF file to split
                settings:
                  type: string
                  description: |
                    JSON string with options:
                    - `mode` (string, default "range") - One of: range, every
                    - `range` (string, optional) - Page range in qpdf syntax, e.g. "1-5,8,10-z" (required when mode is "range")
                    - `everyN` (integer 1-500, optional) - Split into chunks of N pages (required when mode is "every")
      responses:
        '200':
          description: Split PDF or ZIP archive of parts
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ToolResponse'
        '400':
          description: Invalid input
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Error'
        '401':
          description: Authentication required
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/UnauthorizedError'
  /api/v1/tools/pdf/compress-pdf:
    post:
      operationId: compressPdf
      tags:
        - Tools
      summary: Сжатие PDF
      description: |
        Уменьшение размера файла PDF путём понижения разрешения встроенных изображений.
        В режиме "quality" ползунок 1-100 управляет DPI изображений. В режиме "targetSize"
        бинарный поиск находит наибольший DPI, вписывающийся в целевой размер. Если сжатие
        увеличило бы файл, исходные байты возвращаются без изменений.
      security:
        - bearerAuth: []
      requestBody:
        required: true
        content:
          multipart/form-data:
            schema:
              type: object
              required:
                - file
              properties:
                file:
                  type: string
                  format: binary
                  description: PDF file to compress
                settings:
                  type: string
                  description: |
                    JSON string with options:
                    - `mode` (string, default "quality") - One of: quality, targetSize
                    - `quality` (integer 1-100, optional) - Compression quality (higher = less compression). Used in quality mode
                    - `targetSizeKb` (number, optional) - Target file size in kilobytes. Used in targetSize mode
      responses:
        '200':
          description: Compressed PDF
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ToolResponse'
        '400':
          description: Invalid input
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Error'
        '401':
          description: Authentication required
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/UnauthorizedError'
  /api/v1/tools/pdf/rotate-pdf:
    post:
      operationId: rotatePdf
      tags:
        - Tools
      summary: Поворот PDF
      description: Поворот страниц в PDF на 90, 180 или 270 градусов.
      security:
        - bearerAuth: []
      requestBody:
        required: true
        content:
          multipart/form-data:
            schema:
              type: object
              required:
                - file
              properties:
                file:
                  type: string
                  format: binary
                  description: PDF file to process
                settings:
                  type: string
                  description: |
                    JSON string with options:
                    - `angle` (integer, default 90) - One of: 90, 180, 270
                    - `range` (string, default "1-z") - Page range in qpdf syntax, e.g. "1-5,8" ("1-z" = all pages)
      responses:
        '200':
          description: Rotated PDF
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ToolResponse'
        '400':
          description: Invalid input
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Error'
        '401':
          description: Authentication required
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/UnauthorizedError'
  /api/v1/tools/pdf/extract-pages:
    post:
      operationId: extractPages
      tags:
        - Tools
      summary: Извлечение страниц
      description: Извлечение выбранных страниц в новый PDF.
      security:
        - bearerAuth: []
      requestBody:
        required: true
        content:
          multipart/form-data:
            schema:
              type: object
              required:
                - file
              properties:
                file:
                  type: string
                  format: binary
                  description: PDF file to extract from
                settings:
                  type: string
                  description: |
                    JSON string with options:
                    - `range` (string, required) - Page range in qpdf syntax, e.g. "1-5,8,10-z"
      responses:
        '200':
          description: Extracted pages as a new PDF
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ToolResponse'
        '400':
          description: Invalid input
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Error'
        '401':
          description: Authentication required
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/UnauthorizedError'
  /api/v1/tools/pdf/remove-pages:
    post:
      operationId: removePages
      tags:
        - Tools
      summary: Удаление страниц
      description: Удаление определённых страниц из PDF. Нельзя удалить все страницы.
      security:
        - bearerAuth: []
      requestBody:
        required: true
        content:
          multipart/form-data:
            schema:
              type: object
              required:
                - file
              properties:
                file:
                  type: string
                  format: binary
                  description: PDF file to process
                settings:
                  type: string
                  description: |
                    JSON string with options:
                    - `pages` (string, required) - Page range to remove in qpdf syntax, e.g. "3,5-7"
      responses:
        '200':
          description: PDF with pages removed
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ToolResponse'
        '400':
          description: Invalid input
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Error'
        '401':
          description: Authentication required
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/UnauthorizedError'
  /api/v1/tools/pdf/organize-pdf:
    post:
      operationId: organizePdf
      tags:
        - Tools
      summary: Организация PDF
      description: Изменение порядка страниц с явным порядком страниц.
      security:
        - bearerAuth: []
      requestBody:
        required: true
        content:
          multipart/form-data:
            schema:
              type: object
              required:
                - file
              properties:
                file:
                  type: string
                  format: binary
                  description: PDF file to reorganize
                settings:
                  type: string
                  description: |
                    JSON string with options:
                    - `order` (string, required) - Desired page order in qpdf syntax, e.g. "3,1,2,5-z"
      responses:
        '200':
          description: Reorganized PDF
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ToolResponse'
        '400':
          description: Invalid input
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Error'
        '401':
          description: Authentication required
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/UnauthorizedError'
  /api/v1/tools/pdf/protect-pdf:
    post:
      operationId: protectPdf
      tags:
        - Tools
      summary: Защита PDF
      description: Добавление защиты паролем с шифрованием AES-256. Пароли скрываются из журналов аудита.
      security:
        - bearerAuth: []
      requestBody:
        required: true
        content:
          multipart/form-data:
            schema:
              type: object
              required:
                - file
              properties:
                file:
                  type: string
                  format: binary
                  description: PDF file to protect
                settings:
                  type: string
                  description: |
                    JSON string with options:
                    - `userPassword` (string 1-256 chars, required) - Password required to open the PDF
                    - `ownerPassword` (string 1-256 chars, optional) - Owner password for permissions; defaults to userPassword if omitted
      responses:
        '200':
          description: Encrypted PDF
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ToolResponse'
        '400':
          description: Invalid input
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Error'
        '401':
          description: Authentication required
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/UnauthorizedError'
  /api/v1/tools/pdf/unlock-pdf:
    post:
      operationId: unlockPdf
      tags:
        - Tools
      summary: Снятие защиты PDF
      description: Снятие защиты паролем с PDF. Необходимо указать правильный пароль. Пароли скрываются из журналов аудита.
      security:
        - bearerAuth: []
      requestBody:
        required: true
        content:
          multipart/form-data:
            schema:
              type: object
              required:
                - file
              properties:
                file:
                  type: string
                  format: binary
                  description: Password-protected PDF file
                settings:
                  type: string
                  description: |
                    JSON string with options:
                    - `password` (string 1-256 chars, required) - Password to decrypt the PDF
      responses:
        '200':
          description: Decrypted PDF
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ToolResponse'
        '400':
          description: Invalid input
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Error'
        '401':
          description: Authentication required
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/UnauthorizedError'
  /api/v1/tools/pdf/repair-pdf:
    post:
      operationId: repairPdf
      tags:
        - Tools
      summary: Восстановление PDF
      description: Попытка восстановить повреждённый или испорченный PDF. Структурная проверка на входе пропускается для допуска некорректных файлов.
      security:
        - bearerAuth: []
      requestBody:
        required: true
        content:
          multipart/form-data:
            schema:
              type: object
              required:
                - file
              properties:
                file:
                  type: string
                  format: binary
                  description: Damaged PDF file to repair
      responses:
        '200':
          description: Repaired PDF
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ToolResponse'
        '400':
          description: Invalid input
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Error'
        '401':
          description: Authentication required
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/UnauthorizedError'
  /api/v1/tools/pdf/linearize-pdf:
    post:
      operationId: linearizePdf
      tags:
        - Tools
      summary: Веб-оптимизация PDF
      description: Линеаризация PDF для быстрого просмотра в вебе (прогрессивная загрузка).
      security:
        - bearerAuth: []
      requestBody:
        required: true
        content:
          multipart/form-data:
            schema:
              type: object
              required:
                - file
              properties:
                file:
                  type: string
                  format: binary
                  description: PDF file to linearize
      responses:
        '200':
          description: Linearized PDF
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ToolResponse'
        '400':
          description: Invalid input
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Error'
        '401':
          description: Authentication required
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/UnauthorizedError'
  /api/v1/tools/pdf/grayscale-pdf:
    post:
      operationId: grayscalePdf
      tags:
        - Tools
      summary: Оттенки серого PDF
      description: Преобразование всех цветов в PDF в оттенки серого.
      security:
        - bearerAuth: []
      requestBody:
        required: true
        content:
          multipart/form-data:
            schema:
              type: object
              required:
                - file
              properties:
                file:
                  type: string
                  format: binary
                  description: PDF file to convert
      responses:
        '200':
          description: Grayscale PDF
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ToolResponse'
        '400':
          description: Invalid input
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Error'
        '401':
          description: Authentication required
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/UnauthorizedError'
  /api/v1/tools/pdf/pdfa-convert:
    post:
      operationId: pdfaConvert
      tags:
        - Tools
      summary: Конвертация в PDF/A
      description: Конвертация PDF в архивный формат PDF/A-2 для долгосрочного хранения.
      security:
        - bearerAuth: []
      requestBody:
        required: true
        content:
          multipart/form-data:
            schema:
              type: object
              required:
                - file
              properties:
                file:
                  type: string
                  format: binary
                  description: PDF file to convert
      responses:
        '200':
          description: PDF/A-2 compliant file
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ToolResponse'
        '400':
          description: Invalid input
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Error'
        '401':
          description: Authentication required
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/UnauthorizedError'
  /api/v1/tools/pdf/crop-pdf:
    post:
      operationId: cropPdf
      tags:
        - Tools
      summary: Обрезка PDF
      description: Обрезка всех страниц с равномерным отступом.
      security:
        - bearerAuth: []
      requestBody:
        required: true
        content:
          multipart/form-data:
            schema:
              type: object
              required:
                - file
              properties:
                file:
                  type: string
                  format: binary
                  description: PDF file to crop
                settings:
                  type: string
                  description: |
                    JSON string with options:
                    - `margin` (number 0-2000, default 20) - Uniform crop margin in points
      responses:
        '200':
          description: Cropped PDF
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ToolResponse'
        '400':
          description: Invalid input
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Error'
        '401':
          description: Authentication required
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/UnauthorizedError'
  /api/v1/tools/pdf/nup-pdf:
    post:
      operationId: nupPdf
      tags:
        - Tools
      summary: N-up PDF
      description: Размещение нескольких страниц на листе (например, 2-up, 4-up).
      security:
        - bearerAuth: []
      requestBody:
        required: true
        content:
          multipart/form-data:
            schema:
              type: object
              required:
                - file
              properties:
                file:
                  type: string
                  format: binary
                  description: PDF file to process
                settings:
                  type: string
                  description: |
                    JSON string with options:
                    - `perSheet` (integer, default 2) - One of: 2, 3, 4, 8, 9, 12, 16
      responses:
        '200':
          description: N-up PDF
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ToolResponse'
        '400':
          description: Invalid input
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Error'
        '401':
          description: Authentication required
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/UnauthorizedError'
  /api/v1/tools/pdf/booklet-pdf:
    post:
      operationId: bookletPdf
      tags:
        - Tools
      summary: Буклет PDF
      description: Размещение страниц для складывания в буклет (спуск полос для двусторонней печати).
      security:
        - bearerAuth: []
      requestBody:
        required: true
        content:
          multipart/form-data:
            schema:
              type: object
              required:
                - file
              properties:
                file:
                  type: string
                  format: binary
                  description: PDF file to process
                settings:
                  type: string
                  description: |
                    JSON string with options:
                    - `perSheet` (integer, default 2) - One of: 2, 4, 6, 8
      responses:
        '200':
          description: Booklet-imposed PDF
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ToolResponse'
        '400':
          description: Invalid input
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Error'
        '401':
          description: Authentication required
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/UnauthorizedError'
  /api/v1/tools/pdf/watermark-pdf:
    post:
      operationId: watermarkPdf
      tags:
        - Tools
      summary: Водяной знак PDF
      description: Добавление текстового водяного знака на каждую страницу PDF.
      security:
        - bearerAuth: []
      requestBody:
        required: true
        content:
          multipart/form-data:
            schema:
              type: object
              required:
                - file
              properties:
                file:
                  type: string
                  format: binary
                  description: PDF file to watermark
                settings:
                  type: string
                  description: |
                    JSON string with options:
                    - `text` (string 1-200 chars, required) - Watermark text
                    - `position` (string, default "c") - One of: tl, tc, tr, l, c, r, bl, bc, br
                    - `fontSize` (integer 6-72, default 48) - Font size in points
                    - `opacity` (number 0.05-1, default 0.3) - Watermark opacity
                    - `rotation` (number -180 to 180, default 45) - Rotation angle in degrees
      responses:
        '200':
          description: Watermarked PDF
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ToolResponse'
        '400':
          description: Invalid input
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Error'
        '401':
          description: Authentication required
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/UnauthorizedError'
  /api/v1/tools/pdf/pdf-page-numbers:
    post:
      operationId: pdfPageNumbers
      tags:
        - Tools
      summary: Номера страниц PDF
      description: Добавление номеров страниц ("Страница N из M") на каждую страницу PDF.
      security:
        - bearerAuth: []
      requestBody:
        required: true
        content:
          multipart/form-data:
            schema:
              type: object
              required:
                - file
              properties:
                file:
                  type: string
                  format: binary
                  description: PDF file to process
                settings:
                  type: string
                  description: |
                    JSON string with options:
                    - `position` (string, default "bc") - One of: bl, bc, br, tl, tc, tr
                    - `fontSize` (integer 6-24, default 10) - Font size in points
      responses:
        '200':
          description: PDF with page numbers
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ToolResponse'
        '400':
          description: Invalid input
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Error'
        '401':
          description: Authentication required
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/UnauthorizedError'
  /api/v1/tools/files/convert-document:
    post:
      operationId: convertDocument
      tags:
        - Tools
      summary: Конвертация документа
      description: Конвертация между форматами Word, OpenDocument, RTF и обычного текста.
      security:
        - bearerAuth: []
      requestBody:
        required: true
        content:
          multipart/form-data:
            schema:
              type: object
              required:
                - file
              properties:
                file:
                  type: string
                  format: binary
                  description: Document file to convert (.docx, .doc, .odt, .rtf, .txt)
                settings:
                  type: string
                  description: |
                    JSON string with options:
                    - `format` (string, required) - One of: docx, odt, rtf, txt
                clientJobId:
                  type: string
                  description: Client-provided job ID for SSE progress tracking
      responses:
        '202':
          description: Accepted for async processing. Track progress via SSE at /api/v1/jobs/{jobId}/progress.
          content:
            application/json:
              schema:
                type: object
                properties:
                  jobId:
                    type: string
                  async:
                    type: boolean
                    example: true
        '400':
          description: Invalid input
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Error'
        '401':
          description: Authentication required
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/UnauthorizedError'
  /api/v1/tools/files/convert-presentation:
    post:
      operationId: convertPresentation
      tags:
        - Tools
      summary: Конвертация презентации
      description: Конвертация между форматами презентаций PowerPoint и OpenDocument.
      security:
        - bearerAuth: []
      requestBody:
        required: true
        content:
          multipart/form-data:
            schema:
              type: object
              required:
                - file
              properties:
                file:
                  type: string
                  format: binary
                  description: Presentation file to convert (.pptx, .ppt, .odp)
                settings:
                  type: string
                  description: |
                    JSON string with options:
                    - `format` (string, required) - One of: pptx, odp
                clientJobId:
                  type: string
                  description: Client-provided job ID for SSE progress tracking
      responses:
        '202':
          description: Accepted for async processing. Track progress via SSE at /api/v1/jobs/{jobId}/progress.
          content:
            application/json:
              schema:
                type: object
                properties:
                  jobId:
                    type: string
                  async:
                    type: boolean
                    example: true
        '400':
          description: Invalid input
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Error'
        '401':
          description: Authentication required
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/UnauthorizedError'
  /api/v1/tools/files/convert-spreadsheet:
    post:
      operationId: convertSpreadsheet
      tags:
        - Tools
      summary: Конвертация таблицы
      description: Конвертация между форматами Excel, OpenDocument и CSV. Многолистовые книги экспортируют первый лист в CSV.
      security:
        - bearerAuth: []
      requestBody:
        required: true
        content:
          multipart/form-data:
            schema:
              type: object
              required:
                - file
              properties:
                file:
                  type: string
                  format: binary
                  description: Spreadsheet file to convert (.xlsx, .xls, .ods, .csv)
                settings:
                  type: string
                  description: |
                    JSON string with options:
                    - `format` (string, required) - One of: xlsx, ods, csv
                clientJobId:
                  type: string
                  description: Client-provided job ID for SSE progress tracking
      responses:
        '202':
          description: Accepted for async processing. Track progress via SSE at /api/v1/jobs/{jobId}/progress.
          content:
            application/json:
              schema:
                type: object
                properties:
                  jobId:
                    type: string
                  async:
                    type: boolean
                    example: true
        '400':
          description: Invalid input
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Error'
        '401':
          description: Authentication required
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/UnauthorizedError'
  /api/v1/tools/files/excel-to-pdf:
    post:
      operationId: excelToPdf
      tags:
        - Tools
      summary: Excel в PDF
      description: Конвертация таблиц в PDF. Широкие листы могут разбиваться на несколько страниц.
      security:
        - bearerAuth: []
      requestBody:
        required: true
        content:
          multipart/form-data:
            schema:
              type: object
              required:
                - file
              properties:
                file:
                  type: string
                  format: binary
                  description: Spreadsheet file to convert (.xlsx, .xls, .ods, .csv)
                clientJobId:
                  type: string
                  description: Client-provided job ID for SSE progress tracking
      responses:
        '202':
          description: Accepted for async processing. Track progress via SSE at /api/v1/jobs/{jobId}/progress.
          content:
            application/json:
              schema:
                type: object
                properties:
                  jobId:
                    type: string
                  async:
                    type: boolean
                    example: true
        '400':
          description: Invalid input
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Error'
        '401':
          description: Authentication required
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/UnauthorizedError'
  /api/v1/tools/files/word-to-pdf:
    post:
      operationId: wordToPdf
      tags:
        - Tools
      summary: Word в PDF
      description: Конвертация документов Word в PDF.
      security:
        - bearerAuth: []
      requestBody:
        required: true
        content:
          multipart/form-data:
            schema:
              type: object
              required:
                - file
              properties:
                file:
                  type: string
                  format: binary
                  description: Word document to convert (.docx, .doc, .odt, .rtf, .txt)
                clientJobId:
                  type: string
                  description: Client-provided job ID for SSE progress tracking
      responses:
        '202':
          description: Accepted for async processing. Track progress via SSE at /api/v1/jobs/{jobId}/progress.
          content:
            application/json:
              schema:
                type: object
                properties:
                  jobId:
                    type: string
                  async:
                    type: boolean
                    example: true
        '400':
          description: Invalid input
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Error'
        '401':
          description: Authentication required
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/UnauthorizedError'
  /api/v1/tools/files/powerpoint-to-pdf:
    post:
      operationId: powerpointToPdf
      tags:
        - Tools
      summary: PowerPoint в PDF
      description: Конвертация презентаций в PDF.
      security:
        - bearerAuth: []
      requestBody:
        required: true
        content:
          multipart/form-data:
            schema:
              type: object
              required:
                - file
              properties:
                file:
                  type: string
                  format: binary
                  description: Presentation file to convert (.pptx, .ppt, .odp)
                clientJobId:
                  type: string
                  description: Client-provided job ID for SSE progress tracking
      responses:
        '202':
          description: Accepted for async processing. Track progress via SSE at /api/v1/jobs/{jobId}/progress.
          content:
            application/json:
              schema:
                type: object
                properties:
                  jobId:
                    type: string
                  async:
                    type: boolean
                    example: true
        '400':
          description: Invalid input
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Error'
        '401':
          description: Authentication required
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/UnauthorizedError'
  /api/v1/tools/files/html-to-pdf:
    post:
      operationId: htmlToPdf
      tags:
        - Tools
      summary: HTML в PDF
      description: Конвертация файла HTML в PDF. Удалённые ресурсы отключены для приватности.
      security:
        - bearerAuth: []
      requestBody:
        required: true
        content:
          multipart/form-data:
            schema:
              type: object
              required:
                - file
              properties:
                file:
                  type: string
                  format: binary
                  description: HTML file to convert (.html, .htm)
                clientJobId:
                  type: string
                  description: Client-provided job ID for SSE progress tracking
      responses:
        '202':
          description: Accepted for async processing. Track progress via SSE at /api/v1/jobs/{jobId}/progress.
          content:
            application/json:
              schema:
                type: object
                properties:
                  jobId:
                    type: string
                  async:
                    type: boolean
                    example: true
        '400':
          description: Invalid input
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Error'
        '401':
          description: Authentication required
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/UnauthorizedError'
  /api/v1/tools/files/markdown-to-docx:
    post:
      operationId: markdownToDocx
      tags:
        - Tools
      summary: Markdown в Word
      description: Конвертация файла Markdown в документ Word (DOCX).
      security:
        - bearerAuth: []
      requestBody:
        required: true
        content:
          multipart/form-data:
            schema:
              type: object
              required:
                - file
              properties:
                file:
                  type: string
                  format: binary
                  description: Markdown file to convert (.md, .markdown)
      responses:
        '200':
          description: Processed document
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ToolResponse'
        '400':
          description: Invalid input
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Error'
        '401':
          description: Authentication required
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/UnauthorizedError'
  /api/v1/tools/files/markdown-to-html:
    post:
      operationId: markdownToHtml
      tags:
        - Tools
      summary: Markdown в HTML
      description: Конвертация файла Markdown в автономную HTML-страницу. Удалённые изображения в исходнике остаются как есть в выводе.
      security:
        - bearerAuth: []
      requestBody:
        required: true
        content:
          multipart/form-data:
            schema:
              type: object
              required:
                - file
              properties:
                file:
                  type: string
                  format: binary
                  description: Markdown file to convert (.md, .markdown)
      responses:
        '200':
          description: Processed document
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ToolResponse'
        '400':
          description: Invalid input
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Error'
        '401':
          description: Authentication required
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/UnauthorizedError'
  /api/v1/tools/files/markdown-to-pdf:
    post:
      operationId: markdownToPdf
      tags:
        - Tools
      summary: Markdown в PDF
      description: Конвертация файла Markdown в стилизованный PDF. Удалённые ресурсы отключены для приватности.
      security:
        - bearerAuth: []
      requestBody:
        required: true
        content:
          multipart/form-data:
            schema:
              type: object
              required:
                - file
              properties:
                file:
                  type: string
                  format: binary
                  description: Markdown file to convert (.md, .markdown)
                clientJobId:
                  type: string
                  description: Client-provided job ID for SSE progress tracking
      responses:
        '202':
          description: Accepted for async processing. Track progress via SSE at /api/v1/jobs/{jobId}/progress.
          content:
            application/json:
              schema:
                type: object
                properties:
                  jobId:
                    type: string
                  async:
                    type: boolean
                    example: true
        '400':
          description: Invalid input
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Error'
        '401':
          description: Authentication required
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/UnauthorizedError'
  /api/v1/tools/files/epub-convert:
    post:
      operationId: epubConvert
      tags:
        - Tools
      summary: Конвертация EPUB
      description: Конвертация EPUB в PDF, DOCX, HTML или Markdown. Удалённые ресурсы внутри книги не загружаются.
      security:
        - bearerAuth: []
      requestBody:
        required: true
        content:
          multipart/form-data:
            schema:
              type: object
              required:
                - file
              properties:
                file:
                  type: string
                  format: binary
                  description: EPUB file to convert (.epub)
                settings:
                  type: string
                  description: |
                    JSON string with options:
                    - `format` (string, required) - One of: pdf, docx, html, md
                clientJobId:
                  type: string
                  description: Client-provided job ID for SSE progress tracking
      responses:
        '202':
          description: Accepted for async processing. Track progress via SSE at /api/v1/jobs/{jobId}/progress.
          content:
            application/json:
              schema:
                type: object
                properties:
                  jobId:
                    type: string
                  async:
                    type: boolean
                    example: true
        '400':
          description: Invalid input
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Error'
        '401':
          description: Authentication required
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/UnauthorizedError'
  /api/v1/tools/files/to-epub:
    post:
      operationId: toEpub
      tags:
        - Tools
      summary: Конвертация в EPUB
      description: Конвертация файлов Word, Markdown, HTML или обычного текста в EPUB.
      security:
        - bearerAuth: []
      requestBody:
        required: true
        content:
          multipart/form-data:
            schema:
              type: object
              required:
                - file
              properties:
                file:
                  type: string
                  format: binary
                  description: Document file to convert (.docx, .md, .html, .txt)
                clientJobId:
                  type: string
                  description: Client-provided job ID for SSE progress tracking
      responses:
        '202':
          description: Accepted for async processing. Track progress via SSE at /api/v1/jobs/{jobId}/progress.
          content:
            application/json:
              schema:
                type: object
                properties:
                  jobId:
                    type: string
                  async:
                    type: boolean
                    example: true
        '400':
          description: Invalid input
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Error'
        '401':
          description: Authentication required
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/UnauthorizedError'
  /api/v1/tools/pdf/flatten-pdf:
    post:
      operationId: flattenPdf
      tags:
        - Tools
      summary: Сведение PDF
      description: Впечатывание форм и аннотаций в содержимое страниц.
      security:
        - bearerAuth: []
      requestBody:
        required: true
        content:
          multipart/form-data:
            schema:
              type: object
              required:
                - file
              properties:
                file:
                  type: string
                  format: binary
                  description: PDF file to process (.pdf)
      responses:
        '200':
          description: Processed PDF
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ToolResponse'
        '400':
          description: Invalid input
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Error'
        '401':
          description: Authentication required
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/UnauthorizedError'
  /api/v1/tools/pdf/redact-pdf:
    post:
      operationId: redactPdf
      tags:
        - Tools
      summary: Редактирование PDF
      description: Безвозвратное удаление вхождений текста (проверенное истинное редактирование).
      security:
        - bearerAuth: []
      requestBody:
        required: true
        content:
          multipart/form-data:
            schema:
              type: object
              required:
                - file
              properties:
                file:
                  type: string
                  format: binary
                  description: PDF file to process (.pdf)
                settings:
                  type: string
                  description: |
                    JSON string with options:
                    - `terms` (string[], required) - Text strings to redact (1-50 terms, each up to 200 characters)
                    - `caseSensitive` (boolean, default false) - Whether matching is case-sensitive
      responses:
        '200':
          description: Processed PDF
          content:
            application/json:
              schema:
                allOf:
                  - $ref: '#/components/schemas/ToolResponse'
                  - type: object
                    properties:
                      found:
                        type: integer
                        description: Number of redacted occurrences found
        '400':
          description: Invalid input
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Error'
        '401':
          description: Authentication required
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/UnauthorizedError'
  /api/v1/tools/pdf/pdf-to-text:
    post:
      operationId: pdfToText
      tags:
        - Tools
      summary: PDF в текст
      description: Извлечение обычного текста из PDF.
      security:
        - bearerAuth: []
      requestBody:
        required: true
        content:
          multipart/form-data:
            schema:
              type: object
              required:
                - file
              properties:
                file:
                  type: string
                  format: binary
                  description: PDF file to process (.pdf)
      responses:
        '200':
          description: Extracted text file
          content:
            application/json:
              schema:
                allOf:
                  - $ref: '#/components/schemas/ToolResponse'
                  - type: object
                    properties:
                      chars:
                        type: integer
                        description: Number of characters extracted
        '400':
          description: Invalid input
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Error'
        '401':
          description: Authentication required
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/UnauthorizedError'
  /api/v1/tools/pdf/pdf-to-word:
    post:
      operationId: pdfToWord
      tags:
        - Tools
      summary: PDF в Word
      description: Лучше всего для текстовых PDF; для отсканированных страниц нужен OCR.
      security:
        - bearerAuth: []
      requestBody:
        required: true
        content:
          multipart/form-data:
            schema:
              type: object
              required:
                - file
              properties:
                file:
                  type: string
                  format: binary
                  description: PDF file to convert (.pdf)
                clientJobId:
                  type: string
                  description: Client-provided job ID for SSE progress tracking
      responses:
        '202':
          description: Accepted for async processing. Track progress via SSE at /api/v1/jobs/{jobId}/progress.
          content:
            application/json:
              schema:
                type: object
                properties:
                  jobId:
                    type: string
                  async:
                    type: boolean
                    example: true
        '400':
          description: Invalid input
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Error'
        '401':
          description: Authentication required
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/UnauthorizedError'
  /api/v1/tools/pdf/pdf-metadata:
    post:
      operationId: pdfMetadata
      tags:
        - Tools
      summary: Метаданные PDF
      description: Чтение и запись метаданных документа PDF.
      security:
        - bearerAuth: []
      requestBody:
        required: true
        content:
          multipart/form-data:
            schema:
              type: object
              required:
                - file
              properties:
                file:
                  type: string
                  format: binary
                  description: PDF file to process (.pdf)
                settings:
                  type: string
                  description: |
                    JSON string with options:
                    - `title` (string, optional, max 500) - Document title
                    - `author` (string, optional, max 500) - Document author
                    - `subject` (string, optional, max 500) - Document subject
                    - `keywords` (string, optional, max 500) - Document keywords
      responses:
        '200':
          description: Processed PDF with updated metadata
          content:
            application/json:
              schema:
                allOf:
                  - $ref: '#/components/schemas/ToolResponse'
                  - type: object
                    properties:
                      metadata:
                        type: object
                        description: The resulting PDF metadata fields
        '400':
          description: Invalid input
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Error'
        '401':
          description: Authentication required
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/UnauthorizedError'
  /api/v1/tools/pdf/ocr-pdf:
    post:
      operationId: ocrPdf
      tags:
        - Tools
      summary: OCR для PDF
      description: |-
        Извлекайте текст из отсканированных документов PDF постранично. Fast использует
        встроенная среда выполнения Tesseract. Balanced и Best используют необязательный подписанный
        Среда выполнения RapidOCR с закрепленными моделями PP-OCR ONNX. Явные запросы уровня
        никогда молча не понижайте рейтинг. Запрос может выбрать не более 50 страниц.
      security:
        - bearerAuth: []
      requestBody:
        required: true
        content:
          multipart/form-data:
            schema:
              type: object
              required:
                - file
              properties:
                file:
                  type: string
                  format: binary
                  maxLength: 536870912
                  description: PDF file to process (.pdf)
                settings:
                  type: string
                  description: |
                    JSON string with options:
                    - `quality` (string, optional) - One of: fast, balanced, best. When both quality and engine are omitted, uses the best available tier in this order: best, balanced, fast. Korean never selects fast; it uses best, then balanced, or returns an accurate-runtime install/compatibility error
                    - `language` (string, default "auto") - One of: auto, en, de, fr, es, zh, ja, ko. Korean is unsupported by fast and the legacy tesseract alias
                    - `pages` (string, default "all") - Page selection, e.g. "all", "1-3", "1,3,5"
                    - `enhance` (boolean, optional) - Improve local contrast. Fast applies it directly; accurate tiers retain the variant only when calibrated scoring improves the result. Defaults to true for Best and false for Fast/Balanced
                    - `engine` (string, deprecated) - Compatibility alias: tesseract maps to fast; the legacy paddleocr value maps to balanced but does not load PaddlePaddle
                clientJobId:
                  type: string
                  description: Client-provided job ID for SSE progress tracking
      responses:
        '202':
          description: Accepted for async processing. Track progress via SSE at /api/v1/jobs/{jobId}/progress.
          content:
            application/json:
              schema:
                type: object
                required:
                  - jobId
                  - async
                properties:
                  jobId:
                    type: string
                  progressJobId:
                    type: string
                  artifactJobId:
                    type: string
                  async:
                    type: boolean
                    const: true
        '400':
          description: Invalid input
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Error'
        '401':
          description: Authentication required
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/UnauthorizedError'
        '413':
          description: Encoded PDF exceeds the hard 512 MiB safety limit
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Error'
        '422':
          description: Processing failed
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Error'
        '501':
          description: Requested Balanced/Best runtime is not installed or is incompatible
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/FeatureNotInstalledError'
        '503':
          description: PDF OCR could not be queued
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Error'
  /api/v1/tools/files/chart-maker:
    post:
      operationId: chartMaker
      tags:
        - Tools
      summary: Создание диаграмм
      description: Создание столбчатых, линейных или круговых диаграмм из данных CSV или JSON. Возвращает изображение PNG.
      security:
        - bearerAuth: []
      requestBody:
        required: true
        content:
          multipart/form-data:
            schema:
              type: object
              required:
                - file
              properties:
                file:
                  type: string
                  format: binary
                  description: CSV or JSON data file
                settings:
                  type: string
                  description: |
                    JSON string with options:
                    - `kind` (string, default "bar") - One of: bar, line, pie
                    - `title` (string, optional) - Chart title (max 120 characters)
                    - `width` (integer 320-2048, default 960) - Chart width in pixels
                    - `height` (integer 240-1536, default 540) - Chart height in pixels
      responses:
        '200':
          description: Generated chart image
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ToolResponse'
        '400':
          description: Invalid input
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Error'
        '401':
          description: Authentication required
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/UnauthorizedError'
  /api/v1/tools/files/csv-excel:
    post:
      operationId: csvExcel
      tags:
        - Tools
      summary: CSV в Excel
      description: Конвертация между CSV и Excel (XLSX) в обоих направлениях. Загрузите файл CSV или TSV для получения XLSX, или загрузите файл XLSX для получения CSV.
      security:
        - bearerAuth: []
      requestBody:
        required: true
        content:
          multipart/form-data:
            schema:
              type: object
              required:
                - file
              properties:
                file:
                  type: string
                  format: binary
                  description: CSV, TSV, or XLSX file to convert
                settings:
                  type: string
                  description: |
                    JSON string with options:
                    - `sheet` (integer, min 1, default 1) - Worksheet number to export when converting from XLSX
      responses:
        '200':
          description: Converted file
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ToolResponse'
        '400':
          description: Invalid input
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Error'
        '401':
          description: Authentication required
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/UnauthorizedError'
  /api/v1/tools/files/csv-json:
    post:
      operationId: csvJson
      tags:
        - Tools
      summary: CSV в JSON
      description: Конвертация между CSV и JSON в обоих направлениях. Загрузите файл CSV или TSV для получения JSON, или загрузите JSON-массив объектов для получения CSV.
      security:
        - bearerAuth: []
      requestBody:
        required: true
        content:
          multipart/form-data:
            schema:
              type: object
              required:
                - file
              properties:
                file:
                  type: string
                  format: binary
                  description: CSV, TSV, or JSON file to convert
                settings:
                  type: string
                  description: |
                    JSON string with options:
                    - `pretty` (boolean, default true) - Pretty-print JSON output with indentation
      responses:
        '200':
          description: Converted file
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ToolResponse'
        '400':
          description: Invalid input
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Error'
        '401':
          description: Authentication required
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/UnauthorizedError'
  /api/v1/tools/files/json-xml:
    post:
      operationId: jsonXml
      tags:
        - Tools
      summary: JSON в XML
      description: Конвертация между JSON и XML в обоих направлениях. Загрузите файл JSON для получения XML, или загрузите файл XML для получения JSON.
      security:
        - bearerAuth: []
      requestBody:
        required: true
        content:
          multipart/form-data:
            schema:
              type: object
              required:
                - file
              properties:
                file:
                  type: string
                  format: binary
                  description: JSON or XML file to convert
                settings:
                  type: string
                  description: |
                    JSON string with options:
                    - `pretty` (boolean, default true) - Pretty-print output with indentation
      responses:
        '200':
          description: Converted file
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ToolResponse'
        '400':
          description: Invalid input
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Error'
        '401':
          description: Authentication required
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/UnauthorizedError'
  /api/v1/tools/files/split-csv:
    post:
      operationId: splitCsv
      tags:
        - Tools
      summary: Разделение CSV
      description: Разделение файла CSV на меньшие файлы по количеству строк. Возвращает ZIP-архив с частями.
      security:
        - bearerAuth: []
      requestBody:
        required: true
        content:
          multipart/form-data:
            schema:
              type: object
              required:
                - file
              properties:
                file:
                  type: string
                  format: binary
                  description: CSV or TSV file to split
                settings:
                  type: string
                  description: |
                    JSON string with options:
                    - `rowsPerFile` (integer 1-1000000, default 1000) - Number of data rows per output file
                    - `keepHeader` (boolean, default true) - Repeat the header row in each output file
      responses:
        '200':
          description: ZIP archive containing split CSV parts
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ToolResponse'
        '400':
          description: Invalid input
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Error'
        '401':
          description: Authentication required
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/UnauthorizedError'
  /api/v1/tools/files/merge-csvs:
    post:
      operationId: mergeCsvs
      tags:
        - Tools
      summary: Объединение CSV
      description: Объединение нескольких файлов CSV или TSV с совпадающими столбцами в один файл. Все файлы должны иметь одинаковые заголовки столбцов.
      security:
        - bearerAuth: []
      requestBody:
        required: true
        content:
          multipart/form-data:
            schema:
              type: object
              required:
                - file
              properties:
                file:
                  type: array
                  items:
                    type: string
                    format: binary
                  description: Two or more CSV or TSV files to merge (2-20 files)
      responses:
        '200':
          description: Merged CSV file
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ToolResponse'
        '400':
          description: Invalid input
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Error'
        '401':
          description: Authentication required
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/UnauthorizedError'
  /api/v1/tools/files/yaml-json:
    post:
      operationId: yamlJson
      tags:
        - Tools
      summary: YAML / JSON
      description: Конвертация между YAML и JSON в обоих направлениях. Загрузите файл YAML для получения JSON, или загрузите файл JSON для получения YAML.
      security:
        - bearerAuth: []
      requestBody:
        required: true
        content:
          multipart/form-data:
            schema:
              type: object
              required:
                - file
              properties:
                file:
                  type: string
                  format: binary
                  description: YAML (.yaml, .yml) or JSON file to convert
      responses:
        '200':
          description: Converted file
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ToolResponse'
        '400':
          description: Invalid input
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Error'
        '401':
          description: Authentication required
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/UnauthorizedError'
  /api/v1/tools/files/xml-to-csv:
    post:
      operationId: xmlToCsv
      tags:
        - Tools
      summary: XML в CSV
      description: Извлечение повторяющихся элементов из файла XML в таблицу CSV. Автоматически находит первый массив объектов в дереве XML.
      security:
        - bearerAuth: []
      requestBody:
        required: true
        content:
          multipart/form-data:
            schema:
              type: object
              required:
                - file
              properties:
                file:
                  type: string
                  format: binary
                  description: XML file to convert
      responses:
        '200':
          description: Extracted CSV file
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ToolResponse'
        '400':
          description: Invalid input
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Error'
        '401':
          description: Authentication required
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/UnauthorizedError'
  /api/v1/tools/files/create-zip:
    post:
      operationId: createZip
      tags:
        - Tools
      summary: Создание ZIP
      description: Упаковка нескольких файлов в один ZIP-архив. Дублирующиеся имена файлов автоматически устраняются.
      security:
        - bearerAuth: []
      requestBody:
        required: true
        content:
          multipart/form-data:
            schema:
              type: object
              required:
                - file
              properties:
                file:
                  type: array
                  items:
                    type: string
                    format: binary
                  description: Two or more files to bundle (2-50 files, any type)
      responses:
        '200':
          description: ZIP archive
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ToolResponse'
        '400':
          description: Invalid input
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Error'
        '401':
          description: Authentication required
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/UnauthorizedError'
  /api/v1/tools/files/extract-zip:
    post:
      operationId: extractZip
      tags:
        - Tools
      summary: Распаковка ZIP
      description: Безопасная распаковка файлов из ZIP-архива с защитой от bomb. Архивы с одним файлом возвращают файл напрямую; архивы с несколькими файлами возвращают плоский ZIP.
      security:
        - bearerAuth: []
      requestBody:
        required: true
        content:
          multipart/form-data:
            schema:
              type: object
              required:
                - file
              properties:
                file:
                  type: string
                  format: binary
                  description: ZIP archive to extract
      responses:
        '200':
          description: Extracted file or repackaged ZIP archive
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ToolResponse'
        '400':
          description: Invalid input
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Error'
        '401':
          description: Authentication required
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/UnauthorizedError'
  /api/v1/tools/image/background-replace:
    post:
      operationId: backgroundReplace
      tags:
        - Tools
      summary: Замена фона
      description: |
        Замена фона на базе AI. Удаляет фон объекта и накладывает его на
        сплошной цвет или градиент. Требует установки набора функций
        background-removal.
      security:
        - bearerAuth: []
      requestBody:
        required: true
        content:
          multipart/form-data:
            schema:
              type: object
              required:
                - file
              properties:
                file:
                  type: string
                  format: binary
                  description: Image file to process
                settings:
                  type: string
                  description: |
                    JSON string with options:
                    - `backgroundType` (string, default "color") - One of: color, gradient
                    - `color` (string, default "#ffffff") - Background hex color (when backgroundType is color)
                    - `gradientColor1` (string, optional) - First gradient hex color
                    - `gradientColor2` (string, optional) - Second gradient hex color
                    - `gradientAngle` (integer 0-360, default 180) - Gradient angle in degrees
                    - `feather` (integer 0-20, default 0) - Edge feathering radius
                    - `format` (string, default "png") - One of: png, webp
                clientJobId:
                  type: string
                  description: Client-provided job ID for SSE progress tracking
      responses:
        '202':
          description: Accepted for async processing. Track progress via SSE at /api/v1/jobs/{jobId}/progress.
          content:
            application/json:
              schema:
                type: object
                properties:
                  jobId:
                    type: string
                  async:
                    type: boolean
                    example: true
        '400':
          description: Invalid input
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Error'
        '401':
          description: Authentication required
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/UnauthorizedError'
        '422':
          description: Processing failed
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Error'
        '501':
          description: Feature not installed
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/FeatureNotInstalledError'
  /api/v1/tools/image/blur-background:
    post:
      operationId: blurBackground
      tags:
        - Tools
      summary: Размытие фона
      description: |
        Размытие фона на базе AI. Удаляет фон, размывает оригинал и
        накладывает чёткий объект поверх. Требует установки набора функций
        background-removal.
      security:
        - bearerAuth: []
      requestBody:
        required: true
        content:
          multipart/form-data:
            schema:
              type: object
              required:
                - file
              properties:
                file:
                  type: string
                  format: binary
                  description: Image file to process
                settings:
                  type: string
                  description: |
                    JSON string with options:
                    - `intensity` (integer 1-100, default 50) - Blur intensity
                    - `feather` (integer 0-20, default 0) - Edge feathering radius
                    - `format` (string, default "png") - One of: png, webp
                clientJobId:
                  type: string
                  description: Client-provided job ID for SSE progress tracking
      responses:
        '202':
          description: Accepted for async processing. Track progress via SSE at /api/v1/jobs/{jobId}/progress.
          content:
            application/json:
              schema:
                type: object
                properties:
                  jobId:
                    type: string
                  async:
                    type: boolean
                    example: true
        '400':
          description: Invalid input
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Error'
        '401':
          description: Authentication required
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/UnauthorizedError'
        '422':
          description: Processing failed
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Error'
        '501':
          description: Feature not installed
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/FeatureNotInstalledError'
  /api/v1/tools/image/barcode-generate:
    post:
      operationId: generateBarcode
      tags:
        - Tools
      summary: Генератор штрихкодов
      description: Создание изображения штрихкода из текста. Этот инструмент принимает JSON body, а не multipart.
      security:
        - bearerAuth: []
      requestBody:
        required: true
        content:
          application/json:
            schema:
              type: object
              required:
                - text
              properties:
                text:
                  type: string
                  minLength: 1
                  maxLength: 256
                  description: Text to encode in the barcode
                type:
                  type: string
                  enum:
                    - code128
                    - ean13
                    - upca
                    - code39
                    - itf14
                    - datamatrix
                  default: code128
                  description: Barcode format
                scale:
                  type: integer
                  minimum: 1
                  maximum: 8
                  default: 3
                  description: Image scale factor
                includeText:
                  type: boolean
                  default: true
                  description: Whether to render the text below the barcode
      responses:
        '200':
          description: Generated barcode
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ToolResponse'
        '400':
          description: Invalid input
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Error'
        '401':
          description: Authentication required
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/UnauthorizedError'
  /api/v1/tools/image/circle-crop:
    post:
      operationId: circleCrop
      tags:
        - Tools
      summary: Круглая обрезка
      description: Обрезка изображения до центрированного круга с прозрачными углами, опциональной рамкой и настраиваемым зумом/смещением.
      security:
        - bearerAuth: []
      requestBody:
        required: true
        content:
          multipart/form-data:
            schema:
              type: object
              required:
                - file
              properties:
                file:
                  type: string
                  format: binary
                  description: Image file to process
                settings:
                  type: string
                  description: |
                    JSON string with options:
                    - `zoom` (number 1-5, default 1) - Zoom factor (>=1 crops tighter)
                    - `offsetX` (number 0-1, default 0.5) - Horizontal center position
                    - `offsetY` (number 0-1, default 0.5) - Vertical center position
                    - `borderWidth` (integer 0-200, default 0) - Border width in pixels
                    - `borderColor` (string, default "#ffffff") - Border hex color
                    - `background` (string, default "transparent") - "transparent" or a hex color for corners
                    - `outputSize` (integer 16-4096, optional) - Final square dimension in pixels
      responses:
        '200':
          description: Processed image
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ToolResponse'
        '400':
          description: Invalid input
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Error'
        '401':
          description: Authentication required
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/UnauthorizedError'
  /api/v1/tools/image/duotone:
    post:
      operationId: duotone
      tags:
        - Tools
      summary: Дуотон
      description: Применение двухцветного эффекта дуотона с настраиваемыми цветами теней и светов.
      security:
        - bearerAuth: []
      requestBody:
        required: true
        content:
          multipart/form-data:
            schema:
              type: object
              required:
                - file
              properties:
                file:
                  type: string
                  format: binary
                  description: Image file to process
                settings:
                  type: string
                  description: |
                    JSON string with options:
                    - `shadow` (string, default "#1e3a8a") - Shadow hex color
                    - `highlight` (string, default "#fbbf24") - Highlight hex color
                    - `intensity` (integer 0-100, default 100) - Effect intensity (0 = original, 100 = full duotone)
      responses:
        '200':
          description: Processed image
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ToolResponse'
        '400':
          description: Invalid input
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Error'
        '401':
          description: Authentication required
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/UnauthorizedError'
  /api/v1/tools/image/image-pad:
    post:
      operationId: imagePad
      tags:
        - Tools
      summary: Заполнение изображения
      description: Заполнение изображения до целевого соотношения сторон сплошным цветом, прозрачным или размытым фоном.
      security:
        - bearerAuth: []
      requestBody:
        required: true
        content:
          multipart/form-data:
            schema:
              type: object
              required:
                - file
              properties:
                file:
                  type: string
                  format: binary
                  description: Image file to process
                settings:
                  type: string
                  description: |
                    JSON string with options:
                    - `target` (string, default "1:1") - One of: 16:9, 9:16, 1:1, 4:3, 3:4, custom
                    - `ratioW` (integer 1-100, default 1) - Custom ratio width (when target is custom)
                    - `ratioH` (integer 1-100, default 1) - Custom ratio height (when target is custom)
                    - `background` (string, default "color") - One of: color, transparent, blur
                    - `color` (string, default "#ffffff") - Background hex color (when background is color)
                    - `padding` (integer 0-50, default 0) - Extra padding as percentage of canvas
      responses:
        '200':
          description: Processed image
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ToolResponse'
        '400':
          description: Invalid input
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Error'
        '401':
          description: Authentication required
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/UnauthorizedError'
  /api/v1/tools/image/pixelate:
    post:
      operationId: pixelate
      tags:
        - Tools
      summary: Пикселизация
      description: Применение эффекта пикселизации ко всему изображению или к определённой прямоугольной области.
      security:
        - bearerAuth: []
      requestBody:
        required: true
        content:
          multipart/form-data:
            schema:
              type: object
              required:
                - file
              properties:
                file:
                  type: string
                  format: binary
                  description: Image file to process
                settings:
                  type: string
                  description: |
                    JSON string with options:
                    - `blockSize` (integer 2-128, default 12) - Pixel block size
                    - `region` (object, optional) - Restrict pixelation to a rectangle:
                      - `left` (integer >= 0) - Left offset in pixels
                      - `top` (integer >= 0) - Top offset in pixels
                      - `width` (integer >= 1) - Region width in pixels
                      - `height` (integer >= 1) - Region height in pixels
      responses:
        '200':
          description: Processed image
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ToolResponse'
        '400':
          description: Invalid input
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Error'
        '401':
          description: Authentication required
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/UnauthorizedError'
  /api/v1/tools/image/vignette:
    post:
      operationId: vignette
      tags:
        - Tools
      summary: Виньетка
      description: Добавление эффекта виньетки с настраиваемой силой, цветом, радиусом, мягкостью, округлостью и положением центра.
      security:
        - bearerAuth: []
      requestBody:
        required: true
        content:
          multipart/form-data:
            schema:
              type: object
              required:
                - file
              properties:
                file:
                  type: string
                  format: binary
                  description: Image file to process
                settings:
                  type: string
                  description: |
                    JSON string with options:
                    - `strength` (number 0.1-1, default 0.5) - Vignette opacity
                    - `color` (string, default "#000000") - Vignette hex color
                    - `radius` (integer 0-100, default 70) - Outer radius as percentage of half-diagonal
                    - `softness` (integer 0-100, default 50) - Feather softness (higher = more gradual)
                    - `roundness` (integer 0-100, default 100) - 100 = circle, 0 = ellipse matching image aspect ratio
                    - `centerX` (integer 0-100, default 50) - Horizontal center position as percentage
                    - `centerY` (integer 0-100, default 50) - Vertical center position as percentage
      responses:
        '200':
          description: Processed image
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ToolResponse'
        '400':
          description: Invalid input
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Error'
        '401':
          description: Authentication required
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/UnauthorizedError'
  /api/v1/tools/image/gif-webp:
    post:
      operationId: gifWebp
      tags:
        - Tools
      summary: Конвертер GIF/WebP
      description: Конвертация анимированного GIF в WebP и обратно с сохранением всех кадров. Принимает только входные файлы .gif и .webp.
      security:
        - bearerAuth: []
      requestBody:
        required: true
        content:
          multipart/form-data:
            schema:
              type: object
              required:
                - file
              properties:
                file:
                  type: string
                  format: binary
                  description: GIF or WebP file to convert
                settings:
                  type: string
                  description: |
                    JSON string with options:
                    - `quality` (integer 1-100, default 80) - Output quality for WebP encoding
                    - `lossless` (boolean, default false) - Use lossless WebP compression
                    - `resizePercent` (integer 10-100, default 100) - Scale the output by percentage
      responses:
        '200':
          description: Converted animation
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ToolResponse'
        '400':
          description: Invalid input
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Error'
        '401':
          description: Authentication required
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/UnauthorizedError'
  /api/v1/tools/image/histogram:
    post:
      operationId: histogram
      tags:
        - Tools
      summary: Гистограмма
      description: |
        Создание диаграммы RGB-гистограммы из изображения. Возвращает изображение
        гистограммы PNG плюс статистику по каналам и сырые данные гистограммы на
        256 корзин в JSON ответа.
      security:
        - bearerAuth: []
      requestBody:
        required: true
        content:
          multipart/form-data:
            schema:
              type: object
              required:
                - file
              properties:
                file:
                  type: string
                  format: binary
                  description: Image file to analyze
                settings:
                  type: string
                  description: |
                    JSON string with options:
                    - `scale` (string, default "linear") - One of: linear, log
      responses:
        '200':
          description: Histogram image and channel statistics
          content:
            application/json:
              schema:
                allOf:
                  - $ref: '#/components/schemas/ToolResponse'
                  - type: object
                    properties:
                      bins:
                        type: object
                        description: Raw 256-bin histogram arrays per channel
                        properties:
                          r:
                            type: array
                            items:
                              type: integer
                          g:
                            type: array
                            items:
                              type: integer
                          b:
                            type: array
                            items:
                              type: integer
                          lum:
                            type: array
                            items:
                              type: integer
                      stats:
                        type: object
                        description: Per-channel statistics
                        properties:
                          r:
                            type: object
                            properties:
                              mean:
                                type: integer
                              median:
                                type: integer
                              stdev:
                                type: number
                          g:
                            type: object
                            properties:
                              mean:
                                type: integer
                              median:
                                type: integer
                              stdev:
                                type: number
                          b:
                            type: object
                            properties:
                              mean:
                                type: integer
                              median:
                                type: integer
                              stdev:
                                type: number
                          lum:
                            type: object
                            properties:
                              mean:
                                type: integer
                              median:
                                type: integer
                              stdev:
                                type: number
                      mean:
                        type: object
                        description: Backward-compatible mean values
                        properties:
                          r:
                            type: integer
                          g:
                            type: integer
                          b:
                            type: integer
                      max:
                        type: object
                        description: Backward-compatible max bin values
                        properties:
                          r:
                            type: integer
                          g:
                            type: integer
                          b:
                            type: integer
        '400':
          description: Invalid input
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Error'
        '401':
          description: Authentication required
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/UnauthorizedError'
  /api/v1/tools/image/lqip-placeholder:
    post:
      operationId: lqipPlaceholder
      tags:
        - Tools
      summary: Плейсхолдер LQIP
      description: |
        Создание крошечного плейсхолдера изображения низкого качества. Возвращает
        маленький файл изображения плюс data URI в base64, готовый к использованию
        тег HTML img и фрагмент CSS background в JSON ответа.
      security:
        - bearerAuth: []
      requestBody:
        required: true
        content:
          multipart/form-data:
            schema:
              type: object
              required:
                - file
              properties:
                file:
                  type: string
                  format: binary
                  description: Image file to process
                settings:
                  type: string
                  description: |
                    JSON string with options:
                    - `width` (integer 4-64, default 16) - Target width in pixels
                    - `blur` (number 0-20, default 2) - Blur radius (for blur strategy)
                    - `strategy` (string, default "blur") - One of: blur, pixelate, solid
                    - `format` (string, default "webp") - One of: webp, png, jpeg
                    - `quality` (integer 1-100, default 50) - Output quality
      responses:
        '200':
          description: LQIP placeholder image and data URI
          content:
            application/json:
              schema:
                allOf:
                  - $ref: '#/components/schemas/ToolResponse'
                  - type: object
                    properties:
                      dataUri:
                        type: string
                        description: Complete data URI (data:mime;base64,...)
                      width:
                        type: integer
                        description: Placeholder width in pixels
                      height:
                        type: integer
                        description: Placeholder height in pixels
                      bytes:
                        type: integer
                        description: Placeholder file size in bytes
                      strategy:
                        type: string
                        description: Strategy used (blur, pixelate, or solid)
                      html:
                        type: string
                        description: Ready-to-use HTML img tag
                      css:
                        type: string
                        description: Ready-to-use CSS background-image snippet
        '400':
          description: Invalid input
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Error'
        '401':
          description: Authentication required
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/UnauthorizedError'
  /api/v1/tools/image/sprite-sheet:
    post:
      operationId: spriteSheet
      tags:
        - Tools
      summary: Спрайт-лист
      description: |
        Объединение нескольких изображений в единую сетку спрайт-листа. Принимает
        от 2 до 64 изображений. Каждое изображение изменяется под размеры первого
        изображения. Возвращает изображение спрайт-листа плюс метаданные координат
        кадров.
      security:
        - bearerAuth: []
      requestBody:
        required: true
        content:
          multipart/form-data:
            schema:
              type: object
              required:
                - file
              properties:
                file:
                  type: array
                  items:
                    type: string
                    format: binary
                  description: Two or more image files to combine (max 64)
                settings:
                  type: string
                  description: |
                    JSON string with options:
                    - `columns` (integer 1-16, default 4) - Number of columns in the grid
                    - `padding` (integer 0-64, default 0) - Padding between cells in pixels
                    - `background` (string, default "#ffffff") - Background hex color
                    - `format` (string, default "png") - One of: png, webp, jpeg
                    - `quality` (integer 1-100, default 90) - Output quality
      responses:
        '200':
          description: Sprite sheet image with frame metadata
          content:
            application/json:
              schema:
                allOf:
                  - $ref: '#/components/schemas/ToolResponse'
                  - type: object
                    properties:
                      frames:
                        type: array
                        description: Per-frame coordinates in the sprite sheet
                        items:
                          type: object
                          properties:
                            index:
                              type: integer
                            left:
                              type: integer
                            top:
                              type: integer
                            width:
                              type: integer
                            height:
                              type: integer
                      cols:
                        type: integer
                        description: Number of columns
                      rows:
                        type: integer
                        description: Number of rows
                      cellWidth:
                        type: integer
                        description: Cell width in pixels
                      cellHeight:
                        type: integer
                        description: Cell height in pixels
                      canvasWidth:
                        type: integer
                        description: Total canvas width in pixels
                      canvasHeight:
                        type: integer
                        description: Total canvas height in pixels
        '400':
          description: Invalid input
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Error'
        '401':
          description: Authentication required
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/UnauthorizedError'
  /api/v1/tools/image/passport-photo:
    post:
      operationId: passportPhotoTool
      tags:
        - Tools
      summary: Фото на паспорт
      description: |
        Справочный эндпоинт для двухэтапного потока создания фото на паспорт.
        Используйте `/api/v1/tools/image/passport-photo/analyze` для анализа изображения,
        затем `/api/v1/tools/image/passport-photo/generate` для создания итогового
        фото на паспорт. Этот базовый маршрут возвращает справочную ошибку 400,
        когда требуемые наборы функций установлены, или 501, когда они отсутствуют.
      security:
        - bearerAuth: []
      responses:
        '400':
          description: Use the analyze or generate endpoint
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Error'
        '401':
          description: Authentication required
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/UnauthorizedError'
        '501':
          description: Feature not installed
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/FeatureNotInstalledError'
  /api/v1/tools/pdf/sign-pdf:
    post:
      operationId: signPdfTool
      tags:
        - Tools
      summary: Подпись PDF
      description: Наложение одного или нескольких PNG-изображений подписи на PDF с использованием нормализованных позиций на страницах.
      security:
        - bearerAuth: []
      requestBody:
        required: true
        content:
          multipart/form-data:
            schema:
              type: object
              required:
                - file
                - placements
                - sig0
              properties:
                file:
                  type: string
                  format: binary
                  description: PDF file to sign
                sig0:
                  type: string
                  format: binary
                  description: First signature image. Additional signatures use sig1, sig2, and so on.
                placements:
                  type: string
                  description: JSON array of placement objects with sig, page, x, y, w, and h. Coordinates are page fractions with top-left origin.
                clientJobId:
                  type: string
                  description: Client-provided job ID for SSE progress tracking
                fileId:
                  type: string
                  description: Optional file library ID to save the signed result as a new version
      responses:
        '200':
          description: Signed PDF
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ToolResponse'
        '202':
          description: Accepted for async processing. Track progress via SSE at /api/v1/jobs/{jobId}/progress.
          content:
            application/json:
              schema:
                type: object
                properties:
                  jobId:
                    type: string
                  async:
                    type: boolean
                    example: true
        '400':
          description: Invalid input or settings
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Error'
        '401':
          description: Authentication required
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/UnauthorizedError'
        '422':
          description: Processing failed
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Error'
  /api/v1/tools/image/jpg-to-png:
    post:
      operationId: jpgToPngTool
      tags:
        - Tools
      summary: JPG в PNG
      description: |
        Конвертация JPG в PNG
        Это выделенный эндпоинт-пресет конвертации на базе `convert`.
      security:
        - bearerAuth: []
      requestBody:
        required: true
        content:
          multipart/form-data:
            schema:
              type: object
              required:
                - file
              properties:
                file:
                  type: string
                  format: binary
                  description: JPG to PNG input file (.jpg, .jpeg)
                settings:
                  type: string
                  description: |
                    Dedicated JPG to PNG converter using the convert pipeline.
                    Optional settings: quality (number 1-100). The output format is locked by the endpoint.
                clientJobId:
                  type: string
                  description: Client-provided job ID for SSE progress tracking
      responses:
        '200':
          description: Processed file
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ToolResponse'
        '400':
          description: Invalid input or settings
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Error'
        '401':
          description: Authentication required
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/UnauthorizedError'
        '422':
          description: Processing failed
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Error'
  /api/v1/tools/image/png-to-jpg:
    post:
      operationId: pngToJpgTool
      tags:
        - Tools
      summary: PNG в JPG
      description: |
        Конвертация PNG в JPG
        Это выделенный эндпоинт-пресет конвертации на базе `convert`.
      security:
        - bearerAuth: []
      requestBody:
        required: true
        content:
          multipart/form-data:
            schema:
              type: object
              required:
                - file
              properties:
                file:
                  type: string
                  format: binary
                  description: PNG to JPG input file (.png)
                settings:
                  type: string
                  description: |
                    Dedicated PNG to JPG converter using the convert pipeline.
                    Optional settings: quality (number 1-100). The output format is locked by the endpoint.
                clientJobId:
                  type: string
                  description: Client-provided job ID for SSE progress tracking
      responses:
        '200':
          description: Processed file
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ToolResponse'
        '400':
          description: Invalid input or settings
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Error'
        '401':
          description: Authentication required
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/UnauthorizedError'
        '422':
          description: Processing failed
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Error'
  /api/v1/tools/image/jpg-to-webp:
    post:
      operationId: jpgToWebpTool
      tags:
        - Tools
      summary: JPG в WebP
      description: |
        Конвертация JPG в WebP
        Это выделенный эндпоинт-пресет конвертации на базе `convert`.
      security:
        - bearerAuth: []
      requestBody:
        required: true
        content:
          multipart/form-data:
            schema:
              type: object
              required:
                - file
              properties:
                file:
                  type: string
                  format: binary
                  description: JPG to WebP input file (.jpg, .jpeg)
                settings:
                  type: string
                  description: |
                    Dedicated JPG to WebP converter using the convert pipeline.
                    Optional settings: quality (number 1-100). The output format is locked by the endpoint.
                clientJobId:
                  type: string
                  description: Client-provided job ID for SSE progress tracking
      responses:
        '200':
          description: Processed file
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ToolResponse'
        '400':
          description: Invalid input or settings
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Error'
        '401':
          description: Authentication required
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/UnauthorizedError'
        '422':
          description: Processing failed
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Error'
  /api/v1/tools/image/png-to-webp:
    post:
      operationId: pngToWebpTool
      tags:
        - Tools
      summary: PNG в WebP
      description: |
        Конвертация PNG в WebP
        Это выделенный эндпоинт-пресет конвертации на базе `convert`.
      security:
        - bearerAuth: []
      requestBody:
        required: true
        content:
          multipart/form-data:
            schema:
              type: object
              required:
                - file
              properties:
                file:
                  type: string
                  format: binary
                  description: PNG to WebP input file (.png)
                settings:
                  type: string
                  description: |
                    Dedicated PNG to WebP converter using the convert pipeline.
                    Optional settings: quality (number 1-100). The output format is locked by the endpoint.
                clientJobId:
                  type: string
                  description: Client-provided job ID for SSE progress tracking
      responses:
        '200':
          description: Processed file
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ToolResponse'
        '400':
          description: Invalid input or settings
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Error'
        '401':
          description: Authentication required
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/UnauthorizedError'
        '422':
          description: Processing failed
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Error'
  /api/v1/tools/image/webp-to-jpg:
    post:
      operationId: webpToJpgTool
      tags:
        - Tools
      summary: WebP в JPG
      description: |
        Конвертация WebP в JPG
        Это выделенный эндпоинт-пресет конвертации на базе `convert`.
      security:
        - bearerAuth: []
      requestBody:
        required: true
        content:
          multipart/form-data:
            schema:
              type: object
              required:
                - file
              properties:
                file:
                  type: string
                  format: binary
                  description: WebP to JPG input file (.webp)
                settings:
                  type: string
                  description: |
                    Dedicated WebP to JPG converter using the convert pipeline.
                    Optional settings: quality (number 1-100). The output format is locked by the endpoint.
                clientJobId:
                  type: string
                  description: Client-provided job ID for SSE progress tracking
      responses:
        '200':
          description: Processed file
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ToolResponse'
        '400':
          description: Invalid input or settings
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Error'
        '401':
          description: Authentication required
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/UnauthorizedError'
        '422':
          description: Processing failed
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Error'
  /api/v1/tools/image/webp-to-png:
    post:
      operationId: webpToPngTool
      tags:
        - Tools
      summary: WebP в PNG
      description: |
        Конвертация WebP в PNG
        Это выделенный эндпоинт-пресет конвертации на базе `convert`.
      security:
        - bearerAuth: []
      requestBody:
        required: true
        content:
          multipart/form-data:
            schema:
              type: object
              required:
                - file
              properties:
                file:
                  type: string
                  format: binary
                  description: WebP to PNG input file (.webp)
                settings:
                  type: string
                  description: |
                    Dedicated WebP to PNG converter using the convert pipeline.
                    Optional settings: quality (number 1-100). The output format is locked by the endpoint.
                clientJobId:
                  type: string
                  description: Client-provided job ID for SSE progress tracking
      responses:
        '200':
          description: Processed file
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ToolResponse'
        '400':
          description: Invalid input or settings
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Error'
        '401':
          description: Authentication required
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/UnauthorizedError'
        '422':
          description: Processing failed
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Error'
  /api/v1/tools/image/jpg-to-avif:
    post:
      operationId: jpgToAvifTool
      tags:
        - Tools
      summary: JPG в AVIF
      description: |
        Конвертация JPG в AVIF
        Это выделенный эндпоинт-пресет конвертации на базе `convert`.
      security:
        - bearerAuth: []
      requestBody:
        required: true
        content:
          multipart/form-data:
            schema:
              type: object
              required:
                - file
              properties:
                file:
                  type: string
                  format: binary
                  description: JPG to AVIF input file (.jpg, .jpeg)
                settings:
                  type: string
                  description: |
                    Dedicated JPG to AVIF converter using the convert pipeline.
                    Optional settings: quality (number 1-100). The output format is locked by the endpoint.
                clientJobId:
                  type: string
                  description: Client-provided job ID for SSE progress tracking
      responses:
        '200':
          description: Processed file
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ToolResponse'
        '400':
          description: Invalid input or settings
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Error'
        '401':
          description: Authentication required
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/UnauthorizedError'
        '422':
          description: Processing failed
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Error'
  /api/v1/tools/image/png-to-avif:
    post:
      operationId: pngToAvifTool
      tags:
        - Tools
      summary: PNG в AVIF
      description: |
        Конвертация PNG в AVIF
        Это выделенный эндпоинт-пресет конвертации на базе `convert`.
      security:
        - bearerAuth: []
      requestBody:
        required: true
        content:
          multipart/form-data:
            schema:
              type: object
              required:
                - file
              properties:
                file:
                  type: string
                  format: binary
                  description: PNG to AVIF input file (.png)
                settings:
                  type: string
                  description: |
                    Dedicated PNG to AVIF converter using the convert pipeline.
                    Optional settings: quality (number 1-100). The output format is locked by the endpoint.
                clientJobId:
                  type: string
                  description: Client-provided job ID for SSE progress tracking
      responses:
        '200':
          description: Processed file
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ToolResponse'
        '400':
          description: Invalid input or settings
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Error'
        '401':
          description: Authentication required
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/UnauthorizedError'
        '422':
          description: Processing failed
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Error'
  /api/v1/tools/image/webp-to-avif:
    post:
      operationId: webpToAvifTool
      tags:
        - Tools
      summary: WebP в AVIF
      description: |
        Конвертация WebP в AVIF
        Это выделенный эндпоинт-пресет конвертации на базе `convert`.
      security:
        - bearerAuth: []
      requestBody:
        required: true
        content:
          multipart/form-data:
            schema:
              type: object
              required:
                - file
              properties:
                file:
                  type: string
                  format: binary
                  description: WebP to AVIF input file (.webp)
                settings:
                  type: string
                  description: |
                    Dedicated WebP to AVIF converter using the convert pipeline.
                    Optional settings: quality (number 1-100). The output format is locked by the endpoint.
                clientJobId:
                  type: string
                  description: Client-provided job ID for SSE progress tracking
      responses:
        '200':
          description: Processed file
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ToolResponse'
        '400':
          description: Invalid input or settings
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Error'
        '401':
          description: Authentication required
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/UnauthorizedError'
        '422':
          description: Processing failed
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Error'
  /api/v1/tools/image/heic-to-jpg:
    post:
      operationId: heicToJpgTool
      tags:
        - Tools
      summary: HEIC в JPG
      description: |
        Конвертация HEIC в JPG
        Это выделенный эндпоинт-пресет конвертации на базе `convert`.
      security:
        - bearerAuth: []
      requestBody:
        required: true
        content:
          multipart/form-data:
            schema:
              type: object
              required:
                - file
              properties:
                file:
                  type: string
                  format: binary
                  description: HEIC to JPG input file (.heic, .heif)
                settings:
                  type: string
                  description: |
                    Dedicated HEIC to JPG converter using the convert pipeline.
                    Optional settings: quality (number 1-100). The output format is locked by the endpoint.
                clientJobId:
                  type: string
                  description: Client-provided job ID for SSE progress tracking
      responses:
        '200':
          description: Processed file
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ToolResponse'
        '400':
          description: Invalid input or settings
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Error'
        '401':
          description: Authentication required
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/UnauthorizedError'
        '422':
          description: Processing failed
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Error'
  /api/v1/tools/image/heic-to-png:
    post:
      operationId: heicToPngTool
      tags:
        - Tools
      summary: HEIC в PNG
      description: |
        Конвертация HEIC в PNG
        Это выделенный эндпоинт-пресет конвертации на базе `convert`.
      security:
        - bearerAuth: []
      requestBody:
        required: true
        content:
          multipart/form-data:
            schema:
              type: object
              required:
                - file
              properties:
                file:
                  type: string
                  format: binary
                  description: HEIC to PNG input file (.heic, .heif)
                settings:
                  type: string
                  description: |
                    Dedicated HEIC to PNG converter using the convert pipeline.
                    Optional settings: quality (number 1-100). The output format is locked by the endpoint.
                clientJobId:
                  type: string
                  description: Client-provided job ID for SSE progress tracking
      responses:
        '200':
          description: Processed file
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ToolResponse'
        '400':
          description: Invalid input or settings
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Error'
        '401':
          description: Authentication required
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/UnauthorizedError'
        '422':
          description: Processing failed
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Error'
  /api/v1/tools/image/heic-to-avif:
    post:
      operationId: heicToAvifTool
      tags:
        - Tools
      summary: HEIC в AVIF
      description: |
        Конвертация HEIC в AVIF
        Это выделенный эндпоинт-пресет конвертации на базе `convert`.
      security:
        - bearerAuth: []
      requestBody:
        required: true
        content:
          multipart/form-data:
            schema:
              type: object
              required:
                - file
              properties:
                file:
                  type: string
                  format: binary
                  description: HEIC to AVIF input file (.heic, .heif)
                settings:
                  type: string
                  description: |
                    Dedicated HEIC to AVIF converter using the convert pipeline.
                    Optional settings: quality (number 1-100). The output format is locked by the endpoint.
                clientJobId:
                  type: string
                  description: Client-provided job ID for SSE progress tracking
      responses:
        '200':
          description: Processed file
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ToolResponse'
        '400':
          description: Invalid input or settings
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Error'
        '401':
          description: Authentication required
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/UnauthorizedError'
        '422':
          description: Processing failed
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Error'
  /api/v1/tools/image/jpg-to-gif:
    post:
      operationId: jpgToGifTool
      tags:
        - Tools
      summary: JPG в GIF
      description: |
        Конвертация JPG в GIF
        Это выделенный эндпоинт-пресет конвертации на базе `convert`.
      security:
        - bearerAuth: []
      requestBody:
        required: true
        content:
          multipart/form-data:
            schema:
              type: object
              required:
                - file
              properties:
                file:
                  type: string
                  format: binary
                  description: JPG to GIF input file (.jpg, .jpeg)
                settings:
                  type: string
                  description: |
                    Dedicated JPG to GIF converter using the convert pipeline.
                    Optional settings: quality (number 1-100). The output format is locked by the endpoint.
                clientJobId:
                  type: string
                  description: Client-provided job ID for SSE progress tracking
      responses:
        '200':
          description: Processed file
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ToolResponse'
        '400':
          description: Invalid input or settings
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Error'
        '401':
          description: Authentication required
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/UnauthorizedError'
        '422':
          description: Processing failed
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Error'
  /api/v1/tools/image/png-to-gif:
    post:
      operationId: pngToGifTool
      tags:
        - Tools
      summary: PNG в GIF
      description: |
        Конвертация PNG в GIF
        Это выделенный эндпоинт-пресет конвертации на базе `convert`.
      security:
        - bearerAuth: []
      requestBody:
        required: true
        content:
          multipart/form-data:
            schema:
              type: object
              required:
                - file
              properties:
                file:
                  type: string
                  format: binary
                  description: PNG to GIF input file (.png)
                settings:
                  type: string
                  description: |
                    Dedicated PNG to GIF converter using the convert pipeline.
                    Optional settings: quality (number 1-100). The output format is locked by the endpoint.
                clientJobId:
                  type: string
                  description: Client-provided job ID for SSE progress tracking
      responses:
        '200':
          description: Processed file
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ToolResponse'
        '400':
          description: Invalid input or settings
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Error'
        '401':
          description: Authentication required
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/UnauthorizedError'
        '422':
          description: Processing failed
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Error'
  /api/v1/tools/image/gif-to-jpg:
    post:
      operationId: gifToJpgTool
      tags:
        - Tools
      summary: GIF в JPG
      description: |
        Конвертация GIF в JPG
        Это выделенный эндпоинт-пресет конвертации на базе `convert`.
      security:
        - bearerAuth: []
      requestBody:
        required: true
        content:
          multipart/form-data:
            schema:
              type: object
              required:
                - file
              properties:
                file:
                  type: string
                  format: binary
                  description: GIF to JPG input file (.gif)
                settings:
                  type: string
                  description: |
                    Dedicated GIF to JPG converter using the convert pipeline.
                    Optional settings: quality (number 1-100). The output format is locked by the endpoint.
                clientJobId:
                  type: string
                  description: Client-provided job ID for SSE progress tracking
      responses:
        '200':
          description: Processed file
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ToolResponse'
        '400':
          description: Invalid input or settings
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Error'
        '401':
          description: Authentication required
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/UnauthorizedError'
        '422':
          description: Processing failed
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Error'
  /api/v1/tools/image/gif-to-png:
    post:
      operationId: gifToPngTool
      tags:
        - Tools
      summary: GIF в PNG
      description: |
        Конвертация GIF в PNG
        Это выделенный эндпоинт-пресет конвертации на базе `convert`.
      security:
        - bearerAuth: []
      requestBody:
        required: true
        content:
          multipart/form-data:
            schema:
              type: object
              required:
                - file
              properties:
                file:
                  type: string
                  format: binary
                  description: GIF to PNG input file (.gif)
                settings:
                  type: string
                  description: |
                    Dedicated GIF to PNG converter using the convert pipeline.
                    Optional settings: quality (number 1-100). The output format is locked by the endpoint.
                clientJobId:
                  type: string
                  description: Client-provided job ID for SSE progress tracking
      responses:
        '200':
          description: Processed file
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ToolResponse'
        '400':
          description: Invalid input or settings
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Error'
        '401':
          description: Authentication required
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/UnauthorizedError'
        '422':
          description: Processing failed
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Error'
  /api/v1/tools/image/webp-to-gif:
    post:
      operationId: webpToGifTool
      tags:
        - Tools
      summary: WebP в GIF
      description: |
        Конвертация WebP в GIF
        Это выделенный эндпоинт-пресет конвертации на базе `convert`.
      security:
        - bearerAuth: []
      requestBody:
        required: true
        content:
          multipart/form-data:
            schema:
              type: object
              required:
                - file
              properties:
                file:
                  type: string
                  format: binary
                  description: WebP to GIF input file (.webp)
                settings:
                  type: string
                  description: |
                    Dedicated WebP to GIF converter using the convert pipeline.
                    Optional settings: quality (number 1-100). The output format is locked by the endpoint.
                clientJobId:
                  type: string
                  description: Client-provided job ID for SSE progress tracking
      responses:
        '200':
          description: Processed file
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ToolResponse'
        '400':
          description: Invalid input or settings
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Error'
        '401':
          description: Authentication required
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/UnauthorizedError'
        '422':
          description: Processing failed
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Error'
  /api/v1/tools/image/jpg-to-tiff:
    post:
      operationId: jpgToTiffTool
      tags:
        - Tools
      summary: JPG в TIFF
      description: |
        Конвертация JPG в TIFF
        Это выделенный эндпоинт-пресет конвертации на базе `convert`.
      security:
        - bearerAuth: []
      requestBody:
        required: true
        content:
          multipart/form-data:
            schema:
              type: object
              required:
                - file
              properties:
                file:
                  type: string
                  format: binary
                  description: JPG to TIFF input file (.jpg, .jpeg)
                settings:
                  type: string
                  description: |
                    Dedicated JPG to TIFF converter using the convert pipeline.
                    Optional settings: quality (number 1-100). The output format is locked by the endpoint.
                clientJobId:
                  type: string
                  description: Client-provided job ID for SSE progress tracking
      responses:
        '200':
          description: Processed file
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ToolResponse'
        '400':
          description: Invalid input or settings
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Error'
        '401':
          description: Authentication required
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/UnauthorizedError'
        '422':
          description: Processing failed
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Error'
  /api/v1/tools/image/png-to-tiff:
    post:
      operationId: pngToTiffTool
      tags:
        - Tools
      summary: PNG в TIFF
      description: |
        Конвертация PNG в TIFF
        Это выделенный эндпоинт-пресет конвертации на базе `convert`.
      security:
        - bearerAuth: []
      requestBody:
        required: true
        content:
          multipart/form-data:
            schema:
              type: object
              required:
                - file
              properties:
                file:
                  type: string
                  format: binary
                  description: PNG to TIFF input file (.png)
                settings:
                  type: string
                  description: |
                    Dedicated PNG to TIFF converter using the convert pipeline.
                    Optional settings: quality (number 1-100). The output format is locked by the endpoint.
                clientJobId:
                  type: string
                  description: Client-provided job ID for SSE progress tracking
      responses:
        '200':
          description: Processed file
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ToolResponse'
        '400':
          description: Invalid input or settings
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Error'
        '401':
          description: Authentication required
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/UnauthorizedError'
        '422':
          description: Processing failed
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Error'
  /api/v1/tools/image/tiff-to-jpg:
    post:
      operationId: tiffToJpgTool
      tags:
        - Tools
      summary: TIFF в JPG
      description: |
        Конвертация TIFF в JPG
        Это выделенный эндпоинт-пресет конвертации на базе `convert`.
      security:
        - bearerAuth: []
      requestBody:
        required: true
        content:
          multipart/form-data:
            schema:
              type: object
              required:
                - file
              properties:
                file:
                  type: string
                  format: binary
                  description: TIFF to JPG input file (.tiff, .tif)
                settings:
                  type: string
                  description: |
                    Dedicated TIFF to JPG converter using the convert pipeline.
                    Optional settings: quality (number 1-100). The output format is locked by the endpoint.
                clientJobId:
                  type: string
                  description: Client-provided job ID for SSE progress tracking
      responses:
        '200':
          description: Processed file
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ToolResponse'
        '400':
          description: Invalid input or settings
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Error'
        '401':
          description: Authentication required
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/UnauthorizedError'
        '422':
          description: Processing failed
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Error'
  /api/v1/tools/image/tiff-to-png:
    post:
      operationId: tiffToPngTool
      tags:
        - Tools
      summary: TIFF в PNG
      description: |
        Конвертация TIFF в PNG
        Это выделенный эндпоинт-пресет конвертации на базе `convert`.
      security:
        - bearerAuth: []
      requestBody:
        required: true
        content:
          multipart/form-data:
            schema:
              type: object
              required:
                - file
              properties:
                file:
                  type: string
                  format: binary
                  description: TIFF to PNG input file (.tiff, .tif)
                settings:
                  type: string
                  description: |
                    Dedicated TIFF to PNG converter using the convert pipeline.
                    Optional settings: quality (number 1-100). The output format is locked by the endpoint.
                clientJobId:
                  type: string
                  description: Client-provided job ID for SSE progress tracking
      responses:
        '200':
          description: Processed file
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ToolResponse'
        '400':
          description: Invalid input or settings
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Error'
        '401':
          description: Authentication required
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/UnauthorizedError'
        '422':
          description: Processing failed
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Error'
  /api/v1/tools/image/psd-to-jpg:
    post:
      operationId: psdToJpgTool
      tags:
        - Tools
      summary: PSD в JPG
      description: |
        Конвертация PSD в JPG
        Это выделенный эндпоинт-пресет конвертации на базе `convert`.
      security:
        - bearerAuth: []
      requestBody:
        required: true
        content:
          multipart/form-data:
            schema:
              type: object
              required:
                - file
              properties:
                file:
                  type: string
                  format: binary
                  description: PSD to JPG input file (.psd)
                settings:
                  type: string
                  description: |
                    Dedicated PSD to JPG converter using the convert pipeline.
                    Optional settings: quality (number 1-100). The output format is locked by the endpoint.
                clientJobId:
                  type: string
                  description: Client-provided job ID for SSE progress tracking
      responses:
        '200':
          description: Processed file
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ToolResponse'
        '400':
          description: Invalid input or settings
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Error'
        '401':
          description: Authentication required
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/UnauthorizedError'
        '422':
          description: Processing failed
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Error'
  /api/v1/tools/image/psd-to-png:
    post:
      operationId: psdToPngTool
      tags:
        - Tools
      summary: PSD в PNG
      description: |
        Конвертация PSD в PNG
        Это выделенный эндпоинт-пресет конвертации на базе `convert`.
      security:
        - bearerAuth: []
      requestBody:
        required: true
        content:
          multipart/form-data:
            schema:
              type: object
              required:
                - file
              properties:
                file:
                  type: string
                  format: binary
                  description: PSD to PNG input file (.psd)
                settings:
                  type: string
                  description: |
                    Dedicated PSD to PNG converter using the convert pipeline.
                    Optional settings: quality (number 1-100). The output format is locked by the endpoint.
                clientJobId:
                  type: string
                  description: Client-provided job ID for SSE progress tracking
      responses:
        '200':
          description: Processed file
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ToolResponse'
        '400':
          description: Invalid input or settings
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Error'
        '401':
          description: Authentication required
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/UnauthorizedError'
        '422':
          description: Processing failed
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Error'
  /api/v1/tools/image/png-to-eps:
    post:
      operationId: pngToEpsTool
      tags:
        - Tools
      summary: PNG в EPS
      description: |
        Конвертация PNG в EPS
        Это выделенный эндпоинт-пресет конвертации на базе `convert`.
      security:
        - bearerAuth: []
      requestBody:
        required: true
        content:
          multipart/form-data:
            schema:
              type: object
              required:
                - file
              properties:
                file:
                  type: string
                  format: binary
                  description: PNG to EPS input file (.png)
                settings:
                  type: string
                  description: |
                    Dedicated PNG to EPS converter using the convert pipeline.
                    Optional settings: quality (number 1-100). The output format is locked by the endpoint.
                clientJobId:
                  type: string
                  description: Client-provided job ID for SSE progress tracking
      responses:
        '200':
          description: Processed file
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ToolResponse'
        '400':
          description: Invalid input or settings
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Error'
        '401':
          description: Authentication required
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/UnauthorizedError'
        '422':
          description: Processing failed
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Error'
  /api/v1/tools/image/jpg-to-eps:
    post:
      operationId: jpgToEpsTool
      tags:
        - Tools
      summary: JPG в EPS
      description: |
        Конвертация JPG в EPS
        Это выделенный эндпоинт-пресет конвертации на базе `convert`.
      security:
        - bearerAuth: []
      requestBody:
        required: true
        content:
          multipart/form-data:
            schema:
              type: object
              required:
                - file
              properties:
                file:
                  type: string
                  format: binary
                  description: JPG to EPS input file (.jpg, .jpeg)
                settings:
                  type: string
                  description: |
                    Dedicated JPG to EPS converter using the convert pipeline.
                    Optional settings: quality (number 1-100). The output format is locked by the endpoint.
                clientJobId:
                  type: string
                  description: Client-provided job ID for SSE progress tracking
      responses:
        '200':
          description: Processed file
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ToolResponse'
        '400':
          description: Invalid input or settings
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Error'
        '401':
          description: Authentication required
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/UnauthorizedError'
        '422':
          description: Processing failed
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Error'
  /api/v1/tools/image/eps-to-png:
    post:
      operationId: epsToPngTool
      tags:
        - Tools
      summary: EPS в PNG
      description: |
        Конвертация EPS в PNG
        Это выделенный эндпоинт-пресет конвертации на базе `convert`.
      security:
        - bearerAuth: []
      requestBody:
        required: true
        content:
          multipart/form-data:
            schema:
              type: object
              required:
                - file
              properties:
                file:
                  type: string
                  format: binary
                  description: EPS to PNG input file (.eps)
                settings:
                  type: string
                  description: |
                    Dedicated EPS to PNG converter using the convert pipeline.
                    Optional settings: quality (number 1-100). The output format is locked by the endpoint.
                clientJobId:
                  type: string
                  description: Client-provided job ID for SSE progress tracking
      responses:
        '200':
          description: Processed file
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ToolResponse'
        '400':
          description: Invalid input or settings
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Error'
        '401':
          description: Authentication required
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/UnauthorizedError'
        '422':
          description: Processing failed
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Error'
  /api/v1/tools/image/eps-to-jpg:
    post:
      operationId: epsToJpgTool
      tags:
        - Tools
      summary: EPS в JPG
      description: |
        Конвертация EPS в JPG
        Это выделенный эндпоинт-пресет конвертации на базе `convert`.
      security:
        - bearerAuth: []
      requestBody:
        required: true
        content:
          multipart/form-data:
            schema:
              type: object
              required:
                - file
              properties:
                file:
                  type: string
                  format: binary
                  description: EPS to JPG input file (.eps)
                settings:
                  type: string
                  description: |
                    Dedicated EPS to JPG converter using the convert pipeline.
                    Optional settings: quality (number 1-100). The output format is locked by the endpoint.
                clientJobId:
                  type: string
                  description: Client-provided job ID for SSE progress tracking
      responses:
        '200':
          description: Processed file
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ToolResponse'
        '400':
          description: Invalid input or settings
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Error'
        '401':
          description: Authentication required
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/UnauthorizedError'
        '422':
          description: Processing failed
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Error'
  /api/v1/tools/image/png-to-svg:
    post:
      operationId: pngToSvgTool
      tags:
        - Tools
      summary: PNG в SVG
      description: |
        Конвертация PNG в SVG
        Это выделенный эндпоинт-пресет конвертации на базе `vectorize`.
      security:
        - bearerAuth: []
      requestBody:
        required: true
        content:
          multipart/form-data:
            schema:
              type: object
              required:
                - file
              properties:
                file:
                  type: string
                  format: binary
                  description: PNG to SVG input file (.png)
                settings:
                  type: string
                  description: |
                    Dedicated PNG to SVG converter using the vectorize pipeline.
                    The output format is locked by the endpoint.
                clientJobId:
                  type: string
                  description: Client-provided job ID for SSE progress tracking
      responses:
        '200':
          description: Processed file
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ToolResponse'
        '400':
          description: Invalid input or settings
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Error'
        '401':
          description: Authentication required
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/UnauthorizedError'
        '422':
          description: Processing failed
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Error'
  /api/v1/tools/image/jpg-to-svg:
    post:
      operationId: jpgToSvgTool
      tags:
        - Tools
      summary: JPG в SVG
      description: |
        Конвертация JPG в SVG
        Это выделенный эндпоинт-пресет конвертации на базе `vectorize`.
      security:
        - bearerAuth: []
      requestBody:
        required: true
        content:
          multipart/form-data:
            schema:
              type: object
              required:
                - file
              properties:
                file:
                  type: string
                  format: binary
                  description: JPG to SVG input file (.jpg, .jpeg)
                settings:
                  type: string
                  description: |
                    Dedicated JPG to SVG converter using the vectorize pipeline.
                    The output format is locked by the endpoint.
                clientJobId:
                  type: string
                  description: Client-provided job ID for SSE progress tracking
      responses:
        '200':
          description: Processed file
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ToolResponse'
        '400':
          description: Invalid input or settings
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Error'
        '401':
          description: Authentication required
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/UnauthorizedError'
        '422':
          description: Processing failed
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Error'
  /api/v1/tools/image/tiff-to-svg:
    post:
      operationId: tiffToSvgTool
      tags:
        - Tools
      summary: TIFF в SVG
      description: |
        Конвертация TIFF в SVG
        Это выделенный эндпоинт-пресет конвертации на базе `vectorize`.
      security:
        - bearerAuth: []
      requestBody:
        required: true
        content:
          multipart/form-data:
            schema:
              type: object
              required:
                - file
              properties:
                file:
                  type: string
                  format: binary
                  description: TIFF to SVG input file (.tiff, .tif)
                settings:
                  type: string
                  description: |
                    Dedicated TIFF to SVG converter using the vectorize pipeline.
                    The output format is locked by the endpoint.
                clientJobId:
                  type: string
                  description: Client-provided job ID for SSE progress tracking
      responses:
        '200':
          description: Processed file
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ToolResponse'
        '400':
          description: Invalid input or settings
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Error'
        '401':
          description: Authentication required
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/UnauthorizedError'
        '422':
          description: Processing failed
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Error'
  /api/v1/tools/image/psd-to-svg:
    post:
      operationId: psdToSvgTool
      tags:
        - Tools
      summary: PSD в SVG
      description: |
        Конвертация PSD в SVG
        Это выделенный эндпоинт-пресет конвертации на базе `vectorize`.
      security:
        - bearerAuth: []
      requestBody:
        required: true
        content:
          multipart/form-data:
            schema:
              type: object
              required:
                - file
              properties:
                file:
                  type: string
                  format: binary
                  description: PSD to SVG input file (.psd)
                settings:
                  type: string
                  description: |
                    Dedicated PSD to SVG converter using the vectorize pipeline.
                    The output format is locked by the endpoint.
                clientJobId:
                  type: string
                  description: Client-provided job ID for SSE progress tracking
      responses:
        '200':
          description: Processed file
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ToolResponse'
        '400':
          description: Invalid input or settings
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Error'
        '401':
          description: Authentication required
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/UnauthorizedError'
        '422':
          description: Processing failed
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Error'
  /api/v1/tools/image/eps-to-svg:
    post:
      operationId: epsToSvgTool
      tags:
        - Tools
      summary: EPS в SVG
      description: |
        Конвертация EPS в SVG
        Это выделенный эндпоинт-пресет конвертации на базе `vectorize`.
      security:
        - bearerAuth: []
      requestBody:
        required: true
        content:
          multipart/form-data:
            schema:
              type: object
              required:
                - file
              properties:
                file:
                  type: string
                  format: binary
                  description: EPS to SVG input file (.eps)
                settings:
                  type: string
                  description: |
                    Dedicated EPS to SVG converter using the vectorize pipeline.
                    The output format is locked by the endpoint.
                clientJobId:
                  type: string
                  description: Client-provided job ID for SSE progress tracking
      responses:
        '200':
          description: Processed file
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ToolResponse'
        '400':
          description: Invalid input or settings
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Error'
        '401':
          description: Authentication required
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/UnauthorizedError'
        '422':
          description: Processing failed
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Error'
  /api/v1/tools/image/svg-to-png:
    post:
      operationId: svgToPngTool
      tags:
        - Tools
      summary: SVG в PNG
      description: |
        Конвертация SVG в PNG
        Это выделенный эндпоинт-пресет конвертации на базе `svg-to-raster`.
      security:
        - bearerAuth: []
      requestBody:
        required: true
        content:
          multipart/form-data:
            schema:
              type: object
              required:
                - file
              properties:
                file:
                  type: string
                  format: binary
                  description: SVG to PNG input file (.svg, .svgz)
                settings:
                  type: string
                  description: |
                    Dedicated SVG to PNG converter using the svg-to-raster pipeline.
                    Optional settings: quality (number 1-100). The output format is locked by the endpoint.
                clientJobId:
                  type: string
                  description: Client-provided job ID for SSE progress tracking
      responses:
        '200':
          description: Processed file
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ToolResponse'
        '400':
          description: Invalid input or settings
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Error'
        '401':
          description: Authentication required
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/UnauthorizedError'
        '422':
          description: Processing failed
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Error'
  /api/v1/tools/image/svg-to-jpg:
    post:
      operationId: svgToJpgTool
      tags:
        - Tools
      summary: SVG в JPG
      description: |
        Конвертация SVG в JPG
        Это выделенный эндпоинт-пресет конвертации на базе `svg-to-raster`.
      security:
        - bearerAuth: []
      requestBody:
        required: true
        content:
          multipart/form-data:
            schema:
              type: object
              required:
                - file
              properties:
                file:
                  type: string
                  format: binary
                  description: SVG to JPG input file (.svg, .svgz)
                settings:
                  type: string
                  description: |
                    Dedicated SVG to JPG converter using the svg-to-raster pipeline.
                    Optional settings: quality (number 1-100). The output format is locked by the endpoint.
                clientJobId:
                  type: string
                  description: Client-provided job ID for SSE progress tracking
      responses:
        '200':
          description: Processed file
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ToolResponse'
        '400':
          description: Invalid input or settings
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Error'
        '401':
          description: Authentication required
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/UnauthorizedError'
        '422':
          description: Processing failed
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Error'
  /api/v1/tools/image/jpg-to-pdf:
    post:
      operationId: jpgToPdfTool
      tags:
        - Tools
      summary: JPG в PDF
      description: |
        Конвертация JPG в PDF
        Это выделенный эндпоинт-пресет конвертации на базе `image-to-pdf`.
      security:
        - bearerAuth: []
      requestBody:
        required: true
        content:
          multipart/form-data:
            schema:
              type: object
              required:
                - file
              properties:
                file:
                  type: array
                  items:
                    type: string
                    format: binary
                  description: JPG to PDF input files (.jpg, .jpeg)
                settings:
                  type: string
                  description: |
                    Dedicated JPG to PDF converter using the image-to-pdf pipeline.
                    Optional settings: pageSize (A4, Letter, A3, A5), orientation (portrait or landscape), margin, targetSize, and collate.
                clientJobId:
                  type: string
                  description: Client-provided job ID for SSE progress tracking
      responses:
        '200':
          description: Processed file
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ToolResponse'
        '400':
          description: Invalid input or settings
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Error'
        '401':
          description: Authentication required
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/UnauthorizedError'
        '422':
          description: Processing failed
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Error'
  /api/v1/tools/image/png-to-pdf:
    post:
      operationId: pngToPdfTool
      tags:
        - Tools
      summary: PNG в PDF
      description: |
        Конвертация PNG в PDF
        Это выделенный эндпоинт-пресет конвертации на базе `image-to-pdf`.
      security:
        - bearerAuth: []
      requestBody:
        required: true
        content:
          multipart/form-data:
            schema:
              type: object
              required:
                - file
              properties:
                file:
                  type: array
                  items:
                    type: string
                    format: binary
                  description: PNG to PDF input files (.png)
                settings:
                  type: string
                  description: |
                    Dedicated PNG to PDF converter using the image-to-pdf pipeline.
                    Optional settings: pageSize (A4, Letter, A3, A5), orientation (portrait or landscape), margin, targetSize, and collate.
                clientJobId:
                  type: string
                  description: Client-provided job ID for SSE progress tracking
      responses:
        '200':
          description: Processed file
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ToolResponse'
        '400':
          description: Invalid input or settings
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Error'
        '401':
          description: Authentication required
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/UnauthorizedError'
        '422':
          description: Processing failed
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Error'
  /api/v1/tools/image/heic-to-pdf:
    post:
      operationId: heicToPdfTool
      tags:
        - Tools
      summary: HEIC в PDF
      description: |
        Конвертация HEIC в PDF
        Это выделенный эндпоинт-пресет конвертации на базе `image-to-pdf`.
      security:
        - bearerAuth: []
      requestBody:
        required: true
        content:
          multipart/form-data:
            schema:
              type: object
              required:
                - file
              properties:
                file:
                  type: array
                  items:
                    type: string
                    format: binary
                  description: HEIC to PDF input files (.heic, .heif)
                settings:
                  type: string
                  description: |
                    Dedicated HEIC to PDF converter using the image-to-pdf pipeline.
                    Optional settings: pageSize (A4, Letter, A3, A5), orientation (portrait or landscape), margin, targetSize, and collate.
                clientJobId:
                  type: string
                  description: Client-provided job ID for SSE progress tracking
      responses:
        '200':
          description: Processed file
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ToolResponse'
        '400':
          description: Invalid input or settings
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Error'
        '401':
          description: Authentication required
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/UnauthorizedError'
        '422':
          description: Processing failed
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Error'
  /api/v1/tools/image/tiff-to-pdf:
    post:
      operationId: tiffToPdfTool
      tags:
        - Tools
      summary: TIFF в PDF
      description: |
        Конвертация TIFF в PDF
        Это выделенный эндпоинт-пресет конвертации на базе `image-to-pdf`.
      security:
        - bearerAuth: []
      requestBody:
        required: true
        content:
          multipart/form-data:
            schema:
              type: object
              required:
                - file
              properties:
                file:
                  type: array
                  items:
                    type: string
                    format: binary
                  description: TIFF to PDF input files (.tiff, .tif)
                settings:
                  type: string
                  description: |
                    Dedicated TIFF to PDF converter using the image-to-pdf pipeline.
                    Optional settings: pageSize (A4, Letter, A3, A5), orientation (portrait or landscape), margin, targetSize, and collate.
                clientJobId:
                  type: string
                  description: Client-provided job ID for SSE progress tracking
      responses:
        '200':
          description: Processed file
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ToolResponse'
        '400':
          description: Invalid input or settings
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Error'
        '401':
          description: Authentication required
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/UnauthorizedError'
        '422':
          description: Processing failed
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Error'
  /api/v1/tools/image/webp-to-pdf:
    post:
      operationId: webpToPdfTool
      tags:
        - Tools
      summary: WebP в PDF
      description: |
        Конвертация WebP в PDF
        Это выделенный эндпоинт-пресет конвертации на базе `image-to-pdf`.
      security:
        - bearerAuth: []
      requestBody:
        required: true
        content:
          multipart/form-data:
            schema:
              type: object
              required:
                - file
              properties:
                file:
                  type: array
                  items:
                    type: string
                    format: binary
                  description: WebP to PDF input files (.webp)
                settings:
                  type: string
                  description: |
                    Dedicated WebP to PDF converter using the image-to-pdf pipeline.
                    Optional settings: pageSize (A4, Letter, A3, A5), orientation (portrait or landscape), margin, targetSize, and collate.
                clientJobId:
                  type: string
                  description: Client-provided job ID for SSE progress tracking
      responses:
        '200':
          description: Processed file
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ToolResponse'
        '400':
          description: Invalid input or settings
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Error'
        '401':
          description: Authentication required
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/UnauthorizedError'
        '422':
          description: Processing failed
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Error'
  /api/v1/tools/image/gif-to-pdf:
    post:
      operationId: gifToPdfTool
      tags:
        - Tools
      summary: GIF в PDF
      description: |
        Конвертация GIF в PDF
        Это выделенный эндпоинт-пресет конвертации на базе `image-to-pdf`.
      security:
        - bearerAuth: []
      requestBody:
        required: true
        content:
          multipart/form-data:
            schema:
              type: object
              required:
                - file
              properties:
                file:
                  type: array
                  items:
                    type: string
                    format: binary
                  description: GIF to PDF input files (.gif)
                settings:
                  type: string
                  description: |
                    Dedicated GIF to PDF converter using the image-to-pdf pipeline.
                    Optional settings: pageSize (A4, Letter, A3, A5), orientation (portrait or landscape), margin, targetSize, and collate.
                clientJobId:
                  type: string
                  description: Client-provided job ID for SSE progress tracking
      responses:
        '200':
          description: Processed file
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ToolResponse'
        '400':
          description: Invalid input or settings
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Error'
        '401':
          description: Authentication required
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/UnauthorizedError'
        '422':
          description: Processing failed
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Error'
  /api/v1/tools/image/eps-to-pdf:
    post:
      operationId: epsToPdfTool
      tags:
        - Tools
      summary: EPS в PDF
      description: |
        Конвертация EPS в PDF
        Это выделенный эндпоинт-пресет конвертации на базе `image-to-pdf`.
      security:
        - bearerAuth: []
      requestBody:
        required: true
        content:
          multipart/form-data:
            schema:
              type: object
              required:
                - file
              properties:
                file:
                  type: array
                  items:
                    type: string
                    format: binary
                  description: EPS to PDF input files (.eps)
                settings:
                  type: string
                  description: |
                    Dedicated EPS to PDF converter using the image-to-pdf pipeline.
                    Optional settings: pageSize (A4, Letter, A3, A5), orientation (portrait or landscape), margin, targetSize, and collate.
                clientJobId:
                  type: string
                  description: Client-provided job ID for SSE progress tracking
      responses:
        '200':
          description: Processed file
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ToolResponse'
        '400':
          description: Invalid input or settings
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Error'
        '401':
          description: Authentication required
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/UnauthorizedError'
        '422':
          description: Processing failed
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Error'
  /api/v1/tools/pdf/pdf-to-jpg:
    post:
      operationId: pdfToJpgTool
      tags:
        - Tools
      summary: PDF в JPG
      description: |
        Конвертация PDF в JPG
        Это выделенный эндпоинт-пресет конвертации на базе `pdf-to-image`.
      security:
        - bearerAuth: []
      requestBody:
        required: true
        content:
          multipart/form-data:
            schema:
              type: object
              required:
                - file
              properties:
                file:
                  type: string
                  format: binary
                  description: PDF to JPG input file (.pdf)
                settings:
                  type: string
                  description: |
                    Dedicated PDF to JPG converter using the pdf-to-image pipeline.
                    The output format is locked by the endpoint.
                clientJobId:
                  type: string
                  description: Client-provided job ID for SSE progress tracking
      responses:
        '200':
          description: Processed file
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ToolResponse'
        '400':
          description: Invalid input or settings
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Error'
        '401':
          description: Authentication required
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/UnauthorizedError'
        '422':
          description: Processing failed
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Error'
  /api/v1/tools/pdf/pdf-to-png:
    post:
      operationId: pdfToPngTool
      tags:
        - Tools
      summary: PDF в PNG
      description: |
        Конвертация PDF в PNG
        Это выделенный эндпоинт-пресет конвертации на базе `pdf-to-image`.
      security:
        - bearerAuth: []
      requestBody:
        required: true
        content:
          multipart/form-data:
            schema:
              type: object
              required:
                - file
              properties:
                file:
                  type: string
                  format: binary
                  description: PDF to PNG input file (.pdf)
                settings:
                  type: string
                  description: |
                    Dedicated PDF to PNG converter using the pdf-to-image pipeline.
                    The output format is locked by the endpoint.
                clientJobId:
                  type: string
                  description: Client-provided job ID for SSE progress tracking
      responses:
        '200':
          description: Processed file
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ToolResponse'
        '400':
          description: Invalid input or settings
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Error'
        '401':
          description: Authentication required
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/UnauthorizedError'
        '422':
          description: Processing failed
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Error'
  /api/v1/tools/pdf/pdf-to-tiff:
    post:
      operationId: pdfToTiffTool
      tags:
        - Tools
      summary: PDF в TIFF
      description: |
        Конвертация PDF в TIFF
        Это выделенный эндпоинт-пресет конвертации на базе `pdf-to-image`.
      security:
        - bearerAuth: []
      requestBody:
        required: true
        content:
          multipart/form-data:
            schema:
              type: object
              required:
                - file
              properties:
                file:
                  type: string
                  format: binary
                  description: PDF to TIFF input file (.pdf)
                settings:
                  type: string
                  description: |
                    Dedicated PDF to TIFF converter using the pdf-to-image pipeline.
                    The output format is locked by the endpoint.
                clientJobId:
                  type: string
                  description: Client-provided job ID for SSE progress tracking
      responses:
        '200':
          description: Processed file
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ToolResponse'
        '400':
          description: Invalid input or settings
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Error'
        '401':
          description: Authentication required
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/UnauthorizedError'
        '422':
          description: Processing failed
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Error'
  /api/v1/tools/video/mov-to-mp4:
    post:
      operationId: movToMp4Tool
      tags:
        - Tools
      summary: MOV в MP4
      description: |
        Конвертация MOV в MP4
        Это выделенный эндпоинт-пресет конвертации на базе `convert-video`.
      security:
        - bearerAuth: []
      requestBody:
        required: true
        content:
          multipart/form-data:
            schema:
              type: object
              required:
                - file
              properties:
                file:
                  type: string
                  format: binary
                  description: MOV to MP4 input file (.mov)
                settings:
                  type: string
                  description: |
                    Dedicated MOV to MP4 converter using the convert-video pipeline.
                    Optional settings: quality (high, balanced, small). The output format is locked by the endpoint.
                clientJobId:
                  type: string
                  description: Client-provided job ID for SSE progress tracking
      responses:
        '202':
          description: Accepted for async processing. Track progress via SSE at /api/v1/jobs/{jobId}/progress.
          content:
            application/json:
              schema:
                type: object
                properties:
                  jobId:
                    type: string
                  async:
                    type: boolean
                    example: true
        '400':
          description: Invalid input or settings
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Error'
        '401':
          description: Authentication required
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/UnauthorizedError'
        '422':
          description: Processing failed
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Error'
  /api/v1/tools/video/webm-to-mp4:
    post:
      operationId: webmToMp4Tool
      tags:
        - Tools
      summary: WEBM в MP4
      description: |
        Конвертация WEBM в MP4
        Это выделенный эндпоинт-пресет конвертации на базе `convert-video`.
      security:
        - bearerAuth: []
      requestBody:
        required: true
        content:
          multipart/form-data:
            schema:
              type: object
              required:
                - file
              properties:
                file:
                  type: string
                  format: binary
                  description: WEBM to MP4 input file (.webm)
                settings:
                  type: string
                  description: |
                    Dedicated WEBM to MP4 converter using the convert-video pipeline.
                    Optional settings: quality (high, balanced, small). The output format is locked by the endpoint.
                clientJobId:
                  type: string
                  description: Client-provided job ID for SSE progress tracking
      responses:
        '202':
          description: Accepted for async processing. Track progress via SSE at /api/v1/jobs/{jobId}/progress.
          content:
            application/json:
              schema:
                type: object
                properties:
                  jobId:
                    type: string
                  async:
                    type: boolean
                    example: true
        '400':
          description: Invalid input or settings
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Error'
        '401':
          description: Authentication required
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/UnauthorizedError'
        '422':
          description: Processing failed
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Error'
  /api/v1/tools/video/mkv-to-mp4:
    post:
      operationId: mkvToMp4Tool
      tags:
        - Tools
      summary: MKV в MP4
      description: |
        Конвертация MKV в MP4
        Это выделенный эндпоинт-пресет конвертации на базе `convert-video`.
      security:
        - bearerAuth: []
      requestBody:
        required: true
        content:
          multipart/form-data:
            schema:
              type: object
              required:
                - file
              properties:
                file:
                  type: string
                  format: binary
                  description: MKV to MP4 input file (.mkv)
                settings:
                  type: string
                  description: |
                    Dedicated MKV to MP4 converter using the convert-video pipeline.
                    Optional settings: quality (high, balanced, small). The output format is locked by the endpoint.
                clientJobId:
                  type: string
                  description: Client-provided job ID for SSE progress tracking
      responses:
        '202':
          description: Accepted for async processing. Track progress via SSE at /api/v1/jobs/{jobId}/progress.
          content:
            application/json:
              schema:
                type: object
                properties:
                  jobId:
                    type: string
                  async:
                    type: boolean
                    example: true
        '400':
          description: Invalid input or settings
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Error'
        '401':
          description: Authentication required
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/UnauthorizedError'
        '422':
          description: Processing failed
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Error'
  /api/v1/tools/video/avi-to-mp4:
    post:
      operationId: aviToMp4Tool
      tags:
        - Tools
      summary: AVI в MP4
      description: |
        Конвертация AVI в MP4
        Это выделенный эндпоинт-пресет конвертации на базе `convert-video`.
      security:
        - bearerAuth: []
      requestBody:
        required: true
        content:
          multipart/form-data:
            schema:
              type: object
              required:
                - file
              properties:
                file:
                  type: string
                  format: binary
                  description: AVI to MP4 input file (.avi)
                settings:
                  type: string
                  description: |
                    Dedicated AVI to MP4 converter using the convert-video pipeline.
                    Optional settings: quality (high, balanced, small). The output format is locked by the endpoint.
                clientJobId:
                  type: string
                  description: Client-provided job ID for SSE progress tracking
      responses:
        '202':
          description: Accepted for async processing. Track progress via SSE at /api/v1/jobs/{jobId}/progress.
          content:
            application/json:
              schema:
                type: object
                properties:
                  jobId:
                    type: string
                  async:
                    type: boolean
                    example: true
        '400':
          description: Invalid input or settings
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Error'
        '401':
          description: Authentication required
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/UnauthorizedError'
        '422':
          description: Processing failed
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Error'
  /api/v1/tools/video/mp4-to-mov:
    post:
      operationId: mp4ToMovTool
      tags:
        - Tools
      summary: MP4 в MOV
      description: |
        Конвертация MP4 в MOV
        Это выделенный эндпоинт-пресет конвертации на базе `convert-video`.
      security:
        - bearerAuth: []
      requestBody:
        required: true
        content:
          multipart/form-data:
            schema:
              type: object
              required:
                - file
              properties:
                file:
                  type: string
                  format: binary
                  description: MP4 to MOV input file (.mp4)
                settings:
                  type: string
                  description: |
                    Dedicated MP4 to MOV converter using the convert-video pipeline.
                    Optional settings: quality (high, balanced, small). The output format is locked by the endpoint.
                clientJobId:
                  type: string
                  description: Client-provided job ID for SSE progress tracking
      responses:
        '202':
          description: Accepted for async processing. Track progress via SSE at /api/v1/jobs/{jobId}/progress.
          content:
            application/json:
              schema:
                type: object
                properties:
                  jobId:
                    type: string
                  async:
                    type: boolean
                    example: true
        '400':
          description: Invalid input or settings
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Error'
        '401':
          description: Authentication required
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/UnauthorizedError'
        '422':
          description: Processing failed
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Error'
  /api/v1/tools/video/mp4-to-webm:
    post:
      operationId: mp4ToWebmTool
      tags:
        - Tools
      summary: MP4 в WEBM
      description: |
        Конвертация MP4 в WEBM
        Это выделенный эндпоинт-пресет конвертации на базе `convert-video`.
      security:
        - bearerAuth: []
      requestBody:
        required: true
        content:
          multipart/form-data:
            schema:
              type: object
              required:
                - file
              properties:
                file:
                  type: string
                  format: binary
                  description: MP4 to WEBM input file (.mp4)
                settings:
                  type: string
                  description: |
                    Dedicated MP4 to WEBM converter using the convert-video pipeline.
                    Optional settings: quality (high, balanced, small). The output format is locked by the endpoint.
                clientJobId:
                  type: string
                  description: Client-provided job ID for SSE progress tracking
      responses:
        '202':
          description: Accepted for async processing. Track progress via SSE at /api/v1/jobs/{jobId}/progress.
          content:
            application/json:
              schema:
                type: object
                properties:
                  jobId:
                    type: string
                  async:
                    type: boolean
                    example: true
        '400':
          description: Invalid input or settings
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Error'
        '401':
          description: Authentication required
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/UnauthorizedError'
        '422':
          description: Processing failed
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Error'
  /api/v1/tools/video/webm-to-mov:
    post:
      operationId: webmToMovTool
      tags:
        - Tools
      summary: WEBM в MOV
      description: |
        Конвертация WEBM в MOV
        Это выделенный эндпоинт-пресет конвертации на базе `convert-video`.
      security:
        - bearerAuth: []
      requestBody:
        required: true
        content:
          multipart/form-data:
            schema:
              type: object
              required:
                - file
              properties:
                file:
                  type: string
                  format: binary
                  description: WEBM to MOV input file (.webm)
                settings:
                  type: string
                  description: |
                    Dedicated WEBM to MOV converter using the convert-video pipeline.
                    Optional settings: quality (high, balanced, small). The output format is locked by the endpoint.
                clientJobId:
                  type: string
                  description: Client-provided job ID for SSE progress tracking
      responses:
        '202':
          description: Accepted for async processing. Track progress via SSE at /api/v1/jobs/{jobId}/progress.
          content:
            application/json:
              schema:
                type: object
                properties:
                  jobId:
                    type: string
                  async:
                    type: boolean
                    example: true
        '400':
          description: Invalid input or settings
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Error'
        '401':
          description: Authentication required
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/UnauthorizedError'
        '422':
          description: Processing failed
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Error'
  /api/v1/tools/video/mkv-to-mov:
    post:
      operationId: mkvToMovTool
      tags:
        - Tools
      summary: MKV в MOV
      description: |
        Конвертация MKV в MOV
        Это выделенный эндпоинт-пресет конвертации на базе `convert-video`.
      security:
        - bearerAuth: []
      requestBody:
        required: true
        content:
          multipart/form-data:
            schema:
              type: object
              required:
                - file
              properties:
                file:
                  type: string
                  format: binary
                  description: MKV to MOV input file (.mkv)
                settings:
                  type: string
                  description: |
                    Dedicated MKV to MOV converter using the convert-video pipeline.
                    Optional settings: quality (high, balanced, small). The output format is locked by the endpoint.
                clientJobId:
                  type: string
                  description: Client-provided job ID for SSE progress tracking
      responses:
        '202':
          description: Accepted for async processing. Track progress via SSE at /api/v1/jobs/{jobId}/progress.
          content:
            application/json:
              schema:
                type: object
                properties:
                  jobId:
                    type: string
                  async:
                    type: boolean
                    example: true
        '400':
          description: Invalid input or settings
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Error'
        '401':
          description: Authentication required
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/UnauthorizedError'
        '422':
          description: Processing failed
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Error'
  /api/v1/tools/video/avi-to-mov:
    post:
      operationId: aviToMovTool
      tags:
        - Tools
      summary: AVI в MOV
      description: |
        Конвертация AVI в MOV
        Это выделенный эндпоинт-пресет конвертации на базе `convert-video`.
      security:
        - bearerAuth: []
      requestBody:
        required: true
        content:
          multipart/form-data:
            schema:
              type: object
              required:
                - file
              properties:
                file:
                  type: string
                  format: binary
                  description: AVI to MOV input file (.avi)
                settings:
                  type: string
                  description: |
                    Dedicated AVI to MOV converter using the convert-video pipeline.
                    Optional settings: quality (high, balanced, small). The output format is locked by the endpoint.
                clientJobId:
                  type: string
                  description: Client-provided job ID for SSE progress tracking
      responses:
        '202':
          description: Accepted for async processing. Track progress via SSE at /api/v1/jobs/{jobId}/progress.
          content:
            application/json:
              schema:
                type: object
                properties:
                  jobId:
                    type: string
                  async:
                    type: boolean
                    example: true
        '400':
          description: Invalid input or settings
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Error'
        '401':
          description: Authentication required
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/UnauthorizedError'
        '422':
          description: Processing failed
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Error'
  /api/v1/tools/video/mp4-to-avi:
    post:
      operationId: mp4ToAviTool
      tags:
        - Tools
      summary: MP4 в AVI
      description: |
        Конвертация MP4 в AVI
        Это выделенный эндпоинт-пресет конвертации на базе `convert-video`.
      security:
        - bearerAuth: []
      requestBody:
        required: true
        content:
          multipart/form-data:
            schema:
              type: object
              required:
                - file
              properties:
                file:
                  type: string
                  format: binary
                  description: MP4 to AVI input file (.mp4)
                settings:
                  type: string
                  description: |
                    Dedicated MP4 to AVI converter using the convert-video pipeline.
                    Optional settings: quality (high, balanced, small). The output format is locked by the endpoint.
                clientJobId:
                  type: string
                  description: Client-provided job ID for SSE progress tracking
      responses:
        '202':
          description: Accepted for async processing. Track progress via SSE at /api/v1/jobs/{jobId}/progress.
          content:
            application/json:
              schema:
                type: object
                properties:
                  jobId:
                    type: string
                  async:
                    type: boolean
                    example: true
        '400':
          description: Invalid input or settings
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Error'
        '401':
          description: Authentication required
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/UnauthorizedError'
        '422':
          description: Processing failed
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Error'
  /api/v1/tools/video/mov-to-avi:
    post:
      operationId: movToAviTool
      tags:
        - Tools
      summary: MOV в AVI
      description: |
        Конвертация MOV в AVI
        Это выделенный эндпоинт-пресет конвертации на базе `convert-video`.
      security:
        - bearerAuth: []
      requestBody:
        required: true
        content:
          multipart/form-data:
            schema:
              type: object
              required:
                - file
              properties:
                file:
                  type: string
                  format: binary
                  description: MOV to AVI input file (.mov)
                settings:
                  type: string
                  description: |
                    Dedicated MOV to AVI converter using the convert-video pipeline.
                    Optional settings: quality (high, balanced, small). The output format is locked by the endpoint.
                clientJobId:
                  type: string
                  description: Client-provided job ID for SSE progress tracking
      responses:
        '202':
          description: Accepted for async processing. Track progress via SSE at /api/v1/jobs/{jobId}/progress.
          content:
            application/json:
              schema:
                type: object
                properties:
                  jobId:
                    type: string
                  async:
                    type: boolean
                    example: true
        '400':
          description: Invalid input or settings
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Error'
        '401':
          description: Authentication required
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/UnauthorizedError'
        '422':
          description: Processing failed
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Error'
  /api/v1/tools/video/mkv-to-avi:
    post:
      operationId: mkvToAviTool
      tags:
        - Tools
      summary: MKV в AVI
      description: |
        Конвертация MKV в AVI
        Это выделенный эндпоинт-пресет конвертации на базе `convert-video`.
      security:
        - bearerAuth: []
      requestBody:
        required: true
        content:
          multipart/form-data:
            schema:
              type: object
              required:
                - file
              properties:
                file:
                  type: string
                  format: binary
                  description: MKV to AVI input file (.mkv)
                settings:
                  type: string
                  description: |
                    Dedicated MKV to AVI converter using the convert-video pipeline.
                    Optional settings: quality (high, balanced, small). The output format is locked by the endpoint.
                clientJobId:
                  type: string
                  description: Client-provided job ID for SSE progress tracking
      responses:
        '202':
          description: Accepted for async processing. Track progress via SSE at /api/v1/jobs/{jobId}/progress.
          content:
            application/json:
              schema:
                type: object
                properties:
                  jobId:
                    type: string
                  async:
                    type: boolean
                    example: true
        '400':
          description: Invalid input or settings
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Error'
        '401':
          description: Authentication required
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/UnauthorizedError'
        '422':
          description: Processing failed
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Error'
  /api/v1/tools/video/avi-to-mkv:
    post:
      operationId: aviToMkvTool
      tags:
        - Tools
      summary: AVI в MKV
      description: |
        Конвертация AVI в MKV
        Это выделенный эндпоинт-пресет конвертации на базе `convert-video`.
      security:
        - bearerAuth: []
      requestBody:
        required: true
        content:
          multipart/form-data:
            schema:
              type: object
              required:
                - file
              properties:
                file:
                  type: string
                  format: binary
                  description: AVI to MKV input file (.avi)
                settings:
                  type: string
                  description: |
                    Dedicated AVI to MKV converter using the convert-video pipeline.
                    Optional settings: quality (high, balanced, small). The output format is locked by the endpoint.
                clientJobId:
                  type: string
                  description: Client-provided job ID for SSE progress tracking
      responses:
        '202':
          description: Accepted for async processing. Track progress via SSE at /api/v1/jobs/{jobId}/progress.
          content:
            application/json:
              schema:
                type: object
                properties:
                  jobId:
                    type: string
                  async:
                    type: boolean
                    example: true
        '400':
          description: Invalid input or settings
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Error'
        '401':
          description: Authentication required
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/UnauthorizedError'
        '422':
          description: Processing failed
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Error'
  /api/v1/tools/video/mp4-to-gif:
    post:
      operationId: mp4ToGifTool
      tags:
        - Tools
      summary: MP4 в GIF
      description: |
        Конвертация MP4 в GIF
        Это выделенный эндпоинт-пресет конвертации на базе `video-to-gif`.
      security:
        - bearerAuth: []
      requestBody:
        required: true
        content:
          multipart/form-data:
            schema:
              type: object
              required:
                - file
              properties:
                file:
                  type: string
                  format: binary
                  description: MP4 to GIF input file (.mp4)
                settings:
                  type: string
                  description: |
                    Dedicated MP4 to GIF converter using the video-to-gif pipeline.
                    The output format is locked by the endpoint.
                clientJobId:
                  type: string
                  description: Client-provided job ID for SSE progress tracking
      responses:
        '202':
          description: Accepted for async processing. Track progress via SSE at /api/v1/jobs/{jobId}/progress.
          content:
            application/json:
              schema:
                type: object
                properties:
                  jobId:
                    type: string
                  async:
                    type: boolean
                    example: true
        '400':
          description: Invalid input or settings
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Error'
        '401':
          description: Authentication required
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/UnauthorizedError'
        '422':
          description: Processing failed
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Error'
  /api/v1/tools/video/mov-to-gif:
    post:
      operationId: movToGifTool
      tags:
        - Tools
      summary: MOV в GIF
      description: |
        Конвертация MOV в GIF
        Это выделенный эндпоинт-пресет конвертации на базе `video-to-gif`.
      security:
        - bearerAuth: []
      requestBody:
        required: true
        content:
          multipart/form-data:
            schema:
              type: object
              required:
                - file
              properties:
                file:
                  type: string
                  format: binary
                  description: MOV to GIF input file (.mov)
                settings:
                  type: string
                  description: |
                    Dedicated MOV to GIF converter using the video-to-gif pipeline.
                    The output format is locked by the endpoint.
                clientJobId:
                  type: string
                  description: Client-provided job ID for SSE progress tracking
      responses:
        '202':
          description: Accepted for async processing. Track progress via SSE at /api/v1/jobs/{jobId}/progress.
          content:
            application/json:
              schema:
                type: object
                properties:
                  jobId:
                    type: string
                  async:
                    type: boolean
                    example: true
        '400':
          description: Invalid input or settings
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Error'
        '401':
          description: Authentication required
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/UnauthorizedError'
        '422':
          description: Processing failed
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Error'
  /api/v1/tools/video/mkv-to-gif:
    post:
      operationId: mkvToGifTool
      tags:
        - Tools
      summary: MKV в GIF
      description: |
        Конвертация MKV в GIF
        Это выделенный эндпоинт-пресет конвертации на базе `video-to-gif`.
      security:
        - bearerAuth: []
      requestBody:
        required: true
        content:
          multipart/form-data:
            schema:
              type: object
              required:
                - file
              properties:
                file:
                  type: string
                  format: binary
                  description: MKV to GIF input file (.mkv)
                settings:
                  type: string
                  description: |
                    Dedicated MKV to GIF converter using the video-to-gif pipeline.
                    The output format is locked by the endpoint.
                clientJobId:
                  type: string
                  description: Client-provided job ID for SSE progress tracking
      responses:
        '202':
          description: Accepted for async processing. Track progress via SSE at /api/v1/jobs/{jobId}/progress.
          content:
            application/json:
              schema:
                type: object
                properties:
                  jobId:
                    type: string
                  async:
                    type: boolean
                    example: true
        '400':
          description: Invalid input or settings
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Error'
        '401':
          description: Authentication required
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/UnauthorizedError'
        '422':
          description: Processing failed
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Error'
  /api/v1/tools/video/avi-to-gif:
    post:
      operationId: aviToGifTool
      tags:
        - Tools
      summary: AVI в GIF
      description: |
        Конвертация AVI в GIF
        Это выделенный эндпоинт-пресет конвертации на базе `video-to-gif`.
      security:
        - bearerAuth: []
      requestBody:
        required: true
        content:
          multipart/form-data:
            schema:
              type: object
              required:
                - file
              properties:
                file:
                  type: string
                  format: binary
                  description: AVI to GIF input file (.avi)
                settings:
                  type: string
                  description: |
                    Dedicated AVI to GIF converter using the video-to-gif pipeline.
                    The output format is locked by the endpoint.
                clientJobId:
                  type: string
                  description: Client-provided job ID for SSE progress tracking
      responses:
        '202':
          description: Accepted for async processing. Track progress via SSE at /api/v1/jobs/{jobId}/progress.
          content:
            application/json:
              schema:
                type: object
                properties:
                  jobId:
                    type: string
                  async:
                    type: boolean
                    example: true
        '400':
          description: Invalid input or settings
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Error'
        '401':
          description: Authentication required
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/UnauthorizedError'
        '422':
          description: Processing failed
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Error'
  /api/v1/tools/video/gif-to-mp4:
    post:
      operationId: gifToMp4Tool
      tags:
        - Tools
      summary: GIF в MP4
      description: |
        Конвертация GIF в MP4
        Это выделенный эндпоинт-пресет конвертации на базе `gif-to-video`.
      security:
        - bearerAuth: []
      requestBody:
        required: true
        content:
          multipart/form-data:
            schema:
              type: object
              required:
                - file
              properties:
                file:
                  type: string
                  format: binary
                  description: GIF to MP4 input file (.gif)
                settings:
                  type: string
                  description: |
                    Dedicated GIF to MP4 converter using the gif-to-video pipeline.
                    The output format is locked by the endpoint.
                clientJobId:
                  type: string
                  description: Client-provided job ID for SSE progress tracking
      responses:
        '200':
          description: Processed file
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ToolResponse'
        '400':
          description: Invalid input or settings
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Error'
        '401':
          description: Authentication required
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/UnauthorizedError'
        '422':
          description: Processing failed
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Error'
  /api/v1/tools/video/gif-to-webm:
    post:
      operationId: gifToWebmTool
      tags:
        - Tools
      summary: GIF в WEBM
      description: |
        Конвертация GIF в WEBM
        Это выделенный эндпоинт-пресет конвертации на базе `gif-to-video`.
      security:
        - bearerAuth: []
      requestBody:
        required: true
        content:
          multipart/form-data:
            schema:
              type: object
              required:
                - file
              properties:
                file:
                  type: string
                  format: binary
                  description: GIF to WEBM input file (.gif)
                settings:
                  type: string
                  description: |
                    Dedicated GIF to WEBM converter using the gif-to-video pipeline.
                    The output format is locked by the endpoint.
                clientJobId:
                  type: string
                  description: Client-provided job ID for SSE progress tracking
      responses:
        '200':
          description: Processed file
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ToolResponse'
        '400':
          description: Invalid input or settings
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Error'
        '401':
          description: Authentication required
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/UnauthorizedError'
        '422':
          description: Processing failed
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Error'
  /api/v1/tools/video/gif-to-mov:
    post:
      operationId: gifToMovTool
      tags:
        - Tools
      summary: GIF в MOV
      description: |
        Конвертация GIF в MOV
        Это выделенный эндпоинт-пресет конвертации на базе `gif-to-video`.
      security:
        - bearerAuth: []
      requestBody:
        required: true
        content:
          multipart/form-data:
            schema:
              type: object
              required:
                - file
              properties:
                file:
                  type: string
                  format: binary
                  description: GIF to MOV input file (.gif)
                settings:
                  type: string
                  description: |
                    Dedicated GIF to MOV converter using the gif-to-video pipeline.
                    The output format is locked by the endpoint.
                clientJobId:
                  type: string
                  description: Client-provided job ID for SSE progress tracking
      responses:
        '200':
          description: Processed file
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ToolResponse'
        '400':
          description: Invalid input or settings
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Error'
        '401':
          description: Authentication required
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/UnauthorizedError'
        '422':
          description: Processing failed
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Error'
  /api/v1/tools/video/mp4-to-mp3:
    post:
      operationId: mp4ToMp3Tool
      tags:
        - Tools
      summary: MP4 в MP3
      description: |
        Конвертация MP4 в MP3
        Это выделенный эндпоинт-пресет конвертации на базе `extract-audio`.
      security:
        - bearerAuth: []
      requestBody:
        required: true
        content:
          multipart/form-data:
            schema:
              type: object
              required:
                - file
              properties:
                file:
                  type: string
                  format: binary
                  description: MP4 to MP3 input file (.mp4)
                settings:
                  type: string
                  description: |
                    Dedicated MP4 to MP3 converter using the extract-audio pipeline.
                    The output format is locked by the endpoint.
                clientJobId:
                  type: string
                  description: Client-provided job ID for SSE progress tracking
      responses:
        '200':
          description: Processed file
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ToolResponse'
        '400':
          description: Invalid input or settings
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Error'
        '401':
          description: Authentication required
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/UnauthorizedError'
        '422':
          description: Processing failed
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Error'
  /api/v1/tools/video/mov-to-mp3:
    post:
      operationId: movToMp3Tool
      tags:
        - Tools
      summary: MOV в MP3
      description: |
        Конвертация MOV в MP3
        Это выделенный эндпоинт-пресет конвертации на базе `extract-audio`.
      security:
        - bearerAuth: []
      requestBody:
        required: true
        content:
          multipart/form-data:
            schema:
              type: object
              required:
                - file
              properties:
                file:
                  type: string
                  format: binary
                  description: MOV to MP3 input file (.mov)
                settings:
                  type: string
                  description: |
                    Dedicated MOV to MP3 converter using the extract-audio pipeline.
                    The output format is locked by the endpoint.
                clientJobId:
                  type: string
                  description: Client-provided job ID for SSE progress tracking
      responses:
        '200':
          description: Processed file
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ToolResponse'
        '400':
          description: Invalid input or settings
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Error'
        '401':
          description: Authentication required
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/UnauthorizedError'
        '422':
          description: Processing failed
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Error'
  /api/v1/tools/video/mkv-to-mp3:
    post:
      operationId: mkvToMp3Tool
      tags:
        - Tools
      summary: MKV в MP3
      description: |
        Конвертация MKV в MP3
        Это выделенный эндпоинт-пресет конвертации на базе `extract-audio`.
      security:
        - bearerAuth: []
      requestBody:
        required: true
        content:
          multipart/form-data:
            schema:
              type: object
              required:
                - file
              properties:
                file:
                  type: string
                  format: binary
                  description: MKV to MP3 input file (.mkv)
                settings:
                  type: string
                  description: |
                    Dedicated MKV to MP3 converter using the extract-audio pipeline.
                    The output format is locked by the endpoint.
                clientJobId:
                  type: string
                  description: Client-provided job ID for SSE progress tracking
      responses:
        '200':
          description: Processed file
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ToolResponse'
        '400':
          description: Invalid input or settings
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Error'
        '401':
          description: Authentication required
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/UnauthorizedError'
        '422':
          description: Processing failed
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Error'
  /api/v1/tools/video/webm-to-mp3:
    post:
      operationId: webmToMp3Tool
      tags:
        - Tools
      summary: WEBM в MP3
      description: |
        Конвертация WEBM в MP3
        Это выделенный эндпоинт-пресет конвертации на базе `extract-audio`.
      security:
        - bearerAuth: []
      requestBody:
        required: true
        content:
          multipart/form-data:
            schema:
              type: object
              required:
                - file
              properties:
                file:
                  type: string
                  format: binary
                  description: WEBM to MP3 input file (.webm)
                settings:
                  type: string
                  description: |
                    Dedicated WEBM to MP3 converter using the extract-audio pipeline.
                    The output format is locked by the endpoint.
                clientJobId:
                  type: string
                  description: Client-provided job ID for SSE progress tracking
      responses:
        '200':
          description: Processed file
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ToolResponse'
        '400':
          description: Invalid input or settings
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Error'
        '401':
          description: Authentication required
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/UnauthorizedError'
        '422':
          description: Processing failed
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Error'
  /api/v1/tools/video/avi-to-mp3:
    post:
      operationId: aviToMp3Tool
      tags:
        - Tools
      summary: AVI в MP3
      description: |
        Конвертация AVI в MP3
        Это выделенный эндпоинт-пресет конвертации на базе `extract-audio`.
      security:
        - bearerAuth: []
      requestBody:
        required: true
        content:
          multipart/form-data:
            schema:
              type: object
              required:
                - file
              properties:
                file:
                  type: string
                  format: binary
                  description: AVI to MP3 input file (.avi)
                settings:
                  type: string
                  description: |
                    Dedicated AVI to MP3 converter using the extract-audio pipeline.
                    The output format is locked by the endpoint.
                clientJobId:
                  type: string
                  description: Client-provided job ID for SSE progress tracking
      responses:
        '200':
          description: Processed file
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ToolResponse'
        '400':
          description: Invalid input or settings
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Error'
        '401':
          description: Authentication required
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/UnauthorizedError'
        '422':
          description: Processing failed
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Error'
  /api/v1/tools/video/mp4-to-wav:
    post:
      operationId: mp4ToWavTool
      tags:
        - Tools
      summary: MP4 в WAV
      description: |
        Конвертация MP4 в WAV
        Это выделенный эндпоинт-пресет конвертации на базе `extract-audio`.
      security:
        - bearerAuth: []
      requestBody:
        required: true
        content:
          multipart/form-data:
            schema:
              type: object
              required:
                - file
              properties:
                file:
                  type: string
                  format: binary
                  description: MP4 to WAV input file (.mp4)
                settings:
                  type: string
                  description: |
                    Dedicated MP4 to WAV converter using the extract-audio pipeline.
                    The output format is locked by the endpoint.
                clientJobId:
                  type: string
                  description: Client-provided job ID for SSE progress tracking
      responses:
        '200':
          description: Processed file
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ToolResponse'
        '400':
          description: Invalid input or settings
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Error'
        '401':
          description: Authentication required
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/UnauthorizedError'
        '422':
          description: Processing failed
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Error'
  /api/v1/tools/video/mov-to-wav:
    post:
      operationId: movToWavTool
      tags:
        - Tools
      summary: MOV в WAV
      description: |
        Конвертация MOV в WAV
        Это выделенный эндпоинт-пресет конвертации на базе `extract-audio`.
      security:
        - bearerAuth: []
      requestBody:
        required: true
        content:
          multipart/form-data:
            schema:
              type: object
              required:
                - file
              properties:
                file:
                  type: string
                  format: binary
                  description: MOV to WAV input file (.mov)
                settings:
                  type: string
                  description: |
                    Dedicated MOV to WAV converter using the extract-audio pipeline.
                    The output format is locked by the endpoint.
                clientJobId:
                  type: string
                  description: Client-provided job ID for SSE progress tracking
      responses:
        '200':
          description: Processed file
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ToolResponse'
        '400':
          description: Invalid input or settings
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Error'
        '401':
          description: Authentication required
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/UnauthorizedError'
        '422':
          description: Processing failed
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Error'
  /api/v1/tools/video/mp4-to-ogg:
    post:
      operationId: mp4ToOggTool
      tags:
        - Tools
      summary: MP4 в OGG
      description: |
        Конвертация MP4 в OGG
        Это выделенный эндпоинт-пресет конвертации на базе `extract-audio`.
      security:
        - bearerAuth: []
      requestBody:
        required: true
        content:
          multipart/form-data:
            schema:
              type: object
              required:
                - file
              properties:
                file:
                  type: string
                  format: binary
                  description: MP4 to OGG input file (.mp4)
                settings:
                  type: string
                  description: |
                    Dedicated MP4 to OGG converter using the extract-audio pipeline.
                    The output format is locked by the endpoint.
                clientJobId:
                  type: string
                  description: Client-provided job ID for SSE progress tracking
      responses:
        '200':
          description: Processed file
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ToolResponse'
        '400':
          description: Invalid input or settings
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Error'
        '401':
          description: Authentication required
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/UnauthorizedError'
        '422':
          description: Processing failed
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Error'
  /api/v1/tools/audio/m4a-to-mp3:
    post:
      operationId: m4aToMp3Tool
      tags:
        - Tools
      summary: M4A в MP3
      description: |
        Конвертация M4A в MP3
        Это выделенный эндпоинт-пресет конвертации на базе `convert-audio`.
      security:
        - bearerAuth: []
      requestBody:
        required: true
        content:
          multipart/form-data:
            schema:
              type: object
              required:
                - file
              properties:
                file:
                  type: string
                  format: binary
                  description: M4A to MP3 input file (.m4a)
                settings:
                  type: string
                  description: |
                    Dedicated M4A to MP3 converter using the convert-audio pipeline.
                    The output format is locked by the endpoint.
                clientJobId:
                  type: string
                  description: Client-provided job ID for SSE progress tracking
      responses:
        '200':
          description: Processed file
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ToolResponse'
        '400':
          description: Invalid input or settings
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Error'
        '401':
          description: Authentication required
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/UnauthorizedError'
        '422':
          description: Processing failed
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Error'
  /api/v1/tools/audio/m4a-to-wav:
    post:
      operationId: m4aToWavTool
      tags:
        - Tools
      summary: M4A в WAV
      description: |
        Конвертация M4A в WAV
        Это выделенный эндпоинт-пресет конвертации на базе `convert-audio`.
      security:
        - bearerAuth: []
      requestBody:
        required: true
        content:
          multipart/form-data:
            schema:
              type: object
              required:
                - file
              properties:
                file:
                  type: string
                  format: binary
                  description: M4A to WAV input file (.m4a)
                settings:
                  type: string
                  description: |
                    Dedicated M4A to WAV converter using the convert-audio pipeline.
                    The output format is locked by the endpoint.
                clientJobId:
                  type: string
                  description: Client-provided job ID for SSE progress tracking
      responses:
        '200':
          description: Processed file
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ToolResponse'
        '400':
          description: Invalid input or settings
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Error'
        '401':
          description: Authentication required
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/UnauthorizedError'
        '422':
          description: Processing failed
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Error'
  /api/v1/tools/audio/aac-to-mp3:
    post:
      operationId: aacToMp3Tool
      tags:
        - Tools
      summary: AAC в MP3
      description: |
        Конвертация AAC в MP3
        Это выделенный эндпоинт-пресет конвертации на базе `convert-audio`.
      security:
        - bearerAuth: []
      requestBody:
        required: true
        content:
          multipart/form-data:
            schema:
              type: object
              required:
                - file
              properties:
                file:
                  type: string
                  format: binary
                  description: AAC to MP3 input file (.aac)
                settings:
                  type: string
                  description: |
                    Dedicated AAC to MP3 converter using the convert-audio pipeline.
                    The output format is locked by the endpoint.
                clientJobId:
                  type: string
                  description: Client-provided job ID for SSE progress tracking
      responses:
        '200':
          description: Processed file
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ToolResponse'
        '400':
          description: Invalid input or settings
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Error'
        '401':
          description: Authentication required
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/UnauthorizedError'
        '422':
          description: Processing failed
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Error'
  /api/v1/tools/audio/aac-to-wav:
    post:
      operationId: aacToWavTool
      tags:
        - Tools
      summary: AAC в WAV
      description: |
        Конвертация AAC в WAV
        Это выделенный эндпоинт-пресет конвертации на базе `convert-audio`.
      security:
        - bearerAuth: []
      requestBody:
        required: true
        content:
          multipart/form-data:
            schema:
              type: object
              required:
                - file
              properties:
                file:
                  type: string
                  format: binary
                  description: AAC to WAV input file (.aac)
                settings:
                  type: string
                  description: |
                    Dedicated AAC to WAV converter using the convert-audio pipeline.
                    The output format is locked by the endpoint.
                clientJobId:
                  type: string
                  description: Client-provided job ID for SSE progress tracking
      responses:
        '200':
          description: Processed file
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ToolResponse'
        '400':
          description: Invalid input or settings
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Error'
        '401':
          description: Authentication required
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/UnauthorizedError'
        '422':
          description: Processing failed
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Error'
  /api/v1/tools/audio/aac-to-flac:
    post:
      operationId: aacToFlacTool
      tags:
        - Tools
      summary: AAC в FLAC
      description: |
        Конвертация AAC в FLAC
        Это выделенный эндпоинт-пресет конвертации на базе `convert-audio`.
      security:
        - bearerAuth: []
      requestBody:
        required: true
        content:
          multipart/form-data:
            schema:
              type: object
              required:
                - file
              properties:
                file:
                  type: string
                  format: binary
                  description: AAC to FLAC input file (.aac)
                settings:
                  type: string
                  description: |
                    Dedicated AAC to FLAC converter using the convert-audio pipeline.
                    The output format is locked by the endpoint.
                clientJobId:
                  type: string
                  description: Client-provided job ID for SSE progress tracking
      responses:
        '200':
          description: Processed file
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ToolResponse'
        '400':
          description: Invalid input or settings
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Error'
        '401':
          description: Authentication required
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/UnauthorizedError'
        '422':
          description: Processing failed
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Error'
  /api/v1/tools/audio/ogg-to-mp3:
    post:
      operationId: oggToMp3Tool
      tags:
        - Tools
      summary: OGG в MP3
      description: |
        Конвертация OGG в MP3
        Это выделенный эндпоинт-пресет конвертации на базе `convert-audio`.
      security:
        - bearerAuth: []
      requestBody:
        required: true
        content:
          multipart/form-data:
            schema:
              type: object
              required:
                - file
              properties:
                file:
                  type: string
                  format: binary
                  description: OGG to MP3 input file (.ogg)
                settings:
                  type: string
                  description: |
                    Dedicated OGG to MP3 converter using the convert-audio pipeline.
                    The output format is locked by the endpoint.
                clientJobId:
                  type: string
                  description: Client-provided job ID for SSE progress tracking
      responses:
        '200':
          description: Processed file
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ToolResponse'
        '400':
          description: Invalid input or settings
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Error'
        '401':
          description: Authentication required
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/UnauthorizedError'
        '422':
          description: Processing failed
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Error'
  /api/v1/tools/audio/ogg-to-wav:
    post:
      operationId: oggToWavTool
      tags:
        - Tools
      summary: OGG в WAV
      description: |
        Конвертация OGG в WAV
        Это выделенный эндпоинт-пресет конвертации на базе `convert-audio`.
      security:
        - bearerAuth: []
      requestBody:
        required: true
        content:
          multipart/form-data:
            schema:
              type: object
              required:
                - file
              properties:
                file:
                  type: string
                  format: binary
                  description: OGG to WAV input file (.ogg)
                settings:
                  type: string
                  description: |
                    Dedicated OGG to WAV converter using the convert-audio pipeline.
                    The output format is locked by the endpoint.
                clientJobId:
                  type: string
                  description: Client-provided job ID for SSE progress tracking
      responses:
        '200':
          description: Processed file
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ToolResponse'
        '400':
          description: Invalid input or settings
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Error'
        '401':
          description: Authentication required
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/UnauthorizedError'
        '422':
          description: Processing failed
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Error'
  /api/v1/tools/audio/wav-to-mp3:
    post:
      operationId: wavToMp3Tool
      tags:
        - Tools
      summary: WAV в MP3
      description: |
        Конвертация WAV в MP3
        Это выделенный эндпоинт-пресет конвертации на базе `convert-audio`.
      security:
        - bearerAuth: []
      requestBody:
        required: true
        content:
          multipart/form-data:
            schema:
              type: object
              required:
                - file
              properties:
                file:
                  type: string
                  format: binary
                  description: WAV to MP3 input file (.wav)
                settings:
                  type: string
                  description: |
                    Dedicated WAV to MP3 converter using the convert-audio pipeline.
                    The output format is locked by the endpoint.
                clientJobId:
                  type: string
                  description: Client-provided job ID for SSE progress tracking
      responses:
        '200':
          description: Processed file
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ToolResponse'
        '400':
          description: Invalid input or settings
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Error'
        '401':
          description: Authentication required
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/UnauthorizedError'
        '422':
          description: Processing failed
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Error'
  /api/v1/tools/audio/mp3-to-wav:
    post:
      operationId: mp3ToWavTool
      tags:
        - Tools
      summary: MP3 в WAV
      description: |
        Конвертация MP3 в WAV
        Это выделенный эндпоинт-пресет конвертации на базе `convert-audio`.
      security:
        - bearerAuth: []
      requestBody:
        required: true
        content:
          multipart/form-data:
            schema:
              type: object
              required:
                - file
              properties:
                file:
                  type: string
                  format: binary
                  description: MP3 to WAV input file (.mp3)
                settings:
                  type: string
                  description: |
                    Dedicated MP3 to WAV converter using the convert-audio pipeline.
                    The output format is locked by the endpoint.
                clientJobId:
                  type: string
                  description: Client-provided job ID for SSE progress tracking
      responses:
        '200':
          description: Processed file
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ToolResponse'
        '400':
          description: Invalid input or settings
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Error'
        '401':
          description: Authentication required
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/UnauthorizedError'
        '422':
          description: Processing failed
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Error'
  /api/v1/tools/audio/flac-to-mp3:
    post:
      operationId: flacToMp3Tool
      tags:
        - Tools
      summary: FLAC в MP3
      description: |
        Конвертация FLAC в MP3
        Это выделенный эндпоинт-пресет конвертации на базе `convert-audio`.
      security:
        - bearerAuth: []
      requestBody:
        required: true
        content:
          multipart/form-data:
            schema:
              type: object
              required:
                - file
              properties:
                file:
                  type: string
                  format: binary
                  description: FLAC to MP3 input file (.flac)
                settings:
                  type: string
                  description: |
                    Dedicated FLAC to MP3 converter using the convert-audio pipeline.
                    The output format is locked by the endpoint.
                clientJobId:
                  type: string
                  description: Client-provided job ID for SSE progress tracking
      responses:
        '200':
          description: Processed file
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ToolResponse'
        '400':
          description: Invalid input or settings
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Error'
        '401':
          description: Authentication required
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/UnauthorizedError'
        '422':
          description: Processing failed
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Error'
  /api/v1/tools/files/excel-to-csv:
    post:
      operationId: excelToCsvTool
      tags:
        - Tools
      summary: Excel в CSV
      description: |
        Конвертация Excel в CSV
        Это выделенный эндпоинт-пресет конвертации на базе `convert-spreadsheet`.
      security:
        - bearerAuth: []
      requestBody:
        required: true
        content:
          multipart/form-data:
            schema:
              type: object
              required:
                - file
              properties:
                file:
                  type: string
                  format: binary
                  description: Excel to CSV input file (.xlsx, .xls)
                settings:
                  type: string
                  description: |
                    Dedicated Excel to CSV converter using the convert-spreadsheet pipeline.
                    The output format is locked by the endpoint.
                clientJobId:
                  type: string
                  description: Client-provided job ID for SSE progress tracking
      responses:
        '202':
          description: Accepted for async processing. Track progress via SSE at /api/v1/jobs/{jobId}/progress.
          content:
            application/json:
              schema:
                type: object
                properties:
                  jobId:
                    type: string
                  async:
                    type: boolean
                    example: true
        '400':
          description: Invalid input or settings
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Error'
        '401':
          description: Authentication required
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/UnauthorizedError'
        '422':
          description: Processing failed
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Error'
  /api/v1/readyz:
    get:
      operationId: readinessCheck
      tags:
        - System
      summary: Проверка готовности
      description: Проверяет PostgreSQL, Redis, свободное место на локальном диске и доступность S3, когда она настроена. Публичный эндпоинт для балансировщиков нагрузки и оркестраторов.
      security: []
      responses:
        '200':
          description: Instance is ready to receive traffic
        '503':
          description: Instance is not ready to receive traffic
  /api/v1/jobs/{jobId}/cancel:
    post:
      operationId: cancelJob
      tags:
        - System
      summary: Отмена задачи
      description: Запрос отмены задачи в очереди или выполняющейся задачи.
      security:
        - bearerAuth: []
      parameters:
        - name: jobId
          in: path
          required: true
          schema:
            type: string
      responses:
        '200':
          description: Cancellation request result
        '401':
          description: Authentication required
  /api/v1/preferences:
    get:
      operationId: getPreferences
      tags:
        - Settings
      summary: Получить настройки пользователя
      description: Возвращает карту настроек текущего пользователя.
      security:
        - bearerAuth: []
      responses:
        '200':
          description: Preference map
        '401':
          description: Authentication required
    put:
      operationId: updatePreferences
      tags:
        - Settings
      summary: Обновление настроек пользователя
      description: Вставка или обновление одной или нескольких настроек для текущего пользователя.
      security:
        - bearerAuth: []
      requestBody:
        required: true
        content:
          application/json:
            schema:
              type: object
      responses:
        '200':
          description: Preferences updated
        '400':
          description: Invalid preferences body
        '401':
          description: Authentication required
  /api/auth/mfa/enroll:
    post:
      operationId: enrollMfa
      tags:
        - Auth
      summary: Начать регистрацию MFA
      description: Начало регистрации TOTP MFA для текущего пользователя. Требует enterprise-функцию MFA.
      security:
        - bearerAuth: []
      responses:
        '200':
          description: Enrollment URI and recovery codes
        '401':
          description: Authentication required
        '403':
          description: MFA feature not licensed
        '409':
          description: MFA enrollment already enabled or pending
  /api/auth/mfa/verify:
    post:
      operationId: verifyMfaEnrollment
      tags:
        - Auth
      summary: Подтверждение регистрации MFA
      description: Подтверждение ожидающей регистрации MFA с помощью кода TOTP.
      security:
        - bearerAuth: []
      requestBody:
        required: true
        content:
          application/json:
            schema:
              type: object
              required:
                - code
              properties:
                code:
                  type: string
      responses:
        '200':
          description: MFA enabled
        '400':
          description: No pending enrollment or invalid body
        '401':
          description: Invalid TOTP code
  /api/auth/mfa/complete:
    post:
      operationId: completeMfaLogin
      tags:
        - Auth
      summary: Завершение входа с MFA
      description: Завершение ожидающего запроса входа с помощью кода TOTP или кода восстановления.
      security: []
      requestBody:
        required: true
        content:
          application/json:
            schema:
              type: object
              required:
                - mfaToken
                - code
              properties:
                mfaToken:
                  type: string
                code:
                  type: string
      responses:
        '200':
          description: Session token and user profile
        '400':
          description: Invalid request body
        '401':
          description: Invalid or expired MFA challenge
  /api/auth/mfa/disable:
    post:
      operationId: disableMfa
      tags:
        - Auth
      summary: Отключение MFA
      description: Отключение MFA для текущего пользователя после проверки кода TOTP.
      security:
        - bearerAuth: []
      requestBody:
        required: true
        content:
          application/json:
            schema:
              type: object
              required:
                - code
              properties:
                code:
                  type: string
      responses:
        '200':
          description: MFA disabled
        '400':
          description: MFA is not enabled or body is invalid
        '401':
          description: Authentication required or invalid code
  /api/auth/users/{id}/mfa/reset:
    post:
      operationId: resetUserMfa
      tags:
        - Auth
      summary: Сброс MFA пользователя
      description: Административный сброс регистрации MFA пользователя. Требует разрешение users:manage.
      security:
        - bearerAuth: []
      parameters:
        - name: id
          in: path
          required: true
          schema:
            type: string
      responses:
        '200':
          description: MFA reset
        '401':
          description: Authentication required
        '403':
          description: Insufficient permissions
        '404':
          description: User not found
  /api/auth/oidc/login:
    get:
      operationId: oidcLogin
      tags:
        - Auth
      summary: Начать вход через OIDC
      description: Перенаправление на настроенного провайдера OpenID Connect.
      security: []
      responses:
        '302':
          description: Redirect to identity provider
        '500':
          description: OIDC provider discovery failed
  /api/auth/oidc/callback:
    get:
      operationId: oidcCallback
      tags:
        - Auth
      summary: Callback OIDC
      description: Завершение потока authorization code OIDC и создание сессии.
      security: []
      responses:
        '302':
          description: Redirect to the application
  /api/auth/saml/metadata:
    get:
      operationId: samlMetadata
      tags:
        - Auth
      summary: Метаданные SAML
      description: Возвращает XML метаданных поставщика услуг SAML.
      security: []
      responses:
        '200':
          description: SAML metadata XML
  /api/auth/saml/login:
    get:
      operationId: samlLogin
      tags:
        - Auth
      summary: Начать вход через SAML
      description: Перенаправление на настроенного поставщика идентификации SAML.
      security: []
      responses:
        '302':
          description: Redirect to identity provider
  /api/auth/saml/callback:
    post:
      operationId: samlCallback
      tags:
        - Auth
      summary: Callback SAML
      description: Эндпоинт assertion consumer service для входа через SAML.
      security: []
      responses:
        '302':
          description: Redirect to the application
  /api/v1/files/{id}/preview:
    get:
      operationId: getFilePreview
      tags:
        - Files
      summary: Предпросмотр файла
      description: Потоковая передача или создание совместимого с браузером предпросмотра для сохранённого файла PDF, офисного документа, видео или аудио.
      security:
        - bearerAuth: []
      parameters:
        - name: id
          in: path
          required: true
          schema:
            type: string
      responses:
        '200':
          description: Preview binary
        '400':
          description: Preview not supported
        '401':
          description: Authentication required
        '404':
          description: File not found
        '422':
          description: Could not generate preview
  /api/v1/preview/generate:
    post:
      operationId: generatePreview
      tags:
        - Files
      summary: Создать предпросмотр загрузки
      description: Создание по требованию предпросмотра MP4 или MP3 для загруженного медиафайла.
      security: []
      requestBody:
        required: true
        content:
          multipart/form-data:
            schema:
              type: object
              required:
                - file
              properties:
                file:
                  type: string
                  format: binary
      responses:
        '200':
          description: Preview binary
        '400':
          description: Unsupported file type or missing file
        '422':
          description: Could not generate preview
  /api/v1/admin/log-level:
    get:
      operationId: getLogLevel
      tags:
        - Admin
      summary: Получить уровень логирования
      description: Чтение текущего уровня логирования во время выполнения. Требует разрешение settings:write.
      security:
        - bearerAuth: []
      responses:
        '200':
          description: Current log level
        '403':
          description: Insufficient permissions
    post:
      operationId: setLogLevel
      tags:
        - Admin
      summary: Установить уровень логирования
      description: Изменение уровня логирования во время выполнения. Требует разрешение settings:write.
      security:
        - bearerAuth: []
      requestBody:
        required: true
        content:
          application/json:
            schema:
              type: object
              required:
                - level
              properties:
                level:
                  type: string
                  enum:
                    - fatal
                    - error
                    - warn
                    - info
                    - debug
                    - trace
                    - silent
      responses:
        '200':
          description: New log level
        '400':
          description: Invalid log level
        '403':
          description: Insufficient permissions
  /api/v1/metrics:
    get:
      operationId: getMetrics
      tags:
        - Admin
      summary: Метрики Prometheus
      description: Возвращает метрики Prometheus. Требует разрешение system:health.
      security:
        - bearerAuth: []
      responses:
        '200':
          description: Prometheus text format
        '403':
          description: Insufficient permissions
  /api/v1/admin/support-bundle:
    get:
      operationId: getSupportBundle
      tags:
        - Admin
      summary: Пакет поддержки
      description: Скачивание отредактированного диагностического пакета поддержки ZIP. Требует разрешение system:health.
      security:
        - bearerAuth: []
      responses:
        '200':
          description: Support bundle ZIP
        '403':
          description: Insufficient permissions
  /api/v1/admin/usage:
    get:
      operationId: getAdminUsage
      tags:
        - Admin
      summary: Данные панели использования
      description: Возвращает данные локальной панели использования. Требует разрешение audit:read.
      security:
        - bearerAuth: []
      parameters:
        - name: days
          in: query
          required: false
          schema:
            type: string
      responses:
        '200':
          description: Usage dashboard data
        '400':
          description: Invalid query parameters
        '403':
          description: Insufficient permissions
  /api/v1/admin/backup-status:
    get:
      operationId: getBackupStatus
      tags:
        - Admin
      summary: Статус резервного копирования
      description: Возвращает метаданные последнего резервного копирования и его актуальность. Требует разрешение system:health.
      security:
        - bearerAuth: []
      responses:
        '200':
          description: Backup status
        '403':
          description: Insufficient permissions
    post:
      operationId: setBackupStatus
      tags:
        - Admin
      summary: Запись статуса резервного копирования
      description: Запись завершённого резервного копирования. Требует разрешение system:health.
      security:
        - bearerAuth: []
      requestBody:
        required: true
        content:
          application/json:
            schema:
              type: object
              required:
                - type
              properties:
                type:
                  type: string
                sizeBytes:
                  type: integer
                notes:
                  type: string
      responses:
        '200':
          description: Backup status recorded
        '400':
          description: Invalid backup status
        '403':
          description: Insufficient permissions
  /api/v1/admin/features/import:
    post:
      operationId: importFeatureBundle
      tags:
        - Features
      summary: Импорт набора функций
      description: |-
        Импортируйте устаревший архив пакета AI или импортируйте точный автономный OCR.
        выпустить, используя подписанный индекс и соответствующий архив платформы. OCR
        импорт выполняет ту же подпись, хеш, совместимость, извлечение,
        и smoke-test проверяет онлайн-установку, включая точную
        Минимум эффективной памяти во время выполнения 4 GiB. Fast Tesseract OCR не является
        при условии соблюдения этого требования. Требуется разрешение features:manage.
      security:
        - bearerAuth: []
      requestBody:
        required: true
        content:
          multipart/form-data:
            schema:
              type: object
              oneOf:
                - required:
                    - file
                - required:
                    - index
                    - archive
              properties:
                file:
                  type: string
                  format: binary
                  description: One legacy AI bundle archive; mutually exclusive with index/archive
                index:
                  type: string
                  format: binary
                  description: Signed ocr-runtime-index.json from the SnapOtter release
                archive:
                  type: string
                  format: binary
                  description: OCR runtime archive for the current Linux amd64 or arm64 target; must follow index
      responses:
        '200':
          description: Bundle verified, imported, and activated
        '400':
          description: Invalid archive
        '403':
          description: Insufficient permissions
        '409':
          description: Import lock is held
        '507':
          description: Insufficient storage for the authenticated archive and expanded runtime
  /api/v1/enterprise/scim/token:
    post:
      operationId: createScimToken
      tags:
        - Enterprise
      summary: Создание токена SCIM
      description: Создание bearer-токена SCIM. Требует разрешение users:manage и enterprise-функцию SCIM.
      security:
        - bearerAuth: []
      responses:
        '201':
          description: SCIM token created
        '403':
          description: SCIM feature not licensed or insufficient permissions
    delete:
      operationId: revokeScimToken
      tags:
        - Enterprise
      summary: Отзыв токена SCIM
      description: Отзыв текущего bearer-токена SCIM. Требует разрешение users:manage и enterprise-функцию SCIM.
      security:
        - bearerAuth: []
      responses:
        '204':
          description: SCIM token revoked
        '403':
          description: SCIM feature not licensed or insufficient permissions
  /api/v1/scim/v2/ServiceProviderConfig:
    get:
      operationId: getScimServiceProviderConfig
      tags:
        - Enterprise
      summary: Конфигурация поставщика услуг SCIM
      description: Публичный эндпоинт обнаружения SCIM.
      security: []
      responses:
        '200':
          description: SCIM service provider capabilities
  /api/v1/scim/v2/Schemas:
    get:
      operationId: getScimSchemas
      tags:
        - Enterprise
      summary: Схемы SCIM
      description: Публичный эндпоинт обнаружения схем SCIM.
      security: []
      responses:
        '200':
          description: SCIM schemas
  /api/v1/scim/v2/ResourceTypes:
    get:
      operationId: getScimResourceTypes
      tags:
        - Enterprise
      summary: Типы ресурсов SCIM
      description: Публичный эндпоинт обнаружения типов ресурсов SCIM.
      security: []
      responses:
        '200':
          description: SCIM resource types
  /api/v1/scim/v2/Users:
    get:
      operationId: listScimUsers
      tags:
        - Enterprise
      summary: Список пользователей SCIM
      description: Список пользователей SCIM. Требует bearer-токен SCIM.
      responses:
        '200':
          description: SCIM user list
    post:
      operationId: createScimUser
      tags:
        - Enterprise
      summary: Создание пользователя SCIM
      description: Создание пользователя SCIM. Требует bearer-токен SCIM.
      responses:
        '201':
          description: SCIM user created
  /api/v1/scim/v2/Users/{id}:
    get:
      operationId: getScimUser
      tags:
        - Enterprise
      summary: Получить пользователя SCIM
      description: Получение пользователя SCIM по ID. Требует bearer-токен SCIM.
      parameters:
        - name: id
          in: path
          required: true
          schema:
            type: string
      responses:
        '200':
          description: SCIM user
    put:
      operationId: updateScimUser
      tags:
        - Enterprise
      summary: Обновление пользователя SCIM
      description: Замена пользователя SCIM по ID. Требует bearer-токен SCIM.
      parameters:
        - name: id
          in: path
          required: true
          schema:
            type: string
      responses:
        '200':
          description: SCIM user updated
    delete:
      operationId: deleteScimUser
      tags:
        - Enterprise
      summary: Удаление пользователя SCIM
      description: Мягкая деактивация пользователя SCIM по ID. Требует bearer-токен SCIM.
      parameters:
        - name: id
          in: path
          required: true
          schema:
            type: string
      responses:
        '200':
          description: SCIM user deactivated
  /api/v1/scim/v2/Groups:
    get:
      operationId: listScimGroups
      tags:
        - Enterprise
      summary: Список групп SCIM
      description: Список групп SCIM. Требует bearer-токен SCIM.
      responses:
        '200':
          description: SCIM group list
    post:
      operationId: createScimGroup
      tags:
        - Enterprise
      summary: Создание группы SCIM
      description: Создание группы SCIM. Требует bearer-токен SCIM.
      responses:
        '201':
          description: SCIM group created
  /api/v1/scim/v2/Groups/{id}:
    get:
      operationId: getScimGroup
      tags:
        - Enterprise
      summary: Получить группу SCIM
      description: Получение группы SCIM по ID. Требует bearer-токен SCIM.
      parameters:
        - name: id
          in: path
          required: true
          schema:
            type: string
      responses:
        '200':
          description: SCIM group
    put:
      operationId: updateScimGroup
      tags:
        - Enterprise
      summary: Обновление группы SCIM
      description: Замена группы SCIM по ID. Требует bearer-токен SCIM.
      parameters:
        - name: id
          in: path
          required: true
          schema:
            type: string
      responses:
        '200':
          description: SCIM group updated
    delete:
      operationId: deleteScimGroup
      tags:
        - Enterprise
      summary: Удаление группы SCIM
      description: Удаление группы SCIM по ID. Требует bearer-токен SCIM.
      parameters:
        - name: id
          in: path
          required: true
          schema:
            type: string
      responses:
        '204':
          description: SCIM group deleted
  /api/v1/enterprise/audit/export:
    get:
      operationId: exportAuditLog
      tags:
        - Enterprise
      summary: Экспорт журнала аудита
      description: Экспорт записей аудита в формате JSON или CSV. Требует разрешение audit:read и enterprise-функцию экспорта аудита.
      security:
        - bearerAuth: []
      responses:
        '200':
          description: Audit export
        '400':
          description: Invalid query parameters
        '403':
          description: Feature not licensed or insufficient permissions
  /api/v1/enterprise/config/export:
    get:
      operationId: exportEnterpriseConfig
      tags:
        - Enterprise
      summary: Экспорт конфигурации
      description: Экспорт отредактированной конфигурации экземпляра, пользовательских ролей и команд. Требует разрешение system:health.
      security:
        - bearerAuth: []
      responses:
        '200':
          description: Configuration export
        '403':
          description: Feature not licensed or insufficient permissions
  /api/v1/enterprise/config/import:
    post:
      operationId: importEnterpriseConfig
      tags:
        - Enterprise
      summary: Импорт конфигурации
      description: Импорт конфигурации экземпляра, пользовательских ролей и команд. Требует разрешение system:health.
      security:
        - bearerAuth: []
      responses:
        '200':
          description: Import result
        '400':
          description: Invalid import payload
        '403':
          description: Feature not licensed or insufficient permissions
  /api/v1/enterprise/ip-allowlist:
    get:
      operationId: getIpAllowlist
      tags:
        - Enterprise
      summary: Получить список разрешённых IP
      description: Чтение enterprise-списка разрешённых IP. Требует разрешение security:manage.
      security:
        - bearerAuth: []
      responses:
        '200':
          description: IP allowlist
        '403':
          description: Feature not licensed or insufficient permissions
    put:
      operationId: updateIpAllowlist
      tags:
        - Enterprise
      summary: Обновление списка разрешённых IP
      description: Обновление записей CIDR в списке разрешённых с предотвращением самоблокировки. Требует разрешение security:manage.
      security:
        - bearerAuth: []
      responses:
        '200':
          description: IP allowlist updated
        '400':
          description: Invalid CIDR entries
        '403':
          description: Feature not licensed or insufficient permissions
  /api/v1/enterprise/legal-hold:
    get:
      operationId: listLegalHolds
      tags:
        - Enterprise
      summary: Список юридических блокировок
      description: Список пользователей и команд под юридической блокировкой. Требует разрешение compliance:manage.
      security:
        - bearerAuth: []
      responses:
        '200':
          description: Legal holds
        '403':
          description: Feature not licensed or insufficient permissions
    put:
      operationId: updateLegalHold
      tags:
        - Enterprise
      summary: Обновление юридической блокировки
      description: Применение или снятие юридической блокировки для пользователя или команды. Требует разрешение compliance:manage.
      security:
        - bearerAuth: []
      responses:
        '200':
          description: Legal hold updated
        '400':
          description: Invalid request body
        '403':
          description: Feature not licensed or insufficient permissions
  /api/v1/enterprise/siem/config:
    get:
      operationId: getSiemConfig
      tags:
        - Enterprise
      summary: Получить конфигурацию SIEM
      description: Чтение конфигурации пересылки в SIEM. Требует разрешение webhooks:manage.
      security:
        - bearerAuth: []
      responses:
        '200':
          description: SIEM config
        '403':
          description: Feature not licensed or insufficient permissions
    put:
      operationId: updateSiemConfig
      tags:
        - Enterprise
      summary: Обновление конфигурации SIEM
      description: Обновление конфигурации пересылки в SIEM. Требует разрешение webhooks:manage.
      security:
        - bearerAuth: []
      responses:
        '200':
          description: SIEM config updated
        '400':
          description: Invalid SIEM config
        '403':
          description: Feature not licensed or insufficient permissions
  /api/v1/enterprise/webhooks:
    get:
      operationId: listWebhooks
      tags:
        - Enterprise
      summary: Список вебхуков
      description: Список назначений вебхуков. Требует разрешение webhooks:manage.
      security:
        - bearerAuth: []
      responses:
        '200':
          description: Webhook destinations
        '403':
          description: Feature not licensed or insufficient permissions
    post:
      operationId: createWebhook
      tags:
        - Enterprise
      summary: Создание вебхука
      description: Создание назначения вебхука. Требует разрешение webhooks:manage.
      security:
        - bearerAuth: []
      responses:
        '201':
          description: Webhook created
        '400':
          description: Invalid webhook destination
        '403':
          description: Feature not licensed or insufficient permissions
  /api/v1/enterprise/webhooks/{index}:
    put:
      operationId: updateWebhook
      tags:
        - Enterprise
      summary: Обновление вебхука
      description: Обновление назначения вебхука по индексу. Требует разрешение webhooks:manage.
      security:
        - bearerAuth: []
      parameters:
        - name: index
          in: path
          required: true
          schema:
            type: string
      responses:
        '200':
          description: Webhook updated
        '400':
          description: Invalid index or destination
        '404':
          description: Webhook not found
    delete:
      operationId: deleteWebhook
      tags:
        - Enterprise
      summary: Удаление вебхука
      description: Удаление назначения вебхука по индексу. Требует разрешение webhooks:manage.
      security:
        - bearerAuth: []
      parameters:
        - name: index
          in: path
          required: true
          schema:
            type: string
      responses:
        '200':
          description: Webhook deleted
        '400':
          description: Invalid index
        '404':
          description: Webhook not found
  /api/v1/enterprise/webhooks/{index}/test:
    post:
      operationId: testWebhook
      tags:
        - Enterprise
      summary: Тест вебхука
      description: Отправка тестовой полезной нагрузки на назначение вебхука. Требует разрешение webhooks:manage.
      security:
        - bearerAuth: []
      parameters:
        - name: index
          in: path
          required: true
          schema:
            type: string
      responses:
        '200':
          description: Test result
        '400':
          description: Invalid index
        '404':
          description: Webhook not found
  /api/v1/enterprise/users/{id}/export:
    post:
      operationId: startGdprUserExport
      tags:
        - Enterprise
      summary: Запустить экспорт GDPR
      description: Запуск задачи экспорта данных пользователя по GDPR. Требует разрешение compliance:manage.
      security:
        - bearerAuth: []
      parameters:
        - name: id
          in: path
          required: true
          schema:
            type: string
      responses:
        '202':
          description: Export job started
        '403':
          description: Feature not licensed or insufficient permissions
        '404':
          description: User not found
  /api/v1/enterprise/users/{id}/export/{jobId}:
    get:
      operationId: getGdprUserExport
      tags:
        - Enterprise
      summary: Получить статус экспорта GDPR
      description: Чтение статуса и URL для скачивания задачи экспорта пользователя по GDPR. Требует разрешение compliance:manage.
      security:
        - bearerAuth: []
      parameters:
        - name: id
          in: path
          required: true
          schema:
            type: string
        - name: jobId
          in: path
          required: true
          schema:
            type: string
      responses:
        '200':
          description: Export status
        '404':
          description: Export job not found
  /api/v1/enterprise/users/{id}/purge:
    delete:
      operationId: purgeEnterpriseUser
      tags:
        - Enterprise
      summary: Очистка данных пользователя
      description: Безвозвратная очистка данных пользователя после явного подтверждения. Требует разрешение compliance:manage.
      security:
        - bearerAuth: []
      parameters:
        - name: id
          in: path
          required: true
          schema:
            type: string
      responses:
        '200':
          description: User purged
        '400':
          description: Confirmation required
        '409':
          description: User or team is under legal hold
  /api/v1/enterprise/teams/{id}/purge:
    delete:
      operationId: purgeEnterpriseTeam
      tags:
        - Enterprise
      summary: Очистка данных команды
      description: Безвозвратная очистка данных команды после явного подтверждения. Требует разрешение compliance:manage.
      security:
        - bearerAuth: []
      parameters:
        - name: id
          in: path
          required: true
          schema:
            type: string
      responses:
        '200':
          description: Team purged
        '400':
          description: Confirmation required
        '409':
          description: Team is under legal hold
  /api/v1/admin/version:
    get:
      operationId: getAdminVersion
      tags:
        - Admin
      summary: Метаданные версии
      description: Чтение версии приложения, даты сборки, версии Node и версии схемы. Требует разрешение system:health и enterprise-функцию управления обновлениями.
      security:
        - bearerAuth: []
      responses:
        '200':
          description: Version metadata
        '403':
          description: Feature not licensed or insufficient permissions
  /api/v1/admin/migrations/pending:
    get:
      operationId: getPendingMigrations
      tags:
        - Admin
      summary: Статус миграций
      description: Сравнение упакованных миграций с применёнными миграциями. Требует разрешение system:health и enterprise-функцию управления обновлениями.
      security:
        - bearerAuth: []
      responses:
        '200':
          description: Migration status
        '403':
          description: Feature not licensed or insufficient permissions
  /api/v1/admin/upgrade-check:
    get:
      operationId: getUpgradeCheck
      tags:
        - Admin
      summary: Готовность к обновлению
      description: Запуск проверок готовности к обновлению. Требует разрешение system:health и enterprise-функцию управления обновлениями.
      security:
        - bearerAuth: []
      responses:
        '200':
          description: Upgrade readiness result
        '403':
          description: Feature not licensed or insufficient permissions
  /llms.txt:
    get:
      operationId: getLlmsTxt
      tags:
        - System
      summary: Сводка для LLM
      description: Дружелюбная к LLM текстовая сводка API, сгенерированная из спецификации OpenAPI.
      security: []
      responses:
        '200':
          description: Plain-text LLM summary
          content:
            text/plain:
              schema:
                type: string
  /llms-full.txt:
    get:
      operationId: getLlmsFullTxt
      tags:
        - System
      summary: Полная документация API для LLM
      description: Полная текстовая документация API, сгенерированная из спецификации OpenAPI.
      security: []
      responses:
        '200':
          description: Full plain-text API docs
          content:
            text/plain:
              schema:
                type: string
  /api/v1/openapi.yaml:
    get:
      operationId: getOpenApiYaml
      tags:
        - System
      summary: OpenAPI YAML
      description: Машиночитаемая спецификация OpenAPI 3.1 для этого экземпляра SnapOtter.
      security: []
      responses:
        '200':
          description: OpenAPI YAML document
          content:
            text/yaml:
              schema:
                type: string
  /api/v1/tools/popular:
    get:
      operationId: getPopularTools
      tags:
        - Tools
      summary: Популярные инструменты
      description: Возвращает до 12 ID популярных инструментов, переходя к курируемому списку по умолчанию при недостатке данных об использовании.
      security: []
      responses:
        '200':
          description: Popular tool IDs
          content:
            application/json:
              schema:
                type: object
                properties:
                  tools:
                    type: array
                    items:
                      type: string
x-i18n:
  locale: ru
  generator: scripts/i18n/adapters/api-spec.mjs
  entries:
    info.description:
      sourceHash: 0a7d56a602a9
      provenance: machine
      outputHash: 3f496cc72023
    tags.Tools.description:
      sourceHash: 9c71553d06df
      provenance: machine
      outputHash: 668ef301c469
    tags.Batch.description:
      sourceHash: f9c99eda7653
      provenance: machine
      outputHash: 3514d998b79b
    tags.Pipelines.description:
      sourceHash: 18d60931b26d
      provenance: machine
      outputHash: 4f258e7f59c5
    tags.Files.description:
      sourceHash: c3b0e1590a61
      provenance: machine
      outputHash: 23a99be27f96
    tags.Auth.description:
      sourceHash: 23f6a0fcf611
      provenance: machine
      outputHash: 3171bee57ab8
    tags.API Keys.description:
      sourceHash: fb725a6a642a
      provenance: machine
      outputHash: 64f2a910896b
    tags.Settings.description:
      sourceHash: 25788b024fe2
      provenance: machine
      outputHash: d64642bd93fc
    tags.Teams.description:
      sourceHash: 750d92c8bb9f
      provenance: machine
      outputHash: a78f582eea58
    tags.Roles.description:
      sourceHash: d031f589a884
      provenance: machine
      outputHash: 8f18a6818a16
    tags.Audit.description:
      sourceHash: ed43b35a8636
      provenance: machine
      outputHash: 00dc2cdbeb0b
    tags.Analytics.description:
      sourceHash: 36832a1b1df0
      provenance: machine
      outputHash: 4f292d95ead6
    tags.Features.description:
      sourceHash: a6cdd16968b5
      provenance: machine
      outputHash: 95248cc80ce8
    tags.Admin.description:
      sourceHash: c19c4b4f68c4
      provenance: machine
      outputHash: 21688a62f512
    tags.System.description:
      sourceHash: 91561bc11296
      provenance: machine
      outputHash: 24fa65f98c4d
    tags.Enterprise.description:
      sourceHash: 91b6b46b0dd1
      provenance: machine
      outputHash: b427b41fa0e2
    paths./api/v1/health.get.summary:
      sourceHash: 14290e1d0681
      provenance: machine
      outputHash: 13c4c6b288c8
    paths./api/v1/health.get.description:
      sourceHash: 784e94eaa9df
      provenance: machine
      outputHash: aad7191a65c5
    paths./api/v1/tools/image/resize.post.summary:
      sourceHash: 2956e06ac065
      provenance: machine
      outputHash: b702087cbff5
    paths./api/v1/tools/image/resize.post.description:
      sourceHash: 3dd014df5258
      provenance: machine
      outputHash: e6710a7b792a
    paths./api/v1/tools/image/content-aware-resize.post.summary:
      sourceHash: 7f86a6451d78
      provenance: machine
      outputHash: 60e8b16cb4ad
    paths./api/v1/tools/image/content-aware-resize.post.description:
      sourceHash: fe6de26388f5
      provenance: machine
      outputHash: a3ddce912d58
    paths./api/v1/tools/image/crop.post.summary:
      sourceHash: 98caf7c43c6f
      provenance: machine
      outputHash: 1750bfde7de9
    paths./api/v1/tools/image/crop.post.description:
      sourceHash: db6c5c89d359
      provenance: machine
      outputHash: 0930cc75564d
    paths./api/v1/tools/image/rotate.post.summary:
      sourceHash: c98856efbb08
      provenance: machine
      outputHash: bbbe7efe973c
    paths./api/v1/tools/image/rotate.post.description:
      sourceHash: 5360b7d1a4c3
      provenance: machine
      outputHash: 63355c9b07a1
    paths./api/v1/tools/image/convert.post.summary:
      sourceHash: 3c2ee9bc3ec8
      provenance: machine
      outputHash: 4d8f98b23ca8
    paths./api/v1/tools/image/convert.post.description:
      sourceHash: 2298d8af460d
      provenance: machine
      outputHash: e1a8ab266d04
    paths./api/v1/tools/image/compress.post.summary:
      sourceHash: 033fc0445b90
      provenance: machine
      outputHash: 9d075c6c6973
    paths./api/v1/tools/image/compress.post.description:
      sourceHash: 01f63e9e928a
      provenance: machine
      outputHash: bfb07ac6708a
    paths./api/v1/tools/image/strip-metadata.post.summary:
      sourceHash: 013af4252599
      provenance: machine
      outputHash: ec034c354a73
    paths./api/v1/tools/image/strip-metadata.post.description:
      sourceHash: b5718bfd5335
      provenance: machine
      outputHash: aff39627eda1
    paths./api/v1/tools/image/border.post.summary:
      sourceHash: 1d23e2771077
      provenance: machine
      outputHash: d5dcda8c8447
    paths./api/v1/tools/image/border.post.description:
      sourceHash: 6d48679c08cd
      provenance: machine
      outputHash: eccf1e713c9d
    paths./api/v1/tools/image/beautify.post.summary:
      sourceHash: 096b365107c5
      provenance: machine
      outputHash: 08d2b63258ba
    paths./api/v1/tools/image/beautify.post.description:
      sourceHash: 7db7fb58263f
      provenance: machine
      outputHash: fc6c6eb794c9
    paths./api/v1/tools/image/adjust-colors.post.summary:
      sourceHash: a45b7d1e2535
      provenance: machine
      outputHash: d39f2445d609
    paths./api/v1/tools/image/adjust-colors.post.description:
      sourceHash: 1fb36db32da7
      provenance: machine
      outputHash: 1502b0fe7d6c
    paths./api/v1/tools/image/sharpening.post.summary:
      sourceHash: 73fb568dcab6
      provenance: machine
      outputHash: 9ae6317d5062
    paths./api/v1/tools/image/sharpening.post.description:
      sourceHash: 8080cda0610a
      provenance: machine
      outputHash: 3cc1c7695948
    paths./api/v1/tools/image/optimize-for-web.post.summary:
      sourceHash: 54245d072fef
      provenance: machine
      outputHash: 67629ba714bf
    paths./api/v1/tools/image/optimize-for-web.post.description:
      sourceHash: e846eaabbabc
      provenance: machine
      outputHash: 8df7918f2e56
    paths./api/v1/tools/image/optimize-for-web/preview.post.summary:
      sourceHash: f50e6149b56c
      provenance: machine
      outputHash: a2cb65cfb337
    paths./api/v1/tools/image/optimize-for-web/preview.post.description:
      sourceHash: 10a427197f6e
      provenance: machine
      outputHash: f4d7b4589e3e
    paths./api/v1/tools/image/image-enhancement.post.summary:
      sourceHash: 246a536c8207
      provenance: machine
      outputHash: 6e9bf38b7cee
    paths./api/v1/tools/image/image-enhancement.post.description:
      sourceHash: 17ec5e5e2b04
      provenance: machine
      outputHash: 80f324a20055
    paths./api/v1/tools/image/image-enhancement/analyze.post.summary:
      sourceHash: 2697cf8b079f
      provenance: machine
      outputHash: ea2d9e922631
    paths./api/v1/tools/image/image-enhancement/analyze.post.description:
      sourceHash: 7712bfd40c3a
      provenance: machine
      outputHash: edb48ea97638
    paths./api/v1/tools/image/noise-removal.post.summary:
      sourceHash: 9301fe47b563
      provenance: machine
      outputHash: 1d2ecd5379c8
    paths./api/v1/tools/image/noise-removal.post.description:
      sourceHash: e16aa743abcb
      provenance: machine
      outputHash: 58bcadd8a804
    paths./api/v1/tools/image/red-eye-removal.post.summary:
      sourceHash: d9315c5a87b4
      provenance: machine
      outputHash: 1f6fd5e05bb9
    paths./api/v1/tools/image/red-eye-removal.post.description:
      sourceHash: 479c687988eb
      provenance: machine
      outputHash: bdb251acf5d8
    paths./api/v1/tools/image/restore-photo.post.summary:
      sourceHash: 3ef425c0ba9b
      provenance: machine
      outputHash: c839d0c5f6e5
    paths./api/v1/tools/image/restore-photo.post.description:
      sourceHash: 6e3b2de0372c
      provenance: machine
      outputHash: 0638b241ab3e
    paths./api/v1/tools/image/passport-photo/analyze.post.summary:
      sourceHash: 0a3c3687ff5f
      provenance: machine
      outputHash: bfc635bcd5b7
    paths./api/v1/tools/image/passport-photo/analyze.post.description:
      sourceHash: 235c51cb497e
      provenance: machine
      outputHash: 4007efd5af56
    paths./api/v1/tools/image/passport-photo/generate.post.summary:
      sourceHash: 029eecf0438e
      provenance: machine
      outputHash: ec1a236f8915
    paths./api/v1/tools/image/passport-photo/generate.post.description:
      sourceHash: d0edd7c634d9
      provenance: machine
      outputHash: fa2bcb229a15
    paths./api/v1/tools/image/colorize.post.summary:
      sourceHash: 34df56e96234
      provenance: machine
      outputHash: 24d383312845
    paths./api/v1/tools/image/colorize.post.description:
      sourceHash: 04d7dc54396a
      provenance: machine
      outputHash: b6a82d055a96
    paths./api/v1/tools/image/enhance-faces.post.summary:
      sourceHash: 74f27131fec9
      provenance: machine
      outputHash: 7e5139426d4b
    paths./api/v1/tools/image/enhance-faces.post.description:
      sourceHash: beec2f7e8561
      provenance: machine
      outputHash: 4ba2ddcd0c0c
    paths./api/v1/tools/image/image-to-base64.post.summary:
      sourceHash: 440737c09f0d
      provenance: machine
      outputHash: 6a2ba6bc40b7
    paths./api/v1/tools/image/image-to-base64.post.description:
      sourceHash: 02a80d960185
      provenance: machine
      outputHash: 5ada574e14a3
    paths./api/v1/tools/image/watermark-text.post.summary:
      sourceHash: dfef3d1052de
      provenance: machine
      outputHash: 317ab117228b
    paths./api/v1/tools/image/watermark-text.post.description:
      sourceHash: 158e444c0a77
      provenance: machine
      outputHash: 0e30094bef81
    paths./api/v1/tools/image/text-overlay.post.summary:
      sourceHash: 7311c0ffc340
      provenance: machine
      outputHash: 3193f055c595
    paths./api/v1/tools/image/text-overlay.post.description:
      sourceHash: e5dd74752a49
      provenance: machine
      outputHash: 82807e736603
    paths./api/v1/tools/image/replace-color.post.summary:
      sourceHash: 80e60e5fe0ed
      provenance: machine
      outputHash: ff8da5f2551e
    paths./api/v1/tools/image/replace-color.post.description:
      sourceHash: cbafc4d168c3
      provenance: machine
      outputHash: bfe0f3ad5747
    paths./api/v1/tools/image/color-blindness.post.summary:
      sourceHash: 1ab73f30893d
      provenance: machine
      outputHash: 3306d832eb71
    paths./api/v1/tools/image/color-blindness.post.description:
      sourceHash: 1e362d094237
      provenance: machine
      outputHash: 1eb201b255c5
    paths./api/v1/tools/image/gif-tools.post.summary:
      sourceHash: 985d94d31548
      provenance: machine
      outputHash: 54b02fb6775f
    paths./api/v1/tools/image/gif-tools.post.description:
      sourceHash: fe7113c6fefd
      provenance: machine
      outputHash: 2f28ca542454
    paths./api/v1/tools/image/gif-tools/info.post.summary:
      sourceHash: e864c10b29f7
      provenance: machine
      outputHash: b68ac9ed6e5e
    paths./api/v1/tools/image/gif-tools/info.post.description:
      sourceHash: aaeba6b11dbf
      provenance: machine
      outputHash: d6ddefbc877f
    paths./api/v1/tools/image/smart-crop.post.summary:
      sourceHash: 890848540d35
      provenance: machine
      outputHash: 6d4e4f19eaac
    paths./api/v1/tools/image/smart-crop.post.description:
      sourceHash: 0f00f334ba29
      provenance: machine
      outputHash: d1828702f7dc
    paths./api/v1/tools/image/vectorize.post.summary:
      sourceHash: ffa9db773ada
      provenance: machine
      outputHash: 98a8ca57d6ae
    paths./api/v1/tools/image/vectorize.post.description:
      sourceHash: 3d5de38dc3f3
      provenance: machine
      outputHash: 4cb101fddb64
    paths./api/v1/tools/image/svg-to-raster.post.summary:
      sourceHash: bccef23ad06b
      provenance: machine
      outputHash: 3ad3b4ac70ee
    paths./api/v1/tools/image/svg-to-raster.post.description:
      sourceHash: 27a0b0dfe170
      provenance: machine
      outputHash: 31fb22b934ac
    paths./api/v1/tools/image/svg-to-raster/batch.post.summary:
      sourceHash: 340743c4e68b
      provenance: machine
      outputHash: 5e63a18bb91a
    paths./api/v1/tools/image/svg-to-raster/batch.post.description:
      sourceHash: a0069d315b7e
      provenance: machine
      outputHash: e775033e9b10
    paths./api/v1/tools/image/image-to-pdf.post.summary:
      sourceHash: 966eba6e1412
      provenance: machine
      outputHash: 96b856b00c54
    paths./api/v1/tools/image/image-to-pdf.post.description:
      sourceHash: f5a0819f56f7
      provenance: machine
      outputHash: 87101572b70b
    paths./api/v1/tools/pdf/pdf-to-image.post.summary:
      sourceHash: 4c3c2ae28b1f
      provenance: machine
      outputHash: e138d3d0eb36
    paths./api/v1/tools/pdf/pdf-to-image.post.description:
      sourceHash: 4f0f5c643801
      provenance: machine
      outputHash: 43074c72b98b
    paths./api/v1/tools/pdf/pdf-to-image/info.post.summary:
      sourceHash: d9398268b597
      provenance: machine
      outputHash: ed5848a11280
    paths./api/v1/tools/pdf/pdf-to-image/info.post.description:
      sourceHash: 9ace4aaa3978
      provenance: machine
      outputHash: 6ed9dccf9367
    paths./api/v1/tools/pdf/pdf-to-image/preview.post.summary:
      sourceHash: 3bd369a401ba
      provenance: machine
      outputHash: a04e311e4db0
    paths./api/v1/tools/pdf/pdf-to-image/preview.post.description:
      sourceHash: 90db92475b9a
      provenance: machine
      outputHash: a977e68b1974
    paths./api/v1/tools/image/split.post.summary:
      sourceHash: 21b17b422f71
      provenance: machine
      outputHash: ec700f353dbe
    paths./api/v1/tools/image/split.post.description:
      sourceHash: d7d4988765b3
      provenance: machine
      outputHash: 06ea115812ed
    paths./api/v1/tools/image/bulk-rename.post.summary:
      sourceHash: 5489fe2b69b0
      provenance: machine
      outputHash: b5ef996a91a5
    paths./api/v1/tools/image/bulk-rename.post.description:
      sourceHash: 093bf3688b13
      provenance: machine
      outputHash: b4455b883da3
    paths./api/v1/tools/image/favicon.post.summary:
      sourceHash: 7a587af97797
      provenance: machine
      outputHash: bda11d3db5dd
    paths./api/v1/tools/image/favicon.post.description:
      sourceHash: 5351a1c80471
      provenance: machine
      outputHash: 0382398a89cc
    paths./api/v1/tools/image/watermark-image.post.summary:
      sourceHash: 577e5e431f77
      provenance: machine
      outputHash: 98056afa7f26
    paths./api/v1/tools/image/watermark-image.post.description:
      sourceHash: 4e103b8a7d17
      provenance: machine
      outputHash: 2b1d0e54a593
    paths./api/v1/tools/image/compose.post.summary:
      sourceHash: 06718c2a3b98
      provenance: machine
      outputHash: 1a4e426dd710
    paths./api/v1/tools/image/compose.post.description:
      sourceHash: d48d40e77edf
      provenance: machine
      outputHash: d4081e0c008e
    paths./api/v1/tools/image/meme-generator.post.summary:
      sourceHash: a4e89614a5c1
      provenance: machine
      outputHash: 383f17a5bd17
    paths./api/v1/tools/image/meme-generator.post.description:
      sourceHash: 7aa04984be62
      provenance: machine
      outputHash: 675514ef2c82
    paths./api/v1/meme-templates.get.summary:
      sourceHash: fd3392277306
      provenance: machine
      outputHash: 02172abf562c
    paths./api/v1/meme-templates.get.description:
      sourceHash: a147e348597c
      provenance: machine
      outputHash: c08d682b683a
    paths./api/v1/meme-templates/full/{filename}.get.summary:
      sourceHash: fd501704c913
      provenance: machine
      outputHash: be22bbc1540d
    paths./api/v1/meme-templates/full/{filename}.get.description:
      sourceHash: 3bb962f4402c
      provenance: machine
      outputHash: 2fb7cf9268fc
    paths./api/v1/meme-templates/thumbs/{filename}.get.summary:
      sourceHash: d2102190f5a2
      provenance: machine
      outputHash: f2ff18d13301
    paths./api/v1/meme-templates/thumbs/{filename}.get.description:
      sourceHash: 77c69805732d
      provenance: machine
      outputHash: 2d30a8f9ec63
    paths./api/v1/meme-templates/fonts/{filename}.get.summary:
      sourceHash: 3627280f2609
      provenance: machine
      outputHash: 39bc78adbc46
    paths./api/v1/meme-templates/fonts/{filename}.get.description:
      sourceHash: 717c6764f472
      provenance: machine
      outputHash: 77520ffe142e
    paths./api/v1/tools/image/compare.post.summary:
      sourceHash: 46e3612ffee1
      provenance: machine
      outputHash: 39ca1caafd2a
    paths./api/v1/tools/image/compare.post.description:
      sourceHash: ac21fee13ecc
      provenance: machine
      outputHash: 8b173727e40c
    paths./api/v1/tools/image/erase-object.post.summary:
      sourceHash: 21f13ba2930d
      provenance: machine
      outputHash: f8537d8f77d7
    paths./api/v1/tools/image/erase-object.post.description:
      sourceHash: 9d6614ed28c6
      provenance: machine
      outputHash: 349fc4fa635c
    paths./api/v1/tools/image/ai-canvas-expand.post.summary:
      sourceHash: 990aeca723ff
      provenance: machine
      outputHash: deff3f00b334
    paths./api/v1/tools/image/ai-canvas-expand.post.description:
      sourceHash: 084de536af45
      provenance: machine
      outputHash: c5e71bfa5bd0
    paths./api/v1/tools/image/collage.post.summary:
      sourceHash: 792f730c3eb6
      provenance: machine
      outputHash: 4467c8006b11
    paths./api/v1/tools/image/collage.post.description:
      sourceHash: fb0ba3d2cf44
      provenance: machine
      outputHash: f722ea0ca6de
    paths./api/v1/tools/image/stitch.post.summary:
      sourceHash: dd874f58b1aa
      provenance: machine
      outputHash: 68ede18142a3
    paths./api/v1/tools/image/stitch.post.description:
      sourceHash: 914837a5d232
      provenance: machine
      outputHash: b278c9aa3a1c
    paths./api/v1/tools/image/remove-gif-background.post.summary:
      sourceHash: a316267cf5d1
      provenance: machine
      outputHash: 7bf6d3418509
    paths./api/v1/tools/image/remove-gif-background.post.description:
      sourceHash: ef1d100f2345
      provenance: machine
      outputHash: afd06a06fde7
    paths./api/v1/tools/image/remove-background.post.summary:
      sourceHash: a8d1ab54b32c
      provenance: machine
      outputHash: 3f3e245106f1
    paths./api/v1/tools/image/remove-background.post.description:
      sourceHash: 3bb193c0a2f9
      provenance: machine
      outputHash: 89d44989cc93
    paths./api/v1/tools/image/remove-background/effects.post.summary:
      sourceHash: 2194d6947f3f
      provenance: machine
      outputHash: 36d3c3f24e01
    paths./api/v1/tools/image/remove-background/effects.post.description:
      sourceHash: 1e2ab8e3b1ae
      provenance: machine
      outputHash: 96c891b9c8d4
    paths./api/v1/tools/image/transparency-fixer.post.summary:
      sourceHash: 0cf307cacb91
      provenance: machine
      outputHash: beb68634b170
    paths./api/v1/tools/image/transparency-fixer.post.description:
      sourceHash: 67b2a3c1cb7b
      provenance: machine
      outputHash: 0216933649be
    paths./api/v1/tools/image/upscale.post.summary:
      sourceHash: ce44856d9e64
      provenance: machine
      outputHash: 2f50779f6268
    paths./api/v1/tools/image/upscale.post.description:
      sourceHash: e603db60e711
      provenance: machine
      outputHash: cf75a9c91892
    paths./api/v1/tools/image/blur-faces.post.summary:
      sourceHash: 635ee975dace
      provenance: machine
      outputHash: d8d7510e7b59
    paths./api/v1/tools/image/blur-faces.post.description:
      sourceHash: 28790ae08ac7
      provenance: machine
      outputHash: 7c82de7cddd2
    paths./api/v1/tools/image/ocr.post.summary:
      sourceHash: 0b508f19963b
      provenance: machine
      outputHash: db60973538f8
    paths./api/v1/tools/image/ocr.post.description:
      sourceHash: 7b74b30329a5
      provenance: machine
      outputHash: 8000bfdbd475
    paths./api/v1/tools/image/info.post.summary:
      sourceHash: 1823e118aed5
      provenance: machine
      outputHash: 85d004abcee6
    paths./api/v1/tools/image/info.post.description:
      sourceHash: 196d7190f16c
      provenance: machine
      outputHash: 6eeb43523890
    paths./api/v1/tools/image/color-palette.post.summary:
      sourceHash: 81a9be1a8044
      provenance: machine
      outputHash: 01e241afa337
    paths./api/v1/tools/image/color-palette.post.description:
      sourceHash: d34d5ff2ade7
      provenance: machine
      outputHash: 0dca24b658d2
    paths./api/v1/tools/image/barcode-read.post.summary:
      sourceHash: b51dcac65420
      provenance: machine
      outputHash: 49f6f0908e9b
    paths./api/v1/tools/image/barcode-read.post.description:
      sourceHash: ae952218a233
      provenance: machine
      outputHash: f82c5cd2daf7
    paths./api/v1/tools/image/find-duplicates.post.summary:
      sourceHash: bd6e5d3e4d26
      provenance: machine
      outputHash: 63751c138e3a
    paths./api/v1/tools/image/find-duplicates.post.description:
      sourceHash: 61322792b4bd
      provenance: machine
      outputHash: 53d7f5babb5b
    paths./api/v1/tools/image/qr-generate.post.summary:
      sourceHash: 799653bb4ee1
      provenance: machine
      outputHash: '382762518641'
    paths./api/v1/tools/image/qr-generate.post.description:
      sourceHash: ad4154c7b555
      provenance: machine
      outputHash: 04739721501f
    paths./api/v1/tools/image/html-to-image.post.summary:
      sourceHash: d4dcd8d52cfd
      provenance: machine
      outputHash: bc559430b526
    paths./api/v1/tools/image/html-to-image.post.description:
      sourceHash: adb3e27eb0e6
      provenance: machine
      outputHash: ff479d417589
    paths./api/v1/tools/image/strip-metadata/inspect.post.summary:
      sourceHash: 26c48e56ad62
      provenance: machine
      outputHash: 0ffb588681e7
    paths./api/v1/tools/image/strip-metadata/inspect.post.description:
      sourceHash: a6a7d5c72088
      provenance: machine
      outputHash: 722f61a80081
    paths./api/v1/tools/image/edit-metadata.post.summary:
      sourceHash: a4ce10f7b615
      provenance: machine
      outputHash: dee72cd5df28
    paths./api/v1/tools/image/edit-metadata.post.description:
      sourceHash: 70d277ec5566
      provenance: machine
      outputHash: c7ccc1e57c8f
    paths./api/v1/tools/image/edit-metadata/inspect.post.summary:
      sourceHash: a9b31ad7470f
      provenance: machine
      outputHash: 58cfb3bd6e75
    paths./api/v1/tools/image/edit-metadata/inspect.post.description:
      sourceHash: 71173fd89d89
      provenance: machine
      outputHash: 9f445fe78a3a
    paths./api/v1/tools/{section}/{toolId}/batch.post.summary:
      sourceHash: c48b2b0a9212
      provenance: machine
      outputHash: 387e097cde8b
    paths./api/v1/tools/{section}/{toolId}/batch.post.description:
      sourceHash: e2ddfe55094d
      provenance: machine
      outputHash: affd1b3e0c40
    paths./api/v1/pipeline/execute.post.summary:
      sourceHash: 6357b47fa7a9
      provenance: machine
      outputHash: e67d9621f5b1
    paths./api/v1/pipeline/execute.post.description:
      sourceHash: ca3c9eb4acf1
      provenance: machine
      outputHash: 8be49b60a46a
    paths./api/v1/pipeline/save.post.summary:
      sourceHash: 8209ec3e0c9b
      provenance: machine
      outputHash: 3ffc08b82538
    paths./api/v1/pipeline/save.post.description:
      sourceHash: a904ab4f3701
      provenance: machine
      outputHash: 5db9575dfc00
    paths./api/v1/pipeline/list.get.summary:
      sourceHash: c8d290eff4a7
      provenance: machine
      outputHash: 86c982d78f21
    paths./api/v1/pipeline/list.get.description:
      sourceHash: d11170073568
      provenance: machine
      outputHash: d1789ad22267
    paths./api/v1/pipeline/{id}.delete.summary:
      sourceHash: 0a798831c1be
      provenance: machine
      outputHash: d0fbdb4ee44a
    paths./api/v1/pipeline/{id}.delete.description:
      sourceHash: e3b43bf6d73d
      provenance: machine
      outputHash: 0cdcc767feb1
    paths./api/v1/pipeline/tools.get.summary:
      sourceHash: 79f28e6075f7
      provenance: machine
      outputHash: a23b20061125
    paths./api/v1/pipeline/tools.get.description:
      sourceHash: 6f341cc670d3
      provenance: machine
      outputHash: 0754261a4d72
    paths./api/v1/pipeline/batch.post.summary:
      sourceHash: 14abb819b562
      provenance: machine
      outputHash: f59944bf210b
    paths./api/v1/pipeline/batch.post.description:
      sourceHash: 0c7b90acb5d8
      provenance: machine
      outputHash: 78bfe9498882
    paths./api/v1/upload.post.summary:
      sourceHash: 25d0eba68bdc
      provenance: machine
      outputHash: c757a52281cc
    paths./api/v1/upload.post.description:
      sourceHash: a9e18800407e
      provenance: machine
      outputHash: 99336aa09ece
    paths./api/v1/download/{jobId}/{filename}.get.summary:
      sourceHash: 6246d8c07e4b
      provenance: machine
      outputHash: d7fb8c9ae106
    paths./api/v1/download/{jobId}/{filename}.get.description:
      sourceHash: a09d603b069c
      provenance: machine
      outputHash: 105b997a43c0
    paths./api/v1/preview.post.summary:
      sourceHash: 8266fbd3ff72
      provenance: machine
      outputHash: 68174f8abf6e
    paths./api/v1/preview.post.description:
      sourceHash: d5e04d49c594
      provenance: machine
      outputHash: 635b1ddf0587
    paths./api/v1/files.get.summary:
      sourceHash: aefb51ff8aaa
      provenance: machine
      outputHash: bd72d640b508
    paths./api/v1/files.get.description:
      sourceHash: 45f45ad625ed
      provenance: machine
      outputHash: be9c01e33526
    paths./api/v1/files.delete.summary:
      sourceHash: 179bb72a6deb
      provenance: machine
      outputHash: be4a71d1b2c7
    paths./api/v1/files.delete.description:
      sourceHash: 1bfaa5c376bb
      provenance: machine
      outputHash: 8239ac6c3704
    paths./api/v1/files/upload.post.summary:
      sourceHash: b94c597782ee
      provenance: machine
      outputHash: 885994e5c2f2
    paths./api/v1/files/upload.post.description:
      sourceHash: 8a070581c1e5
      provenance: machine
      outputHash: b891b2f22b0f
    paths./api/v1/files/save-result.post.summary:
      sourceHash: c35e320d9164
      provenance: machine
      outputHash: ff316e2a723a
    paths./api/v1/files/save-result.post.description:
      sourceHash: 3c434713031e
      provenance: machine
      outputHash: 64a8f6c34913
    paths./api/v1/files/{id}.get.summary:
      sourceHash: a09c1fb7e33a
      provenance: machine
      outputHash: 2f7476e27896
    paths./api/v1/files/{id}.get.description:
      sourceHash: 8193066e23c5
      provenance: machine
      outputHash: 63fc3b18db3c
    paths./api/v1/files/{id}/download.get.summary:
      sourceHash: c0043bd46394
      provenance: machine
      outputHash: 26f47ca5ffab
    paths./api/v1/files/{id}/download.get.description:
      sourceHash: 96df2a599e22
      provenance: machine
      outputHash: e2b08daee941
    paths./api/v1/files/{id}/thumbnail.get.summary:
      sourceHash: 07714ad0c5f8
      provenance: machine
      outputHash: 30ec42c413f4
    paths./api/v1/files/{id}/thumbnail.get.description:
      sourceHash: 9b04bbf75cba
      provenance: machine
      outputHash: 104cd432860e
    paths./api/v1/fetch-urls.post.summary:
      sourceHash: a00341e59161
      provenance: machine
      outputHash: 4d62327041ea
    paths./api/v1/fetch-urls.post.description:
      sourceHash: ac474639261c
      provenance: machine
      outputHash: 4225a4afb058
    paths./api/v1/config/locale.get.summary:
      sourceHash: 78de2255222d
      provenance: machine
      outputHash: 5f08722efe7d
    paths./api/v1/config/locale.get.description:
      sourceHash: b9967e400d09
      provenance: machine
      outputHash: efa04fa2a88b
    paths./api/auth/login.post.summary:
      sourceHash: c189840cf7e2
      provenance: machine
      outputHash: 63f5c2c2f95a
    paths./api/auth/login.post.description:
      sourceHash: 91ad9a828bf3
      provenance: machine
      outputHash: 86a30b493b4a
    paths./api/auth/logout.post.summary:
      sourceHash: 49616145514e
      provenance: machine
      outputHash: 75cd24c315d4
    paths./api/auth/logout.post.description:
      sourceHash: 0b9ae300808d
      provenance: machine
      outputHash: 495ca5dcc431
    paths./api/auth/session.get.summary:
      sourceHash: f4f7376ca1ea
      provenance: machine
      outputHash: eec0f1ec346c
    paths./api/auth/session.get.description:
      sourceHash: 2dad822c46ea
      provenance: machine
      outputHash: 45bf5a91e5c5
    paths./api/auth/change-password.post.summary:
      sourceHash: 3f9c991f63a9
      provenance: machine
      outputHash: 354d5f5b7d50
    paths./api/auth/change-password.post.description:
      sourceHash: 5190db8a04e7
      provenance: machine
      outputHash: 2a2b4b21b60f
    paths./api/auth/users.get.summary:
      sourceHash: '9029730603e0'
      provenance: machine
      outputHash: cf4220f475d2
    paths./api/auth/users.get.description:
      sourceHash: 34d5a11733bd
      provenance: machine
      outputHash: bf7e502ea3e7
    paths./api/auth/register.post.summary:
      sourceHash: f06da128c342
      provenance: machine
      outputHash: 5e4451c5086e
    paths./api/auth/register.post.description:
      sourceHash: 34d5a11733bd
      provenance: machine
      outputHash: bf7e502ea3e7
    paths./api/auth/users/{id}.put.summary:
      sourceHash: 602b8b1607b0
      provenance: machine
      outputHash: aa37c465c96c
    paths./api/auth/users/{id}.put.description:
      sourceHash: 34d5a11733bd
      provenance: machine
      outputHash: bf7e502ea3e7
    paths./api/auth/users/{id}.delete.summary:
      sourceHash: 58363fd94240
      provenance: machine
      outputHash: 1206949b85c0
    paths./api/auth/users/{id}.delete.description:
      sourceHash: 34d5a11733bd
      provenance: machine
      outputHash: bf7e502ea3e7
    paths./api/auth/users/{id}/reset-password.post.summary:
      sourceHash: aae899c2d3ad
      provenance: machine
      outputHash: 2473e7b1ab71
    paths./api/auth/users/{id}/reset-password.post.description:
      sourceHash: 34d5a11733bd
      provenance: machine
      outputHash: bf7e502ea3e7
    paths./api/v1/api-keys.get.summary:
      sourceHash: ad0527749f03
      provenance: machine
      outputHash: 995f8e057cb0
    paths./api/v1/api-keys.get.description:
      sourceHash: 470cee6220f0
      provenance: machine
      outputHash: 32c3fda0333c
    paths./api/v1/api-keys.post.summary:
      sourceHash: eb136082215a
      provenance: machine
      outputHash: 29335fbcb9a0
    paths./api/v1/api-keys.post.description:
      sourceHash: 7568aec4ab9b
      provenance: machine
      outputHash: 06bfcb15d403
    paths./api/v1/api-keys/{id}.delete.summary:
      sourceHash: 9ebde15b7f02
      provenance: machine
      outputHash: d171f8e7e55d
    paths./api/v1/api-keys/{id}.delete.description:
      sourceHash: 46a57dbc68a5
      provenance: machine
      outputHash: ad908be00a3a
    paths./api/v1/settings.get.summary:
      sourceHash: 8530e17de153
      provenance: machine
      outputHash: 924fe311b5da
    paths./api/v1/settings.get.description:
      sourceHash: 65d8ff0f5873
      provenance: machine
      outputHash: 752016209a09
    paths./api/v1/settings.put.summary:
      sourceHash: 46ed69579c39
      provenance: machine
      outputHash: 603ae7d76ad5
    paths./api/v1/settings.put.description:
      sourceHash: 887f8facdd94
      provenance: machine
      outputHash: 7987df38d257
    paths./api/v1/settings/{key}.get.summary:
      sourceHash: 186add054525
      provenance: machine
      outputHash: 633988a54360
    paths./api/v1/settings/{key}.get.description:
      sourceHash: 962db9ab06a3
      provenance: machine
      outputHash: 4d5189e0bab6
    paths./api/v1/teams.get.summary:
      sourceHash: 863546c57b78
      provenance: machine
      outputHash: 30cd603c4ade
    paths./api/v1/teams.get.description:
      sourceHash: 88ce5170c6ed
      provenance: machine
      outputHash: 1a26bc7c1d6c
    paths./api/v1/teams.post.summary:
      sourceHash: 00766ad4b096
      provenance: machine
      outputHash: 4d5298296f0c
    paths./api/v1/teams.post.description:
      sourceHash: 88ce5170c6ed
      provenance: machine
      outputHash: 1a26bc7c1d6c
    paths./api/v1/teams/{id}.put.summary:
      sourceHash: c5dd33827a5b
      provenance: machine
      outputHash: feeb96b36ff3
    paths./api/v1/teams/{id}.put.description:
      sourceHash: 88ce5170c6ed
      provenance: machine
      outputHash: 1a26bc7c1d6c
    paths./api/v1/teams/{id}.delete.summary:
      sourceHash: b51488367ba6
      provenance: machine
      outputHash: b9ebe6d22b17
    paths./api/v1/teams/{id}.delete.description:
      sourceHash: 88ce5170c6ed
      provenance: machine
      outputHash: 1a26bc7c1d6c
    paths./api/v1/config/auth.get.summary:
      sourceHash: 13189298880a
      provenance: machine
      outputHash: 5d5bbebf878a
    paths./api/v1/config/auth.get.description:
      sourceHash: 404909083a24
      provenance: machine
      outputHash: c1d756e78f63
    paths./api/v1/jobs/{jobId}/progress.get.summary:
      sourceHash: bcf19846aa43
      provenance: machine
      outputHash: bc22ee48ca3f
    paths./api/v1/jobs/{jobId}/progress.get.description:
      sourceHash: 2df913c75d85
      provenance: human
      outputHash: 58704fa8f50b
    paths./api/v1/admin/health.get.summary:
      sourceHash: 92f8575c9c94
      provenance: machine
      outputHash: 8e6a2d417ff7
    paths./api/v1/admin/health.get.description:
      sourceHash: 4abbba893ea4
      provenance: machine
      outputHash: f7adc6981b8a
    paths./api/v1/config/analytics.get.summary:
      sourceHash: b7fdcf112830
      provenance: machine
      outputHash: cb3ea7302b8a
    paths./api/v1/config/analytics.get.description:
      sourceHash: 2447c239373f
      provenance: machine
      outputHash: 95b86cdfa3ef
    paths./api/v1/features.get.summary:
      sourceHash: e45ee3163f96
      provenance: machine
      outputHash: e7b6f02d6f12
    paths./api/v1/features.get.description:
      sourceHash: aa0b433ca9a9
      provenance: machine
      outputHash: 2f6b6bdf6007
    paths./api/v1/admin/features/{bundleId}/install.post.summary:
      sourceHash: bebc6f7ea7c3
      provenance: machine
      outputHash: 0255181c7bcb
    paths./api/v1/admin/features/{bundleId}/install.post.description:
      sourceHash: 6051af19be7e
      provenance: machine
      outputHash: 7e1711dc5628
    paths./api/v1/admin/features/{bundleId}/uninstall.post.summary:
      sourceHash: 160eaf179d4f
      provenance: machine
      outputHash: aa3aa71fc307
    paths./api/v1/admin/features/{bundleId}/uninstall.post.description:
      sourceHash: 35bdb459a1a7
      provenance: machine
      outputHash: 8675effbdc09
    paths./api/v1/admin/features/disk-usage.get.summary:
      sourceHash: 54dc8991b9a7
      provenance: machine
      outputHash: 4a1af4e3b69a
    paths./api/v1/admin/features/disk-usage.get.description:
      sourceHash: a78fac026ea3
      provenance: machine
      outputHash: cd9ff35b4e21
    paths./api/v1/audit-log.get.summary:
      sourceHash: 05536545727c
      provenance: machine
      outputHash: 1d9b5fb85faa
    paths./api/v1/audit-log.get.description:
      sourceHash: b7950dcf7e49
      provenance: machine
      outputHash: a8d1f8f1292f
    paths./api/v1/roles.get.summary:
      sourceHash: 833a75e86b93
      provenance: machine
      outputHash: 5466ec29a7f3
    paths./api/v1/roles.get.description:
      sourceHash: 9ccdec8630d3
      provenance: machine
      outputHash: c88c4d882a11
    paths./api/v1/roles.post.summary:
      sourceHash: 6dd06b2529d4
      provenance: machine
      outputHash: 10b7cc698973
    paths./api/v1/roles.post.description:
      sourceHash: d740216038e7
      provenance: machine
      outputHash: 45a580f516a0
    paths./api/v1/roles/{id}.put.summary:
      sourceHash: 6c0cda1bc967
      provenance: machine
      outputHash: 298d4b187113
    paths./api/v1/roles/{id}.put.description:
      sourceHash: e06c42a9c3ed
      provenance: machine
      outputHash: 9fd495d86e41
    paths./api/v1/roles/{id}.delete.summary:
      sourceHash: d5a3ab01055a
      provenance: machine
      outputHash: d6beacf82161
    paths./api/v1/roles/{id}.delete.description:
      sourceHash: b4c1c8c0c9e0
      provenance: machine
      outputHash: e549ddce5f92
    paths./api/v1/tools/video/convert-video.post.summary:
      sourceHash: b4defe1d6906
      provenance: machine
      outputHash: 2d5255386712
    paths./api/v1/tools/video/convert-video.post.description:
      sourceHash: 00ab19fbffe4
      provenance: machine
      outputHash: 45257847f0f3
    paths./api/v1/tools/video/compress-video.post.summary:
      sourceHash: 8af3d4b27a4b
      provenance: machine
      outputHash: 67e2c70d5ced
    paths./api/v1/tools/video/compress-video.post.description:
      sourceHash: 7f517e885483
      provenance: machine
      outputHash: 5000cb6af413
    paths./api/v1/tools/video/trim-video.post.summary:
      sourceHash: af6ed946e090
      provenance: machine
      outputHash: e85122a0d9c4
    paths./api/v1/tools/video/trim-video.post.description:
      sourceHash: c01dd24a19dd
      provenance: machine
      outputHash: 8cdf54e99dbd
    paths./api/v1/tools/video/mute-video.post.summary:
      sourceHash: 48ed61cb4802
      provenance: machine
      outputHash: d690129f8f81
    paths./api/v1/tools/video/mute-video.post.description:
      sourceHash: 76898fc22f53
      provenance: machine
      outputHash: a04b9f6cdea5
    paths./api/v1/tools/video/video-to-gif.post.summary:
      sourceHash: 4abd5ab74c35
      provenance: machine
      outputHash: 17447d4759da
    paths./api/v1/tools/video/video-to-gif.post.description:
      sourceHash: d2d195459149
      provenance: machine
      outputHash: bea00ade9ab6
    paths./api/v1/tools/video/resize-video.post.summary:
      sourceHash: 99fde7cc129d
      provenance: machine
      outputHash: 06f69979f175
    paths./api/v1/tools/video/resize-video.post.description:
      sourceHash: ffe090ccf55b
      provenance: machine
      outputHash: fc8ba291a31c
    paths./api/v1/tools/video/crop-video.post.summary:
      sourceHash: f042b21b17c0
      provenance: machine
      outputHash: e85122a0d9c4
    paths./api/v1/tools/video/crop-video.post.description:
      sourceHash: a70528f0d600
      provenance: machine
      outputHash: 42a7c7bc8297
    paths./api/v1/tools/video/rotate-video.post.summary:
      sourceHash: a1d5d2488fd5
      provenance: machine
      outputHash: e47504fb73dd
    paths./api/v1/tools/video/rotate-video.post.description:
      sourceHash: 0858085b737b
      provenance: machine
      outputHash: 50dad0cfde75
    paths./api/v1/tools/video/change-fps.post.summary:
      sourceHash: bc48dbea9413
      provenance: machine
      outputHash: 95549246873b
    paths./api/v1/tools/video/change-fps.post.description:
      sourceHash: 20c9ae3f7c23
      provenance: machine
      outputHash: de62ca22fe6e
    paths./api/v1/tools/video/video-color.post.summary:
      sourceHash: 6005cc99c426
      provenance: machine
      outputHash: 2d7857777689
    paths./api/v1/tools/video/video-color.post.description:
      sourceHash: f8ce7dae4f04
      provenance: machine
      outputHash: 5ee7fa75d258
    paths./api/v1/tools/video/video-speed.post.summary:
      sourceHash: 4439901f7d61
      provenance: machine
      outputHash: bf175d43c217
    paths./api/v1/tools/video/video-speed.post.description:
      sourceHash: 7a4b64e60829
      provenance: machine
      outputHash: b464fa69b613
    paths./api/v1/tools/video/reverse-video.post.summary:
      sourceHash: 4f0d7b5ea9ae
      provenance: machine
      outputHash: 1e6c15184f7b
    paths./api/v1/tools/video/reverse-video.post.description:
      sourceHash: 77abcb48d823
      provenance: machine
      outputHash: c43a796f0ebe
    paths./api/v1/tools/video/video-loudnorm.post.summary:
      sourceHash: f5298327c322
      provenance: machine
      outputHash: ac49d599b324
    paths./api/v1/tools/video/video-loudnorm.post.description:
      sourceHash: 019f6147f722
      provenance: machine
      outputHash: be9d5c3cb1ec
    paths./api/v1/tools/video/aspect-pad.post.summary:
      sourceHash: aa7c02eeacf1
      provenance: machine
      outputHash: 248c0255d1d6
    paths./api/v1/tools/video/aspect-pad.post.description:
      sourceHash: 288b33cc62de
      provenance: machine
      outputHash: de0464ddb4a1
    paths./api/v1/tools/video/blur-pad.post.summary:
      sourceHash: 84af9c217d52
      provenance: machine
      outputHash: 36b9513be953
    paths./api/v1/tools/video/blur-pad.post.description:
      sourceHash: d49a3fab5e02
      provenance: machine
      outputHash: c5ebf8a5a3d5
    paths./api/v1/tools/video/watermark-video.post.summary:
      sourceHash: bbbe197c46c5
      provenance: machine
      outputHash: 4ee7da38cf60
    paths./api/v1/tools/video/watermark-video.post.description:
      sourceHash: d914078985d7
      provenance: machine
      outputHash: ce482c8d8427
    paths./api/v1/tools/video/stabilize-video.post.summary:
      sourceHash: f76615a74aac
      provenance: machine
      outputHash: 2ad30df97535
    paths./api/v1/tools/video/stabilize-video.post.description:
      sourceHash: 42e5e8a954ae
      provenance: machine
      outputHash: 8fdb4957c3c2
    paths./api/v1/tools/video/gif-to-video.post.summary:
      sourceHash: 766d5d54badb
      provenance: machine
      outputHash: 54bdea727d1f
    paths./api/v1/tools/video/gif-to-video.post.description:
      sourceHash: 22a13cbfe672
      provenance: machine
      outputHash: d3ff46c0adf1
    paths./api/v1/tools/video/video-to-webp.post.summary:
      sourceHash: 0c4028ecac61
      provenance: machine
      outputHash: feca009dae13
    paths./api/v1/tools/video/video-to-webp.post.description:
      sourceHash: 48fa0306cbe8
      provenance: machine
      outputHash: cd5155221ddb
    paths./api/v1/tools/video/video-to-frames.post.summary:
      sourceHash: 8941a5926866
      provenance: machine
      outputHash: f357e4eeb690
    paths./api/v1/tools/video/video-to-frames.post.description:
      sourceHash: 907e19fcf6a2
      provenance: machine
      outputHash: 266ee6f202de
    paths./api/v1/tools/video/merge-videos.post.summary:
      sourceHash: afbd2e62e1dd
      provenance: machine
      outputHash: 02ceddaa2c11
    paths./api/v1/tools/video/merge-videos.post.description:
      sourceHash: 30e77c05314f
      provenance: machine
      outputHash: b89436518226
    paths./api/v1/tools/video/replace-audio.post.summary:
      sourceHash: 0cd096774105
      provenance: machine
      outputHash: 1a20ad018842
    paths./api/v1/tools/video/replace-audio.post.description:
      sourceHash: f9aef92c2ad2
      provenance: machine
      outputHash: 7108040005ec
    paths./api/v1/tools/video/burn-subtitles.post.summary:
      sourceHash: 53e23068613b
      provenance: machine
      outputHash: c62a94d2e0db
    paths./api/v1/tools/video/burn-subtitles.post.description:
      sourceHash: a501f3253970
      provenance: machine
      outputHash: ff55bd55eaa2
    paths./api/v1/tools/video/embed-subtitles.post.summary:
      sourceHash: 55a0c6104e9e
      provenance: machine
      outputHash: 73402e00eaa7
    paths./api/v1/tools/video/embed-subtitles.post.description:
      sourceHash: 9ed219a50b61
      provenance: machine
      outputHash: 5790c0f0829d
    paths./api/v1/tools/video/extract-subtitles.post.summary:
      sourceHash: 0ae3dd9f7012
      provenance: machine
      outputHash: ed96deaea676
    paths./api/v1/tools/video/extract-subtitles.post.description:
      sourceHash: 4daf70632777
      provenance: machine
      outputHash: f7ae48142f53
    paths./api/v1/tools/video/images-to-video.post.summary:
      sourceHash: feb4b6d19902
      provenance: machine
      outputHash: 4ad568672acf
    paths./api/v1/tools/video/images-to-video.post.description:
      sourceHash: c7ec30bbfec8
      provenance: machine
      outputHash: 1733b96d1652
    paths./api/v1/tools/video/video-metadata.post.summary:
      sourceHash: f2902a6112ff
      provenance: machine
      outputHash: 88e28546b787
    paths./api/v1/tools/video/video-metadata.post.description:
      sourceHash: 25b0eb0d1f64
      provenance: machine
      outputHash: 1b31d1984ee9
    paths./api/v1/tools/video/auto-subtitles.post.summary:
      sourceHash: 61b5c42d8cd0
      provenance: machine
      outputHash: 41d1031adf6d
    paths./api/v1/tools/video/auto-subtitles.post.description:
      sourceHash: fe628b19b3dc
      provenance: machine
      outputHash: e02eb2064ace
    paths./api/v1/tools/video/extract-audio.post.summary:
      sourceHash: ad7f2c51f215
      provenance: machine
      outputHash: 5900703b0b79
    paths./api/v1/tools/video/extract-audio.post.description:
      sourceHash: 97425b1da99f
      provenance: machine
      outputHash: b7e65d2e960a
    paths./api/v1/tools/audio/convert-audio.post.summary:
      sourceHash: 295e4a5939e1
      provenance: machine
      outputHash: 8219cc53e04e
    paths./api/v1/tools/audio/convert-audio.post.description:
      sourceHash: db7c32326757
      provenance: machine
      outputHash: 9c73570d874a
    paths./api/v1/tools/audio/trim-audio.post.summary:
      sourceHash: 6fdc83d79136
      provenance: machine
      outputHash: 33837930bcee
    paths./api/v1/tools/audio/trim-audio.post.description:
      sourceHash: 1150da04e398
      provenance: machine
      outputHash: 24173c1866f5
    paths./api/v1/tools/audio/volume-adjust.post.summary:
      sourceHash: 399e64735825
      provenance: machine
      outputHash: a1b9f9613ef2
    paths./api/v1/tools/audio/volume-adjust.post.description:
      sourceHash: 18308a469600
      provenance: machine
      outputHash: 6a6e2c4ce8ca
    paths./api/v1/tools/audio/normalize-audio.post.summary:
      sourceHash: f5298327c322
      provenance: machine
      outputHash: ac49d599b324
    paths./api/v1/tools/audio/normalize-audio.post.description:
      sourceHash: d9993db45ccb
      provenance: machine
      outputHash: 23f0dc066f10
    paths./api/v1/tools/audio/fade-audio.post.summary:
      sourceHash: fc7e035a63d5
      provenance: machine
      outputHash: 1ff4aa431b78
    paths./api/v1/tools/audio/fade-audio.post.description:
      sourceHash: a85ba1a120e5
      provenance: machine
      outputHash: b65358213161
    paths./api/v1/tools/audio/reverse-audio.post.summary:
      sourceHash: b2eddae3a717
      provenance: machine
      outputHash: 473e2028450e
    paths./api/v1/tools/audio/reverse-audio.post.description:
      sourceHash: 218f9b33f5eb
      provenance: machine
      outputHash: 244fd65d6955
    paths./api/v1/tools/audio/audio-speed.post.summary:
      sourceHash: aee2a47692bd
      provenance: machine
      outputHash: 2dfc9ce6becc
    paths./api/v1/tools/audio/audio-speed.post.description:
      sourceHash: 51fe7745adbd
      provenance: machine
      outputHash: a06181a7f06f
    paths./api/v1/tools/audio/pitch-shift.post.summary:
      sourceHash: b26d4482b076
      provenance: machine
      outputHash: 4877f0bc2aac
    paths./api/v1/tools/audio/pitch-shift.post.description:
      sourceHash: 04cf48eca03e
      provenance: machine
      outputHash: b8c2ca290b49
    paths./api/v1/tools/audio/audio-channels.post.summary:
      sourceHash: 409c74651939
      provenance: machine
      outputHash: 1d985bcac25f
    paths./api/v1/tools/audio/audio-channels.post.description:
      sourceHash: 0974271a6730
      provenance: machine
      outputHash: d507b82689a4
    paths./api/v1/tools/audio/silence-removal.post.summary:
      sourceHash: dc27812cd572
      provenance: machine
      outputHash: 334308d37fc0
    paths./api/v1/tools/audio/silence-removal.post.description:
      sourceHash: 29d4e890a62d
      provenance: machine
      outputHash: fc9b9ce3d2e6
    paths./api/v1/tools/audio/noise-reduction.post.summary:
      sourceHash: 0bcf30a006cb
      provenance: machine
      outputHash: 72bbc88e1738
    paths./api/v1/tools/audio/noise-reduction.post.description:
      sourceHash: 032c91b97641
      provenance: machine
      outputHash: 83e068ce0a77
    paths./api/v1/tools/audio/merge-audio.post.summary:
      sourceHash: 780965910dff
      provenance: machine
      outputHash: 6e2dc99fa199
    paths./api/v1/tools/audio/merge-audio.post.description:
      sourceHash: 1397edf49056
      provenance: machine
      outputHash: f96b4de4deac
    paths./api/v1/tools/audio/split-audio.post.summary:
      sourceHash: e59c513c0ba2
      provenance: machine
      outputHash: 025e36913cdd
    paths./api/v1/tools/audio/split-audio.post.description:
      sourceHash: 763e0458c032
      provenance: machine
      outputHash: c1cebb48b073
    paths./api/v1/tools/audio/ringtone-maker.post.summary:
      sourceHash: f0e142322ff9
      provenance: machine
      outputHash: b46842f92791
    paths./api/v1/tools/audio/ringtone-maker.post.description:
      sourceHash: c301dcbbedc1
      provenance: machine
      outputHash: 6dfb2a589f6b
    paths./api/v1/tools/audio/waveform-image.post.summary:
      sourceHash: 0e373ca45197
      provenance: machine
      outputHash: 3361642cbc4a
    paths./api/v1/tools/audio/waveform-image.post.description:
      sourceHash: 70f63b279b5d
      provenance: machine
      outputHash: 3966d52ed0f5
    paths./api/v1/tools/audio/audio-metadata.post.summary:
      sourceHash: 55d1a5a0e65d
      provenance: machine
      outputHash: d169945977a6
    paths./api/v1/tools/audio/audio-metadata.post.description:
      sourceHash: 5c799135421c
      provenance: machine
      outputHash: c66b85392a28
    paths./api/v1/tools/audio/transcribe-audio.post.summary:
      sourceHash: a723c84c3ab0
      provenance: machine
      outputHash: 6ca4fbaa4012
    paths./api/v1/tools/audio/transcribe-audio.post.description:
      sourceHash: 2967ad141d14
      provenance: machine
      outputHash: cf2f798a9cb0
    paths./api/v1/tools/pdf/merge-pdf.post.summary:
      sourceHash: 43d1539b7596
      provenance: machine
      outputHash: d581c4ca8e2b
    paths./api/v1/tools/pdf/merge-pdf.post.description:
      sourceHash: 728388cddb8a
      provenance: machine
      outputHash: ea986525a6cd
    paths./api/v1/tools/pdf/split-pdf.post.summary:
      sourceHash: 4f1b846cb144
      provenance: machine
      outputHash: ae5efede3f32
    paths./api/v1/tools/pdf/split-pdf.post.description:
      sourceHash: fbe9d9c35006
      provenance: machine
      outputHash: e096c6f5d703
    paths./api/v1/tools/pdf/compress-pdf.post.summary:
      sourceHash: 932c3c48a4de
      provenance: machine
      outputHash: 49fe4d86ea81
    paths./api/v1/tools/pdf/compress-pdf.post.description:
      sourceHash: 89f23fb6b3c8
      provenance: machine
      outputHash: 69572a6854f6
    paths./api/v1/tools/pdf/rotate-pdf.post.summary:
      sourceHash: 1eccabbce838
      provenance: machine
      outputHash: 6c29903a0670
    paths./api/v1/tools/pdf/rotate-pdf.post.description:
      sourceHash: 7c561cc7ed48
      provenance: machine
      outputHash: 294cba940386
    paths./api/v1/tools/pdf/extract-pages.post.summary:
      sourceHash: 0247b9f3e6cf
      provenance: machine
      outputHash: ad2c4bfe280e
    paths./api/v1/tools/pdf/extract-pages.post.description:
      sourceHash: c07eea0c017f
      provenance: machine
      outputHash: 51040622d6e1
    paths./api/v1/tools/pdf/remove-pages.post.summary:
      sourceHash: 670a5909f883
      provenance: machine
      outputHash: 479e944b5a92
    paths./api/v1/tools/pdf/remove-pages.post.description:
      sourceHash: 6231b16e7bea
      provenance: machine
      outputHash: 7298c2e419df
    paths./api/v1/tools/pdf/organize-pdf.post.summary:
      sourceHash: d5a641ebd93a
      provenance: machine
      outputHash: c6c6215e927a
    paths./api/v1/tools/pdf/organize-pdf.post.description:
      sourceHash: ac32f48665e7
      provenance: machine
      outputHash: 0e24ffbd329d
    paths./api/v1/tools/pdf/protect-pdf.post.summary:
      sourceHash: dae6c607e963
      provenance: machine
      outputHash: b001c817e2a5
    paths./api/v1/tools/pdf/protect-pdf.post.description:
      sourceHash: 600a3be47970
      provenance: machine
      outputHash: c3755ff22e32
    paths./api/v1/tools/pdf/unlock-pdf.post.summary:
      sourceHash: 893947d82ded
      provenance: machine
      outputHash: a778b6928d62
    paths./api/v1/tools/pdf/unlock-pdf.post.description:
      sourceHash: 1d199055f386
      provenance: machine
      outputHash: da80d95bc48c
    paths./api/v1/tools/pdf/repair-pdf.post.summary:
      sourceHash: 43faa052d4cf
      provenance: machine
      outputHash: 9fc004a672d3
    paths./api/v1/tools/pdf/repair-pdf.post.description:
      sourceHash: 77f71d364df4
      provenance: machine
      outputHash: 7d11351a26aa
    paths./api/v1/tools/pdf/linearize-pdf.post.summary:
      sourceHash: 75fa522e3fe3
      provenance: machine
      outputHash: 7687bf8ba5a7
    paths./api/v1/tools/pdf/linearize-pdf.post.description:
      sourceHash: 200f45aa4a91
      provenance: machine
      outputHash: e64ef7e3e23b
    paths./api/v1/tools/pdf/grayscale-pdf.post.summary:
      sourceHash: 58b0007c3425
      provenance: machine
      outputHash: 4e36aded8d9f
    paths./api/v1/tools/pdf/grayscale-pdf.post.description:
      sourceHash: 7b7e2b4857e6
      provenance: machine
      outputHash: 54a55a4b6630
    paths./api/v1/tools/pdf/pdfa-convert.post.summary:
      sourceHash: 1a5ec4af8f0e
      provenance: machine
      outputHash: 035e09597a02
    paths./api/v1/tools/pdf/pdfa-convert.post.description:
      sourceHash: 3c8ad47aa904
      provenance: machine
      outputHash: b231bd0e6ccc
    paths./api/v1/tools/pdf/crop-pdf.post.summary:
      sourceHash: dc4bdbce825f
      provenance: machine
      outputHash: 95b9c9d14ebf
    paths./api/v1/tools/pdf/crop-pdf.post.description:
      sourceHash: 94eb9575aecc
      provenance: machine
      outputHash: 354001d22ff7
    paths./api/v1/tools/pdf/nup-pdf.post.summary:
      sourceHash: 779949cba319
      provenance: machine
      outputHash: 779949cba319
    paths./api/v1/tools/pdf/nup-pdf.post.description:
      sourceHash: 5e2a264dafb0
      provenance: machine
      outputHash: bb641a84c016
    paths./api/v1/tools/pdf/booklet-pdf.post.summary:
      sourceHash: a1fdff0bc065
      provenance: machine
      outputHash: fab3ba632ba3
    paths./api/v1/tools/pdf/booklet-pdf.post.description:
      sourceHash: 0ce5f88843c3
      provenance: machine
      outputHash: f5f005f97797
    paths./api/v1/tools/pdf/watermark-pdf.post.summary:
      sourceHash: 903ce98c712a
      provenance: machine
      outputHash: 2a97900917c6
    paths./api/v1/tools/pdf/watermark-pdf.post.description:
      sourceHash: c15a57a06624
      provenance: machine
      outputHash: 32cedc5d686d
    paths./api/v1/tools/pdf/pdf-page-numbers.post.summary:
      sourceHash: 44eb045097be
      provenance: machine
      outputHash: e119fcf4bcf1
    paths./api/v1/tools/pdf/pdf-page-numbers.post.description:
      sourceHash: 30ce839cac96
      provenance: machine
      outputHash: c1146d25e222
    paths./api/v1/tools/files/convert-document.post.summary:
      sourceHash: 51556213858a
      provenance: machine
      outputHash: c4bc56500af4
    paths./api/v1/tools/files/convert-document.post.description:
      sourceHash: 8a61bf0f3459
      provenance: machine
      outputHash: b9b25b73bb7b
    paths./api/v1/tools/files/convert-presentation.post.summary:
      sourceHash: 5e2293e30835
      provenance: machine
      outputHash: 255db9fa1fb1
    paths./api/v1/tools/files/convert-presentation.post.description:
      sourceHash: c24f1d70d630
      provenance: machine
      outputHash: b834268a218d
    paths./api/v1/tools/files/convert-spreadsheet.post.summary:
      sourceHash: 670b7aa81c49
      provenance: machine
      outputHash: ce14ec742f8a
    paths./api/v1/tools/files/convert-spreadsheet.post.description:
      sourceHash: 1c254c7db8bc
      provenance: machine
      outputHash: 836f4d613688
    paths./api/v1/tools/files/excel-to-pdf.post.summary:
      sourceHash: edba3432df77
      provenance: machine
      outputHash: 2bf4c7b9cbe0
    paths./api/v1/tools/files/excel-to-pdf.post.description:
      sourceHash: c2e4aaa88cb7
      provenance: machine
      outputHash: 0ed25a2b2d62
    paths./api/v1/tools/files/word-to-pdf.post.summary:
      sourceHash: e6cb1150e762
      provenance: machine
      outputHash: f016ec931a72
    paths./api/v1/tools/files/word-to-pdf.post.description:
      sourceHash: eca3c027bc10
      provenance: machine
      outputHash: cae02d94ee99
    paths./api/v1/tools/files/powerpoint-to-pdf.post.summary:
      sourceHash: 799dd29fe97a
      provenance: machine
      outputHash: b3b2154d149a
    paths./api/v1/tools/files/powerpoint-to-pdf.post.description:
      sourceHash: cdc18f172040
      provenance: machine
      outputHash: ada7ec3835a3
    paths./api/v1/tools/files/html-to-pdf.post.summary:
      sourceHash: e03b0e6dda95
      provenance: machine
      outputHash: 7f3034a8c84d
    paths./api/v1/tools/files/html-to-pdf.post.description:
      sourceHash: e189b21c7bed
      provenance: machine
      outputHash: 4c326d540055
    paths./api/v1/tools/files/markdown-to-docx.post.summary:
      sourceHash: 1162393274fa
      provenance: machine
      outputHash: c4c85553b6b3
    paths./api/v1/tools/files/markdown-to-docx.post.description:
      sourceHash: 2bc0fd718613
      provenance: machine
      outputHash: c99de81370b1
    paths./api/v1/tools/files/markdown-to-html.post.summary:
      sourceHash: e5e6e736b649
      provenance: machine
      outputHash: 31672389c5ae
    paths./api/v1/tools/files/markdown-to-html.post.description:
      sourceHash: 4f6f706c371e
      provenance: machine
      outputHash: fae3dc3fa8e6
    paths./api/v1/tools/files/markdown-to-pdf.post.summary:
      sourceHash: 82712b1fc2b5
      provenance: machine
      outputHash: 8b08f2c6d2d6
    paths./api/v1/tools/files/markdown-to-pdf.post.description:
      sourceHash: f588e3d2847d
      provenance: machine
      outputHash: c5d82e5d53a5
    paths./api/v1/tools/files/epub-convert.post.summary:
      sourceHash: f03cffcd951f
      provenance: machine
      outputHash: 8d020d99d6d1
    paths./api/v1/tools/files/epub-convert.post.description:
      sourceHash: 5a42072194d3
      provenance: machine
      outputHash: ff4b33403c36
    paths./api/v1/tools/files/to-epub.post.summary:
      sourceHash: 542d3fb468be
      provenance: machine
      outputHash: bc9de85969b9
    paths./api/v1/tools/files/to-epub.post.description:
      sourceHash: 1e0b8c39ef9d
      provenance: machine
      outputHash: 2d29651972ee
    paths./api/v1/tools/pdf/flatten-pdf.post.summary:
      sourceHash: 86c838d2d497
      provenance: machine
      outputHash: 4ec25f459148
    paths./api/v1/tools/pdf/flatten-pdf.post.description:
      sourceHash: cb0b0bdeaaa5
      provenance: machine
      outputHash: 4a50e5439cd6
    paths./api/v1/tools/pdf/redact-pdf.post.summary:
      sourceHash: 1437d196213c
      provenance: machine
      outputHash: 04ef87f2bf14
    paths./api/v1/tools/pdf/redact-pdf.post.description:
      sourceHash: c2652c3da04a
      provenance: machine
      outputHash: 3ea8c8843d8e
    paths./api/v1/tools/pdf/pdf-to-text.post.summary:
      sourceHash: f1a719f99a31
      provenance: machine
      outputHash: d516aeeb52c4
    paths./api/v1/tools/pdf/pdf-to-text.post.description:
      sourceHash: f107a4082454
      provenance: machine
      outputHash: a2deb54c35e9
    paths./api/v1/tools/pdf/pdf-to-word.post.summary:
      sourceHash: 283553b063ad
      provenance: machine
      outputHash: f43b92617ef5
    paths./api/v1/tools/pdf/pdf-to-word.post.description:
      sourceHash: 324b6f3d35f7
      provenance: machine
      outputHash: bd59a82de8bb
    paths./api/v1/tools/pdf/pdf-metadata.post.summary:
      sourceHash: 7aa0fbdbeac6
      provenance: machine
      outputHash: c26d952313af
    paths./api/v1/tools/pdf/pdf-metadata.post.description:
      sourceHash: 4267623afc19
      provenance: machine
      outputHash: abf48c5ae966
    paths./api/v1/tools/pdf/ocr-pdf.post.summary:
      sourceHash: 2d9dfe74b2d1
      provenance: machine
      outputHash: 21744922783a
    paths./api/v1/tools/pdf/ocr-pdf.post.description:
      sourceHash: fbed951c39df
      provenance: machine
      outputHash: 979054a618bb
    paths./api/v1/tools/files/chart-maker.post.summary:
      sourceHash: dbcca76ccbf5
      provenance: machine
      outputHash: 5573cec9b0c3
    paths./api/v1/tools/files/chart-maker.post.description:
      sourceHash: e4adb80dab58
      provenance: machine
      outputHash: db5b4607dd63
    paths./api/v1/tools/files/csv-excel.post.summary:
      sourceHash: c53386a6d40d
      provenance: machine
      outputHash: 571fa3b255a8
    paths./api/v1/tools/files/csv-excel.post.description:
      sourceHash: b567a5eb0ee5
      provenance: machine
      outputHash: 2ad0669f8f8e
    paths./api/v1/tools/files/csv-json.post.summary:
      sourceHash: 3ed0542f5ca4
      provenance: machine
      outputHash: c8f36c825815
    paths./api/v1/tools/files/csv-json.post.description:
      sourceHash: 6ed58f7ca5ac
      provenance: machine
      outputHash: fa94bf4b1cec
    paths./api/v1/tools/files/json-xml.post.summary:
      sourceHash: 0e374572749d
      provenance: machine
      outputHash: 2d915d3bbfc5
    paths./api/v1/tools/files/json-xml.post.description:
      sourceHash: bbb56d23a274
      provenance: machine
      outputHash: c94e38a65ce5
    paths./api/v1/tools/files/split-csv.post.summary:
      sourceHash: 06c1cfb6cb2c
      provenance: machine
      outputHash: 47198cc936ec
    paths./api/v1/tools/files/split-csv.post.description:
      sourceHash: 1181658ae239
      provenance: machine
      outputHash: debcbc9960a9
    paths./api/v1/tools/files/merge-csvs.post.summary:
      sourceHash: 0042c1153e55
      provenance: machine
      outputHash: 3b4fe83af7ab
    paths./api/v1/tools/files/merge-csvs.post.description:
      sourceHash: 4956c801900a
      provenance: machine
      outputHash: 004be1d70533
    paths./api/v1/tools/files/yaml-json.post.summary:
      sourceHash: c73b92121dff
      provenance: machine
      outputHash: c73b92121dff
    paths./api/v1/tools/files/yaml-json.post.description:
      sourceHash: a7fa089ac00a
      provenance: machine
      outputHash: ba5c3ade0ace
    paths./api/v1/tools/files/xml-to-csv.post.summary:
      sourceHash: 909195e97a63
      provenance: machine
      outputHash: 871260f27b15
    paths./api/v1/tools/files/xml-to-csv.post.description:
      sourceHash: 156ea77c8a62
      provenance: machine
      outputHash: 3d8e572e9721
    paths./api/v1/tools/files/create-zip.post.summary:
      sourceHash: 8463eb4799ce
      provenance: machine
      outputHash: 56f7822ed65e
    paths./api/v1/tools/files/create-zip.post.description:
      sourceHash: 26c97508b498
      provenance: machine
      outputHash: c427289c8fc2
    paths./api/v1/tools/files/extract-zip.post.summary:
      sourceHash: aa7dad519142
      provenance: machine
      outputHash: bbdd75bd1449
    paths./api/v1/tools/files/extract-zip.post.description:
      sourceHash: 5b025b124d8b
      provenance: machine
      outputHash: b394bd4bd161
    paths./api/v1/tools/image/background-replace.post.summary:
      sourceHash: 971cacdad5ce
      provenance: machine
      outputHash: cf31a621790c
    paths./api/v1/tools/image/background-replace.post.description:
      sourceHash: 5f7a0c0dbbc7
      provenance: machine
      outputHash: 69071e3bbe75
    paths./api/v1/tools/image/blur-background.post.summary:
      sourceHash: 2383b0d9c928
      provenance: machine
      outputHash: 9b189a838778
    paths./api/v1/tools/image/blur-background.post.description:
      sourceHash: f3afcae74767
      provenance: machine
      outputHash: 21ffb4423635
    paths./api/v1/tools/image/barcode-generate.post.summary:
      sourceHash: 4717ee594681
      provenance: machine
      outputHash: cbc78a30ec25
    paths./api/v1/tools/image/barcode-generate.post.description:
      sourceHash: e174f8923d5d
      provenance: machine
      outputHash: cadb6acbfe3c
    paths./api/v1/tools/image/circle-crop.post.summary:
      sourceHash: d76473270976
      provenance: machine
      outputHash: eddc9d066433
    paths./api/v1/tools/image/circle-crop.post.description:
      sourceHash: 3e4d4819d594
      provenance: machine
      outputHash: 7afc818d1095
    paths./api/v1/tools/image/duotone.post.summary:
      sourceHash: 27e78e7e8cca
      provenance: machine
      outputHash: a89a2da2dfad
    paths./api/v1/tools/image/duotone.post.description:
      sourceHash: f3d5b592526a
      provenance: machine
      outputHash: 477c2dd94f34
    paths./api/v1/tools/image/image-pad.post.summary:
      sourceHash: 7cbc7ca5de5b
      provenance: machine
      outputHash: 49559022828c
    paths./api/v1/tools/image/image-pad.post.description:
      sourceHash: f222b64a52bc
      provenance: machine
      outputHash: d24ece2353b9
    paths./api/v1/tools/image/pixelate.post.summary:
      sourceHash: f4982f86e732
      provenance: machine
      outputHash: 3a96c580c712
    paths./api/v1/tools/image/pixelate.post.description:
      sourceHash: c47f345410f8
      provenance: machine
      outputHash: 9f748d47682c
    paths./api/v1/tools/image/vignette.post.summary:
      sourceHash: f841c2080740
      provenance: machine
      outputHash: f843bf20fa75
    paths./api/v1/tools/image/vignette.post.description:
      sourceHash: 9e1e807e89e0
      provenance: machine
      outputHash: 75a8e856ac00
    paths./api/v1/tools/image/gif-webp.post.summary:
      sourceHash: 337e539d70a4
      provenance: machine
      outputHash: 9ae085591009
    paths./api/v1/tools/image/gif-webp.post.description:
      sourceHash: f397405d0b9c
      provenance: machine
      outputHash: 9fc8fce1fad9
    paths./api/v1/tools/image/histogram.post.summary:
      sourceHash: b32dc489fba8
      provenance: machine
      outputHash: cd4124d89cf2
    paths./api/v1/tools/image/histogram.post.description:
      sourceHash: f98c4a1d1bf0
      provenance: machine
      outputHash: 0fc1ce51624e
    paths./api/v1/tools/image/lqip-placeholder.post.summary:
      sourceHash: 878e3f3518db
      provenance: machine
      outputHash: 0f54a8d8573b
    paths./api/v1/tools/image/lqip-placeholder.post.description:
      sourceHash: 2100f7e08142
      provenance: machine
      outputHash: 6b3d0d9fc304
    paths./api/v1/tools/image/sprite-sheet.post.summary:
      sourceHash: f1c0772c4599
      provenance: machine
      outputHash: 2ee3441a480a
    paths./api/v1/tools/image/sprite-sheet.post.description:
      sourceHash: 4ef141ec92ed
      provenance: machine
      outputHash: 70e8efa5614e
    paths./api/v1/tools/image/passport-photo.post.summary:
      sourceHash: 535e5bf9e5e6
      provenance: machine
      outputHash: a039338519e4
    paths./api/v1/tools/image/passport-photo.post.description:
      sourceHash: 7d3a81dbb05c
      provenance: machine
      outputHash: 30028db1b61b
    paths./api/v1/tools/pdf/sign-pdf.post.summary:
      sourceHash: 1bf93e215df9
      provenance: machine
      outputHash: c67a717fd586
    paths./api/v1/tools/pdf/sign-pdf.post.description:
      sourceHash: d66e168d79ba
      provenance: machine
      outputHash: c4148ab5f174
    paths./api/v1/tools/image/jpg-to-png.post.summary:
      sourceHash: 2b455c24e59e
      provenance: machine
      outputHash: 105f3c949621
    paths./api/v1/tools/image/jpg-to-png.post.description:
      sourceHash: 1d504faac90f
      provenance: machine
      outputHash: cd4c1e922355
    paths./api/v1/tools/image/png-to-jpg.post.summary:
      sourceHash: 9e2eef671853
      provenance: machine
      outputHash: d66497677d73
    paths./api/v1/tools/image/png-to-jpg.post.description:
      sourceHash: 12d6c8d6aa01
      provenance: machine
      outputHash: 86ac551637df
    paths./api/v1/tools/image/jpg-to-webp.post.summary:
      sourceHash: 1e67451d3358
      provenance: machine
      outputHash: e8103a2bb1ee
    paths./api/v1/tools/image/jpg-to-webp.post.description:
      sourceHash: 0e7e4b18daa5
      provenance: machine
      outputHash: 0ba355ac3f97
    paths./api/v1/tools/image/png-to-webp.post.summary:
      sourceHash: 6c9410143dd5
      provenance: machine
      outputHash: 49a6d5d73501
    paths./api/v1/tools/image/png-to-webp.post.description:
      sourceHash: 7505ddaf944b
      provenance: machine
      outputHash: 0155fa356f29
    paths./api/v1/tools/image/webp-to-jpg.post.summary:
      sourceHash: 670a9097dec8
      provenance: machine
      outputHash: 6f4f91a736bc
    paths./api/v1/tools/image/webp-to-jpg.post.description:
      sourceHash: 2b7701fe02eb
      provenance: machine
      outputHash: b85b6756910f
    paths./api/v1/tools/image/webp-to-png.post.summary:
      sourceHash: 781a17cb5ec0
      provenance: machine
      outputHash: a2273360e57d
    paths./api/v1/tools/image/webp-to-png.post.description:
      sourceHash: 78645c123b20
      provenance: machine
      outputHash: a9ad927e1231
    paths./api/v1/tools/image/jpg-to-avif.post.summary:
      sourceHash: 1c50ea7bdfce
      provenance: machine
      outputHash: ab2fa2aa4201
    paths./api/v1/tools/image/jpg-to-avif.post.description:
      sourceHash: fbc8f6ae407c
      provenance: machine
      outputHash: cd668a5711e4
    paths./api/v1/tools/image/png-to-avif.post.summary:
      sourceHash: c3ba0668ee1f
      provenance: machine
      outputHash: fdc0e8f42ca6
    paths./api/v1/tools/image/png-to-avif.post.description:
      sourceHash: d0dcdde42555
      provenance: machine
      outputHash: 2008dfcf0743
    paths./api/v1/tools/image/webp-to-avif.post.summary:
      sourceHash: 690ea37f961e
      provenance: machine
      outputHash: 1dce26ca359e
    paths./api/v1/tools/image/webp-to-avif.post.description:
      sourceHash: bcae3fc230b8
      provenance: machine
      outputHash: 8dba367e43c9
    paths./api/v1/tools/image/heic-to-jpg.post.summary:
      sourceHash: 0013ad2f7e97
      provenance: machine
      outputHash: 0e128506442a
    paths./api/v1/tools/image/heic-to-jpg.post.description:
      sourceHash: deb0641a6bfd
      provenance: machine
      outputHash: 1e4cf4424815
    paths./api/v1/tools/image/heic-to-png.post.summary:
      sourceHash: a191f1708025
      provenance: machine
      outputHash: 4b7114ccaadf
    paths./api/v1/tools/image/heic-to-png.post.description:
      sourceHash: 583345b636d6
      provenance: machine
      outputHash: ce9b1658a945
    paths./api/v1/tools/image/heic-to-avif.post.summary:
      sourceHash: a03031e5c80d
      provenance: machine
      outputHash: 1b73800996fa
    paths./api/v1/tools/image/heic-to-avif.post.description:
      sourceHash: 6812ebf99c0b
      provenance: machine
      outputHash: c8ce950aaa5e
    paths./api/v1/tools/image/jpg-to-gif.post.summary:
      sourceHash: 46b3bcd3a38f
      provenance: machine
      outputHash: 490ae9ac30b3
    paths./api/v1/tools/image/jpg-to-gif.post.description:
      sourceHash: 365097fb68a9
      provenance: machine
      outputHash: ef882a577f78
    paths./api/v1/tools/image/png-to-gif.post.summary:
      sourceHash: e4de0e11bf3d
      provenance: machine
      outputHash: 5d238c08d5fd
    paths./api/v1/tools/image/png-to-gif.post.description:
      sourceHash: 89620988c5bd
      provenance: machine
      outputHash: b4460aac13ca
    paths./api/v1/tools/image/gif-to-jpg.post.summary:
      sourceHash: 016ce94121b8
      provenance: machine
      outputHash: acf6044bb852
    paths./api/v1/tools/image/gif-to-jpg.post.description:
      sourceHash: d3c99d325441
      provenance: machine
      outputHash: 9309bae6c50b
    paths./api/v1/tools/image/gif-to-png.post.summary:
      sourceHash: 8f3239971851
      provenance: machine
      outputHash: ba54d16b3ed4
    paths./api/v1/tools/image/gif-to-png.post.description:
      sourceHash: 6b363c6c5b6a
      provenance: machine
      outputHash: d4fc7011af6e
    paths./api/v1/tools/image/webp-to-gif.post.summary:
      sourceHash: db331b95e19b
      provenance: machine
      outputHash: aa5f2298c43d
    paths./api/v1/tools/image/webp-to-gif.post.description:
      sourceHash: 9295d8ac8c09
      provenance: machine
      outputHash: 7ef4eec6202e
    paths./api/v1/tools/image/jpg-to-tiff.post.summary:
      sourceHash: 86ae950de6a3
      provenance: machine
      outputHash: d3f4cce8e413
    paths./api/v1/tools/image/jpg-to-tiff.post.description:
      sourceHash: 4f0ac37f2e06
      provenance: machine
      outputHash: 114de6f2556c
    paths./api/v1/tools/image/png-to-tiff.post.summary:
      sourceHash: 3fa124cbaa4a
      provenance: machine
      outputHash: 9a13f9ba4d01
    paths./api/v1/tools/image/png-to-tiff.post.description:
      sourceHash: 5cf97e597ee8
      provenance: machine
      outputHash: c86765b5b631
    paths./api/v1/tools/image/tiff-to-jpg.post.summary:
      sourceHash: c803f62926b8
      provenance: machine
      outputHash: af542848afb5
    paths./api/v1/tools/image/tiff-to-jpg.post.description:
      sourceHash: cf928b0513e5
      provenance: machine
      outputHash: 0834ab5db308
    paths./api/v1/tools/image/tiff-to-png.post.summary:
      sourceHash: 73c313976bb4
      provenance: machine
      outputHash: fc2aaf9a2a37
    paths./api/v1/tools/image/tiff-to-png.post.description:
      sourceHash: 52f065d2f324
      provenance: machine
      outputHash: 7474f9829af4
    paths./api/v1/tools/image/psd-to-jpg.post.summary:
      sourceHash: b9d50a4fd5a9
      provenance: machine
      outputHash: 168d51765823
    paths./api/v1/tools/image/psd-to-jpg.post.description:
      sourceHash: a9897ebb6e8c
      provenance: machine
      outputHash: b38d672fc1c2
    paths./api/v1/tools/image/psd-to-png.post.summary:
      sourceHash: af36a6f53221
      provenance: machine
      outputHash: 403d2f88b1d4
    paths./api/v1/tools/image/psd-to-png.post.description:
      sourceHash: b15171b20160
      provenance: machine
      outputHash: 369eba083a8d
    paths./api/v1/tools/image/png-to-eps.post.summary:
      sourceHash: 33ce1b37d63f
      provenance: machine
      outputHash: f1e40b814fb7
    paths./api/v1/tools/image/png-to-eps.post.description:
      sourceHash: 31d8bfe76c0d
      provenance: machine
      outputHash: 4d78fefd7246
    paths./api/v1/tools/image/jpg-to-eps.post.summary:
      sourceHash: e8bde3997aaf
      provenance: machine
      outputHash: 3cd068eb2cc4
    paths./api/v1/tools/image/jpg-to-eps.post.description:
      sourceHash: 62cce0cb73ba
      provenance: machine
      outputHash: faa2b917fe15
    paths./api/v1/tools/image/eps-to-png.post.summary:
      sourceHash: be7ed58f6b7e
      provenance: machine
      outputHash: ee3e5f1c018a
    paths./api/v1/tools/image/eps-to-png.post.description:
      sourceHash: e9157e4e334b
      provenance: machine
      outputHash: 7e9b8a0d4b0b
    paths./api/v1/tools/image/eps-to-jpg.post.summary:
      sourceHash: c05512db4202
      provenance: machine
      outputHash: d735fe06dee7
    paths./api/v1/tools/image/eps-to-jpg.post.description:
      sourceHash: b582104358cd
      provenance: machine
      outputHash: cff0b2553c51
    paths./api/v1/tools/image/png-to-svg.post.summary:
      sourceHash: 651353e8e22c
      provenance: machine
      outputHash: 737542734ac1
    paths./api/v1/tools/image/png-to-svg.post.description:
      sourceHash: 5cbb3b81f383
      provenance: machine
      outputHash: ecc0ce7e3415
    paths./api/v1/tools/image/jpg-to-svg.post.summary:
      sourceHash: 46f10c1c31b4
      provenance: machine
      outputHash: e91cd96447e4
    paths./api/v1/tools/image/jpg-to-svg.post.description:
      sourceHash: ea475d6542cb
      provenance: machine
      outputHash: b0f5aa17dbec
    paths./api/v1/tools/image/tiff-to-svg.post.summary:
      sourceHash: 52f9db13bb53
      provenance: machine
      outputHash: c3624c8247dc
    paths./api/v1/tools/image/tiff-to-svg.post.description:
      sourceHash: 26ac4eed4312
      provenance: machine
      outputHash: 99162b3bb79b
    paths./api/v1/tools/image/psd-to-svg.post.summary:
      sourceHash: 654d378a3882
      provenance: machine
      outputHash: c9e8a2e35c0d
    paths./api/v1/tools/image/psd-to-svg.post.description:
      sourceHash: 990b5ce49f61
      provenance: machine
      outputHash: ca665767a870
    paths./api/v1/tools/image/eps-to-svg.post.summary:
      sourceHash: 36137967b026
      provenance: machine
      outputHash: f34cac32dd16
    paths./api/v1/tools/image/eps-to-svg.post.description:
      sourceHash: 8c2914626a16
      provenance: machine
      outputHash: 3f8c777cec5e
    paths./api/v1/tools/image/svg-to-png.post.summary:
      sourceHash: 1f1eb20e97fe
      provenance: machine
      outputHash: f6fb07755398
    paths./api/v1/tools/image/svg-to-png.post.description:
      sourceHash: 5338d3a81ff7
      provenance: machine
      outputHash: f07d0eb0b9e9
    paths./api/v1/tools/image/svg-to-jpg.post.summary:
      sourceHash: 8e51faf94ff1
      provenance: machine
      outputHash: 31673086199f
    paths./api/v1/tools/image/svg-to-jpg.post.description:
      sourceHash: 23241e664642
      provenance: machine
      outputHash: b6175ac79402
    paths./api/v1/tools/image/jpg-to-pdf.post.summary:
      sourceHash: 480104dd2622
      provenance: machine
      outputHash: 95eced2aa4c7
    paths./api/v1/tools/image/jpg-to-pdf.post.description:
      sourceHash: daffc3441cf6
      provenance: machine
      outputHash: 117b4d4097d9
    paths./api/v1/tools/image/png-to-pdf.post.summary:
      sourceHash: 7cab3612203f
      provenance: machine
      outputHash: da8a7a49988a
    paths./api/v1/tools/image/png-to-pdf.post.description:
      sourceHash: 135646c47b37
      provenance: machine
      outputHash: 34a828f6d527
    paths./api/v1/tools/image/heic-to-pdf.post.summary:
      sourceHash: d9a32701511d
      provenance: machine
      outputHash: d6764f3869f6
    paths./api/v1/tools/image/heic-to-pdf.post.description:
      sourceHash: cb1a4bc6ad48
      provenance: machine
      outputHash: fb0bce001d1a
    paths./api/v1/tools/image/tiff-to-pdf.post.summary:
      sourceHash: 76458bef5bb7
      provenance: machine
      outputHash: 370809e97983
    paths./api/v1/tools/image/tiff-to-pdf.post.description:
      sourceHash: f26e932da894
      provenance: machine
      outputHash: 5504f0fb7149
    paths./api/v1/tools/image/webp-to-pdf.post.summary:
      sourceHash: fdbcd78acb8e
      provenance: machine
      outputHash: 2dd3d47bec56
    paths./api/v1/tools/image/webp-to-pdf.post.description:
      sourceHash: 02d3842d170f
      provenance: machine
      outputHash: 351f311ba64d
    paths./api/v1/tools/image/gif-to-pdf.post.summary:
      sourceHash: 62a5f26018fc
      provenance: machine
      outputHash: d89e3aff8bb3
    paths./api/v1/tools/image/gif-to-pdf.post.description:
      sourceHash: e1d4ce05a711
      provenance: machine
      outputHash: 828c12432ce9
    paths./api/v1/tools/image/eps-to-pdf.post.summary:
      sourceHash: b183686e176a
      provenance: machine
      outputHash: '425995919581'
    paths./api/v1/tools/image/eps-to-pdf.post.description:
      sourceHash: 797845fc1a13
      provenance: machine
      outputHash: 690f7f6ca403
    paths./api/v1/tools/pdf/pdf-to-jpg.post.summary:
      sourceHash: f84d053c3344
      provenance: machine
      outputHash: 73e63dfda438
    paths./api/v1/tools/pdf/pdf-to-jpg.post.description:
      sourceHash: 294e7071a0cd
      provenance: machine
      outputHash: cb431ea838cc
    paths./api/v1/tools/pdf/pdf-to-png.post.summary:
      sourceHash: 18362545fcb8
      provenance: machine
      outputHash: 61b7a333c960
    paths./api/v1/tools/pdf/pdf-to-png.post.description:
      sourceHash: 879e2d2c287b
      provenance: machine
      outputHash: 34e438742e47
    paths./api/v1/tools/pdf/pdf-to-tiff.post.summary:
      sourceHash: cd0065e23473
      provenance: machine
      outputHash: 30664f7dd276
    paths./api/v1/tools/pdf/pdf-to-tiff.post.description:
      sourceHash: 4e46452ab02e
      provenance: machine
      outputHash: 030b49e3a76a
    paths./api/v1/tools/video/mov-to-mp4.post.summary:
      sourceHash: b88833009ff9
      provenance: machine
      outputHash: 591ffadf56cd
    paths./api/v1/tools/video/mov-to-mp4.post.description:
      sourceHash: a248339c26d1
      provenance: machine
      outputHash: dcd008609d38
    paths./api/v1/tools/video/webm-to-mp4.post.summary:
      sourceHash: 66e6dae5210e
      provenance: machine
      outputHash: 53bb054f71ca
    paths./api/v1/tools/video/webm-to-mp4.post.description:
      sourceHash: a9977ca09125
      provenance: machine
      outputHash: ce8def2a99cb
    paths./api/v1/tools/video/mkv-to-mp4.post.summary:
      sourceHash: cffe53cd9489
      provenance: machine
      outputHash: c12fe646559b
    paths./api/v1/tools/video/mkv-to-mp4.post.description:
      sourceHash: d4c9bfc05b71
      provenance: machine
      outputHash: 91d8faef7d4e
    paths./api/v1/tools/video/avi-to-mp4.post.summary:
      sourceHash: a5ed5b5782d3
      provenance: machine
      outputHash: c9b4e5b69762
    paths./api/v1/tools/video/avi-to-mp4.post.description:
      sourceHash: 59f0c2a1860b
      provenance: machine
      outputHash: 949bf851819d
    paths./api/v1/tools/video/mp4-to-mov.post.summary:
      sourceHash: a20af2c4b1fb
      provenance: machine
      outputHash: e9fff34c57c4
    paths./api/v1/tools/video/mp4-to-mov.post.description:
      sourceHash: 8d99e8459b46
      provenance: machine
      outputHash: 305a40c56c17
    paths./api/v1/tools/video/mp4-to-webm.post.summary:
      sourceHash: a1b67df8d74e
      provenance: machine
      outputHash: 84eeb7ecf460
    paths./api/v1/tools/video/mp4-to-webm.post.description:
      sourceHash: 2d0fec602c82
      provenance: machine
      outputHash: d9c8fc3f934d
    paths./api/v1/tools/video/webm-to-mov.post.summary:
      sourceHash: 60df7d3b385b
      provenance: machine
      outputHash: dca260b704ce
    paths./api/v1/tools/video/webm-to-mov.post.description:
      sourceHash: 6982fc1cb933
      provenance: machine
      outputHash: 66923e7a60b6
    paths./api/v1/tools/video/mkv-to-mov.post.summary:
      sourceHash: dc04291270e6
      provenance: machine
      outputHash: 1a4457f2e7fa
    paths./api/v1/tools/video/mkv-to-mov.post.description:
      sourceHash: 71c9c1b99374
      provenance: machine
      outputHash: 10946b1bb020
    paths./api/v1/tools/video/avi-to-mov.post.summary:
      sourceHash: 449c58ca9486
      provenance: machine
      outputHash: 0032e8a88144
    paths./api/v1/tools/video/avi-to-mov.post.description:
      sourceHash: 62b1fd2e8e0c
      provenance: machine
      outputHash: 4cdf9e2a0fc9
    paths./api/v1/tools/video/mp4-to-avi.post.summary:
      sourceHash: 700763fbdb4a
      provenance: machine
      outputHash: b328e8828007
    paths./api/v1/tools/video/mp4-to-avi.post.description:
      sourceHash: da2c6f405f96
      provenance: machine
      outputHash: d35e6c5296af
    paths./api/v1/tools/video/mov-to-avi.post.summary:
      sourceHash: 6cc35a24d9fa
      provenance: machine
      outputHash: ef014422ad21
    paths./api/v1/tools/video/mov-to-avi.post.description:
      sourceHash: e1dd8989a812
      provenance: machine
      outputHash: 3b539a610b89
    paths./api/v1/tools/video/mkv-to-avi.post.summary:
      sourceHash: 993ee5fab472
      provenance: machine
      outputHash: 2fff4013e66a
    paths./api/v1/tools/video/mkv-to-avi.post.description:
      sourceHash: f1b9b4ced057
      provenance: machine
      outputHash: a848eb060ada
    paths./api/v1/tools/video/avi-to-mkv.post.summary:
      sourceHash: 3393bba23656
      provenance: machine
      outputHash: a4d56cc12cba
    paths./api/v1/tools/video/avi-to-mkv.post.description:
      sourceHash: ce821c05956b
      provenance: machine
      outputHash: f4e90aa28482
    paths./api/v1/tools/video/mp4-to-gif.post.summary:
      sourceHash: 62a7b6e94a53
      provenance: machine
      outputHash: cc31e6292a57
    paths./api/v1/tools/video/mp4-to-gif.post.description:
      sourceHash: eb95a94fc0e0
      provenance: machine
      outputHash: 2486e0bd3c6e
    paths./api/v1/tools/video/mov-to-gif.post.summary:
      sourceHash: c0e8c3dcfdfd
      provenance: machine
      outputHash: 8074171b9fd9
    paths./api/v1/tools/video/mov-to-gif.post.description:
      sourceHash: f0f7c24adb4f
      provenance: machine
      outputHash: 85811425615d
    paths./api/v1/tools/video/mkv-to-gif.post.summary:
      sourceHash: c19834912c0e
      provenance: machine
      outputHash: 7d9978c800ad
    paths./api/v1/tools/video/mkv-to-gif.post.description:
      sourceHash: 591062eb518f
      provenance: machine
      outputHash: 49e29f81595b
    paths./api/v1/tools/video/avi-to-gif.post.summary:
      sourceHash: b6286a84b91d
      provenance: machine
      outputHash: 01d89fffca66
    paths./api/v1/tools/video/avi-to-gif.post.description:
      sourceHash: dd44bdc0686d
      provenance: machine
      outputHash: 4bfb4004d728
    paths./api/v1/tools/video/gif-to-mp4.post.summary:
      sourceHash: 9c2d0df2d1e9
      provenance: machine
      outputHash: 15ff4208224a
    paths./api/v1/tools/video/gif-to-mp4.post.description:
      sourceHash: 96914944fdc5
      provenance: machine
      outputHash: 6b4f972ad4da
    paths./api/v1/tools/video/gif-to-webm.post.summary:
      sourceHash: f2b694034e10
      provenance: machine
      outputHash: 2658d3beafbf
    paths./api/v1/tools/video/gif-to-webm.post.description:
      sourceHash: 221c2fee1775
      provenance: machine
      outputHash: 75f153081187
    paths./api/v1/tools/video/gif-to-mov.post.summary:
      sourceHash: 45c5f35f45f3
      provenance: machine
      outputHash: 3f72acf18bdb
    paths./api/v1/tools/video/gif-to-mov.post.description:
      sourceHash: 57996ae8e8cb
      provenance: machine
      outputHash: 1e14ae4e7e78
    paths./api/v1/tools/video/mp4-to-mp3.post.summary:
      sourceHash: 69e107b048a8
      provenance: machine
      outputHash: eb78675edad3
    paths./api/v1/tools/video/mp4-to-mp3.post.description:
      sourceHash: 40de83aa6ed2
      provenance: machine
      outputHash: f58bc86b5966
    paths./api/v1/tools/video/mov-to-mp3.post.summary:
      sourceHash: e149b039deaf
      provenance: machine
      outputHash: 3730e4b39ccc
    paths./api/v1/tools/video/mov-to-mp3.post.description:
      sourceHash: 27f1a1f36a88
      provenance: machine
      outputHash: bfb184f70441
    paths./api/v1/tools/video/mkv-to-mp3.post.summary:
      sourceHash: a017e66660ad
      provenance: machine
      outputHash: ac449214fe13
    paths./api/v1/tools/video/mkv-to-mp3.post.description:
      sourceHash: 12cb145afcd0
      provenance: machine
      outputHash: 3ea7883281e7
    paths./api/v1/tools/video/webm-to-mp3.post.summary:
      sourceHash: 9fbcc26dd542
      provenance: machine
      outputHash: a1e47aa83baa
    paths./api/v1/tools/video/webm-to-mp3.post.description:
      sourceHash: 5213b45f3c7d
      provenance: machine
      outputHash: 6bf15a235f6c
    paths./api/v1/tools/video/avi-to-mp3.post.summary:
      sourceHash: 5f4d7167967b
      provenance: machine
      outputHash: a39799640df0
    paths./api/v1/tools/video/avi-to-mp3.post.description:
      sourceHash: dea6a15cea91
      provenance: machine
      outputHash: bdab1f86ff30
    paths./api/v1/tools/video/mp4-to-wav.post.summary:
      sourceHash: b6769af66349
      provenance: machine
      outputHash: 822af464dd9a
    paths./api/v1/tools/video/mp4-to-wav.post.description:
      sourceHash: 5b9126a6bb99
      provenance: machine
      outputHash: e89eb68b7a7c
    paths./api/v1/tools/video/mov-to-wav.post.summary:
      sourceHash: 4194226fdf68
      provenance: machine
      outputHash: f2c13595092d
    paths./api/v1/tools/video/mov-to-wav.post.description:
      sourceHash: fe4925d7babd
      provenance: machine
      outputHash: 3f61349cb7eb
    paths./api/v1/tools/video/mp4-to-ogg.post.summary:
      sourceHash: dcb33be4e615
      provenance: machine
      outputHash: a3ff9d1c3715
    paths./api/v1/tools/video/mp4-to-ogg.post.description:
      sourceHash: d8be42b38707
      provenance: machine
      outputHash: 2dca3197071e
    paths./api/v1/tools/audio/m4a-to-mp3.post.summary:
      sourceHash: e4f86f589bd4
      provenance: machine
      outputHash: 7037fc80a964
    paths./api/v1/tools/audio/m4a-to-mp3.post.description:
      sourceHash: ad144ccdc1d4
      provenance: machine
      outputHash: e27104b8d318
    paths./api/v1/tools/audio/m4a-to-wav.post.summary:
      sourceHash: 5e8aa1c62150
      provenance: machine
      outputHash: 1cf93c637ff4
    paths./api/v1/tools/audio/m4a-to-wav.post.description:
      sourceHash: dab8ed1c0668
      provenance: machine
      outputHash: a9686c3dcdd8
    paths./api/v1/tools/audio/aac-to-mp3.post.summary:
      sourceHash: dd7d2c46e335
      provenance: machine
      outputHash: d794b81870bb
    paths./api/v1/tools/audio/aac-to-mp3.post.description:
      sourceHash: 4187ef8fb789
      provenance: machine
      outputHash: e687ddc4f668
    paths./api/v1/tools/audio/aac-to-wav.post.summary:
      sourceHash: 6485f4d30fd0
      provenance: machine
      outputHash: e74bf83a66cf
    paths./api/v1/tools/audio/aac-to-wav.post.description:
      sourceHash: c2221db94cf4
      provenance: machine
      outputHash: db081c0b32c5
    paths./api/v1/tools/audio/aac-to-flac.post.summary:
      sourceHash: 9dc0a9886938
      provenance: machine
      outputHash: cf5a6bfeebd6
    paths./api/v1/tools/audio/aac-to-flac.post.description:
      sourceHash: 945f63a2be18
      provenance: machine
      outputHash: 7396b3483768
    paths./api/v1/tools/audio/ogg-to-mp3.post.summary:
      sourceHash: c3cff86b4282
      provenance: machine
      outputHash: 448c34af1257
    paths./api/v1/tools/audio/ogg-to-mp3.post.description:
      sourceHash: 8889a760d19f
      provenance: machine
      outputHash: b483fff7e4a0
    paths./api/v1/tools/audio/ogg-to-wav.post.summary:
      sourceHash: 85fbfd8ee338
      provenance: machine
      outputHash: 152c31adc5d2
    paths./api/v1/tools/audio/ogg-to-wav.post.description:
      sourceHash: 99156f0db542
      provenance: machine
      outputHash: fb4b12047e28
    paths./api/v1/tools/audio/wav-to-mp3.post.summary:
      sourceHash: d8344c7d753c
      provenance: machine
      outputHash: aa8cfd3fe38e
    paths./api/v1/tools/audio/wav-to-mp3.post.description:
      sourceHash: 7f83d006baf8
      provenance: machine
      outputHash: d56892c15f23
    paths./api/v1/tools/audio/mp3-to-wav.post.summary:
      sourceHash: a7de59ab62b9
      provenance: machine
      outputHash: 37c9d5f2e51e
    paths./api/v1/tools/audio/mp3-to-wav.post.description:
      sourceHash: c19d5fba1478
      provenance: machine
      outputHash: 276ba86aaef8
    paths./api/v1/tools/audio/flac-to-mp3.post.summary:
      sourceHash: da353684dd2b
      provenance: machine
      outputHash: 786db4e07e6e
    paths./api/v1/tools/audio/flac-to-mp3.post.description:
      sourceHash: e83797072608
      provenance: machine
      outputHash: b4030db9e5d1
    paths./api/v1/tools/files/excel-to-csv.post.summary:
      sourceHash: 8610a88e662a
      provenance: machine
      outputHash: 9e3d65997791
    paths./api/v1/tools/files/excel-to-csv.post.description:
      sourceHash: c5b6027afaa5
      provenance: machine
      outputHash: ac983283f0f6
    paths./api/v1/readyz.get.summary:
      sourceHash: 2f70aef72373
      provenance: machine
      outputHash: 004612c2140f
    paths./api/v1/readyz.get.description:
      sourceHash: 1600aad09ab9
      provenance: machine
      outputHash: aa99e999233a
    paths./api/v1/jobs/{jobId}/cancel.post.summary:
      sourceHash: a5032d26d61d
      provenance: machine
      outputHash: 169dc1305448
    paths./api/v1/jobs/{jobId}/cancel.post.description:
      sourceHash: c828ad6eed72
      provenance: machine
      outputHash: 0d5241b62709
    paths./api/v1/preferences.get.summary:
      sourceHash: 14eccfecf549
      provenance: machine
      outputHash: e634ea3696ce
    paths./api/v1/preferences.get.description:
      sourceHash: be05d444bc85
      provenance: machine
      outputHash: aee7d55a40da
    paths./api/v1/preferences.put.summary:
      sourceHash: 2649ca8f6a25
      provenance: machine
      outputHash: 0358c081cc9f
    paths./api/v1/preferences.put.description:
      sourceHash: ac325d43a780
      provenance: machine
      outputHash: faf2c7e67d32
    paths./api/auth/mfa/enroll.post.summary:
      sourceHash: c138aec8b79b
      provenance: machine
      outputHash: 113ff88b93fa
    paths./api/auth/mfa/enroll.post.description:
      sourceHash: e00560362017
      provenance: machine
      outputHash: 8a411937872f
    paths./api/auth/mfa/verify.post.summary:
      sourceHash: 12b670ebf850
      provenance: machine
      outputHash: 6a37793eb5ea
    paths./api/auth/mfa/verify.post.description:
      sourceHash: ae628486beb5
      provenance: machine
      outputHash: a0a583845bfb
    paths./api/auth/mfa/complete.post.summary:
      sourceHash: 16f65adb8582
      provenance: machine
      outputHash: bbb7e25d7fbe
    paths./api/auth/mfa/complete.post.description:
      sourceHash: 3dc67142d5da
      provenance: machine
      outputHash: ba7e12fe6cc2
    paths./api/auth/mfa/disable.post.summary:
      sourceHash: 8adb8274a7c6
      provenance: machine
      outputHash: bf656404b70f
    paths./api/auth/mfa/disable.post.description:
      sourceHash: 7980f5f0d9f0
      provenance: machine
      outputHash: ba213c94fb02
    paths./api/auth/users/{id}/mfa/reset.post.summary:
      sourceHash: c765204eb72c
      provenance: machine
      outputHash: 1df0d3057e14
    paths./api/auth/users/{id}/mfa/reset.post.description:
      sourceHash: 9d650aeb7d10
      provenance: machine
      outputHash: 2952bf9200c7
    paths./api/auth/oidc/login.get.summary:
      sourceHash: bac046b3ef24
      provenance: machine
      outputHash: 7f7cafe9488b
    paths./api/auth/oidc/login.get.description:
      sourceHash: e48289d1e534
      provenance: machine
      outputHash: 4b3d9f89d64d
    paths./api/auth/oidc/callback.get.summary:
      sourceHash: 396eda750d3c
      provenance: machine
      outputHash: a7bfd94f666e
    paths./api/auth/oidc/callback.get.description:
      sourceHash: e9437a06c530
      provenance: machine
      outputHash: dd51c0ccf360
    paths./api/auth/saml/metadata.get.summary:
      sourceHash: d6b4c80ec1e4
      provenance: machine
      outputHash: b330fe88d263
    paths./api/auth/saml/metadata.get.description:
      sourceHash: 0b08445f03af
      provenance: machine
      outputHash: eab47e511e36
    paths./api/auth/saml/login.get.summary:
      sourceHash: 6de21463c7cb
      provenance: machine
      outputHash: 3ded4ea2d715
    paths./api/auth/saml/login.get.description:
      sourceHash: eca8bcf506ea
      provenance: machine
      outputHash: 278cdcddb589
    paths./api/auth/saml/callback.post.summary:
      sourceHash: 2a95ad454bd5
      provenance: machine
      outputHash: 28ade6f02ec6
    paths./api/auth/saml/callback.post.description:
      sourceHash: a03dcebde897
      provenance: machine
      outputHash: b29f82808fd2
    paths./api/v1/files/{id}/preview.get.summary:
      sourceHash: 71d50aa39313
      provenance: machine
      outputHash: 98bc1749c8fc
    paths./api/v1/files/{id}/preview.get.description:
      sourceHash: fb3edf8df6cf
      provenance: machine
      outputHash: ffc8142c9def
    paths./api/v1/preview/generate.post.summary:
      sourceHash: cff28fa8e380
      provenance: machine
      outputHash: 47597242917f
    paths./api/v1/preview/generate.post.description:
      sourceHash: 56d484d0c85b
      provenance: machine
      outputHash: 8aa671f48e65
    paths./api/v1/admin/log-level.get.summary:
      sourceHash: f9fa3ff842cd
      provenance: machine
      outputHash: f6fbe4f196d2
    paths./api/v1/admin/log-level.get.description:
      sourceHash: 277cddb1b284
      provenance: machine
      outputHash: 4dedda5ef7c7
    paths./api/v1/admin/log-level.post.summary:
      sourceHash: e9ac46f76c49
      provenance: machine
      outputHash: 6feeaed7f342
    paths./api/v1/admin/log-level.post.description:
      sourceHash: 5f12391622b9
      provenance: machine
      outputHash: 973d347d5080
    paths./api/v1/metrics.get.summary:
      sourceHash: 05be9e156194
      provenance: machine
      outputHash: 824947cd2246
    paths./api/v1/metrics.get.description:
      sourceHash: e5723b8ba79e
      provenance: machine
      outputHash: 889419969d8f
    paths./api/v1/admin/support-bundle.get.summary:
      sourceHash: 3fc31382eaf7
      provenance: machine
      outputHash: 11ab8db67329
    paths./api/v1/admin/support-bundle.get.description:
      sourceHash: c04e47b20915
      provenance: machine
      outputHash: ba484c173faf
    paths./api/v1/admin/usage.get.summary:
      sourceHash: 2f5017840735
      provenance: machine
      outputHash: 103473ef7f5d
    paths./api/v1/admin/usage.get.description:
      sourceHash: 5ba73241349d
      provenance: machine
      outputHash: 07d848da2a96
    paths./api/v1/admin/backup-status.get.summary:
      sourceHash: cea8a0d690fc
      provenance: machine
      outputHash: 5fc572f760c8
    paths./api/v1/admin/backup-status.get.description:
      sourceHash: e42e4494117a
      provenance: machine
      outputHash: f0e3a1468c95
    paths./api/v1/admin/backup-status.post.summary:
      sourceHash: 1a7ed3984596
      provenance: machine
      outputHash: a93ab0d515d1
    paths./api/v1/admin/backup-status.post.description:
      sourceHash: 84b506533dbf
      provenance: machine
      outputHash: 93e646db77e4
    paths./api/v1/admin/features/import.post.summary:
      sourceHash: 31b34aebf83b
      provenance: machine
      outputHash: 13898f373ac3
    paths./api/v1/admin/features/import.post.description:
      sourceHash: 229fcb03f525
      provenance: machine
      outputHash: 61912fb3b4bf
    paths./api/v1/enterprise/scim/token.post.summary:
      sourceHash: e550c4aaeecf
      provenance: machine
      outputHash: ab246e48961a
    paths./api/v1/enterprise/scim/token.post.description:
      sourceHash: 39d3b5d5066b
      provenance: machine
      outputHash: 78598ae2923d
    paths./api/v1/enterprise/scim/token.delete.summary:
      sourceHash: e57912d50189
      provenance: machine
      outputHash: 7a0d16479d60
    paths./api/v1/enterprise/scim/token.delete.description:
      sourceHash: 412b38ac0141
      provenance: machine
      outputHash: 0ca5dad68219
    paths./api/v1/scim/v2/ServiceProviderConfig.get.summary:
      sourceHash: 6d9bb054fb7b
      provenance: machine
      outputHash: 801ca555dd25
    paths./api/v1/scim/v2/ServiceProviderConfig.get.description:
      sourceHash: a111048aed19
      provenance: machine
      outputHash: 3f6c20273e09
    paths./api/v1/scim/v2/Schemas.get.summary:
      sourceHash: c04171c8ce40
      provenance: machine
      outputHash: ea1b2c1289ea
    paths./api/v1/scim/v2/Schemas.get.description:
      sourceHash: 74cf47ef6038
      provenance: machine
      outputHash: f67d7bb88612
    paths./api/v1/scim/v2/ResourceTypes.get.summary:
      sourceHash: b3f61558b888
      provenance: machine
      outputHash: 07dc7b5f0ebe
    paths./api/v1/scim/v2/ResourceTypes.get.description:
      sourceHash: d3935183ed14
      provenance: machine
      outputHash: a7950d0d5f64
    paths./api/v1/scim/v2/Users.get.summary:
      sourceHash: 06aa223783d0
      provenance: machine
      outputHash: 83e066c575ca
    paths./api/v1/scim/v2/Users.get.description:
      sourceHash: dccfb3116fb5
      provenance: machine
      outputHash: 1d1d710466ee
    paths./api/v1/scim/v2/Users.post.summary:
      sourceHash: ba4ce4467898
      provenance: machine
      outputHash: dac5c0e6b12a
    paths./api/v1/scim/v2/Users.post.description:
      sourceHash: ab9d5413dfe0
      provenance: machine
      outputHash: a2b5cd964e4b
    paths./api/v1/scim/v2/Users/{id}.get.summary:
      sourceHash: 06a26f48a1d7
      provenance: machine
      outputHash: 30113044206a
    paths./api/v1/scim/v2/Users/{id}.get.description:
      sourceHash: f643db8015e1
      provenance: machine
      outputHash: 727dfd1ab962
    paths./api/v1/scim/v2/Users/{id}.put.summary:
      sourceHash: acfe35ddd973
      provenance: machine
      outputHash: 5822bf5b17af
    paths./api/v1/scim/v2/Users/{id}.put.description:
      sourceHash: 7051119f905f
      provenance: machine
      outputHash: 72db710ca741
    paths./api/v1/scim/v2/Users/{id}.delete.summary:
      sourceHash: 11c8da919810
      provenance: machine
      outputHash: c6a8899e9a6d
    paths./api/v1/scim/v2/Users/{id}.delete.description:
      sourceHash: f8a3ee46d088
      provenance: machine
      outputHash: 067656ad18c1
    paths./api/v1/scim/v2/Groups.get.summary:
      sourceHash: 7f5cfe4da60d
      provenance: machine
      outputHash: 00fba534676a
    paths./api/v1/scim/v2/Groups.get.description:
      sourceHash: b3dea6667317
      provenance: machine
      outputHash: a37bbded9df1
    paths./api/v1/scim/v2/Groups.post.summary:
      sourceHash: 2477896fba1c
      provenance: machine
      outputHash: 515a12ec9b02
    paths./api/v1/scim/v2/Groups.post.description:
      sourceHash: fbb117796dfb
      provenance: machine
      outputHash: 90a2202aa8ae
    paths./api/v1/scim/v2/Groups/{id}.get.summary:
      sourceHash: cf7956970f8e
      provenance: machine
      outputHash: ab52a8242966
    paths./api/v1/scim/v2/Groups/{id}.get.description:
      sourceHash: 7aeb5a733198
      provenance: machine
      outputHash: 525edb52a5a7
    paths./api/v1/scim/v2/Groups/{id}.put.summary:
      sourceHash: 5878fbd31c27
      provenance: machine
      outputHash: 195dae2b5c0c
    paths./api/v1/scim/v2/Groups/{id}.put.description:
      sourceHash: 0b41d735866c
      provenance: machine
      outputHash: 0d8711322d61
    paths./api/v1/scim/v2/Groups/{id}.delete.summary:
      sourceHash: 37212ab76d6b
      provenance: machine
      outputHash: d8d83b597cc9
    paths./api/v1/scim/v2/Groups/{id}.delete.description:
      sourceHash: faf5572bb482
      provenance: machine
      outputHash: 4549793b4b43
    paths./api/v1/enterprise/audit/export.get.summary:
      sourceHash: bb1ef74df6a7
      provenance: machine
      outputHash: 29a7d16716f4
    paths./api/v1/enterprise/audit/export.get.description:
      sourceHash: 78723fd7a551
      provenance: machine
      outputHash: 6f524eb7cdeb
    paths./api/v1/enterprise/config/export.get.summary:
      sourceHash: 0657bced852f
      provenance: machine
      outputHash: c884141b63c1
    paths./api/v1/enterprise/config/export.get.description:
      sourceHash: 8e3ce9c435f5
      provenance: machine
      outputHash: a56dbe5d051e
    paths./api/v1/enterprise/config/import.post.summary:
      sourceHash: 8a507f40b80f
      provenance: machine
      outputHash: ad05d605d6dc
    paths./api/v1/enterprise/config/import.post.description:
      sourceHash: cf4809a9459d
      provenance: machine
      outputHash: b2ecb106335e
    paths./api/v1/enterprise/ip-allowlist.get.summary:
      sourceHash: 8756df674b27
      provenance: machine
      outputHash: 2f91a5161359
    paths./api/v1/enterprise/ip-allowlist.get.description:
      sourceHash: 42fe8d5e6555
      provenance: machine
      outputHash: 0571af7775a6
    paths./api/v1/enterprise/ip-allowlist.put.summary:
      sourceHash: e0d442c15809
      provenance: machine
      outputHash: 163bd6d1c499
    paths./api/v1/enterprise/ip-allowlist.put.description:
      sourceHash: 0cb040540d15
      provenance: machine
      outputHash: e665f5f8f23b
    paths./api/v1/enterprise/legal-hold.get.summary:
      sourceHash: a3b33d97f1b3
      provenance: machine
      outputHash: 09d9d17ca11e
    paths./api/v1/enterprise/legal-hold.get.description:
      sourceHash: 2b51807e41b2
      provenance: machine
      outputHash: 208f28a623c6
    paths./api/v1/enterprise/legal-hold.put.summary:
      sourceHash: f0f8a1f6bd23
      provenance: machine
      outputHash: acb6e31ce9c8
    paths./api/v1/enterprise/legal-hold.put.description:
      sourceHash: 2f22192732ff
      provenance: machine
      outputHash: b6aba218fa56
    paths./api/v1/enterprise/siem/config.get.summary:
      sourceHash: 7ac4058ce316
      provenance: machine
      outputHash: 9a6ddc028498
    paths./api/v1/enterprise/siem/config.get.description:
      sourceHash: b4c92171b7fb
      provenance: machine
      outputHash: e07754c64e3a
    paths./api/v1/enterprise/siem/config.put.summary:
      sourceHash: b236d6669c61
      provenance: machine
      outputHash: 0d9d759e18bb
    paths./api/v1/enterprise/siem/config.put.description:
      sourceHash: 94cec3b103c8
      provenance: machine
      outputHash: 7d708ba1041a
    paths./api/v1/enterprise/webhooks.get.summary:
      sourceHash: a43cfb1a8c96
      provenance: machine
      outputHash: a79c9f9a3950
    paths./api/v1/enterprise/webhooks.get.description:
      sourceHash: 292a38feca49
      provenance: machine
      outputHash: 8f2a36060bfa
    paths./api/v1/enterprise/webhooks.post.summary:
      sourceHash: 4a2b33ad2c1f
      provenance: machine
      outputHash: 4c2a27aac3e9
    paths./api/v1/enterprise/webhooks.post.description:
      sourceHash: 2cc3c4b18327
      provenance: machine
      outputHash: dbea54648ec2
    paths./api/v1/enterprise/webhooks/{index}.put.summary:
      sourceHash: c5be5387d7be
      provenance: machine
      outputHash: d4c5c5d1ccfb
    paths./api/v1/enterprise/webhooks/{index}.put.description:
      sourceHash: 28806bb5b6ce
      provenance: machine
      outputHash: ffdb796a2595
    paths./api/v1/enterprise/webhooks/{index}.delete.summary:
      sourceHash: 60f85e57e4a7
      provenance: machine
      outputHash: a36213e49c89
    paths./api/v1/enterprise/webhooks/{index}.delete.description:
      sourceHash: 97b9800c59a2
      provenance: machine
      outputHash: a6c09ae76f4f
    paths./api/v1/enterprise/webhooks/{index}/test.post.summary:
      sourceHash: 333e8368ac5c
      provenance: machine
      outputHash: 65b5b5095a4c
    paths./api/v1/enterprise/webhooks/{index}/test.post.description:
      sourceHash: 5626cb4c037d
      provenance: machine
      outputHash: a2df654c1653
    paths./api/v1/enterprise/users/{id}/export.post.summary:
      sourceHash: 75556a6714fc
      provenance: machine
      outputHash: 1cbbdbb7d6f4
    paths./api/v1/enterprise/users/{id}/export.post.description:
      sourceHash: 4f90b2a09ef8
      provenance: machine
      outputHash: c5757701cd72
    paths./api/v1/enterprise/users/{id}/export/{jobId}.get.summary:
      sourceHash: 7fa624472044
      provenance: machine
      outputHash: ac15ea00bb4c
    paths./api/v1/enterprise/users/{id}/export/{jobId}.get.description:
      sourceHash: fb51267dcbf7
      provenance: machine
      outputHash: 51f8c41a9244
    paths./api/v1/enterprise/users/{id}/purge.delete.summary:
      sourceHash: 09c40d574cc0
      provenance: machine
      outputHash: 4f29e3423dc5
    paths./api/v1/enterprise/users/{id}/purge.delete.description:
      sourceHash: 6db9ed4dabf1
      provenance: machine
      outputHash: d09ba922ac43
    paths./api/v1/enterprise/teams/{id}/purge.delete.summary:
      sourceHash: e5f557c6db8b
      provenance: machine
      outputHash: f0da408b859a
    paths./api/v1/enterprise/teams/{id}/purge.delete.description:
      sourceHash: 5bcd69dee315
      provenance: machine
      outputHash: cfbe69db48a7
    paths./api/v1/admin/version.get.summary:
      sourceHash: 1fa5f3f5fa4a
      provenance: machine
      outputHash: 3cad3ecaaf20
    paths./api/v1/admin/version.get.description:
      sourceHash: b72b0bfc24e6
      provenance: machine
      outputHash: c5ea53d572ca
    paths./api/v1/admin/migrations/pending.get.summary:
      sourceHash: dd3d4aea6207
      provenance: machine
      outputHash: f9080fa51e9b
    paths./api/v1/admin/migrations/pending.get.description:
      sourceHash: 434f121758d2
      provenance: machine
      outputHash: 0ca086574953
    paths./api/v1/admin/upgrade-check.get.summary:
      sourceHash: abc755f5b011
      provenance: machine
      outputHash: 53e11f3f5a0d
    paths./api/v1/admin/upgrade-check.get.description:
      sourceHash: a21396abfd74
      provenance: machine
      outputHash: 6adf29a890c1
    paths./llms.txt.get.summary:
      sourceHash: ba31286a938b
      provenance: machine
      outputHash: 8c8968752f3a
    paths./llms.txt.get.description:
      sourceHash: 5f69094320cb
      provenance: machine
      outputHash: 82fbfd684e70
    paths./llms-full.txt.get.summary:
      sourceHash: 6f12d5cecc9d
      provenance: machine
      outputHash: 6ee476c03494
    paths./llms-full.txt.get.description:
      sourceHash: a135dfc6aac1
      provenance: machine
      outputHash: b338e7edcbd1
    paths./api/v1/openapi.yaml.get.summary:
      sourceHash: 9ee7dc77d462
      provenance: machine
      outputHash: 9ee7dc77d462
    paths./api/v1/openapi.yaml.get.description:
      sourceHash: 10ceb656a256
      provenance: machine
      outputHash: 32f024d5729d
    paths./api/v1/tools/popular.get.summary:
      sourceHash: de2139ffd4c3
      provenance: machine
      outputHash: 38d25eb74d62
    paths./api/v1/tools/popular.get.description:
      sourceHash: 40c9637d843b
      provenance: machine
      outputHash: 4953996aebdb
