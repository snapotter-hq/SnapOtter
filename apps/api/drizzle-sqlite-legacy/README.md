# Archived SQLite migrations (v1.x)

Superseded by the Postgres baseline in `apps/api/drizzle/`. Kept for historical
reference only. Do not apply. Note: snapshots 0006-0010 were already absent from
`meta/` before archiving; the SQL files are complete (0000-0012).
