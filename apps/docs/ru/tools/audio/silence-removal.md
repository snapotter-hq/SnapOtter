---
description: "Удаление участков тишины из аудиофайла."
i18n_source_hash: a7624fc99b50
i18n_provenance: human
i18n_output_hash: 47dc22470835
---

# Удаление тишины {#silence-removal}

Обнаружение и удаление участков тишины из аудиофайла на основе настраиваемого порога и минимальной длительности.

## Эндпоинт API {#api-endpoint}

`POST /api/v1/tools/audio/silence-removal`

Принимает multipart form data с аудиофайлом и JSON-полем `settings`.

## Параметры {#parameters}

| Параметр | Тип | Обязательный | По умолчанию | Описание |
|-----------|------|----------|---------|-------------|
| thresholdDb | number | Нет | `-50` | Порог тишины в дБ (от -80 до -20). Аудио ниже этого уровня считается тишиной. |
| minSilenceS | number | Нет | `0.5` | Минимальная длительность тишины в секундах для удаления (от 0.1 до 5) |

## Пример запроса {#example-request}

```bash
curl -X POST http://localhost:1349/api/v1/tools/audio/silence-removal \
  -H "Authorization: Bearer si_your-api-key" \
  -F "file=@audio.mp3" \
  -F 'settings={"thresholdDb": -45, "minSilenceS": 1}'
```

## Пример ответа {#example-response}

```json
{
  "jobId": "a1b2c3d4-e5f6-7890-abcd-ef1234567890",
  "downloadUrl": "/api/v1/download/a1b2c3d4-e5f6-7890-abcd-ef1234567890/audio.mp3",
  "originalSize": 4500000,
  "processedSize": 3200000
}
```

## Примечания {#notes}

- Более высокий (менее отрицательный) порог действует агрессивнее и удаляет как истинную тишину, так и более тихие фрагменты.
- Увеличьте `minSilenceS`, чтобы вырезать только длинные паузы, сохраняя короткие естественные промежутки.
- Полезно для очистки записей подкастов, лекций и голосовых заметок.
- Выходной файл обычно сохраняет контейнер входного. Вход AAC записывается как M4A, а неподдерживаемые входы, доступные только для декодирования, заменяются на MP3.
