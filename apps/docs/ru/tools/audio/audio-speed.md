---
description: "Ускорение или замедление воспроизведения аудио с помощью множителя."
i18n_source_hash: e39ba662e594
i18n_provenance: human
i18n_output_hash: 57e9a643988e
---

# Скорость аудио {#audio-speed}

Ускоряйте или замедляйте воспроизведение аудио, применяя множитель скорости.

## Конечная точка API {#api-endpoint}

`POST /api/v1/tools/audio/audio-speed`

Принимает данные формы multipart с аудиофайлом и полем `settings` в формате JSON.

## Параметры {#parameters}

| Параметр | Тип | Обязательный | По умолчанию | Описание |
|-----------|------|----------|---------|-------------|
| factor | number | Нет | `1.5` | Множитель скорости (от 0.25 до 4). Значения ниже 1 замедляют; выше 1 ускоряют. |

## Пример запроса {#example-request}

```bash
curl -X POST http://localhost:1349/api/v1/tools/audio/audio-speed \
  -H "Authorization: Bearer si_your-api-key" \
  -F "file=@audio.mp3" \
  -F 'settings={"factor": 2}'
```

## Пример ответа {#example-response}

```json
{
  "jobId": "a1b2c3d4-e5f6-7890-abcd-ef1234567890",
  "downloadUrl": "/api/v1/download/a1b2c3d4-e5f6-7890-abcd-ef1234567890/audio.mp3",
  "originalSize": 4500000,
  "processedSize": 2250000
}
```

## Примечания {#notes}

- Коэффициент `0.25` воспроизводит на четверти скорости (в 4 раза дольше). Коэффициент `4` воспроизводит на четырёхкратной скорости (в 4 раза короче).
- Высота тона сохраняется при изменении скорости (растяжение по времени). Используйте сдвиг высоты тона, чтобы регулировать высоту независимо.
- Вывод обычно сохраняет входной контейнер. Вход AAC записывается как M4A, а неподдерживаемые для декодирования входы откатываются к MP3.
