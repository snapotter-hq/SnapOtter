---
// biome-ignore-all lint/correctness/noUnusedImports: Astro template consumes component imports.
// biome-ignore-all lint/correctness/noUnusedVariables: Astro template consumes frontmatter values.
import Footer from "@/components/Footer.astro";
import JsonLd from "@/components/JsonLd.astro";
import Navbar from "@/components/Navbar.astro";
import { LANDING_LOCALES, t } from "@/i18n";
import Base from "@/layouts/Base.astro";
import { localizeHref } from "@/lib/i18n-page";

export function getStaticPaths() {
  return LANDING_LOCALES.map((locale) => ({
    params: { locale: locale === "en" ? undefined : locale },
    props: { locale },
  }));
}

const { locale } = Astro.props;
const PATH = "/privacy";

const breadcrumbJsonLd = {
  "@context": "https://schema.org",
  "@type": "BreadcrumbList",
  itemListElement: [
    { "@type": "ListItem", position: 1, name: "Home", item: "https://snapotter.com" },
    {
      "@type": "ListItem",
      position: 2,
      name: "Privacy Policy",
      item: "https://snapotter.com/privacy",
    },
  ],
};
---

<Base
  title={t(locale, "privacy.metaTitle")}
  description={t(locale, "privacy.metaDescription")}
  canonical={`https://snapotter.com${localizeHref(locale, PATH)}`}
  locale={locale}
  path={PATH}
>
  <Fragment slot="head">
    <JsonLd data={breadcrumbJsonLd} />
  </Fragment>

  <Navbar locale={locale} path={PATH} />

  <main id="main" class="px-6 pt-32 pb-20 md:pt-40 md:pb-28">
    <div class="mx-auto max-w-3xl">
      <div class="reveal text-center">
        <h1 class="font-display text-4xl font-bold tracking-tight md:text-5xl">{t(locale, "privacy.title")}</h1>
        <p class="mt-6 text-lg text-muted">{t(locale, "privacy.lastUpdated")}</p>
      </div>

      <div class="mt-16 space-y-12 text-sm leading-relaxed text-muted">
        <section class="reveal">
          <h2 class="text-lg font-semibold text-foreground">{t(locale, "privacy.controller.title")}</h2>
          <p class="mt-3">
            {t(locale, "privacy.controller.body")}
          </p>
        </section>

        <section class="reveal">
          <h2 class="text-lg font-semibold text-foreground">{t(locale, "privacy.overview.title")}</h2>
          <p class="mt-3">
            {t(locale, "privacy.overview.body")}
          </p>
        </section>

        <section class="reveal">
          <h2 class="text-lg font-semibold text-foreground">{t(locale, "privacy.website.title")}</h2>
          <ul class="mt-3 list-disc space-y-2 ps-5">
            <li>{t(locale, "privacy.website.item1")}</li>
            <li>{t(locale, "privacy.website.item2")}</li>
            <li>{t(locale, "privacy.website.item3")}</li>
          </ul>
        </section>

        <section class="reveal">
          <h2 class="text-lg font-semibold text-foreground">{t(locale, "privacy.selfHosted.title")}</h2>
          <ul class="mt-3 list-disc space-y-2 ps-5">
            <li>{t(locale, "privacy.selfHosted.item1")}</li>
            <li>{t(locale, "privacy.selfHosted.item2")}</li>
            <li>{t(locale, "privacy.selfHosted.item3")}</li>
          </ul>
        </section>

        <section class="reveal">
          <h2 class="text-lg font-semibold text-foreground">{t(locale, "privacy.analytics.title")}</h2>
          <ul class="mt-3 list-disc space-y-2 ps-5">
            <li>{t(locale, "privacy.analytics.item1")}</li>
            <li>{t(locale, "privacy.analytics.item2")}</li>
            <li>
              {t(locale, "privacy.analytics.item3.pre")}
              <code class="text-xs">SNAPOTTER_ANALYTICS=off</code> {t(locale, "privacy.analytics.item3.post")}
            </li>
            <li>
              {t(locale, "privacy.analytics.item4.pre")} <code class="text-xs">feedback_submitted</code>.
              {t(locale, "privacy.analytics.item4.post")}
            </li>
          </ul>
        </section>

        <section class="reveal">
          <h2 class="text-lg font-semibold text-foreground">{t(locale, "privacy.contactForm.title")}</h2>
          <ul class="mt-3 list-disc space-y-2 ps-5">
            <li>{t(locale, "privacy.contactForm.item1")}</li>
            <li>{t(locale, "privacy.contactForm.item2")}</li>
            <li>{t(locale, "privacy.contactForm.item3")}</li>
          </ul>
        </section>

        <section class="reveal">
          <h2 class="text-lg font-semibold text-foreground">{t(locale, "privacy.openSource.title")}</h2>
          <p class="mt-3">
            {t(locale, "privacy.openSource.body")}
          </p>
        </section>

        <section class="reveal">
          <h2 class="text-lg font-semibold text-foreground">{t(locale, "privacy.changes.title")}</h2>
          <p class="mt-3">
            {t(locale, "privacy.changes.body")}
          </p>
        </section>

        <section class="reveal">
          <h2 class="text-lg font-semibold text-foreground">{t(locale, "privacy.contact.title")}</h2>
          <p class="mt-3">
            {t(locale, "privacy.contact.pre")}
            <a
              href="mailto:contact@snapotter.com"
              class="text-primary font-medium hover:underline underline-offset-4"
            >
              contact@snapotter.com
            </a>.
          </p>
        </section>
      </div>
    </div>
  </main>

  <Footer locale={locale} />
</Base>
